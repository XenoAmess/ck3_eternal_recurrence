ox_here_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_invite_knights.dds"
	}

	decision_group_type = ox_here
	sort_order = 10
	ai_check_interval_by_tier = {
		barony = 12
		county = 12
		duchy = 12
		kingdom = 12
		empire = 12
		hegemony = 12
	}

	desc = ox_here_decision_desc

	is_shown = {
		is_ruler = yes
		is_married = yes
	}

	is_valid = {
		is_ruler = yes
		is_married = yes
	}

	is_valid_showing_failures_only = {
		is_ruler = yes
		is_married = yes
	}

	widget = {
		gui = "decision_view_widget_option_list_generic"
		controller = decision_option_list_controller
		decision_to_second_step_button = "ox_here_decision_confirm"
		show_from_start = yes

		item = {
			value = ox_here_recruit
			is_valid = { always = yes }
			current_description = ox_here_decision_option_recruit_desc
			localization = ox_here_decision_option_recruit
			icon = "gfx/interface/icons/traits/giant.dds"
			ai_chance = {
				value = 0
				if = {
					limit = {
						OR = {
							number_of_knights < max_number_of_knights
							any_knight = {
								prowess < decent_skill_rating
							}
						}
					}
					add = 100
				}
				if = {
					limit = {
						any_character_war = {
							OR = {
								AND = {
									attacker_war_score <= -50
									casus_belli = {
										primary_attacker = root
									}
								}
								AND = {
									defender_war_score <= -50
									casus_belli = {
										primary_defender = root
									}
								}
							}
						}
					}
					add = 200
				}
			}
		}

		item = {
			value = ox_here_decline
			is_valid = { always = yes }
			current_description = ox_here_decision_option_decline_desc
			localization = ox_here_decision_option_decline
			icon = "gfx/interface/icons/message_feed/activity.dds"
			ai_chance = { value = 100 }
		}
	}

	effect = {
		if = {
			limit = { scope:ox_here_recruit = yes }
			ox_here_invite_warrior_effect = yes
		}
	}

	ai_potential = {
		is_ruler = yes
		is_married = yes
	}

	ai_will_do = {
		base = 0

		modifier = {
			add = 10
			OR = {
				number_of_knights < max_number_of_knights
				any_knight = {
					prowess < decent_skill_rating
				}
			}
		}

		modifier = {
			add = 60
			any_character_war = {
				OR = {
					AND = {
						attacker_war_score <= -50
						casus_belli = {
							primary_attacker = root
						}
					}
					AND = {
						defender_war_score <= -50
						casus_belli = {
							primary_defender = root
						}
					}
				}
			}
		}
	}
}
