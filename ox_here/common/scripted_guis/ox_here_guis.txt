ox_here_invite_warrior_gui = {
	scope = character
	is_shown = {
		is_ai = no
		has_character_flag = ox_here_invite_warrior_pending
	}
	effect = {
		if = {
			limit = { has_character_flag = ox_here_invite_warrior_pending }
			remove_character_flag = ox_here_invite_warrior_pending
			ox_here_invite_warrior_effect = yes
		}
	}
}
