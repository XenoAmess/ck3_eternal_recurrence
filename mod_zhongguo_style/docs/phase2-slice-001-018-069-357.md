# v0.4.x 二期首纵切：#001 + #018 + #069 + #357

日期：2026-08-30（Asia/Shanghai）

状态：**bounded fixture-live（事实/配额冻结 + 拒签→D+7 见证→同案 receipt）/ `zg361.53` 清算单重开仍为 static-ready / 未并入 0.3.0 / 未发布**

本文是 v0.4.x 的首个领域运行时纵切权威记录。静态里程碑 `b5a0b0e97413a35a3ec50dad525251970fb659f0`
已通过双父 merge `ec339ed56ab94ab2fe2d27ef220709318b5ac7d2` 收入 master；这只统一后续开发基线，不提升
readiness。已公开的 0.3.0、Workshop 页面、旧实机 artifact 和旧标签不受该开发状态影响。

## 1. 为什么先做这四项

- **#001 KPI 分项证据单**：把「系统说你不行」拆成可核对的八项冻结事实，为后续申诉、PIP 和管理者问责提供唯一证据基座。
- **#357 先锁事实、再套配额**：同时冻结「绝对事实档」和「最终配额档」，让玩家能分清自己是确实不达标，还是全员达标却必须有人背 C。
- **#069 正式送达与申诉时钟**：把「拒绝签字」做成程序博弈，而不是逃罚按钮。
- **#018 持久个人清算单**：让事实、定档理由、送达、实扣、退款和减俸状态能被重新调阅，不再是关掉弹窗就消失。

这个组合已形成一条 CK3 决策与后果链：业绩事实被冻结，强制分布可能改写最终档，3.25 必须正式送达，再用同一张幂等 receipt 实施 PIP 与四重后果，申诉成功只能退同一案的实扣金额。这不是把四个互联网热词换成四张卡。

## 2. 产品行为

### 2.1 八项冻结 KPI 证据

`zg361_kpi_value` 不再维护一份难以追溯的大公式，而是严格求和以下八项：

1. `governance`：治理产出；
2. `capability`：职级/贤能品级；
3. `growth`：相对上一期的成长；
4. `superior`：直属上司评价；
5. `values`：战争、派系、重罪等政治/价值观红线；
6. `collaboration`：奋斗/摆烂和同僚举荐/攻讦；
7. `jingcha`：京察履责；
8. `organization`：361 组织制度气候。

八项与总分在同一 tick、同一受评者 scope 冻结。之后清理一次性奋斗、互评或京察输入，不得改写已冻结的本期证据单。

### 2.2 事实档与最终档

事实档只看冻结 KPI：

- KPI ≥ 50 → 3.75；
- 0 ≤ KPI < 50 → 3.5；
- KPI < 0 → 3.25。

最终档仍由排名、361 配额、新人保护和玩家校准会决定。案卷保留事实档、最终档与理由码：

| 码 | 含义 |
|---:|---|
| 0 | 事实档与最终档一致 |
| 1 / 2 | 校准会挤出 / 抬入 3.75 |
| 3 / 4 | 校准会调入 / 救出 3.25 |
| 5 | 事实已达标，但强制分布配额下调 |
| 6 | 新人保护抬升 |
| 7 | 排名/配额抬升 |
| 8 | 一至二人队列不强排，最终档中性化为 3.5 |

理由码只解释最终档，不得反写事实档。

### 2.3 3.25 正式送达

玩家受评者获得三种程序选择：

- 签收；
- 签收并保留异议（「签字不等于认同」）；
- 拒绝签收。

拒收只将案卷从 `PREPARED` 转为 `REFUSED_PENDING_WITNESS`，七日后带 owner/cycle/case/state 不可变 token 的隐藏事件执行见证送达。它最终与直接签收进入同一个清算 effect。**拒收绝不能让 3.25、PIP、四重处分或案卷记录消失。**

授权 AI 受评者不打开玩家事件，在后台用自动公示方式进入同一清算 effect。

### 2.4 幂等清算与申诉

正式送达后才实施：

- 地方国库实扣（适用时）；
- 个人金币实扣；
- 贤能实扣（适用时）；
- 一年俸禄减成状态；
- PIP、连续末位与原考核者关系后果。

地方国库、个人金币和贤能都以「扣前值 − 扣后值」记录实际 receipt，不再用应扣常量冒充实扣金额。`settlement_posted_serial == case_serial` 后，重复送达、重复定时事件或重复调用只能 stale no-op。

送达后开启 90 日申诉时钟。改判只能命中同一 frozen owner/cycle/case 且尚未退款的 receipt；成功后一次退回三项实扣、停止后续减俸、改判为 3.5。新案、重复改判和过期定时事件不得修改当前案。

### 2.5 个人清算单

受评玩家可从原生决议页重新打开最近一期清算单，不新增 HUD 顶层按钮。清算单显示：

- 原考核者、事实档、最终档和定档理由；
- 冻结 KPI、位次/队列人数和八项证据；
- 送达方式、送达年份、申诉窗口；
- 实扣国库/金币/贤能、已退金额和减俸状态；
- 待送达、拒收待见证、已结算、申诉过期或已改判状态。

## 3. 案卷状态与绑定

| 数值 | 模型状态 | 合法后继 |
|---:|---|---|
| 1 | `PREPARED` | 签收/异议签收 → 3；拒收 → 2 |
| 2 | `REFUSED_PENDING_WITNESS` | 七日后见证送达 → 3 |
| 3 | `DELIVERED_SETTLED` | 90 日到期 → 4；申诉改判 → 5 |
| 4 | `APPEAL_CLOSED` | 终态 |
| 5 | `CORRECTED` | 终态 |

每个 latest-result case 至少绑定：

```text
owner     = 冻结原考核者
subject   = 当前受评者 scope
cycle     = 原考核者的 review serial
case      = 原考核者有界递增的 case serial
state     = 当前案卷状态
```

新案卷覆盖「最近一期」读模型后，携旧 owner/cycle/case/state 的见证送达或申诉到期事件必须 stale no-op。这个首刀未实现多期历史档案列表；「最近一期」不得写成「永久保留全部历史」。

## 4. 权限与第二 AI 例外

| 角色 | 能做什么 | 不能做什么 |
|---|---|---|
| 玩家天朝制公爵及以上 | 主持直属 cohort、排名、配额、校准和定档 | 跨级考核、修改别人所有的旧案 |
| 授权 AI 天朝制公爵及以上 | 同一管理资格；后台考核与自动送达 | 打开玩家事件/GUI；越出本 mod 的授权例外 |
| 伯爵/男爵受评者 | 读自己证据、签收/拒收自己结果、申诉、重开自己清算单 | 拥有 cohort、给别人分配 C、校准、发起别人的 PIP 或清算 |
| 非天朝制/无地/伯爵以下管理候选 | 可依其受评者身份处理自己案卷 | 主持考核 |

## 5. 实现映射

| 领域 | 权威入口 |
|---|---|
| KPI 分项与事实档 | `common/script_values/zg361_values.txt` |
| 案卷冻结、送达、清算、退款 | `common/scripted_effects/zg361_effects.txt` |
| 签收/拒收、见证、90 日时钟、清算单 | `events/zg361_events.txt` |
| 申诉互动 | `common/character_interactions/zg361_interactions.txt` |
| 重开决议与隐形 bridge | `common/decisions/zg361_decisions.txt`、`common/scripted_guis/zg361_scoreboard_guis.txt`、`gui/zg361_decision_bridge.gui` |
| 快照榜单后置修正 | `tools/gen_scoreboard_snapshot.py` 生成的 `zg361_generated_scoreboard_snapshots.txt` |
| 运行时 source model | `tools/zg361_phase2_runtime_data.py` |
| 逐号状态 | `tools/gen_361_mechanisms.py` 生成的 manifest v3 |
| 中英文案 | `localization/simp_chinese/zg361_l_simp_chinese.yml`、`localization/english/zg361_l_english.yml` |
| 七语覆盖守卫 | `tools/gen_phase2_placeholder_localization.py` 只向 canonical 主文件补缺失 key，绝不覆盖 consolidation 后的既有译文 |

`docs/361-mechanism-manifest.json` 仅将 001/018/069/357 升级为：

```text
domain_runtime       = partial
player_visible_loop  = partial
runtime_evidence     = static-ready
```

其余357项保持 `domain_runtime = not-implemented`，不得因共享案卷内核而批量升级。

## 6. L0 证据与诚实边界

当前已覆盖：

- 38 项纯 Python 状态、权限、事实/配额与 receipt 模型测试；
- 12 项 CK3 脚本接线测试（含冻结价值观总分的可达清算单 consumer）；
- 考核榜生成器、manifest 生成器、七语占位生成器的可复现检查；
- 本 mod 的 BOM、括号、本地化键对称、入口和旧回归静态门。

这些只支持 `static-ready`。尚未证明：

- CK3 能在当前精确字节上无解析/运行错误加载；
- 延迟事件会按 7/90 日正确携带 saved scope/value；
- 正式送达和 receipt 在 CK3 真实资源上只结算一次；
- 决议 bridge、事件描述、榜单后置修正和关闭/重开在实际 GUI 中可用；
- 玩家、授权 AI、转封、新案覆盖、过期和读档的长链结果。

本纵切不得在完成 L1 前写 `fixture-live`，不得在 L2/L3 前写完整可见闭环或 complete。

## 7. 合批 L1：MCP-first，一次 CK3 启动

### 7.1 不可改写的测试规则

> 任何二期测试问题必须优先用现有 CK3 MCP 查询、动作与独立 ACK 解决，禁止优先 OCR。若 MCP 缺字段/动作，先记录明确 blocker、能力合同和接线入口；OCR 只允许作为最后画面佐证或已记录 MCP 缺口后的临时兜底，不能作为首选导航、状态真相或 GREEN 判据。

合批 runner 必须按状态驱动：每个动作先查询前置状态，发出动作后取得与该动作分开的 ACK，再重新查询状态与资源。截图只在状态闭合后取证，不代替状态查询。

单人项目默认使用最小并发；只有任务独立、边界清晰且能显著缩短关键路径时才开子线程，开始前必须评估共享文件冲突与回收/整合成本。

### 7.2 MCP 预检与缺口记账

启动 CK3 前，先将下列观测/动作映射到当前 MCP 能力。本文不预设它们已存在；若缺失，当场记录 blocker 与只读/动作合同，不把 OCR 升格为代用真相。

| 需要的能力 | 必须返回/确认的字段 | 缺失时的接线入口 |
|---|---|---|
| 查询真实角色与直属关系 | character ID、player/AI、government、highest title tier、liege | CK3 native/bridge 只读 character/title query |
| 查询 mod 角色变量 | owner/cycle/case/state、absolute/final grade、reason、八项证据、receipt serial | 通用只读 character-variable query，绑定 exact build |
| 查询资源与 modifier | treasury、gold、merit、PIP/3.25/减俸状态、opinion | native character/resource/modifier query |
| 查询榜单冻结槽 | owner/cycle、subject slot、grade/streak/PIP、表头人数 | 有界变量列表查询或专用 scoreboard query |
| 触发产品考核 | 真实决议/事件入口的动作 ACK | MCP decision/event action；fixture 只能作编排器，不能伪造 PASS |
| 选择事件选项 | 事件 ID、选项 ID、独立提交 ACK | MCP event-option action |
| 推进 7/90 日时钟 | 时间动作 ACK 与新游戏日期 | MCP time-control action + date query |
| 关闭/重开决议与事件 | 窗口类型、可见性、关闭 ACK | MCP native UI/window action/query；缺失时单独列 GUI blocker |

### 7.3 同一局合批场景

| 组 | 场景 | 必须的状态/守恒断言 |
|---:|---|---|
| A | 3.75 / 3.5 正常公示 | 八项之和 = KPI；事实档/最终档可读；不创建 3.25 处分 |
| B | 3.25 直接签收 | state 1→3；清算 serial 入账一次；三项实扣差额与资源变化相等 |
| C | 异议签收 | delivery method=2；异议记录存在；处分与 B 一致 |
| D | 拒收 + 七日见证 | state 1→2，暂不清算；七日后 2→3 并与 B 同额入账 |
| E | 重复送达/重复清算 | receipt serial 不变；金币/国库/贤能/连击均不二次变化 |
| F | 旧见证 token 遇到新案 | owner/cycle/case/state 任一不匹配即 stale no-op |
| G | 90 日申诉到期 | 当前案 state 3→4、appeal_open 1→0；旧到期 token 不关闭新案 |
| H | 申诉改判 | 同一 receipt 退回一次；state 3→5；减俸/PIP 停止；重复改判 no-op |
| I | 调任后申诉 | 原 frozen owner 而非新 liege 接收统计/榜单修正；后续新轮 summary 不被旧案改写 |
| J | 授权 AI 路径 | 天朝制 AI 公爵及以上可管理；AI 受评者自动送达且不弹玩家事件 |
| K | 伯爵/男爵权限 | 作为本人受评者可签收/拒收/申诉/查单；作为 manager 无考核/配额/校准入口 |
| L | 小队列中性化 | 事实档独立保留；最终 3.5；非一致时 reason=8，不写成强制 C |
| M | 配额 C | 事实档为 3.5/3.75、最终档 3.25、reason=5；正常执行四重处分 |
| N | 清算单重开 | 不新增 HUD 按钮；决议 bridge 可反复打开；数值与 MCP 状态查询一致 |
| O | 榜单后置修正 | 延迟送达后 PIP/streak 补写原 owner/cycle 槽；改判后同槽改为 3.5 |
| P | GUI 关闭/重开与阻塞 | 决议、事件、考核榜互不阻塞；Escape/关闭/再开正常；DPI/分辨率不造成不可达控件 |

同一启动中先用数据夹具建立可重复的队列/资源前置，但 PASS 必须来自产品 effect 的状态、资源和可见投影，不得用 fixture 直接写后置值。实机使用真实历史角色；后续宣传录制使用产品 staging，画面中不出现测试决议或 fixture 标签。

### 7.4 2026-08-30 L1 attempt 账本（滚动）

首次启动 artifact：

```text
Z:\ck3_mod_rewrite_process_assets\zg361\phase2-worktrees\v0.4-main_process_assets\zg361\runs\zga_20260830_181953_c6b2f449
```

- 结果：`RED`，在主菜单 error gate 主动终止，尚未进入 fixture 场景；
- 精确错误：`Variable 'zg361_result_values_frozen' is set but is never used`；
- 这是真实产品 CK3 analyzer FATAL，不是 OCR、MCP、fixture 或 runner 假阳性；首个 RED、完整 userdir/native state、日志和 sidecar 均保留；
- 最小修复：保留冻结字段，在可重开的 `zg361.53` 个人清算单事件中真实读取为 `zg361_stmt_values_score`，并在中英文证据单显示冻结价值观总分；没有新增机制或绕过 error gate；
- 修复后定向静态：runtime `38/38`、CK3 wiring `12/12`、clean fixture GREEN、runner contract GREEN、`validate_local` GREEN；重试需等待共享桌面上的其他 CK3 工作包退出。

后续同矩阵 attempt（全部原样保留，不把分段 PASS 拼成完整 L1）：

| attempt / artifact | 分类与终止点 | 已确认事实 | 最小修复 |
|---|---|---|---|
| 02 / `zga_20260830_182645_a0c6958a` | 产品 tooltip RED + harness 合同代际 RED；玩家案卷已到 `PREPARED`，查询 `.50` 前终止 | 旧 DLL 缺 Python 已要求的 `root_scope_ready` / `saved_scopes_ready`；`.50` option speculative tooltip 同时读取未初始化 settlement serial | posted/refund serial 改用显式 `0` 未过账 sentinel；从当前源码 fresh build matching bridge，CTest `44/44`，DLL SHA-256 `2E33BA5BC499C2408A6951EC56DC1273975E829E77F45267CF14BF42D68DD65C` |
| 03 / `zga_20260830_184158_565907a0` | 产品 receipt gate RED；fixture 初始化即主动终止 | CK3 的多项 `NOT` 不能承担“变量存在且 serial 不同”的短路语义；初始化 sentinel 后旧 gate 会把合法首次送达判为 stale | delivery、settlement、refund 只以各自 `posted_serial = 0` 进入，成功后仍写 exact positive case serial；owner/cycle/case/state 守卫不变 |
| 04 / `zga_20260830_184837_43ff65e6` | harness revision race；个人切换前清理原版多选 interruption 时终止 | fourfold、事实/配额理由、送达/清算幂等、精确退款/重复申诉全部 PASS；7/14/2、考核榜阻塞审计、京察 PASS；matching MCP 对原版事件 canonical identity 查询成功 | `set-speed-1` 后对所有多选 interruption 等待新 paused revision，再做 snapshot-bound identity query；不放宽 MCP validator，不回退 OCR |
| 05 / `zga_20260830_185833_aaedc8dc` | harness fixture 期望漂移；真实目标 `han_6443` 完成上司复评后在 `events/zga_events.txt:179` 终止 | 原版 interruption revision race 已越过；receipt、榜单、京察、真实目标、末二位投影与新人保护均 PASS。产品新案合法初始化 `settlement/refund posted_serial = 0`，但 `.2` 仍断言 settlement 变量不存在，所以 compound gate 确定性失败 | 只把 fixture 准备态断言改为两个 serial 均为 `0`，并让静态契约拒绝旧 `NOT has_variable`；grade、owner/cycle/case/state、未提前 PIP/处分与最终 receipt 门均未放宽 |
| 06 / `zga_20260830_191131_7e82d061` | **GREEN**；同一矩阵完整结束，墙钟 `477.458s` | 真实目标 `han_6875`；玩家 3.25 准备态、`.50` 拒签、D+7 `.51` 见证、四重处分与 receipt 幂等、`.4` 结果 ACK 全部闭合；严格 7/14/2、京察、考核榜阻塞审计和既有 361 ledger 同局通过 | 无新增产品修复。保留本次与前五次全部 artifact；只按实际覆盖面升级 bounded fixture-live |

attempt 06 的 exact native 证据：

- `.50` canonical identity 为 event instance `8`；notice identity snapshot revision `53`。拒签使用 resolved option 第 `3` 项（native index `2`），typed submission 从 revision `54` 到 `55`，独立 ACK 为 `event_instance_advanced / postcondition_verified=true`；
- D+7 隐藏事件 `.51` 在 `final_debug.log:10453` 以 `refused notice delivered by witness` 恰好出现一次；fixture 的 `phase2_refused_notice_witnessed_and_settled` 与 `phase2_refused_delivery_receipt_idempotent` 也各恰好一次。隐藏事件没有可见 event window，不能伪造 canonical-window ACK；
- 随后的可见结果 `.4` 为 event instance `9`；identity snapshot revision `60`，第 `1` 项（native index `0`）typed submission 从 revision `61` 到 `62`，同样取得 instance-advanced 独立 ACK；
- `.52` 是隐藏的 90 日申诉时钟。整局日志有 `formal appeal clock closed` 10 次与 stale token 1 次，但当前 MCP 不能把 generic value saved scope 绑定回 `han_6875`，所以这里只记录“隐藏 timer 在引擎中运行”，不把它冒充本案 target-bound 关闭证明；
- `.53` 本局没有打开，日志命中为 0。它的事件、决议 bridge、冻结字段消费和中英文文案仍有 L0 证明，但“关闭后重开同一清算单”继续保持 static-ready；
- phase2 delivery evidence 明确记录 `ocr_used_for_navigation_or_green=false`、`visual_evidence_captured_after_native_identity=true`。整个 runner **不是 zero-OCR**：New Game/bookmark 与 acceptance-only 初始化决议使用已登记的 startup/bootstrap 视觉辅助，既有一期 GUI 可见性审计也保留 OCR 截图；这些都不参与二期角色、事件 identity、ACK、receipt 或 GREEN 判定。

artifact 与清理：

- artifact：`Z:\ck3_mod_rewrite_process_assets\zg361\phase2-worktrees\v0.4-main_process_assets\zg361\runs\zga_20260830_191131_7e82d061`；
- `report.json` SHA-256 `409CF4366A7A96801CFE56C91792D11C7B779A6DEECB1D2DDDF4876C87D8DD6B`；`evidence-index.json` SHA-256 `140E5FF79F37717B25D1233E02427ADE7F2031FAC6B7944B2ACCDA6AF979B450`；`cell/report.json` SHA-256 `B81CD18C5CED74671E0F081ABB6D797335B4A55F0443A1D70C5954C54BEBBF32`；
- product/fixture runtime tree SHA-256 分别为 `ddac4703d99b7e498e276c37c685af28b2006ad73f4124f9cd77e745aa14a693` 与 `e2c092a464b53aa3b6161f4f3559ae74e817fe2ecf0327011979c41573eaf5de`，前后不变；source/runtime source 也未变；
- tracked CK3 PID `31904`，job active `1→0`，`tree_gone=true / cleanup_proven=true / native_driver_closed=true / native_runtime_locks_released=true`；事后双源 `CK3_COUNT=0`，protected storage unchanged，隔离 userdir/native state 按“保留过程素材”要求未删除。

修后最终 preflight：phase2 runtime `38/38`、CK3 wiring `12/12`、clean fixture GREEN、runner contract GREEN、`validate_local` GREEN、`git diff --check` GREEN；六份 CK3 runtime/fixture txt/yml 均为 UTF-8 BOM `EF BB BF`。runner 与 CK3 输入线程从启动到退出均保持 US English HKL `0x04090409`。

因此 readiness 必须拆开写：#001/#357 的 frozen facts/quota reason 与 #069 的拒签—见证—同案 receipt 纵切达到 `fixture-live`；#018 的 receipt/退款部分达到 `fixture-live`，但 `.53` 重开仍为 `static-ready`。现有 MCP 仍缺通用 mod 变量/资源查询，不能外推为 production-live，也不升级其他 357 项。

本轮 MCP-first 边界：

- `zg361.50` 和 `zg361.4` 的 canonical event identity、可见/可用 option、选择提交与独立 ACK 由 exact-build native MCP 决定；拒收通过 resolved 文本定位，不用 OCR 坐标；
- 现有 MCP 仍缺通用 mod character-variable、treasury/gold/merit/modifier 查询。当前 fixture 只建立前置条件，并在产品 effect 完成后用脚本 trigger 读取其状态/资源守恒、输出 PASS/FAIL；它不得直接写案卷、receipt、处分或退款后状态。该层最多支持 `fixture-live`，不能冒充通用 production query；
- 初始化 acceptance-only 决议仍缺 native decision-list/action MCP。它只负责启动夹具，不参与 phase2 状态真相；New Game/bookmark 与该决议的视觉点击是记录过 MCP 缺口后的 startup/bootstrap fallback。二期角色/头衔、`.50` / `.4` event identity、选项提交、ACK、状态与 GREEN 判定不得回退 OCR；OCR 只在 native identity 闭合后保存最终画面证据。

## 8. 后续范围（本切片不实现）

- #001 尚未实现目标立项、目标版本、期中调整、证据来源图或作假审计；
- #018 尚未实现全历史多期清算单或按月减俸回溯；
- #069 尚未实现独立复核人、反报复观察期或完整程序责任分级；
- #357 尚未实现跨期配额债、改判后配额回流或经理集体拒背 C。

因此四项仍是 `partial`，其他 357 项仍保持原状态。
