# Acceptance-only orchestration. This fixture only selects a live player and
# one already-existing direct reviewable vassal. It never opens B1 and never
# writes a zg361_* product variable, flag, receipt, or rolling history row.
zga_phase2_manager_seed_maybe_begin_effect = {
	if = {
		limit = {
			is_ai = no
			is_alive = yes
			is_landed = yes
			zg361_is_celestial_liege_trigger = yes
			has_game_rule = zg361_on
			zg361_review_now_business_valid_trigger = yes
			prestige >= 150
			NOT = { has_character_flag = zga_phase2_manager_seed_bootstrap_started }
		}
		# A failed zero-day handoff carrier may leave this acceptance-only flag
		# behind while the real B1 continues. The direct retry has revalidated the
		# complete business gate, so consume that stale fixture state here.
		remove_character_flag = zga_phase2_manager_seed_handoff_pending
		add_character_flag = zga_phase2_manager_seed_bootstrap_started
		debug_log = "ZGAP2MANAGERSEED: load-safe manager eligibility observed"
		trigger_event = zga_phase2_manager_seed.100
	}
	else_if = {
		limit = {
			this = character:han_6875
			is_ai = no
			is_alive = yes
			is_landed = yes
			has_game_rule = zg361_on
			zg361_is_reviewable_vassal_trigger = yes
			NOT = { has_character_flag = zga_phase2_manager_seed_handoff_started }
			has_variable = zg361_b2_m015_state
			var:zg361_b2_m015_state = 5
			has_variable = zg361_b2_m015_object_active
			var:zg361_b2_m015_object_active = 0
			has_variable = zg361_b2_m015_object_consumed
			var:zg361_b2_m015_object_consumed = 1
			has_variable = zg361_b2_m015_object_subject
			var:zg361_b2_m015_object_subject = this
			has_variable = zg361_b2_m015_object_owner
			var:zg361_b2_m015_object_owner = liege
			has_variable = zg361_b2_pip_subject_response
			var:zg361_b2_pip_subject_response = 3
			liege = {
				is_ai = yes
				is_alive = yes
				is_landed = yes
				zg361_is_celestial_liege_trigger = yes
			}
		}
		add_character_flag = zga_phase2_manager_seed_handoff_started
		save_scope_as = zga_phase2_manager_subject
		liege = {
			save_scope_as = zga_phase2_manager_owner
			# Block any duplicate acceptance entry until the hidden carrier has
			# revalidated the switched player on its own engine turn.
			add_character_flag = zga_phase2_manager_seed_handoff_pending
		}
		debug_log = "ZGAP2MANAGERSEED: post-PIP typed subject-to-manager handoff"
		set_player_character = scope:zga_phase2_manager_owner
		scope:zga_phase2_manager_owner = {
			trigger_event = { id = zga_phase2_manager_seed.11 days = 0 }
		}
	}
}

# The direct source already plays the contract's target manager. This generic
# current-human-player probe is independent of the lobby-only on_action
# carrier: the runner binds its GetPlayer root to the exact contracted native
# CharacterID before the invisible GUI records each gate exactly once.
zga_phase2_manager_seed_apply_survivability_effect = {
	if = {
		limit = {
			is_ai = no
			is_alive = yes
			NOT = {
				has_character_modifier = zga_phase2_manager_seed_survivability_modifier
			}
		}
		# Acceptance-only mitigation authorized after R133, R137 and R143 all
		# reached the same native death-terminal before this B1 could close.
		# The 1100-day duration exactly matches the runner's bounded recovery
		# horizon; it neither enters the product tree nor survives beyond it.
		add_character_modifier = {
			modifier = zga_phase2_manager_seed_survivability_modifier
			days = 1100
		}
		debug_log = "ZGAP2MANAGERSEED: acceptance survivability health=10 epidemic_resistance=100 days=1100"
	}
}

zga_phase2_manager_seed_log_direct_gates_effect = {
	zga_phase2_manager_seed_apply_survivability_effect = yes
	if = {
		limit = {
			is_ai = no
			NOT = { has_character_flag = zga_phase2_manager_seed_direct_gates_logged }
		}
		add_character_flag = zga_phase2_manager_seed_direct_gates_logged
		if = {
			limit = { is_ai = no }
			debug_log = "ZGAP2MANAGERSEED: direct gate human=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate human=RED" }
		if = {
			limit = { is_alive = yes }
			debug_log = "ZGAP2MANAGERSEED: direct gate alive=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate alive=RED" }
		if = {
			limit = { is_landed = yes }
			debug_log = "ZGAP2MANAGERSEED: direct gate landed=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate landed=RED" }
		if = {
			limit = { zg361_is_celestial_liege_trigger = yes }
			debug_log = "ZGAP2MANAGERSEED: direct gate celestial=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate celestial=RED" }
		if = {
			limit = { has_game_rule = zg361_on }
			debug_log = "ZGAP2MANAGERSEED: direct gate game_rule=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate game_rule=RED" }
		if = {
			limit = { zg361_review_now_business_valid_trigger = yes }
			debug_log = "ZGAP2MANAGERSEED: direct gate review_now=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate review_now=RED" }
		if = {
			limit = { prestige >= 150 }
			debug_log = "ZGAP2MANAGERSEED: direct gate prestige_150=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate prestige_150=RED" }
		if = {
			limit = { NOT = { has_character_flag = zg361_b1_cycle_active } }
			debug_log = "ZGAP2MANAGERSEED: direct gate b1_inactive=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate b1_inactive=RED" }
		if = {
			limit = { NOT = { has_character_flag = zg361_review_in_progress } }
			debug_log = "ZGAP2MANAGERSEED: direct gate review_in_progress_inactive=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate review_in_progress_inactive=RED" }
		if = {
			limit = {
				trigger_if = {
					limit = { has_variable = zg361_p2c_active }
					var:zg361_p2c_active != 1
				}
				trigger_else = { always = yes }
			}
			debug_log = "ZGAP2MANAGERSEED: direct gate p2c_inactive=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate p2c_inactive=RED" }
		if = {
			limit = {
				trigger_if = {
					limit = { has_variable = zg361_pp_portfolio_queue_active }
					var:zg361_pp_portfolio_queue_active != 1
				}
				trigger_else = { always = yes }
			}
			debug_log = "ZGAP2MANAGERSEED: direct gate pp_inactive=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate pp_inactive=RED" }
		if = {
			limit = {
				any_vassal = {
					count >= 1
					zg361_is_reviewable_vassal_trigger = yes
					liege = root
				}
			}
			debug_log = "ZGAP2MANAGERSEED: direct gate direct_reviewable_vassal=PASS"
		}
		else = { debug_log = "ZGAP2MANAGERSEED: direct gate direct_reviewable_vassal=RED" }
	}
	# The diagnostic flag suppresses repeated log spam, not eligibility retries.
	# R132 proved that a switched manager can legitimately remain inside the
	# source B1 for hundreds of days before the clean review boundary exists.
	zga_phase2_manager_seed_maybe_begin_effect = yes
}

# Generic post-switch failure diagnostics. The hidden .11 carrier calls this
# only after a real PIP refusal and player handoff failed its complete gate.
# It is read-only: every branch emits evidence and changes no product state.
zga_phase2_manager_seed_log_post_switch_gates_effect = {
	if = {
		limit = { is_ai = no }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate human=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate human=RED" }
	if = {
		limit = { is_alive = yes }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate alive=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate alive=RED" }
	if = {
		limit = { is_landed = yes }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate landed=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate landed=RED" }
	if = {
		limit = {
			exists = scope:zga_phase2_manager_owner
			this = scope:zga_phase2_manager_owner
		}
		debug_log = "ZGAP2MANAGERSEED: post-switch gate owner_binding=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate owner_binding=RED" }
	if = {
		limit = { has_character_flag = zga_phase2_manager_seed_handoff_pending }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate handoff_pending=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate handoff_pending=RED" }
	if = {
		limit = { zg361_is_celestial_liege_trigger = yes }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate celestial=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate celestial=RED" }
	if = {
		limit = { has_game_rule = zg361_on }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate game_rule=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate game_rule=RED" }
	if = {
		limit = { zg361_review_now_business_valid_trigger = yes }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate review_now=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate review_now=RED" }
	if = {
		limit = { prestige >= 150 }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate prestige_150=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate prestige_150=RED" }
	if = {
		limit = { NOT = { has_character_flag = zg361_b1_cycle_active } }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate b1_inactive=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate b1_inactive=RED" }
	if = {
		limit = { NOT = { has_character_flag = zg361_review_in_progress } }
		debug_log = "ZGAP2MANAGERSEED: post-switch gate review_in_progress_inactive=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate review_in_progress_inactive=RED" }
	if = {
		limit = {
			trigger_if = {
				limit = { has_variable = zg361_p2c_active }
				var:zg361_p2c_active != 1
			}
			trigger_else = { always = yes }
		}
		debug_log = "ZGAP2MANAGERSEED: post-switch gate p2c_inactive=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate p2c_inactive=RED" }
	if = {
		limit = {
			trigger_if = {
				limit = { has_variable = zg361_pp_portfolio_queue_active }
				var:zg361_pp_portfolio_queue_active != 1
			}
			trigger_else = { always = yes }
		}
		debug_log = "ZGAP2MANAGERSEED: post-switch gate pp_inactive=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate pp_inactive=RED" }
	if = {
		limit = {
			exists = scope:zga_phase2_manager_subject
			scope:zga_phase2_manager_subject = {
				is_ai = yes
				is_alive = yes
				is_landed = yes
				zg361_is_reviewable_vassal_trigger = yes
				liege = root
			}
		}
		debug_log = "ZGAP2MANAGERSEED: post-switch gate subject_binding=PASS"
	}
	else = { debug_log = "ZGAP2MANAGERSEED: post-switch gate subject_binding=RED" }
}
