# R0167 Robert lifestyle readback: three private queries observed

Status: **production-live read-only input primitive** on one bounded Robert
checkpoint. This is not a typed perk action, a public capability, an M4
completion, or a governance loop. The R0128 h1094 no-focus result remains a
separate historical observation.

## Source and no-launch binding

R0167 used the immutable R0164 Robert h1082 pair at date raw `53190528`,
actor `29829`, episode `native-29829-2bc2d599f7f9`. The original save SHA-256
was `C2E8778D5914BD14DE1636DA8C77A87DA8FDD821A9D5373FB83806C382B2F7E2`;
the original driver SHA-256 was
`3024D5CE24CE481EC7CDC0444BC84161CA200D4838CDBD77B24AF028BED2C799`.
Both were unchanged after the read-only run. This pair is distinct from the
R0128 h1094 source and from the PRV008 preview checkpoint.

The official `prepare-state` and ordinary-seed rebind produced a separate
`ordinary_campaign_succession` / `xar_off` profile. Its no-launch preflight
reported `READY_NO_LAUNCH`, paired h1082/date/actor/episode, zero CK3 processes,
and zero gameplay or date movement. Candidate manifest SHA-256:
`265ADF2D78343780FA3795FB33C5857884EB7083652B33216DAF363BA770B3F1`.
Prepared driver SHA-256:
`525EAB0DCF9BA5384618E489D0918A47409B40D031FE6FD8525AAAA0A87CECEC`.
Profile environment SHA-256:
`BA1A164F9898097355EB8B8D32A77608648B942E50B50579294E7A22DC859AC4`.

The frozen CK3 build was `1.19.0.6-steam23530548`, EXE SHA-256
`2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86`.
The private DLL SHA-256 was
`A9E1293F1E5912439793E51FD55282EDCB58359CB49737993900FD941D763CDB`
from native source `752c5ebec32ec1d87fc932f5ce4f21576bdadf8f`.
The Python runner source was `37ff65f8dd965292ee1bbebfc81be91f3ca7db16`.
The latter commit only makes the prepare evidence name the actual source
history index; it does not change query, action, or qualification logic.

## Exact live observation

The allocated R0167 cold load used a new CK3 PID `177600` (creation
`20260923014414.567565+000`). The formal bounded runner ended
`three_queries_observed` in `126.98` seconds. All three private queries and
their independent after-frames stayed paused, map-ready and alive at
`native:3`, date raw `53190528`, actor `29829`, and the same episode. The
runner recorded **zero gameplay actions, zero UI inputs, and no date advance**.

| Read | Observed value | Limit |
| --- | --- | --- |
| LIFE2 current state | `stewardship_wealth_focus` present; current `stewardship_lifestyle` progress present; XP total `213125000` raw = `2131.25`, within-level `13125000` raw = `131.25`, XP per level `1000`; unspent/used perk points `2/4` | These are the current actor's read-only values; the two points were not spent in R0167. |
| Stock focus | Same target `stewardship_wealth_focus` returned `observed_native_illegal`; target stewardship XP/points were independently read with exact native getters | The current focus was already selected; no second focus selection was attempted. |
| Formal perk | `formal_precondition_status=ready`; policy-target `legal_perk_candidates` was available with exactly one item, `cutting_corners_perk` in `stewardship_lifestyle` | `legal_focus_candidates` remained `unavailable` because the lifestyle window was unbound. No perk was submitted. |

The original R0164 h1082 source had two active defensive wars; R0166 also
restored that h1082 date with both wars active. R0167 did not run a same-frame
public war-root query, so its lifestyle-only readback cannot independently
prove peace or qualify a gameplay action. The native AI tree and private
minimum policy identify `cutting_corners_perk` as the one controlled
build-cost candidate. Unlocking a perk normally consumes one same-lifestyle
point according to the exact-build GUI/getter/command analysis; that cost is
an **inference**, not a measured R0167 deduction.

## Formal action boundary and next entry

The current `choose_one_life_turn` handles forced states and war before its
ordinary `life-advance` branch. The private M4 consumer additionally requires
an independently observed same-frame feudal government and an empty active-war
list. With the h1082 wartime source, the pure minimum policy reports
`outside_admitted_scene`; it cannot submit a perk merely because R0167 saw a
final-legal candidate. In a *hypothetical* admitted peaceful scope, feeding
the source-bound R0167 formal snapshot to the pure policy recommends
`cutting_corners_perk`. That calculation is no live action and cannot carry
the old native frame into a later submit.

Two legitimate next routes remain:

1. Let the Robert war chain reach a fresh, officially paired peaceful feudal
   checkpoint. Prepare/rebind a new candidate on the then-current integrated
   Python source and matching exact-build private DLL. Re-read the public
   scope and private final legality in that new paused frame; use the bounded
   formal `native-auto-run` trial to choose one perk, submit its typed action,
   verify `HasPerk=true` on an independent later paused frame, consume the
   receipt on the next formal turn, and checkpoint/cold-restore under its
   contract.
2. If wartime spending is needed before peace, first research and document
   the exact-build wartime scheduling inputs, then revise the M4 action
   contract and strategy with focused tests and a new bounded acceptance
   candidate. R0167 alone does not authorize bypassing the current peace
   gate.

The R0167 post-run prepared driver SHA-256 is
`B2640C345CAEE742C9F21134CC50A27E032915623E3109940B19D2B6E86BAD52`;
it differs from the prepared input because it has a live restore tail and is
not a fresh source for either route. Start from a valid immutable pair and use
the official prepare/rebind path. At execution time, recheck native legality
and points;
ACK alone is not an acquired perk. The private action/query/advertising
switches remain OFF by default.

## Immutable evidence

The read-only freeze is
`Z:\ck3_mod_rewrite_process_assets\g2-m4-robert-r0167-three-query-green-20260923\R0167-evidence-freeze`.
Its `R0167-readback-freeze.json` SHA-256 is
`AEAB84B6BDD6548685FC89757675CEBB7BD05532EE6AA984CE373B559B9259D3`.
Eight copied evidence files were checked against their originals and marked
read-only. The formal `report.json` SHA-256 is
`890D0D09046D62D4B7F45605B3AA73E7749F20628D31E18A0DDB940B3621BA78`.
The three raw query files, no-launch evidence, candidate manifest, official
rebind receipt, and round ownership are listed with full SHA-256 values in the
freeze manifest. Official cleanup reported process tree gone, job active
count zero, CK3/injector/watchdog inventory empty, and owner control files
absent. The persistent R0167 allocation is `completed-green` for this bounded
read-only run only.
