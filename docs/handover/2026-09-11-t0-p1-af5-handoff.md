# 天朝二期 P1 / AF5 收尾交接

交接时间：2026-09-11 00:02（Asia/Shanghai）

工作树：`Z:\ck3_mod_rewrite\_root-rebase-r345-20260909`

分支：`root-rebase-r345-20260909`

交接前 Git：`HEAD == origin/master == a0997e65e6b79d0d772383202317f2bf519a7de8`，ahead/behind `0/0`；
全程只允许 rebase，禁止 merge 和 force push。

## 2026-09-11 00:23 起追加：失败阶段勘误与方法论评审

接手者只读复核 R400 driver state 后确认：本次已经读到 `zg361pp.147` / instance `272`，
提交了源选项 authored `1` / native `0`，并观察到旧事件消失、revision `4 → 5`、`paused=true`。
随后超时的是等待结果事件 `zg361comp.1` 的 production callback。下文“source event wait”以及
接班步骤 2 中“是否恢复源事件”的排查起点据此勘误；应继续定位源选项之后的产品状态与事件调度链。
未执行 AF5 authored `42` / native `41` 的结论不变，不能把源事件关闭当成业务成功。

完整事实、原始证据链接、测试与执行组织评估、七项改进建议及后续实施顺序见
[T0 接手评估与工作流改进建议](../autonomous-agent-progress/retrospectives/2026-09-11-t0-methodology-review.md)。
本次仅落盘评估，未实施建议或运行 CK3；原交接正文保留为 00:02 的历史记录。

## 一句话结论

T0 仍是最高优先级。P1 正式机器门为 **1/9（11.1%）**，当前唯一已签收项是 final-candidate L0；P2 最终宣传视频继续
硬锁定，宣传工具未检查、未更新、未预热。R399/R400 已自然运行结束并由 operator MCP 受管清理，当前 CK3 inventory
为空。AF5 的真实 RED 是：载入冻结 `.147` checkpoint 后，action cell 在 `revision=5` 等待目标事件时看到
`active_event=null`，最终 exact-event wait timeout；没有执行 AF5 选项，不能把 loader GREEN 或 cleanup GREEN 记成 AF5
业务完成。

## 冻结候选与版本

- 当前仓库 HEAD：`a0997e65e6b79d0d772383202317f2bf519a7de8`。该提交只追加 CoA 文档/测试；本轮实机冻结的产品候选仍来自
  `e47d818ee91e10ac404816048e68ac85432da391`。
- production candidate：`Z:\ck3_mod_rewrite\_runtime\r393-production-candidate-e47d818-20260910`
- projection manifest：`Z:\ck3_mod_rewrite\_runtime\r393-production-candidate-e47d818-20260910-projection.json`
  - manifest SHA-256：`B6793C97124A8A7B42FAFBE3ECF7D6716ACDA5560CDFA1B5EEF713FA8EE3533B`
  - product tree SHA-256：`88076DBF403A34427327D0972E4422FBF31365C0F393D02B7DFE3666F75BD3CF`
  - 文件数：`1,031`
- promotion-only bridge：`Z:\ck3_mod_rewrite\_runtime\r391pc-bld2\xar_ck3_bridge.dll`
  - SHA-256：`F478B5AFFF054B4E8518BE8CC6CEAB3B1A7367103742F8E0C648D57D876D421B`
- CK3 exact build：`1.19.0.6`
  - EXE SHA-256：`2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86`
- AF5 source checkpoint：
  `Z:\ck3_mod_rewrite\_runtime\p2r294bresume3\promotion-source-checkpoints\01-phase2-promotion-compensation-zg361pp-147-d4f625c84e900966.ck3`
  - SHA-256：`D4F625C84E900966E0B70CA8FD65CD33D63205B479A3CBC15BC0DF4B02B319F0`

当前 HEAD 已前进，因此旧 AF5 v2 profile 的 `code_commit=e47d818...` 冻结门不再满足；下次运行必须新建 runtime 槽并重新
冻结当前 HEAD/候选关系，不能覆盖或复用 v2 输出目录。

## P1 九项门

| # | 机器门 | 交接状态 | 说明 |
|---:|---|---|---|
| 1 | 当前 B1 fix live | `PENDING` | R390 有 B1 GREEN 历史证据，但后续 parser 修复形成新候选，尚未装入最终 evidence manifest。 |
| 2 | AF5 terminal | `RED / PENDING` | R400 loader GREEN，但 source event wait timeout；没有 authored42/native41 或独立 terminal provider。 |
| 3 | Central stage 9 | `PENDING` | 历史 receipt 不满足当前最终组合口径；新稳定谱系待执行。 |
| 4 | Central stage 10 | `PENDING` | 未签收。 |
| 5 | Central stage 11 terminal | `PENDING` | R398 只证明首入旧错误签名归零，不是 terminal。 |
| 6 | 代表性终态 cold restore | `PENDING` | 需不同 PID/递增 generation，回读 B1/AF5/Central/Workforce。 |
| 7 | gameplay-window error scan | `PENDING` | R400 只有 loader-window GREEN，不能代替最终 gameplay window。 |
| 8 | final managed cleanup | `PENDING` | R400 cleanup GREEN 只属于失败 attempt，不是最终候选收口门。 |
| 9 | final candidate L0 | `GREEN` | 当前正式进度唯一计数项。 |

因此正式进度保持 `1/9 = 11.1%`，本轮净增 `0`。R394/R398 的回归证据有价值，但不得折算成额外百分比。

## 已完成但不计正式门的最新证据

### R394：computed-equality parser RED 归零

- `e47d818` 已把 B1 非法 computed equality 改为同算式的 `>=` / `<=` 双界。
- fresh loader slice 中旧 `Failed to parse ... Link: value` / `Unknown trigger: add` 签名为 0。
- 这是当前候选的 loader 回归，不单独构成 P1 业务门。

### R395/R396 与 R397/R398：Stage 11 首入回归

- R396 暴露 mailbox 尚未 ready 的确定性启动竞态；cleanup 时 heartbeat 已证明 mailbox 后续进入
  `installed=true / executor_submission_enabled=true / ready=true`。
- v5 wrapper 为该 exact startup-incomplete 状态增加有界等待，normal / `-O` 各 `13/13` GREEN。
- R398 唯一 gameplay PID 为 `185980`；成功到达 `zg361we.242`，并在选择前停泊。
- fresh Stage 11 slice 对旧五字段/15 条 unset-variable 签名扫描为 0，scan SHA-256
  `B555AD0700678E680ECB0530E9250DD22207B170DEEE31F68ADBB5BD7B18E1CD`。
- wrapper 最终只因 whole-object `target_binding_matches` 过严而 RED；所有逐字段身份、PID、generation、paused、target
  未选择与签名归零均成立。没有为满足 wrapper 外观重复启动 CK3。该证据只关闭首入回归，不冒充 Stage 11 terminal。
- runtime 包：`Z:\ck3_mod_rewrite\_runtime\r391-stage11-smoke-mcp`；最新实机在 `live-v5`。

## R399/R400 AF5 实机结果

runtime 包：`Z:\ck3_mod_rewrite\_runtime\r399-r400-af5-mcp`

### 启动前修复与合同

- v1 在 CK3 启动前因 nested `WindowsPath` 无法 JSON 序列化而 RED；旧证据保留，未消费 R 轮次。
- v2 仅对 bootstrap evidence 做显式递归 `Path -> str`，未知类型继续 `TypeError`，没有使用全局 `default=str`。
- v2 normal / `-O` 各 `8/8` GREEN，OperatorService no-launch preflight GREEN。
- package/profile/activation SHA-256：
  - `74A42501C2F55EC6149F95DA3D2C3425A1EFB1D89D1205B1F22936E0292A7B44`
  - `90D99DA6FC064E440F9A07736706AC415CDD15A5905F5EA3BFA9D66072057822`
  - `0F5A1F337E36D952B4AECAB783024093BD02D8972FCB467AD2C3788D7BBD0549`

### 真实运行

- operator MCP target：`xenoa / WinSta0\Default / XENOAMESS-FULL-TOWER`，port `8777`。
- job：`13586389-bc0b-444d-9503-1ff7dfb9fc48`。
- R399 warm-up PID `112908`，达到 authenticated responsive Frontend 后受管清理。
- R400 gameplay PID `160312`，connection generation `1`，始终为唯一 CK3。
- materialization、native readiness、loader error scan 与 loader gate 均 GREEN；parser 旧签名未复发。
- AF5 action cell 最终 RED：
  `PromotionCompensationActionCellError: ... phase-two exact-event wait timed out: revision=5 has no active event`。
- 未取得 `zg361comp.1` authored `42` / native `41`，未执行 terminal 选项，产品 AF5 结果未签收。
- 受管 cleanup GREEN：R400、watchdog、job worker 均已退出，`ck3.exe=[]`，job exit code `0`。

关键 artifact 与 SHA-256：

| Artifact | SHA-256 |
|---|---|
| `live-artifacts-v2/af5-red.json` | `543DC5528A1C0FED6480BE6810AEF86C63C104C807EF1E35A33105FF3410FA82` |
| `live-artifacts-v2/01_loader_native_readiness.json` | `CB11D92BE9F1395C965F283406AF12E429ADE06BF50EADEA0E7EB79D1008A12B` |
| `live-artifacts-v2/02_loader_error_scan.json` | `0823119F393646B3269CFC8D11096F9143FC76957C42468F1CC21CDBA0EE1D99` |
| `live-artifacts-v2/03_loader_gate.json` | `5D90C9E1CAE7B6B2C1A50F4AAA17E937C8C6AAA5AE6A78F74476A09B374A6342` |
| `live-artifacts-v2/09_phase2_native_session_cleanup.json` | `6EBA33EAAB297E5D5A9287A4111371807A60EE16100A0B5E31E1E1B932C6E001` |
| `live-artifacts-v2/af5-managed-cleanup.json` | `67B77B9CE1EBCDD6B5177EED46BB102B628EAE81BB0AD7922F955F32E213A1C0` |
| operator job `stdout.log` | `D02FE64A9D9C9144B024C38C1C120B4EA2DCE80C80D2A0DF4A2A164195AF671F` |

### 观察到但不要立即扩修的现象

`run-af5` 在 CK3 启动前约 16 分钟没有创建 live 目录，Python CPU/I/O 一度完全不变；模块加载停在
PyAutoGUI/Tk/PIL/NumPy 与 OpenCV 边界。它随后自然恢复并正常完成 R399/R400，因此目前只能记为一次真实启动延迟，不能在没有
稳定复现前扩张成新的产品或安全 blocker。下一位先处理 AF5 的可复现 exact-event RED。

## 当前进程与 MCP

- CK3：`0`；R399/R400 及旧实例均已终止。
- AF5 job：exited，exit code `0`；业务 RED 和 cleanup GREEN 同时保留。
- operator MCP 仍在 `127.0.0.1:8777/mcp` 监听，listener PID `197964`，starter PID `104044`。没有通用 shutdown
  control，按“不要强行停止”要求未杀进程。它只保留只读 status；旧 profile 因输出已存在且 HEAD 已变化，不可直接再次 handoff。

## 接班顺序

1. 先确认 `ck3.exe=[]`，读取本交接和 R400 的 `af5-red.json`、loader gate、driver state；不要立即重启。
2. 只定位 `revision=5 / active_event=null`：判断冻结 `.147` 存档在当前加载路径中是否应保留活动事件，还是 wrapper 必须先执行
   已定义的 promotion-source 进度步骤。保留 RED，不用 ACK、fixture 或预设事件 ID 伪造成功。
3. 做最小 no-launch 修复和定向双模式测试；新建 runtime 槽，绑定届时 HEAD、同一 candidate/bridge/checkpoint hashes。
4. 下一次实际 CK3 启动从 R401 起递增；若仍采用 warm-up + gameplay 两次启动，则分别记 R401/R402。MCP 启动独占串行。
5. AF5 只有在 source option 1 → `zg361comp.1` → authored42/native41 → 独立 terminal snapshot 全部 GREEN 后才关闭第 2 门。
6. 随后按已冻结 split plan 迁移旧稳定谱系到 exact 1.19.0.6，串行完成 stage 9/10/11，再做不同 PID cold restore、最终
   gameplay error scan、final cleanup 与 evidence assembly。旧档计划见
   [`r390-stage11-cleanup-and-split-acceptance-2026-09-10.md`](../phase2-promo/r390-stage11-cleanup-and-split-acceptance-2026-09-10.md)。
7. P1 未达 `9/9` 前不检查宣传工具、不制作/预热/发布最终视频。P1 GREEN 后才按硬门顺序进入 P2。

## T1 / T2 / 通用资产

- T1 G2：最后报告口径仍为 `90%`；本收尾时段没有开展新 G2 工作，也没有占用 CK3。
- T2 open_kaishek：本轮 parser 修复、Stage 11 wrapper 与 AF5 runtime 包没有改变公共 API/schema/ABI/data format，判定
  `NO-CODE-CHANGE`；无需制造兼容层提交。此前 operator MCP `1.1.0` 同步已经完成。
- 原版事件通用资产迁移已完成当时存量收口；本轮没有遇到新的原版事件，也没有新增一次性事件分析。
- P2 视频锁未触碰；没有宣传工具版本检查、pull、预热、素材制作或发布动作。
