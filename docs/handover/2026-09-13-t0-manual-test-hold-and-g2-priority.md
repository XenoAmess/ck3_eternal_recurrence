# 2026-09-13 T0 人工测试冻结与 G2 优先级指令

生效时间：2026-09-13 17:21（Asia/Shanghai）

本指令记录项目所有者对当前执行范围的最新调整，并覆盖此前所有“继续录制、剪辑、审片、导出或发布天朝二期宣传片”的现行计划。旧 P2 文档、录像和 stage ledger 继续作为历史证据保存，但不再产生待办、阻点或完成分母。

## 当前执行口径

1. T0 P1 机器验收维持 `9/9 GREEN`。已签收证据不因宣传计划取消而失效。
2. 当前优先级周期不制作任何天朝二期宣传视频：不启动 FFmpeg 捕获，不补 `8/8` 原片，不做双片候选、审片、导出、发布或预热。
3. 天朝二期不得发布到 Steam Workshop 或其他公开渠道。机器验收和已知 UI 缺陷修复完成后，产品进入项目所有者人工测试冻结；只有项目所有者之后明确下达发布指令，才重新建立发布工作包。
4. 当前已知 T0 产品/UI RED 修复并完成相称的定点验证后，主线立即转入 T1 G2。人工测试期间发现新的真实 T0 bug 时，保留证据并做最小修复、单独 commit/push，完成后回到 G2。
5. 旧 P2 stage ledger 中的 `ACTIVE`、`PENDING`、`0/8` 只描述取消前的素材生产事实，不代表当前授权工作。任何自动执行者不得据此恢复录像或发布链。

## 当前状态

- P1：`9/9 GREEN`。
- 已知人工体验问题：绩效榜固定视口、薪酬案卷按钮/悬浮、提前考核与京察关系及条件悬浮均已完成定点 live 闭环。
- 当前轮次：R637，为唯一 CK3 实例；旧轮次 R636 已终止。
- 宣传视频：`CANCELLED_FOR_CURRENT_PRIORITY_CYCLE`。
- T0 发布：`MANUAL_TEST_HOLD / NOT_AUTHORIZED`。
- 下一主线：`T1 G2`，按 [`g2-requirements-and-execution.md`](../autonomous-agent-progress/g2-requirements-and-execution.md) 的固定八项 OODA 分母推进。

本次只改变执行优先级和发布授权，不改变 mod、DLL、MCP ABI、协议、数据格式、版本或依赖；open_kaishek 不需要兼容层修改，也没有产生新的 MCP 通用资产。
