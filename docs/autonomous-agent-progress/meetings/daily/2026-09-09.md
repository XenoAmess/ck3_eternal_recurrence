# 2026-09-09 早会

状态：**准时召开**

会议时间：2026-09-09 00:00（Asia/Shanghai）

上一日日报：[`../../daily/2026-09-08.md`](../../daily/2026-09-08.md)

本周计划会：[`../weekly/2026-W37.md`](../weekly/2026-W37.md)

开场 HEAD：`master` / `497a8cb7c7d8f10481ecf468b8412963683e89dc`

## Opening baseline

- T0 仍为 `50%`；strict CK3-live `4/361`、剩 `357`；definitions
  `106/626`；完整 stage `8/11`。canonical source registry 已为 schema-v3
  `3/4`，只剩 `capture_cross_cycle_endgame`。
- R344 在午夜正以唯一 CK3 实例从冻结 R248 候选推进 Central 9–11 和三轮
  Workforce；T0-P2 仍 hard-locked，footage `0/8`、MP4 `0/2`。
- T1 `90%`，下一有效入口为占用 CK3 的 GEN-034 production loop；T2 当前兼容
  horizon `100%`，没有公开 API/schema 漂移。
- Git 已恢复稳定 fast-forward push；项目仍禁止 merge，只允许 fetch/rebase 后普通 push。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 完成 cross-cycle/endgame 第四类 source capture | 在第三轮未选择 `zg361we.356` 冻结真实 paused checkpoint；schema-v3 registry `4/4`、manifest/哈希/cleanup 全绿 | CK3 串行独占；未知事件只按真实帧和源码补最小合同 |
| D2 | **P0** | 关闭 source gate 后继续 T0-P1 stage 9–11 验收 | 完成所需 strict live scenes、definitions 与 stage gate；RED 保留并区分产品/合同/runner | 不用 ACK、fixture 或单点 source 冒充 full-tree 完成 |
| D3 | **P0** | 维护精确事件合同与回归 | 每个新实机中断在选择前 fail-closed；normal/`-O` 专测和 runner GREEN；及时 commit/push | 宗教域继续 owner-deferred；只保留圣战/婚姻窄例外与非宗教政治通知 |
| D4 | P1 | 在 T0 不占 CK3 时推进 G2 GEN-034 | 真实 provider/decision/action 闭环与 cleanup artifact，不能只做静态 schema | CK3 启动优先给 T0；启动环节不得并发 |
| D5 | P1 | 保持 open_kaishek 兼容 | T0/T1 公开 API/schema/架构变化后复验并同步；无漂移则明确 no-code-change | 不为内部私有合同制造无意义同步提交 |
| D6 | P1 | 完成当日报告、知识沉淀与 rebase-only Git 交付 | artifact/hash/readiness/RED/commit 入日报和周报；所有完成包及时普通 push | 不触碰用户已有 dirty runtime 文件，不 merge、不 force-push |
| D7 | P1 | 仅在 T0-P1 全部验收后解锁最终宣传片 | 先检查并更新宣传工具主线，再做 source 4/4、footage 8/8、双候选、审核和导出/发布 | **T0-P1 未完成时绝不制作视频** |

## 明确非目标

- 不把 source registry `4/4` 单独算成 T0-P1、stage 或 strict scene 全部完成。
- 不扩展通用 faith/doctrine/tenet/fervor、改宗、宗教改革或 holy order；圣战仅处理完整战争 OODA 所需最小语义。
- 不提前启动 TTS、FFmpeg、素材录制或最终宣传视频制作。
- 不 merge、不 force-push，不暂存或改写用户已有的无关 runtime 变更。

## 晚间对照规则

- 2026-09-10 00:00 在当日日报逐项对照 D1–D7，标明完成、部分完成、未完成或取消；计划外完成另列，不倒改本早会。

## 01:30 计划外工作包补录

补录时间：2026-09-09 01:30（Asia/Shanghai）

补录说明：CK3 纹章 MCP-first 调研从前一日跨过午夜；补录时已知基线是 attempt 6 暴露早期注入的 clipboard function slot 尚未就绪，CK3 已按用户要求完全回收。以下计划不冒充 00:00 时已写入。

### CoA 交付项

| # | 优先级 | 交付项 | 验收条件 | 依赖 |
|---:|---|---|---|---|
| C1 | P0 | 离线闭合 CoA MCP 延迟安装修复 | 新 DLL 可构建；Python 官方 MCP SDK 回归与 native protocol/adapter 测试 GREEN；报告诚实记录 live-pending | 无需 CK3 |
| C2 | P1 | 准备可恢复的最终实机矩阵 | runner 通过官方 `mcp==2.0.0 Client.call_tool`，隔离目录/pipe/artifact 名不复用；仅在用户释放 CK3 槽后启动 | 用户允许使用 CK3 |
| C3 | P1 | 完成最终报告与提交推送 | MCP payload matrix 回填、证据哈希、diff check、commit/push | C2 |

### CoA 明确非目标

- 用户占用 CK3 期间不启动、注入或保留任何 CK3 进程。
- 不以 OCR、截图文字或 Paste 按钮状态代替 MCP 判定。
- 本工作包不创建 Vue 编辑器；先冻结原版 reader 与 MCP 合同。

### CoA 调整规则

- 若 CK3 槽当天仍不可用，C1 独立提交为 `static-ready`，C2/C3 的 live 部分顺延，不制造虚假 `mcp-detected`。
- 若恢复实机后仍暴露 MCP 缺口，先补 primitive 和可观测性，再继续语法矩阵。
