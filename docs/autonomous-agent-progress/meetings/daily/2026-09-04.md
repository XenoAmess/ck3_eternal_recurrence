# 2026-09-04 早会

状态：**准时召开**

会议时间：2026-09-04 00:00（Asia/Shanghai）

上一日日报：[`../../daily/2026-09-03.md`](../../daily/2026-09-03.md)

本周计划会：[`../weekly/2026-W36.md`](../weekly/2026-W36.md)

开场 HEAD：`integration/progress-20260902` / `029503faacc6cf7232040c69afc873fe19527dab`

## Opening baseline

- Phase2 已有 53-file case-kernel 与 55-file safe-core full-entry GREEN；旧 58-file 未拆 B1 只有一次 1205.343 秒 pre-menu OCR RED，不足以唯一归因。
- 9 月 3 日已完成八轮 B1 effect 诊断：七个 stub 子集和保留 77/77 原正文的 41/36 双文件候选均 full-entry GREEN。正式 generator 已产出两份 BOM 文件，重组后与旧单文件 495,777 B / `CDB388…01B4` 逐字节一致，167 个定向测试 GREEN。
- 正式 59-file split projection 为 7,858,254 B，source tree `DF46E56E…B2D2`、formal tree `9EED0050…50A`。23:59 启动的 r3 exact-build full-entry 轮次跨日运行；r1/r2 仅因 probe 游戏路径默认值在 0.3 秒启动前 RED，均已保留且未启动 CK3。
- seed、paused native snapshot、8/8 clean spans、两版 MP4 与发布 receipt 均未完成；素材仍 0/8。G2 继续 owner-deferred，不占 CK3 槽。open_kaishek 固定 `15ab978f`，只承担 parser smoke，不把未知 opcode 覆盖缺口写成 CK3 verdict。
- 工作树保留既有 dirty/untracked runtime evidence；不得 reset/clean。最终提交必须显式选择正式代码、测试、权威文档，不纳入 disposable runtime 与临时对象。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 收口正式 B1 双文件 checkpoint | r3 自然结束；59-file candidate 的主菜单、选角、开始、HUD、地图、暂停、exact mount、三 marker、material error 与 cleanup 全部有 report/hash；失败则保留 typed RED，不重复已知子集 | CK3 严格单实例；exact 1.19.0.6 / EXE SHA 固定 |
| D2 | **P0** | 将 generator split、回归工具、证据和权威报告原子提交并推送 | generator `--check`、B1/scoreboard/manager/central、bisect/projection 及相关静态校验 GREEN；显式 stage；非强推方式吸收远端分叉后 push | D1 结论已写入证据；不提交 disposable `_runtime` |
| D3 | **P0** | 若 D1 GREEN，恢复 Phase2 seed/paused-native 最小路径 | 先冻结正式 B1 checkpoint identity，再选择一个不会重复旧 startup probe 的 seed/只读观察入口；取得真实 artifact 或记录明确 RED | D1 GREEN 才开始；不把 entry GREEN 冒充 seed/OODA |
| D4 | **P0** | 仅在真实 seed 与素材门解锁后推进 8-span 与双片 | 每段 clean span 有 postcondition/receipt；8/8 前不启动 TTS/render；两版独立审阅与导出 | 依赖 D3；publish target 未指定则不发布 |
| D5 | P1 | 日/周/交接持续收口 | 9 月 4 日日报滚动记录、周报回链，RED/cleanup/commit/push 可追溯 | 不重复无新证据的重型测试 |
| D6 | **P0（owner 插单）** | B2 起按用途拆分 effect 文件并固化加载性能取证规则 | 目标每文件 1–10 个 effect、原则上不超过 20；generator/tests/projection 覆盖所有分片；加载性能 RED 时保留同条件拆分 A/B 与完整 hash/timing/log/cleanup 证据 | B1 41+36 保留为冻结恢复基线，不倒改既有 live 结论 |

## 明确非目标

- 不恢复 owner 暂停的 G2，不扩展通用宗教域，不猜 open_kaishek opcode allow-list。
- 不重跑 53/55 基线、七个已知 GREEN stub 子集或旧单文件 B1；只有新生成树、新 seed/observer 观察面才占 CK3 槽。
- 不用 entry、parser、fixture 或单场诊断替代 B1 delayed-path、seed、完整 Phase2 OODA、8/8 素材或最终视频完成。
- 不启动购买、付款、充值、商店下单；CK3 游戏内正常资源支出遵循既有 gameplay policy。

## 晚间对照规则

- 2026-09-05 00:00 在当天日报逐项复制 D1–D5，标明完成、部分完成、未完成或取消；不得事后改写本文件。
- 临时插单在日报记为“计划外完成”；若 owner 调整优先级，记录受影响承诺和新的停止条件。
- readiness 始终区分 `research`、`static-ready`、`fixture-live`、`production-live primitive`、`production-live loop` 与 `complete`。
