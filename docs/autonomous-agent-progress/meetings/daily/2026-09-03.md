# 2026-09-03 早会

状态：**准时窗口内召开（紧接上一日日报收口；实际记录于 00:04）**

会议时间：2026-09-03 00:04（Asia/Shanghai）

上一日日报：[`../../daily/2026-09-02.md`](../../daily/2026-09-02.md)

本周计划会：[`../weekly/2026-W36.md`](../weekly/2026-W36.md)

开场 HEAD：`integration/progress-20260902` / `4fd92a75a8df36ebc277f539c5417ae0278b08bb`

## Opening baseline

- P0 天朝二期的 producer observer 已在 exact `da43787` live 中安装成功，但游戏内协议分类门在 native 计数前 typed RED；旧 attempt
  已完整保留且不会原样重复。永久协议/通知授权已落入 `4fd92a7`，共享处理器正在增加 purchase denylist 和前后证据。
- P0 最终宣传片已有十章双语 authoring、严格 8-span intake、执行/完成 attestation 与 publish-target gate；真实同会话 footage、TTS、
  字幕、FFmpeg 成片、审阅、导出和发布均未发生。宣传工具上次验证 clean 且 `HEAD==origin/main==57c42fca`，实际制作前必须再次更新验证。
- P1 G2 已有 public session-binding live 和 index-7 输入 live；passive evaluator observer/readiness 静态接线完成。expiry、generic
  war-bound、决策/动作/postcondition 与完整 `GEN-034` 未闭合，CK3 候选服从 P0 排他槽。
- P2 open_kaishek 固定 `0390b9a`，对当前 native/MCP/media-only 增量无需修改；只有真实 adapter/API/profile/fixture/corpus 变化才同步演进。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 完成共享 CK3 游戏内协议/通知处理器的永久授权接线 | 任何协议/consent 与普通游戏内通知可分类处理；购买/付款/商店硬拒绝；截图、OCR、按钮及 marker 前后证据齐全；定向测试 GREEN、commit/push | 不启动 CK3；未知购买语义 typed stop |
| D2 | **P0** | 冻结并执行一个新的 exact-head producer-observer live | clean ZIP、private DLL/injector、manifest/no-launch gate 全部 SHA 绑定；仅一次 distinct CK3；terminal report、legal/notification evidence、cleanup 完整 | D1 完成；CK3 排他调度 |
| D3 | **P0** | 沿同一 managed session 推进 canonical seed、loaded-seed v2 与 8-span footage | seed/loaded identity、PID/generation/revision、8/8 postcondition、raw recording 与 16 张 clean frames 全部通过 intake；失败保留 typed artifact | 依赖 D2 通过相应 native gate；不以 fixture 替代实录 |
| D4 | **P0** | 完成最终宣传视频并导出/发布 | 实际制作前 fresh fetch/ff-only 宣传工具至远端 `main` 并验证 clean/HEAD 对齐；生成 TTS、中英字幕、FFmpeg 成片、claims、两名不同 reviewer 的 1x receipt、export/publish receipt | 依赖 D3；外部发布仍需明确平台/账号/credential；不得猜测目标 |
| D5 | **P1** | 继续推进 G2 当前迭代 | 优先完成不占 CK3 的 passive evaluator/war-bound/readiness 静态闭环；P0 释放槽后才运行 distinct G2 live；不把 ACK/schema 冒充能力 | CK3 排他槽优先给 P0 |
| D6 | **P2** | open_kaishek 随实际接口/语料变化同步演进 | 每个 P0/P1 集成增量给出 CHANGE/NO-CHANGE；有变化时更新 adapter/profile/fixture 并预验，无变化不制造提交 | `certified=false` 只由真实覆盖提升 |
| D7 | P1 | 跨日与持续报告、CI、commit/push 闭环 | 日报/周报滚动更新，相关测试与 artifact/commit/push 可追溯；RED 诚实保留 | 不重复无新证据的重型测试 |

## 明确非目标

- 不自动购买，不点击任何真实货币、平台商店、DLC/商品订单、payment/checkout/purchase 控件；系统 Toast 只关闭本体，不点正文。
- 不用 fixture、静态门、协议点击成功或旧一期素材冒充天朝二期 production-live、真实 footage 或最终成片。
- 不并发启动两个 CK3；P0 占用 CK3 时继续推进不依赖 CK3 的 G2、open_kaishek、媒体合同和报告工作。
- 不重跑没有改变观察面的旧 attempt，不扩展 owner 暂缓的通用宗教域，不增加没有实证收益的额外门禁。

## 晚间对照规则

- 2026-09-04 00:00 在当天日报逐项复制 D1–D7，标明完成、部分完成、未完成或取消；不得事后改写本文件。
- 临时插单在日报记为“计划外完成”；owner 调整优先级时记录其时间、受影响工作包和新的验收边界。
- readiness 始终区分 `research`、`static-ready`、`fixture-live`、`production-live primitive`、`production-live loop` 与 `complete`。
