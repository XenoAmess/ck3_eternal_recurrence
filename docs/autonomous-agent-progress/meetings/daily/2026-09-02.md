# 2026-09-02 早会

状态：**补录（真实创建于 2026-09-02 00:41，未倒填为 00:00 已知计划）**

会议时间：2026-09-02 00:41（Asia/Shanghai）

上一日日报：[`../../daily/2026-09-01.md`](../../daily/2026-09-01.md)

本周计划会：[`../weekly/2026-W36.md`](../weekly/2026-W36.md)

开场 HEAD：`origin/master` / `c2cc95c3db23ea834341978467e428d829cc3c2f`

## Opening baseline

- 本次补录开始时，P0 宣传工具拆分已经完成：独立工作目录为 `Z:\workspace\xar_promo_toolchain`，公开仓库为
  <https://github.com/XenoAmess/xar_promo_toolchain>，`v0.1.0` 指向 `262af995382949a4f631735c5b5d4ae4f444902f`；
  主仓已在 `c2cc95c` 收口外部 wheel 的独立安装合同。这些是开场基线，不倒填成 00:00 承诺。
- 独立仓的 ZhongGuo 361 一期能力演示由 `fbdf990163786a93d0e6343a9c7bfe657cdb423f` 提供；它是
  `fixture-live` 样例，不是天朝二期实机成片或 production-live 宣传片。
- 天朝二期保持 `preflight-ready/static + seed blocked + not-live`；已知 CK3 startup particle2 `C0000005`
  capability RED 继续保留，不重跑同一 crash。
- G2 保持 `static/query-ready + paused/live=false + GEN-034 unresolved`；truce paused probe 仍是 CK3 启动前的
  harness RED，没有 PID、paused snapshot 或 live MCP terms。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 收口 `xar_promo_toolchain` 独立拆分、GitHub tag/Release 与主仓接线的权威文字账本 | 日报/周报记录独立路径、公开仓库、`v0.1.0`、demo `fbdf990`、主仓 `c2cc95c`、wheel 完整 SHA、CI 与 readiness 边界 | 只复用已有证据；不修改代码或重跑重型验收 |
| D2 | **P0** | 固化主仓对独立工具包的安装与来源边界 | 明确 `tools/requirements-promo-toolchain.txt` 为同一 venv 的单独安装入口，release wheel 版本与 SHA 均固定；历史主仓内路径只保留 provenance | 不把工具包 Release 写成 PyPI、真实成片或项目完成 |
| D3 | P1 | 延续天朝二期的 current-head gate 与 RED 账本 | 记录 no-launch preflight 路径/SHA、`seed_ready=false` 和既有 crash RED；下一门槛仍是解除启动故障后的新 seed/paused gate | 不重复运行已知 crash，不用静态结果冒充 live |
| D4 | P1 | 延续 G2 truce 的 query/readiness 账本 | 记录 probe report/SHA、launch-before harness RED 与 `GEN-034` 未闭合；下一门槛为修复 harness 后的新独立 paused probe | 无 paused artifact 就不升级 live |
| D5 | P1 | 报告闭环并推送 master | 仅日报、周报、早会三个报告文件有 diff；轻量校验、commit、push 完成并记录 SHA | 推送如遇外部认证/远端更新，保留本地 commit 并报告精确阻点 |

## 明确非目标

- 本轮不清理垃圾、构建目录、失败 attempt、crash dump 或历史本地包；待硬盘再次不足时再处理。
- 不修改产品代码，不启动 CK3、FFmpeg 或 TTS，不制作/上传真实宣传视频，不发布 PyPI。
- 不重跑天朝二期已知 startup crash，不把 no-launch preflight、361 演示 fixture 或 CI 写成 production-live。
- 不执行 G2 surrender/action，不把 truce schema、ACK 或 harness RED 写成 paused/live。
- 宗教通用域继续 owner-deferred；本轮不扩展任何额外门禁或理论安全审计。

## 01:20 owner reprioritization（补录）

- 最高优先级改为天朝二期；G2 保持并行推进。
- `open_kaishek` 作为所有 CK3 验收步骤的首选离线预验层：先运行 parser/validator/IR/runtime/replay，记录版本、profile、fixture/corpus provenance 和结果，再决定是否需要启动 CK3；预验不能替代 live artifact。
- 本日新增验收门：天朝二期 settings 修复后只做一次 bounded seed/paused gate；G2 race 修复后只做一次 MCP-first probe，后续根据 native readiness 结果决定下一步。
- open_kaishek 的 CI/preflight 施工与主仓接线列为持续支撑项，但不得抢占天朝二期的 P0 资源。

## 晚间对照规则

- 2026-09-03 00:00 在当天日报逐项对照 D1–D5，标明完成、部分完成、未完成或取消；不得事后改写本文件。
- 临时插单在日报记为“计划外完成”；若它改变承诺优先级，同时记录 owner reprioritization 与受影响项目。
- readiness 必须继续区分 `static-ready`、`fixture-live`、`production-live` 与 `complete`，所有 RED 保留 harness/capability 分类。
