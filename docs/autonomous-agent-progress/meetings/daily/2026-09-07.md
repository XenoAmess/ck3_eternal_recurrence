# 2026-09-07 早会

状态：**准时召开**

会议时间：2026-09-07 00:00（Asia/Shanghai）

上一日日报：[`../../daily/2026-09-06.md`](../../daily/2026-09-06.md)

本周计划会：[`../weekly/2026-W37.md`](../weekly/2026-W37.md)

开场 HEAD：`master` / `e06f34c3edbfce04fab5623575a716c5ee00bcac`

## Opening baseline

- 天朝二期为最高优先级。功能代码已进入冻结 R130 1,031-file product baseline，但 canonical manager seed 尚未物化，完整迁移树没有一次全量 GREEN。
- R131 manager-transition checkpoint 可恢复；R140/R145/R146 到达过 clean business boundary，但 seed GUI 未产出 candidate。R147/R148 分别被精确原版事件
  `stress_threshold.2202` / `birth.1010` 中断；合同已提交，下一轮应继续而不是重跑已知结论。
- 真实二期 footage `0/8`，最终视频未生成。宣传工具链已更新，人物版 `570s`、制度版 `580s` 长片时间线为 static-ready。
- 简中文案机器规则失败为零，但人工语义与实机渲染仍 pending。G2 与 open_kaishek 只占用非 CK3 并行槽。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 继续 manager-seed 串行验收 | R148 后续未知事件按 exact MCP/native frame 建合同；到达 clean boundary 后执行同日 GUI activation pulse，并产出 candidate save/receipt | CK3 唯一实例；默认 5 速；同产品字节不无故重启；日期或身份漂移立即 fail-closed |
| D2 | **P0** | 从 canonical manager seed 完成完整迁移树验收 | seed 加载身份、完整业务树、关键 postcondition、loader/project error scan、保存/恢复和 cleanup 全部形成同 lineage artifact | D1 seed 真实 GREEN；不得用 fixture、ACK、局部纵切替代 |
| D3 | **P0** | 闭合 source registry 与 `8/8` clean footage | 四类真实 source checkpoint 完整，八段逐段通过 intake、媒体和业务 postcondition，记录 save/PID/generation/revision/SHA | D2 full-tree GREEN；CK3 capture 继续串行 |
| D4 | P1 | 制作并验收两条最终宣传片 | 基于 `8/8` 真实素材构建 570s/580s 候选，完成 probe/claims audit、两轮 1× 具名人审、导出与哈希；有发布授权后再发布 | D3；素材不足时不启动最终 TTS/render，不以静态 timeline 冒充成片 |
| D5 | P1 | 保持文案修复闭合 | 新改文案继续通过账本机器规则；在二期实机中记录动态 scope、换行/截断、正文与按钮职责的真实呈现 | 不额外启动独立 CK3；搭载 D1/D2/D3 会话取证 |
| D6 | P1 | G2 与 open_kaishek 伴随推进 | 只推进不占用 T0 CK3 的真实迭代；T0/T1 有 API/schema/架构 delta 时同步并复验 open_kaishek | 无 delta 不制造提交；G2 CK3 live 等待 T0 槽释放 |
| D7 | P1 | 同步日报、周报与权威证据 | 每个 live/RED 记录 artifact、hash、能力等级、cleanup 和 commit；不把 partial 写成 complete | 当场更新，避免跨日补账 |

## 明确非目标

- 不讨论单文件过大是否有问题；发现大文件直接按用途拆分。effect 文件目标 `1–10` 个，原则不超过 `20`，例外必须有理由和实机证据。
- 不依赖 OCR 判断玩法状态；OCR/image 仅允许非玩法 legal-consent/front-end gate，玩法使用 MCP/native。
- 不重复启动已经由相同 save SHA、产品字节和失败签名证明无新信息的 CK3 轮次。
- 不扩张 owner-deferred 通用宗教域；只保留圣战战争 OODA 与婚姻必要判定两项最小例外。

## 晚间对照规则

- 2026-09-08 00:00 在当日日报逐项对照 D1–D7，标明完成、部分完成、未完成或取消；临时插单另列，不倒改本计划。
