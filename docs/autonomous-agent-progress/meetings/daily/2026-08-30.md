# 2026-08-30 早会

状态：**准时召开**

会议时间：2026-08-30 00:00（Asia/Shanghai）

上一日日报：[`../../daily/2026-08-29.md`](../../daily/2026-08-29.md)

开场 HEAD：`mod-zhongguo-style-wip` / `f0791af`

## Opening baseline

- 项目所有者已冻结 `mod_zhongguo_style` 当前版本功能；361 新机制仅继续预研和记录，下版本才允许接产品运行时。
- 当前 mod 静态测试与双构建可复现；但尚无本候选的完整 MCP/GUI 实机、fresh Workshop cache、8/8 媒体、正式 GREEN capture 或最终宣传片。
- title-map exact-build reverse、Python facade 与 native production wire 已 `static-ready`，fresh Release CTest `41/41`；没有 CK3 live，不得称 `fixture-live`。
- 录制前相机仍接 OCR/键鼠旧路径；runner 另有 `close_window` 旧字面假 RED，必须在下一次 CK3 启动前修复并静态验收。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | P0 | 闭合 title-map MCP production wire 与专用 live runner | full Release/CTest GREEN；同一 managed PID 完成 `c_bianzhou`、`b_kaifeng`、repeat already-centered、unknown typed RED，保存 binding/camera/cleanup artifact；零 OCR/键鼠/剪贴板 | live 前必须冻结 DLL/injector/EXE hash；若 capability RED，保留 attempt 并只修该 blocker |
| D2 | P0 | 修正当前 acceptance harness 并批量验收冻结版本 | `close_window` 合同修正；一次或尽量少的 CK3 启动覆盖当前 361 配置 smoke、考核榜双视角、所有新增 GUI 阻塞态、京察/拒办、3.25/PIP/申诉、第二 AI 例外；日志/截图/report 留存 | D1 先为镜头/片场提供稳定 MCP；任何产品 RED 定向修复，不新增机制 |
| D3 | P0 | 重基线并构建当前版本 release candidate | README/checklist 明确当前版真实边界；下版本 38 领域不再是当前硬门；静态/loc/thumbnail/builder 全 GREEN，生成 clean staging/manifest/ZIP | D2 当前功能实机通过；不得把 361 配置 smoke 写成 361 独立机制 |
| D4 | P1 | 补齐 Workshop/演示物料并准备上传 | 8/8 真实实机图、BBCode 限额内、真实角色/零测试 UI、中文主叙事+中英字幕 Xiaoxiao 宣传片 `<20:00`；所有 raw/音轨/ASS/中间产物保留 | 必须来自同一正式 GREEN capture；缺图时不伪造或复用 RED 根报告 |
| D5 | P1 | 创建并验证 Steam Workshop 物品 | 仅从 formal staging 上传；记录新 item ID/日志，随后重建无 ID staging；fresh download cache/production smoke GREEN | 需要用户 Steam/Launcher 会话实际可用；外层 `.mod` 才允许保存 ID |

## 明确非目标

- 不实现 38 领域、361 套独立状态机或任何其它新机制；只更新下版本研究文档。
- 不恢复 OCR、输入法、键盘、鼠标作为头衔定位正式 fallback；CK3 输入保持 US English HKL `0x04090409`。
- 不把 static fixture、transport ACK、配置 361/361 或历史 RED capture 写成 live/release GREEN。
- 不让自动游玩副线、宗教域或理论安全加固挤占当前 mod 验收、发版、上传和演示主线。

## 晚间对照规则

- 次日 00:00 在当天日报逐项复制 D1…D5，标明完成、部分完成、未完成或取消；不得事后改写本文件。
- 临时插单在日报记为“计划外完成”；若它改变承诺优先级，同时记录 owner reprioritization 与受影响项目。
