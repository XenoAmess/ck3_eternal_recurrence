# 2026-08-30 早会

状态：**15:06 实时补录；不是 00:00 同时记录**

会议时间：2026-08-30 15:06（Asia/Shanghai）

上一日日报：[`../../daily/2026-08-29.md`](../../daily/2026-08-29.md)（原定 00:00 收口未执行；已于本会议前延迟补收口并明确记录偏差）

本周计划会：[`../weekly/2026-W35.md`](../weekly/2026-W35.md)

开场 HEAD：`agent-mainline-20260827` / `f41c12a49b78bef91ee3d8887b797aaa56655ebf`；`origin/master` 同步。

## Opening baseline

- G1 仍绑定 CharacterID `29829` / episode `native-29829-ee172aa720db`。会议补录前，owner 已授权恢复 handoff 唯一 P0；本日
  15:02 已完成四项 pin 的 no-launch preflight，并从 `0DF9CB66...69C` 启动正式 production 长跑。
- 会议补录时首个新 durable checkpoint 已存在：`date_raw=53281440 / history=6200 / 1D8944B0...0C583`；角色仍存活，G1
  尚未完成。这是会议前已经发生的计划外基线，不倒填为 00:00 已知。
- `Z:\ck3_mod_rewrite` 的 autonomous-player 工作树存在其他执行者/用户脏改动；本日只使用 handoff 指定的独立 mainline
  worktree，不读取这些改动作为 runtime，也不覆盖、清理或提交它们。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 继续 CharacterID `29829` 的同一正式一生长跑 | 无人工 gameplay；持续保存 run/report/history/checkpoint；自然死亡后继续等匹配的琉焰卿 committed settlement | 普通 turn/wall bound 只续跑；真实 B0/B1 才停下最小修复 |
| D2 | **P0** | 严格判定 G1 terminal | `ready=true / commit_serial=1`；source CharacterID 匹配；三处 score 相等；record/no-heir/cleanup 全绿；结算后不启动继承人 gameplay | 在 committed score 出现前禁止宣告 G1 GREEN |
| D3 | P1 | 滚动维护可复核交接记录 | 当日日报和 W35 周报记录每个 attempt、最新 durable anchor、PID/命令、GREEN/RED 与真实 readiness 边界 | 不因写报告中断 live runner；不 commit/push，由主线程协调 |

## 明确非目标

- 不重复 20-turn canary，不做 speed 4/5 预研，不为吞吐改变宣战、参战、续战、议和、投降或终战意愿。
- 不扩展宗教域，不做理论安全审计，不打开与真实 blocker 无关的新功能包。
- 不触碰 `mod_zhongguo_style` 发布文件，不操作 `Z:\ck3_mod_rewrite` 的脏 autonomous-player 文件。
- 所有目标窗口输入线程保持 US English HKL `0x04090409`，不切回中文输入法。

## 晚间对照规则

- 2026-08-31 00:00 在 [`../../daily/2026-08-30.md`](../../daily/2026-08-30.md) 对照 D1–D3；G1 未自然结束时按
  “部分完成/顺延”记录，不把 runner 仍在运行或普通 bound 误记为失败。
- 同一 W35 增量继续写入 [`../../weekly/2026-W35.md`](../../weekly/2026-W35.md)，周报也在 08-31 00:00 正式收口。
## 00:00 ZhongGuo 361 并行早会（独立分支记录）

状态：**该工作流准时召开；与上文 15:06 G1 补录是两条并行记录**

开场分支/HEAD：`mod-zhongguo-style-wip` / `f0791af`

### ZhongGuo Opening baseline

- 项目所有者已冻结 `mod_zhongguo_style` 当前版本功能；361 新机制只继续预研和记录，下版本才接产品运行时。
- 当前 mod 静态测试与双构建可复现，但尚无本候选的完整 MCP/GUI 实机、fresh Workshop cache、8/8 媒体、
  正式 GREEN capture 或最终宣传片。
- title-map exact-build reverse、Python facade 与 native production wire 为 `static-ready`，fresh Release CTest
  `41/41`；尚无 CK3 live，不称 `fixture-live`。
- 录制前相机仍接 OCR/键鼠旧路径；runner 另有 `close_window` 旧字面假 RED，必须在下一次 CK3 启动前修复并静态验收。

### ZhongGuo 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| Z1 | P0 | 闭合 title-map MCP production wire 与专用 live runner | full Release/CTest GREEN；同一 managed PID 完成 `c_bianzhou`、`b_kaifeng`、repeat already-centered、unknown typed RED，保存 binding/camera/cleanup artifact；零 OCR/键鼠/剪贴板 | live 前冻结 DLL/injector/EXE hash；capability RED 只修该 blocker |
| Z2 | P0 | 修正 acceptance harness 并批量验收冻结版本 | 修正 `close_window` 合同；一次或尽量少的 CK3 启动覆盖 361 配置 smoke、考核榜双视角、全部新增 GUI 阻塞态、京察/拒办、3.25/PIP/申诉、第二 AI 例外；保存日志/截图/report | Z1 先提供稳定 MCP；产品 RED 定向修复，不新增机制 |
| Z3 | P0 | 重基线并构建 release candidate | README/checklist 陈述真实边界；下版本 38 领域不作为当前硬门；静态/loc/thumbnail/builder 全 GREEN，生成 clean staging/manifest/ZIP | Z2 实机通过；不得把 361 配置 smoke 写成 361 独立机制 |
| Z4 | P1 | 补齐 Workshop/演示物料并准备上传 | 8/8 真实实机图、BBCode 限额内、真实角色/零测试 UI、中文主叙事+中英字幕 Xiaoxiao 宣传片 `<20:00`；保留 raw/音轨/ASS/中间产物 | 必须来自正式 GREEN capture；不伪造或复用 RED 根报告 |
| Z5 | P1 | 创建并验证 Steam Workshop 物品 | 仅从 formal staging 上传；记录 item ID/日志，随后重建无 ID staging；fresh download cache/production smoke GREEN | 需要用户 Steam/Launcher 会话；仅外层 `.mod` 保存 ID |

### ZhongGuo 非目标与晚间对照

- 不实现 38 领域、361 套独立状态机或其它新机制；只更新下版本研究文档。
- 不恢复 OCR、输入法、键盘或鼠标作为头衔定位正式 fallback；CK3 输入保持 US English HKL `0x04090409`。
- 不把 static fixture、transport ACK、配置 361/361 或历史 RED capture 写成 live/release GREEN。
- 不让自动游玩副线、宗教域或理论安全加固挤占当前 mod 验收、发版、上传和演示主线。
- 次日 00:00 在当天日报逐项对照 Z1–Z5；临时插单单列，并记录任何 owner reprioritization。
