# 2026-09-01 早会

状态：**准时召开**

会议时间：2026-09-01 00:00（Asia/Shanghai）

上一日日报：[`../../daily/2026-08-31.md`](../../daily/2026-08-31.md)

本周计划会：[`../weekly/2026-W36.md`](../weekly/2026-W36.md)

开场 HEAD：`master` / `adc70c7ec705cbb3a9fc04f43e3c1c93f9eed68f`

## Opening baseline

- 项目所有者于跨日前把宣传视频工具链抽离提升为最高优先级，并明确要求已经运行的二期/G2线程自然完成、不得暂停或浪费工作量。
- 通用工具链已有经过 quick validation 的 Codex Skill 初稿与完整提取清单；真实 package/CLI、CK3 adapter、audit/evidence、TTS
  与 legacy wrapper 尚在并行落盘，不能把 Skill 文档冒充可运行工具。
- ZhongGuo 二期为 Python/CK3 static/central-wired 361/361，但正式产品口径仍是
  `domain_runtime=partial ×361 / complete ×0`、bounded live 4/361。B2 与 AI-owned action helper 已完成；Incident、Workforce、
  current-runtime seed、scoreboard named-widget action 与合批 live 仍在途。
- G2 第二寿命与再次跨 episode 已完成；Raiktor surrender favor-hook WarID 双采样已 static/fixture GREEN，尚未进入统一 MCP/live。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 落地可安装的 `xar_promo` 核心与 CLI | `pyproject`、schema、真实 `init/validate`、append-only run/preserve/signoff 合同、单测、安装后 `--help` 全 GREEN；禁止空壳成功 | 只使用通用配置，不写 CK3/361 硬编码 |
| D2 | **P0** | 并行抽出 CK3 adapter、audit/evidence 与 Edge TTS | capture bundle/clean spans/hash、确定性审计记录、人工 signoff 边界、content-addressed TTS cache 均有独立测试；失败材料保留 | 依赖 D1 的最小接口时以独立模块施工，禁止双写 core |
| D3 | **P0** | 迁移现有宣传脚本到兼容 wrapper + ZhongGuo preset | 旧 CLI 参数/退出码/sidecar 与现有 validate-only 测试不变；361/真实角色/晓晓/20 分钟规则留在 preset/adapter | D1/D2 稳定后接入，不删除旧脚本或过程素材 |
| D4 | P1 | 自然收口已在运行的二期/G2线程 | Incident/Workforce action、seed、Workforce multi-owner、scoreboard reverse 和 G2 后续结果逐批测试、提交、推送；不因优先级调整丢弃 | 已运行线程完成即释放并发给 D1–D3 |
| D5 | P1 | 继续二期 MCP-first live 前置 | 生成当前-runtime 真实 seed，集成 action cells 与 scoreboard safe invoke；Loader/paused/save-restore 前禁止宣称 live | 禁止 OCR、假 ID、旧一期视觉路径 |
| D6 | P2 | G2 favor-hook 接统一 terms wire/MCP | exact WarID/CB/roles 与 paused provider 可查询，有 fixture 与 paused artifact 才提升 readiness | 不重复已完成第二寿命；宗教通用域继续暂缓 |
| D7 | P1 | 日/周证据收口与 CI | 每批 commit/push、官方 CI、artifact/RED/readiness 实时落盘 | 不为报告中断长跑任务 |

## 明确非目标

- 不暂停或关闭跨日时已经运行的二期/G2线程；它们完成后再重新分配，不制造返工。
- 不在工具链中硬编码 ZhongGuo 361、CK3、晓晓或中英字幕；这些属于 adapter/preset。
- 不自动伪造人工 signoff，不删除失败 take、原始录屏、TTS、字幕、日志或中间导出。
- 二期未达到全功能与实机门前，不启动七语正式审计、Steam 更新或最终宣传片发布。
- 不用 OCR、坐标、测试决议或 command ACK 冒充二期/G2 的状态真值。

## 晚间对照规则

- 2026-09-02 00:00 在 [`../../daily/2026-09-01.md`](../../daily/2026-09-01.md) 逐项复制 D1–D7，标明完成、部分完成、未完成或取消；不得事后改写本文件。
- 临时插单单列；若再次改变优先级，记录 owner reprioritization 与对已有线程的实际影响。
