# 2026-09-01 早会

状态：**准时召开**

会议时间：2026-09-01 00:00（Asia/Shanghai）

上一日日报：本项目此前无 2026-08-31 日报；本次为 `open_kaishek` Phase 0 开工补录。

开盘 HEAD：`detached` / `9c978b3`

## Opening baseline

- `open_kaishek` 已获授权从前期预研进入 Phase 0；里程碑 M0 为 `in-progress`。
- 当前能力仅处于 `research` / `static-ready`：已有项目章程、架构/路线图、ADR、corpus manifest 与工程骨架；尚无 CK3 live artifact。
- M0 仍需冻结 JDK、构建工具链、第三方许可与版本指纹，并完成 schema/合同审阅；不得把静态 fixture、ACK 或文档宣称为 live。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | P0 | 启动 `open_kaishek` Phase 0 合同冻结工作，审阅 ADR-0002 与首批 corpus manifest | 形成可审阅的 M0 输入清单，明确 pending 决策、readiness 与禁止越界项 | 依赖 JDK/构建/许可决策；未冻结前不进入 Runtime 或 CK3 live |
| D2 | P0 | 维护进度报告与证据索引 | 日报/周报链接 ADR 与 manifest，明确无 live artifact、测试与阻点 | 任何未实际运行的内容标记 research/static-ready |

## 明确非目标

- 不实现或发布 Runtime、CK3 exact-build/MCP 差分、Quarkus 服务或完整 parser。
- 不复制未经许可审阅的第三方源码，不把静态构建/fixture 升级为 `fixture-live` 或 `production-live`。

## 晚间对照规则

- 次日 00:00 按 D1–D2 原样核对，标明完成、部分完成、未完成或取消；临时插单仅写入日报“计划外完成”。

## 当日实际更新（2026-09-01 19:30，补录）

用户继续授权后，施工范围按“先形成可复核静态基线、仍不宣称 live”调整。已完成
`open_kaishek` 多模块纯 Java 工程、lossless parser、profile/validator、IR/runtime/
diff 合同、361 schema 和 CLI；并补齐 synthetic 014 的 BOM `.txt` → Parser →
Validator → IR → VM 离线夹具，状态链为 `delivered → appeal_open → closed`。原计划中
“不得实现 parser/runtime”属于开盘时的非目标，已由本次授权后的计划外交付 supersede。
M0 目前为 `static-ready`、待正式评审；M1/M2 为 `static-ready`，M3 为 runtime 原语
加 synthetic 014 夹具的 `static-ready`；完整 361 exact-build/M4、M5 与 CK3 live 仍未
完成/不可用。验收细节和 hash 见 [`phase0-verification.md`](../../../open_kaishek/docs/phase0-verification.md)。
