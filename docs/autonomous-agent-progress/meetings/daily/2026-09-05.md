# 2026-09-05 早会

状态：**准时召开**

会议时间：2026-09-05 00:00（Asia/Shanghai）

上一日日报：[`../../daily/2026-09-04.md`](../../daily/2026-09-04.md)

本周计划会：[`../weekly/2026-W36.md`](../weekly/2026-W36.md)

开场 HEAD：`integration/progress-20260902` / `10affce991f17af642bad94c8952c45778e673d2`

## Opening baseline

- 固定交付物口径为：T0 天朝二期 `50/100`、T1 G2 当前周期 `90/100`、T2 open_kaishek 当前同步 horizon `100/100`；按 `60% / 25% / 15%` 加权，整体为 **67.5%**。T2 的 100% 只表示当前已知同步面闭合，T0/T1 后续 API、schema 或架构变化会重新打开对应同步项。
- B3 R15 在 9 月 4 日结束前已进入 CK3 实机环节，00:00 时仍跨午夜运行并独占 CK3 槽。在它自然结束并完成进程、job、watchdog 与 control-file cleanup 前，不启动任何第二个 CK3 工作包。
- Phase2 可用宣传素材仍为 **0/8**，最终 MP4 仍为 **0/2**。当前 entry、静态解析、fixture、ACK 或单场诊断均不能冒充 promotion source checkpoint、clean span 或成片。
- 今日最短交付路径是先闭合 promotion provider/action/D+1 source checkpoint，再冻结权威 registry 中四类 source checkpoint，最后按 `1 → 8` 严格串行录制 clean spans。每一步只在上一门取得真实 provider postcondition、可复核路径/哈希与 cleanup 后解锁。
- effect 文件继续按用途分组，目标每文件 `1–10` 个，原则上不得超过 `20` 个；超过 20 的例外必须记录理由和实机证据。只有出现可归因到 loader/load-time 的性能 RED 时，才进一步尝试同条件拆分 A/B 并保留 tree hash、timing、日志与 cleanup；业务状态、旧存档兼容或事件语义 RED 不得被记作文件体量问题。
- G2 与 T2 只推进不与当前 CK3 槽冲突的离线工作；任何 G2 实机环节必须等待 T0 当前 CK3 owner 释放。宗教域继续 owner-deferred，仅保留战争圣战与婚姻必要判定两项最小例外。
- 工作树保留既有 tracked/untracked runtime evidence；不得 reset/clean。正式提交继续显式选择代码、测试和权威文档，不纳入 disposable runtime。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 收口跨午夜 B3 R15，并闭合 promotion provider/action/D+1 source checkpoint | R15 自然结束；真实 provider 状态、受限 action、独立 D+1 postcondition 与 source checkpoint 全部绑定 exact build/product/save identity；报告含路径、字节数、SHA-256、日志和 cleanup。失败则保留 typed RED，不把 action ACK 当状态证据 | CK3 严格单实例；R15 cleanup 完成前禁止第二个 CK3 工作包 |
| D2 | **P0** | 冻结四类 source registry | 权威 registry 达到 `4/4`：每类都有真实 checkpoint、exact owner/event/date/product identity、独立 postcondition、路径/字节/SHA 与不可变性复核；fixture、旧 artifact 或静态可达性不得代替 live source | D1 GREEN 才开始；任一类别 RED 则保留证据并停在该 gate，不伪造余项 |
| D3 | **P0** | 按 `1 → 8` 顺序串行录制 Phase2 clean spans | 每段从 registry 对应 checkpoint 恢复，取得可见过程、独立 postcondition/receipt、媒体探针与 cleanup；累计素材从 `0/8` 推进到 `8/8`，不得并发启动 CK3 或跳号拼接 | D2 `4/4` GREEN；上一段 cleanup 完成后才释放并重新取得 CK3 槽 |
| D4 | P1 | 仅在素材门闭合后解锁双片制作 | `8/8` 前不启动最终 TTS/render；`8/8` 后按已更新至远端 main 的宣传工具链生成、全长审阅并分别导出两条 MP4，保留媒体参数、字幕安全区检查与 SHA-256 | 依赖 D3；任一素材需重录时先回到对应 serial span |
| D5 | P1 | G2/T2 非冲突伴随推进 | 只运行不启动/附加 CK3 的 G2 离线收口和 T2 parser/round-trip/compat/corpus 检查；若 T0/T1 出现新 API/schema/架构 delta，则同步修正并复验 | CK3 槽始终让位于 D1–D3；无真实接口 delta 时不制造 open_kaishek 改动 |
| D6 | P1 | 当日进度、证据与提交持续收口 | 9 月 5 日日报逐项记录 GREEN/RED、artifact、readiness、commit/push；正式改动通过相称验证后显式 stage、commit、push | 不重复已有结论的重型检查；不提交 disposable runtime |

## 明确非目标

- 不把 R15、action ACK、parser、fixture 或 source 可达性冒充 promotion provider/D+1 postcondition、四类 registry、8/8 素材或最终视频。
- 不在 CK3 槽已被 R15 或 Phase2 录制占用时启动 G2/其他 CK3 工作包；非 CK3 工作可以并行。
- 不因为一般业务 RED、旧存档兼容 RED 或事件语义 RED继续细拆 effect 文件。按用途分组、每文件 `1–10` / 原则 `≤20` 是常态边界；只有 loader-performance RED 才触发额外拆分取证。
- 不扩展通用 faith/doctrine/tenet/fervor、改宗、宗教改革或 holy order；仅允许圣战战争 OODA 与婚姻必要判定的最小原生结果/原因。
- 不启动购买、付款、充值或商店下单；CK3 游戏内正常资源支出遵循既有 gameplay policy。

## 晚间对照规则

- 2026-09-06 00:00 在当天日报逐项复制 D1–D6，标明完成、部分完成、未完成或取消；不得事后改写本文件。
- 临时插单在日报记为“计划外完成”；若 owner 调整优先级，记录受影响承诺和新的停止条件。
- readiness 始终区分 `research`、`static-ready`、`fixture-live`、`production-live primitive`、`production-live loop` 与 `complete`。
