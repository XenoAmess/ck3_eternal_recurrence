# 2026-08-28 早会

状态：**00:02 实时补录；不是 00:00 同时记录**

会议时间：2026-08-28 00:02（Asia/Shanghai）

上一日日报：[`../../daily/2026-08-27.md`](../../daily/2026-08-27.md)（已于 00:00 正式收口）

本周计划会：[`../weekly/2026-W35.md`](../weekly/2026-W35.md)；ISO 周未变化，继续执行同一 W35 计划，不另建周计划会。

开场 HEAD：`agent-mainline-20260827` / `79b8d2acfbe1f80a7a7f88da3fed7cda1791017c`；`origin/master` 同步。

## Opening baseline

- G1 仍绑定 CharacterID `29829` / episode `native-29829-ee172aa720db`。最新可恢复 checkpoint 为
  `date_raw=53209560`，SHA-256 `A8DD4034C32856B8D1E05D6B834BBBF3C51AA74DA038BB22A0CA23A998AD76CF`；角色尚未完成
  自然死亡、琉焰卿 committed settlement、人生分数与终局 cleanup，不能标记 G1。
- `a8ff95f` 的 first-safe 路线已用真实生产帧把旧 `132` 次 move preview + `35` 次 route-contact query（共 `167` 条）缩为
  第一个安全候选的 `1` 次 exact preview；选中 Province `3708` 的路线为 `12` hops，不保证全局最短。
- `7cb0b75` 已批量延迟成功只读 query/preview 的 driver-state 持久化，动作、失败、checkpoint/restore 与 close 仍是 durable barrier。
  它解决了约 79 MB state 被逐查询 deepcopy + atomic rewrite 的实证写放大；尚无独立 post-fix 121-query live 计时。
- 首轮速度 A/B 已 live 执行 `12` 次 life advances、推进 `30` 游戏日；玩家已有 `12` hops route 且敌军追尾，12 次均正确选择
  speed 1。adaptive speed 3 因没有实际命中样本，仍是 `static-ready / production-live pending`。
- timeline wait 的历史热点约复制完整 history `30` 次、累计 `18.58s`。lean snapshot 的局部确定性测量为复制 `9 → 1`、
  `6.669s → 0.632s`；截至昨日日报 cutoff 只算测试/static-ready。独立审查及提交已在会前 00:01 完成，形成并推送
  `79b8d2a`；这项 08-28 开场事实不倒填为 08-27 的 commit。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 对 `79b8d2a` 做同 checkpoint lean snapshot live A/B | 从 `53209560 / A8DD4034...AD76CF` 的隔离副本运行；记录 wall-time、实际 history/snapshot 复制行为、turn/game-day、checkpoint 与 cleanup；相同行为合同不漂移，保留完整 artifact | 只使用干净 runtime 和隔离 state；测试计时不能替代 CK3 live；若出现真实行为差异立即保留 RED 并停止推广 |
| D2 | **P0** | 继续 adaptive speed-3 production gate | 真实命中 `remote_enemy_route` 时，逐条记录 requested horizon、实际 elapsed、before/after route/contact/combat/siege、paused post-state 与吞吐；满足既定 gate 才升 production-live | 玩家 route、敌军追尾、战斗、撤退、Assault 或观测缺失时必须保持 speed 1；未命中只能记 inconclusive，不能强造 speed-3 样本 |
| D3 | **P0** | 恢复正式一代人 G1 长跑 | D1/D2 不再构成真实 blocker 后，从最新 durable anchor 继续同一 CharacterID/episode 的正式 `native-one-generation` 合同；普通 turn/wall bound 直接续跑 | 仅真实 B0/B1 才保留 report/first-blocker、先更新对应 exact-build 原生树并做最小解除 |
| D4 | **P0 / terminal** | 完成死亡后的琉焰卿人生分数结算 | 自然死亡后继续等待 `ready=true / commit_serial=1`；记录 `terminal-settlement.json.one_life_settlement.final_score`，并证明它等于顶层 `score` 与 `recorded_episode.score`；record persistence、cross-run record、零继承人 gameplay、cleanup 全绿 | 角色存活或 committed settlement 未发布时继续等待；不得在死亡瞬间提前结算 |
| D5 | P0 | 次日午夜逐项收口本早会并滚动更新 W35 周报 | 2026-08-29 00:00 的 08-28 日报逐项标记完成/部分/未完成/取消并写明顺延；同一 W35 周报更新，不创建 W36 文件 | 不用滚动工作记录冒充正式日结；周报仍到 08-31 00:00 才正式关闭 |

## 执行顺序

1. 会前已完成 lean snapshot 的独立审查与 `79b8d2a` 提交；从该 clean commit 立即进行同 checkpoint live A/B。
2. A/B 保持行为与恢复合同后，继续寻找真实、合法的 speed-3 gate 命中；没有命中就诚实保留 pending。
3. 随后恢复正式 G1，不让非阻塞的完整 effects、forecast、reinforcement/join 或通用治理质量债抢占全寿命闭环。
4. 死亡后等待琉焰卿 committed scoring，记录真实人生分数后才结算。

## 明确非目标

- 不为了展示提速而在玩家已有路线、敌军追尾、战斗/撤退/Assault 或观测缺失时强制 speed 3；speed 1 是这些帧的正确选择。
- 不重复 167-query 全量路线扫描，也不恢复逐条只读命令全量重写 driver-state。
- 不为未发生的安全形状新增门禁、WAL 或防御协议；只处理生产日志、计时与 live A/B 已证明的功能/性能问题。
- 不让事件完整 effects、通用宣战、reinforcement/join、forecast 或宗教通用域延迟 G1；宗教继续 owner-deferred，仅保留圣战战争
  OODA 与婚姻必要判定两项最小例外。

## 晚间对照规则

- 次日 00:00 在 `daily/2026-08-28.md` 原样对照 D1–D5；计划外插单另列，不事后修改本早会提高完成率。
- 本周仍是 `2026-W35`，每日增量继续写入同一份 [`../../weekly/2026-W35.md`](../../weekly/2026-W35.md)。
## 01:27 所有者插单补充（Edge TTS / Sonia）

状态：**按真实时间补录；不冒充 00:00 已知计划**

开场分支/HEAD：`master` / `5bc54852cd87d71012fbc25e6f8d451b4439ddd9`

### 补充 Opening baseline

- 所有者在 01:27 前已批准把完整能力 show-off 构建器从 Windows SAPI 迁移到
  `edge-tts==7.2.8`，默认声音指定为 `en-GB-SoniaNeural`，并明确不重制 2026-08 canonical 成片。
- 补录时核心代码、离线测试和在线短句/title-card smoke 已开始执行；这些是补录前事实，不倒填为 00:00 已知计划。

### 补充承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| E1 | P0 | 完成 Edge TTS / Sonia 展示视频构建迁移 | SAPI 路径移除；缓存与 sidecar 记录 provider/version/voice；离线测试、静态校验、在线短句和单章媒体 smoke 全绿；历史成片不变；提交并 push | Edge 在线服务可用于一次 smoke；不等待或重制 27 章历史成片 |

### 补充非目标与对照

- 不重制或替换 2026-08 的 20 分钟 canonical 视频，不改其路径、时长、媒体哈希或证据等级。
- 不新增 SAPI fallback、离线模式、语速/音高/音量配置面，也不扩展任何 CK3 gameplay capability。
- 2026-08-29 00:00 在当天日报单独对照 E1；本插单与既有一代人主线的关系标为计划外工具链工作。
