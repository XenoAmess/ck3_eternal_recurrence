# 2026-09-08 早会

状态：**准时召开**

会议时间：2026-09-08 00:00（Asia/Shanghai）

上一日日报：[`../../daily/2026-09-07.md`](../../daily/2026-09-07.md)

本周计划会：[`../weekly/2026-W37.md`](../weekly/2026-W37.md)

开场 HEAD：`master` / `f747a6ad0b7e9060d23674697a3eeed8bf38b424`

## Opening baseline

- 天朝二期仍为最高优先级：T0 `50%`，strict CK3-live `4/361`、尚余 `357`；full-tree definitions `106/626`，完整阶段 `8/11`。canonical manager seed R159 已完成，但 post-turn-abort recovery 尚未恢复 loader/map/mailbox。
- R280 已用 `/Z7` dump 直证：`pdx_paths` task 进入一次；`paths.settings` 与 `paths_checksummed.settings` lookup 均返回 NULL；parser/insert 均未进入，named-path table 保持空。新静态逆向已把 lookup 的最强分支收敛到 VFS manager `+8` mount list 为空或所有 backend miss。
- T0-P2 继续 hard-locked：source registry `0/4`、footage `0/8`、MP4 `0/2`。宣传工具已更新但不得开始最终媒体制作。
- T1 `90%`，下一有效入口需占用 CK3 GEN-034 live；T2 当前 horizon `100%`，仅在公开 API/schema/架构变化时同步。
- 工作树 evidence 未丢失，但 Git metadata 仍不可写；commit/push pending，不能伪称已交付远端。

## 今日承诺

| # | 优先级 | 可交付项 | 验收条件 | 依赖 / 停止条件 |
|---:|---|---|---|---|
| D1 | **P0** | 闭合 `0x3BE2340` lookup 与前置 VFS publisher 的 exact-build 原生树 | 固定 PathView ABI、normalization、mount-list head、prefix/resolve、registration callsite 与 NULL 分支；更新原生专题并给出一个可判别的最小 read-only seam | 不启动 CK3；不 patch lookup、不盲拷文件；已有结论不重复逆向 |
| D2 | **P0** | 实现并静态验收最小 VFS mount-state observer | default-OFF、exact-build、initial-suspended、read-only；事务式安装；synthetic/source contract GREEN；与现有三 observer 可同 build dump 解码 | D1 先给出明确挂点；没有判别价值则不实现 |
| D3 | **P0** | 只执行一次有新信息的 CK3 recovery | no-launch preflight、空进程槽、同 autosave/product；实机必须区分 empty mount list 与 backend miss，并保留 dump/cleanup artifact | CK3 串行独占；D2 GREEN 后执行；同 failure shape 不原样重跑 |
| D4 | **P0** | 若 startup 恢复，重建二期 late-save lineage | Frontend、loader `303/303`、paused map/mailbox；重放既有四合同后执行 `.2211` authored/native `4/3` 并保存耐久 checkpoint | 仅在 D3 证明启动恢复时进入；否则继续最小根因施工 |
| D5 | P0 | 恢复完整迁移树推进 | 重抵 AF5，先验 route 3 authored/native `42/41`，再推进 stage 9 `.390`、stage 10 manager、stage 11 Workforce 与剩余 full-tree gate | D4 durable checkpoint；不以局部 ACK/fixture 代替 full-tree |
| D6 | P1 | 保持 G2/open_kaishek 同步演进 | T0 不占 CK3 时执行 GEN-034 live；任何公开 delta 后复验 open_kaishek，否则维持 no-code-change | CK3 仍按 T0→T1 串行；owner utility 未批准时不造默认模型 |
| D7 | P1 | 当场维护报告、验证与提交 | 每个 GREEN/RED 写 artifact/hash/cleanup/readiness；验证完成后提交推送；Git metadata 仍不可写则保留具体 blocker | 不重复无新证据的测试；不触碰用户已有 dirty runtime 文件 |
| D8 | P1 | 在 T0-P1 完成后进入最终宣传视频 | 先完成工具版本复核/主线更新验证，再做 source `4/4`、footage `8/8`、两条候选片与审阅/导出/发布 | **硬停止条件：T0-P1 未完成时保持 locked，不制作媒体** |

## 明确非目标

- 不把 startup/diagnostic GREEN 或 RED 计入场景覆盖、产品 readiness 或 T0-P1 完成度。
- 不复制安装树 `paths*.settings`/`map_data` 到 userdir、mod 或 release staging；没有实证时不增加 guard 或改写 VFS 控制流。
- 不提前启动 TTS、FFmpeg、素材录制或最终宣传片制作。
- 不扩张 owner-deferred 通用宗教域；仅保留圣战战争 OODA 与婚姻必要判定两项最小例外。

## 晚间对照规则

- 2026-09-09 00:00 在当日日报逐项对照 D1–D8，标明完成、部分完成、未完成或取消；计划外完成另列，不倒改本计划。
