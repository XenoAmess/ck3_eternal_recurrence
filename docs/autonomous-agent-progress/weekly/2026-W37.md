# 2026-W37 周报

状态：**滚动周报；现行 T0-P1 机器门 `1/9`，R400 AF5 在源选项执行后等待结果事件超时，RED 已保留并完成受管清理；T0-P2 继续 `LOCKED`。历史 `50% / stage 8/11 / source 3/4` 只作旧口径记录；`11.1%` 仅表示九项签收计数比例**

周期：2026-09-07 至 2026-09-13

正式收口：2026-09-14 00:00（Asia/Shanghai）

本周计划会：[`../meetings/weekly/2026-W37.md`](../meetings/weekly/2026-W37.md)

提交范围：`e06f34c3edbfce04fab5623575a716c5ee00bcac` 至当前 `master`（滚动）

## 09 月 11 日：接手方法论评估与 R400 失败阶段勘误

- 用户要求把只读评估落进 docs，已形成
  [T0 接手评估与工作流改进建议](../retrospectives/2026-09-11-t0-methodology-review.md)，并在原交接追加带时间的勘误。
- 原始 driver state 已记录 `.147` / instance `272`、源选项 authored1/native0、instance 消失及 revision `4 → 5`；
  之后等待 `.comp.1` 超时。因此本周下文历史记录中的“source event wait”应按此勘误理解，
  不再从“源事件未恢复”重复排查；AF5 authored42/native41 与业务 terminal 仍未取得。
- 评审提出七项改进及实施顺序；本轮只完成文档，没有修复推进逻辑、修改验收门或产生新 live artifact。
  P1 净增 0，仍为 `1/9`；P2 保持 LOCKED。测试替身与生产转换的差异只定位了覆盖缺口，根因仍待后续归因。
- 原始证据、SHA、验证范围、当前早会对照与提交追溯见 [09-11 日报](../daily/2026-09-11.md)。
  本文档工作包只做 diff、空白和链接检查；提交信息为 `Document T0 methodology review and AF5 handoff correction`，
  精确 SHA 以评审正文首次引入的 Git commit 为准；普通 push 结果以远端包含该提交为准。
- 任务总线文档中的 `D:\workspace` 已安装脚本路径本机不存在，本轮使用仓库现有 `tools/codex_task_bus.py`
  的默认总线路径完成登记与轮询；未扩展成安装或工具修复任务。

## 本周目标

- 天朝二期最高优先级：闭合 canonical manager seed，按 owner-approved 重基线完成完整 L0、canonical 产品路径、代表性高风险
  实机切片和真实遇到 RED 的闭环，再推进 `8/8` 真实素材与最终双片；不以 `361/361 live` 或 `626/626 sampled` 为完成门。
- G2 与 open_kaishek 仅在不占用天朝二期 CK3 串行槽时并行推进。

## 周计划承诺滚动对照

| # | 周计划承诺 | 当前结果 | 证据 / 未完成原因 | 是否顺延 |
|---:|---|---|---|---|
| W1 | canonical manager seed | **完成** | R159 clean seed GREEN；manager/player `32904`、subject `26347`，candidate/save/cleanup 均有 hash-bound artifact | 否 |
| W2 | canonical 产品路径与代表性高风险验收 | **进行中** | 历史 R248–R255 的 startup/harness RED 原样保留；后续已恢复到 product-only 长时间线。当前完成门是完整 L0、B1–B8 / stage 9–11 / 四类业务后置条件 / save-restore / cleanup 与代表性高风险 live slices；只闭环实机真实遇到的 RED | 不顺延到逐项 `361/626` 刷数；canonical gate 未闭合则顺延 |
| W3 | `8/8` 真实 clean footage | **T0-P2 `LOCKED` / source `3/4`** | hard gate 要求 T0-P1 完成后才制作视频；promotion、projects/metrics 与 incidents/operations 三项真实 source checkpoint 已完成，尚缺 cross-cycle/endgame；footage 仍为 `0/8` | 依赖 T0-P1/W2，并先闭合 source `4/4` |
| W4 | 最终双宣传片 | **T0-P2 `LOCKED`** | 工具链 prep 未越过 preflight；未制作、审阅、导出或发布任何媒体，最终 MP4 `0/2` | 依赖 W3 与 hard gate 解锁 |
| W5 | 简中文案 live closure | **进行中** | 静态复审未修项为 0；动态 scope、换行/截断及实机呈现仍待 canonical/capture 与代表性 UI 会话 | 本周继续 |
| W6 | G2 当前迭代与 open_kaishek 同步 | **live attempted / startup-blocked** | T1 已增加完整 settings + warm shadercache、固定 WarID、surrender 前 durable checkpoint，并据 r2 实证修复 target identity 的 CIM→Toolhelp fallback；r3 最终在 observer 前命中与 T0 相同的 `ck3+0x1DABD89`，无 mutation。T2 `80fb3a4f...` 仍为 `NO-CODE-CHANGE` | `GEN-034` 待启动环境解除；公共 API/架构变化时再同步 |
| W7 | 证据与报告制度 | **进行中** | 9 月 7 日日报持续记录 R151–R253 的 exact RED/GREEN、retained reconnect、startup failure 与 readiness 边界 | 本周继续 |

2026-09-09 owner-approved rebaseline：本周此前使用的“完整迁移树/full-tree”仍作为历史 run 名称和路径事实保留，但不再表示
逐场景或逐 definition 的穷举门禁。`strict 4/361`、`definitions 106/626` 及剩余数只作为非阻塞 backlog 度量；T0-P1 的权威
完成证据改为完整 L0、canonical 产品路径、代表性高风险实机切片和真实遇到 RED 的最小闭环。自然遇到的原版事件分析还必须
转化为 CK3 自动玩家与其他 mod 可共同消费的 shared registry assets，而不是只留在天朝 runner 私有合同中。

本轮 shared registry / MCP / production cutover 已达到首个 **`production-live primitive`**：registry
默认 157 条，MCP 真实 `list/call` 已接通；production runtime 已接入 296 条记录，17 个 literal 与 31 个
wrapper 已切换。vanilla migration 全集在 normal / `-O` 下各为 `60 passed, 271 subtests`，checkpoint runner `90/90` 均为 GREEN；旧消费者
复跑也在 normal / `-O` 下各为 `160 passed, 1 skipped, 61 subtests`。R372 已在同 PID/session 对 instance `668` 的 `TGP0160`
完成真实 drain/advance，证明 registry-backed production-live 首例；该单例不外推为全部 registry breadth，也不增加 T0 百分比。
T2 `open_kaishek` 初始能力提交 `6b38d9c` 后，BOM source hash 重绑提交
`edcd1ba` 已推送，API 语义不变；父仓 pin verifier normal / `-O` GREEN。

## 09 月 07 日：R159 manager seed GREEN 与 promotion 启动前 RED

- R159 冻结 `8a0482c`；产品树 SHA-256 仍为
  `196e557e06ffa09ef488c819168f16237f5466089ab0d8087ac3f977398e4205`，preflight GREEN。
- manager seed capture GREEN：manager/player `32904`、subject `26347`、默认 5 速、MCP-only gameplay；candidate contract
  SHA-256 `bad92e689fedf460f5e194e55ae1d56be881d0d83ac091113593150182131033`，immutable save SHA-256
  `cb08fce64ced7f3540b68c067aeb6c81bd740a03b92f6fd416cf03c6d0a669ec`。
- runner report SHA-256 `0247fc2130e02b5e45c5099b9ff8b39acc1f62964ca0d74d87670689fdcc5e4f`；cleanup receipt
  SHA-256 `3030ed9d87f3bf0399492673f95fcf065859419015abc46ecf837903132e2928`。final PID `34736` 已回收、
  `restart_count=0`，最终 CK3 inventory 为空。
- 该 seed 只用于 player-manager/promotion 上游，不满足 B3 的两层 AI 管理关系，也没有验收 scoreboard；W1 GREEN 不外推为
  B3、scoreboard 或 full-tree GREEN。
- 紧随其后的 promotion preflight 因 candidate 新增 `domain_query_matrix` 与 runner 旧 root-key 白名单不一致而启动前 RED；
  validator 已修。本次没有启动 CK3，下一门槛是 preflight 复验后再进入 promotion live。

## 09 月 07 日：R160 promotion action-dispatch RED

- R160 冻结 `bf314be`；source ZIP SHA-256
  `3a31b71c537588da5ffdc6fb967236fb008211d7828235284ea6976addf694a0`。frozen/current runner 字节一致，
  SHA-256 均为 `9a4a7ccd4db4b1d9bcd9a5979435c662d52c24ffb52255ba2f15bda181584457`。
- frozen path 的 no-launch preflight 因 ignored `Crusader Kings III` junction 缺失而在启动前 RED；字节一致的 current runner
  preflight GREEN 后才进入 live，未以代码差异绕过门禁。
- PID `126732` 的健康会话当前 retained。真实 progress query 证明 review eligible 且 B1/Central/PP 全 inactive；review action
  却返回 `accepted=false / status=unavailable / action_dispatch_rejected`，且
  `production_capability_advertised=false`。未进入 B1、未捕获 `.147`、未修改产品状态；ACK 未冒充业务后置。
- 该轮是 native action-dispatch/harness RED。下一门槛是最小修复 dispatch 后，继续用 progress/event/postcondition 验证 promotion。

## 09 月 07 日：dispatch 根因修复与 exact full-tree gate

- retained live 内存证明共享 dispatcher 把 `third+0x3D0` 的 owner-lookup host 错当成 action dispatch context，导致
  `target_context != context` 并在 binder 前拒绝；hidden/disabled、vtable、callback 与 modal admission 均通过。
- `01a1251` 已最小修复 context 绑定并增加真实双对象布局及 review-now wrapper 回归；fresh promotion-enabled DLL
  SHA-256 为 `16fa6143130b1ac8eb70b4d6b0458f07ece3615121b319b9c52b0f7d360dffe9`。该 DLL 尚待下一次受管理重启实机互证。
- `f3b3ecf` 把 full-tree 末端无条件 RED 替换为 exact completion gate：七项 gameplay action、四项 observation、
  restore consistency 与逐项 provider product evidence 必须全部闭合；重复项、未知项、ACK 替代或 completed-label 伪造均 RED。
  focused normal/`-O` 与完整 promo contract normal/`-O` 均 GREEN，但静态门禁修复不等于 full-tree live GREEN。

## 09 月 07 日：R193–R198 AF5 修复后的同 PID 推进

- `f9580ca0ae94076dc4732521a976af12d27c6d08` 对 AF5 路线 1/2 增加 M300 真实可执行门槛，保留资源无关路线 3，并把
  exact live 选择固定为终止路线 authored/native `42/41`。生成器 `--check`、35 个 compensation runtime tests 与 72 个
  promotion-source checkpoint runner tests 均 GREEN；compensation effect 家族保持 29 个用途分片、每文件 1–9 个顶层 effect，
  未超过 `1–10` 目标或硬上限 `20`。
- R193 使用新的 1,031 文件 `phase2-full-release-r193-f9580ca` 产品投影，source/product tree SHA-256 为
  `3adf4297df0699f903d152561d2a9b5231d7934e15bc2255dbb88d0541161bcd`，fresh launch PID `44264`。R194–R198 均 reattach
  同一 retained PID，`launch_performed=false`、`restart_performed=false`、`restart_count=0`，默认 5 速；所有 GREEN drain
  绑定 exact event identity 和 event-instance-advanced postcondition，ACK 不作状态证据。
- R193–R196 依次补出 nickname、debate host-win、spymaster 与 physician-free health 的精确边界，并推进 through AE1–AF4；
  R197 对 `health.1001` authored/native `1/0` GREEN 后停在 `health.3001`。R198 已将 `health.3001` 精确 GREEN，随后在
  `health.3101` strict fail-closed。最后观测为 Central active、B1/PP inactive，blocking diagnostics `0`，健康暂停会话继续 retained。
- 合同按用途拆分：nickname `ce0476d`、debate variant `b894723`、spymaster `bfa32c2`、physician-free health `bdeec72`、
  physician recruitment health `3e1a25c`；nickname/spymaster/health 各自进入独立用途合同文件，而非继续堆入主 runner。
- R196、R197、R198 report SHA-256 分别为
  `f53b1015fa39ff01f804340ebb696a269aab53483e8ede6758dce8588dafb675`、
  `ceddae32788abe73a6326b499990025b849f0d36fdaa191d45fd09d2fb883e7e`、
  `d1c3418b53bc038255615205d6a75d082f052861c42c6dc61e6ece2a433eab26`。这些 artifact 只证明已列出的事件与推进状态。
  R196 仅到 AF4，R197/R198 被 health 事件链截住，尚未重抵 AF5；故 `f9580ca` 仍为 `static-ready-live-pending`，
  不得提升为 AF5、promotion 或 full-tree GREEN。
- G2 `ed26ac35349b0557d1b817c5dda3afed1aa439f0` 新增独立 `surrender_execution_readiness` 投影，
  `ca677d3c01f7f7c56dc15a4dd5af358d53a97110` 为 hash-bound file intake 增加带 aggregate session binding
  的 v2 manifest；focused normal/`-O` 回归 GREEN，但 action/postcondition readiness 与 literal 仍保持 false/null，不构成
  production/action readiness。open_kaishek 对两项增量均审计为 `NO-CODE-CHANGE`，29 项 focused checks 与 `GREEN_STATIC`
  通过；该结果不外推为 G2 live readiness。

## 09 月 07 日：R199–R200 health 链继续推进

- `8e51a0b3770154a68a4b1066ae8187db4d4bcdb9` 为 `health.3101` 加入 exact safe-treatment 合同，并把 health 合同继续按用途
  拆为 aging 4 个与 disease/treatment 7 个；后者加入 `health.3103` 后增至 8 个，两个文件始终保持在 `1–10` 目标内。R199 在 PID `44264` 上把 safe route 验收为
  GREEN，随后于新出现的 `health.3103` strict fail-closed。
- `5be0bc4702e810a69b678d247cc79df8a344281c` 把 `health.3103` 的单选结果确认补入 disease/treatment 分片。R200 随后依次
  精确消化 `health.3103`、`zg361p2c.2`、`zg361.40` 与 `spymaster_task.0359`；最后观测为 B1 active、Central/PP inactive，
  blocking diagnostics `0`。接着暴露无 physician 的 `health.1101` scope drift，runner 未选择按钮并按 harness-contract RED 停止。
- R199/R200 均通过 retained reconnect 复用 PID `44264`，没有 launch/restart，`restart_count=0`，默认 5 速。
  report SHA-256 分别为 `26f144ffc3ff1c0f007fb1811c67b22bae12c21158bc4ae268cee7c6eb06b2db` 与
  `29c706f3656ab1b9617093932e5f60a7b953727ef68aebda549972a38931fb91`。
- 随后的 connection generation `11` 只读 query 将 `health.1101` 冻结为恰含 `sick_character` 与 `disease_type`、无 physician
  scope 的 exact frame；没有执行选择或修改产品状态。R200 仍未重抵 AF5，因此上述 GREEN 不提升 AF5、promotion 或 full-tree readiness。

## 09 月 07 日：R201–R204 从 B1 转入 Central

- R201 connection generation `12` 将无 physician `health.1101` 与 `spymaster_task.0359` instance `91` 精确消化，
  随后第二个 `.0359` instance `92` 超出原单次上限。generation `13` 只读 query 冻结该 exact instance、未点击；
  `1bd045deca5bb2dd2c78fd020079130f0ccf93e0` 仅将已证明的同类 discovery 上限扩为两次。
- R202 generation `14` 将 instance `92` GREEN 后停在 `befriend_outcome.0002` failure shape；
  `a4983bf5d71cfb3c680eb67198887fb0813cd00c` 补入该精确变体，并把 befriend 合同拆入仅 1 个合同的用途文件。
- R203 generation `15` 连续完成 6 个 exact drains，产品状态从 B1 active 转为 Central active，随后在 `zg361.1` 第七套
  46-scope 年度总结 shape 上 fail-closed。`07b06eb76b4f2aba7dedec4ccb9e5a19369f73f9` 将 annual-summary 拆入仅 1 个合同的
  独立用途文件，并从主 entry 删除 268 行；`e380ddd234c285a2d75e94f2b5e0e9c009db55d6` 清理分片 EOF。
- R204 generation `16` 将该 `zg361.1` 精确 GREEN，随后在 Central active 状态遇到 3 个可见选项的产品事件 `zg361.5`。
  runner 未点击；源码审计计划选择最温和的 authored/native `3/2`，并把 `.5/.6` 拆成只有 2 个合同的专用文件。
- R201–R204 全部 retained reconnect 到 PID `44264`，默认 5 速、无 launch/restart、`restart_count=0`、blocking diagnostics `0`。
  report SHA-256 依次为 `1cd356c6af126e305e680d4f5ca27f655a3b0167b6b643e96c9f511fec210a09`、
  `4732ea95e5c446537fc955432d5cf46704667c0e0dd76eeaa864a0bfd0fa08db`、
  `417785701a602c64f5e5e0bbd6c8614307a8d48ead7219db3d0d78e66ebfa7dc`、
  `24b5aa92827ec20fb8f723b2456d511ecdb62f40ab718b394b22e36aacbdefa6`。
  上述证据仍未重抵 AF5，不能提升 AF5、promotion 或 full-tree readiness。

## 09 月 07 日：R205 Central/PP 28-drain 推进

- `4807587cad2b808832106bb11d7b3d2ff2abf883` 已推送 `zg361.5/.6` elimination 专用分片；文件仅含 2 个合同，
  `zg361.5` 绑定最温和的 authored/native `3/2`，未回填主 entry。
- R205 connection generation `17` 在 retained PID `44264` 上先将 `zg361.5` GREEN，再完成 26 个 Central/vanilla drains，
  最后将 `zg361pp.9100` GREEN，总计 28 个 exact drains。26 项依次覆盖 `zg361ch.950`、`zg361.30`、`debate_event.5110`，及
  `zg361ch.21/.24/.25/.901/.93/.902/.101/.103/.104/.105/.903/.107/.108/.111/.112/.113/.904/.114/.115/.116/.119/.120/.905`。
- 首个 RED 为 `zg361pp.149`：`date_raw=53186856`、event instance `128`、48 scopes（17 character + 31 value）、
  可见 native options `0/1/2`。runner 未点击；下一项施工是专用 PP compensation exact contract。
- 最后状态为 Central/PP active、B1 inactive，blocking diagnostics `0`。本轮无 launch/restart，`restart_count=0`、默认 5 速；
  report `Z:\p2r205promo_resume\report.json` SHA-256 为
  `201bbb139560d08084b952b1db79cbcbe99d84bf095f517777317d2f4371929d`。
  本轮没有重新抵达 AF5，故不提升 AF5、promotion 或 full-tree readiness。

## 09 月 07 日：R206 PP bargaining 继续推进

- `ffa7152ff9c70b4c58a801798a6f24e296c07f12` 已推送 `.149` 的 PP bargaining 专用合同。R206 connection generation `18`
  在 PID `44264` 上将 `zg361pp.149` authored/native `3/2` 精确 GREEN。
- 随后 `zg361pp.150` 于 `date_raw=53186880`、event instance `129` 出现；其 48 scopes（17 character + 31 value）、
  alias/type 集与 `.149` 完全一致，可见 native options 同为 `0/1/2`。runner 未点击；当前正并入同一个最终仅含 2 个合同的
  PP bargaining 文件，计划继续选择无资源消耗的 authored/native `3/2`。
- R206 为 retained reconnect：PID `44264`、默认 5 速、无 launch/restart、`restart_count=0`、blocking diagnostics `0`。
  `Z:\p2r206promo_resume\report.json` SHA-256 为
  `5da38b3f05d92722bccb685d538f4f02d9c5fc0468f162fa4b7f33cfd5bc651d`。
  `.150`、AF5 terminal route 与 full-tree 仍 pending。

## 09 月 07 日：R207 PP bargaining 继续推进

- `4c7476f` 已提交并推送 `.150` 的 exact PP bargaining 合同。R207 connection generation `19` 继续连接 retained
  PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、`restart_count=0`，blocking diagnostics `0`。
- `zg361pp.150` instance `129` 在 `date_raw=53186880` 的 exact frame 含 48 scopes（17 character + 31 value）及
  native options `0/1/2`。R207 选择 authored/native `3/2`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361pp.151` instance `130` 在 `date_raw=53186904` 出现，exact saved-scope 集增至 53 项；新增 subject prompt
  恰带入 2 个 character scopes 与 3 个 value scopes，native options 为 `0/1/2`。runner 未点击并按 strict harness gate
  fail-closed；这是 harness-contract RED，不是产品 RED。
- `Z:\p2r207promo_resume\report.json` SHA-256 为
  `BC1EB388686709C0E9FC9CB2438D77423BCDDA9E6118E5073268EE0C3515C80C`。R207 只闭合 `.150`；`.151`、AF5 terminal route
  与 full-tree 仍 pending，未提升 promotion/per-ID readiness。

## 09 月 07 日：R208 PP bargaining 继续推进

- `7cbb3a4` 已提交并推送 `.151` 的 exact PP bargaining 合同。R208 connection generation `20` 继续连接 retained
  PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、`restart_count=0`，blocking diagnostics `0`。
- `zg361pp.151` instance `130` 在 `date_raw=53186904` 的 exact frame 含 53 scopes（19 character + 34 value）及
  native options `0/1/2`。R208 选择 authored/native `3/2`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361pp.153` instance `131` 在 `date_raw=53186928` 出现，exact 53-scope alias/type shape 与 `.151` 相同，
  native options 为 `0/1/2`。runner 未点击并按 strict harness gate fail-closed；这是 harness-contract RED，不是产品 RED。
- `Z:\p2r208promo_resume\report.json` SHA-256 为
  `F9B38744B7DF03661BE6672059664F0428B5B64C852AFC7E4506311A64455748`。R208 只闭合 `.151`；`.153`、AF5 terminal route
  与 full-tree 仍 pending，未提升 promotion/per-ID readiness。

## 09 月 07 日：R209 PP bargaining 继续推进

- `f5204ad` 已提交并推送 `.153` 的 exact PP bargaining 合同。R209 connection generation `21` 继续连接 retained
  PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、`restart_count=0`，blocking diagnostics `0`。
- `zg361pp.153` instance `131` 在 `date_raw=53186928` 的 exact frame 含 53 scopes（19 character + 34 value）及
  native options `0/1/2`。R209 选择 authored/native `3/2`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361pp.155` instance `132` 在 `date_raw=53186952` 出现，exact 53-scope alias/type shape 与 `.153` 相同，
  native options 为 `0/1/2`。runner 未点击并按 strict harness gate fail-closed；这是 harness-contract RED，不是产品 RED。
- `Z:\p2r209promo_resume\report.json` SHA-256 为
  `97E6C901AE185850BAE0D5140883F8C30073F8D8A5DBF06DE181665A4FF6CEE5`。R209 只闭合 `.153`；`.155`、AF5 terminal route
  与 full-tree 仍 pending，未提升 promotion/per-ID readiness。

## 09 月 07 日：R210 PP public feedback 继续推进

- pushed commit `3a5b624`（`Handle PP public feedback prompt`）已加入 `.155` 的 exact contract。R210 connection
  generation `22` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361pp.155` instance `132` 在 `date_raw=53186952` 的 exact frame 含 53 scopes（19 character + 34 value）及
  native options `0/1/2`。R210 选择 authored/native `3/2`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361pp.9001` instance `133` 在 `date_raw=53186976` 出现，exact frame 含 54 scopes
  （20 character + 34 value）且只有 native option `0`。runner 未点击并按 strict harness gate fail-closed；
  这是 PP completion prompt 的 harness-contract RED，不是产品 RED。
- `Z:\p2r210promo_resume\report.json` SHA-256 为
  `96433412CC6B9D055CEC114C21110C261EE0347E5FE06BE68144AA7044EC9DBD`。R210 只闭合 `.155`；PP completion prompt、
  AF5 terminal route 与 full-tree 仍 pending，未提升 readiness。

## 09 月 07 日：T1/T2 非 CK3 并行增量

### T1：G2 provenance-only contract refresh

- `1f5e310` 已提交并推送。11 份 G2 contract/fixture JSON 仅更新 source hash/provenance DAG；未修改生产行为、
  readiness、authorization 或 command。
- Raiktor normal `157/157` + `-O 157/157`、G2 normal `176/176` + `-O 176/176`、5-module normal
  `29/29` + `-O 29/29`，总矩阵 `724/724`；targeted `57/57`，JSON parse 与 diff-check GREEN。本工作包未启动 CK3。
- `GEN-034` 仍需 source-specific lifecycle live；T0 retained PID `44264` 占用 CK3 串行槽期间按门禁暂缓。
  campaign dominance、owner-approved utility、white-peace exact terms 与 typed postcondition 均不因本次离线对齐而升级。

### T2：open_kaishek companion compatibility

- `open_kaishek` commit `80fb3a4f5816ea4fd2c4d2b2a81db176078831dd` 与父仓 pin commit `661226e` 均已推送。
  open metadata、Maven `156/156`、domain `8/8`、CLI/corpus/compat verifier GREEN；父仓 compatibility units
  normal `16/16` + `-O 16/16`。
- 相对 `804ef3d`，公共 `game.command`、`game.contract`、8 个字段与 5 条 invariant 均无变化；本轮是
  `NO-CODE-CHANGE` companion delta，不提升 runtime/live readiness。后续仅在 T0/T1 出现公共 API 或架构变化时再次同步。

## 09 月 07 日：R211 PP queue completion card 闭合

- pushed commit `f857b23`（`Handle PP feedback completion`）已加入 `.9001` 的 exact contract。R211 connection
  generation `23` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361pp.9001` instance `133` 在 `date_raw=53186976` 的 exact frame 含 54 scopes（20 character + 34 value），
  且只有 authored/native `1/0`。R211 选择该唯一选项，并以独立的 `event_instance_advanced` 后置验收为 GREEN；
  PP queue completion card 至此闭合。
- 下一帧 `zg361cp.9050` instance `134` 在 `date_raw=53187000` 出现，exact frame 含 50 scopes
  （21 character + 29 value）和 native options `0/1/2/3`。runner 未点击并按 strict harness gate fail-closed；
  这是 harness-contract RED，不是产品 RED。
- `Z:\p2r211promo_resume\report.json` SHA-256 为
  `E048061FD3200B19E9EA3064593D0DDC794F8DF13F2750898EAC0EF95EEC478F`。完整 promotion/full-tree、AF5 terminal route 3、
  source registry 与最终视频仍 pending；R211 不提升最终 readiness。

## 09 月 07 日：R212 credit project portfolio mode 继续推进

- pushed commit `f990808`（`Handle credit project portfolio mode`）已加入 `.9050` 的 exact contract。R212 connection
  generation `24` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.9050` instance `134` 在 `date_raw=53187000` 的 exact frame 含 50 scopes（21 character + 29 value）及
  native options `0/1/2/3`。R212 选择 itemized full-tree authored/native `4/3`，并以独立的
  `event_instance_advanced` 后置验收为 GREEN。该模式保留完整 27 项项目树，但不等于完整 full-tree acceptance。
- 下一帧 `zg361cp.30` instance `135` 在 `date_raw=53187024` 出现，exact frame 同为 50 scopes
  （21 character + 29 value），native options 为 `0/1/2`。runner 未点击并按 strict harness gate fail-closed；
  这是 harness-contract RED，不是产品 RED。
- `Z:\p2r212promo_resume\report.json` SHA-256 为
  `1207C4F0D4A9579904C6F510C76267817423320285AB7966A5820DB40D14FB05`。完整 full-tree、AF5 terminal route 3、
  source registry 与最终视频仍 pending；R212 不提升最终 readiness。

## 09 月 07 日：T1 G2 source-specific lifecycle no-launch preflight

- T1 commit `bdf5324` 对应 preflight 状态为 `READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE`。artifact：
  `Z:\ck3_mod_rewrite_process_assets\zg361\g2-source-live-adapter-static-20260907\preflight-master-5ffbc1d-r4.json`，
  `7,067` bytes，SHA-256 `6BB2090ADEB83D99906F5D88682CF602A69BD48E14EAA12F98C4F2592EA2CCC6`；manifest SHA-256
  `D888FA2DF6839C5A424D9947EE7BCE746ABF930E45B2384169DD23488ABB5FB0`。
- focused normal `20/20` + `-O 20/20` GREEN。CK3 before/after inventory 相同；本轮未启动 CK3，也未对 T0 retained
  PID `44264` 执行连接、暂停、恢复或输入。
- typed surrender/private receipt 已具备，但没有额外离线实现能解除 campaign dominance 与 owner-approved utility 缺口。
  下一项有效证据必须等 T0 释放独占 CK3 槽后进入 source-specific lifecycle live；唯一显式 mutation 为
  `surrender-war-50331699`，preflight READY 不提升 `GEN-034` live readiness。
- 父仓推进至 `e4398bf` 后再次执行 committed `--verify-only` preflight，状态仍为
  `READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE`，13 项依赖全部匹配，CK3 inventory 前后均为空，未执行 surrender。
  新 artifact 为 `Z:\ck3_mod_rewrite_process_assets\zg361\g2-source-live-adapter-static-20260907\preflight-master-e4398bf-r1.json`，
  `5,111` bytes，SHA-256 `1347C3C426DA8F507561FB777FE1D916E891A2079CFB65A7C48E89EC0C2F2198`；
  `1f5e310..e4398bf` 无 G2 public code、native API/ABI 或 research implementation 漂移，故无需再次修改 manifest 或同步 open_kaishek。

## 09 月 07 日：R213 credit project resource race 继续推进

- pushed commit `a5ba41f`（`Handle credit project resource race`）已加入 `.30` 的 exact contract。R213 connection
  generation `25` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.30` instance `135` 在 `date_raw=53187024` 的 exact frame 含 50 scopes（21 character + 29 value）及
  native options `0/1/2`。R213 选择 subject-led reserve `40h` 的 authored/native `1/0`，保留 itemized 项目树，
  并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.26` instance `136` 在 `date_raw=53187048` 出现，exact frame 同为 50 scopes
  （21 character + 29 value），native options 为 `0/1/2`。runner 未点击并按 strict harness gate fail-closed；
  这是 harness-contract RED，不是产品 RED。
- `Z:\p2r213promo_resume\report.json` SHA-256 为
  `8A9AA71F378AD4A2BF8A4F4C9DA8755A28A7A3744836B38ECE6DF3FD172B66A5`。完整 full-tree、AF5 terminal route 3、
  source registry 与最终视频仍 pending；R213 不提升最终 readiness。

## 09 月 07 日：R214 credit project visibility ledger 继续推进

- pushed commit `ca2bed3`（`Handle credit project visibility ledger`）已加入 `.26` 的 exact contract。R214 connection
  generation `26` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.26` instance `136` 在 `date_raw=53187048` 的 exact frame 含 50 scopes（21 character + 29 value）及
  native options `0/1/2`。R214 选择 authored/native `1/0`，并以独立的 `event_instance_advanced` 后置验收为 GREEN；
  itemized 项目树当前仍有 25 张可见卡。
- 下一帧 `zg361cp.27` instance `137` 在 `date_raw=53187072` 出现，exact 50-scope alias/type shape 与 `.26` 相同，
  native options 为 `0/1/2`。runner 未点击并按 strict harness gate fail-closed；这是 harness-contract RED，不是产品 RED。
- `Z:\p2r214promo_resume\report.json` SHA-256 为
  `E3AC5A9855D83C2058C1BDE2254FFBFFC57B94EE2E17F1F35504E7EA249A234B`。E→I→J→R、完整 full-tree、AF5 terminal route 3、
  source registry 与最终视频仍 pending；R214 不提升最终 readiness。

## 09 月 07 日：R215 credit project contribution shares 继续推进

- pushed commit `6e85772`（`Handle credit project contribution shares`）已加入 `.27` 的 exact contract。R215 connection
  generation `27` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.27` instance `137` 在 `date_raw=53187072` 的 exact frame 含 50 scopes（21 character + 29 value）及
  native options `0/1/2`。R215 选择 authored/native `1/0`，contribution shares 为 `7000/2000/1000`、总计 `10000`，
  并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- hidden `.9231` 未暴露 UI；下一可见帧 `zg361cp.31` instance `138` 在 `date_raw=53187096` 出现，
  exact 50-scope alias/type shape 与 `.27` 相同，native options 为 `0/1/2`。runner 未点击并按 strict harness gate
  fail-closed；这是 harness-contract RED，不是产品 RED。
- `Z:\p2r215promo_resume\report.json` SHA-256 为
  `8CFCF8EB5702CD63B886C907F017D7C2C00E155BDCE6B8A76CDFEC5BECCBD138`。完整 full-tree、AF5 terminal route 3、
  source registry 与最终视频仍 pending；R215 不提升最终 readiness。

## 09 月 07 日：R216 credit project capacity grant 继续推进

- pushed commit `a672dc5`（`Handle credit project capacity grant`）已加入 `.31` 的 exact contract。R216 connection
  generation `28` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.31` instance `138` 在 `date_raw=53187096` 的 exact frame 含 50 scopes（21 character + 29 value）及
  native options `0/1/2`。R216 选择 authored/native `1/0`，取得 `grant=20 / spend=0 / balance=20 / visibility=5`，
  E 从 `2` 推进到 `3`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.28` instance `139` 在 `date_raw=53187120` 出现，exact 50-scope alias/type shape 与 `.31` 相同，
  native options 为 `0/1/2`。runner 未点击并按 strict harness gate fail-closed；这是 harness-contract RED，不是产品 RED。
- `Z:\p2r216promo_resume\report.json` SHA-256 为
  `17AA7DCFCAECEDDA857A0697706E060DE20F3BAB9B8324315B770B1F320BF232`。完整 full-tree、AF5 terminal route 3、
  source registry 与最终视频仍 pending；R216 不提升最终 readiness。

## 09 月 07 日：R217 credit project attribution audit 继续推进

- pushed commit `eff54a2`（`Handle credit project attribution audit`）已加入 `.28` 的 exact contract。R217 connection
  generation `29` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.28` instance `139` 在 `date_raw=53187120` 的 exact frame 含 50 scopes（21 character + 29 value）及
  native options `0/1/2`。R217 选择 authored/native `2/1`；审计回拨后 contribution shares 仍为
  `7000/2000/1000`，E 从 `3` 推进到 `4`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.29` instance `140` 在 `date_raw=53187144` 出现，exact 50-scope alias/type shape 与 `.28` 相同，
  native options 为 `0/1/2`。runner 未点击并按 strict harness gate fail-closed；这是 harness-contract RED，不是产品 RED。
- `Z:\p2r217promo_resume\report.json` SHA-256 为
  `C830D145A332639AFA2A23041AEB19928D49A21DB1BD2722B1B78851E9A9B08D`。完整 full-tree、AF5 terminal route 3、
  source registry 与最终视频仍 pending；R217 不提升最终 readiness。

## 09 月 07 日：R218 credit project value audit，E domain live 完成

- pushed commit `42afe5f`（`Handle credit project value audit`）已加入 `.29` 的 exact contract。R218 connection
  generation `30` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.29` instance `140` 在 `date_raw=53187144` 的 exact frame 含 50 scopes（21 character + 29 value）及
  native options `0/1/2`。R218 选择 authored/native `1/0`，取得 `gain=10 / cost=0`，E 从 `4` 推进到 `5` 且
  `active=0`，并以独立的 `event_instance_advanced` 后置验收为 GREEN；E domain 完成本条 live 路径。
- hidden `.9001/.9261` 未暴露 UI；下一可见帧为 I-domain `zg361cp.61` instance `141`，`date_raw=53187168`，
  exact 58 scopes（27 character + 31 value），native options `0/1/2`。runner 未点击并按 strict harness gate
  fail-closed；这是 harness-contract RED，不是产品 RED。
- `Z:\p2r218promo_resume\report.json` SHA-256 为
  `8B17B5C967690DD066783150EB4C5E110BD0BB0D90839A1081E227ADBC3F86A9`。I/J/R、完整 full-tree、AF5 terminal route 3、
  source registry 与最终视频仍 pending；E domain live 完成不提升最终 full-tree readiness。

## 09 月 07 日：R219 credit project reporting policy，I-domain 首卡 GREEN

- pushed commit `2987950`（`Handle credit project reporting policy`）已加入 `.61` 的 exact contract。R219 connection
  generation `31` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.61` instance `141` 在 `date_raw=53187168` 的 exact frame 含 58 scopes（27 character + 31 value）及
  native options `0/1/2`。R219 选择 authored/native `1/0`，得到 reporting policy `1/1h`，并以独立的
  `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.54` instance `142` 在 `date_raw=53187192` 出现，保持相同的 exact 58-scope split。policy `1`
  正确隐藏 native index `1`，实机仅渲染 native indices `0/2`；runner 未点击并按 strict harness gate fail-closed。
  这是条件渲染下的 harness-contract RED，不是产品 RED。
- `Z:\p2r219promo_resume\report.json` SHA-256 为
  `5C22FAB4F4472B704BCAFA33B500424B66396AAEAEE4F782C2B343B220F83B0C`。I-domain 其余路径、J/R、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R219 不提升最终 readiness。

## 09 月 07 日：R220 credit project report creation，I `1→2`

- pushed commit `2cf9b2e`（`Handle credit project report creation`）已加入 `.54` 的 exact contract。R220 connection
  generation `32` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.54` instance `142` 在 `date_raw=53187192` 的 exact frame 含 58 scopes（27 character + 31 value）。
  完整 snapshot 含 3 个 option，policy 条件下 visible `2`、native indices `0,2`；R220 选择 authored-visible/native
  `1/0`，创建 `1h` report、capacity `-1`、I `1→2`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
  hidden `.9256` 未暴露 UI。
- 下一帧 `zg361cp.56` instance `143` 在 `date_raw=53187216` 出现，保持相同的 exact 58-scope split，native options
  `0/1/2`、snapshot `3`；runner 未点击并按 strict harness gate fail-closed。这是 harness-contract RED，不是产品 RED。
- `Z:\p2r220promo_resume\report.json` SHA-256 为
  `1DF19E9E4446C3B5650C2834A011C29A14D157E3900607C7F0FBD26B0F12AE41`。I-domain 其余路径、J/R、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R220 不提升最终 readiness。

## 09 月 07 日：R221 credit project forwarding，I `2→3`

- pushed commit `83c5c52`（`Handle credit project forwarding`）已加入 `.56` 的 exact contract。R221 connection
  generation `33` 继续连接 retained PID `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，blocking diagnostics `0`。
- `zg361cp.56` instance `143` 在 `date_raw=53187216` 的 exact frame 含 58 scopes（27 character + 31 value），
  native options `0/1/2`。R221 选择 authored/native `1/0`，`forwarded=1`、股比保持不变、I `2→3`，并以独立的
  `event_instance_advanced` 后置验收为 GREEN；hidden `.9257` 未暴露 UI。
- 下一帧 `zg361cp.57` instance `144` 在 `date_raw=53187240` 出现，保持相同的 exact 58-scope split，native options
  `0/1/2`、snapshot `3`；runner 未点击并按 strict harness gate fail-closed。这是 harness-contract RED，不是产品 RED。
- `Z:\p2r221promo_resume\report.json` SHA-256 为
  `5D0DF4B38BEA4EF3290FFBB4BC45A791E30F17FD4B50F0369BAB4BC5D9085337`。I-domain 其余路径、J/R、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R221 不提升最终 readiness。

## 09 月 07 日：R222 credit project freeze signature

- pushed commit `7037bcf` 已加入 `.57` 的 exact contract。R222 connection generation `34` 继续连接 retained PID
  `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、`restart_count=0`，blocking diagnostics `0`。
- `zg361cp.57` instance `144` 在 `date_raw=53187240` 的 exact frame 含 58 scopes（27 character + 31 value）。
  R222 选择 authored/native `1/0`，以 author=subject 冻结 version/signature `1`，该路径 GREEN；hidden `.9258`
  未暴露 UI。
- 下一帧 `zg361cp.58` instance `145` 在 `date_raw=53187264` 出现，保持同一 exact 58-scope split，native options
  `0/1/2`、snapshot `3`；runner 未点击并按 strict harness gate fail-closed。这是 harness-contract RED，不是产品 RED。
- `Z:\p2r222promo_resume\report.json` SHA-256 为
  `48466E88710E5EBA1ED72E7676673491A1346EAD4136C3422F2552B6164D147F`。R222 作为独立 run 收口。

## 09 月 07 日：R223 credit project direct-root delivery

- pushed commit `1a85ee7` 已加入 `.58` 的 exact contract。R223 connection generation `35` 继续连接 retained PID
  `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、`restart_count=0`，blocking diagnostics `0`。
- `zg361cp.58` instance `145` 在 `date_raw=53187264` 的 exact frame 含 58 scopes（27 character + 31 value）。
  R223 选择 authored/native `1/0`，证明 recipient count `1` / direct root 路径 GREEN。
- 下一帧 `zg361cp.59` instance `146` 在 `date_raw=53187288` 出现，保持同一 exact 58-scope split，native options
  `0/1/2`、snapshot `3`；runner 未点击并按 strict harness gate fail-closed。这是 harness-contract RED，不是产品 RED。
- `Z:\p2r223promo_resume\report.json` SHA-256 为
  `BAA3B4E09AA302FE19F0774B5671953CC13F059E4DA0494F2AAA7CB557742E15`。I-domain 余下路径、J/R、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R223 不提升最终 readiness。

## 09 月 07 日：R224 credit project integrity closure，I `3→4`

- pushed commit `4216f8c` 已加入 `.59` 的 exact contract。R224 connection generation `36` 继续连接 retained PID
  `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、`restart_count=0`，blocking diagnostics `0`。
- `zg361cp.59` instance `146` 在 `date_raw=53187288` 的 exact frame 含 58 scopes（27 character + 31 value）。
  R224 选择 authored/native `1/0`，记录 remaining loss `5`、integrity `+1`、I `3→4`，该路径 GREEN。
- 下一帧 `zg361cp.55` instance `147` 在 `date_raw=53187312` 出现，保持同一 exact 58-scope split，native options
  `0/1/2`；runner 未点击并按 strict harness gate fail-closed。这是 harness-contract RED，不是产品 RED。
- `Z:\p2r224promo_resume\report.json` SHA-256 为
  `FC349FEE10CA361117779EB63E760C939E39B8AE51D2674CE467B208D9B08679`。R224 作为独立 run 收口。

## 09 月 07 日：R225 credit project attention accounting

- pushed commit `a27d9b6` 已加入 `.55` 的 exact contract。R225 connection generation `37` 继续连接 retained PID
  `44264`；`restart_count=0`，blocking diagnostics `0`。
- `zg361cp.55` instance `147` 在 `date_raw=53187312` 的 exact frame 含 58 scopes（27 character + 31 value）。
  R225 选择 authored/native `1/0`，attention free `2→1`、used `0→1`、read `1`、visibility `+5`，该路径 GREEN。
- 下一帧 `zg361cp.60` instance `148` 在 `date_raw=53187336` 出现，保持同一 exact 58-scope split，native options
  `0/1/2`；runner 未点击并按 strict harness gate fail-closed。这是 I-domain 最后一张卡的 harness-contract RED，
  不是产品 RED。
- `Z:\p2r225promo_resume\report.json` SHA-256 为
  `06D83C44967DBE627660330D28764002A882B5C11B669ADC09B0809DF02D038B`。I-domain 最后一张 `.60`、J/R、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R225 不提升最终 readiness。

## 09 月 07 日：R226 credit project I completion，I domain live 完成

- pushed commit `50535bc` 已加入 `.60` 的 exact contract。R226 connection generation `38` 继续连接 retained PID
  `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、`restart_count=0`，blocking diagnostics `0`。
- `zg361cp.60` instance `148` 在 `date_raw=53187336` 的 exact frame 含 58 scopes（27 character + 31 value）。
  R226 选择 authored/native `1/0`，保留 subject `idea_owner` / `theft_upheld=1`，I `4→5 / active=0`，该路径
  GREEN；hidden `.9002/.9263` 未暴露 UI，I domain live 至此完成。
- 下一帧进入 J domain：`zg361cp.63` instance `149` 在 `date_raw=53187360` 出现，exact 66 scopes
  （33 character + 33 value）、native options `0/1/2`；runner 未点击并按 strict harness gate fail-closed。
- `Z:\p2r226promo_resume\report.json` SHA-256 为
  `CF9355E25263FE838C91B700E9DB56C9C2A14263BFE71E367B1025E7523A5896`。R226 作为独立 run 收口。

## 09 月 07 日：R227 credit project J weights，J `1→2`

- pushed commit `54c698a` 已加入 `.63` 的 exact contract。R227 connection generation `39` 继续连接同一 retained PID
  `44264`；`restart_count=0`，blocking diagnostics `0`。
- `zg361cp.63` instance `149` 在 `date_raw=53187360` 的 exact frame 含 66 scopes（33 character + 33 value）。
  R227 选择 authored/native `1/0`，weights `70/30`、total `100`、J `1→2`，该路径 GREEN；hidden `.9262`
  未暴露 UI。
- 下一帧 `zg361cp.62` instance `150` 在 `date_raw=53187384` 出现，保持同一 exact 66-scope split，native options
  `0/1/2`；runner 未点击并按 strict harness gate fail-closed。这是 harness-contract RED，不是产品 RED。
- `Z:\p2r227promo_resume\report.json` SHA-256 为
  `749798B33F765F6AA226C11EC9CEB3D190D970540E020F3E39DC8EEB54B0D310`。I domain live 已完成，但 J-domain 余下路径、
  R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R227 不提升最终 readiness。

## 09 月 07 日：R228 credit project J continuation，`.62` GREEN

- `.62` 静态 exact contract 位于 commit `a7f7bf0`。R228 connection generation `40` 继续连接 retained PID
  `44264`；默认 5 速、`launch_performed=false`、`restart_performed=false`、`restart_count=0`，blocking diagnostics `0`。
- `zg361cp.62` instance `150` 在 `date_raw=53187384` 的 exact frame 含 66 scopes（33 character + 33 value）。
  R228 选择 authored/native `1/0`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.65` instance `151` 在 `date_raw=53187408` 出现，保持同一 exact 66-scope split，native options
  `0/1/2`、snapshot `3`；runner 未点击并按 strict harness gate fail-closed。`.65` exact contract commit `6075e2e`
  已 push，但静态入库不等于本轮 live GREEN。
- `Z:\p2r228promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `4129AD0A359F29CEB05229358D72DB5009D79A88AFE0C4BC887D819B81B82281`。J-domain 余下路径、R、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R228 不提升最终 readiness。

## 09 月 07 日：R229 credit project J continuation，`.65` target GREEN / sentinel RED

- `.65` exact contract 位于 commit `6075e2e`。R229 connection generation `41` 继续连接 retained PID `44264`；
  `launch_performed=false`、`restart_performed=false`、`restart_count=0`，runtime diagnostics GREEN，blocking `0`。
- `zg361cp.65` instance `151` 在 `date_raw=53187408` 的 exact frame 含 66 scopes（33 character + 33 value）。
  R229 选择 authored/native `1/0`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.64` instance `152` 在 `date_raw=53187432` 出现，保持同一 exact 66-scope split，native options
  `0/1/2`、snapshot `3`；R229 当时 `.64` 静态包尚在进行，runner 严格不点击。top-level RED 仅来自当时的
  next-event sentinel 合同缺口，不否定 `.65` 目标 GREEN，也不是产品 runtime RED。
- `.64` route B authored/native `2/1` exact contract 随后由 commit `f1518e4` push；focused normal/`-O` 各
  `12/12`、aggregate normal/`-O` 各 `73/73`、real exact `79`、`validate_static` 与 diff-check 均 GREEN。
  这些是 static closure，R229 未补做点击，`.64` 仍 live-pending。
- `Z:\p2r229promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `4888ADAAACA34C6E0910A159614E9F3373BF5DB3E0193AA44BA1269C102A0E1F`。J-domain 余下路径、R、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R229 不提升最终 readiness。

## 09 月 07 日：R230 credit project J route B，`.64` target GREEN / sentinel RED

- `.64` route B exact contract 位于 commit `f1518e4`。R230 connection generation `42` 继续连接 retained PID
  `44264`；`launch_performed=false`、`restart_performed=false`、`restart_count=0`，runtime diagnostics GREEN，blocking `0`。
- `zg361cp.64` instance `152` 在 `date_raw=53187432` 的 exact frame 含 66 scopes（33 character + 33 value）。
  R230 选择 authored/native `2/1`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.66` instance `153` 在 `date_raw=53187456` 出现，保持同一 exact 66-scope split，native options
  `0/1/2`、snapshot `3`；`.66` contract 尚在施工，runner 严格不点击。top-level RED 仅来自 next-event unknown，
  不否定 `.64` 目标 GREEN，也不是产品 runtime RED。
- `Z:\p2r230promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `132FC018A60A9E22A019DF5D0E672D6F498F31D36FDE8153BA684851CC8607EA`。J-domain 余下路径、R、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R230 不提升最终 readiness。

## 09 月 07 日：R231 credit project J capacity preserve，`.66` target GREEN / secret sentinel RED

- `.66` exact contract 位于 commit `833fd50`；focused normal/`-O` 各 `15/15`、aggregate normal/`-O` 各
  `73/73`、real exact `79`、`validate_static` 与 diff-check 均 GREEN。R231 connection generation `43` 继续连接
  retained PID `44264`；`launch_performed=false`、`restart_performed=false`、`restart_count=0`，runtime GREEN / blocking `0`。
- `zg361cp.66` instance `153` 在 `date_raw=53187456` 的 exact frame 含 66 scopes。R231 选择 authored/native
  `2/1`，保留 capacity 供 R-domain `.132` 使用，该路径 GREEN。
- 下一帧原版 `secrets.0122` instance `155` 在 `date_raw=53187480` 含 13 scopes（9 character + 1 secret +
  2 value + 1 flag），snapshot `7`，仅渲染 native `1/2`；R231 strict no-click，top-level RED 仅来自 sentinel。
- secret exact contract commit `fb2a7e9` 将 Forgive 正确绑定为 authored/native `3/2`，不是 rendered ordinal `2`；
  focused normal/`-O` 各 `3/3`、aggregate normal/`-O` 各 `73/73`、exact `26`、`validate_static` 与 diff-check GREEN。
- `Z:\p2r231promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `C14816EA716EC5AA9BDC3E5598E153CDCFBA7601335390457A6DDC0F1E73C793`。R231 作为独立 run 收口。

## 09 月 07 日：R232 native secret interrupt，Forgive target GREEN / `.67` sentinel RED

- R232 connection generation `44` 继续连接同一 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- 原版 `secrets.0122` instance `155` 在 `date_raw=53187480` 选择 Forgive authored/native `3/2`，目标路径 GREEN；
  不把两个 rendered button 的显示序号误写为 authored option。
- 下一帧回到先前已排队的 `zg361cp.67` instance `154`，同在 `date_raw=53187480`；exact 66 scopes
  （33 character + 33 value），native options `0/1/2`、snapshot `3`。`.67` contract 尚在施工，runner strict no-click；
  top-level RED 仅来自 next-event sentinel，不否定 secret target GREEN。
- `Z:\p2r232promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `73D94802BD063FECBF6E72738E7ED5500640722CF03CF8B7E8020F69A5EDBCB3`。J-domain 余下路径、R、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R232 不提升最终 readiness。

## 09 月 07 日：R233 credit project J continuation，`.67` target GREEN / `.68` sentinel RED

- `.67` exact contract 位于 commit `390aad0`；focused normal/`-O` 各 `18/18`、aggregate normal/`-O` 各
  `73/73`、real exact `79`、`validate_static` 与 diff-check 均 GREEN。R233 connection generation `45` 继续连接
  retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- `zg361cp.67` instance `154` 在 `date_raw=53187480` 的 exact frame 含 66 scopes；R233 选择 authored/native
  `1/0`，目标路径 GREEN。
- 下一帧 `zg361cp.68` instance `156` 在 `date_raw=53187504` 出现，exact 66 scopes、native options `0/1/2`、
  snapshot `3`；runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 `.67` target GREEN。
- `Z:\p2r233promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `191280BED635FECE431C42EB59B87B3F7D5335206035E30F929D286B13BC5683`。R233 作为独立 run 收口。

## 09 月 07 日：R234 credit project J completion，J domain live complete

- `.68` exact contract 位于 commit `d47c077`；focused normal/`-O` 各 `21/21`、aggregate normal/`-O` 各
  `73/73`、real exact `79`、`validate_static` 与 diff-check 均 GREEN。R234 connection generation `46` 继续连接
  同一 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- `zg361cp.68` instance `156` 在 `date_raw=53187504` 选择 preserve history / no PIP authored/native `2/1`，
  目标路径 GREEN；J state `4→5 / active=0`。hidden `.9003/.9331` 未暴露 UI，J domain live complete。
- 下一帧进入 R：`zg361cp.131` instance `157` 在 `date_raw=53187528` 出现，exact 74 scopes
  （39 character + 35 value），native options `0/1/2`、snapshot `3`；`.131` contract 尚在施工，runner strict no-click。
  top-level RED 仅来自 next-event sentinel，不否定 `.68` target GREEN。
- `Z:\p2r234promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `7DB45A511366461CAF854F374AADC26526D084FA4CC0DF839BB1F98F7DBC061F`。R、完整 full-tree、AF5 terminal route 3、
  source registry 与最终视频仍 pending；R234 不提升最终 readiness。

## 09 月 07 日：R235 credit project R entry，`.131` target GREEN / `.129` sentinel RED

- `.131` exact contract 位于 commit `ec5ab47`；focused normal/`-O` 各 `3/3`、aggregate normal/`-O` 各
  `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。R235 connection generation `47` 继续连接
  retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- R-domain `zg361cp.131` instance `157` 在 `date_raw=53187528` 的 exact frame 含 74 scopes
  （39 character + 35 value）；R235 选择 authored/native `1/0`，目标路径 GREEN。
- 下一帧 `zg361cp.129` instance `158` 在 `date_raw=53187552` 出现，保持同一 exact 74-scope shape，native options
  `0/1/2`、snapshot `3`；runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 `.131` target GREEN。
- `Z:\p2r235promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `E4F011558A01982DE3F73B2EE45CB943688A4874453BC7B106B859ECE266D3CC`。R235 作为独立 run 收口。

## 09 月 07 日：R236 credit project R queue，`.129` target GREEN / `.134` sentinel RED

- `.129` exact contract 位于 commit `69e8ce5`；focused normal/`-O` 各 `6/6`、aggregate normal/`-O` 各
  `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。R236 connection generation `48` 继续连接
  同一 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- `zg361cp.129` instance `158` 在 `date_raw=53187552` 选择 authored/native `1/0`，queue 路径 GREEN；
  awarded `0`，promotion slot 保持 `free=1 / used=0`。
- 下一帧 `zg361cp.134` instance `159` 在 `date_raw=53187576` 出现，保持同一 exact 74-scope shape，native options
  `0/1/2`、snapshot `3`；`.134` contract 尚在施工，runner strict no-click。top-level RED 仅来自 next-event sentinel。
- `Z:\p2r236promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `379B34E58DDDC08F6E194F28C19AEE9DF8F9BC23C0EBF80DA88E10E809E519ED`。R-domain 余下路径、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R236 不提升最终 readiness。

## 09 月 07 日：R237 credit project R continuation，`.134` target GREEN / `.130` sentinel RED

- `.134` exact contract 位于 commit `d8ef85b`；focused normal/`-O` 各 `9/9`、aggregate normal/`-O` 各
  `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。R237 connection generation `49` 继续连接
  retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- `zg361cp.134` instance `159` 在 `date_raw=53187576` 的 exact frame 含 74 scopes；R237 选择 authored/native
  `1/0`，目标路径 GREEN。
- 下一帧 `zg361cp.130` instance `160` 在 `date_raw=53187600` 出现，保持同一 exact 74-scope shape，native options
  `0/1/2`、snapshot `3`；runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 `.134` target GREEN。
- `Z:\p2r237promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `4BE5F047ECE822DDA2F78D094D91B7B2A51612B25801F3E2B07EED76400003FA`。R237 作为独立 run 收口。

## 09 月 07 日：R238 credit project R outcome，`.130` target GREEN / `.132` sentinel RED

- `.130` exact contract 位于 commit `84fe28a`；focused normal/`-O` 各 `12/12`、aggregate normal/`-O` 各
  `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。R238 connection generation `50` 继续连接
  同一 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- `zg361cp.130` instance `160` 在 `date_raw=53187600` 选择 authored/native `1/0`，目标路径 GREEN；
  `wrong_role=0 / outcome=3`，R `3→4`。
- 下一帧 `zg361cp.132` instance `161` 在 `date_raw=53187624` 出现，保持同一 exact 74-scope shape，native options
  `0/1/2`、snapshot `3`；`.132` contract 尚在施工，runner strict no-click。top-level RED 仅来自 next-event sentinel。
- `Z:\p2r238promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `7534B22FDB901715A944C353EC8EA7E7F33708D7A60FF9AE32B6BE81BE223AF4`。R-domain 余下路径、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R238 不提升最终 readiness。

## 09 月 07 日：R239 credit project R settlement，`.132` target GREEN / `.133` sentinel RED

- `.132` exact contract 位于 commit `a90c12a`；focused postmortem normal/`-O` 各 `3/3`、R regression
  normal/`-O` 各 `15/15`、aggregate normal/`-O` 各 `73/73`、real exact `87`、`validate_static` 与 diff-check
  均 GREEN。R239 connection generation `51` 继续连接 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- `zg361cp.132` instance `161` 在 `date_raw=53187624` 的 exact frame 含 74 scopes。R239 选择 authored/native
  `1/0`，目标路径 GREEN；实际 release `16`，结算后 `available=76 / rem=0 / reserved=24 / spent=24`，project closed。
- 下一帧 `zg361cp.133` instance `162` 在 `date_raw=53187648` 出现，保持同一 exact 74-scope shape，native options
  `0/1/2`、snapshot `3`；runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 `.132` target GREEN。
- `Z:\p2r239promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `D04663E90BD693049A50FEEFBC25756E97D01374DAAB2D6F2F04BF14291F1B19`。R239 作为独立 run 收口。

## 09 月 07 日：R240 credit project R completion，R domain live complete

- `.133` exact contract 位于 commit `584cbe6`；focused normal/`-O` 各 `5/5`、R regression normal/`-O` 各
  `17/17`、aggregate normal/`-O` 各 `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。
  R240 connection generation `52` 继续连接同一 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- `zg361cp.133` instance `162` 在 `date_raw=53187648` 选择 authored/native `1/0`，目标路径 GREEN；
  R `5→6 / active=0`，portfolio finalize，R domain live complete。
- 中央 poll 后下一帧进入 P3：`zg361cp.9000` instance `163` 在 `date_raw=53187720` 出现，exact 54 scopes
  （23 character + 31 typed non-character），native options `0/1/2/3`、snapshot `4`；`.9000` contract 尚在施工，
  runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 `.133` target GREEN。
- `Z:\p2r240promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `BA090BA6DD8E39706182ADAC1BB50237795403A9C393D4F4C5D5718143A47361`。P3 35 卡、stage 9–11、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R240 不提升最终 readiness。

## 09 月 07 日：R241 P3 entry，`.9000` route D target GREEN / `.229` sentinel RED

- `.9000` exact contract 位于 commit `9aa3a85`；focused normal/`-O` 各 `3/3`、aggregate normal/`-O` 各
  `73/73`、real exact `67`、`validate_static` 与 diff-check 均 GREEN。R241 connection generation `53` 继续连接
  retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- `zg361cp.9000` instance `163` 在 `date_raw=53187720` 的 exact frame 含 54 scopes（23 character + 31 value）。
  R241 选择 route D authored/native `4/3`，目标路径 GREEN。
- 下一帧 `.229` instance `164` 在 `date_raw=53187744` 出现，保持同一 exact 54-scope shape，native options
  `0/1/2`、snapshot `3`；runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 `.9000` target GREEN。
- `Z:\p2r241promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `B6E92CF23CAA583B711EDC2CD7D500D022F1C7D7047407341AD45B532235CFB2`。R241 作为独立 run 收口。

## 09 月 07 日：R242 P3 AA batch，13 张逐张 live proven / AA close

- AA batch exact contract 位于 commit `5517db1`；focused normal/`-O` 各 `7/7`、aggregate normal/`-O` 各
  `73/73`、direct `.229` exact verification 与 exact coverage `67`、`validate_static` 与 diff-check 均 GREEN。
  R242 connection generation `54` 继续连接同一 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- AA 13 张按执行顺序为 `.229,.230,.231,.232,.233,.234,.235,.236,.237,.240,.238,.239,.241`，对应 instances
  `164–176`、`date_raw=53187744..53188032`；每张均 authored/native `1/0` GREEN，并各有独立
  `event_instance_advanced` 后置验收。AA close，hidden `.9001` 未暴露 UI。
- 后 12 张先前仅 source-predicted，R242 现已逐张 live proven；不能继续把它们只算预测覆盖。
- 下一帧 AG `.301` instance `177` 在 `date_raw=53188056` 出现，exact 58 scopes
  （25 character + 33 typed non-character），native options `0/1/2`、snapshot `3`；AG contract 尚在施工，
  runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 AA batch target GREEN。
- `Z:\p2r242promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `06A44E2641D4BB576A90DBE6CDDD1735ABC1EB86E4101BEF7FBB32020378F128`。AG、AJ、stage 9–11、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R242 不提升最终 readiness。

## 09 月 07 日：Stage 11 Workforce Endgame 源码路由预审（research）

- 只读 source audit 确认 itemized/full-tree 应选 player batch mode `zg361ch.950.d`；中央先完成 stage 9/10，stage 11 才打开 Workforce。最短 non-debt 合同对适用的 owner-visible 卡选择 A，不能让 timeout 自动落到 route C debt。
- AB 可见序列为 `.242→.243→.244→.245→.246→.247→.248→.249→.250→.251→.252→.253`。AC 为 `.254→.255→.260→.261→.256→.258→.259→.257→.262`；随后必须跨周期通过 `.5262` 才出现 `.263`，再走可见 handoff `.5264→.5265→.5266`（其间 hidden D+30 `.5267/.5268`）及 `.264→.265`。
- 当前 central-owner / AI-subject 的 accepted AD 生产路径为 `.266→.273→WAD referral callback→.271→WAD panel callback→.267→.268→.270→.272→.274→.269→future real probation/outcome→.276→.277`；只有 player 承担对应 actor 时才额外显示 `zg361wad.1` 或 `.11/.12/.13` 之一，accepted 路径的 `.275` 自动记 N/A、不显示。AL 为 `.355→.356→外部真实 357–359 receipts→.360`；冷启动/第一个 eligible history cycle 在 `.360` 后以 status `8`、`operation_used=39/40` 合法关闭，`.361` 仅同 subject/tuple 第三个 eligible cycle 且 owner 为 top celestial liege 时可见，并以 status `6`、`40/40` 关闭。
- `.262` 的硬前置是 `owner.review_serial >= ticket_cycle+1`，所以不存在同一 review cycle 内抵达 summary 的 non-debt 路线；只有 Workforce status `6/8` 关闭且 central stage 11 记录 status `2` 后，才可等待 aggregate `zg361p2c.2`。以上仅为 `source-audited` / `research` 施工边界，本次没有点击或实机观测任何 Stage 11 事件，不提升 live/full-tree readiness。

## 09 月 07 日：R243 P3 AG batch，11 张逐张 live proven / AG close

- AG batch contract 已由 commit `006da51` 提交；R243 前的报告滚动状态已在 commit `c9a5bb1` 提交。
  R243 connection generation `55` 继续连接 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- AG 11 张按正常顺序 `.301–.311` 对应 instances `177–187`、`date_raw=53188056..53188296`；每张均
  authored/native `1/0` GREEN，并各有独立 `event_instance_advanced` 后置验收。AG close，hidden `.9002`
  未暴露 UI，流程同步进入 AJ。
- 下一帧 AJ `.334` instance `188` 在 `date_raw=53188320`、root `32904` 出现，exact 62 scopes
  （27 character + 35 value），native options `0/1/2`、snapshot `3`；runner strict no-click。整体 RED 仅来自
  这个新哨兵，不否定 AG batch target GREEN。
- `Z:\p2r243promo_resume_r1\report.json` 已通过 JSON parse，SHA-256 实算为
  `3D15FBCB042D4C8D98C625599FEDDB70DF96FD4F2CEFA197E102576F4F26607B`。AJ、stage 9–11、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R243 不提升最终 readiness。

## 09 月 07 日：R244 P3 AJ batch，AJ / Phase 3 close / 后续 harness RED

- AJ batch contract 位于 commit `3926006`。R244 connection generation `56` 继续连接 retained PID `44264`；
  runtime diagnostics GREEN、blocking `0`；另有 1 条无关 situation nonblocking warning，不构成产品阻断。
- AJ 11 张按执行顺序 `.334,.335,.336,.338,.339,.340,.342,.337,.341,.343,.344` 对应 instances
  `188–198`、`date_raw=53188320..53188560`；`.336` authored/native `2/1`，其余 `1/0`，所有卡逐张 GREEN。
  AJ / Phase 3 正常 close。
- runner 随后自然推进到 `date_raw=53191392`；pause-map pre-submission revision rebind 4 次失败，最终
  expected `78` / current `80`。整体 verdict 是 harness RED，不是 product RED，不否定 AJ/Phase 3 close；
  最小修复正在进行。
- 下一玩家事件尚未冻结；stage 9 `.390` 仍 pending，不推定下一事件、日期或 readiness。
- `Z:\p2r244promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `E5E710B1B51BBA06F7AFB43743F0E33C15C4BA63EFA2F36926CEA6CBA28D3933`。stage 9–11、完整 full-tree、
  AF5 terminal route 3、source registry 与最终视频仍 pending；R244 不提升最终 readiness。

## 09 月 07 日：R245 旧 absolute bound 输入前拒绝

- R245 generation `57` 在 retained PID `44264` 上从 `date_raw=53194104` 开始，drain `0`。旧 absolute 1900-day bound
  在输入前拒绝，attached snapshot 为 `paused=false`，产品仍继续运行；runtime diagnostics GREEN、blocking `0`。
- 这是 input-free harness RED，不是 product RED；旧 bound 证据单独不足以提升 readiness。
- `Z:\p2r245promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `CBCD5711B76A6253B42EB601EC7A7AF8EA6192E5BAAF3FF76B52C8518A9426D2`。

## 09 月 07 日：R246 pause mismatch，重试次数不是修复

- R246 generation `58` 继续同一 PID；`tribute_mission.1005` instance `199` / `date_raw=53194104` 以 authored/native
  `6/5` GREEN，`spymaster_task.0359` instance `200` / `date_raw=53194896` 以 `1/0` GREEN。
- 随后连续 8 次 pause mismatch 均 input-free；harness RED、runtime diagnostics GREEN、blocking `0`。这否定单纯增加重试，
  不否定两个事件目标 GREEN，也不是 product RED。
- `Z:\p2r246promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `756943C014E5830A740EE93305FA09265767E40D2AF8144FFDB3202114A2CFED`。

## 09 月 07 日：R247 native-fresh-idempotent pause fallback 实机成功

- R247 generation `59` 继续 retained PID `44264`；native-fresh-idempotent pause fallback 实机成功。随后
  `tgp_china_ministry.0100` instance `201` / `date_raw=53198208`、`debate_event.5110` instance `202` /
  `date_raw=53203368`、`ep3_decisions_event.2001` instance `203` / `date_raw=53203440` 均以 authored/native `2/1` GREEN；
  runtime diagnostics GREEN、blocking `0`。
- 下一帧 `natural_disaster.8001` instance `204` / `date_raw=53204688` / root `32904` 含 exact 8 scopes：2 character，
  situation、situation_sub_region、situation_participant_group、province、great_project、landed_title 各 1；仅 native option 0、
  snapshot `1`。runner strict no-click，当前是 harness sentinel，不是 product RED。
- `c0b42cb` 已闭合有限 5000-day absolute bound 的静态证明及 `natural_disaster.8001` exact 合同；该事件的 live continuation 仍待恢复后继续。stage 9 `.390` 尚未出现，不推断其状态；
  stage 9–11、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending，R247 不提升最终 readiness。
- `Z:\p2r247promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `517B41DB465D1C8A48F70CE2FADAAE6802C9D2647C5D549DAFF041579F387E5A`。

## 09 月 07 日：`c0b42cb` 5000-day bound 证明与 R247 后续 incident

- `c0b42cb` 固定单一 absolute 5000-day continuation bound。R245 已消耗 `1962` 天；Stage 11 nominal `3234` 天，
  加两次 `D+90` 与 `60` 天余量后为 `3234 + 2×90 + 60 = 3474` 天，再加 B1 `400` 天和 compensation `1060` 天，审计上界为
  `4934` 天，仍余 `66` 天。absolute raw deadline 为 `53267016`，reconnect 不重置或续期。
- R247 后 retained PID `44264` 于约 13:48:46 无序退出，最新 autosave 为 13:48:10。未发现新的 CK3 crash/WER artifact；
  该缺失不能证明原因，故只记为 `unexplained abrupt exit` / harness incident。
- report generation 存在 liveness race。退出前产品 runtime diagnostics 仍 GREEN、blocking `0`；不回溯否定已闭合事件，
  不宣称 product RED。恢复结论保持 pending，不猜原因或 stage 9 `.390` 状态，也不提升 full-tree readiness。

## 09 月 07 日：`df5ab3c` late-save recovery 竞态修复与 R248

- commit `df5ab3c9110e07ea2501c2aaecf87a5790196ea0`（`Recover late promotion sessions safely`）已推送，
  `HEAD == origin/master`。恢复器以 source-immutable、fresh isolated product-only profile 接管 late save；final handoff
  在 `driver.close()` 后复查 supervisor，若已退出则撤销 retention/reconnect，关闭已死亡 PID 被误报可续跑的竞态。
- 直接覆盖该修复的纯离线单测 normal `79/79`、`-O 79/79` GREEN，包含 driver-close 期间 supervisor exit
  的 retention revocation 回归。本报告代理未启动 CK3。
- R248 report `Z:\p2r248restore\report.json` 已通过 JSON parse，SHA-256
  `DAFADC56F489226BD5C4FBC57C9C236DB119FD54ED663B083ABCCBDBE3FF968D`。源 autosave
  `Z:\p2r193promo_a_native_state\profile\save games\autosave.ck3` 为 `83,449,160` bytes，SHA-256
  `c7e5ca119f4db4d9bfe738859dd5e32dec6f9f1f804d91f9b26ed5d256e219da`。
- projection `phase2-full-release-r248-df5ab3c` 的 source tree SHA-256 为
  `3adf4297df0699f903d152561d2a9b5231d7934e15bc2255dbb88d0541161bcd`，formal overlay/product tree SHA-256 为
  `644dbc006d15bb9cbb193a31323e313682464855c5b8e863b59e7e4fa0e8e197`；manifest
  `Z:\p2p248restore_current\phase2-product-projection.json` SHA-256 为
  `5846F60D99AE6E040E99E65DB96938B4C66B14174479D975D07C20EB6036EBD7`。
- loader `303/303` 与 error scan GREEN；report RED 仅因 `death_management.1001` instance `202` /
  `date_raw=53201424` / root `32904` 是新 exact interrupt，runner 输入前 strict fail-closed、未点击。session 为
  `RETAINED`：PID `113932`、generation `1`、paused/map-ready、reconnect authorized。
- late save 从 `date_raw=53199480` 继续，不是 `natural_disaster.8001` 重放；该事件仍无 live continuation proof，
  T0-P1/full-tree 未完成，stage 9 `.390` 状态亦不推断。
- 宣传工具本轮仅 prep preflight；用户新增 hard gate 下 T0-P2 继续 `LOCKED`，没有 source intake/capture、TTS、
  FFmpeg、候选媒体或审阅/导出，footage `0/8`、最终 MP4 `0/2`。

## 09 月 07 日：R249 retained continuation

- R249 report `Z:\p2r249promo_resume\report.json` JSON parse GREEN，SHA-256
  `4F2B70AC510157DC8DFD978A3A0418B9A28597D5B88EE50460FD10F4BB6964F1`。connection generation `2`
  复用 PID `113932`，无 launch/restart、restart count `0`；runtime diagnostics GREEN / blocking `0`，会话继续保留。
- `death_management.1001` instance `202` / `date_raw=53201424` authored/native `4/3`、
  `debate_event.5110` instance `203` / `53203368` authored/native `2/1`、`ep3_decisions_event.2001` instance `204` /
  `53203440` authored/native `2/1`，三者均以 event-instance advancement GREEN。
- 下一帧 `parent.1005` instance `205` / `date_raw=53205336` / root `32904`，原生 options `0/1`；strict runner
  输入前 fail-closed、未点击。整体 RED 仅由这个新 harness sentinel 引起，不是 product/runtime RED。
- R249 没有 `natural_disaster.8001` drain/replay 证据，T0-P1/full-tree 仍未完成；T0-P2 hard gate 与媒体未制作边界不变。

## 09 月 07 日：R250 retained continuation

- R250 report `Z:\p2r250promo_resume\report.json` JSON parse GREEN，SHA-256
  `E7EE5D62BF4998F1C082849CC9602C8DA4D9796E6396F1C640145F4BEE0A1061`。connection generation `3`
  复用 PID `113932`，无 launch/restart、restart count `0`；runtime diagnostics GREEN / blocking `0`，会话继续保留。
- `parent.1005` instance `205` / `date_raw=53205336` 以 authored/native `2/1` exact GREEN，并具备
  event-instance advancement 后置条件。
- 下一帧 `ep3_emperor_yearly.2211` instance `206` / `date_raw=53206512` 的 snapshot option count 为 `4`，实际渲染
  native indices `1/2/3`；exact scopes 为 `potential_title:landed_title`、`liege:character(32904)`、
  `vassal:character(27275)`。strict runner 输入前 fail-closed、未点击；整体 RED 仅是新 harness sentinel，
  runtime/product 边界保持 GREEN。
- T0-P1/full-tree 仍未完成；T0-P2 hard gate、prep-only 与未制作媒体边界不变。

## 09 月 07 日：R250 后外层 turn abort 会话丢失

- R250 报告写入时的 `RETAINED` handoff 是有效的 artifact-time 证据；随后外层 turn abort 后，OS liveness check 发现 bridge PID `113932` 与 owner PID `151820` 均已不存在，`owner.json` 只剩陈旧记录。
- 该事件严格定性为 `post-turn-abort session loss`：不推断 product fault，不撤销 R250 已完成的 `parent.1005` GREEN；但 `.2211` 的未输入 paused frame 已丢失，不能再声称从同一 PID 续跑。
- 源 autosave 仍为 `83,449,160` bytes，SHA-256 `C7E5CA119F4DB4D9BFE738859DD5E32DEC6F9F1F804D91F9B26ED5D256E219DA`。下一步从该冻结存档 fresh-recover 到新的隔离 userdir，并重放已签约事件至 `.2211`。

## 09 月 07 日：R251 recovery 启动边界 RED

- 两个配置拒绝均发生在 CK3 启动前：首个 pipe 名不满足严格的 32 位十六进制后缀；第二个 userdir 位于当前工作树内，被既有隔离门禁拒绝。两次均未创建 CK3 进程。
- R251a 使用系统临时目录内完整复制的 settings/account/DLC 与 4,944 文件 / 215,620,408 bytes warm shadercache；产品投影、83,449,160 bytes autosave 与双空槽检查均 GREEN。无 bridge 的 Frontend warm-up PID `64324` 在约 2.32 秒、任何 debug log 或 Frontend 证据前以 code `1` 退出。crash bundle 的异常为 `C0000005` at VA `0x00007FF6FC2FBD89`；minidump image base `0x00007FF6FA550000`，即 `ck3+0x1DABD89`。report SHA-256 `325F18550571D0B6ED4A5AD44C66BEF1026A5F86194C24A9FBC2FEDAD099DD5A`。
- R251b 只改变为更短的全新 userdir，约 1.66 秒后重复相同 VA/RVA。report SHA-256 `3DCC64B237C0382466C61D61F73C0441FC69B58246265C94B3BDDBA4FC7817AD`。两轮均未进入 loader、bridge、存档或 gameplay，不能推断产品 RED或增加已测业务场景；最终 CK3/injector inventory 为 `0`。相同 direct-launch 分支停止重试，T0-P2 保持 `LOCKED`。

## 09 月 07 日：R252 Default-desktop `continue-last-save` 启动边界 RED

- recovery relay 固定 child、`WinSta0\Default` 与 `continue-last-save`，默认 no-launch；focused recovery relay normal/`-O` 各 `5/5`、共享 primitive 后的 seed relay各 `4/4` GREEN。实际 no-launch preflight SHA-256 为 `26A563E4FEB7F0359A836826CCFDB82D58164CFA74FEB3AB95F29A2BFAB5AE2A`。
- 唯一 execute 通过 suspended pre-resume bridge injection 连接 PID `171184` / generation `1`，但在 `14.371s` 内、loader/save/gameplay 证据前以 code `1` 退出；没有玩法输入。cleanup 证明 Job active process `1→0`、tree gone、watchdog/control absent，最终 inventory 为空。
- minidump ModuleListStream 将异常 VA `0x00007FF7D823BD89` 与 CK3 base `0x00007FF7D6490000` 精确绑定为同一 `ck3+0x1DABD89 / C0000005`。report/exception/minidump SHA-256 为 `6E88F5A9D5F79F97A3A7BAAF696A720B8D5CBDD50F209858E369CE31DD1C7A2B` / `57AA603055D2AAD3EDA97C95E93E7FE3E9AE86E63EA0B1E1A70B35B53CBD8EDA` / `851FE1D2EADAE466DC624AEEB310823677D1CD2771A2544B8DE9129138B943B8`。
- 该结果证明 Default desktop 与提前 bridge 连接仍不足以解除启动故障；相同形态停止重试。它不增加 scene coverage、不构成 product RED，也不撤销 R248–R250 GREEN。T0 保持 `50%`、T0-P2 保持 `LOCKED`。

## 09 月 07 日：R253 freeze165b bridge-backed Frontend warm-up RED

- 新增 opt-in 双桥 choreography：freeze165b bundle/SHA 与独立 pipe 只用于 Frontend warm-up，warm-up 完全清理后才允许当前 bridge 加载 autosave；pipe 复用会在启动前 fail-closed。focused recovery normal/`-O` 各 `13/13`、Default-desktop relay 各 `6/6`、选定 native lifecycle 各 `3/3`，并通过 `validate_static`。
- no-launch preflight SHA-256 `3914C7CD37BC10BB0019B800C002C259AB6F9A274C514667B69FF6BAFFC5793B`。唯一 execute 的 warm-up PID `42716` 在 1.411 秒内、debug log/loader/save/gameplay 前以 code `1` 退出；final bridge 未启动、无玩法输入，最终 CK3/injector inventory 为空。
- exception VA 与 R252 完全相同，为 `0x00007FF7D823BD89`，仍是 exact `ck3+0x1DABD89 / C0000005`。report/exception/minidump SHA-256 为 `1F91F30CD5DBFF6757A27FC6C335F42C69311BD3DCA69341D727FA92C3F12E39` / `B4C4F21EF5524BE22705811986E745D5543557BE5C55B0F1D0D7C9EC7376560D` / `562C26615B0567EA48CFEB516B20C6BCACEE64FCCBE7D51C5E06425E93A6A79F`。该结果只关闭“历史 bridge + 当前 warm-profile/Default-desktop 编排足以恢复 Frontend”的假设；不增加 scene/readiness，且不再原样重试。

## 09 月 07 日：R254-Z envelope volume A/B RED

- R254-Z 只将 R253 的 fresh state/artifacts/relay logs 从 C: Temp 移至 Z:，其余 Default desktop、p2r193 profile assets、R248 product/autosave、freeze165b warm-up bridge、current final bridge与双桥 choreography 不变。此单变量来自 Sep-03/R248 的 Z: Frontend GREEN 对照；R251b 已排除路径长度。
- no-launch preflight SHA-256 `B9081CB2232DFC99A4A2017B1FCE0DFDCBD6C672C6AEF503D0325DF7C5294E64`。唯一 execute 的 warm-up PID `166664` 在 1.361 秒内、debug log/loader/save/gameplay 前 code `1` 退出；final bridge 未启动、无玩法输入，最终进程槽为空。
- 异常继续为 VA `0x00007FF7D823BD89` / exact `ck3+0x1DABD89 / C0000005`。report/exception/minidump SHA-256 为 `DC31413D20D0DDC18D58AF69F37B64066227B90B0DA448077856D7677B77608D` / `5651953D61B317448E2539F837B9919563B4D596F85A268287FF91A6B72DCD91` / `599648ED0180B0A40FF2AA7A7A3C6B36F312017BAAE729A601420B29D31EE6F6`。backing volume/location 不是充分恢复条件；scene/readiness 不变，且不再原样重试。

## 09 月 07 日：R255-Z no-bridge + Z: 缺失单元 RED

- R255-Z 保持 p2r193、R248 full product、同一 autosave 与 current final bridge，采用 Z: fresh userdir 和无 bridge Frontend warm-up，补齐 R251 C:/no-bridge 与 R254-Z Z:/bridge 之间的 2×2 最后一格。首次 pipe 格式错误在输入校验前置拒绝，未启动 CK3；严格 pipe 修正后只执行一次。
- warm-up PID `143132` 在 `1.311` 秒内、debug log/loader/save/gameplay 前 code `1` 退出；final bridge 未启动、无玩法输入。异常保持 VA `0x00007FF7D823BD89` / exact `ck3+0x1DABD89 / C0000005`。report/exception/minidump SHA-256 为 `5BD27572B78BF3B281608B45726399B824BEB9A209FE6A2C9926F08DE751B201` / `4422784DE31B1EC7CBED40F1471FDFF44F4C42C8CEF424E2F45F2C24A44AC50A` / `32EB5D32D5DAC9F77998146AFFB3F3FA1634518E485F5C9A480A31699CA92FF1`；事后 CK3/injector 槽为空。
- 这只关闭 Z:/no-bridge 启动假设，仍是 startup/harness RED；T0 保持 `50%`、逐 ID `4/361`、全树定义 `106/626`，T0-P2 继续 `LOCKED`。
- R248/R255 artifact 差分证明唯一尚未执行且有直接证据的变量是 Z: 根目录短 userdir：成功 R248 为 `Z:\p2r248restore_state\profile`，失败 R255 仍为仓库嵌套路径。R251b 只排除了 C: 短路径。R256 只有在 `Z:\p2r256restore_state` / `Z:\p2r256restore` 两个新鲜精确目录可写时才 GO；当前 sandbox 不允许创建 Z: 根目录目标，因此 CK3 保持 NO-GO，其他无依据形态不启动。
- 六份 dump 已反汇编为同一直接条件：`ck3+0x1DABD89` 的 `movsxd rdi,[r14+0x4c]` 在 `R14=0x1f8` 时读取 `0x244`；manager 有效但 `[manager+0xA8]` slot0 为 null，caller 首项即消费。没有 PDB/source，故不猜子系统名称；后续非启动工作转为 writer/xref 与 exact-build 只读时序探针，null guard 仅能作为实验而非语义修复。
- xref 继续证明 manager 在 global 发布前同步构建表项；slot0 writer `0x39C70B8` 直接写入 factory `0x3A88920` 的返回值，字符串绑定为 `ParticleTexture` / `gfx/FX/cw/particle2.shader`。因此不是异步等待问题，而是 factory null。新增默认关闭、exact-build-gated 的只读 startup slot0 probe，记录 manager/slot0/graphics-global/进程退出与错误；模块、native 接线、recovery normal/`-O` 分别各 `5/5`、`3/3`、`16/16` GREEN。不写目标内存，显式 capture 不完整时 fail-closed。
- factory null 仅有 graphics global/source/variant/backend 四类。三层 shader 均存在且声明 `ParticleTexture`；R248 prelaunch 与 R252–R255 的 4,908-file DX11 cache 逐字节一致，R251a 复用 R248 post-GREEN 4,944-file cache 仍崩，settings 亦同哈希。由此不清 cache、不改 DX11；probe 的 graphics-global 采样只区分第一类，不能冒充后三类分支执行证明。
- R257 以 R255 不变启动输入加只读 probe 诊断；CK3 仍在 1.312 秒命中同一 RVA，而 probe 首次 module snapshot 遇 `ERROR_PARTIAL_COPY (299)`、零样本 RED。该轮只暴露探针附着竞态，不判断 factory 分支、不增加 scene/readiness。已把下一次限定为修复 24/299 有界重试后的单次 R258 诊断。
- R258 修复后 probe `capture_ok=true`：root/graphics 各 252 次、slot0 109 次成功读取且零错误；graphics global 与 manager 先后非零，但 slot0 直至 exit code 1 始终为零。由此排除 graphics-global-null，只剩 source/variant/backend；CK3 仍为同 RVA startup RED。该轮只提升故障归因证据，不提升 T0 计分或解锁 T0-P2。

## 当前统一进度与计划窗口

| 项目 | 当前进度 | 主要余项 |
|---|---:|---|
| T0 天朝二期 | **50%** | 完整 L0、canonical product-only 路径（含 AF5 terminal route 3、stage 9–11、`7+4+restore`）、代表性高风险 live slices 与真实 encountered RED 闭环；T0-P2 尚未解锁 |
| T1 G2 | **90%** | GEN-034 source-specific lifecycle live 与剩余 provider/decision/action 闭环 |
| T2 open_kaishek | **100%（当前同步 horizon）** | shared-registry 初始能力为 `6b38d9c`，当前 BOM source hash 重绑 pin `edcd1ba` 已推送且 API 语义不变；父仓 pin verifier normal / `-O` GREEN，公共变更时重开 |
| 固定交付积分 | **67.5%** | R248–R255 均未跨越既定计分门 |

共享事件基础设施单列为 `production-live primitive`：registry asset、MCP real list/call 与 production wrapper cutover 已通过
上述回归；旧消费者 normal / `-O` 各 `160 passed, 1 skipped, 61 subtests`，并由 R372 `TGP0160` 同 PID真实 drain/advance
完成首例实机证明。该状态只覆盖首个 encountered-event slice，不改变上表百分比。

非阻塞覆盖 backlog：逐 ID strict bounded CK3-live 为 `4/361 = 1.11%`、未采样 **357**；选定全树路线 distinct
event-definition 采样为 `106/626 = 16.9%`、未采样 **520**。两套分母不能混用，也不参与 T0 完成百分比、P1 签收或 P2
解锁；它们同样不否定 Python L0、CK3-static-ready、central-wired 已为 `361/361`。source registry `0/4`、footage `0/8`、最终 MP4 `0/2`。

- 09-07 16:00–19:00：已冻结 R252/R253并闭合双桥 recovery/Default-desktop 静态回归；并行完成 G2 startup profile-assets gate。
- 09-07 晚间至 09-08：R254-Z 与 R255-Z 已排除 Z: backing volume 及 Z:/no-bridge 缺失单元；继续只读收敛 Sep-03/R248 GREEN 与当前 RED 的尚未解释输入，只在出现确定差异后再开一次启动。
- 09-08 至 09-09：以 recovery GREEN 为前提，先重抵 AF5，再完成 stage 9–11 与 canonical `7+4+restore`，随后补完整 L0 和代表性高风险切片；启动环境仍无可执行差异则顺延。
- 最早 09-09 至 09-10：仅在 T0-P1 全 GREEN 后推进 registry `0/4→4/4`、footage `0/8→8/8` 与双片制作/审阅/导出/发布。
- 09-11+：startup/product RED 的预备修复窗口；hard gate 不因日历时间而放宽。

## 09 月 07 日：T1 GEN-034 启动资产与 mutation checkpoint 门

- concrete adapter 新增显式 `--profile-settings-template`，fresh external userdir 后、任何 `Popen` 前校验复制完整 CK3 settings 及 warm DX11 shadercache；失败证据落 `startup-profile-assets.json`，并保证 CK3 零启动。
- observer detach before bridge、same PID、唯一 `surrender-war-50331699` 保持不变。surrender 前新增 durable `xar_checkpoint.ck3`，对路径/大小/SHA/日期/player/episode/active war 及同 PID/generation/snapshot/revision/native_revision/paused frame 作 hash-bound 验证；checkpoint 失败即不进入 mutation continuation。
- lifecycle + adapter focused normal/`-O` 各 `19/19`，profile settings guard normal/`-O` 各 `1/1` GREEN。实际 no-launch preflight 前后 CK3 inventory 均空，14/14 dependency hash 匹配；settings SHA `592AB6C6...F5244`，shadercache 4,908 files / 213,198,746 bytes / tree `373B8C76...B202`，preflight SHA `BC439E8576AC6DF211E7BE68EDE549BAE95BDA3E06EA4EFBC53973B10FC3210B`。
- 本轮未启动 G2 CK3、未捕获 observer、未连接 bridge、未 surrender，故 T1 保持 `90%`。改动未触及 open_kaishek 公共 API/ABI，T2 当前 horizon 保持 `100%`。

## 09 月 07 日：T1 GEN-034 r2/r3 live startup boundary

- r2 verify-only 对当前 14/14 依赖与 4,944 files / 215,620,408 bytes warm cache GREEN，SHA-256 `3894C49DBB303623339F6468E32150B1BA0FD26ACF58DC1D8947E54BAE946942`。真正 execute 启动 ordinary CK3 后暴露 concrete operation 三处 target identity 仍直调 CIM；`Get-CimInstance Access denied` 使唯一进程未被识别并回收。report SHA `26CC1A3E0692D2E9ABA16283EE072864DA3964B727719D28A4DC68B82CDC20CD`，未到 observer/checkpoint/mutation。
- 最小修复统一 CIM→Toolhelp32 fallback，以 `QueryFullProcessImageNameW` 补全 executable path，并严格校验唯一 PID/name/path/EXE SHA；focused adapter normal/`-O` 各 `14/14`。新 adapter/manifest SHA 为 `24C835E6...A630` / `087EE3BF...21DF`，r3 verify-only SHA 为 `F9705D8D...B4486`。
- r3 启动 PID `62524` 后仍在任何日志/窗口/observer 前命中 `C0000005` at `0x00007FF7D823BD89`；后续 invalid window handle 只是进程先崩溃的结果。cleanup GREEN，remaining CK3 空。report/exception/minidump SHA 为 `2C590C763759500C1CDB2B99FCD4920FC0F230BAA5E283661510AE6B33C9DF9E` / `3CFC1E79F2A940645A4A5BA551AEC336EF20DB3B4A9E8A8C85AA0D23E6B3B504` / `654FF0BCE2B2EADBA1713CE21B3DD3306CA597F7F455F658F6574B9D3EB0FEC9`。
- 未捕获 source-specific loss、未生成 checkpoint、未执行 `surrender-war-50331699`；T1 保持 `90%`。T0/T1 只改变父仓内部 runner 与向后兼容 keyword-only API，open_kaishek 无消费者、verifier `GREEN_STATIC`，T2 保持 `100% / NO-CODE-CHANGE`。

## 09 月 07 日：T1 owner utility 与 same-frame white-peace 静态合同

- 新增 owner exit utility model provider 与全空 draft fixture；缺源、草稿、部分填写、budget ID/SHA、非线性/uncertainty/tail-risk rule 或审批元数据不完整全部 fail-closed，不提供默认权重。draft SHA `0C13AE4B...AF235`；专项 normal/`-O` 各 `10/10` GREEN。
- 新增以 durable pre-mutation checkpoint 为锚的 same-frame white-peace comparator、hash-bound prepare CLI、synthetic fixture 和专题文档。source/white/surrender 必须严格绑定 snapshot/revision/native revision/WarID/日期/PID/primary attacker/coarse frame；输出只到 static-ready，所有 live/recommendation/action/GEN-034 门恒 false。focused normal/`-O` 各 `39/39`，专项各 `7/7` GREEN。
- 集成审计同时修复 outer-owner 默认 continuation 未透传 `expected_war_id` 的实际缺口，并把该 invariant 写入 manifest preflight；outer-owner/live-adapter/lifecycle normal/`-O` 分别 `8/8`、`14/14`、`7/7` GREEN。
- owner 尚未填写并批准真实模型，也没有 production-live evaluator、同帧复算、typed submit 或六域 postcondition，因此 T1 仍为 `90%`，synthetic fixture 不计 live readiness。
- T2 复核为 `NO-CODE-CHANGE`：新增模块没有包级导出，open_kaishek 零消费者，WarID 透传不触及 native ABI/wire/opcode/capability。外仓 clean `HEAD == origin/main == 80fb3a4f...`，跨仓 verifier `GREEN_STATIC / ok=true`；当前同步 horizon 保持 `100%`。
- Git 元数据当前只读：`git add` 无法创建 worktree `index.lock`，因此本批次没有 staged 文件、commit 或 push；改动与全部证据仍保留在目标工作树，等待权限恢复后统一提交。

## 能力状态变化

| 能力域 | 周初 | 当前 | 仍缺 |
|---|---|---|---|
| manager seed | 未物化 canonical candidate | R159 production-projection fixture-live GREEN | 无；作为 W2 上游输入使用 |
| full-tree acceptance | 未开始 | R193–R247 已闭合既有逐卡证据；R248–R250 闭合四个 exact interrupt，并 strict no-click 于 `ep3_emperor_yearly.2211`；外层 turn abort 造成 session loss，R251a/b 又在 pre-Frontend、R252 在 bridge generation 1 后但 loader 前重复已知启动崩溃 | 有实证差异的新路径下从 hash-bound autosave fresh-recover；`natural_disaster.8001` 仍无重放/live proof；stage 9–11、AF5 terminal route 3 live、promotion/B3/scoreboard 及其余业务树完整 action/postcondition GREEN |
| 宣传 | 工具链 static-ready，`0/8` footage | prep 仅 preflight；用户 hard gate T0-P2 `LOCKED`，未制作媒体 | T0-P1/W2 GREEN 后解锁，随后 source registry、`8/8` footage、双片审阅/导出/发布 |
| G2 lifecycle / terms | 既有 static 合同边界 | settings + warm cache 资产门、explicit WarID 与 durable pre-mutation checkpoint 已 static闭合；`BC439E85...210B` no-launch preflight 为 `READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE`，readiness 不变 | T0 释放 CK3 槽后以唯一 mutation `surrender-war-50331699` 执行 `GEN-034` live；campaign dominance 与 owner-approved utility 仍缺 |
| open_kaishek companion | 既有 `NO-CODE-CHANGE` 兼容边界 | commit/pin 已推送，公共合同 8 fields/5 invariants 无变化 | 仅在后续公共 API/架构变化时再次同步 |

## RED、阻点与未闭合

- R159 GREEN 不能代替 B3 或 scoreboard GREEN；这两项仍缺各自正确 checkpoint、动作与 postcondition。
- `df5ab3c` 已修复 final-handoff liveness race；R248–R250 已将 `death_management.1001` 及随后三个原版事件 GREEN，R250 report RED 仅来自 `ep3_emperor_yearly.2211` strict fail-closed，不是 loader/product RED。之后的外层 turn abort 造成 session loss；R251a/b 与 R252 又在业务载入前命中同一 startup RVA，故当前必须等有实证差异的新 recovery 形态。任何启动推进也不能倒推 `natural_disaster.8001` 已重放/live-proven 或 T0-P1 完成。
- 宣传 prep GREEN 只代表 preflight；用户 hard gate T0-P2 仍 `LOCKED`，不得制作或声称已有任何候选/最终媒体。
- R193–R250 尚未重新抵达 AF5；R247 退出前的 sentinel 是 `natural_disaster.8001`，而 R250 artifact-time sentinel 是 `ep3_emperor_yearly.2211`；stage 9 `.390` 尚未出现。不能把 `f9580ca`
  的静态修复写成 AF5 terminal route live GREEN，也不能以 E/I/J/R、Phase 3 或 pause fallback GREEN 提升 promotion/full-tree readiness。
- T1 的离线矩阵和 `READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE` preflight 不能替代 `GEN-034` live；CK3 串行槽由
  T0 有差异的 recovery 及后续串行实机保持优先。typed surrender/private receipt、完整 startup assets 与 durable checkpoint gate 已具备，但 campaign dominance 与 owner-approved utility没有额外离线解法；下一 live 只允许显式 mutation `surrender-war-50331699`，checkpoint 不 GREEN 时 mutation 必须为零。
- 完整迁移树、source registry、真实 footage 与最终双片均未完成。

## R259–R261 startup factory 诊断增量

- R259/R261 两次 exact-build DR0 观测都把相同 `gfx/FX/cw/particle2.shader` + `ParticleTexture` 请求收敛为：graphics global 非零，而 source 与最终 factory output 均为零，即稳定 `source-lookup-null`。artifact SHA-256 为 `0E269C37...629BA` 与 `229A98C0...DE6C`。
- 两轮 v1 都因 `DebugActiveProcessStop` error 5 保持 cleanup RED；R260 则在中间 attach-count 构建中未 resume、无 tuple/branch 证据。三轮均无 gameplay input、bridge/map/mailbox/save/readiness，不增加场景覆盖。
- v2 静态合同已进一步绑定 source lookup/population helper 与 fallback resolver 的独立返回点，并改为诚实的 `terminate-after-capture` 生命周期；其后续实机只回答 resolver 子分支，不计作产品或场景 GREEN。T0 `50%`、`4/361`、`106/626`、`8/11` 与总体 `67.5%` 口径不变，T0-P2 仍锁定。
- R262 已完整观察 helper output 与 fallback resolver raw result 均为零，但暴露 exit-event 后零等待的 cleanup 竞态，总 verdict RED。R263 仅修该等待，最终以 artifact `87A7213F...0C58` 得到 `GREEN_DIAGNOSTIC / source-resolver-null`，七段 capture 与意图性终止清理全部闭合。它没有 product evidence 或场景增量；后续入口收窄为 resolver 内部 null 分支与 VFS provenance 的只读逆向。

## R264–R271 particle2 resolver/backend 诊断收敛

- R264 与 R265 在 exact CK3 `1.19.0.6` / EXE SHA-256 `2D00FF31...DB86` 上独立复现同一 `gfx/FX/cw/particle2.shader` + `ParticleTexture` tuple。两轮均观察到 source cache miss、路径规范化成功、resolver list 非空而搜索结果为零，最终稳定分类为 `GREEN_DIAGNOSTIC / source-resolver-null / resolver-candidates-all-reject`；artifact SHA-256 分别为 `B1677D1C...C32F` 与 `21C98520...7D93`。
- R266 将拒绝点收窄到唯一被遍历 candidate：prefix 为空、primary 与 secondary 均返回 `0`，search/source/factory output 仍为零；artifact SHA-256 `825DEEF2...A986`。R267 再进入 secondary path，证明 backend object、dispatch table 和 callback 地址均非零，但 secondary state `f9/fa=0/0`、callback 与 secondary result 均为 `0`，故“resolver 容器存在”不等于该 source 可解析；artifact SHA-256 `B63A0224...A0AE`。
- R268 继续沿同一 callback 取得 VFS provenance：backend root 为 `Z:/ck3_mod_rewrite/_runtime/p2r267diag-state/profile\\`，请求被展开为 `...\\profile\\gfx\\FX\\cw\\particle2.shader`，backend read result 为 `0`，随后 candidate 与 resolver 仍拒绝；artifact SHA-256 `A30DE8C7...26DD`。这只证明当前 candidate 首先查 disposable userdir overlay，不能外推其余 mount/order。
- R269 在该精确 userdir 放入单文件 overlay 后，分支发生可重复变化：source cache hit、source 与 `ParticleTexture` variant 均为非零，但 backend creation 仍返回零，分类转为 `GREEN_DIAGNOSTIC / backend-creation-null`；artifact SHA-256 `DEEA6C82...659A`。R270 补入最小 include overlay 后仍得到相同 source/variant hit 与 backend null，排除“只缺该 shader 的直接最小 include 文件”作为充分解释；artifact SHA-256 `25AE4EE9...3D7`。
- R271 将剩余边界收敛到 backend 内部：backend entry、device object、dispatch table 和 callback 均已观察到，backend cache output 为零，随后 vcall output 与最终 backend/factory output 仍为零；artifact SHA-256 `3A230624...7FC5`。没有源码/PDB 或更深返回原因证据，因此不把该零值命名为编译器、驱动或 shader 语义故障，也不据此写 guard/伪造对象。
- R264–R271 全部是 debugger 驱动的 `terminate-after-capture` 诊断：`input_sent=false`、bridge/map/mailbox 均未就绪、query/gameplay count 均为 `0`、`readiness_promoted=false`，且清理均为 `diagnostic-termination-clean`。这些结果只提升 startup root-cause 定位，不是产品 live、场景 GREEN、存档恢复或 T0-P1 验收；T0 `50%`、逐 ID `4/361`、全树定义 `106/626`、迁移阶段 `8/11`、T1 `90%`、T2 当前 horizon `100%` 与固定总体 `67.5%` 全部保持不变，T0-P2 继续锁定。

## R272–R273 HLSL 初始化与 13 文件 startup shader 投影

- R272 在 R271 的 source/variant hit 基线上继续进入 backend initializer：resource/graphics global、initializer object 与请求均非零，但首个 active stage `VertexParticle` 的 HLSL 生成直接返回 false，尚未进入该 stage 的 shader-cache lookup 或 shader vcall；initializer/backend/factory 因而均返回零。结果为 `GREEN_DIAGNOSTIC / backend-hlsl-generation-null`，artifact SHA-256 `C528AA12...840D`。
- R273 只在同一 disposable userdir 的既有 11 文件显式 shader/include closure 上补入 Clausewitz 隐式前置 `cw/defines_common.fxh` 与 `cw/defines_hlsl.fxh`。此后 `VertexParticle` 与 `PixelTexture` 两个 active stage 的 HLSL 生成均为 true；两次 shader cache 均 miss，但 shader vcall/getter/output 均非零，initializer 返回 true，backend 与最终 factory output 也均非零，分类变为 `GREEN_DIAGNOSTIC / all-nonnull / backend-initializer-success`。artifact SHA-256 `06328E12...A5D`。这证明该 exact-build 早期 `ParticleTexture` factory 的 13 文件 closure 足以越过先前 null 分支，但尚未证明 CK3 能到 Frontend、加载存档或进入产品循环。
- acceptance bootstrap 已把这 13 个 exact-build 文件建模为固定投影：5 个 `game` 层文件、6 个 Clausewitz/Jomini 显式依赖及上述 2 个隐式 HLSL prelude，逐文件绑定来源层、逻辑路径与 SHA-256，并只复制到 fresh isolated profile 的 `gfx/FX`。EXE hash drift、源文件缺失/漂移、目标越界、目标预存在或复制后 hash 不符均 fail-closed；投影 receipt 随 bootstrap/recovery evidence 保存。
- 该投影标记为 `harness_only=true / release_staging_affected=false`，不进入 mod source、正式 staging 或工坊产物。R272/R273 仍是 debugger 驱动的意图性终止诊断：没有 bridge/map/mailbox、query/gameplay input 均为零、没有 readiness promotion。后续正式 relay 结果见 R274–R276 独立小节；不能把 13 文件 closure 单独写成 startup、recovery 或产品 live GREEN。
- 本节不改变任何进度口径：T0 `50%`、逐 ID `4/361`、全树定义 `106/626`、迁移阶段 `8/11`、T1 `90%`、T2 当前 horizon `100%` 与固定总体 `67.5%` 均保持原值，T0-P2 继续锁定。

## R275–R276 guarded startup 与 cold-map VFS 边界

- R275 在 13 文件 harness-only overlay 上使用 guarded candidate（DLL / injector SHA-256 `CB7E0B09...B950` / `550FC2C5...FDC2D`）启动唯一 `continue-last-save` PID `115468`。本轮越过旧 Particle2 `ck3+0x1DABD89` 与 R274 `ck3+0x3B0AE5F`，但在 loader/map/mailbox/save/gameplay 前命中历史 exact `ck3+0x3BE33A9` cold-map VFS `C0000005`（VA `0x00007FF6BE5A33A9`）。因此 guard/overlay 的可证收益只到跨过前两处故障；新结果仍是 startup RED，不是 Frontend/loader GREEN。
- R275 权威 artifact：`Z:\ck3_mod_rewrite\_runtime\p2r275relay\execution.json` RED，SHA-256 `6FA7A5C600EF3F8A768B56FC851BEFF62F44710C48FA743CCD91C458D80DFD75`；`Z:\ck3_mod_rewrite\_runtime\p2r275restore\report.json` RED，SHA-256 `042DC1E0E8BC0BCA1D85DE530B4E099FB0BF52E9B75B953356F370CA7AAA38E9`；`Z:\ck3_mod_rewrite\_runtime\p2r275restore\00_phase2_native_session_start.json` SHA-256 `C99D75B195453AB2A8DAEDF14272A644ED34919CF748D5B4B7157A29F9DDCAC4`；crash bundle `Z:\ck3_mod_rewrite\_runtime\p2r275restore_state\profile\crashes\ck3_20260907_213439\` 的 exception / dump SHA-256 为 `5E36173202EDA97641B5A25004ED3EA9788FD3F08F7C850356572220158223E0` / `B3911F7C1846279E2DBF3A0D5F1C9FBE80275FDF16C5CC8E4CE1A53AE042677F`。该轮 managed cleanup 完整证明 tree gone、Job `1->0`、final inventory empty 与 watchdog absent。
- R276 改走 `bridge-frontend-first`，让同一 guarded candidate 只承担 warm-up。warm-up PID `56652` 在 Frontend marker/debug log 前再次命中相同 `ck3+0x3BE33A9`；warm-up evidence `status=failed / marker seen=false / log_bytes=0`，为 autosave 配置的 final bridge/process 从未启动，故无 final binding、存档加载或 gameplay。
- R276 权威 artifact 全量冻结：`Z:\ck3_mod_rewrite\_runtime\p2r276restore\report.json` SHA-256 `FD50EEB07DDBC3C7897082A0A67D5F6ACAFBBF9E8A6A5957FEE6800DA9DBECA9`；`00_phase2_native_session_start.json` SHA-256 `28D02C15388005C1525AAB4868FE35C786808A5788750B92CD6A0FD4966A87A5`；`09_phase2_native_session_cleanup.json` SHA-256 `984498177CD3810E19A54B7A7366C6381770A489815AB56140FAE3C210E9D0FA`；`10_phase2_native_session_state.json` SHA-256 `308C214BA24DC2283CE8AA5530683E4AFBA0BAA6A836EDD91525DD558C46B7E5`；`Z:\ck3_mod_rewrite\_runtime\p2r276restore_state\native-session\frontend-first-warmup.json` SHA-256 `206476EF40EC75A710DE1FA4AD8DFD1BEE78E64CD9C0639F0EDF797A1370181B`；crash bundle `Z:\ck3_mod_rewrite\_runtime\p2r276restore_state\profile\crashes\ck3_20260907_214331\` 的 exception / dump SHA-256 为 `F182D0EB087846D0133464A75B4FC8E3BB737E567BD5D73A8542EC5A050E97B6` / `2BC1DA1629EC2F057F2AACFAAB8866D9F3D182B3A56B327B049A7F3E647C7DEE`。
- R276 收口后的实时只读 process inventory 为 `ck3=0 / xar_ck3_bridge_injector=0`，但 formal cleanup 仍必须记为 RED：warm-up 在 binding 前失败，`binding=null` 且 native session `report=null`，所以 formal cleanup 缺 initial PID/generation 与 managed shutdown report，不能用事后空槽替代合同证明。R275/R276 均无场景或 readiness 增量；T0 `50%`、`4/361`、`106/626`、`8/11`、T1 `90%`、T2 `100%` 与总体 `67.5%` 全部冻结，source registry `0/4`、footage `0/8`、MP4 `0/2`，T0-P2 继续锁定；两种 `0x3BE33A9` 形态均不原样重跑。

## R277–R278 no-bridge warm-up 与 typed terminal/cleanup 验收

- R277 错误选择 `bridge-frontend-first` 却没有提供完整 warm-up bridge bundle；child preflight 在创建 CK3 或 state/artifact 目录前以 exit code `2` fail-closed，因此它不是实机 attempt。relay 的粗粒度 `ck3_launch_attempted=true` 只表示 child 被调用，不能覆盖 child 内部 preflight 事实。`Z:\ck3_mod_rewrite\_runtime\p2r277relay\preflight.json` / `execution.json` SHA-256 为 `3025501906039A2B96F6510C418C77AD03514FD245220B879A56F084C287E997` / `FF1852B808F78820B7D654A6AF8B241DC8894E68ACE9E3628E27BD493291EA02`；`p2r277restore_state` 与 `p2r277restore` 均未创建。
- R278 改为正确 Frontend-first：warm-up bridge 完全 disabled，只向 fresh profile 投影 13-file shader bundle，final autosave process 才配置正式 bridge。warm-up PID `151976` 在 `2.169s` 内、Frontend marker/debug log/map/mailbox 前命中 VA `0x00007FF6D733AE5F` / exact R274 `ck3+0x3B0AE5F` 并以 code `1` 退出；final process 从未启动。因此 no-bridge warm-up + shader closure 仍不能跨过该 DX11 pre-draw fault。
- 新 typed terminal 把该 pre-binding exit 固定为 `frontend_warmup_process_exit` / stage `frontend_warmup` / PID `151976` / exit `1` / `cleanup_proven=true` / `tree_gone=true`。session 与 report 仍为 RED，`crash_accepted_as_success=false`；独立 formal cleanup 则为 `GREEN / acceptance_scope=cleanup_only / session_result=RED`，并实机闭合 `supervisor_stopped`、final PID/binding absent、Job/global inventory empty、watchdog/control files absent 与零 contract error。该 GREEN 只认证失败后的回收，不认证启动或产品。
- R278 权威 artifact：`Z:\ck3_mod_rewrite\_runtime\p2r278restore\report.json` SHA-256 `D25DC41B5B5986EE87675A7E8753BC56821B3C0D05E9863D928F5BDC22034E6C`；`00_phase2_native_session_start.json` `2657EE81AD1369448214C55284BF018694B18050439C881D915D6F44BDD66110`；`09_phase2_native_session_cleanup.json` `6B37594D5270315B814897CF7B3CA5BE62C914039791B52D95E56BB03FBA1764`；`10_phase2_native_session_state.json` `A18F8A3A31745ADF41C86108A7C27EACD9C6CEA27207A2ACA4B34C045D9A559A`；`Z:\ck3_mod_rewrite\_runtime\p2r278restore_state\native-session\frontend-first-warmup.json` `68C77909F4783D888B67B1FBCEFD884BB17E1C1B5E438897CE744E92B645C373`；crash bundle `Z:\ck3_mod_rewrite\_runtime\p2r278restore_state\profile\crashes\ck3_20260907_220720\` 的 exception / dump SHA-256 为 `4DDE8E042BE63F459D2234AF017388EFC17D79EA6C33FD1E0914056308916802` / `B1EA380B8004D4D645F235D350DC993F8016EDD895AC5E1594A046683AD2435D`。
- 收口时实时 inventory 为 `ck3=0 / xar_ck3_bridge_injector=0`。R277 无启动，R278 无 Frontend/loader/save/gameplay，故场景与 readiness 计数全部冻结：T0 `50%`、`4/361`、`106/626`、`8/11`、T1 `90%`、T2 `100%`、总体 `67.5%`、registry `0/4`、footage `0/8`、MP4 `0/2`；T0-P2 继续锁定，R278 exact shape 不原样重跑。

## R279 短 Z state 路径 A/B

- R279 相对 R278 只把 disposable state/profile 缩短为 `Z:\ck3_mod_rewrite\s279\profile`（约 29 字符级）；no-bridge Frontend warm-up、13-file shader bundle 与 final-only bridge 配置保持不变。warm-up PID `57688` 在 `2.067s` 内、Frontend/debug log/map/mailbox 前再次命中 VA `0x00007FF6D733AE5F` / exact R274 DX11 `ck3+0x3B0AE5F` 并以 code `1` 退出，final process 未启动。短 Z 路径因此不是跨过该 fault 的充分条件；停止此类路径长度 A/B，不原样重跑 R279。
- session/report 保持 RED；typed terminal 为 `frontend_warmup_process_exit / frontend_warmup / pre_binding=true / PID 57688 / exit 1 / cleanup_proven=true / tree_gone=true`。formal cleanup 单独为 `GREEN / cleanup_only / session_result=RED`，`supervisor_stopped`、Job/global inventory empty、watchdog/control files absent 与零 contract error 均闭合；实时 `ck3` / injector slots 也为空。cleanup GREEN 只认证回收，不认证 startup 或产品。
- 权威 artifact：`Z:\ck3_mod_rewrite\a279\report.json` SHA-256 `4742B12ED6135A1CA93B109B990A0C14A933176E400705011BC52D96F06E11C4`；`00_phase2_native_session_start.json` `A5FE1DFD8B4F5DC5822414431EBE892A12643733C7CA6BA4E30FA0A30832DA1C`；`09_phase2_native_session_cleanup.json` `F22D08C98BB51E94956CBB603DE9D26226642D67E7099EED0307443B8E3C4EA5`；`10_phase2_native_session_state.json` `03187B0D99EEF12A56C2B3258A87CFAFB522F258544D336F2F953B43DE8A8B58`；`Z:\ck3_mod_rewrite\s279\native-session\frontend-first-warmup.json` `0763E32574FF7AFF1DDA1A6AF6CFF9162B4496488B0840AD2B2A27195CC5403C`；crash bundle `Z:\ck3_mod_rewrite\s279\profile\crashes\ck3_20260907_221635\` 的 exception / dump SHA-256 为 `B8E7C8476D7D057E38A76023714A823A658555711C97BF6A922D87DCC61122E8` / `1BFB693AFC0447FC8EF5FC8BA7941FBBC19DEEF7CEA5E17201C46B639CE53DBA`。
- 本轮没有 Frontend、loader、save、map/mailbox、gameplay 或场景/readiness 增量；T0 `50%`、`4/361`、`106/626`、`8/11`、T1 `90%`、T2 `100%`、总体 `67.5%`、registry `0/4`、footage `0/8`、MP4 `0/2` 全部冻结，T0-P2 继续锁定。

## 2026-09-07 日报正式收口

- 当日早会 D1 完成；D2、D5、D6、D7 部分完成；D3/D4 因 T0-P1 hard gate 未完成。canonical manager seed 已由 R159 GREEN；完整迁移树仍为 `8/11`，R280 将 startup blocker 收敛为 `pdx_paths` 两个 settings lookup 均 NULL、parser/insert 为零。
- 周目标 W1 保持完成；W2 仍在进行中；W3/W4 继续锁定。T0 `50%`、strict per-ID `4/361`、full-tree definitions `106/626`、T1 `90%`、T2 current horizon `100%`、总体 `67.5%`。当日未产出 footage/MP4。
- 当日全部 evidence 已落盘，但 `.git/worktrees/.../index.lock` 仍不可创建，commit/push pending；不把工作树保留误写为已提交。
- 下一日日会：[`../meetings/daily/2026-09-08.md`](../meetings/daily/2026-09-08.md)。

## 2026-09-07 日报正式收口

- 当日早会 D1 完成；D2、D5、D6、D7 部分完成；D3/D4 因 T0-P1 hard gate 未完成。canonical manager seed 已由 R159 GREEN；完整迁移树仍为 `8/11`，R280 将 startup blocker 收敛为 `pdx_paths` 两个 settings lookup 均 NULL、parser/insert 为零。
- 周目标 W1 保持完成；W2 仍在进行中；W3/W4 继续锁定。T0 `50%`、strict per-ID `4/361`、full-tree definitions `106/626`、T1 `90%`、T2 current horizon `100%`、总体 `67.5%`。当日未产出 footage/MP4。
- 当日全部 evidence 已落盘，但 `.git/worktrees/.../index.lock` 仍不可创建，commit/push pending；不把工作树保留误写为已提交。
- 下一日日会：[`../meetings/daily/2026-09-08.md`](../meetings/daily/2026-09-08.md)。

## 下一入口

## R280 `pdx_paths` producer/consumer 联合观测

- 冻结 default-OFF、read-only 的 named-path consumer 与 `pdx_paths` producer observer；MSVC Release `/Z7 + /DEBUG:FULL + /MAP` exact artifact focused CTest `5/5` GREEN。实机 PID `91980`、generation `1` 在 `16.143s` 后再次命中 exact `ck3+0x3BE33A9`，但受管清理完整，最终 CK3/injector 槽为空。
- dump 直证 producer 已事务安装（mask `31`、failure `0`），`pdx_paths` task 已进入一次；`paths.settings` 与 `paths_checksummed.settings` 的 `0x3BE2340` lookup 均各返回一次 NULL，同线程、无 read fault。parser 与 insert 均为零，named-path table 保持构造后的空态；consumer resolver/move 尚未执行。故根因边界从 R275 的三分支收敛到“task 已运行，但两个物理存在的 settings 文件在该 lookup 层均不可见”。
- artifact SHA-256：report `9230A6ABB2A27E74211360555A973AC623A6063B21D8A257D11C26711672A2A2`；minidump `8CAD7C1D200E565BA69AF3F07C4F28C93A694B743527071EFF5EA993EBEBB296`；离线三 observer extract `0E95E67496F86285394D78CCFD10F45A84152482247D812EBA1446E9A597C4F6`。本轮没有 loader node、map/mailbox、save 或玩法输入；全部进度口径冻结，T0-P2 仍锁定。下一工作包是 `0x3BE2340` NULL 早退与 `pdx_paths` 前置 VFS 初始化的 exact-build 只读逆向。

1. exact `ep3_emperor_yearly.2211` strict 合同与未知事件耐久 checkpoint 已静态闭合；R251 no-bridge Frontend 与 R252 Default-desktop `continue-last-save` 均不再原样重试。只有启动输入或编排出现有实证差异后，才从同一 SHA-256 绑定 autosave fresh-recover；不得把 startup RED 冒充产品/场景 RED，也不得宣称 `natural_disaster.8001`、T0-P1 或 stage 9 `.390` 已完成。
2. 重抵 AF5 后先以事件与业务后置实机证明 authored/native `42/41` 的 terminal route，再按 exact checkpoint/domain 顺序闭合 promotion、B3、scoreboard 与完整树其余 action/postcondition。
3. full-tree GREEN 后才启动 `8/8` 素材采集与最终双片媒体链。
4. T0 释放 CK3 串行槽后，按 `bdf5324` preflight 运行 G2 `GEN-034` source-specific lifecycle live，唯一显式 mutation 为 `surrender-war-50331699`；`open_kaishek` 仅在 T0/T1 公共 API 或架构变化时再次同步。

## 范围边界

- effect 文件按用途拆分，目标每文件 `1–10` 个、原则不超过 `20`。
- 玩法验收使用 MCP/native；OCR/image 仅限非玩法 legal/front-end gate。
- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。

## 09 月 08 日：D2 static-ready、CI 恢复与 T2 主线同步

- D2 VFS mount lifecycle observer 已完成 default-OFF、exact-build、initial-suspended、read-only、all-or-none 接线。冻结 state 大小 `0x33F8`，保留 64 个 `0xC8` publisher slots，并同时记录 core-init、publisher entry/return、双 settings lookup 与当时 manager/head。minidump extractor 已支持同一冻结布局；MSVC Release `/Z7 + /DEBUG:FULL + /MAP` 全构建及 CTest `102/102` GREEN。当前状态严格为 `static-ready`，R281 尚未开始，不能写成 startup 修复或 production-live。
- GitHub CI RED 已定位到 GENERATED ledger 漂移：R187–R250 与 Stage 11 的 244 行证据只在输出文件中，未进入生成器。最小修复把原文纳入 `gen_361_mechanisms.py`，ledger 保持逐字节零 diff；原失败四命令及 `validate_local.py` 均 GREEN。
- open_kaishek 本地主线已前进到 `33d690234d8217422978ee642055ab1b13e44c76`；增量仅为 CI、文档与 CLI smoke，未改变 Java/POM/profile 或公共 API/schema。父仓 active pin 已同步，跨仓 verifier `ok=true / GREEN_STATIC`，focused unittest `13/13` 与外仓 CLI smoke GREEN；T2 保持当前 horizon `100%`。
- 功能提交 `fb99dbd927f02d22ba1b501c25653446092c3956` 已推送且远端 SHA 精确匹配。Git 多身份操作 blocker 已解除；未纳入用户已有 runtime/artifact dirty 文件。
- T0/T1 readiness 不因诊断或静态兼容更新而上调：T0 `50%`、strict `4/361`、full-tree definitions `106/626`、阶段 `8/11`、T1 `90%`、固定总体 `67.5%`；T0-P2 继续 `LOCKED`。

## R281 VFS lifecycle live 与新 loader terminal 边界

- 唯一一次排他 `continue-last-save` 绑定 PID `128368` / generation `1`。R280 的 `ck3+0x3BE33A9` 未复现；D2 final heartbeat 为 `installed=true / mask=15 / failure=0`，core init 返回 true，publisher `34/34` 全成功且无 correlation miss/overwrite。producer observer 同帧证明两个 settings lookup 非 NULL、两个 parser 均进入、38 次 insert，并将 `0x583` 从 absent 变为 present。
- CK3 随后完成 303 条 database callback，末项 `CJominiInGameMusicDatabase`，没有 crash bundle或 runner-classified fatal error；但 164 秒静默后仍无 Frontend/Load Save/In Game/native-ready，runner 以 `loader_terminal_missing_after_database_callbacks` RED 收口。cleanup GREEN、tree gone、最终 CK3/injector 槽为空。
- report / loader gate / cleanup SHA-256 为 `144065DB...CFAFB` / `10CE0A86...716AC` / `C009FFF4...E4553`。这把 blocker 从 VFS crash 前移到 database callbacks 后，但不证明 observer 修复了竞态，不增加场景、阶段、full-tree 或宣传 readiness。下一次实机必须先增加 completion publish / outer return / terminal consumer 的新只读区分能力，禁止原样重复 R281。

## R282 completion/consumer 联合观测

- D2 + producer/completion correlation + wrapper consumer edge 的同一 build 全量 CTest `102/102` GREEN。唯一 PID `23608` 保持 VFS core-init true、34/34 publishers success，完成 database `303/303`、fatal 0 并明确进入 Frontend。
- producer selected count `1`；publish 后 wrapper 重入 `186870` 次，consumer edge `2466 / 184404` 次，retained selected task identity match `0`。因此已排除“publish 后 wrapper 没再运行”，但该私有调度现象不是 Frontend readiness 的必要条件。
- auto-continue 未进入 Load Save/In Game，R282 以 `frontend_without_load_save` RED 收口；cleanup GREEN、tree gone。report / loader gate / cleanup SHA-256 为 `AA3CCCB0...A69C9` / `12B44A2C...7EE4A` / `41CF1817...033B`。下一 distinct shape 使用既有 bridge-frontend-first 显式加载 `autosave`，不得原样重复 auto-continue。
- 首次 R283 只暴露 no-launch relay 漏检非法 warm-up pipe 后缀，child 在 CK3 创建前 RED。外层现复用 `32 lowercase hex` 格式门禁，normal/`-O` 各 `7/7` GREEN；旧 preflight 作废，不计一次 CK3 实机。

## R283 late-save recovery 与 B1 live blocker

- 修正 pipe 授权后，bridge-frontend-first warm-up PID `199476` 在 `10.723s` 到达 authenticated Frontend 并完整回收；final PID `145824` 显式载入 `autosave`，得到 loader `303/303`、fatal `0`、`GREEN / load_save`、paused/map/mailbox、玩家 `32904` 与日期 `53199480`。同一 PID 的 generation `2` reconnect 依次闭合原版 `birth.3035`、`birth.1010` 与产品 `zg361p2c.2`。
- 未知 `birth.3035` 前的 durable checkpoint 已真实写出，大小 `84,875,343` bytes、SHA-256 `6B5AE12D...20AD`。保存后 fresh snapshot/revision 属正常发布语义；helper 已改为校验稳定日期、PID/generation、玩家、event instance 与 option count，不再把正常 revision 前进误判为事件漂移。该原版事件的唯一 inert option 合同与相关回归 `31/31` GREEN，提交 `44883dd` 已推送。
- 产品入口产生 `4038` 条同源 B1 variable-write 诊断。实证表明同一 effect 中由 `every_vassal` 写入 temporary list、再被 `ordered_in_list` 读出的 Character row 仍可能退化为 weak scope，即使 `is_alive/is_landed` 为真也拒绝变量写入。最小修复改为 `every_vassal` 计数、封顶 80，再由 live `ordered_vassal` 用同一资格条件初始化 durable roster；生成器 `--check`、B1 `75/75` 与 `validate_local.py` GREEN，fresh R284 前保持 `static-ready`。
- R283b report / loader gate / R283c report SHA-256 为 `FF18E811...BF72` / `3C221567...F380` / `2876E02B...B449`；final PID 已通过 native-session queue 停止，外层 relay 完整回收，最终 CK3/injector 槽为空。late-save recovery 能力提升不等于 business scene 完成：T0 `50%`、strict `4/361`、definitions `106/626`、stage `8/11`、T1 `90%`、T2 `100%` 与总体 `67.5%` 均不变，T0-P2 继续锁定。

## R284 fresh B1 opener 归零与 agenda 首错

- `phase2-full-release-r284-04ac889` 为 1,031-file fresh release-identical projection；source/product tree SHA-256 `4960EDEE...AFD30`，projection/release manifest SHA-256 `63E30B4F...365D5` / `30AD0639...5ED19`。source autosave 仍为 `C7E5CA11...219DA`，preflight READY，warm-up/final 串行受管。
- final PID `45100` 获得 loader `303/303`、Load Save、paused/map/mailbox，并依次闭合 `debate_event.5110`、`zg361p2c.2`、`zg361.40`、`death_management.1001`、`ep3_decisions_event.2001`。R283 的 4038 条 weak-variable opener 旧族在 fresh 产品中为 `0`，故该最小修复已有 live 归零证据。
- strict checkpoint 停在 `chancellor_task.1002`；exact 三 Character scopes 与唯一 native option 0 均由 source/live 合同绑定。合同提交 `8e467fc` 已推送；generation `2` 同 PID reconnect 验证 instance `207` 关闭后继续自然推进。
- `.124` huddle 暴露新的 `11` 条产品 RED：temporary agenda candidate rows 丢失 `agenda_sort_key` variables，继而使 consumer 读取缺失 `pending_grade`。最小修复给全部 durable roster row 预置低 sentinel，eligible row 覆盖真实 key，再直接排序 durable roster；完全移除该 temporary list。生成器 `--check`、B1 `75/75`、`validate_local.py` GREEN，仍待 fresh R285 live 归零。
- initial report / loader gate / resume1 / relay execution SHA-256 为 `FD684169...1FEC` / `AC5864A6...4041` / `2944B00D...7B85E` / `5B9EB8F1...1438`；native-session queue 与 relay 已回收 PID，CK3/injector 槽为空。本轮不增加业务计数，T0 `50%`、`4/361`、`106/626`、`8/11`、T1 `90%`、T2 `100%`、总体 `67.5%` 与宣传 `0/4、0/8、0/2` 均保持不变。

## R285 fresh harness occurrence RED

- agenda 最小修复及报告已以 `3f42df2` 推送。fresh `phase2-full-release-r285-3f42df2` 为 1,031 files；tree / projection / release manifest / ZIP SHA-256 为 `A2956F69...4598D` / `8EB01E14...D373` / `40499BA0...C056` / `87F14C40...E96B`，verify GREEN；no-launch preflight `09F1E164...E977` READY。
- warm-up PID `76624` 在 11.003 秒达到 authenticated Frontend 并回收；final PID `63200` 的 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox GREEN，依次 exact 关闭 debate `202` / `53203368`、ep3 `203` / `53203440`、birth `204` / `53206920`。
- 同一客户端在 `53211192` 第二次投递 `debate_event.5110`，旧 `max_occurrences=1` 在第二次身份核验前 fail-closed。它是 harness bound RED，不是 mod runtime RED，不能给 agenda 修复记 live 归零。合同只扩到两次已观测投递，第三次仍停止，待 R286 验证第二次 exact shape。
- report / loader gate / relay execution SHA-256 为 `97743C6D...36EC` / `6631296D...DB83` / `578377A7...7547`；cleanup proven/tree gone，最终进程槽为空。所有业务/阶段/宣传计数保持不变。

## R286 dynamic exotic-arms lineage RED

- `bacde5a` 已推送 debate occurrence 修复；focused 双模式 `2/2` 与 checkpoint runner `77/77` GREEN。R286 的 1,031-file product tree 保持 `A2956F69...4598D`，projection / release manifest / ZIP 为 `55D514F5...8FA0` / `EBA2AE35...7B3A` / `87F14C40...E96B`；verify 与 no-launch preflight `41C80CA1...7762` GREEN。
- final PID `199412` 获得 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox，并 exact 关闭 debate `202`、ep3 `203` 与 movement `204`。随后 `ep1_flavor.2040` 在 `53206056` 仅因 R183 冻结的 holder/owner/merchant 数字 ID 漂移而停止。
- CK3 1.19.0.6 原版 source SHA `CC4CD67B...0924` 证明 holder 为 2041 caller、artifact owner 与 holder 同一、target 为收到事件的玩家、merchant 为当次动态创建。合同改为关系绑定并继续严格冻结 11-scope 类型/名称/数量、完整 option projection 与 refusal native 2；fresh R287 前只记 static-ready。
- report / loader / entry / cleanup / relay SHA-256 为 `BC26E32B...43E1` / `733D09E3...E44A` / `A9132557...81E6` / `C697EBD1...93AD` / `5A9667BE...F412`；最终 CK3/injector 为 0，计数不变。
- 连续 fresh 启动的时间线随机分叉已证明：input-free known-contract RED 后清理健康 PID 会拖慢同一首错闭环。recovery 现为 occurrence-bound / identity drift 两类专用异常记录完整 evidence，并仅在 `selection_attempted=false` 与既有健康门同时通过时保留 session；其余 RED 清理语义不变。recovery normal/`-O` 各 `20/20`、checkpoint runner `77/77` GREEN；R287 起可由原 resume client 直接续接纯合同修复。

## R287 same-PID drain 与 scoreboard product RED

- `541c962` 的 fresh R287 projection 为 1,031 files；product tree / projection / release manifest / ZIP 为 `A2956F69...4598D` / `8A54E4C5...926A` / `4FB228CB...1ABF` / `87F14C40...E96B`，verify 与 no-launch preflight `6644B565...2D03` GREEN。final PID `214748` loader `303/303`、fatal `0`、Load Save、paused/map/mailbox。
- generation 1 关闭 debate 与 ep3 decision 后，在未知 `ep3_emperor_yearly.2170` 前无输入 fail-closed并保留 PID。CK3 1.19.0.6 source/live 绑定 root `32904`、`our_county: landed_title` 与 native `(0,1)`；选不写 25 年 governor-efficiency flag 的 authored/native `1/0`。合同提交 `35c36f5` 已推送，focused 双模式各 `7/7`、checkpoint runner 各 `77/77`。
- generation 2 复用同 PID、restart `0`，将 `.2170` exact 后置条件关闭 GREEN，并继续关闭产品 `.2`、mandate `.40`、`birth.3035` 与第二次 debate。R284 的 11 条 agenda sort-key/pending-grade 签名为 `0`，agenda 修复已有 fresh-live 归零证据。
- `.122` 结算触发新的 953 条产品 RED；全部栈进入 scoreboard publish，首错是 weak Character `27928` 的 `zg361_kpi` 无变量存储，后续 generated fixed-slot consumer 连锁报错。权威 roster 已刚完成 prune，失效 row 来自同一 delayed chain 的旧 `add_to_list` scratch candidates。最小修复令 B1 直接排序 `zg361_b1_subjects`、非 B1 直接 `ordered_vassal`，完整身份过滤和 80 行上限不变。
- 修复已过 B1 normal/`-O` 各 `75/75`、scoreboard `36/36`、生成器 check、`validate_local`、release test `9/9` 与可复现构建；manifest / ZIP 为 `9F17FB96...406` / `EB637106...60A7`，fresh R288 前只记 static-ready。initial / loader / resume / entry / runtime / relay SHA-256 为 `3FF264C2...9B3` / `6FC63E9B...C8E2` / `1407717C...20CD` / `919D28DE...D3DD` / `FBBC6BE6...E650` / `3A2E5BDF...D509`。PID 已经控制队列停止，实时 CK3/injector 槽为空。
- 本轮关闭的是 harness 合同和旧错误族，没有新增 strict business scene 或 full-tree stage：T0 `50%`、`4/361`、`106/626`、`8/11`、T1 `90%`、T2 current horizon `100%`、总体 `67.5%` 与宣传 `0/4、0/8、0/2` 均不变；T0-P2 继续 `LOCKED`。

## R288 fresh scoreboard 归零与合法 debate 关系分支

- 已推送基线 `c8656a0` 的 fresh projection 为 1,031 files；tree / projection / release manifest / ZIP SHA-256 为 `7CA07BE0...B910` / `E8A34E17...D5E3` / `D192EF74...371D` / `EB637106...60A7`，verify GREEN。修正后的 no-launch preflight `F8525705...4227` READY 且进程槽为空；一个缺少恢复参数的早期 child 仅 argparse RED，没有启动 CK3 或生成 live 状态。
- final PID `38352` 达到 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox；R287 的 scoreboard 953 条旧签名与 R284 agenda 11 条旧签名均未重现，故 `c8656a0` scoreboard direct-roster 修复升级为 fresh-live 归零。
- strict drain 关闭 `.2`、ep3 decision 与 `.40` 后，在 debate instance `205` / date `53203368` 的合法第三方 outcome 分支 fail-closed：`host/opponent/contender=29363`，winner/loser 为 `30177/27051`。原版 source 证明 delivery-window host 与 activity durable outcome pair 并非必须重合；合同只修正这一关系，其他 identity/options/bounds 不放宽。checkpoint runner normal/`-O` 各 `77/77`、`py_compile` GREEN，健康同 PID 已保留待 generation `2`。
- 业务计数不变：T0 `50%`、strict `4/361`、definitions `106/626`、stage `8/11`、T1 `90%`、T2 `100% horizon`、总体 `67.5%`；T0-P2 继续锁定。
- debate 合同提交 `a071cd2` 已推送；generation `2` 在同 PID、restart `0` 下关闭 instance `205`。下一原版 interrupt 为 `tgp_movement_events.0080` instance `206` / date `53205336`，checkpoint `83,777,761` bytes / `F36CED92...CDE9E` 已落盘。source/live 合同冻结 `root_scope/my_movement/family_member` 三 scope、authored 4 / rendered native `(1,2,3)`，选择不写 estate modifier 或 movement influence/power 的终止 native 3；focused 双模式各 `14/14`、checkpoint 双模式各 `77/77` GREEN。resume report / entry / checkpoint artifact 为 `12ED14E2...DBD1` / `E00778CC...1D94` / `26233701...F300`，PID 继续健康保留。
- `.0080` 合同提交 `5e5cd67` 已推送；generation `3` exact 关闭它，并继续关闭 birth 与 tgp decision。产品 `zg361.1` 随后仅在 scope-name set 上停止：R288 frame 在既有 Central-active tuple 上精确携带 CP-E 八项和 P3-AA 四项生产 scope。合同增加这一个 58-name 集合及新增 tuple 的 character/value 类型检查，不接受任意超集；checkpoint runner normal/`-O` 各 `77/77` GREEN。generation 3 report / entry / diagnostic 为 `4287ABE0...23EB` / `FC9C7086...FCC3` / `029CE7BB...73E1`，同 PID 保留待下一 reconnect。

## R288 generation 4 产品 Route C 回执 RED

- annual-summary 合同 `e0502d2` 已推送并由同 PID generation `4` exact 关闭。随后 `zg361pp.146` Route C 产生三条真实产品诊断：`receipt_owner` 未设置、scope link unset、比较右值无效；调用栈绑定 case-kernel guard、m146 consumer/core 与产品 event option。report / entry / diagnostics / relay SHA-256 为 `A32CA516...AB434` / `52C6E860...0C588` / `34BAD630...C6DCA` / `B15EDF3C...992E`。
- 146–191 共用模板错误地把嵌套作用域型 `$TICKET_*` 参数作为 Route C 回执变量值。最小修复在 full guard 成功后复制当前 case 的权威五元组；A/B、资源账本、业务对象和严格 guard 均不变。25 个 lifecycle shard 已由生成器重建，核心运行时 normal/`-O` 各 `65/65`、恢复器各 `6/6`、经理中断各 `3/3`、release test `9/9` 与 1,031-file reproducible build GREEN，manifest / ZIP 为 `18231A80...6743` / `CBD576C2...E6CA`。fresh R289 前只记 `static-ready`。
- PID `38352` 与 relay 已受管回收，CK3/injector 槽为空。业务、阶段、T1/T2 与宣传计数不变；下一工作包是提交/push 后以 fresh R289 验证 `.146` 旧签名归零并继续 full-tree。

## R289 fresh 启动与 EP1 exact interrupt

- Route C 修复 `1afcc7a` 已推送。fresh R289 的 1,031-file product tree / projection / release manifest / ZIP 为 `0F314BCB...17FBC` / `1018A7B5...5435` / `B0F6CA33...66AAD` / `CBD576C2...0E6CA`，verify 与 no-launch preflight `D3DA088B...31F9D` GREEN。final PID `122132` 已到 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox。
- 时间线关闭 debate、ep3 decision 与 death-management 后，在原版 `ep1_flavor.0021` `205/53215344` 零输入停止并写出 86,280,051-byte checkpoint `0D1095DA...DCB1C`。source/live 冻结 root `32904`、rival title、动态 rival/nitpicker 两个互异 Character 与三项 native `(0,1,2)`；选择无随机 duel、无十年 modifier/后续 event 的正面 authored/native `2/1`。manager tests 双模式各 `16/16`、checkpoint runner 双模式各 `77/77` GREEN，健康 PID 保留待 generation `2`。
- 本轮尚未触发 `.146`，所以五个旧签名为零不能把 Route C 修复升级为 live。所有业务、阶段、T1/T2 和宣传计数不变。

## R289 generation 2 疫病医师分支

- EP1 合同已在远端合法 `9de69ce` 增量上 rebase 并以 `4224c9b` 推送，未 force-push，用户 runtime 脏文件由 autostash 原样恢复。generation `2` exact 关闭 `.0021`、debate、`.2` 与 `.40`，88 次增量 runtime scan blocking diagnostic 为 `0`。
- `epidemic_events.1100` 仅 option projection 从 R151 无医师 `(0,2)` 漂移到 R289 有医师 `(0,1)`。原版 source 证明 1/2 为互斥的已有医师/寻找医师后续链，native 0 在两形状中始终是无后续事件的最小路线；合同只增加这两个精确 variants。manager 双模式各 `16/16`、checkpoint runner 双模式各 `77/77` GREEN；report / entry / diagnostics 为 `FB489BBC...8C810` / `E7FB583A...150B` / `BDD68D50...E9DDB`。PID `122132` 健康保留，业务计数不变。

## R289 generation 3 疫病替罪羊分支

- `37a23aa` 已推送；同 PID generation `3` exact 关闭医师存在的 `.1100`、ep3 decision 与 adultery，104 次增量 runtime scan blocking diagnostic 为 `0`。随后在 `epidemic_events.1060` `212/53225568` 前无输入停止并保存 87,832,911-byte checkpoint `2169F03A...EF7C`。
- exact frame 为 root `32904`，三个 scope 分别是两项 `epidemic` alias 与一项 `story`，snapshot 原生三项、完整可见投影 native `(1,2)`。1.19.0.6 source 证明 plague-witch-hunt story 在 immediate 已创建；隐藏 native 0 有极高虔诚门槛，native 1 允许猖獗审判，native 2 减缓审判且不触发额外 event，故冻结并选择 authored/native `3/2`。manager 双模式各 `17/17`、checkpoint runner 双模式各 `77/77`、durable-checkpoint `3/3` GREEN。report / entry / diagnostics / checkpoint artifact 为 `D3C24AC6...01996` / `BACF1020...7D42` / `FF07C01B...5445` / `D6E9909D...008F`；业务计数不变，PID 健康保留待 generation `4`。

## R289 generation 4 民粹派系分支

- `f5d3fdd` 已推送；同 PID generation `4` exact 关闭 `.1060` 与 tgp movement，63 次增量 runtime scan 无 blocking 或非阻断 warning。随后在 `faction_demand.1001` `214/53229048` 前无输入停止并保存 88,162,779-byte checkpoint `24025151...D86F0`。
- exact frame 为 root/target `32904`、动态 peasant leader 与 faction/two-title 五 scope，snapshot 四项、完整可见 native `(2,3)`。1.19.0.6 source 证明 native 2 当场割让目标/破坏 realm roster，native 3 拒绝并开战但保留选择时的当前头衔结构；合同选择 authored/native `4/3`，由既有 gameplay surface 继续观测战争，不扩展宗教专用树。manager 双模式各 `18/18`、checkpoint runner 双模式各 `77/77` GREEN。report / entry / diagnostics / checkpoint artifact 为 `BBC5A78A...98E9` / `D834E9AB...07B6` / `7537209B...AFA8A` / `20E692FF...EC91`；业务计数不变，PID 保留待 generation `5`。

## R289 generation 5 朝贡终止通知

- `7a0469e` 已推送；同 PID generation `5` exact 拒绝派系要求并启动战争，16 次增量 runtime scan 无 blocking/warning。随后在 `char_interaction.0370` `215/53229720` 前无输入停止，checkpoint 为 88,098,259 bytes / `D664E0FC...A491`。
- exact frame 为 root/recipient `32904`、动态 actor 与三个 unavailable character slots，snapshot 两项、可见 native `(0)`。1.19.0.6 source 证明 tributary 已在 immediate 结束；retaliation 条件为假时 native 1 隐藏且 native 0 的合法性损失也不执行，故合同选择该无进一步 mutation 的 authored/native `1/0`。manager 双模式各 `19/19`、checkpoint runner 双模式各 `77/77` GREEN。report / entry / diagnostics / checkpoint artifact 为 `F0209094...023A` / `54472F24...525A` / `C952CE15...49D1` / `B0B6E990...B623`；业务计数不变，PID 保留待 generation `6`。

## R289 generation 6/7 省份请愿 alias

- `7817320` 已推送；generation `6` exact 关闭朝贡通知，60 次增量 runtime scan 无 blocking/warning，随后 `.0101` 仅 province recipient alias 关系失败且未选择，PID/restart `122132/0` 健康保留。generation `7` 只读直连确认 petitioner/recipient 同为 `30938`、other member 为 `28590`、七 scope 与三 option 稳定。
- exact-build `.0100` option A 明确将 petitioner 自己写入 `province_change`，B-D 才写三个 movement member，`.0101` 物化该值；合同因此只把 petitioner 加进完整 source-authored alias 集，任意第三方仍拒绝。checkpoint runner normal/`-O` 各 `77/77` GREEN。generation 6 report / entry / diagnostics 为 `18112CA6...C46C` / `FC7BFB93...F5AA` / `B96CEC3A...E0B2`；业务计数不变，健康 PID 待下一 reconnect。

## R289 generation 8 省份请愿 participant 组合

- `92ed519` 已推送；generation `8` exact 关闭 petitioner-self `.0101`，第 2 次 occurrence 随后以 disciple+other/no-house 八 scope 形状停在未选择边界，49 次增量 runtime scan 无 blocking/warning，PID/restart `122132/0` 健康保留。
- `.0100` 对三个 member 类型独立查询且 other 有独立 fallback，故该组合为源码明确 cross-product 成员；枚举只补这一项，次数仍上限 2、完整 names/types/alias/options 不放宽。checkpoint runner normal/`-O` 各 `77/77` GREEN。generation 8 report / entry / diagnostics 为 `8D89FBDD...7AED` / `F6595272...A359` / `ABC79B27...A8D90`；业务计数不变。

## R289 generation 9 疫病施舍提案

- `c143067` 已推送；同 PID generation `9` exact 关闭第 2 次 `.0101` instance `221/53235864`，34 次增量 runtime scan blocking diagnostic 为 `0`，只有一条 situation participant removal 原版引擎非阻断 warning。随后在 `physician_epidemic_events.1040` `222/53237808` 前无输入停止。
- exact frame 为 root `32904`、动态 physician 与两个 epidemic alias 三 scope，native options `(0,1)`。1.19.0.6 source SHA-256 `51ADEEA52F9A93406156ABAFA6608B0425003F63098F0CB033A5CB515C90F493` 证明 native 0 写十年染疫伯爵领 modifier 并执行 5% 疾病感染判定，native 1 只有少量虔诚损失和特质压力，无 modifier、感染 RNG 或后续事件；合同选择 authored/native `2/1`。manager 双模式各 `20/20`、checkpoint runner 双模式各 `77/77`、`py_compile` GREEN。report / entry / diagnostics / checkpoint artifact 为 `953D3EDC...81EF` / `B6C58FF7...421D` / `4D15F63A...E856` / `A9ECB8EF...747D`，checkpoint 为 88,883,657 bytes / `619CB89A...0CF2`。业务与宣传计数不变，PID 健康保留待 generation `10`。

## R289 generation 10 民粹领袖动态头衔形状

- `508ca13` 已推送；同 PID generation `10` exact 关闭疫病施舍 instance `222`，11 次增量 runtime scan 无 blocking/warning，随后第二次 `faction_demand.1001` `223/53239560` 仅因多出 `new_title` 停在未选择边界。只读 generation `11` 直连确认原五项身份与 native `(2,3)` 均稳定，新增项精确为 `new_title: landed_title`。
- 1.19.0.6 `setup_populist_leader_effect` 在新选领袖无 primary title 时通过 `create_dynamic_title` 产生该 scope，已有头衔时不产生；合同只列举这两个完整集合并检查可选项类型。manager 双模式各 `20/20`、checkpoint runner 双模式各 `77/77`、`py_compile` GREEN。generation 10 report / entry / diagnostics 为 `FC1DB1A1...D947` / `42CB837E...07A1` / `BAE99D10...CB5C`；业务计数不变，健康 PID 待下一产品 reconnect。

## R289 generation 12/13 EP3 动态 vassal 关系

- `0735699` 已推送；generation `12` exact 关闭动态头衔派系事件，再关闭 EP3 决议、军事援助与国库俸禄事件。34 次增量 runtime scan 无 blocking，只有一条原版 participant-removal warning；随后 `.2211` `227/53244264` 仅因 vassal 从 R250 的 `27275` 漂移到 `28662` 停在未选择边界。
- 只读 generation `13` 与 1.19.0.6 source 共同证明 `.2210` 将动态 root 保存为 vassal、将 liege 保存为响应者；合同因此改为 liege=玩家、vassal=非玩家第三方，其他三 scope、native `(1,2,3)` 与 terminal native 3 路线不变。EP3 focused 双模式各 `7/7`、checkpoint runner 双模式各 `77/77`、`py_compile` GREEN。generation 12 report / entry / diagnostics 为 `0BCD0E43...9465` / `FB61F84F...7504` / `80E3C760...10C7`；业务计数不变，PID 继续保留。

## R289 generation 14/15 草药香囊报价

- `b4adcea` 已推送；generation `14` exact 关闭动态 vassal `.2211`，再关闭 debate、省份请愿和国库事件，45 次增量 runtime scan 无 blocking/warning。未知 `.5009` `231/53254032` 随后无输入停止并物化 90,401,985-byte checkpoint `7006EDA2...21BCF`。
- generation `15` 与 1.19.0.6 source 冻结 11 scope、merchant=owner=creator、snapshot 4 / rendered native `(1,2,3)`。前三条路线会转移神器、付款或写长期 modifier，native 3 什么也不买且无后续 event；合同选择 authored/native `4/3`。manager 双模式各 `21/21`、checkpoint runner 双模式各 `77/77`、durable checkpoint `3/3`、`py_compile` GREEN。generation 14 report / entry / diagnostics / checkpoint artifact 为 `4F67B170...02CD4` / `CD8BA0B6...947B7` / `16486F2A...FE837` / `0340B95E...4449D`；业务计数不变，PID 保留待 generation `16`。

## R289 generation 16/17 语言学习结果响应

- `5935fa8` 已推送；generation `16` exact 关闭香囊事件，16 次增量 runtime scan 无 blocking、只有一条原版 participant-removal warning。未知 `.1001` `232/53257296` 随后无输入停止并物化 90,842,473-byte checkpoint `EF8597C8...8B928`。
- generation `17` 与 1.19.0.6 source 冻结 root/target、动态 scheme owner、scheme/artifact/success flag 和 native `(0,1)`。两路均无后续 event；native 0 走尊重/友谊进度，native 1 走负评/敌对进度，故合同选择 authored/native `1/0`。manager 双模式各 `22/22`、checkpoint runner 双模式各 `77/77`、durable `3/3`、`py_compile` GREEN。generation 16 report / entry / diagnostics / checkpoint artifact 为 `CA226B2E...2729B` / `53C6E448...B4B1A` / `D0AC6BFD...B8E7A` / `379AEAB7...33915`；业务计数不变，PID 保留。
## 计划外完成：肃清曼荼罗独立产品 1.0.0

- 独立 `mod_remove_mandala` 已以 commit `9de69ce592cad20e85ba73798af1247c2b2ff532` / tag `remove-mandala-v1.0.0` 公开发布到 Steam Workshop item [`3797711947`](https://steamcommunity.com/sharedfiles/filedetails/?id=3797711947)。主视觉、双语描述与两张实机截图已上传。
- L0、deterministic build、官方 master/tag CI、non-debug CK3 实机验收与 fresh 15-file subscription-cache 复核全部 GREEN；ID-bound manifest / ZIP SHA-256 为 `ec4733c...d1e` / `04cda5dc...0d9`，live report SHA-256 为 `6ebd3d05...6547`。
- 测试计划、验收报告、media ledger 与 initial-baseline release changelog 已永久落盘。该旁路产品不改变本周自动游玩业务、full-tree、T0/T1/T2 或宣传进度计数。

## R289 generation 18 B1 calibration 前配额漂移

- `a686714` 已推送；generation `18` exact 关闭语言学习，并继续关闭 birth、EP3 决议和年度事件。25 次产品进度观测始终是 B1 active、review/central/pp false，最终在既定 5,000-day 产品观察边界 RED；loader、map/mailbox 与同 PID/restart 保持健康，45 条增量 runtime scan 无 blocking，只有 4 条原版 participant-removal warning。
- live ledger 锁定 bank-close target `8/16/2=26` 对 calibration exact rows `8/15/2=25`；没有 unavailable prune 标记，closure/finalized/quota-valid 因此永久为零。`d4f5120` 在 calibration 第一个消费者之前只读核对当前 case 的 state-5 exact tuples，仅在任一计数漂移时放弃当前 manager 的陈旧 pooled book 并调用 canonical local allocator；完整 local/pooled 正常路径不变。
- generator、B1 normal/`-O` 各 `75/75`、local/static validation、release tests `9/9` 与 reproducible 1,031-file build 全部 GREEN，修复已推送但仍仅 `static-ready`。generation 18 diagnostics / entry / report 为 `29101A38…C38` / `3EBC54E6…656` / `9A7C686E…005`；fresh R290 才能给出 live 结论。业务、stage 与宣传计数不变。

## R290 generation 1–3 贪墨合同 GREEN 与 elimination tooltip RED

- fresh R290 的 1,031-file product、projection、release manifest 与 ZIP 为 `813be9e7…b7f` / `480ffc80…6bf` / `a59795ac…cb6` / `ab6909ac…fc`；verify 与 no-launch preflight GREEN。warmup 独立回收，final PID `165204` 达到 loader `303/303`、fatal `0`、paused/map/mailbox/bridge，restart `0`。
- generation `2` 实机验证动态 `secret_owner=embezzler=local_secret_owner` 合同，修复 `c0e583a` 已推送；report / diagnostics 为 `1A62EF79…42A` / `433ED324…CFA`。
- generation `3` 在 `zg361.5` tooltip 构建中发现 12 条真实产品 scratch-variable diagnostics。`12038bd` 用 `hidden_effect` 隔离 delayed event 与 option effect 的预览推演；B2 双模式各 `47/47`、local/static、release `9/9`、reproducible build GREEN，仍待 fresh R291 live。report / diagnostics 为 `333B96B7…707` / `2F00C38A…9D60`。
- R290 已受控回收且进程槽清零。B1 season 虽已打开但尚未到 calibration，故 quota fix 仍 live-pending；业务、stage、T1/T2 与宣传计数不变。下一工作包为 fresh R291 同轮验证 tooltip 归零和 B1 closure。

## 09 月 08 日：R291–R294 promotion source registry `1/4`

- R291 已 fresh-live 闭合 B1 calibration/closure，并保持 tooltip scratch 旧错误归零；工具 scratch 与 loader attribution 修复 `6150543` / `62ef850` 已推送。
- R293 在产品 `.146` tooltip 发现 receipt 五元组尚不存在时调用 full guard 的真实错误。`0188395` 为 46 个 consumer 加入 lazy existence guard，重建 25 个 lifecycle shard；runtime 双模式各 `66/66`、local/static、release `9/9` 与 reproducible build GREEN。fresh R294 验证该错误族归零。
- `bde61ea` 允许 production entry 从可恢复 `.146` 起步，`63337e7` 统一 capture lineage，`4d7f24e` 允许原生保存发布同一事件的新 revision 且继续拒绝事件/日期漂移；上述提交均已推送。
- R294b 在唯一 PID `150452`、restart `0` 上抵达真实 paused `.147` 并完成首个 registry capture。artifact / report 为 `75CB49FD…3AC8` / `6DA96F90…2003`，归档存档 89,342,845 bytes / `D4F625C8…319F0`，运行期 blocking diagnostic `0`。进程随后经 stop queue 完整回收。
- registry 现为 `1/4`，尚缺 projects/metrics、incidents/operations、cross-cycle/endgame；T0-P1 仍未完成，T0-P2 继续锁定。业务与阶段计数保持 T0 `50%`、strict `4/361`、definitions `106/626`、stage `8/11`，T1 `90%`、T2 `100% horizon`、总体 `67.5%`；footage `0/8`、MP4 `0/2`。

## 09 月 08 日：R300–R303 projects/metrics MCP 实机闭环

- R300 证明 `.26` 后 save/load 会丢失待投递 delayed player event；恢复链现将 pending 状态 durable 化，并可按保存的 owner、subject、cycle 与 cursor 重建精确作用域。R301 已 live 推进到 portfolio `.134`，旧 UI `.133` gate 的 RED 被确认是过严验收，不是业务未收口。
- R302 首次由 explicit-subject MCP 在玩家仍为 owner `32904` 时读取 subject `30938` 的 portfolio closure，并在同一暂停帧完成 native 换人；action-cell preflight 仅因 caller 漏传 subject 而 RED。
- R303 修复 caller 后完整 GREEN：closure date raw `53247312`，cycle/case/state `4/2/6`，conservation `1`、pending event absent；native switch `32904 -> 30938` 后，一次 `life-advance` 取得 identity `[32904,30938,4,2]`、receipt `1/3/value 1`、metrics revision `2` 与 `metric_dictionary_subject_v1`。
- report `Z:\ck3_mod_rewrite\_runtime\p2r303projectsmetrics\report.json` SHA-256 为 `926BBD25076F69205B8AAA7CCC366AB470227BCBE174017B7E86C282862D7B01`；cleanup GREEN。89,563,632-byte source checkpoint SHA-256 `7584F32E1D3556A7740FE1E11FA5AF67706A47C99A25E52E1E69213673D15870` 前后未变。
- MCP 仍为 `private candidate / default off`；projects/metrics 的 canonical schema-2 source checkpoint 尚未创建，因此 registry 保持 `1/4`。本轮不提升 strict/stage 统计：T0 `50%`、strict `4/361`、definitions `106/626`、stage `8/11`、T1 `90%`；footage `0/8`、MP4 `0/2`，T0-P2 继续 `LOCKED`。
- 专题见 [`../../phase2-promo/r300-r303-credit-project-resume-projects-metrics-live-2026-09-08.md`](../../phase2-promo/r300-r303-credit-project-resume-projects-metrics-live-2026-09-08.md)。下一工作包为 `capture_projects_metrics` schema-2 source capture。

## 09 月 08 日：R304–R313 projects/metrics source checkpoint `2/4`

- R304–R312 全部按真实结果保留为 RED：R304/R305/R308/R311 为 `cp26_provider_source_not_ready`；R306/R307 揭示 runner 对 `date_raw` 的日单位误判；R309/R310 为 post-CP26 event 暂停帧稳定化失败；R312 在 event instance `314` 越过 source bound。它们均未计作 canonical source evidence，隔离现场完成受管清理，原始 R301 checkpoint 未被改写。
- R313 在 owner `32904` 仍为 played character、source event 为 `zg361cp.26` 的原始暂停帧，以 explicit subject `30938` 首次查询即得到 available：source identity `4/2/32904/30938`、contribution receipt `1/revision 3/value 1`、portfolio `closed=0`，P3 result absent，checkpoint state `cp26_ready_p3_absent`，从 UI/source 帧到 capture 的延迟为 `0` 天。随后 native rebind 到 subject 并写出真实 checkpoint，projects/metrics readiness 升为 `captured-real-checkpoint`。
- captured checkpoint 为 89,548,228 bytes / SHA-256 `72FB7D0F04C8B584555C35AC87313A5581FA8610344F72ABA4758904BC4C433B`；projects/metrics registry SHA-256 `7F80326DA8B0EBBBCEE26DE21E2A55A6F74AAE989D567F9B8909BD1F7B3190DE`；两项累积 artifact SHA-256 `8128750541EE7683EAB5CAD83CFDAF11FCCE47F8017019E3B5541A76F1AD603A`，readiness 为 `captured-real-two-of-four-sources` 且 `canonical_registry_ready=false`。
- R313 report / cleanup SHA-256 为 `9225D94ABCA8E47AFF0D2CBB3BE51785E28F5C0781F412F4B1D917B13046E76F` / `F8B9D5E99DC9234DB7FFD1B256ACE2EB3C33C0CEC2BF15CF3DF18E58E4D74F8E`；cleanup GREEN、failed checks 为空，最终进程槽清零。原始 R301 source checkpoint 前后保持 89,563,632 bytes / `7584F32E1D3556A7740FE1E11FA5AF67706A47C99A25E52E1E69213673D15870`，`original_source_checkpoint_unchanged=true`。
- canonical source evidence 现为 `2/4`，下一项是 incidents/operations `3/4`。业务与阶段口径保持 T0 `50%`、strict `4/361`、definitions `106/626`、stage `8/11`，T1 `90%`、T2 `100% horizon`、总体 `67.5%`；footage `0/8`、MP4 `0/2`，T0-P2 继续 `LOCKED`。实现与报告 commit `4eec135540f51ed2f51879a8aa49a0ae3bcf789f` 已普通 fast-forward push 到 `origin/master`。

## 09 月 08 日：R314–R326 incidents/operations source checkpoint `3/4`

- R314–R323 的 A400 续跑从未产生 `zg361.50`，全部按真实结果保留为 RED。R324 改从共同祖先 `8E6C` 的 R106 直接玩家分支推进，于 raw `53156952` / instance `43` 抵达未选择的 `zg361.50`，并实证 native save 令 revision/native revision 精确各 `+1`、其余事件绑定不变。
- R325 gameplay/save/cleanup 虽 GREEN，但 wrapper 误标 A400 lineage，明确排除。R326 以正确 8E6C lineage 重跑：owner/player `28598/29037`、revision `118 -> 119`，73,156,969-byte checkpoint SHA-256 `5E9A7687AFAF104CB6B68759F2960BEF85A886EF1908382C50528A5D17BAC550`；原始输入 `76E4FC13825C5A5A654CCD6F321808F627F9A47054C76DDA172B2733CC04884B` 未改写，cleanup GREEN。
- schema-v3 联合来源用每个 handler 自己的 lineage，移除错误的顶层单 seed 假设；promotion/projects/incidents 分别来自 A400/A400/8E6C。三源前缀 SHA-256 `0828ED6F8BD2364145506ED452394B62570F837374086AEE8847420F9D339644`，lineage set `zg361-phase2-lineage-set-7970eb0abf9fda090b5a71d2e504bdf13933dd7e5ba12b7678e504341b2ecc34`；composer、preflight、runner、registry、provider 与 incident seams 的 normal/`-O` 回归全部 GREEN，schema-v2 保持兼容。
- canonical source evidence 现为 `3/4`，仅剩第三 workforce 周期的 `capture_cross_cycle_endgame`；下一 CK3 串行工作包从 checkpoint `C7E5CA119F4DB4D9BFE738859DD5E32DEC6F9F1F804D91F9B26ED5D256E219DA` 完成 stage 9–11 与三周期成熟度证明。T0 `50%`、stage `8/11`、T1 `90%`、T2 `100% horizon`、总体 `67.5%` 与 footage `0/8`、MP4 `0/2` 暂不变；`strict 4/361`、`definitions 106/626` 仅为非阻塞 backlog。T0-P2 继续 `LOCKED`。

## 09 月 09 日：R353–R368 cross-cycle/endgame 继续推进

- 第四类 source 长时间线已采用同一 CK3 PID 的严格热恢复：随机原版事件只在
  未知帧选择前 fail-closed，合同审计和 normal / `-O` 回归在游戏暂停期间
  外部完成，然后连接同一 event instance 继续。R368 截至当前保持 PID
  `201320`、generation `1`、restart `0`；合同变化不再造成不必要的 CK3 重启。
- 本周新增闭合了宦官人贡/秘密/密谋、重复辩论、动态 court/health/epidemic/
  EP3/birth/GHW/chaplain 等原版形状；R368 又闭合重复朝贡、两类 grief
  no-confidant 投影、两项 secrets 通知、epidemic-aware `health.2201` 与
  `diarchy.8042`。所有未知帧均保留 RED 且 `selection_attempted=false`，已重跑
  帧均以 instance advancement 验证。
- 产品树的两项真实 RED 已在源生成器最小修复：policy audit 未设置 case
  kernel flag 时改走 lazy else，Career/Learning digest 的 loc value 改用
  `.GetValue`。静态生成、九语言结构、manager `52/52`、career `38/38`、
  copy `6/6` 双模式均 GREEN；fresh R369 实机归零尚未完成，不能标 live。
- 原生 promotion progress 观测从 5 项扩展为 29 项，中央 stage 9、Workforce
  route/rehire fact 与 P3 tooltip 守卫同步补齐；native bridge `102/102`
  GREEN。当前 interrupt 回归为 secret `6/6`、health `12/12`、manager
  `54/54`、checkpoint `88/88` normal / `-O` 全绿。
- 本周 readiness 暂不抬高：source `3/4`、T0 `50%`、stage `8/11`、T1 `90%`、
  T2 `100% horizon`；`strict 4/361`、definitions `106/626` 继续保留为非阻塞 backlog；
  T0-P2 继续 `LOCKED`。下一硬结果是 fresh R369 消除两项产品 RED 并完成
  `capture_cross_cycle_endgame`，随后再进入完整 P1 验收矩阵。

## 09 月 09 日：R369 fresh product 与跨仓同步增量

- R368 在 `10190` 天边界未产生第四类 source，按 bound-exhausted RED 收口并
  受控回收；fresh R369 已加载修复后的 product tree，PID `144008`、generation
  `1`、restart `0` 持续健康。当前日志未再出现 policy-audit unset flag 与
  Career/Learning bare `ROOT.Var` 旧签名，但 source capture 仍未完成。
- R369 同 PID 热恢复闭合 admin-eunuch、measles 与 epidemic treatment-result
  形态；health focused `14/14`、checkpoint `88/88` 双模式 GREEN，修复均已
  rebase-only 推送。当前严格停在未选择的 `health.1112`，合同修复继续不重启
  CK3。
- G2 provenance 9 文件已刷新并通过 focused `80/80`、internal `111/111`、
  Raiktor `174/174` 双模式及 14 项 no-launch preflight；commit `e7191c4` 已推送，
  T1 保持诚实 `90%`，下一 live 工作包等待 CK3 串行槽。
- `open_kaishek` 已把 promotion source profile 从 5 个固定 widget 同步为父仓
  完整 29 项，并在父仓 verifier 补上真实 artifact 哈希/列表核验，消除冻结
  fixture 自洽却漏报父仓漂移的盲点。伴随仓 `8b0d159`、父仓 `1e5ffd2` 已普通
  push；7/7 双模式、伴随仓 156/156、CLI smoke 与 require-clean audit GREEN，
  T2 当前兼容地平线恢复 `100%`。
- 当前总体计数不变：source `3/4`、T0 `50%`、stage `8/11`、T1 `90%`、
  T2 `100% horizon`；`strict 4/361`、definitions `106/626` 是非阻塞 backlog；
  T0-P2 `LOCKED`。

## 09 月 09 日：R369 stale-revision 收口与 R370 续跑

- R369 补齐麻疹康复 `health.1112` 与 infidelity-story `secrets.0108` 合同后，
  因 promotion progress revision stale 的未登记 transient marker 退出；bridge
  fail-close 正确且无输入发生。`4afcf2c` 增加精确受限重试，checkpoint runner
  normal / `-O` 各 `88/88` GREEN。自动存档 109,299,250 bytes / SHA-256
  `E332285A…E2F` 被诚实标为 fresh-resume autosave，不冒充显式 durable checkpoint。
- R370 从相同 bytes 启动唯一 PID `62408`、generation `1`、restart `0`。
  同 PID 热恢复已经越过原版 `secrets.0112` 的 16-scope adultery-carry 变体和
  产品 `zg361ip.190` X 案空操作结案回执；提交 `687ef3f`、`c1ce37b` 均经
  rebase-only 普通 push。前者保持旧 11-scope 形态并增加 exact variant，后者
  将 `.190/.290/.390` 绑定为结果 subject + 唯一 native-0 的 repeatable 回执。
- 当前未产生第四类 source，故计数继续保持 source `3/4`、T0 `50%`、stage
  `8/11`、T1 `90%`、T2 `100% horizon`；`strict 4/361`、definitions
  `106/626` 只作为非阻塞 backlog 续记；
  T0-P2 继续 `LOCKED`。R372 仍持有 CK3 串行槽并自然推进。

## 09 月 09 日：R372 shared-registry production-live 首例与连续合同

- 根仓 `fc84f4c` 后，R372 在同一 PID/session、instance `668` 的 `TGP0160`
  上通过共享 registry 解析并真实 drain/advance，首次把 registry → MCP →
  production runner 的证据边界从 `static-ready / integrated-tested` 提升为
  `production-live primitive`；该单例不代表全 registry breadth。
- 后续 instance `670` 的 `great_holy_war.0011` 在选择前触发 occurrence-bound
  RED，未提交输入且保留 PID。exact-build 原版定义证明它是 repeatable，最小修复
  只移除旧 `max_occurrences=1` 约束，并把分析发布为共享 registry/MCP 资产；
  `081ca93` 已推送。同 PID/session 热重跑通过并继续，没有重启 CK3。
- `TGP0020` 随后在选择前暴露下一条通用合同缺口；`09f7b43` 已推送。
  retry-7 的七项 hot-recovery 身份检查全部为 true，
  `process_restart_required=false`。同一 CK3 PID `28772`、connection generation `1`
  在 driver command `2164` 执行 `select-event-option-3`，返回 `ok=true`；旧 instance
  `777` 已 advance 为 null，选择 authored `3` / native `2`，且
  `postcondition_verified=true`。同一会话随后继续到至少 command `2272` 并处理更多
  事件，因此 `TGP0020` 为 production-live GREEN。
- 冻结 artifact 为
  `_runtime/p2r372-post-bound-continuation-live/driver-state-hot-retry-7-snapshot.json`，
  `9,603,158` bytes，SHA-256
  `07E292E84DCD56E1917EDF6043D2138A0FCD1F8ADC6EFBF028CCA8C1FD4BDA65`。
  测试迁移全集 normal / `-O` 各 `60 passed, 271 subtests`，parity 各
  `5 passed, 6 subtests`，manager 各 `54 passed, 1 skipped, 4 subtests`，static
  校验 GREEN。
- 六个互不冲突的历史分析分片已并行迁移并由共享 MCP 统一接入；连同现有
  TGP/GHW exact-build 记录，合同 `157/157` 均有 JSON-safe 分析元数据，缺失、额外、
  重复键均为 `0`。旧材料没有记录源码 hash 时保留 migration-only 边界，不制造证据；
  代码提交 `23049d9`。

## 09 月 09 日：独立产品《重整河山》0.1.1 正式发布

- `mod_reclaim_the_motherland` 完成游戏规则、动态后朝、尊王派直属封臣树、
  “宣称天命”封锁与“宣称复辟”全链。个人领地以原直辖伯爵领逐项
  `holder = root` 验证；全新 Workshop 订阅缓存 28/28 严格核对后，同矩阵
  MCP-first L3 为 19/19 GREEN、diagnostics 0。
- 最终 run `rqa_workshop_20260909_1952_capital_3798404599_v011` 的
  wrapper/cell SHA-256 为 `6D5AD36C…13C6AB` / `5859E13C…38E30`。
  原生 MCP 将相机定位到 `b_kaifeng` 并证明 settled/postcondition；三张发布截图
  均来自该 run，开封/管城县视觉证据命中，意大利等错误地域负词未出现。
- Workshop item
  [`3798404599`](https://steamcommunity.com/sharedfiles/filedetails/?id=3798404599)
  已公开，匿名接口 `result=1`、`visibility=0`；BBCode、thumbnail、三张真实
  游戏截图与艺术资产均入库，并将 `Crusader Kings III: All Under Heaven`
  登记为必需 DLC，中文全仓统一为《溥天之下》。
- 本独立产品的 acceptance plan/report、截图 ledger、发布交接、正式构建身份、
  tag 与 initial-baseline changelog 构成发布闭环。宣传片目前只完成 85 秒用户
  审核稿，不把剧本或自动检查冒充成片。

## 09 月 09 日：R372 registry 159 项与跨周期产品合同热恢复

- `039a509`、`e6ab3d4`、`f6e2d3f`、`6aa1cf5`、`ab1f15b`、`edfb0db`、
  `264e4c0`、`d0051ce` 均已 push，且 Official Runner CI 均为
  `completed/success`；最新 `d0051ce` run/job 为 `34368907153` /
  `102524733797`，失败步骤为空。shared registry 保持
  `159 contracts / 159 analysis / 7 observations`，production runtime 合同为
  `298`；后四项仅改变 T0 内部产品 occurrence 合同，没有改变 MCP、public
  schema、bridge/native ABI，T2 因此为 `NO-CHANGE`。
- 唯一 PID `28772` / connection generation `1` 已原位热恢复
  `epidemic_events.1064`、十年周期重复的 `epidemic_events.5009`、新 review
  portfolio 的 `zg361ch.950`、新 review value-track 的 `zg361.30`，以及
  `zg361ch.21` 所代表的 51 项 Career/HC 明确事件族。所有真实失败均先冻结
  RED；纯 Python 合同修复后从原 event instance 重试，没有重启 CK3。
- attempt `16` 在 `zg361pp.146` instance `900` / date raw `53489184` 保留
  第二次合法 case-card RED；前次同键为 instance `706` / date raw `53448336`。
  exact generator/caller 审计把最小族冻结为玩家可见 `146..191 + 9001..9004`
  共 50 键，明确排除 subject 回执、hidden audit、deadline 与独立 `9100`
  batch-policy card。`d0051ce` 保留 root、option subset、completion scope 和安全
  路线，仅移除错误的一次性 occurrence 上限；normal/`-O` 主合同各 `60/60`、
  完整 manager-recovery 各 `114/114`、静态校验均 GREEN。
- `.146` 冻结 report / driver-state / park SHA-256 分别为
  `B1A9EC63B9BD9BC2E10AEE2DD4C61B049F8B3C1DC79B9D7E87963DEBA571A1B7`、
  `144AFC4AB9495BCAC53BB14951BDC00478D3F85E47948AABB57FCFFC38B4ADBB`、
  `DFCF1BBBF9A5EAF65E7D1B423CDFED883E7B4455C6C85A48D9E9B4F61BD65246`。
  hot retry `16` 已越过该卡及其后续事件，driver-state 继续写入。
- 周进度仍为 T0 `50%`、stage `8/11`、canonical source `3/4`，P2 硬锁；
  T1 `90%`，`READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE` artifact SHA-256
  `57DA12DFF80E4640DD9BF88895933DC56BD780034A70378BB60DF664869A9E15`
  已 ready、等待 CK3 串行槽；T2 `open_kaishek` commit `9fa5c93` 继续与
  `159/159/7` 兼容。

## 09 月 09 日正式收口增量

- P1 账本已由 `59ec52d` 统一为 T0 `50%`、stage `8/11`、source `3/4`、P2 `LOCKED`，并按 owner-approved
  四门记录真实剩余项；其 Official Runner run/job `34371493257` / `102533564765` GREEN。
- formal live 的 `base → scoreboard → workforce → promotion` PID/session lineage 已由 `34cdaa7` 纳入 Official Runner CI；
  run/job `34372045738` / `102535430053` GREEN。current-HEAD 第四 source 无启动预检 artifact SHA-256
  `FE5BF42B8CEB7E213CE0B95DBFFCEE8240D65F016D316311747C9B426E63FC0C`，normal/`-O` 各 `27/27`。
- R372 在同一 PID 上实证 `zg361we.356` 已有两次独立 occurrence，只差第三次。23:59:52 固定 6 小时 supervisor timeout
  到点后主动回收 CK3；最后 heartbeat `date_raw=53491080`。进程树 cleanup 已证明，旧 runner 因要求 `exit_reason=stop` 与
  final pipe connected 保留 harness RED。冻结 report / driver / cleanup SHA-256 为
  `62D2D0F79B11F928DBB1DA3AE05C630F05B9BAA351921CA15B399552627FCAD0` /
  `C154CA44A69AE5211C36F4EC9F05D002B850C67AA845A36CD891FA848C630633` /
  `2EFF46B12030748D5ACC88291BFD2C8A6B20C3F9F1E8FD10F8C3D1525E383704`。该项是 harness RED，不提升或降低 T0 产品百分比；
  下一轮从最新合法 autosave 恢复并先修复长跑 timeout/cleanup 合同。

## 09 月 10 日：D1 cleanup 分类修正与 R373 准备

- D1 timeout/cleanup 合同修复 commit `f766a58` 已 push；Official Runner CI run `34375813001` / job
  `102548196817` 约 4 分钟完成，结论 `GREEN`、失败步骤为空。
- 新逻辑回放冻结的 R372 实证报告后，结果为 `cleanup_only GREEN`、`failed_checks=0`；会话仍为 `TIMEOUT`，产品仍为
  `INCOMPLETE`，且 `resume_required=true`。该结果修正的是 harness/cleanup 分类，不把超时会话或未完成产品提升为 GREEN，
  也不改变 R372 原始 artifact。
- R373 正从冻结 autosave 准备唯一 continuation；本轮采用有限 `24h` lease，且不续延产品绝对 deadline。当前 CK3 与 control
  进程均为 `0`，准备态不计作 live。
- T0 保持 `50%`、stage `8/11`、canonical source `3/4`，T0-P2 继续 `LOCKED`。T2 为 `NO-CHANGE`：共享资产仍为
  `159 contracts / 159 analysis / 7 observations`，production runtime `298`；本轮没有公共 MCP/schema/bridge ABI 变化。

## 09 月 10 日：R374 `.7031` 热恢复与 `.1001` 新 RED

- `natural_disaster.7031` 的共享合同、analysis 与第八条 observation 已由 commit `d7435cf` push；共享资产增至
  `160 contracts / 160 analysis / 8 observations`，production runtime 增至 `299`。natural-disaster 专项 normal / `-O`
  各 `12/12`，registry/MCP selected tests normal / `-O` 各 `29/29`。Official Runner CI run `34383321634` / job
  `102573260244` 在约 4 分 13 秒内 completed/success，失败步骤为空。
- `.7031` 动作前 RED 的 immutable report / driver-state / park SHA-256 分别为
  `7B1DB1CB63CD309288FAA7774E122D74FE16E11CF3B0D6E51EB112949C21D4E9`、
  `8A63BBB6A611508B782335B4384A6F95EC189A0D7353E5C3E21EC22F8940011D`、
  `86F39D782D159FB58278726255E0862254C221E54882D2B0C8A69BACA964FE57`。
- R374 在同一 PID `51852` / connection generation `1` 上选择 reviewed authored `3` / native `2`；instance `978`
  已由独立后置条件验证 advance。没有重启 CK3，`.7031` 因此仅将这一条实证切片提升为 production-live GREEN。
- 长跑随后在 `ep3_story_cycle_admin_eunuch.1001` instance `988` / `date_raw=53513184` 动作前 RED：raw option count `2`，
  现有合同允许 6 个 scope，真实 paused frame 则有 20 个；尚未选择按钮。距离产品绝对 deadline 尚余 `5113` game days。
  park2 immutable report / driver-state / park SHA-256 分别为
  `FAEFEF7A2F09099A697CE4A0215AECF6CCE080013D2822BBFD65BBB8B38B7116`、
  `446605FC4AB05D1DDED330929E57B2D04A73E91A601E58B4320D6E2037ED9548`、
  `F2265333D99B4E996CBAAFD5F31C616291E03438A976E3D85CD4262D4C9889D0`。该事件继续记为 ongoing RED，不能冒充 GREEN。
- T0 仍为 `50%`、stage `8/11`、canonical source `3/4`，T0-P2 继续 `LOCKED`；本轮 registry/runtime 增量不改变
  产品完成百分比或视频硬门禁。

## 09 月 10 日：R374 `.1001` 热恢复与 tribute park3 RED

- `ep3_story_cycle_admin_eunuch.1001` package commit `a472965` 已按 rebase-only push；primary admin normal / `-O`
  各 `7/7`、recovery normal / `-O` 各 `60/60`、registry/MCP selected normal / `-O` 各 `28/28`，全部 GREEN。
  Official Runner CI run `34386079583` / job `102582565979` 约 4 分 28 秒 completed/success，失败步骤为空。
- 共享 inventory 保持 `160 contracts / 160 analysis`，observation keys 由 `8` 增至 `9`，production runtime 保持
  `299`；MCP schema 不变。
- R374 在同一 PID `51852` / connection generation `1` 上选择 `.1001` authored `2` / native `1`；旧 instance
  `988 -> null`，独立 postcondition GREEN，因此 `.1001` 成为第七条 production-live event primitive。单条 GREEN 不外推
  为 160 条均 live。
- 长跑随后在 `tribute_mission.1005` instance `1007` 的 rejected-eunuch shape 动作前 RED，尚未提交选择。park3
  immutable report / driver-state / park SHA-256 分别为
  `65D685D64EC4016E952A8F439A40F0406A9AFBFF459340CA71AD8564B90063DD`、
  `02CCAA03AD0549CD69708209A39E63E92CB1F57CD342AA428AD41C9E04BEDB1F`、
  `381858FACBAD07092D4876F59D85CAC69607B497A108872F361AC0C078724FCE`。该事件继续记为 ongoing RED，不冒充 GREEN。
- exact-build 审阅确认这是 `.1002` 拒绝宦官贡礼后进入 `.1005` 的原版精确分支；共享合同只新增该 16-scope shape，
  同时移除旧人物/date 绑定并改用 `$player`，安全选择保持 authored `6` / native `5`。focused normal / `-O` 各
  `8/8`、manager recovery normal / `-O` 各 `60/60`、registry/MCP selected normal / `-O` 各
  `37/37 + 42 subtests`，均 GREEN。
- 共享 inventory 现为 `160 contracts / 160 analysis / 10 observation keys`，runtime `299`；MCP 仍是 schema v1。
  open_kaishek 无需代码同步，其兼容模块 29 tests / 0 failure。
- T0 仍为 `50%`、stage `8/11`、canonical source `3/4`，T0-P2 继续 `LOCKED`；本增量不改变产品完成百分比或视频硬门禁。

## 09 月 10 日：R374 `.1005` 同 PID 热恢复 GREEN

- `.1005` package commit `ed25bd4` 已按 rebase-only 推送；Official Runner CI run `34388489589` / job
  `102590688933` 在约 4 分 10 秒内 completed/success，无失败步骤。
- R374 保持 PID `51852` / generation `1`，live driver command `732` 选择 authored `6` / native `5`；old/new
  instance `1007/null`，snapshot `native:998 -> native:999`、revision `999 -> 1000`，
  `postcondition_verified=true`。因此 `.1005` 成为第八条 production-live event primitive。
- park3 动作前 RED 继续作为 observation 保存，但阻塞已经闭合；长跑恢复，继续等待第三次 `zg361we.356` 或
  下一个真实 RED。T0 仍为 `50% / stage 8/11 / source 3/4 / P2 LOCKED`。

## 09 月 10 日：R374 Central summary park4 RED

- 长跑在 `zg361p2c.2` instance `1035` / date `53578992` 第三次跨周期 summary 处 park4；唯一失败是旧
  `max_occurrences=2`。前两次 retained GREEN 分别为 `53438688/674`、`53479536/859`，本次 selection 尚未尝试。
- exact-source 与 debug log 证明该 summary 来自冻结 central tuple 失效后的 typed-RED terminal；它是本周期的诚实失败
  收口，不能冒充成功 cycle，但唯一 authored1/native0 只是清理 `summary_pending` 并消费 pending annual，可安全确认。
  实机日志锚点为 `profile/logs/debug.log:84341`；权威生成器 SHA-256 为
  `BC12BCDDF73E345B90DB2521B346495BD4D1627D5DAF7ECCEF5BFAD46F5182A2`。
- 最小合同修复改为 `repeatable-within-product-observation-window`，不把历史样本上限机械提高到 3。manager recovery
  normal / `-O` 各 `60/60` GREEN；不修改产品脚本、shared vanilla registry、MCP schema 或 native ABI。
- park4 report / driver / park SHA-256 分别为 `21E317707214B52D787226F31B01A26C1372D924AD12E40389C22FDA61A4AF71`、
  `FBAF8DBB359C73F76D01F54C4FFB37B834E51DC4643B7C64B4DCDA6778910BB6`、
  `49E8714A20465392B3F498D1D05F1C59DA9365B1185648E9813E0142FD94F22B`。PID `51852` / generation `1` 保持可热恢复。

## 09 月 10 日：R374 park4 热恢复与 `.0040` park5 RED

- `.p2c.2` repeatable 合同 commit `2ba288b` 已按 rebase-only 推送；Official Runner CI run
  `34391028529` / job `102599080015` 在约 4 分 16 秒内 completed/success，无失败步骤。
- R374 保持 PID `51852` / generation `1`，选择 `.p2c.2` authored `1` / native `0`；old/new instance
  `1035/null`，snapshot `native:1597 -> native:1598`、revision `1598 -> 1599`，`postcondition_verified=true`。
  这只闭合 summary drain；冻结 central tuple 失效导致的 typed-RED cycle 仍按真实失败保留。
- 长跑随后安全 drain `zg361.40` instance `1036` 与 `epidemic_events.5009` instance `1037`，并在
  `vassal_interaction.0040` instance `1038` / date `53582544` 进入 park5。实机 root/player `32904`、actor
  `39232`，唯一显示且可用选项 authored `1` / native `0`；当前 `selection_attempted=false`，snapshot
  `native:1655/revision:1656`，产品绝对截止前尚余 `2223` game days。
- exact-build 原版定义与 interaction 调用链证明：实际宣称/关系变化已在自动接受互动时结算，
  该信件唯一按钮只显示 tooltip，本身无 gameplay effect。因此新包只接受实见的严格 7-scope
  shape，用 `$player` 延迟绑定 recipient，并把存档 ID、日期、PID 及 artifact 仅保存在 observation。完成后的
  共享 inventory 为 `161 contracts / 161 analysis / 11 observation keys`，production runtime `300`，MCP
  schema 仍为 v1；`.0040` 尚未选择，production-live 仍只是八条。
- `.0040` 聚焦测试 normal / `-O` 各 `5/5`；其与 registry/MCP/codex-setup 联合回归 normal / `-O`
  各 `37/37`，全部 GREEN。
- park5 report / driver / park SHA-256 分别为
  `E6E1D565AA74A004B5F995457D4DB52AF5E6829430B2DCB14B16A69F4B7FB529`、
  `D0137F8780BEB0E6A18A4A4E25ABE9E1436FD683A4473CC025CE6258B22FA1E1`、
  `D1CA6DA488048CF7F77A81F53290F57AF4BA36D15ABCD6E502A9B080B2627293`。T0 仍为
  `50% / stage 8/11 / source 3/4 / P2 LOCKED`；该 RED 只触发最小可复用合同迁移，不恢复 361/626 穷举门。

## 09 月 10 日：R374 `.0040` / `.4001` 热恢复与 `.1007` park7 RED

- `.0040` 通用 package commit `eeea8a6` 已按 rebase-only 普通 fast-forward push；Official Runner CI run
  `34395299023` / job `102613407044` 在约 4 分 16 秒内 completed/success，无失败步骤。
- R374 保持 PID `51852` / generation `1`，选择 `vassal_interaction.0040` authored `1` / native `0`；
  old/new instance `1038/null`，snapshot `native:1655 -> native:1656`、revision `1656 -> 1657`，
  `postcondition_verified=true`。因此 `.0040` 成为第九条 production-live primitive，而选择前 RED observation
  仍保留。
- 随后 `debate_event.5110` instance `1039` 以 authored `2` / native `1` GREEN drain；长跑在
  `trait_specific.4001` instance `1040` / date `53583192` 进入 park6。当前 root/player `32904`，
  `created_witch=witch=94245`，`witch_secret` 为 secret；两个 native options `(0,1)` 均 shown/enabled，但
  `selection_attempted=false`。snapshot 为 `native:1669/revision:1670`，产品绝对截止前尚余 `2196` game days。
- park6 report / driver / park SHA-256 分别为
  `9FDF93D053E001C73D9DC469DDE37BC44C3DA8F1872AC3E587C10241EA27FB51`、
  `750EEEB9AC789F075CCA2C9F5AC2E2619D3F6DABE9E12744FE26D25E3C1292ED`、
  `A94103B7788591FFD3379BEB418E6EC7DE675AA0FC4BB252DDB658086BED6A49`。`.4001` exact-build yearly caller、
  11 份原版源码哈希、直接/下游效果边界和实见三 scope shape 已完成共享迁移；合同选择 authored2/native1，
  只直接获得 100 虔诚并避免 native0 的五年 reading modifier 与秘密女巫转化 scheme。normal/`-O` 联合回归各
  `40/40` GREEN；package commit `75a611e` 已按 rebase-only push，Official Runner CI run `34398909398` / job
  `102625565721` completed/success。R374 同 PID/generation 选择 authored2/native1，instance `1040 -> null`、
  snapshot `native:1669 -> native:1670`、revision `1670 -> 1671`、`postcondition_verified=true`，因此成为
  第十条 production-live primitive；选择前 RED observation 继续保留。
- T2 同步审计为 `NO-CODE-CHANGE`：公共 MCP schema/API/ABI 未变，open_kaishek 没有逐事件或库存计数消费者；
  capability profile `3/3` GREEN。
- 其后 #1041--#1045 均安全 drain。当前 park7 为 `death_management.1007` #1046 / date `53590464`；严格无 killer
  shape 含 `new_memory` character_memory、`dead_character` character `39246` 与 `deceased_character_stress` value，
  唯一 authored1/native0 尚未提交。park7 report / driver / park SHA-256 分别为
  `B69243825168B01F11C0CBDEBC321C277E3AC22CEEC2F50650ABB9C0994D1D64`、
  `8702894C65AAF57E0D943C7BAA2FC4D613E4D8C5BDDFC2E4C70DB165C774A5C6`、
  `A2E63CF1EBB67ADB0987F9360B023E1F0F4659242EE048D51A8CA1219674DEE6`；剩余 `1893` game days。
- `.1007` exact-build 调用链、6 份原版源码哈希与唯一按钮效果已完成共享迁移；合同只接受当前严格形状，
  选择 authored1/native0。`.1007` package 时 inventory 增至 `163/163/13`、runtime `302`；selection 尚未尝试，production-live
  暂仍为十条。T0 保持 `50% / stage 8/11 / source 3/4 / P2 LOCKED`。
- T2 `.1007` 审计为 `NO-CODE-CHANGE`：实际 MCP 查询返回 `available`，schema/API/ABI 仍为 v1；Python 最小
  测试 `7/7`、open_kaishek capability profile `3/3` GREEN。

## 09 月 10 日：R374 `.1007` / `.2001` GREEN 与 `.1101` park9 RED

- `.1007` package commit `c6663351915a7285faa600827c9db9b05454f96f` 已按 rebase-only push；对应 Official Runner
  run `34401932801` / job `102635654978` 在约 4 分 03 秒内
  completed/success，失败步骤为空。
- R374 保持 PID `51852` / generation `1`，选择 `death_management.1007` authored1/native0；instance
  `1046 -> null`、snapshot `native:1779 -> native:1780`、revision `1780 -> 1781`、
  `postcondition_verified=true`。这是第十一条 production-live primitive；选择前 RED observation 不删除。
- 随后 `tgp_interaction_event.0010` #1047 authored2/native1、`debate_event.5110` #1048 authored2/native1 与
  `epidemic_events.1100` #1049 authored1/native0 均安全 drain。park8 为 `faction_demand.2001` #1050：
  date `53595360`、player/root `32904`、snapshot `native:1847`、revision `1848`，paused、selection 未尝试且
  `process_restart_required=false`。
- `.2001` 的五个 scope 为 faction/raw25、faction leader character `50355542`、faction target character
  `32904`（等于玩家/root）、claimant character `39232` 与 targeted title landed_title/raw5。三个 native row
  全部 shown，其中 native0/native2 enabled、native1 disabled。安全路线为 authored3/native2：拒绝后进入原版 claimant
  civil war，避免 native0 直接执行 claimant faction war-win 结算，也不走当前 disabled 的 native1 co-emperor counter-offer。
- 本包新增通用 `disabled_native_option_indices=(1,)` 合同：声明的 native row 必须 disabled，其他登记 row 必须
  enabled，selected native 不得属于 disabled 集合。该能力与 `.2001` 的合同/analysis/observation 一起进入共享
  registry，供 CK3 自动玩家、天朝二期、其它 mod 与其它机器的只读 MCP 查询复用，不写天朝私有特判。
- `.2001` source SHA-256 账本：faction_demands `B06241E67B6692F51FCFC6021E4DBE085C9E25A50B9CD08957CBCC9E25AEBEA9`；
  00_factions `0A47171476811DD16EBD44A7335EAEBD17376FD87E41290F72B5C6785365B276`；_factions.info
  `FB47457AABE7C7DF78555B4DFBA74B8932DAE468C45EBB2D1381CADDC2B7E019`；00_defines
  `C1ECA141C71EC1E741CA5336E01BB538EEFAEC05B0684EDEC477CFC9053C3807`；00_faction_modifiers
  `CD3DAA9DA33C3DFD15934CA30C1B2D7381E0B3968337BF52A7CBC2F9F2237102`；00_faction_values
  `4EE4098080B8C4536E74B047801F8715DD5EB0BCDCBAC04A8E3BF563D6555932`；faction_on_actions
  `A4E3EDA2F31CB08D29DEAE1A1CDBD89256973A81FC1A1D4DF00D9CF5BBCB68FB`；00_faction_effects
  `C8E0B3C57665F775973BC8559166CD1F6414471CA25017800777C3C6466DC5D3`；legitimacy
  `DEE9D48221B49EF41490D04451ACD6DBFD4994A50EAD9D006F831F41A6247A83`；00_war_effects
  `A936E09F448EF715580A918165EAB89A9368AD2D3014E425C998CD9D4F0E8D7D`；00_civil_war
  `CFF84009E58F5D6386A6D7501CFF084CB206A542E7D0CFA221ED2CC050A9DA68`。
- park8 report / driver-state / hot-recovery SHA-256 为
  `AC7EF0A37844A7F0B252917DAB0922B77721F0CAE6FB2A7416BC0F4420BCF9CA` /
  `FDFD7C0B2AB7DF6DC936B9FC01D611F1F5425BA6E571CBB74942BF08A68A9F28` /
  `89B4F2F8F6ADD2243C0CAD803766EB6B82491D0EF8E3C3BCCC6B55E5BC41B561`；deadline `53635896`，尚余
  `40536` 小时 / `1689` game days。
- 本包共享 inventory 为 `164 contracts / 164 analysis / 14 observations`、production runtime `303`；
  legacy migration 仍为 `156`、独立记录为 `8`、embedded bucket 为 `79`。
- T2 `.2001` 审计为 `NO-CODE-CHANGE`：实际 MCP 查询返回 `available`，三投影与
  `disabled_native_option_indices=[1]` 均正确；Python normal `23/23`、`-O` `7/7`、open_kaishek `3/3` GREEN，
  MCP v1 schema/API/ABI 未变。
- `.2001` / disabled-native package commit `3bb5169ca2b81701a8ea49e9d842de36f39fd87b` 已按 rebase-only
  push；Official Runner run `34404896747` / job `102645406781` 在约 4 分 26 秒内 completed/success，失败步骤为空。
  R374 在同一 PID `51852` / generation `1` 原位选择 authored3/native2，instance `1050 -> null`、snapshot
  `native:1847 -> native:1848`、revision `1848 -> 1849`、`postcondition_verified=true`。`.2001` 因此成为第十二条
  production-live primitive；选择前 RED observation 继续保留，inventory 不变，当前非 live 为 `152`。
- 其后 `ep3_decisions_event.2001` #1051 authored2/native1、`tgp_decision_events.0101` #1052
  authored3/native2、`travel_danger_events.3002` #1053 authored2/native1 与 `debate_event.5110` #1054
  authored2/native1 均安全 drain。当前 park9 为新的 `faction_demand.1101` #1055：date `53607792`、
  player/root `32904`、snapshot `native:2057`、revision `2058`，paused、selection 未尝试且
  `process_restart_required=false`。
- `.1101` 当前实见 4 scopes：`faction` faction/raw25、`peasant_county` landed_title/raw5、
  `peasant_leader` character `33633057`/raw4、`new_title` landed_title/raw5；native0/native1 均
  shown/enabled、非 fallback/cancel。deadline `53635896`，尚余 `28104` 小时 / `1171` game days。
  exact-build 合同选择 authored1/native0 接受：eligible 月检普通约 50 个月积累、高加成约 7 个月，另有 90 日更新
  上界，并非 daily pulse；接受有 legitimacy、县控制和十年 modifier 等代价，但避免拒绝立即发动农民战争。
- park9 report / driver-state / hot-recovery SHA-256 为
  `BA39B64ACC3224E8F1F5D5C04719A04106430D7F2F2551DEC52CCEC514FC3AA6` /
  `78C84C846ED9D2F05AF153EC9302C4C3A85C645FD0B33316CD212B10E342C494` /
  `4E3B59B3EB6328939D2008BD2DD4138546C0224F76D62297D19415138DF577E6`。
- `.1101` 已进入共享 registry：本包为 `165 contracts / 165 analyses / 15 observations`、runtime `304`，live 仍为
  `12`、non-live `153`。双模式静态测试与实际 MCP list/call/JSON roundtrip GREEN，T2 为 `NO-CODE-CHANGE`；
  Python normal/`-O` 各 `42/42`、open_kaishek `3/3` GREEN。
- 下一步是按 rebase-only commit/push，再在同一 PID/generation 原位热恢复；Python-only 合同变化不重启 CK3。T0 仍为
  `50% / stage 8/11 / source 3/4 / P2 LOCKED`。

## 09 月 10 日：《重整河山》宣传片进入人工签核门

- A03 权利说明、现稿、`zh-CN-XiaoxiaoNeural` 与片尾口号均经用户锁定；96.021354 秒候选片 SHA-256 为
  `3E30299AAB75944D2A1C54E7044576816CEE5B61ED5032C5A1A7A917C577BE0B`。
- 实机素材按 MCP-first 流程锚定大宋开封，capture a02 及 11/11 视觉样本 GREEN；最终镜头不含意大利、印度或验收控制面板。
- `xar-promo` 对 18/18 个边界样本完成 hash-bound 自动审计，native run validation 为 GREEN（55 artifacts、10 chapters）；
  项目集成回归 `16/16` GREEN。a01/a02 两个编辑 RED 与首个根目录错误的 audit RED 均保留。
- review package 已落地但保持 `pending-human-review`、`approval_granted=false`、`signoffs=0`。这表示制作与自动验收完成，
  仍需仓库所有者连续 1× 审片后才能签核；未授权、未执行外部视频发布。
- 仓库所有者随后明确回复“通过”；`signoff-000001` 已绑定精确 SHA-256，离线正式包
  `reclaim-promo-release-20260910-a01` export GREEN。预审包仍按不可变原则保留原状态；没有指定外部视频发布目标，故未上传。

## 09 月 10 日：R374 `.1101` GREEN 与“宋国库”publisher 自旋 RED

- `.1101` package commit `0880b4d579b92685be0696df5a5a32d701db5ed2` 已按 rebase-only push；Official Runner
  run `34408112586` / job `102655870657` 在约 3 分 31 秒内 completed/success，失败步骤为空。
- R374 保持 PID `51852` / generation `1`，选择 `faction_demand.1101` authored1/native0；old instance
  `1055 -> null`、snapshot `native:2057 -> native:2058`、revision `2058 -> 2059`、
  `postcondition_verified=true`。该条因此成为第十三条 production-live primitive；当前已提交 registry 总数仍为
  `165`，其中 live `13`、non-live `152`，runtime `304`。
- 紧随其后，CK3 前景出现原版 `tgp_china_ministry.0100`“宋国库”三选项窗口，但 native publisher 仍返回
  `paused=false / active_event=null`。runner 对 `pause-map=already_paused` 的 ACK 后继续读取陈旧帧并重复 pause，形成
  可复现自旋 RED。随后只为解开前景遮挡执行一次明确标注的 visual-coordinate fallback：按 source-reviewed safe route 选择
  authored2/native1，再关闭宣战通知、activity detail 与唯一选项胜利窗，并以 Windows `SendInput` 发送空格。这些操作都不是
  MCP action；仍没有可登记的 event instance/date/root/saved scopes/option-state，截图身份与视觉选择不得冒充 paused live
  observation。driver command `#3200` 返回 `pause-map=submitted`，`#3201`--`#3355` 共 `155` 次均返回
  `already_paused`，但旧 runner 仍未消费到新 paused frame。
- 前景截图 `_runtime/p2r374-active-boundary-continuation-live/park9-post-selection-ck3-screen.png` SHA-256 为
  `B2E57E4B90EC82EBA96500B7D0B1F963A6EE46F521E4E5250C1A3BE113C7047B`。exact-build source SHA-256 为
  event definition `87358436D60431BC80DA0376DD1BB2C47696EFCDE813148C69883768439980EE`、yearly caller
  `0FC85A284224A68D1CA0A4EF071D4F4A4F49896753AEC463975A12EE4E1116FA`、budget effects
  `AEF36B884DC5E315DD5C655BC96012FF9FA8BB46BB0AF2C18FA878C890907747`、TGP triggers
  `8294C1D72ECC909428ABFBA27D6F10B127D796D2BEE350D1BB95024F36D60C99`、简中 localization
  `6AD38C15CAE1CD3EAC713EDBC1566C7D42D8CE3B94291139F976C439020F8210`。source-reviewed 安全路线为
  authored2/native1“维持原有分配方案”。该路线后来只通过上述视觉兜底执行，不能给 `.0100` 记 live。通用合同已从旧
  manager key 抽取并由远端 commit `b51ccd9` 推送；contract/analysis 总数仍为 `165/165`，identity-only RED 使
  observation keys 增至 `16`。
- visual recovery sidecar
  `_runtime/p2r374-active-boundary-continuation-live/r374-visual-recovery-red-evidence.json` SHA-256 为
  `003875E75BB05A8CF74AF27760DDC311B9F504C3A9FA51F5E3FDBD01831AB4EC`；冻结 driver snapshot SHA-256 为
  `B83EFAAA2574ACCDEF533C5F2B8A38BAC20255237D18D32C35B87D3F42F48F43`。它明确记录
  `mcp_only=false / coordinates_used=true / send_input_used=true / production_live_credit=false`，所以本轮 live 仍为 `13`。
- Python bounded no-spin 修复已由 commit `ac6f63c` rebase-only 推送：direct 幂等 map-control ACK 必须在同一命令
  deadline 内绑定相符 semantic frame，否则只提交一次并转为 wrapper 可捕获的 hot-recovery typed RED；无需另加循环次数门，
  也不伪造 Python paused 状态。normal/`-O` driver 与 production-entry 定向测试 GREEN，Official Runner run
  `34413044308` / job `102671610904` completed/success。
- native 根因是 ACK 判定已读取可信 snapshot，bridge 却弃用该帧并立刻二次全量读取；自动暂停边界的第二读可返回陈旧
  `paused=false / active_event=null`。commit `9ccd3995418549b18f46190c3061ede5714c81e5` 已按 rebase-only push，
  改为把 ACK 使用的同一 snapshot 交给 timeline publisher，并在幂等 ACK 时强制发布。MSVC Release 定向 CTest `2/2`
  GREEN；新 DLL SHA-256 为 `18787F7F7F0A9BAF85C7D6979AB8AD20C8CF80D553AA6CA8D2AB87CF18DB0840`。Official
  Runner run `34415670708` / static job `102679886593` completed/success，约 4 分 36 秒、失败步骤为空。
- 因 DLL 已变化，R375 必须受管重启，不能把它当 Python-only 热恢复。恢复基线 `autosave` 为
  `date_raw=53602440`、`144254384` bytes、SHA-256
  `2B683351CF79ADE9DD5A931090DE8E6AF1D4F57FD65FFAFBB3CF18B7436BD97E`。R374 final report 的
  `target_progression.timeline_interrupt_drains` 实际 list length 为 `456`；其中
  `retained_timeline_interrupt_drain_count=455` 是落后一条的内部计数，不能作 final ledger 总数。按
  `date_raw < 53602440` 严格裁切只保留 `453` 条 durable seed，丢弃并重采 indices `453..455`：
  `travel_danger_events.3002@53603376`、`debate_event.5110@53605080`、
  `faction_demand.1101@53607792`。`.1101` 已写入 R374 final ledger，只是尚未进入源 autosave。新进程必须重新取得 native
  同帧与 MCP postcondition，不能沿用 visual fallback 给 live 记账。T2 当前版本 pin commit `720ff9b` 已推送，Official Runner
  CI run `34411520520` GREEN；公共 MCP schema/API/ABI 未变。T1 保持 `90%`，等待唯一 CK3 串行槽。

## 09 月 10 日：R375 回放、五条新 live 与产品 liveness RED

- R375 从上述 `453` 条 durable seed 开始，在 PID `180544` / connection generation `1` 上追加五条全 GREEN drain：
  `travel_danger_events.3002` instances `1053/1054`、`debate_event.5110` #1055、`faction_demand.1101` #1056、
  `tgp_china_ministry.0100` #1057。`.0100` query command `#291` 在 `native:356 / revision 357` 发布唯一窗口、root
  `32904`、saved scopes `treasury_ruler=32904 / steward=38076 / military_budget=32904`，native `0/1/2` 均
  shown/enabled；command `#292` 以 authored2/native1 选择，instance `1057 -> null`、snapshot
  `native:356 -> native:357`、revision `357 -> 358`、`postcondition_verified=true`。因此 `.0100` 是第十四条
  production-live primitive；R374 UI-only RED 继续保留。不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-014-tgp-china-ministry-0100-green.json` SHA-256 为
  `6C1407AF00D2E767FA201DA2411619D5724C86951BB5CEFF006DAB50ABC6C779`。源 autosave 到 `.0100` 为
  `312` game days，不是旧估算的 `223` days / `3` events。
- 新 DLL 同帧 map-control 在 command `#343` 取得实机证据：`pause-map=already_paused` 的后置帧在同一
  `date_raw=53611224` 从 `native:424 / revision 425` 前进到 `native:425 / revision 426`，保持
  `paused=true` 且 rejected state frames 为 `0`；紧邻 event snapshot 为 `native:426 / revision 427`。这关闭了 R374
  所见 ACK/publisher 分裂的 exact live slice，不把一般性结论外推到别的构建。
- `.0081` 的选择前 native RED 已保留；exact-build 通用包由 commit
  `862fc7fc89e61a290295a25dcdf2ca31434509be` 推送后，同一 PID/generation 原位热恢复，选择
  `tgp_dynastic_cycle.0081` #1058 authored1/native0，instance `1058 -> null`、snapshot
  `native:426 -> native:427`、revision `427 -> 428`、`postcondition_verified=true`。这是第十五条
  production-live primitive。不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-015-tgp-dynastic-cycle-0081-green.json` SHA-256 为
  `573992BD13E2373DB3A827698B770D703263EC678ECDEE98595C0D48C6671789`。当前 inventory 为
  `167 contracts / 167 analyses / 19 observation keys`、production runtime `306`；新增量来自随后由 commit `5c8a526`
  rebase-only 推送的 `.1000` 通用 package。
- `epidemic_events.0110` #1059 的选择前 RED 位于 `date_raw=53611320`、snapshot `native:432`、revision `433`。
  实见 saved scopes 只有 `epidemic`；旧合同另要求 `new_preferred_capital`，所以失败项精确为
  `saved_scope_count`、`saved_scope_names_exact`、`scope:new_preferred_capital:type`，且
  `selection_attempted=false`。不可变 RED freeze
  `_runtime/p2r375-post-publisher-fix-live/r375-epidemic-events-0110-red-freeze.json` SHA-256 为
  `1CD3DEC1ED7DB6F3C7DBE8D485DC2B38753F1D7E1D90BD9E8FA99A8ADADC7448`。commit
  `6fc2ef2167f4af1dbb2ff4bb1be7fa70825203ea` 收口 exact-build 最小合同后，同一 PID/generation 原位热恢复
  authored3/native2，instance `1059 -> null`、snapshot `native:432 -> native:433`、revision `433 -> 434`、
  `postcondition_verified=true`。不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-016-epidemic-events-0110-green.json` SHA-256 为
  `1C96D13A88D9F96575CA6DD78E5E02C0483ECB1ADC09505949868EC387994EEF`，并由 commit `0a65adf` 接入 MCP
  observations。这是第十六条 production-live primitive。
- `stress_threshold.1721` #1060 的选择前 RED 位于 `date_raw=53611536`、snapshot `native:440`、revision `441`。
  实见 rendered native indices 为 `(7,9,12)`，旧合同期望 `(7,10,12)`；唯一失败项为
  `authored_options_exact`，`selection_attempted=false`。不可变 RED freeze
  `_runtime/p2r375-post-publisher-fix-live/r375-stress-threshold-1721-red-freeze.json` SHA-256 为
  `6F0F849C88EC48D78803B27B6BDE94C334FDD94F61024AA1BAA48EC88E2F4508`。commit
  `27921a9b96c40043ddb70c89b1614f170e4ba192` 收口 exact-build 最小 variant 后，同一 PID/generation 原位热恢复
  authored10/native9，instance `1060 -> null`、snapshot `native:440 -> native:441`、revision `441 -> 442`、
  `postcondition_verified=true`。query `#371`、select `#372` 的不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-017-stress-threshold-1721-green.json` SHA-256 为
  `869BFE72B6FF2ABEF55FF3F4191F13D3A9F57E696F02B011367527256AD57EF3`，并由 commit `76f9a83` 接入 MCP
  observations。这是第十七条 production-live primitive。
- `ep3_landless_admin.1000` #1062 的选择前 RED 位于 PID `180544` / generation `1`、
  `date_raw=53619912`、snapshot `native:720`、revision `721`；saved scope 为 `proposed_councillor` character
  `33643335`，三个 rendered native indices `(0,1,2)` 均 shown/enabled，`selection_attempted=false`。不可变 RED
  freeze `_runtime/p2r375-post-publisher-fix-live/r375-ep3-landless-admin-1000-red-freeze.json` SHA-256 为
  `F74447FE01DDB9BC890C757578DBA477373167EA0DA862BB987D26A61904DFA1`；其绑定的 report / driver / park4 SHA-256 为
  `AB75764DE8E02157A8E21B69C20F9081AFB59162C70044FD401260C8D9E57FF4` /
  `20F2186105B53050504C6FCA09E910A6F01F6CD2BD9D5B6AA74D167443758976` /
  `C382B9DD4C8ABC45664B68BAFEACF63B9AB718D2E8D0433DB558F0BACAAC9E01`。exact-build 分析已闭合：默认
  authored3/native2 是不花钱、不招募、不进入随机 duel 的确定性拒绝路线；通用 package 已通过 normal/`-O` 回归并由
  commit `5c8a526` 推送。同一 PID/generation 随后以 query `#602`、select `#603` 原位热恢复，instance `1062 -> null`、
  snapshot `native:720 -> native:721`、revision `721 -> 722`、`postcondition_verified=true`。不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-018-ep3-landless-admin-1000-green.json` SHA-256 为
  `F545AC8E85102FBFE83B6EFEC23AEB47978844B9EF3F8FE4538C720369BAA8C4`；这是第十八条 live，当前 live `18`、non-live `149`。
- R375 最终 rolling drain list 实际为 `464` 条，durable seed `453` 后新增 `11` 条 GREEN drains：indices `453..463`
  依次为 `.3002#1053@53603136`、`.3002#1054@53603160`、`.5110#1055@53605080`、`.1101#1056@53607792`、
  `.0100#1057@53609928`、`.0081#1058@53611224`、`.0110#1059@53611320`、`.1721#1060@53611536`、
  `death_management.1001#1061@53618256`、`.1000#1062@53619912`、`tgp_movement_events.0080#1063@53624256`。
  GREEN sidecar 的 retained count `462` 是尾段写入前的滞后字段，不能作 final ledger。
- runner 随后在 `date_raw=53635920`、无 active event 时超过 exact bound `53635896`；后续复盘确认该 terminal 混合了
  过严的产品场景要求与 harness 失效处理，不能继续整体标成产品 liveness RED。
  `10190-day product observation bound` 已覆盖两个 400-day authored B1 opportunities、一个 4200-day post-publication
  critical-path tail 与三个有限 730-day Workforce windows，但 `production_capability_advertised=false / production_live_ready=false`。
  immutable bound sidecar `_runtime/p2r375-post-publisher-fix-live/r375-product-observation-bound-red.json` SHA-256 为
  `C3D4503117ED687D3289AD612B25C1AA36A0FA1329572D607E604E0CE7EAA9F8`；final report / driver / park5 SHA-256 为
  `19969B3802F038C9AA7EFFF65BE78C8E0FEC73C2FE7914F86930C09618CCE4FF` /
  `78BC8A47A1F874D56194846C00FB8055FD65274B8A5B801A513362C1AC555914` /
  `3338D5779E51D8C75B705475C91F408E9755EA13D41AB31A12D91D036417BD6D`。第三次 `.356` 现仅为可选第三周期业务证据，
  不再是 P1 source-readiness 硬门；前两条独立 `.355 -> .356` 已覆盖重复事件链。commit `9caf735` 在
  `tgp_dynastic_cycle.0081` 改变经理/场景身份时于任何输入前返回 `SCENARIO_INVALID / NOT_EVALUATED`，commit
  `734ddbc` 将产品 invalidation overlay 与通用 registry 分离。Python-only 热重跑已确认不再发送输入。
  当前 `autosave.ck3` / `last_save.ck3` 只到 `date_raw=53611200`、`144950977` bytes、SHA-256
  `F67B86E41A566EB0622A4108B44634DBDFBD2F9277A85B5B04487B1515D4EB43`，不得冒充 `.0081`、`.0110`、`.1721`、`.1000` 或终态 bound 的同帧存档。
- T0 保持 `50%`、canonical stage `8/11`、source checkpoint `3/4`、target `.356` `2/3`，P1 尚未签收且
  T0-P2 继续 `LOCKED`。`strict 4/361` 与 `definitions 106/626` 仅是非阻塞 backlog，不恢复穷举门。
- 独立的真实 B1 RED 在当时仅形成 R374 中间帧假设：D340 同 tick 两次 quota rebuild 可能令 temporary scratch
  累计。后续 R384 D+403 完整帧与 exact-build list 语义已经推翻该根因；commit `95f6824` 的 scratch 清理只作为
  独立防回归保留。最晚可重放 checkpoint 是
  `p2r374-active-boundary-continuation-state/.../autosave_2.ck3`，SHA-256
  `F1188E048188AA37FAF42168E64F5E56E8413358E26D627BC6EFEA09FC452CBB`。新 B1 MCP/DLL 合入后须从这里 fresh live 验证。
- manager A/B 与 embedded A/B/C 已宽并行迁移并逐包推送；当前 `158/167 = 94.6%` 运行合同已 campaign-neutral，
  对应旧日期和动态 CharacterID 仅保留在 `legacy-live-binding / migration-only` observations。`79/167 = 47.3%`
  已绑定 exact-build source path/SHA；剩余 9 条固定身份合同和 88 条 source 缺口继续并行收口，safe option 不变。
- 纯 Python discovery index 已由 `b1bedad` 推送，支持 167-key 稳定分页、搜索与 typed unavailable；内容寻址 source/raw
  evidence 包仍在生成，现阶段继续明确“路径元数据不等于其他机器可读 evidence”。
- R375 scenario-invalid cleanup sidecar SHA-256
  `9558CECB5B32EEBEBF66058893FA0662416E71E7D5F2A5B3FB758CE859EF3387` 的 16 项自检全真：PID `180544` 是
  ad-hoc wrapper 用历史 instance park 后执行的受控 cleanup，不是 CK3 crash 或产品 RED；canonical runner 已有 terminal short-circuit。
- 通用 Git operator setup/invoke 已由 `6d0a37a` 推送，强制 rebase-only 且不依赖用户 profile。xenoa 三仓
  status/ls-remote 与两仓 push dry-run 已 GREEN；CodexSandboxOffline 的 ACL/key 已就绪，但真实 token E2E 仍待该账户运行，
  不冒充验证完成。宣传工具保持只读、未 pull，T0-P2 锁未触碰。
- `f4e2d8b`、`95f6824`、`d0f16f7`、`9caf735`、`734ddbc`、`2fa9034` 已按 rebase-only 推送；其中
  `f4e2d8b`、`95f6824`、`9caf735`、`2fa9034` 的 Official Runner CI 均 GREEN。当前 Git HEAD 与
  `origin/master` 同为 `2fa9034`，T0-P2 仍保持 `LOCKED`，当前 CK3 槽为空。

## 09 月 10 日：通用原版事件证据 MCP 完整覆盖

- 原版事件迁移已从本周中间态推进到 `167/167` campaign-neutral 与 `167/167` exact-build definition index；
  内容寻址 portable 包共 `213` 份 evidence（`157` 个唯一 source 文件、`56` 份 observation artifact），并明确区分
  `497` 条 lexical caller candidate 与已审调用证据，避免把文本命中冒充运行时调用链。
- 新增四个只读查询面：知识列表/搜索、evidence 列表、内容哈希读取与 source provenance；既有单事件查询保持兼容。
  manifest SHA-256 为 `BC843955BF8EAC8E0133386EFDD0D9DEF4DE794C50559E4F8C93EA6DFF9A5545`，source index
  dataset SHA-256 为 `50DAE4938EF8D2D7677C10C55CB89FB34189B1E11F6788DCB00A4C8FA248759B`；双模式
  `423 passed + 839 subtests`，真实 MCP smoke `5/5` GREEN。
- 主仓 commit `df1ed7c` 已通过严格主机密钥校验的 SSH-over-HTTPS-proxy 路径完成 rebase-only 推送；当前
  `HEAD == origin/master == df1ed7c`。未 merge、未 force push、未修改凭据，先前的 Git 同步 RED 已关闭。
- P1 成功门同步改按产品关键链、代表性高风险与真实 RED：B1 fresh、AF5 `42/41`、stage 9/10/11、一次终态
  cold restore、error scan/cleanup 与 final L0 保持硬门；exact `7+4`、source4、第三次 `.356`、三周期、10190 天和
  P1 前置录像均降为覆盖指标。当前仍为 `50% / stage 8/11 / P2 LOCKED`，CK3 槽为空。

## 09 月 10 日：R376 startup RED 与 open_kaishek portable MCP 同步

- R376 在零 CK3 进程前置条件下以唯一 PID `43984` 启动，使用 CK3 `1.19.0.6` 与 B1 DLL SHA-256
  `539A1BC1D737A303EFCA61918D867540529D230F7BDF22D8BFBD47A2BA58707B`；`3.293s` 后在产品 query 前以
  `C0000005 / RVA 0x1DABD89` 退出。该 RVA 已有 no-DLL 同结果的历史控制证据，故本轮分类为
  CodexSandboxOffline interactive-desktop startup/environment RED，而非 B1 产品判决。R376 已死亡，当前 CK3=0；
  下次只能是零实例确认后的 R377，并优先走 Windows 交互操作者/通用 operator MCP，不做同环境重复启动。
- open_kaishek commit `58d513b0b4a94c3e24f758841c43c475c050edf4` 已同步四个 portable vanilla-event MCP、schema 与
  内容版本哈希，并 rebase-only 推送到 `origin/main`。Java `167/167`、Python `8/8`、metadata/domain/diff-check GREEN；
  本轮没有改变 B1 行为。
- B1 fresh-live 证明仍是当前产品 RED，T0 因而保持 `50% / stage 8/11 / P1 未签收 / P2 LOCKED`。本周没有触碰
  宣传工具或最终视频物料。

## 09 月 10 日：P1 范围门、通用 Git 操作者与 operator MCP

- commit `d7a7c40` 已按 rebase-only 推送并把 P1 成功门固定为产品关键链、代表性高风险路径和真实 RED；361 场景与
  626 definitions 保持非阻塞覆盖账本。T0 仍为 `50% / stage 8/11 / P1 未签收 / P2 LOCKED`，没有触碰宣传工具或
  最终视频物料。
- commit `252c579` 已按 rebase-only 推送通用 Git 操作者传输：严格主机密钥校验、SSH-over-HTTPS-proxy、无固定账号/
  机器路径/凭据依赖，定向测试 `17/17` GREEN。
- commit `e684fd0` 已按 rebase-only 推送通用 operator MCP；capabilities/status/preflight/handoff 四工具以 profile
  表达目标操作者、交互桌面、冻结命令、路径/hash、输出与 CK3 排他门。normal/`-O` 各 `6/6` GREEN，当前沙箱的
  实际 Streamable HTTP MCP list/call/JSON roundtrip GREEN。
- 目标身份验收仍是 RED：HTTP preflight 仅 `target_identity_exact=false`，Shell bootstrap probe 也仍运行于
  CodexSandboxOffline sandbox desktop。未调用 handoff、未启动 CK3；R376 已死亡，当前 CK3=0，R377 未启动。
- `e684fd0` 新增接口/协议已触发 T2，open_kaishek operator MCP 兼容层正在同步；同步完成前不将该新增 horizon
  记为兼容完成。B1 fresh-live 产品 RED 仍未关闭。

## 09 月 10 日：T2 operator 同步与 P1 九项关键门预收口

- open_kaishek 以单父、rebase-only commit `82d03cf81eb82b5ef5c65f93b92ea03b7617d170` 完成 operator MCP
  capability/profile/client adapter 同步并推送；远端一致。全 Maven `176/176`、provider normal/`-O` 各 `6/6`、
  domain normal/`-O` 各 `8/8`，当前新增接口 horizon 已恢复兼容。
- 固定候选 `e684fd0` 的 AGENTS 静态 L0 `9/9` GREEN；Phase2 `broad` 产品 tree SHA-256 为
  `5fad3533cd9296ebe03222e16bf06dee1b3231f36dda08f03f28b80139bf5d01`。这只证明 candidate/tested 相同，
  live loaded hash 尚未证明。
- fail-closed P1 assembler 当前诚实报告 `1/9 GREEN + 8/9 PENDING`，未写最终 manifest。剩余八项均须由真实
  B1/AF5/stage 9-11/cold restore/error scan/cleanup artifact 闭合，旧 361 场景与 626 definitions 仍非门禁。
- 单客户端 named pipe 与每次 reconnect 递增 generation 的静态证据已写入测试流程；同 generation 续跑改为
  当前 owner 原位复用 service。目标 operator endpoint 未监听、CK3=0、R377 未启动，P1/P2 状态不变。

## 09 月 10 日：operator MCP 1.1.0 与 R377 静态准备

- operator MCP `1.1.0` 已由 commit `5610de6` 按 rebase-only 推送；五工具使用 profile-frozen job/control 合同，
  状态变更控制保持同 request/job 幂等。normal/`-O` 各 `9/9`、官方 MCP client smoke 与 `py_compile` GREEN。
- 该提交再次改变 MCP 接口/协议并触发 T2；open_kaishek 已由单父提交
  `193551f148f2786a6baf91d85b63eadb80b1793d`（parent `82d03cf`）完成 1.1 同步，按 rebase-only 推送后
  `HEAD == origin/main == remote`、ahead/behind=`0/0`，未 merge、未 force push。provider normal/`-O` 各
  `9/9`、focused `15/15`、Maven `182/182`、domain normal/`-O` 各 `8/8`，metadata/validator/portability PASS；
  1.0 四工具保持兼容，1.1 五工具及 `job_controls` / control 幂等语义已同步，T2 当前 horizon 恢复一致。
- T1 非 CK3 复查为 `NO-ACTION`，进度仍为 `90%`；剩余项需要 CK3 实机证据或 owner-approved utility。
- T0 runtime prep 的双段日志与 cold-cleanup lineage 已静态 GREEN，但不构成 P1 实机验收增量。T0 仍为
  `50% / stage 8/11 / P1 未签收 / P2 LOCKED`；CK3=0，R377 未启动，视频锁未触碰。

## 09 月 10 日：R377 冻结包终审

- no-launch profile SHA-256 为 `CE35940E9C7D4212BABB29EEF97F47B15211B30241199EE18D41F606F369B498`，job spec
  SHA-256 为 `C52BF39912BB44C94AAFCD49AE6C8928DCCEC21659C5B6A0B296E246526B8E90`；15/15 required hashes、
  7/7 absent live outputs 与 `resume-auto` / `cleanup` 两个冻结控制均通过独立复核。
- 完整自动链 normal/`-O` 全部 GREEN，终审未发现代码级 blocker。目标 `xenoa` 交互桌面的 `127.0.0.1:8766`
  尚未监听，官方客户端诚实保持 `RED/PENDING`，未 handoff、未启动 CK3。历史 R376 已终止，当前 CK3=0，R377 尚未启动。
- T0 仍为 `50% / stage 8/11 / P1 1/9 GREEN + 8/9 PENDING / P2 LOCKED`；T1 为 `90%`；T2 当前兼容 horizon
  为 `100%`。下一动作是目标 MCP 上线后先验五工具、profile SHA、操作者身份与 preflight，再幂等 handoff 唯一 R377。

## 09 月 10 日：operator MCP 自启动与 B1 executor allowlist 修复

- 当前执行者实测运行于目标 `xenoa / WinSta0\Default`，已自行启动 operator MCP 并通过五工具、身份、profile 与 job
  preflight；不再把“等待外部 xenoa shell”列为 blocker。R377 warmup 已清理，R378 / PID `156280` 为当前唯一 CK3。
- R378 在 B1 业务判定前以 `application-main ZhongGuo B1-cycle executor is unavailable` 保留 RED，`product_result` 为
  `NOT_EVALUATED`。根因是既有 B1 executor 漏出 application-main 固定 allowlist；这不是等待时序，也不能用 Python 热重跑修复。
- commit `9033732930d10293a3e1c6b37942611b55b324f6` 已增加第 29 个固定槽并绑定 B1 executor，按 rebase-only 普通
  push 后本地/远端一致。MSVC Release build `524/524`、CTest `103/103` GREEN；新 DLL SHA-256 为
  `54DD192F21936D4B4C4D721D9D3D7900EE40FE8AB94DF1848EF806B6000E1C7D`。
- open_kaishek 审计为 `NO-CODE-CHANGE`：内部 C++ mailbox/DLL ABI 有变，但其不消费该接口；对外 MCP/schema/业务合同未变。
  P1 仍未签收、T0 仍为 `50% / stage 8/11`，P2 保持 `LOCKED`。下一实例只能在受控清理 R378 并确认 CK3=0 后启动，
  且每次 warmup/final/cold-restore 新启动都必须分别递增动态 R 轮次。

## 09 月 10 日：R380 B1 Python 双归一化热修

- R379 warmup 已清理；R380 / PID `175936` / generation `1` 是当前唯一 CK3，paused/map-ready。第 29 个 executor
  已实际返回 available B1 frame，关闭了 R378 allowlist 缺槽；新的 RED 是 native driver 派生
  `invariants`/`anomalies` 后，service 又按 raw-only frame 二次归一化。
- 最小 Python 修复让 normalizer 对 raw 18-field 与 canonical normalized 20-field 幂等，同时重算并严格核对两个派生
  字段，保留 fail-closed。native wire、公共 schema、MCP tool 与 68-field/38-variable 合同不变；B1 合同双模式各
  `13/13`、Gameplay bridge 双模式各 `219/219`、直接 Driver->Service 组合回归、py_compile 和 diff-check 均 GREEN。
- 当前 RED 仍发生在 B1 业务推进前，产品结果保持 `NOT_EVALUATED`；T0 仍为 `50% / stage 8/11 / P1 未签收 / P2 LOCKED`。
  该变化不允许重启 R380。open_kaishek 只需同步父仓 Python 合同哈希/provider commit，不改业务字段或认证状态。

## 09 月 10 日：R384 异步 map-control 后的 B1 probe 热恢复

- R380 与 R382 均已经 operator MCP 受控清理；各自 warmup 也已终止。当前唯一实例为 R384 / PID `38864` / generation `1`，
  初始 B1 available 与 `23/46/7` 守恒已在实机复核，新鲜 400-day 回放边界没有继承或续期全项目 10190-day 窗口。
- R384 在两个已确认日进度后真实撞到 harness RED：`resume-map` submitted 与新 running heartbeat 之间，诊断 probe 仍读取旧
  paused publication；native direct-read gate 正确拒绝已变化帧。该拒绝没有产生 B1 查询结果或状态写入，产品仍
  `NOT_EVALUATED`，且没有事件选项/review/B1 写操作。
- Python 编排层现在只对精确命中的 B1 snapshot-publication rejection 做同 player/generation、有审计、最多四次的只读重绑；
  任何其他错误或超限仍 RED，随后出现的 modal 必须重新查询 live definition 并走共享原版事件合同。定向双模式与既有 map-control
  回归 GREEN；无需重启 R384。
- 公共 B1 schema/wire/readiness、MCP tool、DLL 和游戏文件均未变化，T2 判定 `NO-CODE-CHANGE`。T0 仍为
  `50% / stage 8/11 / P1 未签收 / P2 LOCKED`，视频硬锁未触碰。
- 首次热恢复随后 live 验证 `tgp_decision_events.0101` 并按共享合同推进；第二次 RED 是同一 pre-query 窗口的
  `B1-cycle snapshot revision is stale` 变体。它已加入同一精确、有界、无状态写入的 probe-rebind 合同，不扩展到其他错误。

## 09 月 10 日：R384 `pay_homage.0101` 通用资产

- R384 / PID `38864` / generation `1` 在第二轮 B1 rebuild 中真实停于 `pay_homage.0101` instance `1046`；产品仍
  `NOT_EVALUATED`，本次 RED 未提交事件选项。原版 source authored 三项，但无 faux-pas 的当前窗口只渲染 native `0`，因此最小
  合同只接受 authored1/native0 Smooth，不预先放宽未来 faux-pas 变体。
- 该事件已分层迁移为 campaign-neutral contract、exact-build analysis 与 R384 immutable observation，并接入 source index 和
  portable MCP evidence。共享总数更新为 `168 contracts / 168 analysis / 20 observation keys`，production 为 `307`；bundle 为
  `221 evidence / 913 references / 59 observation artifacts`。公共 MCP schema/API/ABI 未变，T2 为 `NO-CODE-CHANGE`。
- 变更仅涉及 Python/data；完成双模式测试和 rebase-only 推送后，必须通过 operator MCP `resume-auto` 在同一 R384 PID/generation
  热恢复，重新查询同一 event binding 后选择 authored1/native0，并以 instance advance 为动作后证据。P1 仍未签收，P2 继续锁定。

## 09 月 10 日：R384 固定 400 天边界归因修复

- `pay_homage.0101` 已在同一 R384 / PID `38864` 成功关闭；随后 fresh B1 replay 在固定
  `53584920 -> 53594520` 的 400 天窗口后暂停于 D+403，未重启、未续期，当前仍是唯一 CK3 且无活动事件。
- production entry 的停止条件始终使用 retained absolute deadline，但失败文案和 `advance_bound` 曾误报 canonical 10190 天。
  修复后 canonical 全产品窗口保持原有 breakdown，自定义 retained 窗口按真实 origin/end 报告；该变化仅修证据归因，不改变结果。
- 最后完整 B1 query 只到 D+251，D+394 仅有 progress row，故当前保持 observation `RED` / product `NOT_EVALUATED`；下一步是在同 PID
  上做一次不推进日期的最终只读 probe，再依据完整 D+403 frame 判定。定向及直接消费者双模式均 GREEN；公共 MCP/ABI 未变，T2
  `NO-CODE-CHANGE`，T0 P1 与 P2 视频硬锁状态不变。

## 09 月 10 日：R384 D+403 B1 产品 RED 与最小 roster 修复

- R384 / PID `38864` / generation `1` 的最终零时间只读 probe 在 D+403 得到完整 state 7 帧：持久 roster 132、
  target `40/79/13`、80 人 recount `25/45/10`、amendment/audit `61/62`，closure/calibration/reward 均未发生；
  该状态持续 291 游戏日，正式判定产品 liveness RED。report / park SHA-256 为
  `D900B9C29E24D7C7759E8B814B316A4BB0B407728606803C25DDFBA66B9C462C` /
  `768136B6C0896230BD4681E6A69A594A841BED8BC97186B28BBC9FB8C720BC09`。
- 根因是以 temporary-list 的 `list_size:` 读取 persistent variable list，令 prune 后 roster 与 processing count
  变成 0：76 人冻结名单剔除 5 个失效 row 后实际保留 71，却错误接纳全部 61 名 eligible member，最终 132。
  exact-build 原版测试 `event_target_lists_tests.txt:217-231` 明确使用 `variable_list_size`；旧存档还暴露缺失
  `zg361_b1_quota_rebuild_generation` 时直接 increment 的 empty value-scope 错误。
- 最小修复显式累计持久名单、统一计入 backfill/late join、为旧存档 materialize generation 1，并移除 band max
  与 scoreboard 的错误 persistent-list selector/`list_size:`。目标 fresh 结果为 roster 80、quota `24/48/8`、amendment/audit `14/15`
  并进入 state 8。生成器、B1/quota/MCP 双模式、本地静态与 release build/check 已全 GREEN；但仍是
  `static-ready`，不得在 fresh CK3 成功前关闭 RED。
- 游戏脚本有变，R384 不能热恢复；rebase-only 推送后将由 operator MCP 自主受控清理，零实例确认后启动递增
  新轮次复验。公共 MCP/schema/ABI 未变，open_kaishek 为 `NO-CODE-CHANGE`；T0 仍为 `50% / stage 8/11 / P1 未签收`，
  P2 最终视频硬锁未触碰。

## 09 月 10 日：R386 final-survivor compact/reseal

- R386 首先以 74 人 state-7 完整域、`22/45/7` 守恒结果证明上一轮持久名单计数修复有效；最后一条 reopen callback
  期间另有一个 weak row 失效，旧 resolver prune 为 73 后仍沿用 74 人 quota/agenda 并发布，判为产品 RED。
- 修复只压缩最后 barrier 的幸存域：保留 grade/order，不 rerank；target 收敛到实际 recount；agenda 与 M360 hash 从
  pruned processing 域重建；board/reward receipt 重封；最终 conservation 失败时禁止发奖/发布。scoreboard 的固定
  max80 同时改成实际 cohort 动态上限。
- `resume-map` 后 paused-required 是独立 harness publication race；加入精确、最多四次、只读 rebind，不吞稳定或未知错误。
  当前静态矩阵 GREEN，但游戏脚本有变，须在提交推送、MCP 清理 R386 后以递增新轮次 fresh replay；P1/P2 状态不变，
  公共 MCP/ABI 未变，open_kaishek 为 `NO-CODE-CHANGE`。

## 09 月 10 日：R390 `diplomacy_majesty.4033` 通用合同

- R390 / PID `190136` / generation `1` 在 B1 GREEN 后继续 AF5→stage 9，真实停于原版
  `diplomacy_majesty.4033` instance `1089`；选择前 RED 及完整 scope/option shape 已冻结，事件尚未处理。
- exact-build 原版定义和季度生活方式调用链证明它不是每日事件，且唯一 authored1/native0 仅产生五年技能增益、
  +25 好感和潜在朋友关系，无资源、死亡、战争或 follow-up 风险。
- `b4bfc42` 将该结论迁移到共享 registry、source index 和 portable MCP evidence。事件资产现为
  `169 contracts / 169 analysis / 21 observation keys`，portable bundle 为 `227 evidence / 922 references`；
  normal / `-O` 双模式与两项生成器 check 均 GREEN。
- 修复仅为 Python/data/docs；完成 rebase-only 推送后必须在同一 R390 PID/generation 重新绑定并热恢复，
  以 instance advance 关闭 RED。T0 仍为 `50% / stage 8/11 / P1 未签收`，P2 最终视频保持硬锁定。

## 09 月 08–11 日：CK3 纹章剪贴板 MCP-first 调研与编辑器基线

- 完成 1.19.0.6 原版 CoA clipboard update / reader / paste / copy 静态链路与字段边界，新增 `ck3_probe_coat_of_arms_source_v1` MCP 工具、原生同线程 designer hook、三态 exact binding 和闭合结果 schema；第三方项目仅作固定提交只读参考，未复制代码或资源。
- 实机逐步补齐 frontend snapshot、clipboard slot 延迟与 frontend 生命周期安装；attempt 8 最终在完整 designer
  执行 27 条原生 probe，hook 与 managed cleanup GREEN。核心 render-description 组合及静态 `@变量` 已实际应用，
  effect/trigger/event/template-list 等反例均由原生 reader 拒绝；artifact SHA-256 为 `B40E16...81CD2F`。
- Python 聚焦回归 `15` 项 GREEN（1 项可选 SDK skip），native protocol/adapter `2/2` GREEN；LF/CRLF/CR 已统一为
  Windows clipboard 的 CRLF 规范线尾。报告和 MCP 修复由 `a0997e65` 推送。
- 已建立 `coat_of_arms_editer_of_ck3` Vue 3 + Element Plus + TypeScript 基线：CK3 子集 parser、诊断、结构化图层/实例
  编辑、确定性 CRLF serializer 与明确标注的浏览器近似预览；5 项单测与 production build GREEN，提交 `c537e172` 已推送。
  当前不需要后端；资源索引、原生 Copy/export 与“发送到游戏”仍优先继续补 MCP。这项旁路不改变天朝二期 P1/P2 计数。

## 09 月 10 日：R390 Stage 11 首入未设变量 RED

- R390 / PID `190136` 在 Stage 10 长跑窗口首次进入 Stage 11 Workforce 时，五个前置终态/等待/active 读取点在
  portfolio 初始化之前读取未设字段，产生 15 条 fetch / unset-scope / invalid-comparison 产品错误。后续
  `tgp_dynastic_cycle.0081` 令场景失效，只能终止本次链路，不能覆盖此前明确归因的产品 RED。
- 生成器现为 Stage 11 全部相关可选变量与 M360 status 建立真正 lazy presence/read 边界，并以 first-entry
  读取/guard 一一对应回归锁定。commit `a507d89ec1d236d0010ea2bf81d5ae35ea70676b` 已 rebase-only 推送；
  聚焦双模式各 45/45、CI 同口径双模式各 `144 + 1492`、静态/本地化与 1031-file 可复现发布构建均 GREEN。
- 当前严格为 `static-ready`：游戏脚本变化不能热应用到 R390，须由下一唯一 CK3 轮次归零旧日志签名。
  open_kaishek 无接口/协议/schema 变化，判定 `NO-CODE-CHANGE`；T0 P1 未签收，P2 视频锁未触碰。

## 09 月 10 日：T0 进度延期复盘

- `5497920` 至 `1b2c391` 的约 27 小时内有 125 个 first-parent、零 merge 提交，但现行 P1 正式机器门
  仍为 `1/9`，净增 `0`。本周不再以提交数、测试数、事件合同数或历史 `50%` 解释产品进度。
- 09-07、09-08、09-09 三次日级 P1 节点连续失约；最早一项截至 09-10 21:51 已延迟约 69 小时 51 分。
  根因是验收分母固定过晚、单一长谱系耦合独立门、operator 判断和启动前 fixture 失误、支线并发侵入主控，
  而不是必须闭环的真实产品 RED 本身。
- 权威复盘与恢复计划：
  [`T0 天朝二期进度延期复盘`](../retrospectives/2026-09-10-t0-schedule-slip.md)。
  在 P1 `9/9` 前停止 361/626/source4/第三次 `.356`、非 blocker 迁移、新 MCP 架构及 P2 视频工作；
  CK3 每轮只对应一个预先声明的机器门或真实 RED 复验。
- 补救交付目标：无新产品 RED 时 09-11 03:30 完成 P1（04:15 缓冲）；至多一个最小 RED 时 10:00；
  P1 GREEN 后再执行宣传工具 fresh update、`8/8` capture、两版并行候选、真实审阅、导出与发布，目标
  09-11 18:00。一个产品 RED加一次视频重制的保守恢复上界为 09-12 12:00；外部发布/人工签核阻点
  必须在出现后 20 分钟内报告。
- 22:10 Stage 11 MCP control 在 CK3 启动前发现 game-root 推导错误；`launch_count=0`、产品未评估、
  CK3=0。该 harness RED 已绑定 artifact/stdout SHA，修复仅增加 exact game rule 文件的 profile/preflight
  哈希门，不扩 MCP 架构；窗口因此顺延 15 分钟。

## 09 月 10 日：R392 启动时序与 computed-equality parser RED

- R391 warm-up 受管退出后，唯一 R392 / PID `106788` 的 native pipe 在地图语义状态可用前约 220 秒被旧
  wrapper 当作 binding GREEN；首个 snapshot 因此以 `BridgeUnavailableError` 保留 harness RED，未执行事件
  选择且产品未评估。MCP cleanup 后 R391/R392、watchdog 与 operator job 均退出，CK3 inventory 为空。
- 最终 loader 日志同时在产品 `zg361_b1_runtime_007_pending_reopen_effects.txt:885` 重现 R387/R389 已出现的
  computed-equality parser RED。最小修复只把 direct `=` 改为同算式的 `>=` / `<=` 双界，不新增持久字段；
  B1 及 sharding 双模式、根静态双模式、1031-file release reproducibility 均 GREEN。下一 fresh candidate
  loader 仍须把 exact parser 签名归零，当前只算 `static-ready`。
- v3 wrapper 只为 exact no-semantic-snapshot transient 增加最长 600 秒等待，其他异常不吞；no-launch 双模式各
  `9/9` GREEN。正式 P1 保持 `1/9`、净增 `0`，P2 视频锁未触碰；恢复计划进入“一个最小产品 RED”分支，
  P1 目标改为 09-11 10:00。

## 09 月 10 日收尾：R394–R400 与换班

- R394 fresh loader 已把 computed-equality 的两条旧 parser 签名归零。R398 到达 Stage 11 首入事件
  `zg361we.242`，旧五字段/15 条错误的 fresh scan 为 0；两项均是回归证据，不替代 B1/Stage 11 terminal 门。
- R399 warm-up 与 R400 gameplay 严格单实例串行。R400 的 materialization、native readiness、loader error scan
  和 loader gate GREEN，但 AF5 在 `revision=5 / active_event=null` 等待 source event 超时；没有执行
  authored42/native41，故业务门保持 RED/PENDING。MCP managed cleanup GREEN，最终 CK3 inventory 为空。
- 本周正式 P1 仍为 `1/9 = 11.1%`，净增 `0`；P2 继续硬锁。完整换班入口：
  [`2026-09-11-t0-p1-af5-handoff.md`](../../handover/2026-09-11-t0-p1-af5-handoff.md)。
