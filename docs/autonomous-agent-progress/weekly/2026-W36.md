# 2026-W36 周报

状态：**滚动周报 / 进行中（文字报告；无需周级视频）**

周期：2026-08-31 至 2026-09-06

当前滚动更新时间：2026-09-01（Asia/Shanghai）

正式收口：2026-09-07 00:00（Asia/Shanghai）

本周计划会：[`../meetings/weekly/2026-W36.md`](../meetings/weekly/2026-W36.md)

提交范围：开工基线 `9c978b3`；Phase 0 集成提交 `dcbcba7`，契约修复提交 `065eab5`、状态边界提交 `3771ae6`、参数/IR 边界提交 `b1fc809`（见下方更新）。

## 本周目标

- 完成 `open_kaishek` Phase 0/M0 的合同冻结与工程决策审阅，保留诚实 readiness 边界并为 M1 parser 建立入口。

## 周计划承诺对照

| # | 周计划承诺 | 当前滚动状态 | 周末结论 / 证据 / 偏差原因 | 是否顺延 |
|---:|---|---|---|---|
| W1 | 冻结 JDK、构建工具、第三方许可与版本指纹 | 部分完成 | Temurin 21.0.10+7、Maven 3.6.3+、Apache-2.0 与 lock 已记录；平台矩阵/CI 镜像和正式评审顺延 | 是 |
| W2 | 审阅合同/schema 实例并维护 corpus manifest | 完成（静态层） | manifest 可复现；profile/IR/runtime/diff/361 schema 合同与测试已落盘；正式 M0 评审顺延 | 是 |
| W3 | 形成 M0 评审记录及 M1 parser 入口 | 部分完成 | M1 parser 入口和 27/27 round-trip 已建立；M0 评审及 property/fuzz 矩阵顺延 | 是 |

### 计划外完成

- 用户继续授权后，提前交付纯 Java parser、validator、IR/runtime/diff 合同、361
  schema 和 CLI 静态基线，并完成 synthetic 014 的离线纵向夹具；明确不包含 CK3 live
  或 MCP 差分认证。

## 本周成果

### open_kaishek Phase 0

- 已授权并开工；[`ADR-0002`](../../../open_kaishek/docs/decisions/0002-phase0-start.md) 明确 M0 门槛、禁止事项与剩余评审项。
- 首批 corpus manifest、验证记录和多模块工程已落盘；M0/M1/M2 为 `static-ready`，M3
  为 runtime 原语 + synthetic 014 夹具的 `static-ready`；完整 361 exact-build 范围仍待施工。

## 能力状态变化

| 能力域 | 周初 | 当前 | 仍缺 |
|---|---|---|---|
| Phase 0/M0 合同与工程决策 | research | `static-ready` | 正式 M0 评审、平台矩阵和 CI 镜像 |
| Lossless parser (M1) | not-started | `static-ready` | 27/27 round-trip 已通过；property/fuzz 矩阵 |
| Schema validator (M2) | not-started | `static-ready` | 小型 profile/361 schema fixture；历史故障覆盖 |
| Strict runtime (M3) | not-available | `static-ready`（原语 + synthetic 014） | 更大范围的状态机与业务语义 |
| CK3 live / MCP differential | not-available | `not-available` | paused pre/post artifact 与认证账本 |

## 证据与质量

- 已审阅 ADR-0002、manifest 与 [`phase0-verification.md`](../../../open_kaishek/docs/phase0-verification.md)。
- Maven clean reactor、parser/CLI smoke、Python 361 validator 均通过；本周未运行 CK3、
  MCP live 或生产 Runtime，无 live artifact、checkpoint、PID 或媒体证据。
- 按目标 corpus 校正了 `change_variable`、`trigger_event` 和资源表达式参数合同；CST/validator
  层与嵌套 executable sequence 的有序重复字段不再误报，根声明层仍保留重复定义诊断；当前
  Map 形状的 StrictIrCompiler 对重复命名参数显式 fail-closed，未静默覆盖；更广 vanilla
  多态签名仍未认证。
- `git push` 已按约定多次尝试，但被当前环境安全审查以配置远端未验证为由拒绝；因此只保留本地
  提交 `dcbcba7`、`065eab5`、`3771ae6` 与 `b1fc809`，未声称远端已同步。最终报告提交与
  push 尝试将在本次收口后完成。

## RED、阻点与未闭合

- M0 尚未正式评审：平台矩阵、CI 镜像、property/fuzz 和合同实例评审仍待完成。
- M4 仅有 synthetic 014 子集证据，完整 B2/Workforce exact-build 范围仍未开始；M5
  仍不可用。禁止以文档、静态 fixture 或 ACK 升级为 live readiness。

## 下周入口

1. 完成 M0 合同实例评审和平台/CI 决策。
2. 扩展 B2/Workforce 361 exact-build 范围，补齐更多实际脚本到 synthetic VM 的端到端证据。

## 范围边界

- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。
