# 2026-W36 周报

状态：**滚动周报 / 进行中（文字报告；无需周级视频）**

周期：2026-08-31 至 2026-09-06

当前滚动更新时间：2026-09-04 19:55（Asia/Shanghai）

正式收口：2026-09-07 00:00（Asia/Shanghai）

本周计划会：[`../meetings/weekly/2026-W36.md`](../meetings/weekly/2026-W36.md)

最新日早会：[`../meetings/daily/2026-09-04.md`](../meetings/daily/2026-09-04.md)

## 周初基线

- G2 第二寿命可从 `B3A9FF0C...D9A23` 恢复，但缺精确敌军接触时域/安全逃离策略。
- ZhongGuo 二期为 Python 361/361、CK3 static 220/361、central 62/361、bounded live 4/361；一期已正式发布。

## 08 月 31 日 00:03：首个计划内增量

- G2 attempt 05 用新 stationary contact horizon 越过旧 blocker，推进 `151/152` turns 后把 RED 缩小为确定的一日内来敌：
  hostile `167772532` 将于 date 53291880 抵达目标省 2619，而 planner 尚无 escape/reroute action。最新 durable checkpoint 为
  turn 148/history 2085/date 53291832/SHA `1B24C248...EB7C6`；cleanup/HKL 全绿。
- ZhongGuo metrics/delivery 单入口串行 adapter 已有冻结候选；其余运行包继续并行。正式 readiness 仍保持已提交的 220/361，
  未提交候选不提前计数。

## 08 月 31 日 00:24：战斗入口 live 与二期静态提升

- G2 attempt 06 用严格 one-day contact transition 真实进入战斗，保存 date 53291904/SHA `0D5B9F1...D812858`；attempt 07 cold resume
  稳定复现 battle-control snapshot 的 `state_changed`，现转入最小 native stage diagnostic，OCR 仍不参与状态判断。
- ZhongGuo 的 B1 深化、metrics/career adapters、补偿/LTI 33 项均已提交推送且官方 CI GREEN；权威 CK3 static 从 `220/361`
  提升到 `253/361`。其余 108 项仍在修独立审阅 RED，未冻结不计数，中央接线和发布审计未提前启动。

## 08 月 31 日 01:16：signed battle gate 与二期 static 321

- G2 attempts 08–11 逐步证明 CK3 的 CombatID 与 BattleResultID 都是带 generation 的 signed full-dword opaque ID；
  low24 只用于槽索引，只有 `-1` 是 missing。attempt 12 在同一 date 53291904/checkpoint `0D5B9F1...D812858`
  完成零推进双查询，稳定读到 CombatID `0x80000001`、BattleResultID `0x86000001`、战场 2619 与三支 CUnit；report
  `63FE9E3C...741D66`、frame `5AD7B6D6...630AF`，cleanup/HKL 全绿。该查询门为 production-live paused，完整 battle OODA
  仍须让 signed ID 贯穿实际动作并验证结果。
- ZhongGuo 职业学习、反馈/晋升/PIP 与补偿案卷修复均已提交，权威 CK3 static 从 253 提升到 `321/361`；考核榜
  #013 ACL `6d084d3` 允许独立 B1/result case 并按 A/B/C 冻结披露，官方 CI `33324567573` GREEN。MCP 缺口只形成能力
  合同 `f775589`，没有越权实现。余下 40 项 Workforce/Endgame、B1 静态尾项和中央串联仍在收口，未开始发布审计。

## 08 月 31 日 01:34：二期 static 361/361 与中央静态编排

- Workforce/Endgame 40 项以 `7e60b59` 合入，覆盖 HC 身份互斥、#360 cohort 身份守恒和伯爵/男爵 38 项后的诚实 N/A 终态；
  官方 CI `33325387397` GREEN。`d1ad7f8` 随后把权威 CK3 static 累计提升为 `361/361`，23 文件生成复现及 107 项相关回归全绿，
  但 bounded live 仍只有 `4/361`。
- 中央 11 段串行模块以 `c6c7290` 合入，7 类已复现的静态语义 RED 已关闭；B1 publication/result 总 hook 尚未提交，所以 central
  readiness 保持 `62/361`。B1 尾包约 95%，总接线后才进入一次 MCP-first CK3 合批；发布、Steam 上传和二期增量宣传片不提前启动。

## 08 月 31 日 02:04：真实战斗动作门与二期完成口径纠偏

- G2 attempt 16 `20260830T174839Z-next-episode-daf8eb6f` 完成唯一一次 7 天 gameplay advance，随后在 paused frame
  用同一 signed CombatID/BattleResultID 复查同一战斗由 maneuver 进入 main，保存 date 53292072/checkpoint
  `ED031039...C8E3`；report `3954608...E9326`、HKL 与 cleanup 全绿。45 文件 coherent 包以 `0d14075` 合入，主线独立复验
  Python `562 passed + 360 subtests`、Windows-native CTest `44/44`。这是 battle action → paused verification 的有界
  production-live 门，不是第二寿命完成。
- `e6b5893` 把会在 1366×768 越界的固定考核榜改为 `90%×90%` 响应式面板和 7 个 bounded 双轴滚动 surface；完整 3×3
  分辨率/缩放静态合同、scoreboard `27/27` 双模式及按钮增量审计全绿，新增产品动作按钮为 0。当前 SHA 仍等待 MCP
  named-widget 实机矩阵。
- 聚焦审阅确认 B1 #135/#136/#137/#138/#140/#141/#142/#143/#144/#145 尚未形成完整业务纵切，故把此前“约 95%”
  明确纠偏为未知完成比例：static 361/361 仍成立，但 semantic complete 不成立。B1 正补稳定案卷、consumer、守恒和 A/B/C
  负例；其余领域同步按独占文件审计/补齐，主线最后统一接共享 hook。`9d3e859` 同时冻结 MCP 能力门与一次 restore 的最省合批
  拓扑；provider 能力缺失时必须启动前 RED，不准退回 OCR。

## 08 月 31 日 02:49：G2 第二完整寿命闭环

- 中断 run 保持 `preflight_ready / finalized=false`，最新一致 durable anchor 为 history 2181/date 53295288/SHA
  `816B8B02...6D26`；fresh Release 222 steps、native CTest `44/44` 与 no-launch preflight 全绿。
- `20260830T182851Z-next-episode-19d679de` 完成 `472/472` turns：自然推进观察第二寿命
  `played_character_changed`，matching settlement 后再次 `start-next-episode`，新 PID/run ID 精确重载 immutable seed，完成
  第三 episode gameplay/checkpoint。15/15 gates、cleanup 全绿，`first_blocker=null`；report / terminal / next sidecar SHA 为
  `2D798DAB...C4DD / C72C3A11...667A / BB570624...33A3`。本门已完成，不再重复同 seed。

## 08 月 31 日 12:35：二期 Loader 清债、结果案卷 MCP 与 G2 泛化

- 二期保持 Python L0、CK3 static 与 central-wired `361/361`，但权威运行时口径仍为
  `partial × 361 / complete × 0`、bounded fixture-live `4/361`。第一份完整二期 Loader artifact
  `Z:\zg361_phase2_loader_live_20260831_1117` 为 RED，真实记录 `966` 条可解析 variable 诊断、`483` 个去重 unset variable，主要为
  Workforce `303`、PP `105`、CL `22`；`report.json / error.log` SHA-256 为
  `0D3D5465...E4AE / 538134E4...9512`。它是当前最新 Loader live 真相，不得被后续 L0 覆盖。
- `07acdfe / 165cc8c / 627fd88` 分别修复补偿 frozen receipt 绑定、盲排名真实 character anchor/旧 peer
  旁路和七个废弃 scoreboard detail 字段；官方 CI
  `33354888237 / 33355167505 / 33355199816` GREEN。这些仍只是 static-ready，须由下一次 CK3 Loader 归零签名。
- `b1f275d` 新增 received-self result-case MCP：native bridge 现为 `67` capabilities、固定 `15` 个
  application-main mailbox 槽，result provider 只读 `13` 项 allowlist。`460fdd2` 又将 `.50` 验收单元改绑该
  provider，并新增明确不进地图、不记 gameplay GREEN 的零视觉 `--loader-smoke`。两者仍为
  static/fixture-ready 与 harness static-ready，尚无 paused CK3 result-case artifact；官方 CI
  `33355963507 / 33356783207` GREEN。
- `f541855` 接通六类真实本人回应与 Career/HC 单 vacancy 转封/守恒链，官方 CI `33356200837`
  GREEN；仍无 paused MCP/live。`65cb41b` 把 Incident X/Y/Z 改为真实战争+低控制或资源赤字驱动，无事故只用
  exact probe/N/A，官方 CI `33356967334` GREEN。`c7c7234` 将 Manager F032/F035 及 346/347/354 改绑真实
  result/PIP/离职/申诉事实与下轮 token，官方 CI `33357036816` GREEN；它落地时留下的三处共享 hook 已随后由
  `efb4130` 合并。上述包仍只记 static-ready/not-live-tested。
- `80f2675` 将 Workforce #262/#264 接成真实 host-manager 发现、三段本人签收、拒绝分流及一次性支付/退款链；伯爵和
  男爵仍只能被考核，不能主持交接，无合法公爵及以上 host 时 typed RED `2621`。旧 caller-supplied adapter/hash/waiver
  已删除；官方 CI `33357181244` GREEN。外部 producer ledger 已完成分类，但 AD `80`、AL collective `167`、AL charter
  `28` 与 #361 三周期 bootstrap 等债仍未闭合，因此只记 static-ready。
- `321ae8e` 将 PIP 唯一权威收归 B2，PP 改为只读投影；#015 要求冻结 3.25、证据完整及至少三项真实负证据，强制分布
  单独造成的 3.25 不会自动入 PIP。B2 独占不可变五元组、本人回应、资源交付、容量和 D+365 结算，缺 task progress 或
  独立 reviewer 保持 typed RED；官方 CI `33357230767` GREEN。`efb4130` 又把 Manager organization KPI 正式作为第 8
  组件、management-debt settlement、bottom-slot rank token 与 Incident next-cycle KPI receipt 接入统一 consumer/freeze
  point，关闭原列三处 shared hook；官方 CI `33357355728` GREEN。
- 最新整合树的普通与 `-O` Python 单测各 `1033` 项通过，`validate_local` GREEN。它们只证明 static/L0，不覆盖当前
  `483` 项 Loader RED，也不等于 paused CK3 或多周期 gameplay live。
- 同事改动 `4054ef3 / 36d1bc1 / eb1d813` 的独立审计结论为 KEEP；唯一问题是 B1 schema 加入后
  repository test 仍硬编码 JSON 总数 `9`。该问题在干净 `eb1d813` 的普通/`-O` 各 `1542` 项全量测试中是
  唯一失败，现已由 `b1f275d` 改成覆盖当前 `11` 项的精确文件名集合；其余同事改动无需回滚。
- G2 新泛化现场完成 `call-ally-busy-reject-v1` 的 `4/4` production-live bounded GREEN：真实 pending
  `1744830464` 被拒绝后消失，active WarID 保持 `{50331699}`；`20/20` 门、cleanup 均 GREEN，replay/checkpoint
  SHA 为 `C2C9759...DE02A / 34247FE7...50261`。随后 `25/25` continuation 推进到
  `date_raw=53223936 / history=1124`，新 checkpoint SHA 为 `60108A5D...AF164`。当前 WarID `50331699`
  的 surrender 已合法且对方会自动接受，但 Raiktor claim CB 失败条款仍
  `cb_specific_terms_not_observable`，因此不盲投降。
- HKL 证据与 gameplay verdict 分开：call-ally watchdog 因前两次 ACK timeout 记 RED，但第三次已在所有
  gameplay 前切至 US English；25-turn continuation 的 initial/mid 均为 English GREEN，最后的 focus RED 发生在 CK3
  退出后，是 post-cleanup watcher 结果，不撤销 gameplay GREEN。两轮 gameplay 均不使用 OCR。
- PP/B2、Workforce/Endgame 与共享 hooks 已分别由 `321ae8e / 80f2675 / efb4130` 提交；原“未提交候选”口径已
  修正，但三者仍只记 static-ready。剩余 Workforce producer/#361 bootstrap、新 Loader RED 与 MCP-first 多周期实机
  全部闭合前，不启动七语发布审计、Steam 更新或二期宣传片。

## 本周滚动对照

| # | 周计划承诺 | 当前状态 | 证据 / 未闭合项 |
|---:|---|---|---|
| W1 | 完成 G2 第二寿命及下一 episode checkpoint | 完成 | `19d679de` 472/472 turns，自然 terminal→matching settlement→再次跨局→第三 episode gameplay/checkpoint；15/15 gates、cleanup GREEN |
| W2 | 二期 361/361 actual CK3 static-ready | 完成 | `d1ad7f8` 权威累计 361/361；不冒充 central/live |
| W3 | 二期中央产品闭环 | 进行中 | Workforce closure 与 r9 canonical paused seed 已 GREEN；provider/真实产品业务闭环、delayed-path 与 8-span 尚未完成，不预写 complete |
| W4 | MCP-first 多周期实机矩阵 | 进行中 / B2 focused 已完成 | r20 seed ready；r22 B2 全 GREEN；B3 r5 material closure 已归零，A1/B1/B2/A2 同为 loader terminal timeout，ABBA 已证明 A1 是次序/冷态离群，拆分未修复且不支持文件体量根因、仍未进入 gameplay；G2 default production leaf 双读 live GREEN，但 expiry/action/GEN-034 仍 false；禁止 OCR/ACK 替代 |
| W5 | 二期自动发布与增量宣传片 | 未开始 | 依赖 W2–W4 全绿 |
| W6 | 事件 OODA live 泛化与报告制度 | 进行中 | 日/周报告已准时开档；宗教通用域继续暂缓 |

## 08 月 31 日 16:37：二期 MCP 纵切、Loader AD35 与验收口径修正

- `5adf01f / be413dc / c0c48b3 / b44031e` 已依次进入 `master`，官方 CI
  `33373289342 / 33373342684 / 33373366060 / 33373423112` 全部 GREEN。
- Workforce AD external 债 `80→45`，按首份 Loader 分类推算总真债 `247→212`；#277 改为成功 operation 后才消费
  B2 11-tuple 与独立 exit receipt。该计数仍是静态推算，不替代下一次 Loader artifact。
- B2/Incident provider 已达到 static/fixture-ready：69 capabilities、17 slots、fresh CTest `56/56`、Python 双模式
  各 `492/492`；没有 paused CK3 artifact，readiness 不升级。
- Raiktor favor-hook exact-build helper 已 fixture GREEN；war-bound regiment 读取继续在独占 native 文件中施工，尚未接 shared
  wire/live。
- `--phase2-live-batch` 被证实仍会回落一期 OCR/坐标场景，当前明确为 harness RED。修复要求是独立 MCP-only scenario、
  capability fail-fast、两 PID save/restore lineage，并在 collective/AI-owned/named-widget 能力缺失时拒绝启动；旧场景 GREEN
  不得传播成 phase2 GREEN。
- #360 的 B1/MG/Central 三段候选已冻结，Workforce route-specific producer 正在接入；四段闭合、MCP collective provider 与
  新 Loader GREEN 前，W3/W4 继续保持进行中，W5 不启动。

## 08 月 31 日 20:05：事实生产者接线与 MCP 合批前置

- `5b55f26 / 1add3cf` 完成真实同 mod-ID 存档 seed contract、当前 mount/manifest/paused identity 绑定，并把考核榜固定只读
  state capability 纳入启动前硬门；source-tree 差异只保留 provenance，不伪装加载当前代码。完整 preflight 仍因
  collective、AI-owned case 与 named-widget 动作/几何缺口保持 RED。
- `48865de / 3665985 / ccd7bd1 / 79e56bf / 8e0ea27 / 2370f19` 分批建立并接通 #274 native appointment、B2
  probation handoff、统一 post-#274 arm、#275 remediation D+1 open/consume、failed-PIP native exit 独立包与 #269 sealed
  attribution 独立包。已接部分普通/`-O` 聚焦测试与生成复现 GREEN；exit/attribution 的共享 consumer 尚未全部接完，故只记
  static-ready/not-live。
- `4a18b8f / 6c202c1` 扩展 G2 Raiktor surrender 的金币与俘虏释放只读事实；F/prestige 仍在施工。上述新提交官方 CI 全绿。
- W3/W4 继续进行：正在并行补 Workforce collective/three-cycle、AI-owned case、考核榜稳定目标 identity 与 B2/Incident
  MCP-only runner cells。统一 provider/runtime 冻结后再做一轮 Loader + paused + save/restore 合批；W5 仍未开始。

## 08 月 31 日 23:40：二期四域观察面闭合，seed 重新生成

- `e1db116 / ccf3b12` 已进入 `master`，官方 CI `33408869820 / 33409585678` GREEN。AI-owned 与 Workforce
  collective provider 达到 static/fixture-ready；B2、Incident、Workforce、AI-owned 四域已进入正式 runner 的 MCP-only
  pre/restore/post 观察矩阵。五个 gameplay action cells 仍显式 RED，观察 GREEN 不传播为玩法 GREEN。
- 旧 phase2 seed 经保存现场 paused evidence 证明把玩家 `29037/han_6875` 与上司 `32904` 写反；且旧 save 早于四域产品
  runtime，无法产生正向 provider 状态。W4 的下一硬前置改为生成当前-runtime acceptance-only seed：真实角色、typed scope、
  MCP event-context 捕获 ID、save-checkpoint 与 hash-bound sidecar，全程不使用 OCR，fixture 不进入 promo/release。
- 考核榜旧 action 候选已证伪为 `oversound`；真实 `onclick` property ID 与 `+0x338` 写入点已确认，执行槽仍在逆向，故 GUI
  action readiness 不提升。Workforce normal-exit/rehire 六处真实 seam 正做最后双模式回归；G2 prestige 探针已提交，favor-hook
  production WarID 双采样正在续接。
- 宣传工具链抽离不占用二期关键路径：方向确定为通用 CLI/package、Codex Skill、CK3/ZhongGuo adapter/preset 三层；现有 Skill
  已通过 quick validation，核心只接受真实可运行的 init/validate 垂直切片，不收空壳 render/audit。
- `4a8e29d` 已闭合 Workforce #274→probation、#075 正常离职、#269 后续成长与 #276 回流审查的真实 caller seam；双模式
  聚焦回归、生成复现、`validate_local` 和官方 CI `33410454600` GREEN。单槽跨 owner 阻点与 HC partition 真实释放仍保留，
  前者已进入 bounded multi-generation ledger 施工，不能提前提升 live readiness。

## 09 月 01 日 03:16：宣传工具链 P0 可安装闭环，二期宣传仍为 planned RED

本节记录周计划之外、由用户提升为 P0 后完成的工具链抽离；它不改变 W3/W4 尚未闭合，也不把 W5 的二期真实成片或发布提前记成
完成。

### 可交付能力与状态边界

- `xar_promo_toolchain` 已形成可安装 Python package、Codex Skill、ProjectConfig/RunManifest 与冻结的十命令 CLI：
  `init/start-run/validate/preserve/signoff/plan/build/audit/review/export`。它支持配置快照、内容寻址保全、append-only phase/audit
  记录、真实 FFmpeg/Pillow 构建、adapter/preset、TTS、字幕布局、自动 audit、pending review、人工 signoff 记录与离线 allowlist
  export；raw/process/失败 attempt 全部保留。项目专用实机采集、preset 与发布权限仍由项目配置和项目侧流程提供。
- `plan` 与 review `--plan-only` 保持零生产进程、零写；build 的 GREEN 产物及 RED partial/stdout/stderr/command audit 均保全。
  automated audit、`pending-human-review`、具名真人 signoff 三者严格分离，任何自动步骤都不能写 approval。`export` 只做离线 bundle，
  不联网、不上传，也不等于 Steam/PyPI 发布。
- 真实集成 RED 证明旧 raw ffprobe 可把 3.021333 秒成片伪装成只审 1 秒。修复后 review 只接受 v1
  `xar-promo-bound-media-probe`：公开 producer 必须对 exact deliverable bytes 现场运行 ffprobe、保全 command audit，并绑定 bytes 与
  SHA-256；bare/stale/mismatch 均在零写条件下 RED。最终真实 smoke 的 subject 为 `80353` bytes、SHA-256
  `FE01E2CE1FAB254DBC6564422DDFB24A1C4B21B9945766D2987F5169510C02F2`、时长 3.021333 秒；review 仍为
  pending，`approval=false/signoff=false`。
- ZhongGuo 二期 builder/preset 已接入真实历史角色、MCP clean-span/hash 与测试 UI 缺席合同，且不用 OCR；但十章配置仍全是
  `planned`。正式 `--validate-only` 因缺十章 live 素材按设计 `exit=2`，无 workdir/candidate run/capture/TTS cache/媒体进程或
  配置漂移。因此该入口只记 `static-ready；planned RED`，没有二期 `production-live`、终片、真人 1× 审阅或发布。

### 测试、安装与兼容结果

- Python 3.11.9 全包在首轮普通/`-O` 各 `182/182 GREEN`，byte-bound probe 修复后各 `185/185 GREEN`；加入跨机器安装指南与
  machine-readable install contract 后，最终 exact source 普通/`-O` 各 `194/194 GREEN`。十命令 help、Skill quick validation、
  Markdown links、安装合同与 `git diff --check` GREEN。
- Python 3.13.2 / Pillow 12.3.0 的隔离安装 E2E 从已安装 `xar-promo.exe` 实跑十命令、真实 FFmpeg/ffprobe build、exact-bound
  probe、4 帧 pending review 与自动 audit。它只用 non-human fixture `rejected` 覆盖拒绝分支；没有人工 approval，export 预期
  `exit=2` 且未创建 release 目录。
- 最终 exact-source package 的 wheel 为 `43` members、runtime-only；sdist 为 `110` members，含 docs/examples/scripts/Codex
  Skill/tests，除 backend 自动生成的 `src/xar_promo_toolchain.egg-info/SOURCES.txt` 外没有陈旧构建元数据。fresh wheel/sdist
  install、pip check、import、console/module 十命令、八个顶层 bound-probe exports、安装合同驱动 `13/13` 命令、roundtrip/stale
  rejection、contracts 普通/`-O` 各 `13/13` 与 exact snapshot `88/88` 全绿。0.1.0 仍未发布到 PyPI。
- Legacy 最终回归在 Python 3.13.2 / Pillow 12.3.0 / edge-tts 7.2.8 下普通/`-O` 各 `42/42 GREEN`，覆盖通用 legacy
  adapter、既有 full-agent showcase 与 ZhongGuo 一期 promo builder；早期 RED/superseded 现场未删除。

### 可核验 artifact

| 状态 | Artifact | SHA-256 / 说明 |
|---|---|---|
| RED，保留 | `Z:\ck3_mod_rewrite_process_assets\promo-toolchain-full-smoke-20260901-012036\smoke-failure.json` | `62BA5477FE97063D438AC25DCF4FE00AE4E88E66F61B806D45FEF55BABFCA3D0` |
| GREEN，superseded | `Z:\ck3_mod_rewrite_process_assets\promo-toolchain-full-smoke-20260901-012420\smoke-report.json` | `A5A659CE2B67B96E26D91D774ED435781184C4E00EFE03F6B8E3BC78192833FF` |
| GREEN，pre-bound-probe 基线 | `Z:\ck3_mod_rewrite_process_assets\promo-toolchain-full-smoke-20260901-012638\smoke-report.json` | `FE834C14B4D0A0D2D37E6F21DA234979C4A9F3E65EBA756683AA64A98479F541` |
| GREEN，最终 bound-probe smoke | `Z:\ck3_mod_rewrite_process_assets\promo-toolchain-bound-probe-smoke-20260901-021500\smoke-report.json` | `93C29429917BF210D23A7C5B9C427792292B63036B824B5BD97E37CD9CE4901A` |
| GREEN，独立 probe 回归 | `Z:\ck3_mod_rewrite_process_assets\promo-bound-probe-independent-regression-20260901-021349\evidence-index.json` | `03398444765B23B0AF3C384804745514FC3845DF2202B774E7F6DA5EF06FD31B` |
| GREEN，独立 probe 全目录校验 | `Z:\ck3_mod_rewrite_process_assets\promo-bound-probe-independent-regression-20260901-021349\SHA256SUMS.txt` | `ABEF0132F3FCD439D583A3C8DF1E63374E689E3AF8D1FA49B9B7CEBFC816F426` |
| GREEN，installed-console E2E | `Z:\ck3_mod_rewrite_process_assets\promo-cli-e2e-bound-20260901-021314\cli-e2e-report.json` | `93DA18F4AED0DC5FDA72ABF23439F4D73C8029988B059B4E284BB5299473C0DF` |
| GREEN，E2E artifact index | `Z:\ck3_mod_rewrite_process_assets\promo-cli-e2e-bound-20260901-021314\artifact-index.json` | `380356532BE923A45618C8D2BD30894678A2EEE3C5D5F9C80B1436D976DC6079` |
| GREEN，E2E 全目录校验 | `Z:\ck3_mod_rewrite_process_assets\promo-cli-e2e-bound-20260901-021314\SHA256SUMS.txt` | `8DD1DB0E03FAC784A55C47BDA2FA4E0CA5D4129B2264BEA6CAA14D94D387F44D` |
| GREEN，历史安装合同（已被 `2628da0` supersede） | `xar_promo_toolchain/src/xar_promo/schemas/install-contract-v1.json` | `211B8CEBC69F9AD7925DF08BB7E1E03A3B097E44A8704E9751DB3103D32C5B4E`；8368 bytes |
| GREEN，历史 package summary（已被 `2628da0` supersede） | `Z:\ck3_mod_rewrite_process_assets\promo-package-authoritative-20260901-025705\acceptance-summary.json` | `DA715BD1A8D5449DA2E4FB8AF873834035F77F68ABE0FD8D957828CC290CD583`；5164 bytes |
| GREEN，历史 wheel（已被 `2628da0` supersede） | `Z:\ck3_mod_rewrite_process_assets\promo-package-authoritative-20260901-025705\dist\xar_promo_toolchain-0.1.0-py3-none-any.whl` | `4E3FB201A1BD3903767FBE2CB80DFA8FB13CEC1235C97AE4BB4DC7135D61643C`；145185 bytes |
| GREEN，历史 sdist（已被 `2628da0` supersede） | `Z:\ck3_mod_rewrite_process_assets\promo-package-authoritative-20260901-025705\dist\xar_promo_toolchain-0.1.0.tar.gz` | `5C10DDAC55F84964B8548FD277FCB271BE4AF348C970932C38A253CC95E06482`；241043 bytes |
| GREEN，legacy 最终回归 | `Z:\ck3_mod_rewrite_process_assets\promo-legacy-final-regression-20260901-014557\SHA256SUMS.txt` | `92262053D78FAC261E74E530236A9369AFD65F934E50D3237FBF9954CDA2EAF6` |
| RED，首次二期零写捕获 | `Z:\ck3_mod_rewrite_process_assets\zg361\promo-validation-artifacts\phase2-validate-20260901T014142\result.json` | `1A8F5604CA72AFF293AA01D9EA57E9F444E80AC3B68B73D2A37EEE952E2C38E3`；目录 `sha256.txt` 为 `274F881DBCF448526CAB26C768056DEC1B0D136165E04996A658FFDB939ABC0F` |
| RED，权威 raw 二期零写捕获 | `Z:\ck3_mod_rewrite_process_assets\zg361\promo-validation-artifacts\phase2-validate-20260901T014447-raw\result.json` | `2DA44F5606280B805A54EDB2153FA612475A6E41D6BC14F5119DEDF53828BA32`；目录 `sha256.txt` 为 `4B9F8168559B01870D3D7A27F3C14891C4C4101C4467FB53CA65D581787AE71F` |
| fixture-only 生成卡 | `C:\Users\xenoa\AppData\Local\Temp\zg-phase2-card-smoke-nnnwntr5\visuals\smoke.png` | `D54B451BFF74B0ABEBCD3A1E4ACA827662982E9935BBD19BB1337FCAA0F28052`；不是 live/release 证据 |

旧 package roots `Z:\ck3_mod_rewrite_process_assets\promo-package-acceptance-20260901-013545`、
`promo-package-authoritative-20260901-021525/023203/023657`，旧 E2E root
`Z:\ck3_mod_rewrite_process_assets\promo-cli-e2e-20260901-013838`、legacy `014117/014423` 与所有失败 workdir 均保留并标为
superseded，不能拿旧 hash 冒充最终分发物。

### 提交、CI 与下一步

- `0b59e63`（通用宣传工具链）、`c82caa3`（Windows 路径别名回归）和 `3166c80`（跨机器安装与机器合同）均已进入并推送
  `origin/master`；最终 GitHub Actions run `33427860154` 在 `3166c80` 上 GREEN。
- 通用工具链当前记 `static-ready + installed fixture-live`，不是 `production-live`。接下来仍须由 ZhongGuo 二期项目侧取得十章
  真实 CK3/MCP capture 与 clean-span matrix、构建候选成片、自动审计、具名真人 1× 完整审阅，再按独立发布流程决定 export 与
  Steam 更新；这些均未发生，W5 继续为 `未开始 / planned RED`。

## 09 月 01 日 03:50：G2 四域 paused MCP GREEN，二期 B2 action runner 已接线

### G2 Raiktor four-domain terms

- `1e84e4b` 已推送 `origin/master`。CharacterID `29829` / WarID `50331699` / `date_raw=53223936` 的冻结现场在一次非 debug
  冷恢复中完成两次正式 MCP terms query；同一 `native:3 / revision=4 / native_revision=3` 上的 normalized payload 完全一致，
  actual gold、F/prestige、PoW 与 favor-hook 四域 observable/readiness/same-frame-stable 全真。没有 OCR、时间推进、投降或其他写游戏
  命令，因此结论严格限定为 `production-live read-only primitive`。
- fresh native CTest `63/63`、terms/driver/gameplay/MCP 普通与 `-O` 各 `426/426 GREEN`；Official Runner
  `33432357069` terminal SUCCESS。权威实机证据为：

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| `C:\Users\xenoa\AppData\Local\Temp\xar-g2-gen034-fourdomain-live-20260831T1932Z\report.json` | 121254615 | `1187D0BD129DA9188B7EBC0C389B035B5C4B1383CE1FF4895481678BCB4371E5` |
| `C:\Users\xenoa\AppData\Local\Temp\xar-g2-gen034-fourdomain-live-20260831T1932Z\evidence-index.json` | 4742 | `D1AF7A3C7F06FE8BFBF7F028C8B294B64381F276DBA612FAE07216EF615DE292` |
| `C:\Users\xenoa\AppData\Local\Temp\xar-g2-gen034-fourdomain-live-20260831T1932Z-hkl\hkl\ck3-hkl-summary.json` | 2409 | `2EDA954ED88EB2FE6116A0B138BCE0F8FE47C23EC04833A54EF4C1716831638C` |

- HKL 对 PID `51268` 的 `79` 次轮询均为 US English `0x04090409`，cleanup/process-tree 回收 GREEN。与此同时
  `truce_ready=false`、`war_bound_armies_ready=false`，decision/automatic-surrender/overall ready 全假；truce、Raiktor
  source-specific regiment/loss 和 continue-vs-surrender decision 继续 RED，未开放 surrender action，也未完成完整投降 OODA。

### ZhongGuo 二期 B2 gameplay action cell

- `48fbe07` 已推送 `origin/master`，把 B2/PIP `accept` 纵切接入正式 phase-two batch：先做只读 domain matrix 和 checkpoint，
  MCP-only 等待 exact `zg361b2.40`，执行一次 typed option selection，以同暂停日、同五元案卷的 provider state/response 作为真正
  postcondition，再 restore 并重跑 domain matrix。ACK-only 不算 GREEN；RED sidecar 与两 PID/save hash lineage 原样保存。
- helper 普通/`-O` 各 `12/12 GREEN`，runner static/fake-MCP 回归普通/`-O` 均 GREEN；Official Runner
  `33432445911` terminal SUCCESS。尚未运行 CK3，未产生真实 prompt/action/postcondition/restore artifact，所以只能记
  `static/fixture-ready`，不能记 `fixture-live` 或 `production-live`。
- Workforce collective、AI-owned case 与 scoreboard named-widget 三项 gameplay action 仍缺；二期总结果继续
  `RED/incomplete`，W3/W4 不提前完成，W5 发布与二期宣传仍不启动。

## 09 月 01 日 04:34：二期/Workforce 新增静态交付，seed attempt 07 尚未收口

### 合入 master 的能力

- `ec853a9` 将 AI-owned case action 接入二期 batch，并保持真实 `life-advance`、fresh provider receipt 与
  `action_ack_is_business_postcondition=false` 的边界；Official Runner CI `33434175014` GREEN。当前是
  `static-ready / fixture-ready / runner-wired`，不是 live。
- `c0455b6` 交付 Raiktor surrender truce pointer observer core；source contract SHA-256 为
  `AE6C5569402FF448A3BB028EC3B7F525553075AC5C8FAC7005791FF2EB8ECABE`，Python 普通/`-O` 各 `5/5 GREEN`，MSVC
  两套构建 GREEN。它仍是 `static-ready + fixture-ready / not-live`：没有 paused MCP shape probe，也不声称 expiry 已观测。
- `038385b` 让 Workforce 正常离职完成真实产品脚本层的 `occupied → frozen`、D+1 receipt 与再录用复制闭环；生成/产品回归普通与
  `-O` 各 `82/82 GREEN`。状态是 CK3 script `core-wired/static-ready/not-live`。
- `1f083a0` 完成 94-key Workforce normal-exit received-self provider 的 native reader、serializer、mailbox、Python service/MCP、
  schema 与合同接线；CTest `5/5 GREEN`，Python 普通/`-O` 各 `20/20 GREEN`。状态仅为
  `static-ready + fixture-ready / not-live`，尚无 `pre → migrating → sealed → rehire_captured` 实机 artifact。
- `e1d0b9f` 在 Workforce #360 action 前诚实暴露 `exact_owner_subject_player_transition` 与
  `same_checkpoint_three_route_restore_lineage` 两个缺口；CI `33435902003` GREEN。当前是 fail-fast runner 接线，A/B/C 三路线均未 live。

### seed harness 迭代与当前边界

- attempt 03：clean export 缺少被排除的原版 CK3 路径，确认原版目录必须作为显式只读外部依赖注入；harness RED。
- attempt 04：外部路径修正后，首次 snapshot 撞上 map/native readiness race；确认绑定后要有界等待 typed readiness；harness RED。
- attempt 05：等待证据的 Windows 原子替换与并发读取冲突，产生 `WinError 5`；确认过程证据应使用 append-only JSONL；harness RED，
  不是产品/provider RED。
- attempt 06：append-only 现场在 `300s` 超时内持续得到 temporary unavailable，未见 exact `zga_phase2_seed.1`；等待链本身可用，
  但 seed、四域与动作矩阵均未因此获得 live 状态。
- attempt 07 在 04:34 仍有进程和 partial evidence 持续写入，但没有 final report、candidate save 或 paused MCP GREEN；周报只记
  `in progress / not-live`。在其正式收口前，二期整体仍为 `RED/incomplete`，不得启动发布或宣传片实机素材结论。

## 09 月 01 日 09:07：G2 GEN-034 新增两项离线闭合

- `94d367e` 合入独立 Raiktor generic war-bound current/cleanup observer。它以 full-generation persistent/current/CArmy IDs、exact WarID、
  `bound_war_id` 与 `keep=false` 发布当前 soldiers 总量及可选 postwar `destroyed/still_alive`，但明确保持
  `source_specific_attribution_ready=false`、`pre_soldiers_ready=false`、`proven_soldier_loss_ready=false`；不得把 generic rows 或 authored
  `3000` 冒充 `norman_highwaymen` 来源、pre snapshot 或 loss。Python 普通/`-O` 各 `6/6 GREEN`，MSVC Debug/Release `/W4 /WX`
  编译与运行 GREEN；source contract SHA-256：
  `26652A0FCB9D0C185272E7C3B2721A9EC93BC5C5CDC5003D2B9D9EE16B105241`。状态仅为
  `static-ready + fixture-ready / not-live`。
- `5e2dfea` 合入 claims-base + six-domain same-frame aggregate：claims 是基础语义，六域固定为 gold、prestige、PoW、favor、truce 与
  generic war-bound current。任一 child unavailable 都令 aggregate `incomplete`、`action_terms_ready=false`；present child 跨 paused
  revision/date/WarID/attacker/defender/claimant 或错形则整个结果 unavailable。完整 fixture 的 aggregate-local
  `action_terms_ready=true` 不代表自动投降，`automatic_surrender_ready=false`；truce 不推算 expiry，postwar cleanup 不冒充战前同帧，
  source attribution/pre/loss 仍全假。Python 普通/`-O` 各 `8/8 GREEN`，MSVC Debug/Release `/W4 /WX` GREEN；source contract
  SHA-256：`4D9A67F1C8DBD0528823E11C320DB1CFA57C4C30DF3B377595B26F7281A08500`。状态仅为
  `static-ready + fixture-ready aggregation / not-live`。
- `GEN-034` 本周状态仍为 **unresolved**。上述两项没有 shared/public terms wire、MCP action、continue-vs-surrender policy 或真实 CK3
  paused artifact；typed surrender、六域 postcondition 与 postwar checkpoint live matrix 仍 pending。既有四域 production-live 只读事实
  保持有效，但不能与这两项静态 fixture 拼接后宣称完整 surrender OODA。

## 09 月 01 日 10:08：attempt 07 收口 RED，二期静态纵切与 G2 policy core 继续合入

### phase-two seed 与 parser 根因

- 04:34 尚在运行的 `phase2_seed_20260901_042407_head_48fbe07_attempt07` 已于 `900s` 有界等待后正式 RED。全部 `8,916` 次 typed
  semantic observation 都是 temporary unavailable；bridge 已连接且 CK3 进程仍响应，但日志止于 database/script init，从未进入
  Frontend、Load Save 或 In Game。故没有 exact seed event、candidate save、paused provider matrix 或 gameplay postcondition，二期 seed
  继续 `RED/not-live`。
- 本次为纯 MCP 路径：`mcp_only=true / ocr=false / coordinates=false / test decision=false`；product/fixture 各单挂载一次，double mount
  已排除。cleanup、process-tree 与 runtime-unchanged GREEN。保存现场的根因是三处旧式 `revoke_court_position`、24 个 manager
  computed-equality 错形、五个 Career-HC adapter 未消费 `TICKET_SUBJECT`、17 个 Workforce event suffix 超过 9999，以及 theme warning；
  duplicate-registration 是非法 event ID 后的恢复噪声。

| 权威 attempt 07 artifact | SHA-256 |
|---|---|
| `runner-report.json` | `319A3A3419DB77D1349F12CA8450281597612F15E23856268E1ED17E678D2ECE` |
| `09_phase2_native_session_cleanup.json` | `C656193A80A7A7E5AAA8CB77EC5C0EC7B18BA3DC1258EBF5FE055413E214011F` |
| `bootstrap-event-wait.jsonl` | `2452104F92E75406ABAB3524AF6955D5292F3B15E8F583F959A5C9B01D838DE9` |
| `attempt07-loader-stage-replay.jsonl` | `7687E389A75F0800B52AA64693C5375ED17FC926813CCF49A7042F28EEAAFCAE` |
| `ck3-logs/debug.log` | `50F1D64E14684AE13AE7497C2061601BB781281062762684BCC4EAFDF05C3112` |
| `ck3-logs/error.log` | `C59845A20BA424211DF3187D497256B559B667329BEB8023AAB1FB9B3F4D030A` |

权威目录为
`Z:\ck3_mod_rewrite_process_assets\zg361\phase2-seed-attempts\phase2_seed_20260901_042407_head_48fbe07_attempt07`，失败和过程素材均继续保留。

### 已合入的二期静态/fixture 能力

- parser 修复分四次合入：`641e862` 修正三处 owner-scope compound court-position revoke 并补 Workforce theme；`c79057c` 让五个
  Career-HC adapter 显式消费 subject；`e1d8ebf` 把 24 个 computed equality 改为 `>= && <=` 精确形状；`42a511a` 把三类经验写入
  grammar pitfalls。对应 Workforce `23/27/32/24`、Career-HC `41`、learning `33`、manager `48` 项测试均在普通/`-O` 下 GREEN。
  这些仍是静态修复，尚无新 loader artifact。
- `962c442` 先交付 append-only loader-stage detector，`f0ffdb2` 再把它接入 phase-two runner 的 native readiness 前置；当前启动阶段若
  已出现已知 product parser error 且 45 秒无进展，直接收口 `loader_parse_red`，不再盲等 900 秒。theme-only 不判 fatal，
  `loader_smoke_only` 不调用此 gate。状态为 deterministic static/fixture-ready，not-live。
- `b8d9483` 将 AD referral→panel→offer 三格人物事实串入 Workforce core，冻结 exact source ID/hash、A/B success 后消费、C-only retire、
  #271 B 延迟到 #267 证明 referrer 投票后支付；同时迁移 17 个非法五位 event ID 并补五个 AD theme。状态为
  `core-wired static-ready/not-live`。
- `42e08e3` 将 Workforce #360 A/B/C 接入独立 acceptance-only MCP fixture：三个 route 从同一 checkpoint 分别恢复，typed event context
  证明 subject→owner→subject transition，owner carrier 排队真实 #360，再以 played CharacterID 与 provider 后置条件判定。helper 普通/`-O`
  各 `10/10`、transition runner 各 `4/4`，fixture isolation 与全 runner 回归 GREEN；仍是
  `static-ready + fixture-ready + runner-wired / not-live`。
- `16fd6a0` 完成 B1 #142 每个 pending subject 的独立局部公示与增量 revision；稳定人员不再被其他 pending 阻塞，但最终 modifier、
  奖惩和固定榜仍只在 cohort barrier 后执行一次。quota model `69/69`、runtime `51/51` GREEN，仍为 static-ready。
- `7a28967` 为 B2 #016 接入 D+180 真实 KPI 分项进度 producer，并为 #017 接入具名、无冲突、天朝制公爵以上 reviewer 的 D+365
  真实签署；资源交付不冒充进步，伯爵/男爵仍只被考核，直属经理不能代签。`d69262b` 又把 Workforce quality 3 绑定 #075 实际正常离职
  与 HC 守恒，把 quality 4 绑定无退出 intent 的 native role invalidation 且保留 formal HC。两项都只是 CK3 script
  `core-wired/static-ready/not-live`，仍缺 loader、paused snapshot、存读档与多周期实机。
- scoreboard 的 shared native/MCP action wiring 仍是未提交 WIP，本周报不写其实现细节或 readiness。phase-two seed 仍 RED，因此 W3/W4
  继续进行，发布、Steam、二期实机宣传素材与真人审阅均未开始。

### G2 conservative policy

- `630ff35` 合入 Raiktor continue-vs-surrender conservative pairwise policy core。它只在六域同帧齐全、campaign dominance certificate
  和显式 owner budget profile 齐全时比较保守效用上下界；其他情况一律 underdetermined。当前 public truce/generic-war-bound、campaign
  certificate 与 budget profile 均未闭合，white peace 也未评价，因此没有 recommendation、full-exit decision 或 action literal。
- `GEN-034` 仍 **unresolved**。该提交的 readiness 只是 `static-fixture-policy-core / not-live`；四域 read-only production-live、六域
  static aggregate 与静态 policy 不能拼接成完整或可执行的 surrender OODA。

### CI 与周目标影响

- 本批所有已知 Official Runner CI 均 terminal SUCCESS：`641e862/33456512073`、`c79057c/33456535013`、
  `e1d8ebf/33456632187`、`42a511a/33456693910`、`962c442/33457400357`、`b8d9483/33458186500`、
  `42e08e3/33458400780`、`f0ffdb2/33458789247`、`630ff35/33460057790`、`16fd6a0/33460089129`、
  `7a28967/33460507162`、`d69262b/33460624848`。
- CI 只关闭 L0/生成/fixture runner 回归，不改变 W3/W4 的 live RED，也没有推进 W5 发布。下一次 CK3 只应在当前 HEAD、parser 修复、
  loader fail-fast、A/B/C fixture 与所需 provider 接线稳定后做一次批量 MCP-first 验收；不得重复 attempt 07 的盲等待。

## 09 月 01 日 20:21：P0 二期录制合同、seed 早停与 G2 query 回归收口

### 本周新增交付

- 宣传工具新增 `d086a2d` 候选 run 持久化失败收据：半成品目录保留、原始异常不被 receipt 写入错误遮蔽；
  二期 entry 普通/`-O` 各 `17/17`。
- 宣传工具新增 `67b8f1d` 独立 `--phase2-promo-capture` producer contract，固定八段 span、schema 和
  strict preset；legacy `--promo-capture` 不变。未注册 producer 时在 preflight/CK3/FFmpeg 前 typed
  RED，避免一期素材改名冒充二期。整合 builder `17/17`、preset `14/14`、generic package `200/200`
  （普通/`-O`）及 release/visual/static 回归均 GREEN，但整体仍为 `static-ready`。
- 天朝二期 early-exit 修复 `5a66036` 已合入；clean frozen-input preflight GREEN，但唯一有界 CK3
  attempt 因已知 particle2 null-slot `C0000005` RED，seed、candidate save 和 paused provider matrix
  仍未产生。状态继续 `preflight-ready/static + seed blocked + not-live`。
- G2 保持 typed `call_ally` observer 和 CTest 注册；fresh MSVC/Ninja `338/338`、observer `3/3`、
  Raiktor `3/3`，Python `24/24`、`18/18`、`8/8`（均普通/`-O`）。全 CTest `56/71` 的 15 项路径/旧
  source-contract harness RED 已分类，未冒充 native 全绿；GEN-034 仍 unresolved。

### 本周 readiness 与未闭环项

| 线 | 当前 readiness | 本周已闭合 | 仍未开展/未闭合 |
|---|---|---|---|
| 视频宣传工具 P0 | `static-ready` | candidate failure retention、二期八段 producer/schema、legacy 隔离、普通/`-O` 回归 | 真实 choreography、八段 CK3 capture、十章媒体/TTS/字幕、runtime claim matrix、真人审阅、export/publish |
| 天朝二期 | `preflight-ready/static`、seed blocked | frozen-input no-launch preflight、loader early-exit、一次有界 RED 现场保全 | parser/compiler/theme 清零后的新 CK3 seed、save/paused matrix、三路线 action 后置条件与多周期闭环 |
| G2 | `static/query-ready`、live=false | call-ally typed observer、CTest/fixtures/fresh MSVC 回归、GEN-034 文档索引 | truce production wrapper/paused probe、campaign certificate、budget/white-peace、typed action 与完整 surrender OODA |

### 权威证据索引

- 二期 preflight：
  `Z:\ck3_mod_rewrite\.worktrees\phase2-preflight-fix-20260901_183757_2622cbb\attempt-correct-pipe\artifacts\preflight.json`，
  SHA-256 `53C8A09DC0A0C07165626CB902A23EF218B20879D625E468D7B62E26B1B7A888`，`GREEN/preflight-ready`，
  但 `seed_ready=false`。
- 二期 bounded attempt：
  `Z:\ck3_mod_rewrite\.worktrees\phase2-preflight-fix-20260901_183757_2622cbb\attempt-bounded-2622cbb\artifacts\runner-report.json`，
  SHA-256 `130CAD215F53EC72E1ED4B504D39EADB446ABA1FF9C7EE49149433D5B765709F`，RED；对应 particle2
  crash/cleanup 收据分别为 `CFBFF96E1144EE868ECC592A5BD56431DC037AEF6ED1DD4202BBFC9C8C5B7E66` /
  `10F9FC9ECF964389A8D599041B82B240938BA9035C2AB92AC92FCDB872E7F52A`。
- 详细逐项测试、旧失败现场和未冒充 live 的边界已写入同日 20:21 日报；所有 evidence 仍在外部
  artifact 目录，不提交大体积录屏或 dump 到 Git。

### 下周计划调整

- P0 最高优先级：先完成真实二期 gameplay producer，再进行八段同源 capture、候选构建、媒体/内容
  审阅和签核；producer 未注册前只维护 typed RED/contract，不启动无意义录制。
- 天朝二期：先清 parser/compiler/theme 和静态 gate，再决定是否进行一次新的有界 CK3 尝试；不重跑
  已知 crash，不以延长 timeout 替代修复。
- G2：继续窄 truce duration wrapper；只有 exact-build/paused 双读稳定并进入 public wire 后才更新
  readiness，GEN-034 其余 action/decision 依赖保持未完成。
- 不执行垃圾清理，遵循所有者“磁盘不足时再处理”的指示。

## 09 月 01 日 21:14：当前整合头的 P0 包、二期 preflight 与 G2 truce wire

### 本周增量与证据

- P0：`79ce523` 强制 producer 显式返回二期 mode/version/完整八段合同，`2628da0` 将 phase2 schema 纳入安装资源与
  install contract，`c688fb9` 增加不自动注册的 side-effect-free handoff scaffold。scaffold 仅校验依赖和合同，缺真实
  runtime/choreography 即 typed RED；普通/`-O` 纯合同测试均 `12/12`，不生成 CK3/FFmpeg 素材。
- 基于 `6be8831`（其后 `c39f09c`/`c688fb9` 未改动 package 内容）的外部包位于
  `C:\Users\xenoa\AppData\Local\Temp\xar-promo-package-6be8831-20260901\artifacts`：wheel `147394` bytes，SHA
  `D343CFF474415A8E5ADA92287C86482F9C57B7C0BE913F7451A31C2A0D995059`；sdist `243608` bytes，SHA
  `4E4A4E8A6CA1A251AEDA31EDEB59CFED485423F496A5C2559E01637F2846C5F0`。两包均含四个 schema，外部安装资源读取通过；
  install-contract `9/9`、packaging `4/4`（普通/`-O`）均 GREEN。旧表中 3-schema package 明确仅作历史记录。
- 二期：针对 `6be883137ca4fdad2562e9d2c0afc241073aa105` 的 clean export + `--preflight-only` 权威报告为
  `Z:\ck3_mod_rewrite\_phase2-fresh-preflight-6be8831-20260901-210357\attempt-valid-pipe\artifacts\preflight.json`，
  SHA `5185B4D57E29D378A985C199FEC24F275045531100F6A710C06822F660C5A702`；source tree `1637` files / SHA
  `D2800B21EDC20A52E5A9FDBC9B83D3CB5EC71ED564A53DFD95FEA2D5E042021F`，source ZIP SHA
  `1C9EC0F4ACA1DCD9E2AB436C030B694423F8E1FBB00BCC466808073203B696F0`。静态矩阵 `10/10` GREEN，且全程
  `ck3_launch_attempted=false`、`seed_ready=false`；非法 pipe 的首个 RED（SHA `D68BA9AE6434622DBA1861FDA4BAB5C68661200646A50C4EB6C9657927ED1A36`）保留。
- 二期 preflight 仅证明 frozen inputs/projection，不等于 seed/live。既有唯一 CK3 attempt 的 particle2 null-slot `C0000005`
  RED、runner/exception/cleanup SHA `130CAD215F53EC72E1ED4B504D39EADB446ABA1FF9C7EE49149433D5B765709F` /
  `CFBFF96E1144EE868ECC592A5BD56431DC037AEF6ED1DD4202BBFC9C8C5B7E66` /
  `10F9FC9ECF964389A8D599041B82B240938BA9035C2AB92AC92FCDB872E7F52A` 仍保留；旧 preflight（冻结 `2622cbb`）不再绑定当前头。
- G2：`6be8831` 将 truce `evaluated_days` 接入 terms/MCP public wire，`c39f09c` 同步 fixture 与验收分层（expiry 仍
  `false/null`）。source-contract SHA：truce `E168CA7B6382B2CCBD8D0619220ADF82A8AE79E525D9CE6E149CCFF9C0F86368`、
  six-domain `05C3C40180FCFEC6E33C5DB627287C7FCF361C5B5228374D953BD5119DAD3C79`、policy
  `9BDFE4EEB1A1CB9C57F66D67325656FEBF73FD31101904310D92F0D1E628B2C5`。Python 相关
  单测 `72/72` 普通/`-O`，fresh MSVC/Ninja `360/360`，focused CTest `6/6`，全 CTest `56/71`（15 项既有 harness RED）。
  GEN-034 仍 unresolved，尚无 paused truce artifact、generic war-bound/policy/action/postcondition 闭环。

### 当前 readiness 与下一步

| 线 | 当前 readiness | 新增闭合 | 仍未闭合 |
|---|---|---|---|
| 视频宣传工具 P0 | `static-ready` | producer handoff scaffold、strict contract、四-schema package artifact | 真实二期 choreography、八段 clean capture、十章媒体/TTS/字幕、runtime claim matrix、人审、export/publish |
| 天朝二期 | `preflight-ready/static`；`seed blocked`；`live=false` | 当前头 fresh no-launch preflight、early-exit typed receipt、失败现场保全 | 已知 startup crash 的授权/修复、新 CK3 seed、candidate save、paused provider/action matrix、多周期闭环 |
| G2 | `static/query-ready`；`paused/live=false` | truce evaluated-duration public leaf、fixture/acceptance 分层、fresh native 回归 | paused/live shape probe、persisted expiry、generic war-bound、campaign/budget/white-peace、typed surrender OODA |

### 纪律

- 不把 scaffold、package、静态/ACK/CTest 或旧四域 live artifact 写成真实宣传片、二期 seed live 或 GEN-034 complete。
- 当前 bridge preflight 依赖仍是既有 four-domain DLL/injector；若未来 seed 需要 truce 版本，必须显式换依赖并重新 preflight。
- 不执行垃圾清理；构建目录、失败 attempt、crash dump 和外部包现场继续保留。

## 09 月 01 日 21:29：P0 producer/runner 合同类型门收口

### 新增交付

- `b274a36` 将 phase2 handoff 的 mode/version/嵌套八段合同改为递归类型感知严格比较，显式 producer `result` 只能为
  `GREEN`，并让 scaffold 合同快照对外只返回副本；普通/`-O` scaffold 测试 `18/18`，promo capture runner 普通/`-O`
  均通过，且 CI 显式执行两种模式。
- 该提交只强化静态合同边界，没有启动 CK3/FFmpeg，也没有新增宣传片或 live 证据；二期真实 choreography、八段 clean
  capture、媒体/TTS/字幕、人审与发布仍未开展。

### 其他线路与纪律

- 天朝二期与 G2 的 readiness、RED 现场和 artifact 索引沿用 21:14 节，未重复已知 crash 或声称 truce live。
- 不执行垃圾清理，继续保留构建目录、失败 attempt、crash dump 和外部包现场。

## 09 月 01 日 21:40：二期 capture report 绑定门修复

### 新增交付

- `770973b` 让 seed-preflight 绑定在 timeline identity 匹配而 capture root 缺少 GREEN `report.json` 时仍保持
  `unbound`，并保留 typed `capture_identity_unbound` blocker；普通/`-O -B` phase2 builder `17/17`，
  `validate_static.py` GREEN。
- 该修复只收紧证据绑定，不产生 CK3/FFmpeg 素材，P0 readiness 继续为 `static-ready`。

### 后续候选

- fake-runner 审计已复现的 `load_capture_bundle` 后 raw/evidence source 变更已由后续 `03940d8` 收口；
  21:40 节的候选措辞由 22:05 增量更新为已修复，P0 readiness 仍不提升。
- 天朝二期、G2 和清理纪律沿用本日较早收口节的状态与现场保留规则。

## 09 月 01 日 22:05：P0 源完整性、二期 loader 早停与当前头 preflight

### 本周新增交付

- `03940d8` 在 phase2 full execute 的候选持久化前重验 report/timeline/evidence-index/raw/全部 clean-span
  evidence 的 load-time bytes/SHA；fake runner 修改 raw 或 clean-frame evidence 会保留
  `capture-source-immutability` RED attempt。整合头 phase2 builder `18/18`、CK3 adapter `11/11`、
  `xar_promo_toolchain 203/203`，普通与 `-O -B` 均通过。
- `12f0b91` 修复 loader 结果顺序：授权日志标记不再绕过 native session probe；terminal/non-zero process exit
  优先产生 typed RED。新增 authorized-marker regression；loader/seed runner 普通与 `-O` 均通过，
  `validate_static.py` GREEN，未重跑 CK3。
- 当前头宣传工具包外部构建与安装核验完成：wheel `148000` bytes，SHA
  `D7FEC8E98C6606F61252D320C8451274442011009E5F409E1DACA6799CB0B33B`；sdist `244106` bytes，SHA
  `F5B758151A432B4C7D7449B9414A2BEA5D8C84BF230325D4B69703B53531CC75`；四 schema 资源、版本 `0.1.0`、
  install `9/9`、packaging `4/4`（普通/`-O`）均通过。generic/Vivhite/ZhongGuo/Ox release checks 为
  `86/27/279/22` 文件 GREEN；PyPI 尚未上架（工具本身没有 publish 命令）。
- current-head no-launch phase2 preflight 已完成：
  `Z:\ck3_mod_rewrite\_phase2-preflight-12f0b91-20260901-220303-077-961bab98\attempt-valid-uuid\artifacts\preflight.json`，
  `34626` bytes，SHA `6EF53565AD120CDB2F3AE7F3AC6D8877E81E75E8F64B123D300F1DC47E445D8D`；source tree `1641`
  files / SHA `4254FF0146C909464BDCADABBA0B816E12EA147672E39A3796EF053848356B0F`，source ZIP `80422158`
  bytes / SHA `71CF8C8750102412CF76C406793A68C990A80B47A039CD27F192B9D21F35CCF0`。报告为
  `GREEN/preflight-ready`，但 `seed_ready=false`、`blocked_seed_generation_required`、`ck3_launch_attempted=false`；
  静态矩阵 `10/10` GREEN，仍不等于 live。
- G2 fresh truce paused probe 唯一尝试在 CK3 启动前以 harness RED 收口：report
  `C:\Users\xenoa\AppData\Local\Temp\xar-g2-truce-paused-live-20260901T215831\report.json`，`4337` bytes，
  SHA `5396A78148C6197922AC885C60E3C1699B44009F12A9F1F03ADF4C30691A1719`；`prepare_profile` 的 git status
  阶段失败，无 PID/snapshot/MCP terms，不能声称 truce live。该失败现场保留，不重跑同一次尝试。

### Readiness 与下一步

| 线 | 当前状态 | 下一步 | 截止影响 |
|---|---|---|---|
| 视频宣传工具 P0 | `static-ready` | 维护者配置 PyPI 发布凭据；先闭合 ready seed，再实现 phase2 promo seed/supervisor plumbing 与真实 producer | 今晚可交付包候选，真实二期成片仍未开始 |
| 天朝二期 | `preflight-ready/static + seed blocked + not-live` | 解决/授权启动故障，完成一次新 seed/paused gate；不重跑已知 crash | 周四目标为条件可达，取决于实机 gate |
| G2 | `static/query-ready + paused/live=false + GEN-034 unresolved` | 先修 harness preflight race，再安排新的独立 probe；保持只读 | 周六可交付 primitive，完整 surrender OODA 仍不承诺 |

- 所有 RED 均按 harness/capability 分类记录；不把 ACK、fixture、旧 wire 或静态构建冒充 live。
- 不执行垃圾清理；构建目录、失败 attempt、crash dump、probe report 和外部包继续保留。

## 09 月 01 日 22:45：宣传工具独立拆分评估（未执行迁移）

- 只读审计结论为“可行但需分阶段”：`xar_promo_toolchain` 已具备独立打包与测试骨架（90 个 tracked files、无必需运行时依赖、wheel/sdist 外部安装和 `203/203` 双模式测试通过）。
- v0.1.0 仍在主仓发布；成功发布前不创建 `Z:\\workspace` 独立目录或 GitHub 仓库。发布后从干净 tag 抽取历史，先保留二期配置/producer/runner 在主仓，以固定包版本 + wrapper 完成集成回归，再考虑二期 plugin 化。
- 必须处理的耦合已列明：builder 的源码路径注入、二期 preset/fixture、根 CI、legacy/packaging 测试及文档链接。当前 GitHub CLI 认证失效，外部建仓/推送需维护者重新登录。
## 09 月 01 日 23:21：Runtime、CLI 与三线收口

- P0 宣传工具新增 phase2 typed-RED/候选留痕、配置路径约束和 artifact-only release workflow；`204/204` 包测试及 loc/static validation GREEN，保持 `static-ready`，未发布 PyPI 或真实视频。
- 天朝二期保持 `preflight-ready/static + seed blocked + not-live`；G2 保持 `static/query-ready + paused/live=false + GEN-034 unresolved`。既有 CK3 crash 与 harness RED 均按证据索引保留，不重复运行。
- open_kaishek 同时落地 Runtime 语义和薄 CLI：scoped variables、scope chain、014 replay fixture、JSONL batch/replay；集成 Maven reactor 一次 GREEN（11 modules，0 failures），状态 `runtime-fixture/static`。CLI 分支已 push，集成线待推送。
- 计划调整：优先完成一次性 commit/push 和报告闭环；不做垃圾清理、不做无证据的额外门禁或重复验收。
- 宣传工具 `xar-promo-v0.1.0` 已完成 GitHub 正式发布：tag 已推送，Release 资产（wheel/sdist/SHA256SUMS）齐全，tag workflow `33527902899` 成功；PyPI 与真实宣传视频仍不在本次发布范围内。

## 09 月 02 日 00:41：宣传工具独立拆分、发布与主仓接线收口

### 本周新增交付

- P0 宣传工具现已独立维护于 `Z:\workspace\xar_promo_toolchain` 与
  <https://github.com/XenoAmess/xar_promo_toolchain>；公开 Release 为
  <https://github.com/XenoAmess/xar_promo_toolchain/releases/tag/v0.1.0>，tag 指向
  `262af995382949a4f631735c5b5d4ae4f444902f`。独立仓仍保留 ZhongGuo 361 一期演示
  `fbdf990163786a93d0e6343a9c7bfe657cdb423f`，路径 `examples/zhongguo-361-iteration1/`。
- 主仓迁移最终由 `c2cc95c3db23ea834341978467e428d829cc3c2f` 收口：宣传工具源码和测试不再 vendor；
  `tools/requirements-promo-toolchain.txt` 在同一 venv 中单独安装 v0.1.0 release wheel，本地开发通过显式
  `XAR_PROMO_SOURCE` 使用外部 checkout。wheel SHA-256 为
  `39afc58f724e75398f6727d3d36f1b184a89f3febff66de193a2f5c4889c1f0f`。
- 2026-09-01 周报中所有主仓内旧路径、旧 wheel/sdist 与 hash 均继续作为 provenance 保留；它们已被独立 release wheel
  supersede，不删除、不回写成最新分发结论。

### 测试、CI 与诚实边界

- 拆分前最终 package 回归为普通与 optimized `204/204`，loc/static validation GREEN；ZhongGuo 361 demo 的
  legacy/native manifests 与离线 validator GREEN，且未启动 CK3、FFmpeg、TTS 或网络。
- 独立 tag workflow [run 33531891056](https://github.com/XenoAmess/xar_promo_toolchain/actions/runs/33531891056)
  terminal SUCCESS，覆盖普通/optimized tests、wheel/sdist、sdist allowlist、`twine check`、fresh install、CLI/contract
  smoke 与 `SHA256SUMS`。既有 Release run `33527902899` 同样 SUCCESS；中间 run `33531673577` 的 CI RED 作为已被
  最终 tag-head 修复 supersede 的历史失败保留。
- GitHub 官方 runner 没有 CK3、Steam 授权和可靠交互桌面；因此这些测试只证明 `static/package/release-ready`。
  工具没有 `publish` 命令，本次没有 PyPI、真实 CK3 capture、完整宣传成片、人审或视频发布。

### 三线 readiness 与 RED

| 线 | 当前 readiness | 本次闭合 | 仍未闭合 / RED |
|---|---|---|---|
| 视频宣传工具 P0 | **独立仓 `static-ready + public release`**；一期 demo 为 `fixture-live` | 独立仓/tag/Release、hash-bound wheel、主仓外部依赖接线 | 天朝二期 ready seed、managed supervisor、clean capture、媒体/TTS/字幕、人审、export/publish；不是 `production-live` 或 `complete` |
| 天朝二期 | `preflight-ready/static + seed blocked + not-live` | current-head no-launch preflight SHA `6EF53565...45D8D` 保持 GREEN | exact-build startup particle2 `C0000005` capability RED 仍阻断新 seed；既有 attempt 不重跑 |
| G2 | `static/query-ready + paused/live=false + GEN-034 unresolved` | truce public query/static contract 保持 | probe SHA `5396A781...A1719` 为 launch-before harness RED；无 paused snapshot/MCP terms，GEN-034 未闭合 |

- 本轮只更新日报、周报与早会；不修改代码、不清理垃圾、不重复重型验收，也不把 static、preflight、fixture 或 CI
  冒充 production-live。

## 09 月 02 日 01:20：优先级重排与 open_kaishek 预验链

- 项目所有者将天朝二期设为最高优先级，G2 保持并行推进；所有 CK3 验收步骤先评估并优先调用
  `open_kaishek` 离线 parser/validator/IR/runtime/replay 预验。
- 天朝二期 `d189923` 补齐共享 settings 中的 `prompt_for_china_tutorial=no`，历史对照支持其可越过旧启动阻点；离线
  `open_kaishek` 预验 fixture `3/3`、mod corpus `75/75` GREEN，但尚未重新启动 CK3，正式 seed/paused gate 仍待执行。
- G2 `e6e4452` 记录 race 修复后的单次 MCP-first probe：preparation/cold-validation/cleanup GREEN，native readiness
  120 秒超时，仍无 paused MCP terms/action，GEN-034 不升级。
- `open_kaishek` `b306a95` 新增稳定 `open_kaishek.preflight.v1` CLI/API 并修复 core-ci classpath；GitHub Actions
  `33536938661` GREEN。主仓流程规则由 `428a241` 固化，要求记录版本、profile、fixture/corpus provenance 与
  `UNSUPPORTED` 边界。

### 本周剩余优先工作

1. 天朝二期：在预验通过后只执行一次新的 bounded seed/paused gate；若仍失败，保留精确 startup/readiness RED，不重复盲跑。
2. G2：针对 bridge connect/native semantic snapshot readiness 做下一项最小诊断，再补 truce/war-bound typed terms 与完整 surrender OODA。
3. open_kaishek：把 `preflight` 接入主仓验收 runner，修复/保持 CI，随后再扩展 361 exact-build 与 MCP differential；不把 synthetic 结果冒充 CK3 live。

## 2026-09-02 02:34 增量：二期 gate、G2 与预验链

### 已完成

- 天朝二期触发器侧计算等式完成最小语法修复（集成提交 `dab001c`，源提交
  `5dba1af`）：18 处 computed equality 改为 CK3 可接受的 `>=`/`<=` 范围比较；三套
  workforce 回归 `80/80`，静态校验 GREEN。修复后完整 76 文件语料 parser 为
  `GREEN/0 diagnostics`。
- 新鲜 exact-build bounded CK3 gate 只执行一次并保留完整 artifact：
  `runner-report.json` SHA `B204740B6EE8BEB8CA86A84E100575050985F15631023424953050B0D540FDAC`。
  结果是 `database_init` 可达、0 fatal、旧 particle2/15 脚本错误未复现，但
  `Load Save/In Game/native_ready` 未达，native semantic/mailbox 未安装，seed/paused/live
  仍未取得；cleanup/driver/tree GREEN。
- gate 暴露的 detached watchdog 字节码污染以最小修复收口（`eaf47de` / `24f75e4`）：
  子进程 `-B` 与入口禁写 bytecode；crash probe `48/48`、watchdog environment
  `16 passed/1 skipped`、phase2 runner contract GREEN。
- G2 readiness audit 已合入（`c4192f5`,`b18f704`），明确唯一下一依赖为 fresh
  MCP-first paused truce-shape artifact；`GEN-034`、action/postcondition 不升级。
- `open_kaishek` 固定路径已同步到 `main/aecb14f`，Maven 全量测试、CLI smoke、synthetic
  preflight GREEN，jar SHA `47E20B1B3814A5CFBC4CCF7347C0C5CF0128961991FD8436BE2C00701AA969E6`；
  临时 `integration/zg361-preflight-20260902` 已合入并删除，原 feature 分支保留。
  父仓 adapter 已 pin `aecb14f`、默认 180 秒，七个 CK3/desktop 入口覆盖 `7/7`。

### 新 RED / 边界

- 用最新 jar 对完整二期语料的离线预验在有效 180 秒内超时：artifact
  `postfix-preflight-aecb14f-parent.json`，SHA
  `F7169F7ADDE435A6EF805A768CD3CFD0E1FBD2EA9B36B1AD35BE573FCFADEC59`，结果
  `FAILED/cli-timeout`。这是 adapter 性能边界，不是 CK3 或脚本语法结论；正在做阶段/分片
  诊断，不盲目继续加 timeout。
- bridge fresh MSVC 候选的 suspended-injection CTest 通过，DLL/Injector SHA 为
  `63EEE32FB2217A75380152C99919152C95476BBFE70FC18DFCD47D3F11A21104` /
  `65BC8FA13C0E5CA3E084D3266A8C4334E89AB3851C76B60A69C96DA8C50A0E4F`；没有证据支持
  翻开 containment 开关。database_init 未到 map-ready 是当前二期主阻点。
- 预验性能已分层：同一最新 jar 的 parser-only 全语料（75 files / 23,831,185 bytes）
  为 `2.556s GREEN/0 errors`；最大生成 scoreboard 文件的 validator 单文件探针在
  `90.267s` 仍无输出。瓶颈是同步 Validator，而非 parser/IO/hash；后续优先采用
  parser-only/按目录 bounded slice 或 validator profiling/cache，不继续盲目加 timeout。
- 最终 Python 3.13/项目 venv 静态回归为 GREEN；一次并行素材调试窗口中的 stale-DDS 瞬时
  观察已复核为无字节差异，不构成产品 RED。
- open_kaishek 性能修复已合入主线 `5672c98` 并 push；固定 jar SHA
  `A14644331FBBA16E1DDA0B84DCB81F24D39F98CE25702E42ADED93D63FFA9398`。完整语料预验由
  约 316 秒降至约 3.37 秒，JSON/根 hash 与基线一致；当前二期最新预验 artifact
  `postfix-preflight-5672c98-parent.json`（SHA
  `A812B36995C6A9B8BA442ADDCA5CF48D0B8EDB10E8D3B88DC01EB6F89EED56D3`）为
  `parser GREEN/0`、fixture IR/runtime GREEN、root validator RED（306,655 条
  `UNKNOWN_OPCODE` profile 覆盖边界），不是 parser/CK3 failure。

### Readiness 汇总

| 线 | 当前状态 | 下一闭环条件 |
|---|---|---|
| 天朝二期 | `static-ready + loader-passed/no-fatal + native-readiness RED + not-live` | loader 进入 map-ready/player snapshot，取得 seed/paused artifact，再做业务与宣传素材 producer |
| G2 | `static/query-ready + paused/live=false + GEN-034 unresolved` | 一次 exact-build MCP-first paused truce read（两次同帧）后才扩 war-bound/策略/action |
| open_kaishek | `runtime-fixture/static + parent-adapter-ready` | 完整语料可在 bounded slice 内完成、361 exact-build/MCP differential；synthetic 不能替代 live |
| 宣传工具 | `static-ready + public release`；一期 demo `fixture-live` | 二期 ready seed、managed supervisor、clean capture、字幕/TTS、人审与视频 export/publish |

### 计划与边界调整

1. 继续保持天朝二期 P0；先处理 loader/readiness 与 bytecode immutability，不重复旧 crash。
2. G2 只推进 readiness 依赖和离线契约，未取得 paused artifact 前不执行 surrender/white-peace 写操作。
3. 每个 CK3 入口先跑可覆盖的 open_kaishek 预验；无覆盖时记录 `NOT_APPLICABLE`，
   `UNSUPPORTED/FAILED` 与 CK3 结果分开。固定路径临时 integration 分支合入 main 后删除，
   遵守少分支原则。
4. 不清理垃圾、不删除失败 artifact；下一次实机 gate 必须由新离线证据触发。

## 2026-09-02 03:15 增量

- G2 的下一步只读交付已落地主线 `d5b15fd`（源 `c9a1283`）：probe 校验同一暂停帧
  两次读取、WarID/角色/provenance、唯一 `CAddTruce` pointer shape、非负且相等
  `evaluated_days`，并拒绝 surrender/white-peace/enforce mutation。3 个新增单测与
  `250/250` native 回归通过；没有 paused CK3 artifact，`GEN-034` 不升级。
- `open_kaishek` `d207707` 已合入 `main`，`2872554` 记录主线优先分支约定；新 JAR
  `CBCD5F868F5C46AA7B5A2C70E11705B978F95DE3437E0809D20A4139F62DD0E4` 对同一二期
  语料给出 parser `GREEN/0`、IR/runtime `GREEN`、validator `RED/233,708`，运行约
  `3.144s`。这是 schema 覆盖边界，不能替代 CK3 live。
- 天朝二期差异 gate 正在执行本轮唯一实机尝试：候选仅移除 d692 role-failure block，
  其余 runtime 文件保持与 `be35868` 完全一致；输入冻结、archive equivalence、
  static preflight、bridge/exe hashes 均 GREEN。结果将决定是否需要最小代码回合；
  无论结果如何不追加同类盲跑。

### 03:22 gate 收口

唯一差异 gate 已结束：`RED/LoaderStageTimeout`，`database_init`、`299.9s`，
`fatal_error_count=0`，没有 Frontend/Load Save/native readiness。报告
`delta-live-pre-d692-20260902/artifacts/runner-report.json` 的 SHA 为
`9EA7AFBEBCD210997B77F794574E7D36FEE3CA1C700C02EE707FE7389F9DF460`；候选 source
tree 为 `1b1db978…`。日志发现删除 d692 block 会留下
`zg361_workforce_exit_fact_verify_role_failure_publish_effect` 的悬空调用
（`events/zg361_workforce_exit_fact_events.txt:64`），因此不合入。后续仅准备兼容
stub/调用点的离线候选，等待新证据后再安排实机。

### 03:24 兼容候选收口

- d692 删除候选的悬空 effect 引用已确认；stub/删调用点均无法通过生成 parity 与 role-failure 契约测试，因此不合入。
- 完整树的 open_kaishek parse 与静态校验仍为 GREEN；`database_init` timeout 仍是独立 live 阻点。保持 mainline-first，不增加临时分支。

### 03:28 open_kaishek 证据切片

- `1c320ad` 在 `main` 合入有 exact-build 证据的 `has_game_rule` 与 `has_character_modifier`；临时分支已立即删除。
- 新 JAR/预验 artifact 已记录，validator 从 `233,708` 降至 `233,115`（UNKNOWN -601；8 条 WRONG_DOMAIN 为既有边界），parser/IR/runtime 与 focused tests GREEN。该离线结果不升级 CK3 live readiness。

## 2026-09-02 04:06 phase2 loader diagnostic closure

- Projection bisect index retained at
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-bounded-gate-20260902\projection-bisect-20260902\index.json`
  (SHA `6582011DE1AB4E6826194036D243A656DF12C8454C582B35538A77F9F4FFE078`).
  Single-cluster removals (`scoreboard_snapshots`, `workforce_endgame_runtime_effects`,
  `scoreboard_slots`) were parser-green diagnostic projections only; they
  removed required semantics and were not merged.
- A complete-tree endgame symbol-shell projection was preflighted with
  open_kaishek `1c320ad` before one exact-build CK3 run. The offline preflight
  was structurally green (`ck3_started=false`) with the declared schema-only
  validator boundary. The live artifact is
  `loader-shell-gate-20260902/artifacts4-live/runner-report.json`, SHA
  `3549502304DD58AB39AE553B546F6A5331CD1778F03027C71B03BA04C7D3A0C8`.
  It remained `RED/LoaderStageTimeout` at `database_init` (`299.962s`, quiet
  `195.089s`, zero fatal errors), and never reached Frontend, Load Save, In
  Game, or native readiness. The shell is explicitly non-shippable and was
  not merged.
- The live report retains `clean_source_unchanged=false` because six Python
  `pyc` files were emitted during the run; source content was not modified.
  This environmental boundary is recorded rather than rewritten.
- This closes the current loader diagnostic gate. The next phase2 item is a
  real generated-source decomposition/readiness repair preserving symbol and
  contract parity; no timeout extension, deletion, or no-op compatibility
  shell is planned. Failed artifacts remain retained and no garbage cleanup
  was performed.

## 2026-09-02 04:18 G2 artifact inventory

- No new paused truce artifact was found. The latest three
  `xar-g2-truce-paused-live-*` attempts remain RED without an MCP terms
  sequence. The reusable frozen checkpoint/driver pair is pinned by SHA
  `60108A5DA03DC3A8315A3E79897D9CF2F49763910A8AA15A462E7DD0B6AAF164` /
  `4FB901C77AF6D95A05EAB2B0E900AE2E07A652B4C14729835DECB69FC8CFF57E`.
- The older paused/map-ready launch lacks
  `query-war-termination-terms-v1-50331699`, so it cannot stand in for truce
  evidence. G2 remains `static/query-ready + paused/live=false +
  GEN-034 unresolved`; the next live entry must run open_kaishek preflight
  first and stop at the current capability/readiness boundary if native
  semantic readiness is absent.

## 2026-09-02 04:31 open_kaishek performance and preflight

- Mainline is `19ff306cd902978dac43a56377720be874c51cb8`; the temporary
  performance branch was merged and deleted immediately. The six static
  `Pattern` changes preserve parser semantics and reduce the bounded corpus
  warm parser from about `1066ms` to `421ms`, parse+validate from
  `1240--1300ms` to `574--610ms`, and fresh-process preflight from
  `2.65--3.10s` to `1.43--1.49s`. Maven, parser self-tests, phase-one syntax,
  and 760-case fuzz are GREEN; JAR SHA-256 is
  `3DF2B4463EDC1D732DE1FAA85CE2803F8995DEAE1640D5F2D93001FEE53174C2`.
- A fresh-copy parent preflight for `a89282d` is retained at
  `postschema-preflight-19ff306-clean/preflight.json` (SHA-256
  `004AD71FD182590FAB7A8EE932BFE58536E42B98A32D476734293E6B614F248F`):
  `GREEN/preflight-ready`, parser/IR/runtime/static checks GREEN,
  `ck3_launch_attempted=false`; validator remains the explicit schema-only
  `RED/233,115` and seed generation is blocked. The stale-`pyc` environment
  RED attempt remains preserved and is not conflated with CK3 capability.
- This improves feedback speed only; phase2's loader `database_init` RED and
  G2's missing paused truce artifact are unchanged. No garbage cleanup was
  performed, and no extra long-lived branch was created.

## 2026-09-02 04:36 G2 bridge capability gap

- Read-only review confirms the bridge has termination-options and pointer-only
  `query-war-termination-terms-v1-N` shapes, while exit-terms reading remains
  explicitly disabled after the recorded exact-build crash RVA. Surrender and
  white-peace writes stay frozen pending claim-exit-terms and campaign proof.
- The reusable paused/map-ready report's concrete gap is
  `action_step_family=false`: the literal WarID-specific query action was not
  advertised, so no MCP terms sequence occurred. Existing static contract
  coverage is `17 passed / 51 subtests`; no code or branch changed.
- Next G2 work is one fresh semantic-ready exact-build entry, preceded by the
  `open_kaishek` offline preflight; no ABI widening or inferred expiry is
  planned.
- The current Python action-step builder emits the exact WarID-specific query
  when the capability and active-war input are present; the older
  `action_step_family=false` is runner/DLL provenance, not a current generator
  defect, so no speculative patch was added.
- The bounded G2 diagnostic slice is on parent mainline as `de4c7be` (source
  `d1850b3`): early exact-build proof now retains terms-query action-step
  literals, with template-versus-WarID tests. The related suites are
  `18 passed / 51 subtests`; no CK3 launch or action authorization changed,
  and the temporary branch/worktree was removed promptly.

## 2026-09-02 04:52 phase2 effect-cluster gate

- Candidate `041fd68` (`a1177f1`) split the generated workforce/endgame effects
  into 14 ordinary files with byte-identical offline aggregation. Generator,
  local validator, release check, and 165 related tests were GREEN.
- The required `open_kaishek` preflight was GREEN at the structural level
  (parser `89/0`, IR/runtime/static checks, no CK3 launch; validator retained
  schema-only `RED/233,115`). The single exact-build gate is retained at
  `cluster-split-041fd68/artifacts3/runner-report.json`, SHA-256
  `FDF3A172C6BED99221B48446577F47535F7B036111826F9B2A8D79D7C7D7D115`:
  `database_init` timeout `299.915s`, quiet `207.595s`, zero fatal errors,
  no native-ready frame.
- Because the layout change did not alter the transition, it was reverted by
  `e4a964a` immediately. The failed candidate is diagnostic evidence only;
  no same-shape retry or timeout workaround is planned.

## 2026-09-02 04:58 loader dependency observability

- Parent commit `7413346` adds read-only `database_node_count` and
  `last_database_node` to loader progress; stage/authorization/timeout/bridge
  semantics are unchanged and the offline fixture is GREEN.
- Retained-log extraction shows the old successful run reached `303`
  `database_dependencies.cpp:433` nodes and `CJominiInGameMusicDatabase/in_game`,
  whereas current, shell, and reverted-cluster runs reached only `2` and
  `CJominiLoadScreenDatabase/database_init`. Inventory SHA-256 is
  `808328DCFA319ED3CC327F8AE201A5D6F0C269343768EA342688CF34EBE6D185`.
- This is an observation boundary, not a root-cause claim; no second CK3 gate
  was run.

## 2026-09-02 05:01 open_kaishek schema slice and phase2 inventory

- `open_kaishek` `feat/government-flag-schema-20260902` was fast-forwarded to
  `main` and deleted locally/remotely. Mainline is
  `450b559c892228b6ab650c6fa68bece6defdfec7`; only pre-existing user branches
  remain. The exact-build-backed `government_has_flag` scalar trigger schema
  and regressions are GREEN under Maven. CLI JAR SHA-256 is
  `06E71C403924412A32EE307149AF6DB5E4A40886064263CF1FFDDE5615538C4D`.
- Direct preflight of the frozen phase2 tree is retained as
  `kaishek-government-flag-preflight-20260902.json`, SHA-256
  `6C4BEAC7FF87CA29B9481FB247CC4AD1E02350D279D9AA644C9A5D6661E28FDA`:
  parser `76/23,831,410/0` GREEN, bounded validator/root scan
  `RED/233,014`, no CK3 launch. This is an offline accelerator, not live
  readiness.
- The 24-file dependency inventory reports `1,656` definitions, `2,764`
  edges, zero duplicate business definitions, and no actionable newly
  unresolved call-like symbol. Report SHA-256 is
  `8E2C811BC95DE60ED37700DF6692979B677E909064B36C73688A6655E6CF697A`.
  Phase2 stays frozen until a concrete dependency node/file/symbol change is
  observed; no same-shape CK3 gate is added.

## 2026-09-02 05:14 G2 fresh semantic-ready entry sheet

- The independent next-entry sheet `docs/ck3-native-ai/g2-next-run-preflight-2026-09-02.md`
  was merged to parent mainline as `a1f424e`; its temporary branch/worktree was
  removed immediately. It binds the frozen CK3/WarID/checkpoint/driver identity
  and the current `open_kaishek` `a670589`/JAR provenance.
- The focused offline G2 contract suites passed `6` tests. No CK3 launch,
  timeout extension, ABI widening, or surrender/white-peace authorization was
  made. A future native entry is permitted only after exact-build proof exposes
  the concrete WarID action literal and the required semantic-ready snapshot;
  otherwise the attempt remains `semantic-ready RED`.

## 2026-09-02 05:20 open_kaishek `has_perk` schema slice

- `open_kaishek` advanced to `a6705894bb41d87aa5e53530d77910369c6eb209`;
  the exact-build-backed scalar character `has_perk` descriptor, profile and
  validator regressions, and schema evidence are on `main`. The temporary
  branch was merged and deleted immediately. Focused profile/validator tests
  (`10`/`20`) and offline packaging are GREEN.
- The JAR SHA-256 is
  `392B130B7F6DCB516627EAE284CF673C7F109D6857A5C6388AE56F02EC0BF1AD`.
  Preflight artifact SHA-256 is
  `F37560B6C0B3113C3B6F0EDE04368E9C5B57BE55F473938DCE7A2D355787E8E9`:
  parser `76/23,831,410/0` GREEN, bounded validator `RED/233,014`, no CK3
  launch. This improves offline schema coverage only; phase2 and G2 live
  readiness are unchanged.

## 2026-09-02 05:24 phase2 node-to-source localization closure

- A read-only localization pass confirms the current loader parser exposes only
  `database_node_count` and `last_database_node`; CK3 debug node lines carry no
  production filename or callback, so no changed symbol can be mapped offline.
  Current runner report SHA-256 is
  `B204740B6EE8BEB8CA86A84E100575050985F15631023424953050B0D540FDAC`;
  its debug log SHA-256 is
  `7757FEAE6A93230F04B23533D4E6A93040947C7A1EE2AFE329874B7F8F94CAB0` and
  `error.log` is empty.
- Existing inventories remain the evidence boundary (`2` versus `303`
  database nodes; `1,656/2,764` focused graph; no actionable unresolved
  delta). No duplicate gate or speculative generator change was made. The next
  phase2 entry requires a loader-side callback/node observation tied to a file
  or symbol; until then `a89282d` stays frozen.

## 2026-09-02 05:35 branch consolidation

- After parent master `87d557c` reached official CI GREEN, merged or
  patch-equivalent temporary agent/wip refs were retired. Their clean
  worktrees were detached and retained; no source, logs, artifacts, or
  process-assets directories were removed. User-owned release/evidence refs
  remain.
- `open_kaishek` remains on `main` at
  `a6705894bb41d87aa5e53530d77910369c6eb209`; its two temporary schema refs
  are gone, with only the pre-existing `feat/cli-batch-replay` and
  `feat/zg361-appeal-replay` branches retained. The canceled garbage cleanup
  was not performed.

## 2026-09-02 09:40--09:46 parallel phase2/G2/accelerator increment

- Phase2 offline screening used current `open_kaishek` once against frozen
  `a89282d`. Artifact
  `static-candidate-mod-preflight-20260902.json` has SHA-256
  `1EC2D07D2ADD278C61BDE98F3536364C77282DA4D6BD7CE6D005EF2A98EB21A1`:
  parser/IR/runtime GREEN, bounded validator `RED/233,014`, no CK3 launch.
  The dependency graph remained `1,656/2,764` with zero duplicate business
  definitions and the generator/wiring suites were `577 + 43` GREEN, so no
  speculative source/effect repair or duplicate live gate was added.
- Parent `a5c079e` adds typed loader-node context while preserving the existing
  count/last scalar contract. The focused fixture is GREEN; retained logs still
  parse as current `2` versus old-success `303` nodes. Stage, timeout, event
  authorization, and readiness are unchanged.
- Parent `a84c53d` locks the G2 template-to-concrete termination-query action
  boundary at `NativeProtocolState.capabilities()`; `204` focused tests are
  GREEN. It adds no ABI or write authorization and does not produce a paused
  semantic-ready artifact, so `GEN-034` remains unresolved.
- `open_kaishek` is clean at
  `main == origin/main == 757fb1b0d0b92fd234961f32e853f9cdef7069d1`.
  Its exact-build `has_dynasty_perk` schema slice passed `11 + 21` focused
  tests and packaging; JAR SHA-256 is
  `D4BA0FF5E6A9C85ED0853FD78D44940E98445F2867E9D6CA5902AF0E19B29476`.
  Parent adapter artifact SHA-256 is
  `37A1B6788306FD7FF8C1DFBAEDE1BE44B5B26602CE7E85334FF6A8B042A14426`;
  parser/root scan and fixture IR/runtime are GREEN, bounded validator stays
  `RED/233,014`, and `ck3_started=false`. The temporary schema branch was
  merged and removed immediately; only the two pre-existing user branches
  remain.
- A read-only promo release audit confirmed independent repo main
  `262af995382949a4f631735c5b5d4ae4f444902f`, formal GitHub release `v0.1.0`,
  matching wheel/sdist hashes, iteration-one demo, and current parent links.
  No republish or cleanup action was performed.

## 2026-09-02 09:55 accelerator schema follow-up

- `open_kaishek` advanced cleanly to
  `7da444d6afbeb98ec6c9d91da49535a43d55d0ce` with the exact-build-backed
  scalar `has_dlc_feature` trigger (`certified=false`). Focused profile and
  validator tests (`12` and `22`) and offline packaging passed; JAR SHA-256 is
  `78AFD52B147874070813B5E77FA710B082065C6BB14EB3C9071A833BB0FEF2A9`.
  GitHub CI `33581626962` succeeded. Full Tianchao-root preflight was
  parser/IR/runtime GREEN with bounded validator `RED/39,360`, no CK3 launch;
  stdout was not persisted as an artifact.

## 2026-09-02 10:07 loader callback static closure

- Exact-build binary review located the database-node initialization loop at
  RVA `0x3B9AB00–0x3B9ACED`; `0x3B9AB90` calls the per-node init virtual
  callback through `[rax+0x10]`, then aggregates node/dependency timing.
  This is a stable native observation anchor, but not yet production
  script-file attribution. The repeated `onaction.cpp:429 Total of : 881`
  remains non-discriminating, so no speculative `onaction_total` field or
  bridge/ABI change was made. Phase2 baseline `a89282d` remains frozen.

## 2026-09-02 10:08 G2 fresh semantic-ready terms probe

- After the mandated offline `open_kaishek` preflight, one exact-build,
  read-only CK3 entry reached a paused semantic-ready frame. Retained report:
  `Z:\ck3_mod_rewrite_process_assets\zg361\g2-fresh-semantic-ready-20260902T100431236\report.json`,
  SHA-256
  `4A7C66D34C851698572F4BBA2970ECAA00C005C2DC338ABCD75A7FD304F0B991`;
  matching preflight SHA-256
  `5F7F67C439D8721E56C735E2DFA49E07A32630DD7019EB221C1C1215DD68AC90`.
- Concrete WarID action step and paused identity passed; two same-frame terms
  reads were normalized-equal. Gold, prestige, prisoner-release, and favor
  hook are production-live read-only primitives. Truce evaluated duration,
  expiry, and war-bound loss remain unobserved, so `GEN-034`, decision
  readiness, and all termination writes remain closed. No mutation was sent;
  cleanup and source invariants were GREEN.

## 2026-09-02 10:19 loader callback contract

- Parent `56e786c` now carries the exact-build machine-readable loader callback
  ABI/source contracts and synthetic join fixture (source `7758b9b`; temporary
  branch merged and retired). The focused contract test is `4/4` GREEN. The
  contract records the loop/vcall/node-offset/timing RVAs and keeps callback
  return semantics, vtable slot meaning, source-file attribution, and
  production readiness explicitly unknown. No CK3 launch or production ABI
  change occurred; phase2 remains blocked at the existing loader/native gate.

## 2026-09-02 10:24 open_kaishek schema follow-up

- `open_kaishek` advanced cleanly to
  `bd980e787e8e64b64104b43542ae2afefe3e8a06` with the exact-build-backed
  `has_court_position=<court-position-key>` scalar (`certified=false`). The
  synthetic preflight artifact is
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-bounded-gate-20260902\kaishek-court-position-synthetic-preflight-20260902.json`
  (SHA-256
  `B2183C32C18D0F7A6F00A8C96C2D0DEEAD7EAF8BFC358606F92415368E6674B7`),
  with `13/13` profile and `23/23` validator tests GREEN. JAR SHA-256:
  `DCCCF51A8B68EEC6CCF8D391D7B0E839361DECB9E37098C31BBB78EB296247A1`;
  CI `33582994573` succeeded. No parent full preflight or CK3 launch was
  repeated, and no live readiness claim changed.

## 2026-09-02 10:30 phase-two error-cluster closure

- The frozen `a89282d` artifact has `error.log=0` and no debug `[E]`/`[W]`
  entries; the loader reaches two nodes and then stops at the bounded
  timeout. Historical large unset/unknown/`TICKET_STATE` clusters belong to
  stale corpora and are covered by existing fixes (`641e862`, `c79057c`,
  `e1d8ebf`, `bd5c795`, `88095ba`, `32848a5`, `dab001c`). No new
  evidence-backed script edit was identified. The G2 fresh debug comparison
  supports a loader/native callback-context gap, so phase2 remains frozen at
  the callback contract rather than repeating the timeout.

## 2026-09-02 10:36 report synchronization

- The error-cluster closure is on parent `master` as
  `7eb1455a23e757cfc82644b034db21e2540aa699`; [official CI run
  33583459472](https://github.com/XenoAmess/ck3_eternal_recurrence/actions/runs/33583459472)
  is `SUCCESS`. This is provenance only and does not promote any readiness
  gate.
- The retained evidence hashes were rechecked: loader-node inventory
  `808328DCFA319ED3CC327F8AE201A5D6F0C269343768EA342688CF34EBE6D185`,
  dependency inventory
  `8E2C811BC95DE60ED37700DF6692979B677E909064B36C73688A6655E6CF697A`,
  fresh G2 report
  `4A7C66D34C851698572F4BBA2970ECAA00C005C2DC338ABCD75A7FD304F0B991`,
  and its offline preflight
  `5F7F67C439D8721E56C735E2DFA49E07A32630DD7019EB221C1C1215DD68AC90`
  all exist and match the recorded values.
- The truthful labels remain: phase2 is
  `static-ready + loader-passed/no-fatal + native-readiness RED + not-live`;
  G2 has a four-domain `production-live read-only primitive`, but
  `truce_ready=false`, `decision_ready=false`, and `GEN-034` unresolved;
  the `open_kaishek` `bd980e7` slice is offline syntax/profile evidence
  (`certified=false`) only. No CK3 write was issued.
- Next work is limited to phase2 loader callback/source-symbol observation and
  the G2 truce-duration reader, with current `open_kaishek` preflight bound
  before each new CK3 entry. The identical timeout shape will not be repeated.

## 2026-09-02 10:44--10:45 loader telemetry and accelerator increment

- Parent `f20a88d` adds passive database-callback/PostInit telemetry and typed
  loader RED reasons without changing stage, timeout, event authorization, or
  CK3 behavior. Focused loader, optimized loader, seed-capture, callback
  contract, and static validation are GREEN.
- The retained-log replay report SHA-256 is
  `56233A3CAF9062B347C1661132E3D16A419CF0698C69CB4C775214B47311E56D`;
  it classifies the frozen two-node boundary as
  `RED/database_callback_stall`, with zero fatal errors and no CK3 launch.
- The preceding offline next-entry preflight is retained at
  `next-entry-open-kaishek-preflight-20260902.json` with SHA-256
  `4C7370C9E0F9E83FB2A6080C8A4AEB467F5F56A4AAD9AFEB46F7009C82FB8F9A`:
  parser `55/2,223,707/0` and fixture IR/runtime GREEN, bounded validator
  RED/`39,360`, `ck3_started=false`. It was used to bind the replay, not to
  authorize another CK3 gate.
- `open_kaishek` is clean on `main == origin/main ==
  36b4743d5da013ba1f85790ebcffad2629442ed1`. The exact-build `war_days`
  syntax slice passed `14/24/12` focused tests, CLI smoke, and CI
  `33584241395`; JAR SHA-256 is
  `DFEA464B657D627BBB1AEF34C12CD91419830644D47F891782A3C9D718C44D61`.
  Parent preflight SHA-256 is
  `CDC7DCD22888208C7585A2932F266912CAE4C08A9D0836D7EF6580796FC8571F`:
  product parser and dedicated fixture validator GREEN, bounded root
  validator RED, no CK3/save/network activity.
- The proposed G2 public failure-reason field was discarded because it would
  break exact-key v1 consumers and frozen component hashes. No public ABI,
  wire, or readiness changed; the next G2 entry remains exact-build internal
  truce-shape evidence, not a termination write.

## 2026-09-02 10:50 G2 evaluator call-site closure

- Static exact-build review confirms evaluator RVA `0x3373000` has the
  three-argument `(script_value, effect_context, evaluation_context) -> int32`
  contract. Both CAddTruce call sites use `truce_effect+0x108` and
  `context+0x28`; other duration offsets are different effect types. No
  ABI-preserving alternate offset or code fix was supported, so no public
  schema/ABI or CK3 run changed. The missing truce duration remains the next
  internal-trace entry and termination writes stay disabled.

## 2026-09-02 10:52 loader detour feasibility closure

- The existing bridge has no proven safe pre-loader callback detour lifecycle:
  exact prologue bytes, callback ABI/thread quiescence, trampoline rollback,
  and a private sink are not closed. Existing journals use preallocated
  atomics/rings, so no callback-side file I/O or production hook was added.
  Phase2 remains at the typed loader telemetry/static callback boundary; the
  next work package is offline native evidence, not another identical CK3
  timeout.

## 2026-09-02 10:57 open_kaishek provenance floor

- Parent `b0bbcbb` changes the adapter's metadata floor from the stale moving
  marker to stable contract commit `b306a95`. Each invocation now records the
  actual local checkout HEAD and JAR SHA, while explicit root/JAR/commit
  overrides remain unchanged. Adapter and coverage focused tests are GREEN;
  no CK3, ABI, or readiness behavior changed. The parent docs line was
  subsequently rebased with the loader-feasibility note for direct push.

## 2026-09-02 11:00 open_kaishek `has_innovation` slice

- `open_kaishek` mainline is clean at `dad0ea2a864cbb4b9ea4a4e9dd388f606485830a`.
  The exact-build-backed `has_innovation` descriptor is
  `TRIGGER/CULTURE/STRING`, deterministic/read-only, and `certified=false`;
  profile/runtime type bridging, fixture/validator coverage, CLI smoke, and
  CI `33585124901` are GREEN. The rebuilt JAR SHA-256 is
  `478D2F4040316C5223470BB350AF286256D78ECA3EB7B413B0383EEF88B86911`.
- The parent-adapter artifact
  `Z:\ck3_mod_rewrite_process_assets\zg361\g2-fresh-semantic-ready-20260902T1058-has-innovation\open-kaishek-preflight-has-innovation.json`
  (SHA-256 `7D447CF0F286F253A62EC42EA5AA57B6251777A70111F44A460CCD74984C011A`)
  binds the current checkout/JAR and frozen corpus. Parser is GREEN; the
  bounded root validator is the expected schema-only RED/232,973, fixture
  IR/runtime are SKIPPED, and no CK3 process or mutation occurred. This
  improves offline schema acceleration only; phase-two loader and G2
  `GEN-034` readiness remain unchanged.

## 2026-09-02 11:04 current-adapter fixture preflight

- A single offline fixture-only invocation of the parent adapter bound
  open_kaishek `dad0ea2a864cbb4b9ea4a4e9dd388f606485830a` and JAR SHA-256
  `478D2F4040316C5223470BB350AF286256D78ECA3EB7B413B0383EEF88B86911`.
  Artifact
  `Z:\ck3_mod_rewrite_process_assets\zg361\kaishek-has-innovation-adapter-fixture-only-20260902.json`
  (SHA-256 `A8F852B8586F0AEC3975B75E7A118105286C79E9E64D2C7E8D26EFAADAEA317B`)
  is adapter/parser/validator GREEN with IR/runtime SKIPPED and stable
  contract `b306a95`; no CK3 process or mutation occurred. The full-root
  validator RED remains the known schema-only corpus boundary, and no
  phase-two or G2 readiness label changes.

## 2026-09-02 11:13 G2 truce internal-diagnostic boundary

- A bounded source/fixture audit found that the truce observer's typed failure
  is local-only and is erased by `ReadRaiktorSurrenderTruceDuration` before the
  caller returns; the bridge therefore has only the existing
  `evaluated_days_observable=false/null` public row. The available thread-local
  unavailable reason is scoped to the disabled exit-terms path and is not a
  valid truce channel. Frozen logs and the paused artifact contain no hidden
  diagnostic. No public wire/ABI, offset, or CK3 run changed; any future
  capture must be a separately scoped private/test seam preceded by the
  current open_kaishek preflight.

## 2026-09-02 11:17 phase-two telemetry live recheck

- After one current `open_kaishek` offline preflight, a single exact-build,
  MCP-only read-only entry used frozen source `a89282d` and loader telemetry
  `f20a88d`. The report
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-bounded-gate-20260902\phase2-live-recheck-20260902\artifacts-a89282d-f20-loader\runner-report.json`
  has SHA-256
  `187E1A438F0DCDE838BAC1AF02AAB878393BBAF999D704E3C9F2D843627FBBA9`;
  its paired preflight has SHA-256
  `E18ECD37F2EC8AAA56BCE5CE271F968CB58CEF8ACACEB710E14D5001A272B8DC`.
- The live boundary is `RED/loader_stage_timeout` with
  `reason_code=database_callback_stall` at 299.748 s: two typed callbacks
  (`CGameConceptTypeDatabase` 441 ms and `CJominiLoadScreenDatabase` 9 ms),
  no `PostInit` rows, no fatal script errors, and callback quiet 286.430 s.
  Event/native readiness was not entered; cleanup/tree removal/driver close
  are GREEN and no action/save write occurred. `clean_source_unchanged=false`
  is isolated to six runner-created `__pycache__` files; four path-length
  warnings are non-causal. Phase2 remains loader-stall RED, with bytecode
  hygiene and native callback evidence as the next work packages.

## 2026-09-02 11:20 open_kaishek `has_cultural_tradition` slice

- `open_kaishek` fast-forwarded cleanly through `f12a0a9` and `fd85dd6` to
  `main == origin/main == fd85dd6`. The exact-build-backed
  `has_cultural_tradition` descriptor is `TRIGGER/CULTURE/STRING`,
  deterministic/read-only, and `certified=false`; focused/full Maven,
  validator, fixture, CLI, and package tests plus CI `33586554036` are GREEN.
  The JAR SHA-256 is
  `D94929A6ADF8140483962137E01B6B1319E88216EDD2B6E68612DF286A1CFEF8`.
- Parent-adapter fixture-only preflight
  `Z:\ck3_mod_rewrite_process_assets\zg361\kaishek-has-cultural-tradition-20260902.json`
  (SHA-256 `AB96FBF00CFC4D491FADEABE8F82CB485A8A48B1CDCF68315C87B0DEB80474FF`)
  is parser/validator GREEN with IR/runtime SKIPPED and no CK3 process or
  mutation. This is offline schema acceleration only; phase-two and G2
  readiness labels remain unchanged.

## 2026-09-02 11:28 phase-two clean-export bytecode hygiene

- Parent `e1f9faa6b974a39711bcf458d87aa3426e16247c` is on `master`; static CI
  run `33587172920` is `SUCCESS`. The runner sets the process-wide
  `PYTHONDONTWRITEBYTECODE=1`/`sys.dont_write_bytecode` guard before optional
  imports and passes `-B` to normal and optimized child commands. The added
  import-guard regression confirms that a clean export gains no
  `__pycache__`, `.pyc`, or `.pyo` files.
- The retained preflight is
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-bounded-gate-20260902\pycache-repro-current\artifacts-38cf20e-valid\preflight.json`
  (SHA-256
  `83FE0797D93468086FBC4C5BAB31F2728741922A72FEC1B018CE4A6CA2D0E62F`).
  Its `frozen_git_commit` is `38cf20e63cdd4b41fe772453d1429de3bc06e1ed`,
  which is patch-equivalent to `e1f9faa` with identical tree
  `60100213ff6eb3111e4366b6b6f66f770c02587e`; the artifact is explicitly not
  presented as an `e1f9faa`-bound run. All checks, including
  `clean_source_unchanged`, archive equivalence, and runtime projection
  invariance, are GREEN; `seed_ready=false` and `ck3_launch_attempted=false`.
- The focused seed-capture suite has `14` test functions in normal mode and
  the same `14` under `-O`; both are GREEN. This closes only the runner
  bytecode-hygiene gate. No CK3 rerun occurred and the loader callback-stall
  RED, seed/native readiness, and phase-two capability labels are unchanged.

## 2026-09-02 11:36 phase-two loader callback static slice

- Parent `62011b6` adds the reproducible exact-build extractor,
  machine-readable contract, and regressions; `7ff85d0` records the bounded
  evidence note. CI run `33587751304` is `SUCCESS`. The retained artifact is
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-bounded-gate-20260902\loader-callback-native-slice-20260902.json`
  (SHA-256
  `DB578C67A98FC131B829B718C356E11E31DE84CCC649E81D79EB9EFE5C25452A`).
- The slice pins the CK3 `1.19.0.6` loader loop/prologue/PDATA/unwind,
  `RCX -> node+0x88 -> vptr -> slot 2` call shape, eight direct callers, and
  same-function continuation. Runtime node-vptr identity, callback
  return/lifetime, OS thread, source-file attribution, and readiness remain
  explicitly unknown. Normal and optimized focused contract tests are `8/8`
  GREEN; extractor and compilation checks are offline only.
- No CK3 process, hook/detour, bridge/public ABI field, or readiness promotion
  was introduced. Phase2 remains loader callback-stall RED with seed/native
  readiness unchanged; the next legitimate entry is a private paused
  exact-build callback identity/completion observation, not another identical
  timeout.

## 2026-09-02 11:40 `open_kaishek` culture pillar and parameter slices

- The accelerator mainline now contains two minimal exact-build culture-schema
  increments: `89141b344f92afd41d4798fc4ee0056a2ebc8a43` adds
  `has_cultural_pillar`, and `e92b94f8b4cc03f1a91afafaf2a23f5e4560a1e4` adds
  `has_cultural_parameter`. Both are deterministic/read-only
  `TRIGGER/CULTURE/STRING` descriptors with `certified=false`; no native
  evaluator, runtime bridge, or CK3 behavior was added. Focused profile,
  validator, and fixture suites are GREEN (pillar `17/27/18`, parameter
  `18/28/20`).
- The pillar rebuild is pinned to CLI JAR SHA-256
  `DBE1ECA09FDE50DB43E0537BCFDBB7554E11F3D5229AD1319BBB6B71F286BDF6` and
  CI run
  [`33587855616`](https://github.com/XenoAmess/open_kaishek/actions/runs/33587855616)
  completed `SUCCESS`. Its retained
  parent-adapter artifact is
  `Z:\ck3_mod_rewrite_process_assets\zg361\g2-fresh-semantic-ready-20260902T1140-cultural-pillar\open-kaishek-preflight-cultural-pillar.json`
  (SHA-256
  `33A62106B3F129A480129308359981F72A13DE32F4253A58062F91CEDE6E018E`).
  The dedicated fixture parser/validator are GREEN and IR/runtime are
  SKIPPED; the full-root adapter envelope remains the expected
  `failed/cli-red` (root parser GREEN, bounded schema-only validator RED with
  `232,973` diagnostics), not a CK3 failure.
- The parameter rebuild is pinned to CLI JAR SHA-256
  `98029BA80F688A49BF18116EF9A3DDCF61AB9C61E51F9371CBF7736E3DB6C316`.
  Dedicated fixture artifact
  `Z:\ck3_mod_rewrite_process_assets\zg361\kaishek-has-cultural-parameter-20260902.json`
  (SHA-256
  `D834CE5998C5DB697894A608F2A971051D5F45E8FEDFCD8C9F67682DB8B2FA78`)
  reports parser/validator GREEN and IR/runtime SKIPPED. CI runs
  [`33588081778`](https://github.com/XenoAmess/open_kaishek/actions/runs/33588081778)
  and
  [`33588065770`](https://github.com/XenoAmess/open_kaishek/actions/runs/33588065770)
  were initially `in_progress` and were subsequently verified as `SUCCESS`.
- Both preflights are offline (`ck3_started=false`, `save_mutated=false`,
  `network_used=false`) and `certified=false`; no CK3 launch or runtime claim
  is implied. The slices improve offline schema acceleration only. Phase2
  remains loader callback-stall RED with native/seed readiness unchanged, and
  G2 `GEN-034`/truce decision readiness remains unresolved.

## 2026-09-02 12:15 G2 private truce failure telemetry seam

- Parent mainline `8b6d1d6d6512f36c8309c5d549d3119e9619d398` contains the
  cherry-picked source commit `607338161761ddf69989a26b5c468c17e523a10e`.
  Under `XAR_CK3_WAR_EXIT_TERMS_OFFLINE_RE_TEST`, the offline C++ fixture now
  retains the existing `RaiktorSurrenderTruceFailureV1` in thread-local state
  for a test accessor. The seam is diagnostic-only: it adds no public v1
  wire/schema key, `WarRaiktorSurrenderTermsSnapshot` field, offset, or
  production ABI symbol, and it does not alter readiness.
- The freshly built MSVC `xar_ck3_game_access_test` fixture passed. The same
  CTest run surfaced 15 pre-existing repository-wide source-contract/runtime
  test failures on the older checkout; those failures are retained as a
  baseline RED and are outside the seam. The unsupported-build fixture
  expectation remains explicit.
- No CK3/live run, desktop capture, save mutation, network access, or runtime
  certification was performed. This closes only a private diagnostic seam for
  future offline fixtures; G2 `GEN-034`, truce-duration observation, and
  termination decision/action readiness remain unresolved.

## 2026-09-02 12:37–12:39 delivery continuation

- Phase2 advanced from the existing callback static slice to `7ff9ca1`, adding
  machine-checked CFG/null edges and preserving the callback-probe NO-GO. The
  retained CFG artifact is
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-bounded-gate-20260902\loader-callback-native-slice-cfg-20260902.json`
  (SHA-256
  `0BB03760C6F204F703D2A11AF573CBBE0C1A911D3E95CD0AA9AA7E0161762A5A`).
  No CK3/live/public ABI/readiness change occurred; loader callback/native
  readiness remains RED/not-live.
- G2 added the exact-build evaluator/CAddTruce contract in `580f54f`, with a
  one-pass read-only verifier GREEN and contract SHA-256
  `55E5203647D2D0D0F017E990CBB736F42E306EFEE1269D7918DF39735A812799`.
  `GEN-034` and truce/action readiness remain unresolved; no CK3 launch or
  mutation was performed.
- `open_kaishek` advanced to `638f4c0` with the `is_acclaimed`
  `CHARACTER/TRIGGER/BOOLEAN` schema-only slice (`certified=false`). The
  parent preflight artifact is
  `Z:\ck3_mod_rewrite_process_assets\zg361\kaishek-is-acclaimed-20260902.json`
  (SHA-256
  `D48913E0F4A831B0220EE2A818393CB3AA42B88D8B976097CF9652DEF30FFC73`),
  with parser/validator GREEN and IR/runtime SKIPPED. No native evaluator or
  CK3 readiness claim was made.
- The child-process `429` recovery rule is now documented in `AGENTS.md` and
  `docs/branch-management.md` (`340fd0b`). Multiple interrupted child tasks
  were retried by reusing their original worktrees; after repeated service
  throttling, the same bounded packages were completed locally without
  repeating unchanged evidence.

## 2026-09-02 12:45 `open_kaishek` can-be-acclaimed slice

- The accelerator advanced to `759199b` with a schema-only
  `can_be_acclaimed` `CHARACTER/TRIGGER/BOOLEAN` descriptor (`certified=false`),
  validator fixture, OfflinePreflight, CLI smoke, and documentation. The
  exact native qualification evaluator remains out of scope and no ACCOLADE
  scope or runtime claim was added.
- Parent preflight artifact:
  `Z:\ck3_mod_rewrite_process_assets\zg361\kaishek-can-be-acclaimed-20260902.json`
  (SHA-256
  `503BA9C9F7D49E4154BC6B0FD29E763A59FD1AAC1BA3AED24767C29DC4ABE409`),
  parser/validator GREEN, IR/runtime SKIPPED, and no CK3/save/network use.
  CLI JAR SHA-256 is
  `421F49C93B21DBE5D96BFD81FFBFE422EB098B2170ECC498A415D4125490F2CB`.
  Final open_k CI runs `33592356916` and `33592340523` both completed
  successfully; parent readiness labels remain unchanged.

## 2026-09-02 13:13 phase-two dispatch-window closure and handoff

- Phase2 mainline commit `fefdb6d6039cdcff2f012b5bfe595962a9123e80` records
  the exact-build dispatch window `[0x3B9AB50,0x3B9AB93)` (67 bytes, 14
  instructions), both null CFG edges, and eight direct-caller continuations.
  The window SHA-256 is
  `AF5F3A3C54CC163F415E7A04FC53BC89BD1200B3977E6044AE5D8BFE0E3CEC8C`.
  Retained extractor output
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-bounded-gate-20260902\loader-callback-boundary-next-20260902.json`
  has SHA-256
  `D2E0677813EC641DB77F0F8F81ECB6F57F611254A8A183A77776465B24424295`;
  focused static checks are `6/6 + 4/4` GREEN and Official Runner CI
  [`33593818448`](https://github.com/XenoAmess/ck3_eternal_recurrence/actions/runs/33593818448)
  completed SUCCESS.
- The vacation handoff documentation chain is `0c9c43a` (audit) → `547c7ac`
  (G2/open_kaishek handoff) → `8d79ae2` (project handoff), with `fefdb6d` as
  the latest phase2 static anchor. This remains offline, read-only evidence:
  no CK3/live run, public ABI/detour change, or readiness promotion occurred;
  phase2 remains `static-ready + native-readiness RED + not-live`.

## 2026-09-02 16:48 resumed parallel delivery

- Owner priority is now explicit: phase two plus its final promotional video is P0; G2 remains P1 in parallel; `open_kaishek`
  evolves only from demonstrated preflight needs. The parent integration sequence is `ea8164b` (promo `v0.2.1` pin), `457c8d8`
  (video readiness), `3559033` (phase-two runtime vtable capture), and `45a87f6` (G2 private failure capture).
- The independent promo checkout is clean at remote `main=57c42fca13ea459432c1caf76e069a1fbccf602c`; release `v0.2.1`
  wheel SHA-256 is `F8DE0711415E7FCE2BF07A34D3DB4EDC0593F32BA1CB61034946665E27014621`. Normal/optimized
  tool tests are `263/263`; parent compatibility and branch CI `33609367342` are GREEN. Phase-two validate-only still correctly
  reports all ten chapters planned, and the real producer remains absent. The new readiness ledger records the eight same-run live spans,
  ten-chapter TTS/subtitle work, claim matrix, two 1× reviews, export, and external publish gates without creating substitute media.
- One private exact-build phase-two debugger run hit callback RVA `0x3B9AB90` after 18.101 seconds and observed
  `RCX=[node+0x88]`, runtime vptr RVA `0x408A450`, and slot-2 target RVA `0x947BD0`. Capture SHA-256 is
  `F78961E40F93C0D91E71EACFEFB65A6F5D0F6CCA9276B42F570D05F97834B816`; post-exit cleanup SHA-256 is
  `35087FA3CBD071AC2DF65666B5EF79BE6E951AEE0DC5BA36A725998A7562112B`. This closes one runtime identity entry and
  disproves the two prior static candidates for this invocation, but RTTI/source ownership, return/lifetime, seed, paused acceptance, and
  video capture remain open. Phase two remains `native-readiness RED + not-live`.
- G2's single private paused capture produced two stable `root_shape_drift` rows before evaluator invocation. Report SHA-256 is
  `31377779207957E3D18D7260945F5CD933DCCC497F2C1844A95D5CC01F02421E`; JSONL SHA-256 is
  `47514E299D87F6878229115BB60897F5E23FA28DACEE81937619D21A44D268EF`. No mutation or time advance occurred and cleanup
  was GREEN. The four read-only live primitives remain valid, while `evaluated_days`/expiry, war-bound, decision/action/postcondition and
  `GEN-034` remain unresolved. The next entry is a staged private loaded-tree shape capture, not a replay of this checkpoint.
- `open_kaishek 17caa28` and CI `33609105817` are GREEN. The update documents the exact phase-two and G2 OfflinePreflight usage and
  retains `certified=false`; it deliberately adds no unsupported opcode, scope, runtime, or native-readiness claim.

## 2026-09-02 17:56 bounded continuation

- Phase two proved both observed callbacks return and their vectors exhaust (live SHA `A55D2E84...47F05`), moving the real stall outside the callback vector. Eight external callers remain statically ambiguous; the next distinct entry is a private `[RSP]` return-address capture at `0x3B9ACE0`. Readiness remains RED/not-live.
- G2 narrowed stale root shape `19/14` to live `13/12`, then five scripted candidates to two distinct index `9/10` paths. Latest report/JSONL SHAs are `3E1F6139...F992B7` / `55769B6F...D70322`; no Truce node, duration, expiry, mutation, or readiness promotion was claimed.

## 2026-09-02 18:16 outer-return and Context-child continuation

- Phase two mapped the two real callback-loop outer returns in one bounded live run. Sequence 2 (`CJominiLoadScreenDatabase`) returned on the same thread to `0x88B5E1`; retained artifact SHA-256 is `D694436E19762B1449FBEE38090A29C4451B64B332015646056868E9861C5CC1`. The selected caller's `0x88B5E2..0x88B648` tail is local teardown only. Static follow-up found no direct caller of `0x88B480`; it is the unique slot-2 target of vtable `0x408DBF0`, so the next distinct live observation is `[RSP]` immediately before `RET 0x88B648`. Parent integration commits are `98abdf5` and `f122cdc`; readiness remains RED/not-live.
- G2's single Context-child run observed index 9 as `0x44D1E18 (1/1)` and index 10 as `0x41E36D0 (6/6)`, with zero matches for Truce vtable `0x4461CA8`. Report/JSONL SHAs are `D2C82BFB...E8FBCC` / `47519D81...B48AA`; cleanup was GREEN and no evaluator or mutation ran. Parent commit `1f8ac3b` preserves the four production-live read-only primitives while `evaluated_days`, expiry, decision/action, and `GEN-034` remain unresolved.
- `open_kaishek 425514e` and CI `33617669452` are GREEN. Current phase-two/G2 preflight SHAs are `80FC14AF...CB70823` and `BBEEE578...E134792`; both remaining gaps are native-only, so no unsupported schema or certification was added.

## 2026-09-02 18:52 wide-parallel delivery

- Phase two advanced through a GREEN selected-lambda outer-return capture (`F10CDD41...A4958`), the completion publish at `0x3B9CFD7` (`D831486D...0158B`), and the unique scheduler consumer at `0x3B9DEA7` (`4E189772...2CDCD9`). The runner now reports `loader_terminal_missing_after_database_completion_publish` instead of the disproved current `database_callback_stall` attribution. Parent commits through `1d37b2e` preserve public readiness as RED until a filtered observer and seed/paused live artifact exist.
- The eight-span choreography (`c8e6e3b`), managed real producer registry (`c98181e`), and media environment preflight (`8d5ded0`) are implemented. The latter verified the current promo main, Xiaoxiao voice, fonts/layout, and actual FFmpeg encode stack; receipt SHA is `35AEC1BB...E1C`. No substitute footage or candidate video was created. Typed blockers remain `seed_not_ready` and `span_handlers_missing`.
- G2 integrated next-layer live shapes (`3c5c3c9`), RTTI narrowing (`b078d3a`), a fail-closed surrender execution gate (`30fe843`), and the exact `spawn_army 0x2E7F951..0x2E7F9A6` war-bound observation seam (`1049b05`). Truce duration/expiry and source-bound losses remain not-live; no mutation or readiness promotion occurred.
- `open_kaishek 0390b9a` remains compatible with all current parent increments. Phase-two/G2 preflight SHAs are `42CF0189...1A674A9D` and `CD2509F...35FFF220`; no script corpus/schema change justified a new descriptor, and uncertified capabilities remain `certified=false`.

## 2026-09-02 19:35 Phase2 seed RED and continuation boundary

- Phase2 integrated the seed deadlock fix, loaded-seed v2, all eight visual handlers, the default managed producer, the media-release receipt gate, and no-launch capture/loaded-seed wrappers. The frozen live candidate manifest is `Z:\p2r-25b6\ready-to-run.json` (SHA-256 `0D374901...2B71F`), built from exact commit `25b6036` with the private completion observer enabled. Its dedicated native, integration, optimized, loader, media, static, and no-launch gates were GREEN; no media was generated.
- The single bounded seed attempt ended typed RED after 299.825 seconds: `loader_terminal_missing_after_database_completion_publish`. Report SHA-256 is `25A3C8C5...C13FE7` and terminal evidence index SHA-256 is `AF847AEF...C8FB9`. The run observed completion publication at `0x3B9CFD7`, with no fatal errors and an empty `error.log`, but never authorized event wait and produced no seed candidate, capture, or native readiness artifact.
- The private observer remained installed with failure 0, yet its final sequence-1200 heartbeat reported zero selected/state2/state3 events and no retire sequence. Cleanup was fully GREEN and source/runtime inputs were unchanged. The next distinct Phase2 observation is a private-only pre-filter raw counter plus last state/callback-slot2/refcount diagnostic; the same timeout will not be repeated.
- A separate integration audit found that the seed runner always tears down its managed session in `finally`, while the loaded-seed wrapper does not bind its expected connection generation to the owner. Minimal owner-process inline continuation and generation binding are in progress so a future GREEN seed can flow directly into loaded-seed proof and eight-span capture without a false cross-session handoff.
- G2 delivered the public aggregate-session live acceptance wrapper (`2b0d81d`, preflight SHA `8A716270...C61DB`) and corrected the residual truce path to index 7 before integrating the targeted private capture (`f893ff8`, manifest SHA `FC80A1F9...CCA81`). Official Runner CI runs `33624581218` and `33624759212` are SUCCESS. `open_kaishek 0390b9a` remains compatible because these increments add no Paradox corpus or adapter/API change; `certified=false` is unchanged.
- The promo tool was fetched again during this interval and remains clean at `HEAD == origin/main == 57c42fca`. It will be fetched once more immediately before real video production, after live footage gates pass.

## 2026-09-02 19:42 same-session correction and G2 index-7 live

- Exact CFG ordering shows the completion wrapper calls its consumer before the producer that publishes the selected task at `0x3B9CFD7`, then returns. The zero selected count is therefore consistent with the wrapper never receiving a subsequent tick; it does not by itself prove a selector mismatch. The private raw-hit increment will distinguish no next consumer call from a filter mismatch.
- `dd096e6` now binds loaded-seed proof to the capture owner's initial PID and connection generation. `7f0c892` binds the canonical seed contract into the capture command and corrects the ownership model: seed generation may clean up, while a new capture owner must perform loaded-seed proof and all eight spans inline in its own still-live managed session. The current handoff is correctly typed `seed_artifact_pending` (manifest SHA `BF84B2B2...DD8B3`).
- The single G2 index-7 run ended before private-reader invocation with harness/native-readiness RED (`last=None`, 126.891 seconds). Report SHA is `6997DDA2...E2C58`; no private JSONL, Truce hit, evaluator call, mutation, or time advance occurred. Cleanup SHA `6014C3EB...91906` and all source/checkpoint invariants are GREEN. Capability readiness remains unchanged and this run shape will not be repeated.

## 2026-09-02 19:58 raw Phase2 candidate and parallel media/G2 delivery

- Phase2 commit `2f4071d` adds private-only raw completion hook/state/callback/refcount diagnostics without changing the selected filter, public ABI, or readiness. Its ready artifact SHA is `16632F0D...776CA`; dedicated native/Python/default-private boundaries and Official Runner CI `33626351177` are GREEN.
- A fresh exact `2f4071d61c9618850f06e03f6eaa59b1b0cf3af8` candidate is frozen in `Z:\p2r-raw`. ZIP/private DLL/injector SHAs are `6A5ACA7D...D5B6F`, `D63DAD6E...276C52`, and `6C4F5DB2...B9EEED`. A launch-free malformed pipe setup attempt was retained RED; the corrected no-launch preflight is GREEN (`C2CB06CF...2A677`) and final manifest SHA is `C81FB6E...F1A34`. Its one authorized live started at 19:57 and remains unclassified until terminal evidence exists.
- The first G2 public session-binding live reached paused `native:3` and passed both query/session/cache identity checks, with typed unavailable truce/war-bound domains, incomplete aggregate, false action readiness, no mutations, and GREEN cleanup. Raw report SHA is `4E7F1F7A...EA94D`. Its overall RED exposed only a legacy base-gate composition bug; `c6efcf3` fixes that runner verdict while preserving mismatch failures and legacy diagnostics. No second CK3 run was performed.
- G2 also integrated the bounded war-bound capture runner (`6651ea9`), whose current `--verify-only` gate is ready without launching CK3. The Phase2 promo authoring ledger (`1099eca`) now contains all ten Chinese-primary, bilingual-subtitle chapter drafts and binds all eight gameplay chapters to future canonical footage/postconditions; every chapter remains `release_usable=false`, and no TTS or media was generated.

## 2026-09-02 20:06 Phase2 raw-consumer live result

- The one raw-observer run ended typed RED after 299.753 seconds with publication at `0x3B9CFD7` but no post-init/event/native readiness. Report and terminal-index SHAs are `DD6F61C8...4098A8` and `7E6B405A...32876487`; no seed candidate or media exists.
- The private hook was installed without failure and recorded 1,908 raw consumer hits, but zero unfiltered state-2/state-3 reads and zero selected events. This proves the hook executes repeatedly while none of those task-state reads is complete; selector, refcount, and retire logic were never reached. Cleanup SHA is `77F30BF2...E677B2` and all process/source/runtime invariants are GREEN.
- `806a8b2` freezes the outer wrapper call boundary: 618 direct callsites owned by 525 functions, with no internal post-publish consumer back-edge. The next distinct observation is `[RSP]` at wrapper entry, mapped through the frozen callsite list, followed by bounded caller/object-flow analysis. The same timeout and selector change are not repeated.

## 2026-09-02 20:12 G2 binding GREEN and war-bound harness RED

- The patched public session-binding rerun is GREEN. Report/session-summary/cleanup SHAs are `DD46F69A...2B135D`, `118A7D0C...60FDD`, and `AF1263B3...4D9B`. Exactly two queries bind the same connection, episode, PID, snapshot, revisions, character, and war; truce/war-bound remain typed unavailable, the aggregate remains incomplete, action readiness remains false, and no mutation occurred. This promotes only the session-binding evidence, not surrender capability.
- The one war-bound live stayed on the loading screen for 120 seconds and ended harness RED before Robert selection, the natural event, action arming, or private capture. Recovered report/cleanup SHAs are `35AB7623...A99460` and `23143DE3...73E84E`. Commit `8566ada` preserves missing/zero-byte/malformed capture as typed evidence instead of masking the terminal with JSON decoding; focused normal/optimized tests are GREEN. A future run requires a distinct staged/extended readiness contract and remains subordinate to Phase2 CK3 priority.

## 2026-09-02 20:22 index-7 input and Phase2 wrapper-entry readiness

- G2's staged index-7 live reached the intended read-only target twice: default `4/4`, hidden child-1 `0x44D1C88 (1/1)`, Context `0x44D27B8 (1/1, scope_count=1)`, Truce object `0x1D0752B8FB0` with exact vtable `0x4461CA8`, and duration script-value at `object+0x108`. Report/JSONL/evidence-summary SHAs are `24D9661A...3706`, `6807E408...05C3`, and `27ABEC1...B0FA`. Evaluator execution, expiry, decision/action readiness, and `GEN-034` remain unresolved; no mutation or time advance occurred.
- Phase2 exact object flow now proves the synchronous completion task enters only the producer list and is published by the wrapper's mode-false synchronous producer loop, not the consumer ring. Artifact SHA is `35359957...7EA1F`; this explains the prior raw-consumer zero-state observations without changing public readiness.
- Commit `54e7a6a` integrates a private-only wrapper-entry observer for `0x3B9E030`, recording the runtime caller/callsite, scheduler owner, producer-list carrier, thread/QPC, and entry count. Frozen v1 has no return hook. Its ready artifact SHA is `73927B85...63D7`; native fixture, Python `15/15`, default/private boundary, and Official Runner CI `33630754182` are GREEN. A fresh exact-master freeze is building default/private Release and no-launch evidence before one authorized P0 live; G2 evaluator preparation continues without CK3.

## 2026-09-02 20:49 Phase2 runtime caller identified

- The one wrapper-entry live ended at its 300-second loader boundary with the prior typed RED, but achieved its distinct observer goal. Report/cleanup SHAs are `0556C55E...3BE7` and `D7B97E0F...00B7C`; PID 20844 and its job/watchdog/control state were fully cleaned, and all frozen inputs stayed unchanged.
- The private hook was installed without failure and counted 1,220 wrapper entries. Its last sampled runtime callsite is `0x3407D9C`, inside the frozen 618-callsite set; scheduler owner `0x22ED9921A00`, producer-list carrier `0xF282FFEE70`, and thread 44900 are nonzero. Postprocessing is GREEN (`entry-caller-owner-carrier-observed`), artifact SHA `D74E8345...B054F8`. Return lifetime is explicitly not observed by v1 and does not affect this result.
- The next Phase2 work is exact-build static analysis of the sole observed caller's PDATA owner, pre-call parameter sources, and post-call synchronous producer-list lifetime; this run shape will not repeat. The postprocessor is integrated at `50b372e` with 18 focused tests GREEN. In parallel, G2's default-OFF index-7 evaluator package is integrated at `e0446a0` and no-launch ready for one read-only live when the Phase2 slot is free.

## 2026-09-02 20:54 G2 evaluator envelope RED

- The one authorized evaluator live ended after 166.314 seconds with a harness RED: `official MCP result lacks structured_content`. Readiness and exact-build proof existed, but CK3 exited before the MCP sequence/private reader; no private JSONL was produced, evaluator call count remained zero, and no `evaluated_days` result exists. This is not a capability RED and does not change readiness.
- Report and terminal-summary SHAs are `4460C7E8...E383` and `EF5AB8F1...5D28`. Process-tree, driver, and source-input cleanup is proven; no mutation, Context execution, war termination, time advance, or second live occurred. The next distinct step is a fixture-backed minimal runner fix for the actual official MCP result envelope, with no CK3 rerun until that static boundary is ready.

## 2026-09-02 21:04 post-call seam and final-promo execution plan

- Phase2 binds runtime callsite `0x3407D9C` to PDATA owner `[0x3407C70,0x3407F80)`. The producer-list fifth argument remains available at `RBP+0xE0` immediately after return, before destination transfer, retry, and release. Commit `ae309fa` integrates the exact static contract (artifact/ABI SHAs `4D79ABEB...7817D` / `677D0F05...2FE73`, 20 focused tests GREEN); the next private-only seam is the exact 14-byte `0x3407DA1..0x3407DAF` continuation.
- G2 commit `85a5ec9` now preserves typed official MCP error envelopes per call, including the failed tool, result type, is_error, structured content, and original content blocks, without accepting text fallback. The fixture binds the actual live failure; focused and optimized tests are GREEN. A separate durable pre-call/post-call row package is in progress before any distinct evaluator rerun.
- Commit `b541362` adds a deterministic final-promo runbook. Its current no-media artifact SHA is `4C7D74E...BB93A7`, correctly typed `footage_pending`; it freezes the ten chapters, eight spans, Xiaoxiao voice, bilingual subtitles, dependency/hash fields, two 1x human reviews, export/publish gates, and a mandatory fresh promo-tool fetch/ff-only/clean/HEAD-alignment first step. No TTS, subtitle media, FFmpeg output, or substitute footage was created.
- `open_kaishek 0390b9a` remains compatible: no adapter/API, `.txt/.gui` corpus, profile, or fixture change exists in these native/MCP increments. Existing Phase2/G2 preflight artifacts remain authoritative and `certified=false` is unchanged.

## 2026-09-02 21:16 durable evaluator boundary and war-bound prelaunch correction

- G2 commit `5dba7f9` adds durable, fully flushed `pre_call`, `post_call_1`, and `post_call_2` rows around the private index-7 evaluator. The pre-call row binds the exact path, Truce/vtable, `+0x108` input, both contexts, evaluator RVA, and planned call count before execution; fixtures cover process exit after pre-call and stable two-call completion. Static artifact SHA is `B94CA685...1025B`; Python/MSVC/native tests are GREEN and public readiness is unchanged.
- The one staged war-bound authorization ended before CK3 creation because the frozen 1,200,000-ms capture timeout exceeded the old executable's 900,000-ms argv bound. Report/cleanup SHAs are `F96DAE07...BC1E2` / `E12A76D3...89D40`; there was no screenshot, Robert/bookmark/action-arm/capture, mutation, or time advance. Commit `053230c` minimally raises the private bound to 1,200,000, rebuilds the executable, and freezes a new verify-only GREEN manifest (`EBD2AFC1...40C18`) without starting another attempt.

## 2026-09-02 21:40 Phase2 list scan and G2 first-call boundary

- The single Phase2 post-call live scanned one nonempty producer list containing 27 descriptors, with no read failure or truncation, but selected zero callbacks matching the frozen target. Report/cleanup SHAs are `35174DC7...025C` / `0E754E64...46CEB`; PID 44916 and all managed state were cleaned. The startup manifest was `464662FE...1EEEA`; a post-start evidence-only revision `1F9AAE5A...66745` added schema/input description but changed no command, pipe, source, or binary.
- Commit `7f59aba` classifies this evidence `NO-GO/scan-no-selected` (artifact SHA `C886C18E...78559`, 12 tests GREEN). The next distinct observer records the bounded list's complete callback slot2 RVA histogram and associated task/owner identities; the last-value-only run will not repeat.
- The G2 durable evaluator live retained exactly one pre-call row (SHA `5CB93ED0...15281`) with the verified index-7 path, Truce/vtable, `+0x108` input, both contexts, evaluator `0x3373000`, and planned count 2. CK3 exited inside the first evaluator call before any post-call or evaluated-days result. Report/cleanup-summary SHAs are `598EC654...15A93` / `C4157B5E...93574`; no Context, mutation, war termination, or time advance occurred. Static exact-build ABI/callsite/TLS analysis is now required before any rerun.

## 2026-09-02 21:48 staged war-bound loader evidence

- The corrected attempt-002 created one CK3 process and retained screenshots at 60/120/180/240/300 seconds; every frame still showed the loading page. It ended typed `HARNESS_RED/MainMenuReadinessTimeout` after 304.042 seconds, before Robert/bookmark/action-arm/private capture, with no mutation or time advance. Report/cleanup SHAs are `A0A20243...A9B9F` / `DBCF0FC9...9784C`; all processes were cleaned and source inputs stayed unchanged.
- This staged evidence closes the debugger-owned cold-start extension path. The next distinct harness starts CK3 normally to main menu, verifies exact PID/build/hash, then performs the private read-only attach; it is being prepared offline and will not claim capability readiness before live evidence.

## 2026-09-02 22:21 full Phase2 task identity and G2 context fix

- The Phase2 list-identity live captured one consistent 27-descriptor snapshot with no read, truncation, capacity, or contention error. All 27 descriptors share one owner and one callback slot2 RVA, `0x817C20`; indices 0–24 are state 1 and 25–26 are state 0. Target `0x88B480` appears zero times. Report/cleanup SHAs are `109CB334...15471` / `2B60336D...A7621`. This proves the observed `0x3407D9C` list is a different task domain; static PDATA/RTTI/owner analysis of `0x817C20` replaces any repeat live.
- G2 exact-build analysis of 79 evaluator xrefs proves the third argument must be the pointer stored at `effect_context+0x28`, not that field's address. The three-argument ABI and `+0x108` input are correct; the prior first-call exit is therefore explained by `wrong_evaluation_context_kind`. Commit `388fa3f` implements a private-only dereference/non-null guard with valid-pointer fixture results `1825/1825`, durable rows intact, and no public readiness/action change.
- Commit `aab14ec` adds the distinct normal-start→private-attach war-bound harness. It starts one non-debugger CK3, waits for main menu with staged evidence, revalidates PID/path/build hash, and only then attaches the private capture. All offline/optimized/self-test/verifier/no-launch checks are GREEN; no live claim has yet been made for this harness.

## 2026-09-02 23:05 P0 parallel task identity, acceptance, and media-entry closure

- Phase2 P0 resumed as three visible non-conflicting workstreams. Commit `5d0a93e` identifies `0x817C20` as the generic `SPdxParallelForOverArray` range worker shared by 278 valid RTTI vtables, not the loader completion callback. The 27-item runtime list is therefore excluded; static/postprocess artifacts are `8B508388...F35A8` / `8F549C73...31959`, and readiness remains RED. The next distinct observer is the true producer boundary at `0x3B9CFD2/0x3B9CFD7`, capturing RBX task, callback/vptr/slot2, owner, and publish state.
- Commit `bbb2037` adds an opt-in Phase2 observer acceptance gate. Missing native manifests stop before supervisor/driver with typed `native_observer_manifest_pending`; complete manifests must bind exact source/game/binaries, ABI/source contract, heartbeat, and report schema. Focused tests and Official Runner CI `33645388541` are GREEN; no CK3 was launched.
- Commits `cadd790` and `3c8e03d` bind the existing 10/10 bilingual authoring ledger and strict same-session 8-span footage intake into the final-promo runbook. False `authoring_pending` is removed; the only current blocker is real `footage_pending`. The runbook artifact SHA is `98A8E1B5...D77E`. No TTS, subtitle, FFmpeg, or substitute media exists; actual production still starts with a fresh promo-tool fetch/fast-forward-only/clean/HEAD-alignment receipt.
- With owner authorization limited to required Paradox User Agreement/EULA/Terms of Use, commit `4b1e0e9` adds legal-title/version, pre/post screenshots, isolated accepted-marker hashes, and an optional-consent denylist to the G2 normal-start attach harness. Commit `52e4e16` adds a default-off private observer at the two native evaluator callsites and ends the crashing direct-evaluator route. Both remain static-only with public readiness unchanged. open_kaishek `0390b9a` requires no change for these commits or the Phase2 media/native increments; adapter/API, profile/fixtures, and `.txt/.gui` corpus remain unchanged and `certified=false`.

## 2026-09-02 23:49 Phase2 producer observer live stopped at legal gate

- Commit `ad9bdc1` integrates the default-off private producer observer at the non-overlapping `0x3B9CFD2/0x3B9CFD7` transaction. A dual-private-option MSVC Release integration build succeeds and both the Phase2 and G2 observer native tests pass. The P0-only frozen candidate binds exact `da43787`, clean source ZIP `7411C913...043A`, private DLL `567B5C18...3DD3C`, and acceptance manifest `4D882BF8...A4C0` with a GREEN static gate.
- Its only live ran from 23:44:54 to 23:45:09 on PID 62944 and stopped typed `RED/LegalConsentNotAuthorized`. The shared handler made zero authorized clicks and accepted no optional consent. The observer installed without failure but D2/D7 counts stayed zero because the legal gate stopped first; this is a harness/legal-evidence RED, not a native capability verdict. Runner/legal/cleanup SHAs are `D663CCF6...D6518`, `F8AB66E6...055E9`, and `6B8D8275...16AF7`; cleanup is fully GREEN and the CK3 slot is free.
- The concrete next P0 repair is bounded to the observed diagnostic gap: persist the pre-click screenshot/hash and normalized OCR rows before classifying every ambiguous modal, then use that evidence to decide whether the modal is an authorized split/localized legal agreement. The failed attempt will not be repeated. Final-video execution remains gated on real 8-span footage and an explicit external publish target; local artifact directories and the Steam static-image media strip are not treated as publication.

## 2026-09-03 00:04 daily rollover and permanent in-game consent authorization

- The 2026-09-02 daily report is formally closed: its Phase2 and G2 execution commitments are partial and carried forward rather than promoted to complete. The new daily meeting and rolling report preserve P0 Phase2/video, P1 G2, and P2 open_kaishek ordering.
- Commit `4fd92a7` records the owner's permanent authorization to accept any CK3 in-game agreement/consent and acknowledge ordinary in-game notifications. Automatic purchase/payment/store/DLC-order controls remain a hard deny and typed stop; Windows/Chrome system Toasts remain close-only.
- The immediate P0 gate is the shared handler's tested implementation, followed by a fresh exact-head freeze and one distinct producer-observer live. Final footage and video status remain `footage_pending + publish_target_pending`; no TTS, FFmpeg candidate, export, or publication is claimed.

## 2026-09-03 00:15 permanent-authorization implementation

- Commit `1b4ff63` (from `b80d650`) applies authorization version `2026-09-03-any-ck3-agreement-and-notice-v2` to the shared handler and both Phase2/G2 runners. Any CK3 agreement/consent is authorized; ordinary notices require tracked CK3 context plus a safe confirm/continue/close/OK control. Purchase/payment/order/checkout/store text or controls stop before safe-button lookup with zero clicks.
- Preclassification screenshots and hashes, raw/normalized OCR, authorization version, visible actions, and isolated marker state remain durable. Agreement marker delta and ordinary-notification post-state are handled separately.
- G2 `16/16`, Phase2 `15/15`, seed-runner normal/optimized, py_compile, diff-check, and Official Runner CI
  [33653736041](https://github.com/XenoAmess/ck3_eternal_recurrence/actions/runs/33653736041) are GREEN without CK3. The candidate was then paused before live for the real-payment boundary correction below.

## 2026-09-03 00:18 real-payment boundary correction

- The owner clarified that the prohibition covers real-money Paradox/Steam/external-store purchases, while CK3 gold, prestige, piety, and other in-game resource spending remain normal gameplay. The `1b4ff63` pre-action body-token stop can overblock an informational notice whose only action is Close/OK.
- The P0 CK3 launch was paused before process creation. The shared classifier is being revised to block actionable real-commerce controls or explicit real-payment confirmation context, allow safe dismissal of informational notices, and retain evidence plus typed stop for ambiguous actionable commerce. A new exact HEAD will be frozen after focused tests; the `1b4ff63` candidate will not launch.

## 2026-09-03 00:30 action-aware commerce, wider parallelism, and G2 evidence finalization

- Commit `dabbd25` (from `badfea7`) implements action-aware commerce v3. Actionable purchase controls, or Confirm/Continue combined with Steam/Paradox Store/DLC/cart/checkout/order/payment/real-currency context, stop with zero clicks. Informational purchase text with only Close/OK can be dismissed. CK3 gold/prestige/piety/renown/influence/legitimacy/claim spending leaves the commerce handler for gameplay policy; conflicting external/in-game actionable signals stop as `CommerceActionAmbiguous`. Focused normal/optimized/compile checks are GREEN. Commit `f850057` aligns `AGENTS.md` and the testing workflow.
- Commit `d0fa156` closes the passive G2 raw-report → typed-postprocess → SHA-bound acceptance-report path. Normal/optimized tests are `24/24`; Official Runner CI [33654519809](https://github.com/XenoAmess/ck3_eternal_recurrence/actions/runs/33654519809) is SUCCESS. It does not promote action, decision, expiry, or war-bound readiness. open_kaishek gives an exact NO-CHANGE verdict and remains `0390b9a` / `certified=false`.
- Commit `6ed194e` requires periodic reassessment of additional parallel work at starts, waits, completions/blockers, priority changes, and CK3 lock transitions. The resulting active non-CK3 lanes cover Phase2 action classification, exact freeze preparation, media-environment preflight, authoring/claim review, G2 native research, CI, and open_kaishek compatibility; only CK3 launch remains exclusive.

## 2026-09-03 00:43 Phase2 producer live and parallel media/G2 closure

- The exact `f850057` Phase2 candidate passed clean freeze/no-launch gates (source ZIP SHA `1257F300...11840A`, manifest `89919DEC...BA193`) and ran once from 00:32:32 to 00:37:55 on PID 67092. Legal v3 was GREEN/no-modal/zero-click. The producer observer installed cleanly and recorded D2/D7=1838 each with zero read failures, but the loader still ended `loader_terminal_missing_after_database_completion_publish`; its last-only sample was generic slot2 `0x817C20`, state 1→2. This proves the producer seam is active but does not expose the full distribution. The next distinct observer is a bounded slot2 histogram plus exact `0x88B480` count and selected first/last identity. Evidence doc commit is `53c248c`; runner/cleanup SHAs are `06D8A458...55CBD` / `BADEC73B...64638`, and cleanup is fully GREEN.
- Phase2 media environment commit `c26cef8` records a no-media GREEN receipt (`DD9D0AEB...63F8B`) for FFmpeg/ffprobe, H.264/AAC/MP4/libass, Pillow, Xiaoxiao provider catalogue, fonts, planned paths, and disk capacity. Final readiness remains `fresh_promo_tool_fetch_required + footage_pending + publish_target_pending`; no TTS, subtitle media, encoding, candidate, workdir, or upload occurred.
- Authoring commit `790373a` (from `c51e613`) narrows six claims that exceeded their future footage postconditions, replaces a misleading finale `COMPLETE` title, and freezes Chinese-primary/English-secondary semantic line breaks plus 8/8 visible observations. All focused normal/optimized validators remain GREEN.
- G2 commit `dbc21bb` (source `390b1ee622b43d9381a2b78005ee7683adddec21`) updates the native-AI evidence/Mermaid first, then wires a strict same-frame generic war-bound current intake. Missing or drifting identity remains unavailable; source attribution, losses, action, and automatic policy remain false. Normal/optimized 243-test slices and Official Runner CI are GREEN. open_kaishek returns NO-CHANGE for both `c26cef8` and `dbc21bb`.

## 2026-09-03 01:22 histogram closure, two-cut direction, and honest pending boundary

- Permanent CK3 in-game agreement/notification handling is implemented through `4fd92a7`, `1b4ff63`, `dabbd25`, and `f850057`.
  The action-aware classifier allows agreements and safe notice dismissal, distinguishes CK3 internal resources from external commerce, and retains a
  zero-click typed stop for actionable Paradox/Steam/real-money purchase ambiguity. This is complete implementation/test work, not a claim that a new
  agreement modal appeared in the latest live.
- The exact `1619625` Phase2 histogram live closes its intended producer observation: D2/D7=`1838`, all observations conserved across 25/64 bins,
  target callback `0x88B480` selected exactly once, with consistent first/last task identity and
  `GREEN / selected-consistent-next-gate`. Runner/postprocess/cleanup SHAs are `1A6D4FD9...BB46F1`, `F50DF377...24E990`, and
  `9C801DA2...9D6A9`; source ZIP/manifest SHAs are `29CDF95B...3D9A4` / `795B6F15...FBD9`. The full seed runner nevertheless remains
  `RED / loader_terminal_missing_after_database_completion_publish`: callback execution is proven, scheduler consumption and loader readiness are not.
- Both requested final-video directions are now durable docs: character-led cut `08b2fc1` and institution-led cut `a9dfaa7`. They are separate final
  deliverables that may reuse one qualifying set of Phase2 gameplay material but require distinct narration/editing and independent review; renaming one
  rendered file cannot satisfy both. Commit `82d6b77` also replaces the overstrict all-spans-one-session rule with a lineage-bound contract: clean restarts
  are allowed between spans, while canonical seed/save lineage, exact source/game/mod mount and each span's continuous pre/action/post identity,
  checkpoints, postcondition and cleanup remain mandatory. Seed generation to loaded-seed proof itself remains one continuous session.
- The promo checkout has been freshly fetched and verified clean at `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`.
  The production gate still requires one more fresh fetch/ff-only receipt immediately before media generation. **Neither cut has been generated**:
  canonical seed, loaded-seed proof, 8/8 real spans, both final scripts/subtitles/TTS/edits, source and final-MP4 1x reviews, export, external publication and
  receipts all remain pending. Director treatments and no-media validation are not live footage or finished videos.
- G2's exact passive candidate ended `NO-GO / no_native_callsite_hit`: both hooks installed, but 241 heartbeat samples retained zero pre/post calls.
  Raw/typed/acceptance SHAs are `1F319AE1...EB0AC9`, `64EC9BAA...F0795693`, and `2D2F2D47...4731E`. Commit `39c4d5e` maps the activation
  boundary: the loaded node is `CAddTruceEffect<0>` and only `0x2EDAF0F` applies; paused heartbeat-only observation never dispatches mutating slot 22,
  while shared preview `0x2E87140` cannot return duration. Static artifact SHA is `23426315...255AD`; no readiness changed. `dbc21bb` separately adds
  strict same-frame generic war-bound current intake while source attribution, pre/loss, action and automatic policy stay false.
- `open_kaishek` verdict remains NO-CHANGE for these native, runner, media-contract and director-doc increments. It stays at
  `0390b9a959fa1a59a968000ed49e827a03b8d4e4`, JAR SHA `421F49C...0F2CB`, with `certified=false`; no unsupported version churn was created.
- Next P0 is the distinct selected-task-to-scheduler-consumer correlation, then canonical seed/loaded-seed and eight lineage-bound spans. Final delivery is
  two separately reviewed cuts, not one; G2 proceeds on its distinct activation/observation path in parallel, and open_kaishek continues change-triggered
  compatibility checks.

## 2026-09-03 01:46 dual-cut execution contract, receipt producer, and next native seam

- Commits `48c378e` and `24d02af` establish `docs/phase2-promo/` as the canonical home for both director treatments. `2d4ebf8` and `908abfd`
  add separate character/institution project configs plus authoring overlays and a shared-source/separate-edit production contract. `09b7524` freezes the
  final filenames as `zhongguo-361-phase2-character-led.mp4` and `zhongguo-361-phase2-institution-led.mp4`. Commit `4773bd3` adds the aggregate
  completion validator: both cuts need distinct candidates, audits, two 1x reviews, exports and real publication receipts before aggregate `COMPLETE`;
  one cut, a renamed duplicate, or shared sign-off remains `both_cuts_pending`.
- This closes canonical docs, two configs/overlays, final names and the dual completion gate, **not media production**. Both project timelines remain
  planned, both overlays remain `release_usable=false`, and media count is zero. Seed, loaded-seed, 8/8 spans, two narration/subtitle/edit paths, two
  independent reviews, exports and publications remain pending.
- Commit `cb99ec0` emits and validates span-session-v2 producer receipts. Every span carries native paused pre/post snapshots, start/end checkpoint bytes
  and SHA, session/PID/generation/revisions, provider postcondition and cleanup. Clean restart between spans is allowed only under one canonical seed/save
  lineage and exact product/game/EXE/product-only-mount identity. Phase1/fixture/substitute footage is rejected. The resulting immutable eight-span bundle
  may be referenced by both edit projects without re-recording; this path is static/fixture GREEN and still has `0/8` real spans.
- The exact `82d6b77` seed live ended typed RED after `299.805s` in `database_init`: `0x3B9CFD7` completion publish was observed, but there was no
  event wait, native readiness, seed candidate, capture or loaded-seed proof. Runner/loader/cleanup SHAs are `3A7D162E...B1B7E7`,
  `3843E358...3B596`, and `4AD8B277...8CD08A`; cleanup and all immutability checks are GREEN.
- The subsequent exact `09b7524` correlation live again selected the real loader producer task exactly once (task `0x18E97E09D50`, state 2,
  slot2 `0x88B480`), with both hooks installed and failure=0. The consumer's inline raw counter at `0x3B9DEA7` remained zero, so identity matching was
  never tested. Its typed result is `RED / no-consumer-hook-hit`, not evidence of a different task. Runner/postprocess/cleanup/index SHAs are
  `DC42BAA0...EE6B21`, `529F41DF...A8703F`, `085E5E29...C89728`, and `0476483A...25A6AD`; cleanup is GREEN and no authorized/external-commerce
  click occurred. The next distinct P0 seam is the `0x3B9E030` wrapper entry plus branch/call edge to the consumer, retaining the selected D7 pointer.
- G2 commit `26784bc` publishes the strict optional `generic_war_bound_current` child on the real public terms query frame. Native focused `5/5` and
  Python normal/optimized `241` tests are GREEN. It can promote only current generic war-bound visibility; source attribution, pre/loss, cleanup, action
  terms and automatic surrender remain false/null. The required paused two-query live is pending.
- `open_kaishek` returns NO-CHANGE for the dual-cut, receipt, native-observer and additive G2 public-child increments, remaining at `0390b9a` with
  `certified=false`. Official Runner CI is GREEN through integration `cb99ec0`; the latest successful run IDs are `33662200348`, `33662304455`, and
  `33662554889`. These are static/fixture results and do not promote live or media readiness.

## 2026-09-03 03:50 wrapper-edge environment RED and guarded G2 live

- The new Phase2 wrapper/consumer-edge observer is source-frozen and static GREEN, but its only CK3 run (PID `60736`) ended before observer
  installation as `harness/legal-capture RED`: the Codex sandbox desktop could not capture the active `WinSta0\\Default` desktop. All zero observer
  counters are therefore unobserved values, not evidence that the wrapper was never scheduled. There were zero agreement/notification/commerce clicks,
  cleanup is GREEN, and source/runtime/external state stayed unchanged. Closure SHA-256:
  `C4B6DA2399717921AE60302448BE0DFC200BD2F2131A6C11C7DAC3962E2CBAD5`.
- G2's one guarded retry (PID `7676`) proved the four existing startup guards advance past the prior `ck3+0x1DABD89` fault, but it then reached the
  next known main-thread fault at `ck3+0x369CB58` (`C0000005`, read `0xD0`) before mailbox readiness. The honest result is still RED with `0/2`
  queries, no mutation or time advance, no readiness promotion, GREEN cleanup, and unchanged checkpoint/driver bytes. Work continues through a
  distinct caller-local `0xAF4EE8` null-input guard; the same live is not repeated. The live-report/dump/exception/typed-terminal SHA-256 values are
  `43351F9F...AA80B`, `4D430A32...34F6`, `ABA231DF...6FDA`, and `DB2FCA90...98A68`.
- The two Phase2 video ledgers independently remain no-media GREEN: each has ten planned chapters and eight future-real-span bindings. Character-led
  and institution-led are both documented and separately configured, but neither is a rendered candidate or final video yet.

### Native-only Phase2 observer result

- The explicit heartbeat-only follow-up eliminated the sandbox screen-capture failure while preserving zero UI/gameplay input. On its one PID `37300`
  run, the observer installed cleanly by heartbeat sequence 2, but CK3 crashed about `13.5s` later at the historical `ck3+0x1DABD89` particle2 fault,
  before producer selection. Zero producer/wrapper/consumer counters are therefore pre-selection observations, not a scheduler verdict. The result is
  `startup-crash-before-producer-selection / capability indeterminate`; physical cleanup and all input/source invariants are GREEN. Closure SHA-256 is
  `9A89777E50E7C8B9C301DB82C368C7E862C280311EBBD897947E57A238AFFE03`. P0 now prepares an observer-plus-startup-guards base without repeating the
  failed shape.

### Fifth startup guard static-ready

- The real G2 `ck3+0x369CB58` crash has been converted into a default-off, caller-local guard at `0xAF4EE8`: null `RDI` skips only that call and
  continues at `0xAF4EED`, while non-null execution preserves the original call. Release/native and Python normal/optimized tests are GREEN; DLL and
  injector SHAs are `A1EFF67B...6136C` / `CF848D94...52E34`, and the official no-launch preflight is `ready-to-run` (`FCEDA319...33A70`). No live
  readiness changes yet. P0 receives the implementation first for its observer-combined candidate; G2 waits for the serial CK3 slot.
- This private native-only change leaves `open_kaishek` at `0390b9a`, JAR `421F49C...0F2CB`, `certified=false`, with a documented NO-CHANGE verdict.

### Phase2 observer with all five startup guards

- Clean commit `eb85ffd1` combined the five guards and Phase2 observer after all focused static tests passed. Its sole PID `31720` live installed every
  component with zero failure flags. The first four guards suppressed `8/1/1/5` known startup faults; the fifth did not fire. Before producer selection,
  the observer saw 190 wrapper/consumer entries, all through exact edge `0x3B9E175`.
- CK3 then exited `C0000005` after `13.557s` without an exception bundle, leaving the next RVA unknown. The typed outcome remains startup/harness RED
  and Phase2 capability indeterminate; it is not evidence that the producer path is absent. Physical cleanup and immutability are GREEN. Closure SHA is
  `FCD6FEB3...5E3D9`. P0 next freezes read-only crash-address capture before considering any further guard.

### G2 fifth-guard live advances to a new exact fault

- The one actual fifth-guard live (PID `6796`) advanced past `ck3+0x369CB58` and then captured `C0000005` at `ck3+0x3B67345`, read `[RCX+8]`
  with `RCX=0`, before mailbox readiness. It remains `0/2` queries with no mutation/time advance and GREEN cleanup/invariants. Dump analysis binds the
  null input to the single caller at `0x390A9ED` (`mov rcx,rbx`, `RBX=0`), so the next static candidate is caller-local and a global callee patch is
  excluded. Live-report/minidump/typed SHAs are `92E18264...ED3AD`, `596171DF...CFF27`, and `2822CE8F...AAE6F`.

### Exact shared startup fault and sixth-guard boundary

- A corrected Phase2 read-only second-chance debugger, synchronized on the exact bridge PID handshake, captured the formerly missing exception as
  `C0000005` at `ck3+0x3B67345` with `RCX=RBX=0`. The concurrent observer again saw 190 pre-selection consumer entries exclusively at edge
  `0x3B9E175`, zero producer selections, and all components installed without failure. This exactly matches G2's fifth-guard live; therefore no
  duplicate Phase2 sixth-guard run is performed and no seed/media readiness is promoted. The frozen exception and closure SHAs are
  `592D2008...65BAF` and `29E1E962...634E6`.
- G2's distinct sixth caller-local guard advanced beyond that RVA but then exposed `C0000005` at `ck3+0x3BE33A9`. The callee received
  `RCX=RBX=0x206E65704F534656`, which decodes to the non-object tag `VFSOpen `. This is materially different from the preceding null chain and may be
  evidence that a prior suppression skipped necessary initialization. The run remains `0/2` queries with no mutation/time advance and GREEN physical
  cleanup; minidump/exception SHAs are `F08D7EC2...C8908` / `9DB3D6B9...6732E`.
- Static data-flow binds this to a cold-map-startup asynchronous VFS error variant: the operation variant contains
  `VFSOpen Error:  not found` while its poll state remains `0`, so the pending branch treats the payload prefix as an object pointer. The configured
  default path is `/default.map`, and the physical file exists under `game/map_data`; a simple missing-file or wrong-working-directory explanation is
  excluded. No existing guard is yet proven to feed this VFS object directly, so containment-induced timing remains only an unproven possibility.
- Work now converges on upstream data-flow and historical no-guard comparison. No seventh guard or duplicate CK3 launch is allowed until that analysis
  proves either an independent faulty path or a minimal restore-initialization change.
- A fresh `open_kaishek` compatibility audit remains NO-CHANGE at clean `0390b9a`, JAR `421F49C...0F2CB`, `certified=false`: product script/GUI
  corpus, adapter/CLI schema, profiles, and parser/runtime fixtures are unchanged by these private native observers and guards.
- Phase2 downstream execution audit exposed additional honest gates hidden behind startup: scoreboard production capability is still fail-closed, and
  workforce m360 lacks exact owner/subject player-transition plus same-checkpoint A/B/C restore lineage. The dual-cut media path also requires explicit
  per-cut preflight binding, an auditable Xiaoxiao cache-prime command, and a cut-specific edit-order/reprise seam. These are now separate parallel P0
  implementation packages; media remains zero and no gate is reinterpreted as complete.
- Historical/current comparison shows the no-guard Phase2 freeze itself crashes at `0x1DABD89` only in the current `CodexSandboxDesktop-*` context,
  while otherwise byte-equivalent historical runs on `xenoa / WinSta0\\Default` survived to `database_init` and producer selection (but still never
  native readiness). The next discriminating launch is therefore the exact no-guard freeze on the normal interactive desktop, not another containment
  guard. Static work continues here; this sandbox does not cross the desktop isolation boundary.
- The shortest honest capture topology is two serial CK3 sessions (new canonical seed, then product-only loaded-seed + eight-span capture), followed by
  read-only intake. Event staging/drain and business postcondition verifiers are now separate P0 implementation packages. Once all code and desktop
  gates close, the downstream seed-to-8/8 path is estimated at `20–40` minutes; present engineering remains hours-scale and media is still zero.
- A default-off cold-map VFS observer is now static-ready at ctor/post-move/poll seams, with all-or-none install and no control-flow/state mutation.
  Its Release/native/Python/suspended-injection checks are GREEN and no CK3 was launched. Artifact index `196D9A72...B37D0`; status remains
  `static-ready-no-launch` for a future normal-Desktop versus sandbox A/B, not a startup fix.
- Phase2 downstream static work advanced on five fronts: Workforce exact rebind/shared-checkpoint A/B/C runner readiness; strict event
  stage/action/hold/close/drain choreography; ACK-resistant business postcondition verifiers; a default-off scoreboard production candidate that still
  advertises false; and a genuinely separate dual-cut media chain with per-cut receipts, reviewed-authoring promotion, Xiaoxiao cache prime, and
  institution-only picture reprises. All focused normal/optimized checks are GREEN; none of these increments are live or media completion.
- The dual-cut post-candidate materializer now removes audit/review/export path placeholders while retaining explicit human signoff and publication
  gates. Phase2 also has a narrow lineage-bound per-span checkpoint registry/restore interface with PID/generation and paused-state proof; only real
  seed/capture lineage can populate it. Both remain static/runner-ready, with media still zero.
- G2's frozen war-bound runner is unchanged and a no-launch verifier confirms it is ready only from a normal Desktop context. The next
  `evaluated_days` observation seam is exact slot-23 preview `0x2E87155`, with an explicit contract that preview does not expose evaluated days.
- Phase2's CP-to-P3 and promotion-to-compensation business chains now have real generated receipt ID/revision lineage plus static native providers.
  This triggered an `open_kaishek` CHANGE: writable-clone commit `81109e46` adds both schema-only capabilities and minimum `NOT`/trigger-scalar
  validation, with parser/validator GREEN and IR/runtime SKIPPED. Canonical push was blocked by read-only checkout/invalid credentials; an apply-checked
  patch is frozen at SHA `C6CE5DA5...D80A`. Certification remains false.
- Both requested Phase2 final-video cuts are now maintained as mandatory independent production lines, not alternates. Their combined planner through
  post-candidate materialization regression is GREEN in normal and optimized Python (`41/41` each). This is static production readiness only: clean
  product-live footage remains `0/8`, no TTS/render/export has occurred, and the promo-tool fresh-update gate remains immediately before actual media
  generation.
- Phase2's combined product/choreography/checkpoint/postcondition pass is GREEN (`225/225` in each Python mode); focused scoreboard/workforce is
  `39/39` in each mode. The full ZhongGuo static validator and release reproducibility check are GREEN for a 279-file projection (ZIP SHA-256
  `538ad2e6...4296d9e`). These are release-tree assurances and do not replace the pending normal-Desktop CK3 seed/capture run.
- The projects/metrics postcondition provider is now fully shared-wired in mailbox slot 24. Native adapter/mailbox/reader/bridge and Python
  provider/MCP/contract checks are GREEN; a split-worktree CTest path error was isolated to the missing relative CK3 EXE and does not represent a
  source-contract or compile failure. No CK3 live claim was made.
- G2 slot-23 truce-preview observer is fully shared-wired as a private, default-off diagnostic. ON/OFF bridge builds, observer ctest, Python
  integration (`205/205` in each mode), py_compile and diff-check are GREEN. Artifact index is
  `02FCF992AEBD7A2063BD913CDE0407D70689DA56C5D150D5FC58EAB5A3C11126`; it does not promote `evaluated_days` or live readiness.
- A clean local source freeze is available at
  `Z:\ck3_mod_rewrite\_local-freezes\phase2-dual-cut-g2-static-integrated-20260903-clean`, commit
  `165b47742fd05ff3713b8be4452711002328d57d`. It captures the static integration and docs without temporary build/process artifacts; canonical push
  remains unavailable because the main `.git` checkout is read-only.

### 2026-09-03 seed/live boundary follow-up

- Corrected seed no-launch preflight is GREEN and binds the clean freeze, source ZIP, exact CK3 build, and run-unique pipe; the seed contract remains
  `blocked_seed_generation_required` until a new real seed is captured.
- Direct sandbox execution stops at legal screen capture; the Default-desktop relay crosses the boundary but reproduces CK3 `C0000005` before loader.
  A minimal no-mod/no-bridge launch reproduces the same `ck3+0x1DABD89` crash, so the immediate blocker is the CK3 startup environment/binary,
  independent of Phase2 content. A four-guard plus read-only cold-map build advances the bridged run to `ck3+0x3BE33A9` (VFS error path), but does
  not make the base no-mod launch healthy. All process trees are cleaned and artifacts are preserved under `_runtime/phase2-seed-20260903/`.
- Promo tool fresh-update preparation is complete in writable clone
  `Z:\\ck3_mod_rewrite\\_runtime\\promo-tool-fresh-20260903`, with `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c` and clean
  detached worktree. No TTS/render/export has started because real footage is still `0/8`; both final cuts remain documented mandatory deliverables.

- A real offline Phase2 preflight against the synced open_kaishek clone (`81109e46`) confirms the boundary: clone build/CLI checks are GREEN, but the
  complete ZhongGuo corpus remains validator RED (`172255` `UNKNOWN_OPCODE`). The clone is therefore synchronized for development handoff, not full
  corpus-certified; no runtime opcode or live readiness is claimed.

- An additional run using the historical `production10` bridge reproduced the earlier `ck3+0x1DABD89` pre-loader crash. Only the newer locally
  compiled guard-on build advanced to the later `0x3BE33A9` VFS branch; neither build is promoted to live readiness.

### Continued static follow-up (latest turn)

- Promo-tool fresh fetch was re-run in the writable checkout and remained clean at `HEAD == origin/main ==
  57c42fca13ea459432c1caf76e069a1fbccf602c`; the dual-cut builder regression is `26/26` GREEN. Both final MP4 targets remain absent because
  the CK3 source intake is still `0/8`.
- G2 targeted slot-23/truce observer/evaluator tests are `28 passed` plus `4` subtests; this does not claim paused live readiness.
- Synced `open_kaishek` full Maven tests pass offline using the existing local dependency cache. Full ZhongGuo corpus validation remains RED with
  `172255 UNKNOWN_OPCODE`, so certification is still false.
- The shift-handoff document received a dated continuation addendum so the next operator has the dual-cut deliverables, tool commit, startup
  minidump evidence, G2 RTTI boundary, and open_kaishek certification state in one place.

### Steam-library startup discriminator (06:18)

- A no-mod/no-bridge launch through the original Steam-library CK3 executable (same EXE SHA-256 as the reference copy) reproduced the same
  pre-loader `C0000005 @ ck3+0x1DABD89` crash. The preserved exception/minidump is under
  `_runtime/phase2-seed-20260903/steam-library-bare-01/crashes/`; the evidence rules out a project-copy-only explanation.

### GDPR parameter discriminator (06:20)

- Replaying Steam's historical `-gdpr-compliant` argument with a correctly formed isolated userdir still reproduced the same pre-loader
  `C0000005 @ ck3+0x1DABD89` crash. The preserved artifact is under `_runtime/phase2-seed-20260903/gdpr-bare-02/crashes/`, ruling out missing
  GDPR consent arguments as the current cause.

### G2 residual RTTI offline recheck (06:23)

- The exact-build residual RTTI extractor was re-run against the Steam-library `1.19.0.6` EXE and `game/` tree without starting CK3. It returned
  `GREEN` and reproduced the unique index-7 path to the expected `CAddTruceEffect<0>`; output SHA-256 is
  `3A56A1ACBF49591C0787EADE412C2C8F23E49E253DAC00C4ADB7A7624B628DB3`. This remains offline evidence and still requires bounded live validation.

### cwd and minidump discriminator (06:26)

- Switching the bare launch cwd to the normal Steam game root did not change the result: `C0000005 @ ck3+0x1DABD89`. Minidump disassembly
  shows CK3 `.text+0x1DABD89` executing `movsxd rdi, dword ptr [r14+0x4c]` with an invalid small `r14`; artifact is under
  `_runtime/phase2-seed-20260903/rootcwd-bare-01/crashes/`. The evidence continues to point at early game initialization, not project code.

### Graphics backend discriminator (06:31)

- A final OpenGL-path bare launch (Steam-library EXE, game-root cwd, GDPR flag, nographics) reproduced the same
  `ck3+0x1DABD89` pre-loader crash. Startup-parameter probing is now closed pending a real external desktop/install change.

### open_kaishek corpus/parser split recheck (06:39)

- The synchronized clone parsed all 75 current ZhongGuo source files (`files=75, parsed=75, errors=0`), corpus SHA-256
  `28e681358558f5e975fae911bf5ddf54eacb3c95dac3133852eaaeca6425c284`.
- The profile-bound `preflight --root` remains parser GREEN but validator RED with `172255 UNKNOWN_OPCODE` (exit code 1). The result confirms
  syntax readability without full semantic compatibility; `certified=false` and the live/runtime boundary remain unchanged.
- Receipts are preserved under `_runtime/open-kaishek-zg361-corpus-latest.json` (SHA-256
  `23932F820036BC6879A2F12207F043BA85D96A2199D3A5497926EC540A29F765`) and `_runtime/open-kaishek-zg361-preflight-latest.json` (SHA-256
  `1632824AB6C18FC0258A644B4F814403ADD6C4A868308AE56E9AAF87D58D617B`).

### G2/open_kaishek continuation regression (06:48)

- G2 preview-entry/seam/contract focused regression passed `15` tests; exact-build residual RTTI remains `GREEN`, output SHA-256
  `3A56A1ACBF49591C0787EADE412C2C8F23E49E253DAC00C4ADB7A7624B628DB3`.
- Synchronized open_kaishek clone `81109e4` passed the full Maven suite offline. These are static/offline results only; live `evaluated_days` and full-corpus
  certification remain unresolved.

### Phase2 provider/choreography/static producer regression (06:50)

- Non-launch Phase2 business-postcondition, provider-packet, source-checkpoint, span-bridge, event-choreography, runner-plumbing, and authoring-promotion
  tests all passed (`12 + 7 + 4 + 3 + 4 + 8 + 15 + 2 + 1 + 3`). Promo-tool-dependent tests passed with `XAR_PROMO_SOURCE` bound to clean clone
  `57c42fca`; these are static tests only and do not promote synthetic runner output to CK3 live evidence.

### Steam `-applaunch` entry discriminator (06:44)

- A bounded Steam-client `-applaunch 1158310` attempt with the copied historical userdir, GDPR flag and nographics produced no CK3 child process or new crash artifact within
  20 seconds. It performed no Mod, purchase, or store action and yielded no usable footage; the startup boundary is unchanged pending an external-state change.

### Historical-userdir copy discriminator (06:42)

- A full copy of the known historical CK3 userdir, used without changing the original, still reproduced `C0000005 @ ck3+0x1DABD89` after about 1.16 seconds with the
  Steam-library EXE, GDPR flag, nographics and no Mod. The preserved crash artifact is under `_runtime/phase2-seed-20260903/userdir-cloned-history-01/crashes/`;
  empty-userdir initialization is not a sufficient explanation.

### Phase2 seed preflight with synchronized open_kaishek (06:51)

- The latest no-launch preflight bound clean freeze `165b47742fd05ff3713b8be4452711002328d57d`, source ZIP SHA-256
  `77aa3e30f1c20763576dbeea71b1c7451cfd63a15fb53a46fc58a373d72338e8`, guard-on bridge, and open_kaishek `81109e46`. It returned `GREEN / preflight-ready`;
  source/archive, dependencies, bridge, projection, and static checks passed, with `ck3_launch_attempted=false`.
- Seed generation remains blocked by the existing `blocked_seed_generation_required` contract; receipt SHA-256 is
  `9057F967CFE97036AD4E3918C2640892371EBB006BBADDE18EE36DA7A4CABE2E`. The open_kaishek full-corpus validator remains `172255 UNKNOWN_OPCODE`, so no live
  or certification claim is made.

### Dual-cut final static gate recheck (06:53)

- Promo builder and dual-cut completion gate re-ran against the freshly updated promo-tool clone: `31 passed, 4 subtests passed`.
- Promo tool is clean at `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`. Both required MP4 targets remain absent and each cut remains at `0/8` real CK3 footage; no TTS/render/export was attempted.

### Documentation commit handoff (06:55)

- Updated dual-cut documentation and progress reports remain in the integration worktree. The scoped commit attempt was blocked by managed worktree permission on `.git/worktrees/_root-promo-split-20260902/index.lock`, and push credentials were unavailable; no remote-publication claim is made.

### Release/static integrity recheck (07:02)

- Main static validation and deterministic release checks are GREEN: `validate_static.py` (40 option keys, 5 custom loc, 88 modifiers, 9 languages), `build_release.py --check` (86 files), and `build_vivhite_release.py --check` (27 files).
- White-Qi manifest/ZIP hashes are recorded as `17EE1D49AA292C26E32025FBEB87C52DB3A2C94C488D378EEC4D3F2DD22CC0F0` / `B5D7F276C128878AE3F6A7F28110840515F7CE585FC6BF2E3CB998D73B460C08`. This is source/release evidence, not CK3 live or video evidence.

### Current-tree ZhongGuo acceptance preflight (07:03)

- With the Steam-library exact-build executable, isolated user directory, workshop cache, and guard-on bridge/injector explicitly bound, `run_zhongguo_acceptance.py --preflight` returned `ZHONGGUO 361 ACCEPTANCE PREFLIGHT: GREEN`.
- The preflight did not launch CK3 and generated no footage; it confirms parameter-bound static acceptance only.

### Phase2 preflight with open_kaishek G2 capability commit (07:07)

- The bounded no-launch seed preflight now binds open_kaishek `981c79388a07e447b18f8e4472a16fd65e28c083` and returns `GREEN / preflight-ready`; `ck3_launch_attempted=false` and source/archive/static projection checks are GREEN.
- Receipt SHA-256: `4D22CA2B43ABF75D3D895BB26C2EE8183909E711337C495F9AE54A5A45948DDB` at `_runtime/phase2-seed-20260903/artifacts-preflight-open981-current-07/preflight.json`.
- Full-corpus validator remains `172255 UNKNOWN_OPCODE` with parser GREEN, so semantic certification and seed/live/video readiness remain unchanged.
- The writable open_kaishek clone is clean and two commits ahead of `origin/main`; the new G2 contract is local-only until credentials permit push.

### open_kaishek replayable handoff artifacts (07:10)

- The two local sync commits (`81109e4`, `981c793`) are preserved as an applyable patch at `artifacts/open-kaishek-sync/2026-09-03/0002-g2-truce-capability-sync.patch` (SHA-256 `24C6260B67DB2811AD4F278659690C27B99260D17A56BED1DA2929B5D32849CF`, 107,588 bytes) and as a Git bundle at `artifacts/open-kaishek-sync/2026-09-03/open-kaishek-main-981c793.bundle` (SHA-256 `30FCA309D8C9F4954DE1A7F101DD9CCE0FC0513D3C1080FDEBD02D5793115588`, 17,057 bytes). Remote publication remains unclaimed.

### G2 capability contract verification boundary (07:13)

- Full Maven test execution is not currently reproducible because the default cache denies access to a JUnit JAR and the copied offline cache lacks a Maven parent POM. The production compile succeeds.
- An independent Java smoke assertion over the compiled capability passed: `G2_CAPABILITY_ASSERTIONS_GREEN game.command.query-g2-truce-evaluated-days-v1`; required fields/invariants and the explicit uncertified read-only boundary were verified.

### Promo tool remote freshness recheck (07:16)

- `git fetch origin main --prune` completed in the writable fresh promo-tool clone; it is clean and remains `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`.
- The formal video pipeline was not started because real CK3 footage remains unavailable.

### Dual-video delivery index and intake boundary (07:20)

- Added `docs/phase2-promo/dual-video-delivery-status-2026-09-03.md` as the explicit two-output delivery index. Character-led and institution-led outputs are both retained as mandatory, independently reviewed deliverables; both are still footage `0/8` and have no MP4.
- A read-only intake of the old fixture returned typed `RED / footage_pending` (`ck3_started=false`, `ffmpeg_started=false`, `media_generated=false`), receipt SHA-256 `D34CF4070C0D474A06F29BA2C3A0707E4BA391D42D5C700992A6BE826D67A2A2`.
- Focused dual-cut builder/completion checks remain `31 passed, 4 subtests passed`; no placeholder media was generated.

### G2/open_kaishek capability binding (07:22)

- Added `docs/ck3-native-ai/g2-truce-open-kaishek-sync-2026-09-03.md`, binding the root G2 truce contract to open_kaishek capability `game.command.query-g2-truce-evaluated-days-v1` at `981c79388a07e447b18f8e4472a16fd65e28c083`.
- The compatibility record explicitly keeps native/runtime certification false until a paused exact-build evaluator artifact is captured.

### G2 focused regression (07:24)

- With the project-standard `PYTHONPATH=ck3_autonomous_player/src`, the bounded G2 truce/preview suite passed: `21 passed, 12 subtests passed` (one pytest cache ACL warning only).

### Phase2 business/choreography static regression (07:26)

- The current Phase2 business postcondition, event choreography, provider packet, source-checkpoint, and span-source bridge tests passed: `30 passed, 19 subtests passed`.
- This validates static contracts only; it does not promote the missing CK3 live capture or video gates.

### Project static validation (07:28)

- `tools/validate_static.py` passed: `LOC VALIDATION OK` and `STATIC VALIDATION OK` (generated parity, BOM/loc, mechanics/AI, release allowlist/assets).
- The tree remains static-ready; native/live and media gates are still unclosed.

### External dependency freshness recheck (07:30)

- Promo tool fetch succeeded and remains clean at `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`.
- open_kaishek fetch succeeded; local branch remains clean at `981c79388a07e447b18f8e4472a16fd65e28c083`, ahead of `origin/main == 0390b9a959fa1a59a968000ed49e827a03b8d4e4`.
- Neither dependency changed, so the frozen tool and capability receipts remain valid.

### Root G2/open_kaishek binding (07:31)

- Added descriptive capability/profile/commit constants to `raiktor_surrender_truce_contract.py`, matching open_kaishek `game.command.query-g2-truce-evaluated-days-v1` at `981c79388a07e447b18f8e4472a16fd65e28c083`.
- The focused root contract test passed: `6 passed, 12 subtests passed`; no readiness gate was promoted.

### G2 full focused regression after binding (07:33)

- Re-ran the complete truce/preview set after the root binding change: `22 passed, 12 subtests passed` (one known pytest cache ACL warning).

### G2 public bridge export (07:35)

- Exported the three frozen open_kaishek compatibility identities from `xar_autoplayer.bridge`; import smoke passed as `G2_OPEN_KAISHEK_PUBLIC_EXPORT_GREEN`.
- This remains descriptive and read-only; no native query, action, or readiness promotion was added.
### Correctly bound ZhongGuo acceptance preflight (07:36)

- Re-ran `run_zhongguo_acceptance.py --preflight` with the Steam-library exact-build executable and guard-on bridge/injector. Result: `ZHONGGUO 361 ACCEPTANCE PREFLIGHT: GREEN`, CK3 `1.19.0.6`, expected EXE SHA, desktop `2560x1440`.
- The previous missing-argument failure was an invocation issue and is superseded; this preflight still launches no CK3 and produces no footage.
### G2 generic war-bound successor seam regression (07:39)

- Ran the cold-map VFS observer, guarded retry, and normal-desktop preflight contracts: `6 passed` (one known pytest cache ACL warning).
- The observer remains `static-ready-no-launch`; the next admissible step is an A/B live capture only after the exclusive CK3 slot and a changed startup environment are available. No seventh guard was added.

### Cross-project acceptance coverage (07:40)

- Open_kaishek coverage, workshop acceptance, Phase2 seed runner, and promo-runner plumbing tests passed: `43 passed, 8 subtests passed`.
### G2 public export regression (07:42)

- Extended the root truce contract test to assert the `xar_autoplayer.bridge` public exports match the internal frozen binding; result `7 passed, 12 subtests passed` (one known pytest cache ACL warning).

### Dual-video production packet and runbooks (08:00)

- Added the dual-video production packet under `docs/phase2-promo/`, preserving two independent formal cuts: character-led and institution-led.
- Generated one runbook per cut under `_runtime/phase2-dual-runbooks-20260903-0800/`; both are typed `RED / footage_pending`, with no media side effects.
- Authoring claims validate GREEN for both cuts, and the Phase2 completion/static checks remain GREEN. The live CK3 startup blocker still prevents the required 8/8 real clean spans, so neither MP4 is yet exportable.

### G2 activity schema boundary (08:08)

- Documented the current open_kaishek activity-type validator RED: parser GREEN, 172,255 unknown-opcode diagnostics concentrated in `zg361_jingcha.txt`.
- Kept the profile fail-closed pending exact-build schema/evaluator evidence; no unsupported activity semantics were inferred.

### Dual-cut builder optimized regression (08:16)

- Phase2 completion/plumbing tests passed under `-O` (`5` and `15` tests).
- With the fresh promo-tool clone explicitly bound, the dual-cut builder passed all `26` tests under `-O`; no CK3, TTS, FFmpeg, export, or publish process was started.

### Current G2/static/release recheck (08:28)

- G2 truce/preview/scoreboard focused tests passed (`16 passed, 12 subtests passed`).
- Static validation and deterministic release check remained GREEN (`86 files`). Promo-tool fetch confirmed `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`; no target MP4 appeared.

### Continued-state audit (08:40)

- CK3 exact build identity and SHA remain unchanged; no CK3/bridge process or new Phase2 media artifact is present.
- The live capture gate remains the next admissible media action; no repeated blind launch was performed without a changed startup environment.

### Independent release regression (08:47)

- White-Qi static validation and deterministic release check passed (`27` files); manifest/ZIP hashes were recorded in the daily report.

### CK3 `-noWorkshop` startup A/B probe (09:02)

- An isolated no-bridge/no-mod CK3 launch with `-noWorkshop` reproduced the same pre-loader `C0000005` at `ck3+0x1DABD89`; no seed or footage was produced.
- The hypothesis is recorded and closed without adding another startup guard; another attempt requires a genuinely changed environment.

### CK3 `--userdir=` argument-form recheck (09:08)

- The equal-sign userdir form reproduced the same pre-loader crash, so the previous result was not caused by short-form argument parsing. No additional guard was added.

### G2 focused contract evidence (08:32)

- Added [`g2-focused-test-evidence-2026-09-03.md`](../../ck3-native-ai/g2-focused-test-evidence-2026-09-03.md) as a reviewable static G2 deliverable. It binds the root truce wire and descriptive `open_kaishek` profile to the exact-build preview observer while keeping the observer private, default-off, read-only, and absent from public gameplay capabilities.
- Focused regression: `18 passed, 12 subtests passed in 14.44s`; no CK3 launch, native install, media process, or guessed activity schema was involved.
- Weekly readiness is unchanged at `static-ready-no-launch`; `evaluated_days` native/runtime certification and persisted expiry remain pending a paused exact-build artifact with two stable same-frame evaluator reads. This is the next G2 value-bearing milestone.

### open_kaishek sync and remote push (08:35)

- The two G2 compatibility commits were pushed to `open_kaishek` `origin/main` at `981c79388a07e447b18f8e4472a16fd65e28c083`.
- Isolated Maven verification passed: `125 tests, 0 failures, 0 errors, 0 skipped`; refreshed patch and bundle SHA receipts are in `artifacts/open-kaishek-sync/2026-09-03/`.
- No unsupported CK3 activity semantics were inferred; native/runtime readiness remains `static-ready-no-launch` pending paused exact-build evaluator evidence.

### Steam `-applaunch` entry probe (09:20)

- A distinct `steam.exe -applaunch 1158310` entry was monitored for 35 seconds; no CK3/dowser child and no new crash artifact appeared.
- This remains a no-launch harness result with no loader/database readiness or footage; no store or payment action was taken.

### Phase2 business/content regression (09:25)

- The local business-postcondition, event-choreography, and provider-packet suites passed `23` tests in total (`12 + 4 + 7`).
- This strengthens static/fixture evidence only; it does not certify CK3 native/live execution or either final MP4.

### Final focused G2/Phase2 regression (09:40)

- Latest root truce, exact-build preview observer, scoreboard batch, Phase2 completion, and planner tests passed `34` tests with `12` subtests.
- The post-push tree is consistent; native paused evidence and both final MP4s remain pending the CK3 startup gate.

## 2026-09-03 B1 effect 拆分实机增量（最新）

在同一 full-entry probe 口径下，B1 effect 的 disposable 诊断投影取得以下结果：

| 候选 | 实机结果 | 耗时 |
|---|---|---:|
| all-stub | full-entry **GREEN** | 255.113 s |
| left-real | full-entry **GREEN** | 180.403 s |
| right-real | full-entry **GREEN** | 178.968 s |
| event-root closure | full-entry **GREEN** | 181.360 s |
| closure + excluded-A | full-entry **GREEN** | 193.588 s |
| closure + excluded-B | full-entry **GREEN** | 184.817 s |
| all-but-76（77 个真 block 中保留 76 个） | full-entry **GREEN** | 171.228 s |
| balanced-files（无 stub） | full-entry **GREEN** | 180.396 s |

`balanced-files` 保留全部 77 个定义，逐 block 正文字节与原始 effect 一致，仅将其拆成两个 effect 文件；候选共 59 files / 7,858,264 B，两份 effect 分别为 255,134 B 与 240,709 B。这使“拆文件”成为可实施的下一候选，但不把因果写死：未改写的 58-file、单 effect full B1 仍只有一次 **1205.343 s RED**，所有真子集和拆分候选 GREEN 不等于原始 full B1 已经 GREEN，也不足以唯一证明根因。

本轮只提升 B1 启动归因精度，未提升产品/媒体 readiness。Phase2 真实 footage 仍为 `0/8`，两条目标 MP4 均未生成；G2 已按用户要求暂停，paused exact-build evaluator 双读仍未取得。本轮 `open_kaishek` 仅是单个 effect 文件的离线 parser smoke：含真实正文的候选仍有 validator `UNKNOWN_OPCODE`，IR/runtime 均 `SKIPPED`，不能替代 CK3 语义或 native/runtime 认证。

### 正式 generator split 与 r3 live

- B1 正式生成器现产出 **59 files / 7,858,254 B** 的双 effect production candidate，两个文件分别承载 `41 + 36` 个定义；按定义顺序重组后与原始单文件正文 **exact**。formal tree SHA-256 为 `9EED00504E1AAF34F352B440CFB4DFEBF3BB1206966457834727A81BAB4FC50A`。
- r1/r2 都在约 `0.3 s` 因 probe game-path 配置错误结束，CK3 未启动；它们保留为 harness/config RED，不计作候选内容或 CK3 live RED。
- 修正路径后的 r3 在 **245.770 s** 完成 full-entry GREEN：8/8 entry gates、3 个 game-state markers、material error `0`、cleanup GREEN。

因此 B1 checkpoint 可以晋级为 **startup/full-entry production-candidate GREEN**。该结论只覆盖正式生成器拆分候选的启动与暂停地图入口；delayed-path、seed、生产 OODA 与 footage 尚未完成，真实素材仍为 `0/8`，两条 MP4 未生成。原始未拆分单文件 full B1 的唯一 `1205.343 s` RED 继续保留，现有证据支持采用拆分实现，但仍不把根因写成已被唯一证明；G2 继续 paused。

正式拆分、诊断工具、回归与证据已在 `4aea7afe3f6d3f4f82f7551869aba8faeae0c66b` 提交；以非强推 merge
`89808bb5799d651847b14460e38c4369e94209f7` 吸收远端重复 fan-out 文档历史后，已推送至
`origin/integration/progress-20260902`。所有者随后明确：B2 起 effect 按用途/调用链拆分，目标每文件 `1–10` 个、原则上不超过
`20` 个；超过 `20` 必须记录理由和实机证据。加载性能 RED 的文件边界 A/B 取证规则已写入 `AGENTS.md` 与
`docs/testing-workflow.md`。

### 2026-09-04 B2 用途分片

- 旧 B2 `253,920 B / 152-effect` 单体已由 generator 替换为 25 个用途分片；152/152 顶层正文逐 block 字节一致，每片
  `1–9` 个、合计 `250,551 B`，无超过 10/20 的例外。分片清单 SHA-256 为
  `06274A5E0D89EF97C19EF3C099E8AEF946C4153BC78C065EC260806D27F67FAB`。
- CI 同口径 Python 1363/1363、B2/manager 聚焦 142/142、`validate_local.py`、native source-contract 1/1、projection 8/8、
  release tests 7/7 与可复现构建均 GREEN。release 为 304 files，manifest `DF1741C3…02D0`、ZIP `2C18050A…1884`；
  broad/release 均包含 25 片且不含旧单体。
- 该 B2 分片工作包收口时 readiness 仅为文件布局 `static-ready`；当时生产可达链仍触及 Workforce endgame 的
  `4,636,271 B / 324-effect` 单体。后续 owner 分片与组合 r2 live 见下文。
- 上述 B2 分片、消费者迁移、测试与证据已由提交 `e1297f83738fd61d53812406b26e23637201d2c5` 推送至
  `origin/integration/progress-20260902`；提交后本地与远端为 `0/0`。

### 2026-09-04 Workforce effect 用途分片

- Workforce 旧 `4,636,271 B / 324-effect` 单体已由 generator 替换为 76 个用途分片；324/324 顶层 block 逐字节一致，
  每片 `1–10` 个且无超过 20 的例外，总计 `4,635,596 B`，清单 SHA-256 为
  `E5DD22CEF71D60E069884A27BE924234B4FAD42490AEF2B52B966AFD95585858`。
- B2 所需的 40 个 Workforce effect 是 16 个完整分片的精确并集（341,602 B，extra=0、missing=0）。分片门禁还会拒绝旧单体、
  stale shard 和 B2/非 B2 混片；历史 bisect 工具改从冻结 aggregate renderer 取源，不把新片拼接冒充旧全局顺序。
- 静态结果：Workforce 主测试 113/113、外围 61/61、bisect 5/5、`test_zg361*` 1262/1262、`test_gen_361*` 107/107、
  projection 8/8、release 8/8、`validate_local.py` 与可复现 release 均 GREEN。release 为 379 files，manifest
  `11E86FA8…8D2E`、ZIP `DF34A91E…C3BC`。该 owner 分片工作包独立收口时仍只是 static-ready；后续已纳入组合 r2 live。
- B2 三 root 的真实固定点为 71 effects / 28 events；旧 68/24 漏扫 `EVENT = <id>` 参数 ABI。
- 本工作包已由提交 `76fbf436a023f7d022f3eab6581e43fc632be3d0` 推送至
  `origin/integration/progress-20260902`；实现提交后本地与远端同步。

### 2026-09-04 Workforce event 用途分片

- Workforce 旧 event 单体为 `168,729 B / 149 unique events`，SHA-256
  `637F65CC72C176E6E19BE982F41B203DC326047939B79A80E5E43D3A9D361EF7`；generator 现将它替换为 35 个用途分片，
  合计 `175,403 B`，单片最小 `349 B`、最大 `16,842 B`。
- 每片 `1–7` 个 event，数量分布为 `{1:2, 2:7, 3:5, 4:4, 5:4, 6:9, 7:4}`，没有超过 20 的例外；
  manifest SHA-256 为 `1E1EEE665105139653BC3D00B092522A41C97FE16164E27ACBDB6FC0C967F8DA`。
- B2 所需 19 个 Workforce event 是 7 个完整分片的精确并集，extra=0、missing=0；旧 event 单体对约 210 个 Workforce
  effect 的引用不再迫使 B2 候选挂载几乎整个图。
- generator `--check` 当前覆盖 120 个输出；Workforce 主测试 116/116、`test_zg361*` 1265/1265、`test_gen_361*` 107/107、
  visual/projection 8/8、release 9/9 与 `validate_local.py` 均 GREEN。可复现 release `--check` 为 413 files，manifest
  `E68E89F3…60B4`、ZIP `4A7C7995…1E67`；该 owner 分片工作包独立收口时只记 `static-ready`，后续已纳入组合 r2 live。
- 该分片工作包结束时的下一项是物化 119-file、无 stub 的 B2 production closure；该项及 full-entry 已由下节 r2 关闭。
- 本工作包已由提交 `0865589cb7ef37160e50247f0fdeb1f27a03fe54` 推送至
  `origin/integration/progress-20260902`；实现提交后本地与远端同步。

### 2026-09-04 fresh B2 no-stub closure r2

- fresh r2 位于 `_runtime/phase2-b2-production-closure-20260904-r2`，在冻结 B1 的 59 文件上叠加 60 个精确 overlay 文件，
  共 119 files / 8,891,635 B；静态物化与重放先为 `GREEN_STATIC`，随后 exact 同树 full-entry GREEN。
- B2 本体为 25 个 effect 分片 / 152 effects，单片 `1–9`；Workforce 依赖精确选择 16 个完整 effect 分片 / 40 effects
  与 7 个完整 event 分片 / 19 events。三 root 固定点为 71 effects / 28 events，selected events 总计 51。
- 候选没有 legacy monolith、stub、duplicate、missing callable 或 missing event。文件边界门只覆盖 B2 起的 60-file overlay：
  44 个 effect 文件 / 218 definitions，最大为 probation owner 的 15，且只有它超过 10；overlay 中超过 20 为 0。
  119-file 整树继承冻结 B1 的 5 个 grandfathered 超 20 effect 文件：B1 part1=41、part2=36、case_kernel=229、
  `zg361_effects`=26、`generated_mechanism`=1449；它们不是本轮新增，且已有 B1 full-entry 证据。
- 身份哈希：source tree `F3B36DFDBE74827FF373B06C7C621D1EC72AA15E575F5ABBF1186E636C625184`；formal tree
  `3DD3DA79F11EC892DF72024E25EE985ACAB26E21FD4C1281C35E5DEB0642C4D3`；file-list
  `6C3FE72E3FBB3E1AB6543BEE9226F24203D1063951999AAF6BD7E9053B7533FF`；projection
  `2C8F0979113F75866191C3AE10C699F3801D83AEF91ADCFD9C4B526B469EAE28`；contract
  `F5BC105C1C3EF8E55E82053F61D3B453E6B92A7E0AE224EDECBD072B6EF49180`。
- r1 只保留为被 r2 取代的静态 artifact。exact r2 live artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-b2-no-stub-full-entry-20260904-r1`；164.781 秒通过 8 个入口门、3 条 marker、
  exact mount、material errors 0 与 `cleanup.ck3_running_after=false`。report SHA 为
  `4DAAD3649E4FC37373EF2B95F9DE24FB15D23E1319403F37A89228E4B44674F1`，地图截图 SHA 为
  `F867A24AE1267BA032BC8CDB2EE623A966FF8E7ED1AE9ED78251B115958F875C`。本轮没有加载性能 RED，故不做 A/B。
- B2 readiness 现为 `startup/full-entry production-candidate GREEN`。该时点曾计划复用 same exact tree 进入
  seed/paused-native；随后 no-launch seed preflight 证明 fixed fixture 还要求 Incident 与 Workforce 两个入口，
  因而不能直接用该 tree 启动 seed。

### 2026-09-04 Incident/X 用途分片与 r3 live

- fixed seed no-launch preflight 的基础设施检查 GREEN，但 `seed_ready=false / ck3_launch_attempted=false`；回执为
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-b2-seed-20260904-r1\artifacts-preflight-02\preflight.json`，SHA-256
  `2F97F23FE474619342B10A01FD22153B8E5F982B3972A03CCD462CDAA8DB6E81`。fixture 还会调用
  `zg361_ip_open_x_case_effect` 与 `zg361_we_open_portfolio_effect`，所以 preflight GREEN 不等于 seed-ready。
- Incident 旧 aggregate effect 为 `700,085 B / 124 effects`，旧 aggregate event 为 `8,573 B / 54 events`；generator
  现按用途输出 27 个 effect 分片和 12 个 event 分片。124/124 与 54/54 顶层 block 逐字节一致，历史 aggregate SHA-256 为
  `0C228FAABB5F6B7DCDABFD32A071CA82F0C1EF15C08AB0BD07C25AF3A467DACD` /
  `49577D5475FBF6B22006021F4FADED2A248C7E193840A283FB043F7AAAC0E091`。所有分片最多 7 个定义，`>10=0 / >20=0`。
- X production 增量精确选择 11 个 effect 分片 / 47 个 Incident effects 与 4 个 event 分片 / 20 events；连同冻结
  case-kernel 后 fixed point 为 `60 effects / 20 events / 6 triggers`，没有 Y/Z 定义夹带。16-file overlay 为 266,807 B。
- exact 候选位于 `_runtime/phase2-incident-x-production-closure-20260904-r3`，共 135 files / 9,158,442 B；
  source/formal/file-list SHA-256 为
  `ACC693206AAA03AEEDFB829911778275ED4D4F2EEF31CF4DCE6DE7D46F34BF3E` /
  `AF8F0DECE9477FDD60B6C96D0E09A27BFC9E55CEED40A9804B70ACB986D57A2D` /
  `045F56D78FB036CDF69225912F9B046673FCD528E99A69571363133A65EA2262`；projection/contract SHA-256 为
  `CF379F38DA8C471DDCCDB6522D7E0864098596737862B5E2A703AECA94E40CB7` /
  `ED4B5E82430187B1C79E2AEFE29748C5D51749782CC340644DB434C14A0B468A`。
- live r1 在 `0.338 s` 因默认 worktree 游戏目录不存在而 harness RED；CK3 未启动，report SHA-256
  `1CF7A2B014D8BBD9F39C6D1423B2E39BB5B7BCDBA21D1505CD4CECB7F5DFDC51`。r2 显式设置 `XAR_CK3_EXE` /
  `XAR_CK3_GAME_DIR` 后，在 exact CK3 `1.19.0.6`、EXE SHA-256
  `2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86` 上以 188.303 秒完成 full-entry GREEN：
  8/8 gates、3/3 markers、exact mount、material errors 0、`cleanup.ck3_running_after=false`。artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-incident-x-full-entry-20260904-r2`；report/map SHA-256 为
  `1D41298B8987AA473304AE70FC53628639DE7700BBE5A9D7484A6BF76F566FE2` /
  `F75097D9C20B610F81CA60837DF879865E26866F65AC76E7D40C1DF300C34B2A`。没有加载性能 RED，故未做追加 A/B。
- 用途分片、消费者迁移与静态合同已由 `df77ed636c51c51f99f534d8efbb559b94c639d2` 提交并推送。Incident/X
  readiness 现为 `startup/full-entry production-candidate GREEN`，但 delayed-path、KPI consumer、paused native 与 seed
  仍 pending。该增量关闭了 fixture 的 Incident root。
- 早先 callable/event-only 扫描的五-root `2034 effects / 578 events` 与 Workforce root `385 effects / 161 events` 漏掉
  appointment + exit court-position definition 的 `on_court_position_*` native callbacks，现已被 supersede。callbacks 对
  五-root union 净增 `8 effects / 2 events`；单独遍历 Workforce root 时还会从 role-failure 继续进入 probation publish，
  另拉入 `4 effects` 与 `zg361wpf.1`。`zg361_we_open_portfolio_effect` 的完整闭包为
  `397 effects / 164 events / 6 triggers / 0 values / 2 court-position definitions`；五个 product roots 在 candidate 内的完整
  materialized closure 为 `2042 effects / 580 events / 6 triggers / 0 values / 2 court-position definitions`。Workforce root 与
  Incident/X r3 provider 的交集为 `83 effects / 22 events / 6 triggers`，故相对 r3 的增量仍为
  `314 effects / 142 events / 0 triggers / 0 values / 2 court-position definitions`，新增 loc 为 `28 keys / 5 files`。
  Manager 43-effect owner 增量仍为 0。最终 overlay/candidate 文件数待 renderer 与 mixed-owner 分片稳定后由 builder 生成。
- 下一项是只按用途物化 `zg361_we_open_portfolio_effect` 所需 Workforce owners、appointment/exit court-position owners 与
  native callbacks；对 seed-capable exact candidate 跑 full-entry，GREEN 后才进入 seed/paused-native。

### 2026-09-04 seed-entry 根因、r8 修复与 r9 canonical seed

- 上节的 Workforce closure 缺口已关闭。最终 production product 为 `249 files / 12,098,441 B`；B2+ 新增层是
  72 个用途 effect 文件 / 314 definitions，单片最大 10，`>10=0 / >20=0`。旧 B1 41-effect hotfix 作为 inherited
  例外单列并绑定 r8 实机证据，不计入新增层门禁。
- seed-entry split-only r2/r3 仍分别在 `302.912s / 303.181s` 主菜单前 RED。调用图审计定位
  `zg361_workforce_appointment_fact_seal_and_publish_effect` 对自身的直接调用；只删除该 self-call 的同框架候选在
  `180.349s` GREEN，generator 修复后的 r4/r5 又在 `167.277s / 174.305s` GREEN。因此本次启动故障的已定位原因是
  direct same-effect recursion；文件体量不是必要或充分原因。用途拆分仍作为 B2+ 闭包、维护与性能 A/B 规范保留。
- r8 首次进入 seed 业务后产生 69 条 optional previous-band unset 错误（23 subjects × 3 fields），证明同级 `AND`
  不能用作 lazy existence guard。generator 已改用外层 `trigger_if.limit`；r9 中这些错误为 0。loader/material/fixture/B1
  guard 修复由 `d473aff`、`d4c7143`、`c32fb6c`、`e332c3b` 提交并推送。
- r9 在 exact CK3 `1.19.0.6` 上完成 `GREEN / paused_seed_ready`：loader `48.19s`，runner `161.686s`，五个 typed
  selectors、paused map、checkpoint、source/product/fixture immutability 与 cleanup 全部通过。artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-seed-r9-20260904-074005\artifacts-live`；runner report SHA-256
  `32186480AAAEFB2F3F10D8606BD6D44E4FFF6DB8A82A7C34D8121B9594D43BA5`，checkpoint SHA-256
  `BFC73FD9E7E80145CDF39AABC66BC2D731881122ADAB0CC0BA675FA07D1E6733`。
- readiness 只从 seed blocked 提升为 canonical paused-seed ready；provider 仍是
  `blocked_provider_matrix_captured`。AI-owned case ready，Incident X ready 但 N/A；B2 PIP、Incident Y/Z、Workforce
  尚缺业务上下文。下一项是 product-only loaded-seed managed provider 与首个可见 OODA，不重复 startup/seed。
  Phase2 footage 仍为 `0/8`，两条 MP4 尚未生成；G2 继续 owner-deferred/paused。

### 2026-09-04 B2 first-use guards、r8/r14 与加载取证增量

- B2 本体继续保持 **25 个用途 effect 文件 / 152 effects / 单文件最大 9**，没有超过 20 的新例外。first-use guard
  修复后的 r8 product 为 **252 files / 12,104,708 bytes**，product tree SHA-256
  `C2E3DEEC48DC31294414FBF140EAF2D0603F3F4B6A5F34AF9C3EC9BBCEBB42CD`。
- r10 已完成 loader readiness、bridge 绑定与 history load，随后第一次结果链触发 93 条 `parser_or_script` 错误。57 条来自
  尚未建立的 `*_policy_debt_active`，其余来自 result freeze/business-object 路径读取首次不存在的 remand、metric、PIP 与
  `object_active` 等可选变量。这是 **product first-use runtime RED**，不是 loader stall。错误 scan SHA-256 为
  `0887BD8249AE5A54108EB557B5371160BD33C13DA37EED4264DB5DA7A7C07A3E`，artifact 位于
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-b2-r10-20260904-102620\artifacts-seed-live`。
- `05e1410bf21b6efdab1492a5919c76a047f2934f` 已在 generator 中加入 first-use `has_variable` 门，并让缺失 active 按 inactive
  处理；修复没有改变 25/152/≤10 文件边界。后续 r14 对 r8 product 的 loader error scan 为 GREEN、`matches=[]`、quiet window
  `16.247 s`；scan SHA-256 为 `EB517B003D216EB17D2C272F4E623E00ACE27CA1D3CE4CA7A2A7598AA5C449F1`。
- r14 seed artifact 位于
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-b2-r14-20260904-111017`。candidate report GREEN / SHA-256
  `60D9E94CE08B6EBCF1C60DCA420D7C1AFF13A8B498E1C61F4AC2A4795F150F19`，runner report SHA-256
  `241A521111CAB9F1FDF11847E49A88BDC4FD063BE1489225C47E37F5423BFF1F`，新 checkpoint 为 57,377,533 B / SHA-256
  `B019F9D50958D9566961E4A5EF7A414FF8A5F491F198AD9C0AE993D56532D1C2`。该结果提升 seed/首用加载证据，不提升 focused
  B2 四路业务或 footage。
- 同一 source save `BFC73FD9…E6733` 在 r11/r13 两个新 CK3 进程中，对同一 `zg361.4` 分别报告
  `calculated_event_id=3030004/2990004`；稳定的 event key、instance、date、root/superior 与 options 仍一致。因此 computed ID
  只作进程内观测，提交 `1ae57f5609c9d2373646fb167dbceb571054919d` 已从跨进程 predecessor gate 移除该等值要求。
- 后续轮次使用 `_runtime/formal-ab-current-buildoff-20260903/profile/shadercache` 的 lightweight cache：
  **4,960 files / 216,650,070 bytes**。cache 复制、哈希与 isolated userdir 建立记作 prelaunch preparation，不能计入 CK3
  loader。只有进程启动后的 loader-stage RED 且没有更早 parser/material/call-graph 首错，才触发同条件文件边界 A/B。
- 截至 r14，focused B2 same-checkpoint 状态仍为 **PENDING**；该时点占位先由 r21 分层回执 supersede，最终已由下文 r22
  GREEN 关闭。完整证据页为
  [`../../phase2-promo/b2-first-use-loader-and-seed-evidence-2026-09-04.md`](../../phase2-promo/b2-first-use-loader-and-seed-evidence-2026-09-04.md)。

### 2026-09-04 r20 seed、r21 focused 分层回执与 B3 文件边界

- r20 seed 位于
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-b2-r20-20260904-121400\artifacts-live`，结果 GREEN。candidate contract
  SHA-256 为 `FD055093617AA78858BB47F6F9F2BE4AA2E1B66ED4CABE4983B5418C6C99B7E7`；checkpoint 为
  `57,377,533 B` / SHA-256 `96D1919D569E6F3EA115BF21882B0F4372246812B1E1F630F3AED44968D49335`；稳定身份
  date/player/B2 owner 为 `53147016 / 29037 / 32904`。
- r20 loader scan GREEN，SHA-256 `BC6C3A21EAED4A1957AB87BED187B6D6302ADD556C508D07AE1A681DB4158D9B`；完整日志
  SHA-256 `B49857293AA260148EF19431A8481A50F5FDC6D9181BA8F4CB7A45BC593A0092`，quiet `16.232 s`、0 matches，
  invalid-left / fetch / unset / m016 均为 0；cleanup、source 与 runtime tree immutable。
- r21 位于
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-b2-r21-20260904-122000\focused-live`。内部 scenario GREEN、A/B/C
  complete，readiness 为 `production-live primitive`；same-checkpoint matrix GREEN / SHA-256
  `22F8C8D07DA573457F1EBD67E70EF4CED1ACE8594210AAA562F0C02859EE49C7`，4 次 restart / 5 个 PID，cleanup、driver、locks
  全部 GREEN。
- r21 outer report 仍为 RED / SHA-256
  `6C03CFFE91EB4BCC829F3C6B74D22B1312886161F184100DA7B395C1250ED237`，唯一原因是 generic diagnostics 将初始 loader log
  已有的 5,607 条静态 liveness 行判为 blocking：5,264 条 `jomini_effect.cpp:1145` set-never-used、338 条
  `jomini_effect.cpp:1161` variable used-never-set、4 条同位置 list used-never-set、1 条 orphaned event `zg361we.361`。
  同轮 loader scan GREEN，故不是 runtime emergence 或 loader performance RED，而是 harness classification inconsistency。
  最小分类修复 `e1c020ad7ad1a8ad4cb9f2ef9676e6517fa9b906` 已落盘；r21 的失败证据继续保留，随后由 r22 完成外层复验。
- B2 当前边界是 **25 个用途 effect 文件 / 152 effects / 单片最大 9**；B3 manager 新增层由
  `4890b17998df1c5586beb36011d283c1a111f388` 拆为 **7 个用途 effect 分片 / 43 effects / 单片最大 10**。二者都没有
  超过 20 的例外，继续满足所有者的 B2+ 文件门。

### 2026-09-04 r22 B2 focused COMPLETE

- r22 artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-b2-r22-20260904-123400\focused-live`，冻结 commit
  `e1c020ad7ad1a8ad4cb9f2ef9676e6517fa9b906`；source ZIP 包含 2,241 files，SHA-256
  `7F5DD7977B6FC7F264275D8C440847F509B8C432473E49D6D74A09FE1E115D93`。
- outer、cell、scenario、matrix 全 GREEN；focused `complete=true`，readiness 为 `production-live primitive`，明确
  `full Phase2 claim=false`。总时长 `495.356 s`；three arms / four exact restores / final baseline、4 restarts / 5 unique PIDs
  全部通过，所有 PID 已退出；cleanup、driver、locks 与 source/product/runtime immutability 全为 true。
- outer/cell/scenario/matrix/cleanup SHA-256 依次为
  `6FC744BA4C5D6BA905A41A0E91EF870452A378DA3431DCBFB537C31AA3533F47` /
  `78BD148ED20BA1BF0D3AF9866BB9D02E7BA4F6060B4D764C658C298D55EE641E` /
  `BD8EBE01C3E228801301FCD3A77B95549A8AC214A6C90EDBB1EA7FE4ADFF9278` /
  `83BB9741E3EBAB28D90787A4FB6F94937ADE5A1D950D2601E18EC9AC4F64E6EC` /
  `875A897E651A49B7A4DC2A42EEFB7DDCAC1E98D05CDBB8BDD0426046F0BD5745`。
- `project_diagnostics=[]`，但 r21 的 5,607 条静态 liveness 行仍原样留作 nonblocking evidence（5,264 / 338 / 4 / 1），
  没有通过删除证据取得 GREEN。loader scan GREEN / 0 matches / quiet `16.226 s`；完整日志 `1,376,819 B` / SHA-256
  `5C55B0C0C188CBE15AB28EF9C7AD88929EEBA34B0C3497BBD93C22D82710C803`，scan SHA-256
  `E6B59CEC859572B9B925FE5759A4F6460190EBAC9369F6172FB091BFF3B7B87D`。
- 结论是 **B2 focused gate COMPLETE**，不是全 Phase2/T0 完成。本轮没有 loader performance RED，不触发额外拆分；下一主线为
  B3 typed selector/provider，并继续保持 7 shards / 43 effects / max 10、无 >20 例外。

### 2026-09-04 13:30 滚动增量：B3 静态接线与剩余业务单元

- B2 起 effect 文件边界现有自动门禁：427 files / 3,718 effects，非遗留最大 10，`>20=0`。probation、case、
  Career HC、learning、Phase3、feedback/PIP、compensation、credit/project、incident 与 Workforce 的用途分片均已合并；
  拆分继续作为维护、闭包与性能 A/B 制度，不替代 direct same-effect recursion 的既有根因证据。
- B3 exact-build manager/subordinate selector 已由 `fefb408` 合入公开 bridge/service/MCP 与正式 runner 默认 provider；资格与
  缺失分支均 typed，调用方不能注入对象。静态矩阵 GREEN，但 fresh paired binary、cumulative projection 与 paused live
  尚待，因此 readiness 只到 `static-ready / live-pending`。
- promotion-compensation、HC-workforce、career-HC、projects-metrics、incidents-operations 与 cross-cycle-endgame 均已有
  独立 action/postcondition preflight，全部拒绝 ACK-only。Incident 的 owner 合同冲突与 endgame 的 owner→subject 双视角问题
  已进入显式缺口账本，不能用同一 checkpoint 或 generic rebind 冒充。
- 宣传四 source event 分片后仍唯一；严格 registry CLI 已可从四份真实 capture manifest 组装 canonical registry。真实素材仍
  0/8、双片 0/2，媒体阶段不会在缺少 provider/UI receipts 时启动。
- G2 leaf-context v2 canonical live r1 到达 paused native snapshot，但 mailbox heartbeat 300 秒不可用，动作未提交、private
  capture 未生成；cleanup 与 source immutability GREEN。它是 bridge readiness RED，不是旧访问冲突重现，下一轮先修具体
  heartbeat 配置再做新的、非重复 live。
- B3 的公开 manager selector 已触发 open_kaishek 同步检查；T2 是否需要代码变化以实际 schema/pin 差异为准。

### 2026-09-04 13:45 滚动增量：G2 heartbeat 修复与 T2 GREEN

- G2 r1 的具体 harness 根因是 leaf preview heartbeat JSON 多一个 `}`，已由 `81cf89b` 修复并补 normal/`-O`
  回归。它解释 snapshot 可读而 heartbeat 始终为空，不属于 loader performance 或 effect 文件边界问题；fresh live 尚待重冻后执行。
- scoreboard 真实双 surface checkpoint/provider 已由 `13d78c6` 合入，合并态测试 GREEN，但没有真实
  managed-capable/received-only checkpoint 前保持 live-pending。
- Incident registry 已按真实 received-self 语义改为 `player=subject` 且 distinct owner，相同 owner/player 明确 typed RED。
- open_kaishek `f4ce25a` 与父仓 pin `48da012` 已完成 B3 API/ABI 同步，compatibility verifier 全字段
  `GREEN_STATIC`；未提升 native/runtime/live readiness。
- 主 mod 的五个 DDS stale 在 fresh `48da012` worktree 和 canonical root 均无法复现；全局 static 与 86-file
  reproducible release 当前 GREEN，故没有无依据重写生成素材。

### 2026-09-04 14:42 滚动增量：G2 private-live、B3 双 material RED 与后续静态接线

- G2 leaf-context v2 r2 在
  `Z:\ck3_mod_rewrite_process_assets\zg361\g2-hbfix-b71b73c\live-leaf-context-dual-query-r2`
  完成 **private-live GREEN**，用时 `148.209 s`。runner report SHA-256
  `00343F7E1186DF65A13A7F85806BB048A8D28C67DF8F451BF559D21B800427D9`，private JSONL SHA-256
  `9F0EF7D9E045AE071B05C6F7DDE4166AE21BA12D98B3CA7DA19A8FE16A35B677`，analysis SHA-256
  `D2A6DC1260FD32ECEE2B48B224F5461BA8EC4A52E9C5C81BF533913C51068A2D`。8 rows / 2 groups 中，两个 public
  terms query 对应的 private evaluator `evaluated_days` 都是 1825，paused same-frame；source checkpoint、driver state 与
  cleanup 均 GREEN。该轮没有 mutation、时间推进或停战动作；当轮 public wire/readiness、expiry、自动投降与 GEN-034 都仍为
  false。精简回执由 `55b09317f001d97ac3a8aedcc1b782e044522785` 落盘；`1941c56` 的 production reader 接入仍是
  default-OFF 静态候选，不能把 private-live 升写为 production-live。
- B3 r1 artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-b3-48da012-20260904-054011Z\artifacts-live`。outer/cell/final-error
  SHA-256 依次为 `B319F853623FF9443F3941F4078559D6E24CDBF0EF5D75BDCEBE269A5D961923` /
  `8CCD24257185BB39529BCEC0FAE9F03ACAABCF281455CE55BC49D6C159E4AC56` /
  `C52DC7DAA975A6EA8EB60CCEF5429E35B0299CCD4A090EEFBEE995EE491956CF`；`310.617 s` 后 RED，cleanup
  GREEN（SHA-256 `860BB73317D9F788B96C8F2491735BC1D5882470E9D44F0F7208CEB668FF4DEB`）。CK3 实际报告缺少
  `zg361_p2c_record_stage_effect` / `zg361_p2c_record_red_effect`；递归静态 closure 另发现同一漏选 provider 还承载
  `mark_lane_busy` / `schedule_pump`。这是 effect material/call-graph closure RED，未进入 gameplay。
- 补入 central dispatch provider 后的 B3 r2 artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\b3c-ce458af-20260904-0613Z\artifacts-live`。outer/cell/final-error SHA-256
  依次为 `2F44D5C06549D0EA6CCDB977A602441C92276CDE80138686648249E003AEFF2F` /
  `3852B4620AC2E4CA0B41F526C6771AE1446AEE623BFE37110AC158B770ACB9AE` /
  `E1CAF82E07B3EE6E4B75D9545BA4576EFC5E8424E656A909B32CA3085839DED4`；`311.098 s` 后 RED，cleanup
  GREEN（SHA-256 `F14A23AB1864245D6FD1AF215A65F3F451EFC5E4B67BB1F14F7E3B1F614A9065`）。真实首错改为
  `Event [zg361p2c.1] not found` / `Event [zg361p2c.2] not found`，故仍是 event provider material closure RED，不是
  loader-performance RED。两轮都不触发文件体量 A/B；只有排除更早 parser/material/call-graph 首错后仍存在纯加载性能 RED，
  才执行只改变顶层文件边界的同条件 A/B。当前边界门仍为 427 files / 3,718 effects / max non-legacy 10 /
  target misses 0 / `>20` violations 0。后续 `0d5b7de` 已将 freezer 扩为 effect→event→effect/event 递归 closure，
  normal/`-O` 各 7 tests GREEN，但尚无第三轮 live。
- B4 HC-workforce：正式 Route-B registry/replay 入口已由 canonical `27b66b3`（等价独立提交 `73476cb`）接入；
  它只消费真实 `zg361we.360` pre-B checkpoint、provider-sealed 13 facts 与 hash-identical restore/replay，ACK 不是业务结果。
  当前树下仍没有合格 checkpoint/registry，本批未启动 CK3，状态保持 `static-ready-live-pending`。基于 `27b66b3` 的
  source-checkpoint capture entry 后续仍在施工，尚无 commit/test，故本次不把它列作完成项。
- B5 projects-metrics：`f4a9e397cf2d9e776c28b881c7a76dfbba3b4764` 在 `ce458af` 重冻 default-OFF private candidate。
  DLL / injector SHA-256 为
  `F03DF885B93A62A44903C3B732A5E2164AF115B4DA02C887425486DACD7B4CF8` /
  `FC47FD40E2414377F25EF13C6504FDBEEBB1DC73B451495A423B8CA051F08663`；native 3/3、Python normal
  27 + 4 subtests、`-O` 27 + 4 subtests、no-launch verifier 18/18 GREEN。因没有 CP26/P3 paused checkpoint，
  production advertisement=false，readiness 仍为 `static-ready-live-pending`；后续 CP26 source-checkpoint capture 入口尚在
  no-launch 施工，尚无 commit，不计作已交付。
- B6 career-HC/workforce：source/freeze 提交为
  `6a6473ac5b4e9afcb406915cd1f61b5cf619a40a` /
  `0df1d5b827c203c35ca93a20fc6631c08d1c4076`。default-OFF 与 candidate-ON native build 均 92/92 GREEN，
  focused native 3/3、Python 34 + 7 subtests GREEN；冻结 DLL / injector SHA-256 为
  `9EE39CAE349180806FA2631805539EFC704C45401A2EB11F7F781F4EE671E7AC` /
  `751D178F692EA029F84A0D4DDA00BDA09C2DB2F85AFAD49833BE0DF8D3C9A994`。capability 仍默认 OFF，没有
  paused provider result，故只到 `static-ready-live-pending`。
- promotion / Incident / endgame 正式 runner 分别由 `894d7a7`、`309fe6f`、`87fb5ad` 接入：promotion 固定
  `zg361pp.147 option 1 → zg361comp.1`；Incident 固定 received-self `player=root=subject` 且 distinct owner，并要求
  X/Y/Z provider 与 wrong-owner negative；endgame 使用真实 M356→M360→M361、owner/result checkpoint→subject provider 的
  双视角 seam，最多两个受管 restore generation。三条路径都拒绝 fixture/console/generic rebind 与 ACK-only；目前都只有
  no-launch/static wiring，没有合格 source checkpoint 或业务 postcondition artifact，状态均为 `static-ready-live-pending`。
- T2 open_kaishek 主仓已到 `98c13d02ba1c772836a35f782716a0b3679b7ee8`：本轮新增 career-HC/workforce 只读
  capability profile；promotion、projects、Incident/endgame 与 G2 leaf-context 没有公开不兼容变化。Maven 135/135、
  父仓 focused 67 + 5 subtests、compatibility verifier `GREEN_STATIC`、product corpus 54/54 parsed / 0 errors；
  JAR 348,438 bytes，SHA-256 `10DFAB8AA729D3B696C518BD532FDE7535DC1605150045828B650546864BD9CF`。
  这不提升 native/runtime/live readiness。
- 基于 `27b66b3` 的 fresh no-launch 合并矩阵由 `ce06733b735c2703d1874787999029f7eb72a6a9` 收口，并已由主线
  cherry-pick 为 `a07887a`：
  native 92/92，B4/B6 Route-B 38/38，promotion + Incident 43/43，endgame 19/19，promo choreography/producer/registry
  82/82，`validate_static.py`、`validate_local.py` 与 effect boundary 全 GREEN。endgame 曾因 merged
  `CompositePhase2SpanDriver` 的 stale static-token 检查出现可复现 18/19，已用只改 preflight 所有权检查的最小修复关闭。
  这组证据全为 static/no-launch，未覆盖真实 CK3。
- 当前诚实总状态不变：B2 focused 已完成，但 B3 尚未跨过 material closure 并取得 paused provider result；B4/B5/B6 与三条
  runner 仍缺真实 source checkpoint/postcondition。Phase2 footage 仍为 **0/8**、两版 MP4 为 **0/2**，
  `full Phase2 claim=false`，T0 远未完成。

### 2026-09-04 15:09 滚动增量：B3 r3 material RED、G2 默认 production reader GREEN 与 T2 FF

- B3 r3 artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\b3e-27b66b3-20260904-063948Z\artifacts-live`。outer/cell/final-error/
  cleanup SHA-256 依次为
  `504591432CA9E39D75A646AE5C1F133AB62F567C181533F27C40C7E9E7867784` /
  `B0F0657421FB5EBC09973F0B9CA7A763370949C57BFBECF53BC30E49D2107A7D` /
  `A09CA7136D4A83D836D5C691287540B48C4C1225388A1F8F0581BD16E098F6D3` /
  `CD76C722E470F61EB45A6B3AC80CB476BF1CE712267BA5856F9B91BFB0C621CB`。cell 在 `312.77 s` 后 RED；
  loader gate 已执行且 CK3 到达 frontend，但没有进入 Load Save/In Game，`gameplay_acceptance_executed=false`、
  `gameplay_green_claimed=false`。native cleanup GREEN、driver closed、runtime locks released。
- r3 的真实日志包含 14 个 unique missing events：
  `zg361cl.100`–`105`、`zg361cl.200`–`204`、`zg361ip.202`、`zg361ip.302`、`zg361we.4804`。
  因此这是继 r1/r2 后继续暴露的 **material event-closure RED**，不是在 parser/material/call-graph 已干净条件下出现的
  纯 loader-performance RED；本轮仍不触发文件边界 A/B。现有边界门保持 427 files / 3,718 effects /
  max non-legacy 10 / target misses 0 / `>20` violations 0。下一候选必须先补齐这些 event providers 并重新执行递归
  effect↔event material closure，不能用继续拆 effect 文件代替闭包修复。
- G2 默认 production leaf reader 的 fresh live artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\g2-production-leaf-1941c56-20260904\live-production-leaf-dual-query-r1`；
  report SHA-256 为 `AD6EEF83DCCA07C3AE280F01CADE6BBD0C1912FF0E086D797604D5F06C99F7C2`，结果 GREEN，
  用时 `151.766 s`。它使用 source commit `1941c56` 的默认 exact-build binary，四个 private/diagnostic options
  全部 OFF；两次 public termination-terms query 在同一 paused frame 都返回
  `evaluated_days_observable=true / evaluated_days=1825`。
- 该 G2 live 没有 mutation command、没有时间推进、没有投降/停战动作；source checkpoint 与 driver state 保持不变，
  `tree_gone=true / cleanup_proven=true`。因此 default production reader 的 **evaluated-days 只读 primitive**
  可以记为 production-live GREEN；但 `actual_expiry_observable=false`、`decision_ready=false`、
  `automatic_surrender_ready=false`，GEN-034 仍未闭合，也没有完整 surrender OODA。
- T2 open_kaishek 已 fast-forward 到
  `320398b7f40ac049742ca78c095f24d1b8fce825`；本地
  `Z:\workspace\open_kaishek` 的 `main == origin/main` 且 worktree clean。该提交保留 G2 profile/source-contract
  provenance；public capability/schema 不变，不增加 parser、IR、runtime handler 或 action capability。静态审计为
  `GREEN_STATIC`：Maven 136/136、domain 8、CLI smoke PASS、product corpus 54/54 parsed / 0 errors。父仓在 canonical
  `1341251` 已同步该 pin。
- T0/Phase2 总状态不因 G2 单字段 primitive 前推：B3 尚未进入 paused gameplay，B4/B5/B6 与 promotion/Incident/endgame
  仍缺各自真实 source checkpoint 和 provider postcondition。Phase2 footage 保持 **0/8**、两版 MP4 保持 **0/2**，
  `full Phase2 claim=false`，最终宣传视频仍未制作。

### 2026-09-04 15:32 滚动增量：B3 r4 trigger material RED 与三项 production choreography 阻点

- B3 r4 artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\b3f-1341251-20260904-070920Z\artifacts-live`，cell 在 `317.408 s`
  后 RED。outer report / cell report / evidence index / final-error SHA-256 依次为
  `55DF5DE1C6988C94E41358946F4DABADDA68A06B137A969668ADA30834098071` /
  `6C3A1EADAAA29DCA174E4B30B1DA911AC7DC34ADB6D189877F3A8B8993E8DF6E` /
  `EF8C24429C874B0FF0E3E18E66E92457E6957FE5BE8CC3DD0DB0B79297DA96F7` /
  `0B084D2B92B9673EBEEEF2AD1DB2CC3477E7AB8C0F083BF98B78011069D54B49`；native cleanup GREEN，进程树清空。
- r4 已把 missing event 降为 0，但 persistent reader 仍有 6 行 / 2 个唯一 unknown scripted triggers：
  `zg361_p2c_m360_frozen_manager_exact_trigger` ×3、`zg361_p2c_m360_candidate_ready_trigger` ×3。因此它仍是
  **scripted-trigger material/call-graph closure RED**，不是纯 loader-performance RED；未执行文件体量 A/B。边界门继续是
  427 files / 3,718 effects / max non-legacy 10 / target misses 0 / `>20` violations 0。r5 trigger 递归 closure 正在施工，
  截至本节没有新 live 结论。
- r5 exact-A 文件边界 fallback 的 no-launch dry-run 为 GREEN：A product 420 effect files / 3,698 definitions /
  14,883,016 B；计划 B 为 615 files / 相同 3,698 definitions，其中 198 个用途 shards、max 10、1–10 miss 0、
  `>20=0`。三个 target owners 与 r3 bytes/hash/count 相同，definition bodies 与 call graph identical；planner 为
  `c6f96c79591cf99c1eb116df0edab66e82d7ca8b`。B 没有物化、CK3 没有启动，这不算已执行的性能 A/B。
- 三项 production choreography 阻点已经独立收敛：B4 在同一 effect 内立即恢复 READY source，无法稳定截取
  `zg361we.360` pre-B，最小入口是 stage 11 前 typed owner handoff 或成熟 owner-played seed；B5 的 stage 7 P3 固定早于
  stage 8 CP，而 provider 又只读 P3 投影，使 CP26-ready/P3-absent/source-ready 合同互相矛盾，最小修复是同一 cycle 内
  CP-before-P3 加直接 CP26 观测或等价可核验后验；promotion 缺 `zg361_review_now_decision` exact stable-identity action 与
  player-owned B1/central/PP observers，现有 `.147` capture 入口不能补足这段 prefix。三项最小修复均已开工，但尚未产生
  合格 source checkpoint、registry 或 postcondition live artifact。
- 这些是业务调用图/观测口阻点，不构成文件大小因果证据，不以追加拆分代替修复。T0 仍为
  `full Phase2 claim=false`，Phase2 footage **0/8**、两版 MP4 **0/2**。

### 2026-09-04 16:14 滚动增量：B3 文件边界 ABBA 终态、B4/B5 修复与 G2/T2

- B3 r5 的 A/B 只改变三个超大 owner 的顶层文件边界：A 为 420 effect files / 3,698 definitions / 14,883,016 B；
  B 为 615 files / 相同 3,698 definitions，以 198 个用途 shards 替换三个 owner，max 10、1–10 miss 0、`>20=0`。
  definition bodies/call graph identical；surface/call-graph SHA-256 为
  `2B3C79D2503941EE1E75E5D111E91DB5EAE37323478C32E1F9DFE0970BA2FEFD` /
  `CE3E82408378AD70376F066EF12F94CC9D68847FCFFE7269378718611E3F5387`，materialization manifest SHA-256
  `1534D9B89FD42B2EF225908F924C74306987171D691119C2AA2D8FBD45C19AF4`。
- A1 artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\b3g-fecd2f2-20260904-073033Z\artifacts-live`，report SHA-256
  `65D2CB67B698892FC2ED17D0C8FCF6DB3270CC5A3A8E21387E7DCEFB3EAFA711`，cell `320.080 s` RED。B1/B2 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\b3g-fecd2f2-20260904-073033Z-boundary-B\artifacts-live-b1` / `...\artifacts-live-b2`，
  report SHA-256 `33BF29986EBBEEFABDA16EE3653E0C871DDE918BE8CAC0DF6C38BE28C87537EA` /
  `F5D11F9638A4567DA06DE1EED72ADAC85C630B9DC40BF9CC37AA3FD3DFD514F9`，cell `318.838 / 321.813 s` RED；
  三轮 cleanup 均 GREEN。
- A2 artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\b3g-fecd2f2-20260904-073033Z\artifacts-live-a2`，outer report / cell report /
  evidence index / final error SHA-256 分别为
  `C5C971401356821E0C476C9F80AE45D1D1BADB22AAE9ABC7B19FE69C47274BC7` /
  `7D094FE9E1435458DEAC48458C5A10F928C5B27AE60BAF29431136FB67B5CC85` /
  `8504210B332EBF727F75211B6FB549FDF5B45088A2AFEA51A4956D8B0209878B` /
  `6AC100FC74FCC36618744A2C83D837C35FDDE51D8CD1F3480C4727FE9EF25050`。本轮 `319.338 s` 后仍为 terminal
  missing RED，cleanup GREEN。
- 四轮 effect/trigger/event missing 都是 0，均完成 303 个 database callbacks、最后节点
  `CJominiInGameMusicDatabase`、fatal 0，随后因 `loader_terminal_missing_after_database_callbacks` 超时。由 loader elapsed
  减 callback-completion quiet 推得 ABBA first-303 依次为 `297.264 / 133.034 / 125.891 / 137.788 s`；A2 的 Boundary A
  与 B1/B2 接近，因此 A1 是次序/冷态离群，不能称 B 稳定加速。四轮都没有 completion publish、没有进入 gameplay。
- ABBA 的终态结论是：拆分未修复启动，且现有证据不支持文件边界/单文件体量是根因；后续应转向四轮共同的 completion
  publish 缺失，不再基于这组结果追加 size A/B 或宣称拆分性能收益。
- B4 `9c87280187250077afd99deb48e4126d5d19f87a` 把 M360 same-effect immediate resume 改为 exact WAIT `360411` +
  D+1 hidden `zg361p2c.7`，从源码上留下真实 READY pause/save/restore 边界；B4 provider seal 只要求 8 项 current-cycle 事实，
  不再把未来三周期/M361 maturity 当作前置。仍缺 seed→WAIT→真实 `.360` pre-B→Route-B seal 的 live artifact，故保持
  `static-ready-live-pending`。
- B5 `953634265EBF298CEC3F2CF3065060E577DC8D17` 把 stage 7/8 修为 CP→P3；provider allowlist 24→40，并以 direct
  CP26 字段和 `cp26_ready_p3_absent` 等 checkpoint states 消除“P3 未初始化却要求 P3 source-ready”的自相矛盾。Central
  仍为 10 个用途 shards、max 9、无 `>20` 例外；capability 默认 OFF、真实 CP26→P3 paused live pending。
- G2 default production live artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\g2-production-leaf-1941c56-20260904\live-production-leaf-dual-query-r1`，report
  SHA-256 `AD6EEF83DCCA07C3AE280F01CADE6BBD0C1912FF0E086D797604D5F06C99F7C2`；它继续只证明同帧双读
  `evaluated_days=1825` 与 generic current soldiers=598。
  `714be35d1e4420889e92775be10d7826b2508da1` 增加 default-OFF read-only 战损候选，只有 termination 后 exact frozen
  generations 全部 destroyed 才能给出 post=0 / boundary loss=598。静态回执 SHA-256
  `6B85024D6964DD715D88F502C5D21BC6987A7DEBAD2A309955E25F3334DDC991`；source attribution、action binding、surrender
  causality、public terms、automatic surrender 与 GEN-034 均未闭合，candidate 不是 production-live。静态验证为 native
  default-OFF 2/2、candidate-ON 1/1，Python normal/`-O` 各 23/23 GREEN；该候选包未启动 CK3。
- T2 open_kaishek 已到 `main == origin/main == a54164625b3ebb7d738d16236ca3080686fa9984` 且 clean，父仓 pin 为
  `5c54014c7317bd2446bd342d7205cf00fe024dc9`。同步只涉及战损 metadata 与 B5 checkpoint-state v2；Maven 138/138、
  domain 8、CLI smoke PASS、corpus 54/54、父仓 verifier 45/45 `GREEN_STATIC`，JAR 351,165 B / SHA-256
  `117CF7D2A768BF81455B073778B09537569A723F58F6583BE2D484669B433F32`；没有新增 action/public query 或 live readiness。
- T0 仍是 `full Phase2 claim=false`；B3 A2、paused provider 与各业务 source checkpoint 尚未闭合，Phase2 footage **0/8**、
  两版 MP4 **0/2**。

### 2026-09-04 18:39 滚动增量：B3 启动链恢复、G2 cleanup 静态接线与交付计分

- B3 的 ABI-safe V1/V2 实机二分排除了 `candidate_ready` 正文，把 terminal loader 因果范围收敛到
  `frozen_manager_exact` 正文/调用结构；两轮 material/parser 门为 0、cleanup GREEN，verdict SHA-256
  `029B3916AB7AF5735CCAC19EEF472CFE7FB5840270B80B387F403A5C10ADDF1D`。生成器随后只增加一层显式
  `AND`，保持条件/参数/call graph，实机在 `125.965s` 恢复 Frontend；outer report SHA-256
  `F0ABC5F24505019061B986DB8992CA0DDD95C0D584C6C9F24B5D4BF6BB9B9B70`。这是 live-confirmed startup
  primitive，不是 B3 gameplay GREEN，也不支持“effect 文件越大就是根因”。
- frontend-first long-root 的最终 mount 长路径达到 264 字符，PhysFS 对其中 255 字符文件明确报错并在 Frontend 前退出；
  相同 565-file 产品在 max-path 197 的 short root
  `Z:\ck3_mod_rewrite_process_assets\zg361\b3j-ff-1817` 完成第一 PID Frontend、第二 PID 加载固定
  `autosave.ck3`、paused `map_ready=true` 与 cleanup GREEN。outer report SHA-256
  `FC8AF93D63B0201198B26BA00935FEE312702CCD2B35CB5044F401070272DD3E`。当前更晚的 fail-closed
  gate 分两类：promotion capability 与 derived support flag 已由 `cac1e85` 放进 candidate-only switch，默认仍 OFF，待 fresh
  binary/live；result-case 则不是 advertisement 缺失，而是 runner 错把带 typed owner/nonce 的 query transport 列成零参
  `action_step`，runner 合同正在修复。manager 业务步骤尚未执行，所以完整 acceptance、OODA、footage 仍为 RED/未发生。
- G2 `ff89dcd` 的 private/default-OFF cleanup dispatch 与 receipt adapter 已到 static-ready：只在同一真实 lifecycle 中接受
  surrender ACK、旧 WarID 缺席、三类 exact stores 双采样 destroyed 与两次一致 actual-expiry。no-launch artifact SHA-256
  `BFB4ACD70B98534ED722F5557504E21FC004C6E568221386A1F05EBB3D539BF6`；没有 CK3 live、没有 expiry 结果、没有
  readiness/automatic-surrender/GEN-034 提升。
- T2 open_kaishek 当前 `main == origin/main == b10b762eefaaf13eb53ded19b88a553d940aed24` 且 clean：
  `3c4e698` 同步 private cleanup metadata，`b10b762` 证明显式 AND 只是已支持的 parser AST，不需 parser/validator/IR/runtime
  代码变化。Maven 147/147、ZhongGuo baseline/current corpus 533/533、主 product 54/54、父仓 verifier 59/59 均
  GREEN_STATIC；当前同步 horizon 完成，但不冒充 native live。
- 宣传工具在 18:39 再核验为 clean，`HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`，
  `describe=v0.2.1-1-g57c42fc`。Phase2 真素材仍 **0/8**、目标 MP4 仍 **0/2**。

本周自此固定用交付物积分报告进度，不按提交数计分：T0 为 **50/100**（产品/生成器/文件政策 18/20，B1+B2
15/15，B3–B8 六个业务纵切 10/30，合并验收/release 2/5，promo 工具/authoring 5/5，八段 registry/capture
0/10，两条成片审阅/导出/发布 0/15）；T1 为 **60/100**（evaluated-days production-live 25/25，retention/loss
10/15，actual expiry 10/15，private cleanup 15/20，同生命周期 live 0/15，decision/readiness 0/10）；T2 当前同步点为
**100/100**（T0 parser 35、T1 metadata 35、clean main/pin/tests 30）。按 CRITICAL/HIGH/NORMAL 固定权重
`60/25/15`，整体为 `50%×0.60 + 60%×0.25 + 100%×0.15 =` **60%**。T2 后续随 T0/T1 接口变化重新开项；
百分比不是 ETA。这个固定 **60%** 取代此前口头粗估的 **68%**；差值来自把 business vertical、0/8 footage 与 0/2
最终成片按交付物计入分母，而不是能力倒退或代码回退。

下一步先修正 result-case typed query 的 runner 合同，再用 short root + 打开 `cac1e85` candidate switch 的 fresh binary
跨过 B3 业务 query/action/postcondition，然后收集其余 source checkpoint
和八段真实 capture；G2 独立完成同生命周期 cleanup/expiry live。八段 intake 前不启动最终 TTS/render，也不把 0/8 写成完成。

### 2026-09-04 19:27 滚动增量：seed 陈旧产品首用 RED、lazy guard 与 G2 no-launch

- explicit-AND seed refresh `Z:\p2s10a` 挂载的外部 product 是 **565 files / 21,607,199 B**，tree
  `D94C2D5D23E9AD254F4B20988FBF3C8E08408BAA61070BD85F42B2D2FCBEA35D`，且相对 current generator 已陈旧。
  最长 mounted path `154`；frontend-first `109.396 s` GREEN，final 进程加载旧 seed 后进入 result-freeze。故本轮进一步
  排除 Frontend/loader-performance、文件数和单文件体量作为直接根因，也支持继续保持短物理路径约束。
- result-freeze 唯一失败是 `93 = 31 × 3` 条 optional-variable 诊断：debt consumers `57`、#069 delivery `30`、
  #072/#081 各 `3`；每个读取点各产生 fetch-variable、unset-scope、invalid-comparison。report / scan / full error / relay
  SHA-256 为 `77E4927B…1BCB` / `7958C5A4…FF5B` / `377B7B5D…CA87` / `2D96AE47…CDCAC`。cleanup GREEN、
  CK3 count `0`，但 candidate seed/contract 未生成；该轮不增加 T0、seed 或 0/8 footage。
- `ee4fab944737685b21d7cc1f18ff572cf0238d90` 已把 B2 首用比较改为真正 lazy 的 `trigger_if` / `trigger_else` 或嵌套
  existence gate，并从 generator 重生成 20 个受影响文件。复核：generator `--check` GREEN、单测 `39/39`、effect
  boundary 单测 `4/4`；全树为 **427 effect files / 3,719 effects / max non-legacy 10 / target misses 0 /
  `>20` violations 0**。B2 仍是 25 个用途分片 / 152 effects / max 9，无超限例外。该提交尚无包含新字节的 cumulative
  seed live，因此保持 static-ready。
- G2 `8f7c352a59318bed46aa1d1ecd103d0ce8a52412` 的 current-pin 包在当时 canonical `549076f` 上完成 formal no-launch：
  86-file product tree `F4E63FFF…944BE`、native `94/94`、preflight `READY_TO_SERIAL_LIVE`，preflight SHA-256
  `3A75BE0E…06A33`；`live_executed=false`，不提升 cleanup/expiry 或自动决策。canonical 后续前进到 `ee4fab9`，按已冻结
  manifest 规则，执行前仍须复核/重冻 source pin。
- T2 已开始同步 `ee4fab9` 的 lazy AST/source 与 G2 no-launch contract；新回执尚未返回。固定计分不随这些 static/no-launch
  增量波动：**T0 50/100、T1 60/100、T2 已闭合 current horizon 100/100、overall 60%**。Phase2 footage 仍
  **0/8**，两条目标 MP4 仍 **0/2**。

### 2026-09-04 19:55 滚动增量：fresh 575-file seed closure 与原版前置事件 RED

- `Z:\p2o` 第三轮从 current canonical 重算 closure，而不是继续复制旧 565 inventory。相对第二轮加入
  `events/zg361_phase2_central_003_m360_resume_events.txt` 和 9 个 promotion-PIP localization 文件，`10 new / 0 removed`；
  product 为 **575 files / 28,438,735 B**，tree
  `361F4785BD626A42AE558F49DB32B79EED69247A0C4CEEF775C8390FADBB93EA`、max mount path `154`。最终 closure
  `3,703 effects / 986 events / 24 triggers`，missing effect/event/trigger 均为 0。
- loader scan 已变为 **GREEN / matches=0 / quiet 16.364 s**；旧 93 条 optional-variable 三联错误与
  `zg361p2c.7` missing 都为 0。Frontend-first 50.066 s direct idler、final `load_save / 303 nodes / fatal 0`、native
  paused/map-ready 与 B2 三方 bytes 均 GREEN；seed fixture markers 已走到 result-bound case prepared/waiting。
  因此 stale bytes 与 stale inventory 两类 product RED 均关闭，本轮也没有文件体量/加载性能 RED。
- 新的唯一 RED 是业务 surface 次序：runner 等待 `zga_phase2_seed.1` 时先看到原版 `spymaster_task.0381` instance 11。
  原版定义位于 `game/events/councillor_task_events/spymaster_task_events.txt:1746`；runner 没有替玩家选择，按 fail-closed
  报 `unexpected visible event before bootstrap`。report / bootstrap wait / loader scan SHA-256 为
  `59F204C4…79B43` / `AB61FE58…081C` / `90253F97…628A3`。
- final cleanup GREEN，PID `29172`、job、watchdog、control files 均清空，审计时 CK3=0；source/runtime/外部依赖
  immutable，clean source pycache/pyc 均为 0。没有 candidate、checkpoint 或新 seed contract，故本周 readiness 不因这轮
  前推：**T0 50/100、T1 60/100、T2 当前 horizon 100/100、overall 60%**；Phase2 footage **0/8**、目标 MP4 **0/2**。
- 下一轮先处理既有存档的真实原版前置 surface，再以相同 fresh closure 与 short-root 门重冻 seed；不得将 vanilla event
  误归因为 loader/product/file-size 故障，也不得在 seed 生成前进入 B4 capture 或宣传片 intake。

### 09 月 04 日 21:40：B2 fresh seed 与 G2 action-bound cleanup/expiry 实机 GREEN

- B2 current cumulative product 已在 629-file / 28,905,596 B 的真实装载树上完成 `paused_seed_ready`；legacy witnessed
  delivery token 被惰性迁移并只交付一次，loader/material/project scan 均 GREEN。全局 effect 边界保持 427 files /
  3,719 definitions / max non-legacy 10 / `>20` violations 0。本轮再次没有加载性能 RED，旧失败归因于延迟 payload
  schema 演进，而非文件体量；因此继续保留用途分片制度，但不把拆分写成已证明的性能修复。
- G2 R2 在 action 前因 private runner 漏传 surrender capability 而成为 harness RED；该失败原样保留。`e72f9fa` 补齐
  private capability 且不开放公共 action，R3 exact freeze/preflight 与串行 CK3 run 全 GREEN：唯一 surrender 后，exact
  store 证明 598 soldiers 的八个冻结 generations 全 destroyed、post=0，并从 persisted relation state 双读到实际 expiry
  raw `53267736`。ticket 19/19、source invariant、process cleanup 全 GREEN；report SHA-256
  `44E1F7C0B470B2CF7B6549192865402F21F88C7CF073E896DE1B93632311D5D0`。
- 交付物固定积分更新为 **T0 50/100、T1 90/100、T2 current horizon 100/100、overall 67.5%**。G2 尚缺公共
  continue/white-peace/surrender comparison 与自动 readiness/GEN-034；天朝二期仍缺 B3–B8 业务纵切、0/8 footage 和
  0/2 最终成片。下一主线返回 B3 fresh exact candidate 与真实 provider postcondition。

### 09 月 04 日 22:06：B3 GUI closure 实机通过，seed 时间线停在 exact PIP

- 630-file / tree `9E75D8E5…EE723` 产品实机完成 frontend 与 load-save；loader 为 GREEN、303 nodes、54.403 s，
  promotion-source GUI provider 不再缺失。没有 loader-performance RED，effect 分片仍保持 max 10 / `>20=0`，不追加
  无证据的 size 拆分。
- 当前 seed 一天后先出现 `zg361b2.40`；runner 在选择前按 fail-closed RED。完整 report SHA-256
  `2EC4F8A8922CFC9733F892364C599C100A393B6052EB6A54C703B38BB39FBB66`，进程回收 GREEN。
- exact-save、日期、root、reviewing superior、PIP owner/subject/target 与三选项形状的一次性 accept drain 已静态实现并在
  normal/`-O` 下 GREEN，实机重跑 pending。交付积分暂不变：T0 50、T1 90、T2 100、overall 67.5%；footage 0/8、MP4 0/2。

### 09 月 04 日 22:19：PIP drain live GREEN，exact vanilla `.0399` 后继待互证

- `zg361b2.40` 的 exact identity、pre-selection 与 option-1 ACK 已在 `945f7d4` clean run 全 GREEN；drain SHA-256
  `A8FE8D70…9C166`。loader 51.955 s / GREEN，进程回收 GREEN。
- 后继 `spymaster_task.0399` 在动作前 fail-closed，report SHA-256 `466B4C33…B2732`。原版定义核对后，已静态登记
  exact-save option 2（保持当前密探任务），并绑定全部可观测人物/boolean scope/选项身份；normal/`-O` GREEN，live pending。
- 没有 loader-performance RED，不触发额外 effect 拆分；seed/candidate、B3 provider、0/8 footage 与 0/2 MP4 均未前推。

### 09 月 04 日 22:27：无窗口 timeline revision race 收敛

- `8d0c408` run 在 loader GREEN 后、任何事件动作前，因 snapshot revision `4→5` 令 `resume-map` 触发
  `PreSubmissionRevisionMismatchError`；report `250B3F28…F19BC`，cleanup/driver close GREEN。
- waiter 已将相同的 typed race 重试语义扩到 `set-speed-1` / `resume-map`，仍使用原总 deadline；normal/`-O` GREEN，
  live pending。它是 harness 修复，不触发 effect 拆分，也不改变 T0 50 / overall 67.5 / footage 0/8 / MP4 0/2。

### 09 月 04 日 22:43：630-file canonical seed 刷新 GREEN

- 从 completed `233e...dc9c` seed 的尝试证明 PIP 与 `.0399` 两个 exact drain 均 live GREEN，但 fixture 不会二次
  bootstrap；转回原始 `bfc73f...` 基线是正确输入路线，不再扩展 timeout/事件白名单。
- `218026a` + 630-file tree `9E75D8E5…EE723` 的 fresh run 为 GREEN：loader 46.583 s，seed event/provider
  capture/checkpoint/cleanup 全 GREEN。report `EE121AA2…F451A`，新 canonical save 57,377,787 B /
  `8E6CEB97…E45B9`。
- canonical 合同及三份 choreography 已同步，27/27 合同测试与 4/4 effect boundary GREEN。B3 gameplay 尚未执行，
  固定积分和 0/8 footage、0/2 MP4 暂不变；下一门是 compensation-ON B3 live。

### 09 月 04 日 22:53：B3 R8 到达 paused map，provider typed-unavailable

- compensation-ON R8 从 refreshed 630-file seed 完成真实加载，loader gate GREEN（68.947 s / 303 nodes），
  scripted-effect database 596 ms / 764 ms；promotion GUI bridge 资源明确加载，loader signature 扫描为 0。
- 第一条 fixed progress query typed-unavailable，任何 review/event 动作均未提交。report `F7ED52BE…CC29F`，cleanup
  `41707CB5…B0284`，现场保留且受管进程已归零。这是 provider/runtime RED，不是文件边界或单文件体量 RED。
- runner 已补原生 unavailable reason 与缺失 widget 名诊断，normal/`-O` 各 9/9 GREEN；不改变 capability/readiness
  或固定积分。下一次串行尝试只用于取得 exact widget 失败身份，再做最小 provider 修复。

### 09 月 04 日 23:04：B3 R9 顶层窗口未实例化

- R9 明确 `widget_not_instantiated` 覆盖 promotion root 与全部 descendants，且在任何动作之前结束；report
  `F75F8015…A2F78`、cleanup `B448DD39…07D7`。这把故障从笼统 provider RED 收窄到 searchable top-level
  instantiation，不是 loader/file-size RED。
- fixed-name 只读 native probe 已加入 scoreboard/decision/mechanism 三个对照 root；compensation-ON build 全量 94/94
  GREEN，仍不开放 production/action capability。下一 serial run 只闭合查找面归因，积分与 footage/MP4 不变。

### 09 月 04 日 23:22：B3 R11 闭合 custom top-level 查询边界

- R10 的 frontend-first evidence 原子替换出现一次性 `WinError 5`，未进入产品查询；同输入 R11 在新 state dir 完成
  Frontend warm-up、PID 回收和最终 paused save-load，因此该 harness RED 暂不扩张施工。
- R11 的 exact reason 为 `widget_not_instantiated:top_level_probe=none`：promotion 之外，scoreboard、decision 与
  mechanism 三个注册 root 也全部不在该 `FindTopLevelWidget` 查询面。这是 ABI/宿主选择 RED，不是单个 GUI 文件位置、
  loader 或 effect 文件体量 RED。
- loader 303 nodes GREEN、scripted-effect 617/779 ms、cleanup GREEN、动作 0；report `DEBD2AEB…233F03`，cleanup
  `5D858C33…6C9C9C`。后继施工改为先冻结一个原生可发现的固定宿主，再迁移 progress witness/action；固定积分与
  0/8 footage、0/2 MP4 不变。

### 09 月 05 日 03:31：B3 product 时间线扩展与 saved-character 观测修复

- R23–R30 把 `.3060`、`zg361.40`、`zg361b1.200`、中国/总督/宦官等八个真实原版或产品中断收进逐 key、逐 scope、
  逐 option 的 exact 合同；R34–R36 又证明 `.3060` 日期为日级浮动，现仅在实测 envelope
  `53148048…53152368` 内放行。R35 证明 `.0381` 可在同一 400-day bound 内复发；默认一次上限不变，仅该 key 最多两次。
- R31/R33 的稳定 `event_saved_scope_invalid` 与同帧 `intrigue_dread.1501:after` dead-character error 共同证明：仍可见事件的
  一个 saved Character 已失效。修复保持 root 严格，只把该 saved identity 显式降级为
  `character_scope_identity_unavailable`，不伪造 ID。新 bridge 94/94 CTest，DLL `6E5BB70B…984F`；后续普通 scope 查询正常，
  但同型 stale frame 的专项 live 再现仍 pending。
- R23–R36 的有效加载全部 GREEN / 303 nodes / fatal 0，当前 RED 位于加载后的事件观测/合同，不是文件体量或 effect
  loader performance。继续维持用途分片 1–10 / 原则 ≤20，不做额外 size split。交付计分仍为 T0 50、T1 90、T2 100、
  overall 67.5%；registry 未满、footage 0/8、MP4 0/2。

### 09 月 05 日 04:51：R37–R46 saved Character live closure 与事件动作编号纠正

- R40 在真实 `ep3_interactions_events.0630` 上证明 native provider 能保留三个 stale named Character 的 name/type，
  并以 `character_scope_identity_unavailable` 取代伪 ID；下游 normalizer 的旧严格分支成为新的真实 RED。修复只对 saved
  Character 开放该精确 shape，root 仍严格。R41 通过同一事件，完成端到端 live closure；四组 Python 回归普通/`-O`
  均 71/71 GREEN（73 subtests），native build 仍为 94/94 CTest GREEN。
- R43 证明 public event option number 是 `authored native index + 1`，不是 rendered ordinal。旧 entry 在 `.3060`
  第三个可见项处误提交 native index 2；该错误动作仅发生在隔离 userdir。现已修正 `.3060/.8100/.9007` 并加入显式映射不变量。
- 本组新冻结 `.0020/.2200/.9007/.0630/.2240/.0002/.0005` 七项 exact contract；R41/R42 还以两次 live
  证据把异步 `.0630` 日期界定在 `53147256…53151600`，其他 scope/option 字段不放宽。
- R37–R46 loader 均 GREEN（303 nodes、fatal 0）；R39–R46 frontend-first 为
  `157/169/151/157/163/164/159/159 s`。没有加载性能 RED，effect 文件不再拆；边界仍为 427 files、3,719 effects、
  max non-legacy 10、无 `>20` 例外。
- promotion `.146/.147` 尚未闭合，registry `1/4`、footage `0/8`、MP4 `0/2`；固定进度仍为
  T0 50%、T1 90%、T2 当前 horizon 100%、overall 67.5%。

### 09 月 05 日 06:20：R47–R52 current-core shard 同步缺陷闭合

- R47–R50 继续按 exact key/date/root/scope/option 收进 `.0010/.0342/.8100/.0346`；R51 虽跑满 400 日且出现真实 B1 publication，却没有任何 `ZG361P2C` trace。
- 根因已收敛到拆分边界：current canonical 的 `zg361_apply_pending_grades_effect` 已增加 P2C publication hook，但 630-file 产品仍由 B2 冻结的同名旧 shard 提供该 effect；name-only closure 因 provider 名存在而看不见正文漂移。它是文件拆分后的语义同步故障，不是单文件过大或 loader-performance RED。
- B3 expander 现在先从 current canonical 重建四个用途 core shard，再执行 fixed point。四文件 effect 数为 `3/6/8/9`、max 9；新产品为 634 files、tree `6AB2B8E1…B7824`，closure `3,706 effects / 988 events / 24 triggers / missing 0`。projection/closure evidence 为 `D99A74E9…006B1` / `69D7CD0A…2150C`。
- R50/R51/R52 frontend-first 约为 `202/159/142 s` 且全部 loader GREEN，单次慢样本没有复现，因此不追加 size split。R52 转而在 B1 publication 前停于原版 `.8010`；该事件已按源码形成 exact contract，固定走第三项。R52 report/evidence SHA-256 为 `ECD2DFA8…50F9A` / `36FFCD65…6AF3`。
- registry `1/4`、footage `0/8`、MP4 `0/2`，交付进度保持 T0 50%、T1 90%、T2 当前 horizon 100%、overall 67.5%；下一轮为修复提交后的 R53。

### 09 月 05 日 06:45：R53 exact sway outcome

- `8c3a649` 推送后，R53 通过 634-file current-core 产品的 frontend/load，推进至 1067-09 并在动作前停于原版 `sway_outcome.2001`；这不是 loader/file-size RED。
- live frame 冻结 `date_raw=53153952`、root/owner 29037、target 27051、opaque scheme/artifact、4 scopes 与唯一 native option 0。原版唯一分支造成 target -10 opinion 并结束既有 sway；现只在完整 exact shape 一致时提交 option 1，普通/`-O` focused 回归均 14/14 GREEN。
- report/evidence 为 `70EA0212…0BEB0` / `CDCE3183…AEAC`，cleanup GREEN。publication hook 尚未 live 命中，registry/footage/MP4/overall 不变，下一轮 R54。

### 09 月 05 日 07:16：B3 R54 current-core hook 实机闭合

- R54 的 634-file 产品 loader 继续 GREEN，并产生 7 条真实 `ZG361P2C` 业务日志，闭合 current-core 发布钩子已进入投影产品的实机证据；第 401 日触发旧 400-day bound，`.146` 尚未出现。
- 400 日继续作为 authored B1 上限，另加 150 日发布后观察窗；freeze wall-clock timeout 调整为 1800 s。该改动是对已观察到的业务时间轴补窗，不是 loader/file-size 归因。
- 同轮 34 条 B2 PIP evidence 未设变量读取已定位到漏改的顶层 same-level `limit`；生成器改为整 tuple lazy guard，双模式 B2 39/39、closure 7/7 GREEN。刷新产品 tree `A6C70F06…4576D`，634 files，closure 3,706/988/24/missing 0，630 个同路径选中文件 byte-exact。
- R54 report/evidence 为 `A7C59C3E…7959B` / `4F8E907D…5E80`。无 loader-performance RED，不追加 effect 拆分；T0/T1/T2/overall 仍为 **50% / 90% / 100% / 67.5%**，registry `1/4`、footage `0/8`、MP4 `0/2`。
