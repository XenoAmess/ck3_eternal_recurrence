# 2026-W35 周报

状态：**滚动周报 / 进行中（文字报告；无需周级视频）**

周期：2026-08-24 至 2026-08-30

当前滚动更新时间：2026-08-28 00:53（Asia/Shanghai）

正式收口：2026-08-31 00:00（Asia/Shanghai）

旧制度阶段记录：2026-08-27 02:36（Asia/Shanghai）；保留为真实历史时间，不再作为正式周报截止。

本周计划会：[`../meetings/weekly/2026-W35.md`](../meetings/weekly/2026-W35.md)（2026-08-27 补录；本周收口时逐项对照）

最新日早会：[`../meetings/daily/2026-08-28.md`](../meetings/daily/2026-08-28.md)（ISO 周未变，继续承接同一 W35 计划会）

## 周计划承诺对照（正式收口时填写）

| # | 周计划承诺 | 当前滚动状态 | 周末结论 / 证据 / 偏差原因 | 是否顺延 |
|---:|---|---|---|---|
| W1 | 收口非空 effect-indicator fixture-live 里程碑 | 完成 | artifact/独立审计、`49/49`、专题与提交 `06bde70` 已 push | 否 |
| W2 | 推进 stable scopes 或 structured effects exact-build 观测口 | 部分完成 | root/named Character scopes 已 `static-ready`；早前隔离 sandbox 启动崩溃只保留为历史环境 RED，正常桌面已可长跑，但 scope live A/B 尚未执行 | 是（仅 live） |
| W3 | 为下一周冻结可直接施工的事件 OODA 入口 | 完成（static） | 多选 fallback、完整候选账本、old-instance 后置验证与 runner artifact 字段已实现；事件自身 live acceptance 仍顺延，但不再受 `GEN-008` 阻塞 | 是（仅 live） |
| W4 | 落地并执行午夜报告闭环 | 进行中 | 08-27 日报已于 08-28 00:00 正式逐项收口，08-28 早会于 00:02 真实补录；W35 周报仍须在 08-31 00:00 正式核对 | 否 |
| W5 | 跑通一代人无人接管 CK3 主循环 | 进行中 / GEN-014 | `GEN-012/013` 已关闭；正式续跑又完成 42 个 gameplay turns 后命中 post-ACK pause B1。RED 已冻结，未修改重放把最新可恢复点推进到 `date_raw=53210760 / 79B71103...85F2`，最小修复待 live revalidation。speed 3 尚未命中，G1 仍待自然死亡与琉焰卿 committed scoring | 否 |

### 截至当前的计划外完成

- 生产长跑暴露旧路线选择同一 paused frame 执行 `167` 条原生查询、121 条查询约耗时 26 分钟；所有者据此把长跑吞吐
  提升为当前运营工作包。first-safe 路线与只读 driver-state 批量持久化已经落地，lean snapshot 已 static-ready。
- 这不改变 W5 的完成定义：性能优化、测试数量与 checkpoint 都不能替代自然死亡后的琉焰卿 committed settlement、人生分数和
  cleanup 全绿。

## 08 月 28 日 00:32：三轮同 checkpoint A/B 关闭 query/history B1

- 三轮都从 `date_raw=53209560 / A8DD4034...AD76CF` 起跑，只改变 Python snapshot/transcript 复制：`79b8d2a`、
  `e0688c7`、`9ff04ae` 的 turn-loop 运行段依次为 `48.134s / 44.875s / 24.684s`。
- 首/尾观测依次为：`79b8d2a` query `3.398s / 2.579s`、life `5.065s / 4.600s`；`e0688c7` query
  `3.317s / 2.516s`、life `4.583s / 4.111s`；`9ff04ae` query 约 `0.050–0.068s`、life
  `4.569s / 3.643s`。最后一轮 query 的数量级下降不能掩盖 life 仍需约 3.6–4.6 秒。
- copy instrumentation 与 live wall-time 一致：life `9→1→0`；planning `1→0`，约 `600.637→5.813ms`；termination query
  内部 `3→0`，约 `1815.527→1.852ms`。全量 unit 为 `1341 passed, 2 skipped, 900 subtests passed`，独立审查 `PASS`。
- 最新轮 `12/12` turns、6 gameplay、6 queries、2 checkpoints；六次 gameplay 全部为
  `player_tactical / speed=1 / elapsed_days=1`，每次均 paused，cleanup 全绿。最终 checkpoint 为
  `date_raw=53209704`、SHA-256 `39379D0224788198FECCCA82DA4B7B7257DB7E1AEE6B3750F62AA845E312678A`；
  driver state SHA-256 记录为 `D47DAA...BDA`。
- speed 3 仍未 production-live：相同玩家 12-hop route 与敌军追尾持续命中 tactical gate，没有 remote-route sample。下一步从
  最新 checkpoint 恢复 G1，在战局自然出现合法帧时继续 gate；不人为取消路线制造 speed-3 证据。

## 08 月 28 日 00:53：GEN-014 post-ACK pause blocker

- 正式 `9ff04ae` run `20260827T163217Z-one-generation-ace7cbcf` 已恢复 G1 并完成 `85/86` turns、43 queries、
  42 gameplay 与 14 checkpoints，随后一次 `life-advance` 在首次 `pause-map` ACK 后未于原窗口观察到 paused。
  report/blocker SHA-256 为 `49D2A8BB...B50808` / `A0BD8C79...A484`；角色 `29829` 仍活，cleanup 全绿。
- 该形状发生在 GEN-012 public-revision gate 已绕过之后，故新记 GEN-014；没有 ACK status/窗口帧证据时不声称 queue drop，也没有
  证据指向去复制优化回归。失败前 `history=2960` checkpoint 为 `date_raw=53210712 / F15D383B...35559`，文件与 driver metadata
  相符并已冻结。
- 未修改 `9ff04ae` 从同 F15D checkpoint 的独立 4-turn cold replay 未复现：两次 life 各推进一日、最终 paused，生成更新 checkpoint
  `date_raw=53210760 / 79B71103...85F2`，cleanup GREEN。它证明该失败是瞬时形状，不撤销正式 RED。
- 最小修复在原 10 秒绝对 deadline 内最多提交两次幂等 pause：首次 ACK 后观察 1 秒；仅同 bridge generation、episode、map-ready、
  speed/event owner 仍 running 时补交一次，最终仍要求真实 paused snapshot；没有第三次、不重置 deadline、不改 direct/query。
  当前 focused `12 passed, 10 subtests passed`、driver `153 passed, 128 subtests passed`、全 unit
  `1344 passed, 2 skipped, 908 subtests passed`，独立复审 PASS；从 `79B71103...85F2` 的 live revalidation 尚待完成。

## 08 月 28 日 00:00 日界更新：性能修复推进，speed 3 仍未实机解锁

- `a8ff95f` 把受威胁攻城路线从全量扫描改为既有排名上的 first-safe 提前停止。冻结生产帧旧路径是 `132` 次 move preview +
  `35` 次 route-contact query，共 `167` 条原生查询；新路径在第一个候选以 `1` 次 exact preview 选中 Province `3708`。该帧路线
  为 `12` hops，明确接受“快速脱离威胁但不保证全局最短”的质量取舍。
- `7cb0b75` 取消成功只读命令对约 79 MB `driver-state.json` 的逐条 deepcopy + pretty atomic rewrite；只读 suffix 延迟到动作、
  任意失败、checkpoint/restore 状态迁移或正常 close 冲刷，最新 checkpoint 与失败 artifact 的 durable barrier 不变。截至日界没有
  单独重跑 121-query wall-time，故只记录实现合同，不声称实机加速倍数。
- 同一 checkpoint 的 live A/B 完成 `12` 次 `life-advance`、推进 `30` 个游戏日；全部因玩家已有 `12` hops route 及敌军追尾而
  正确选择 speed 1。它没有产生 speed-3 样本，故 adaptive speed 3 保持 `static-ready / production-live pending`。
- 最新 durable checkpoint 为 `date_raw=53209560`，SHA-256
  `A8DD4034C32856B8D1E05D6B834BBBF3C51AA74DA038BB22A0CA23A998AD76CF`；CharacterID `29829` 与原 episode 仍是后续 G1 入口。
- timeline wait 的 lean snapshot 针对另一项真实复制热点：历史约 `30` 次完整 history 复制累计 `18.58s`；局部确定性测量把
  `9` 次降为 `1` 次、`6.669s` 降为 `0.632s`。`79b8d2a` 于 08-28 00:01 提交并推送，当前只算测试/
  `static-ready`，同 checkpoint CK3 live A/B 尚未执行。
- 自然死亡后仍须等待琉焰卿 Mod 的 `ready=true / commit_serial=1` committed settlement，记录
  `one_life_settlement.final_score` 为人生分数，并核对顶层 `score`、`recorded_episode.score`、record persistence、cross-run record、
  零继承人 gameplay 与 cleanup；在此之前 G1 不完成。

## 08 月 27 日 21:05：pause blocker 已实机关闭，正式全寿命 run 继续

- 本轮主线提交 `7a89c58`、`4b82d5b`、`e23abe2`、`0848d61`、`726a1c0`、`75c67d2`、`aff784d`、
  `3bd8934`、`8efa23f` 均已 push；干净主线 HEAD 与 `origin/master` 同为 `8efa23f18c23dee0aff05b5606eb70de7bd6ca34`。
  这条线依次收录 production continuation、必要性/成本规则、两日 battle-hold 反例及最窄接纳、琉焰卿 committed life-score
  合同，以及三轮由真实长跑驱动的 composite pause 修复。
- `aff784d` attempt `20260827T112207Z-one-generation-3c7aa5e2` 在 `106/107` turns、`53` gameplay 与
  `17` checkpoints 后以 `expected 183, current 185` 停止；report/blocker SHA 分别为 `BC10E3DE...DBF42DB4` /
  `8FC4B407...B45028B`，最后可信 checkpoint 为 `history=2119 / date_raw=53198376 / AE73EFE1...C75B42`。
  `3bd8934` attempt `20260827T115837Z-one-generation-9bed68f0` 在 `47/48` turns、`23` gameplay 与 `7` checkpoints
  后又证明 speed-five public revision 可在完整 10 秒内持续变化；report/blocker SHA 为 `FF8D78C5...55A470A2` /
  `6DDD926C...2149C97`，可信 checkpoint 为 `history=2169 / date_raw=53199144 / 578B0289...5C38`。两轮角色/episode
  保持、无 terminal、cleanup 全绿，均保留为 immutable harness B1。
- exact DLL `51fe8cf` 的 `pause-map` 自身 fresh-read 并幂等提交 `paused=1`，不消费 wire public revision；Python public-CAS
  因此是已实证的饥饿根因。`8efa23f` 只让 composite `_pause_life_advance` 跳过该冗余预检，仍限一次请求/一次 ACK/同 deadline
  paused 后置验证，direct pause、query 与其它 action 不变。focused `10 + 2 subtests`、driver `128 + 102 subtests`、auto-run +
  gameplay bridge `201 + 48 subtests`、全 unit `1318 passed, 2 skipped, 882 subtests`，编译、diff check 与独立审阅全绿。
- 正式 run `20260827T122055Z-one-generation-aebccf6f` 从上述 `578B0289...5C38` cold checkpoint 恢复，使用
  `50000 / 604800 / 300 / 3` 合同继续 CharacterID `29829` / episode `native-29829-ee172aa720db`。它在
  `history=2176 / date_raw=53199504` 越过旧超时，并继续至 21:05 的 durable
  `history=2380 / date_raw=53203800 / 75,195,047 bytes / A75EF923D369E9E86DDCC20C6B59E9F104F03E2462C98E3FEF62BB197E0AA3A4`；
  随后 `history=2382` 又推进到 `53203848`。持久 history 推导 `186` successful / `109` gameplay turns、`25` checkpoints；
  final report 尚未生成，所以这些只是滚动口径。角色仍存活，terminal/blocker absent，runner/CK3 仍在运行；`GEN-012` 已关闭，
  但 cleanup 与 G1 尚不能声称完成。
- 自然死亡后必须继续等待琉焰卿 Mod 的 `ready=true / commit_serial=1` committed settlement。只有
  `terminal-settlement.json.one_life_settlement.final_score`、顶层 `score`、`recorded_episode.score` 三者严格相等，并同时通过
  record persistence、零继承人 gameplay 与 cleanup，才记录该“人生分数”并标记 G1。

## 08 月 27 日 15:54：100-turn strict 续跑证据

- run `20260827T074858Z-one-generation-0e20ca35` 从 `date_raw=53178504` 的 exact cold checkpoint 恢复，贯穿同一
  CharacterID `29829` / episode `native-29829-ee172aa720db`；完成 `100/100` successful turns、`49` visible gameplay turns 与
  `5` durable checkpoints，日期推进至 `53179800`，没有 recovery 或 terminal turn。
- 最终 checkpoint 为 `67,931,059` bytes、history index `575`，SHA-256
  `71F23BB9F735AE118E4580AE62A9B3CE4C22AEB2AF22A91447A0484FCE38C1BF`。角色仍存活，故 strict runner 正确输出
  `turn_limit / bounded_incomplete / ok=false`；唯一 sidecar kind 为 `run_bound_exhausted`，它是可继续的人工边界，不是新的 B0/B1。
- cleanup 的 session report、shutdown、tree-gone 与 driver-close 全绿，CK3 已退出。权威 report 路径为
  `C:\Users\xenoa\AppData\Local\Temp\xar-one-generation-canary-20260827T1511-claim51fe8cf-state\runs\20260827T074858Z-one-generation-0e20ca35\report.json`，
  SHA-256 `784B6D17AC7F6C220C5E234914E03FA2539CD801E29FAC4F5C2ED4A91D8E827C`；first-blocker SHA-256
  `70B7625263ED09EFD376A727C5CF7274D7038B525C9C8F5C44F0F572813206A3`。
- 本里程碑证明 strict runner 可在正常桌面持续推进并留下 durable recovery anchor，但自然死亡、结算、分数、跨局记录与零继承人
  gameplay gates 均未发生，绝不标记 G1。下一步从最终 checkpoint 直接进入 `50000 / 604800 / 300 / 3` 全寿命续跑。

## 08 月 27 日 15:04 GEN-004 claim_cb 白和平与双 profile canary

- 原生 war-termination / player-exit 树先完成同步，再交付 owner 允许的最小 counter-policy；未采用的 v2/campaign inputs、其它 CB、
  surrender 与完整原生效用均已记账，不把窄实现写成原生等价。宗教没有扩展，holy war 不进入本策略。
- exact-build options 增加逐 option typed final `recipient_response`，旧 DLL 因缺字段会被新 strict schema 有意拒绝；raw acceptance
  不代替 final decision。planner 严格绑定同一 paused frame/full WarID，顺序为 options → v1 terms → offer，门槛为玩家 primary
  attacker、`claim_cb`、战分 `[0,100)`、duration `>=365` 天、WP legality/validator/availability、无 hostage、recipient 明确接受、
  claimant=played character、所有 declared target claims 存在及 literal 可达；weak claim 合法保留。
- direct action fresh 重审全部门，ACK 不算结束。旧 WarID 消失才 `applied`，仍在则 `submitted_pending`；同日先推进一次，随后按
  `720 date_raw` 跨 restore cooldown。跨战争 100% enforce 永远更高优先；终审已修复 battle-control/auto-accept 可能抢先的问题。
- core `51fe8cf6cb55de5ca01db4ed215e0abff52213a6`：focused `322 passed, 170 subtests passed`，native CTest `37/37`；
  排除既知 sandbox `EnumWindows` 用例的全量为 `1297 passed, 2 skipped, 1 deselected, 879 subtests passed`。
- stable Z build 的 DLL 为 `1307136` bytes / SHA-256
  `F52203F2395819CCB7A37153DBD36AB9CC6F6E168F4B44D179D3979ABF939D7B`，injector 为 `39936` bytes / SHA-256
  `8A46DE3BFBF567E34BA99E61AEFA7F59DA248C4AE89791BB74E12820B4380B99`，路径统一在
  `ck3_autonomous_player/native_bridge/build-claim-white-peace-51fe8cf-msvc/`；`g4b` 构建源保留。
- helper `1fd0a94ca8d75b3a98724ca313862c4b6e200253` 以单脚本支持默认 legacy 与 claim profile，保持旧
  `480f287 + 旧 DLL` 独立组合；新 strict schema 不与旧组合混用。helper + runner 为 `15 passed, 2 subtests passed`；claim/legacy
  canonical dry-run 均 exit 0、no target、CK3=0。claim Execute 在 sandbox 被 host guard 正确拒绝为 exit 1、no target、CK3=0；
  正常桌面执行仍 pending，所以本里程碑只升到 `static-ready`。
- 报告提交 `075dd95` 已形成；15:05 root 实际调用 Git Credential Manager 执行 `git push origin HEAD:master`，但
  `wincredman` 无法持久化/读取凭据且没有 `/dev/tty`，命令 exit 1，远端未接收；未声称 push 成功。本周报继续滚动、
  不提前收口。

## 08 月 27 日 13:33 一代人 canary handoff 与 GEN-003

- 新增非破坏的 normal-desktop 20-turn canary handoff，固定 clean `480f287`、production6b checkpoint/driver、exact
  pipe/DLL/injector 与 `20 / 21600 / 300 / 3`。默认 dry-run；执行只允许 `xenoa / WinSta0\\Default` 与 fresh `%TEMP%` target。
- 最终判定绑定 target 最新 persisted report、stdout 全量 JSON、sidecar path/size/SHA/完整 JSON、10 项 terminal gates、cleanup 与
  运行后 CK3=0；20-turn 存活只算预期 bounded canary，不算 G1。helper `6/6`、关联 `13/13`、canonical dry-run 和独立审查全绿。
- production6b 的 stale episode-seed 仅登记为 G2 跨代债务；不影响单寿命 G1，不在 canary 前修改 source。
- GEN-003 在已冻结的 exact-build inbound reply/legality/acceptance 树之后，实现 allowlist-scoped reject-first / unique-accept 最小
  counter-policy。独立审查抓出并修复两项提交阻断：任何 pending 不得越过 100% `enforce-demands`；special-war payload 不适用不能
  冒充通用 non-war 分类。当前唯一 allowlist 是原版完整定义已审计的 `spar_with_knight_interaction`，未知/宗教/其它 stock definition
  均 fail-closed；原生 `ai_accept` 未采用输入与 typed definition/subtype 缺口继续入账。
- runner 新增 pending semantic delta 与 typed old-full-ID lifecycle gate，reply 现在计入 visible/dirty gameplay、保留有界结果并触发尾部
  checkpoint。干净主线 focused 为 `188 tests OK`，六文件聚合集为 `251 passed + 68 subtests`；全量运行 `1281 tests`，只剩既知
  sandbox `EnumWindows` Win32 error 3，另有 `2 skipped`。
- handoff 提交 `3846952`、pending reply 提交 `e4e0620`、进度记录提交 `236b134` 已建立；13:34 聚合 push 再次到达 Git Credential
  Manager，但 sandbox 内 `wincredman` 不可用且没有 TTY，远端未接收，未写成已 push。

## 08 月 27 日 11:16 主线更新

- current-event root/named Character scopes 已完成 production wire/static-ready；live readiness 保持 false。
- 两次 acceptance 均在 query 前固定崩溃于 `ck3+0x1DABD89`。当前 RED 使用 `CodexSandboxOffline` 和独立
  `WinSta0\\CodexSandboxDesktop-*`，而同日既有五个 live GREEN 均来自 `xenoa` 普通交互环境；已新增环境 B0 `GEN-008`，
  待 `WinSta0\\Default` 同命令 A/B，期间不停止离线主线。
- 所有者再次确认“exact-build 原生 AI 树先行、实现不必照抄”：梳理后允许最小 deterministic policy 先解除整局阻塞，未采用分支、
  输入与质量差距必须进账。本周现转事件降级选择与后置状态验证，然后接一代 runner。

## 08 月 27 日 11:34 事件 blocker-removal static 里程碑

- 在已冻结的 exact native selector 之后实现最小 counter-policy，不照搬：typed same-frame shown+enabled 候选 → 有替代时避开明确
  player-death → 有替代时优先 non-cancel → 最低 authored native index；所有缺失输入和偏差均进入 event decision ledger。
- 直接事件动作从 ACK-only 升级为旧 full event instance 必须消失/换代，并要求同 bridge/connection/episode、结束 paused；
  auto-run artifact 同时要求 `event_changed` 和 typed `event_selection` proof。
- 两组定向回归合计 `346 passed + 176 subtests`。全量 unit 得到 `1246 passed, 2 skipped, 854 subtests`；唯一失败来自当前
  `CodexSandboxDesktop` 的既有 `EnumWindows` Win32 error 3，不是事件 capability RED，未为其修改生产逻辑。
- `GEN-002` 已 static-ready，下一施工转一代人 thin runner；scope/event live 在正常交互 `WinSta0\\Default` 做同命令 A/B 后补齐。
- 本里程碑已形成隔离提交 `701b3f80081a564bc7350dd9905c644afc3f9580`；当前 sandbox token 因缺少 GitHub
  credential-manager/交互凭据而 push 失败，未冒充远端已接收。提交保留在隔离 clone，后续提交继续叠加并在可用凭据环境推送。

## 08 月 27 日 11:57 一代人 G0 runner static-ready

- 复用现有 pipe-before-launch/native-session/paused readiness/auto-turn/checkpoint/cleanup owner，新增 strict `one_generation` 合同和
  `native-one-generation` artifact wrapper；固定 cold seed save/driver state 会在首动作前归档。
- `turn_limit` 在 strict 模式改为 `bounded_incomplete`；唯一 GREEN 是本次 `death-terminal` 同时闭合 immutable episode、日期推进、
  settlement source/score、record persistence、cross-run record、no-heir 与 cleanup。first blocker/terminal 各有独立 JSON sidecar。
- 首轮审查修正后定向 `31 passed + 9 subtests`；扩大回归除既有 sandbox `EnumWindows` 环境失败外为
  `198 passed + 36 subtests`。`GEN-001` 由待施工推进为 static-ready，`GEN-006` aggregate terminal gate 亦 static-ready；
  自然死亡 production episode 仍未发生，不能写 G1。
- 最终代码全量 Python unit 为 `1264 passed, 2 skipped, 859 subtests`，同样只剩上述 `EnumWindows` 环境失败，没有新增 capability RED。
- 下一步在 `xenoa` / `WinSta0\\Default` 从最终 clean source 复制旧 production state 到新目录并重新 prepare/verify；先跑 canary，再由
  strict runner 的首个真实 blocker 继续施工。当前 token 没有非破坏性的交互桌面委托入口，所以本轮没有再次错误启动 CK3。
- 首轮审查发现 cadence 的 `365` 实为 eligible action 次数；和平每步约 30 天时会把恢复点拖到约 30 年。默认已改为 3，约为季度级
  恢复点。首阻塞同时改为 current-attempt first-write-wins，保留 append 前失败动作的 result/上下文；seed validation 失败也产出
  finalized report 与 sidecar，不能静默退出。
- checkpoint 提交后若完整验证失败，core 撤销旧同路径 recovery metadata；strict wrapper 改指本次不可变 seed checkpoint +
  driver state，避免把已覆盖字节误报为可恢复。提交前 preflight 失败不撤销完好的旧恢复点。
- 组合式 `auto_turn()` 的 typed outcome 返回前发生异常时，也按“planner 可能已经提交 checkpoint”回落 immutable seed；独立终审以先保存
  再抛错的真实可达 harness 确认该缺口已闭合。
- G0 runner 已形成隔离提交 `14902c980245305707915162be1d2a8ef1ffaae7`；12:30 的 `git push origin HEAD` 因当前 sandbox 缺少
  Git credential-manager/交互凭据而失败，远端尚未接收，未冒充已 push。

## 08 月 27 日补充：Ox Here 插单已收尾

- 发布 `ox-here-v1.0.2`，修复原生决议选项 hover 的 raw localization key；Steam/GitHub/fresh-cache 22/22
  身份已闭合，公开描述、thumbnail 与四张 media 均核验。
- 完整功能 source run GREEN；fresh-cache 八语言 cell GREEN，Korean 两个 tooltip 与到庭事件画面通过但冗余 observer
  marker 未出现，诚实保留该 cell RED。按所有者要求不继续为低优先级 mod 扩张验收工程。
- 本周主线入口不变：立即执行 current-event 非空 effect-indicator exact-build live fixture，再继续 structured
  scopes/effects 与事件选择生命周期。

实现提交范围：`547a537` 至 `9ff04ae`；本轮新增 `7a89c58`、`4b82d5b`、`e23abe2`、`0848d61`、`726a1c0`、
`75c67d2`、`aff784d`、`3bd8934`、`8efa23f`、`f4a044d`、`a8ff95f`、`7cb0b75`、`79b8d2a`、`e0688c7` 与
`9ff04ae` 均已 push。

## 本周目标

2026-08-27 09:47 起，本周最高优先级修订为：让 Agent 作为玩家，在无人代打下完整游玩一局的一代人过程。允许固定
production seed、简化启发式与显式能力债；优先处理会阻断时间推进、动作验证、存档恢复或死亡结算的问题。战争预测、完整
effects 和通用化仍持续打磨，但不再为了单项完美延迟首个一代闭环。

## 本周已完成的阶段成果

### 会话、恢复与 episode 基础

- native bridge 改为版本 adapter，修复玩家军队命令与 deferred movement/disband target。
- 完成最小化窗口 checkpoint 保存/冷恢复、episode 跨冷启动保持、immutable seed 新 episode 和一代结算窄闭环。
- managed native run 已记录 `210/210` 成功回合、78 个可见 gameplay 回合、75 个游戏日和每轮 cleanup。

### 既有战争与真实战斗

- 完成 objective/route/siege 的 typed observation 与路线 preview，能拒绝相交路线、暂停受威胁围城并恢复 split/merge/assault 控制。
- route ETA 与真实接敌帧一致；create-new contact 能读取 CombatID、Province 与双方原生 stored order，并在战中冷恢复后复查。
- ongoing battle frame、bounded planner hold、main ledger delta 已 production-live。
- active retreat 的 day-15 legality、full-side 与 owner-subset 动作/后置状态已 production-live。
- reinforcement assignment 只读分支已 live；normal battle terminal 已通过 journal/query 闭合旧 CombatID removal、战分与玩家 retreat。

### Campaign discovery、互动与事件

- campaign root context 已在 independent/vassal checkpoint 做 production 双查询与冷恢复；loaded feature manifest 已读到当前进程
  44 rows/29 runtime keys。
- pending character interaction typed query 已在普通 white-peace recipient 跨 checkpoint live；special subtype 已绑定 WarID/primary side。
- exact `pay_ransom_interaction` 已在自然 stock 场景完成 typed query→reject→旧 full ID 消失→时间推进/checkpoint；这是
  production-live 的 reject-first continuation，不是赎金语义或原生等价完成。
- 非宗教 definition-only notification fixture 已完成 query/query/fixed ACK/旧 full ID 消失，但仍只算 fixture-live。
- current event 已发布 stable definition identity、实际物化 option 和 typed indicator 静态实现；纠正 timeout/cancel 混淆，允许多个 authored cancel。
- 事件 Attempt1/2/3 均保留 immutable RED；Attempt4 已整体 fixture-scoped GREEN：seed/cold PID `22976/43140`，
  instance `17`、canonical key、`[0,1,3]` presentation 与 native index `3 cancel=true` 跨 checkpoint 一致；artifact SHA
  `690EB5EA...70B7B`。后继非空 Attempt1 又在 PID `23632/35364` 实读 `trait/add brave`、
  `stress/increase affected=false/critical=false` 与 death backing rows，artifact SHA `1DE73B16...8249C3`；full effects 与
  semantic choice 仍缺。

### 原生 AI 与项目治理

- 扩充战争、接敌、战斗 controller、撤退、增援、terminal、事件/互动等原生 AI 决策树与证据账本。
- 明确全能力 P0–P11 路线与五层完成定义；新增统一进度中心、日报/周报及持续更新规则。
- 按所有者最新规则取消日报/周报视频硬门槛；日报与周报只交付文字，视频缺失不影响收口。完整能力 show-off 改为每月
  27 日 00:00（Asia/Shanghai）随月报交付，采用英语主叙事与简体中文画面内字幕。
- 宗教域保持 owner-deferred，只允许圣战战争 OODA 与婚姻必要判定两项最小例外。

## 能力状态变化

| 能力域 | 周初 | 当前 | 仍缺 |
|---|---|---|---|
| save/restore/process ownership | partial | `production-live loop` | 高层 intent 的通用恢复。 |
| actual contact | route inference | create-new + cold restore `production-live loop` | join-existing、multi-compatible。 |
| ongoing battle/retreat | partial query | hold 与 full/subset retreat `production-live loop` | forecast、动态 join/leave。 |
| reinforcement | absent/partial | unassigned/asking `production-live primitive` | assigned ETA、join、reassign。 |
| battle terminal | absent | normal branch `production-live primitive/loop` | no-normal、residual、assignment-reopened。 |
| campaign root/features | narrow/fixed | `production-live primitive` | 更多 rank/government/feature 反例矩阵与开局动作。 |
| pending interactions | ID/count-level | typed query `production-live primitive`；exact `pay_ransom` reject continuation `production-live loop`；notification `fixture-live` | structured terms/effects、其它 natural stock/intermediary、semantic reply。 |
| current event window | ID/count-level | identity/presentation/cancel + bounded nonempty indicators `fixture-live` | stock/其余 indicator branches、selection lifecycle、scopes、full effects、semantic choice。 |
| 窄 `claim_cb` war termination | static/query-only | options→v1 terms→white-peace→WarID 消失→解散→保存/冷恢复 `production-live loop` | 其它 CB/角色、胜/降、完整 outcome utility。 |
| threatened-siege route selection | exhaustive exact scan | first-safe `production-live loop`；真实帧从 167 条查询缩至 1 次 preview | 不保证全局最短；只有实际战争 outcome 变差才重开质量策略。 |
| read-only driver-state persistence | 每条命令全量重写约 79 MB | 批量 durable suffix `production-live loop` | 三轮真实 query/action/checkpoint/close 已闭合；保留动作/失败 durable barrier。 |
| adaptive timeline speed | 战争 route 一律 speed 1 | speed-3 selector `static-ready`；此前 12 次推进与最新 `9ff04ae` 六次 gameplay 均安全回退 speed 1 | 尚无真实 speed-3 elapsed/safety sample。 |
| lean timeline snapshot | wait 中反复复制完整 history | `79b8d2a / e0688c7` `production-live loop`；life copy `9 → 1 → 0` | native life 本身仍约 3.6–4.6s，只有新实测证明阻塞时才继续优化。 |
| planning/termination transcript | planning `1` 次、termination query `3` 次完整复制 | `9ff04ae` `production-live loop`；query 约 `0.050–0.068s` | 不把 query 降本冒充 speed-3 或 terminal 能力。 |
| strict one-generation runner | absent | production-live loop；正式续跑已冻结 GEN-014 RED，未修改重放把最新 durable anchor 推进到 `53210760 / 79B71103...85F2`；bounded pause retry `static-ready / live pending` | 角色仍未完成 terminal settlement/人生分数；G1 未完成。 |

## 失败尝试与学到的事实

- Event Attempt1：Windows/PhysFS 路径过长，是 harness RED；缩短 root 并增加 preflight。
- Event Attempt2：`CEventWindowData+0x2C` 实为 timeout index，不是 cancel；cancel 必须按 authored index 读取
  `CEventOption+0x47A`。这是 capability RED，未通过降低预期掩盖。
- Event Attempt3：calculated event ID/runtime stats ordinal 是 process/playset-local 数值，不能作为 seed/cold identity；它们仍须在
  单进程同帧稳定。cancel/indicator/readiness 字段本身通过。
- loaded-effect preview 在战争退出路径已有真实崩溃证据，因此完整 exit-terms v2 保持 production disabled；不以 ACK 或猜测代替 terms。

## 当前未完成与风险

- P1 aggregate 仍未完成：reinforcement assigned/join、terminal 三类分支、forecast/Monte Carlo 与通用 battle policy 缺失。
- P2 尚不能从任意既有战争自主走到胜/和/降并处理战后；也不能从和平状态做完整效用宣战。
- P3 的 current event query 与三个特定非空 backing rows 已 fixture-live，但完整 effect/scopes、selection lifecycle 和
  semantic decision 仍为 false。
- adaptive speed 3 仍没有 production-live 样本：此前 12 次推进与最新六次 gameplay 都因玩家 `12` hops route 与敌军追尾
  合法选择 speed 1；不能为追求吞吐强行绕过玩家 route/contact 安全门。
- query/history 复制 B1 已经三轮同 checkpoint live A/B 关闭；正式续跑的稳态实测约 `4.81s/游戏日`：query 中位数
  `0.056s`、life-advance 中位数 `3.660s`、每三日 checkpoint 摊销约 `1.031s/日`。所有者已明确要求继续降本，现只并行研究
  能用前后基准证明收益、且不改变恢复语义的最小热路径优化；不得因此阻塞 GEN-014 实机复验和 G1 续跑。
- 战争外大多数能力域仍是 `visual-narrow`、`research` 或 `absent`；当前成果不能称为全游戏自治。
- 宗教通用域与 holy order 是所有者暂缓，不得借圣战/婚姻例外扩大范围。

## 本周剩余工作 / 下周入口

1. 提交 GEN-014 bounded pause retry，从 `53210760 / 79B71103...85F2` cold revalidate；通过后立即从更新 anchor 恢复正式 G1。
2. 保留 speed-3 gate；只在战局自然出现真实 `remote_enemy_route` 帧并闭合 elapsed/safety/post-state 时升级 production-live。
   玩家 route/敌军追尾门仍成立时继续 speed 1，不为制造样本强行提速。
3. 真实 B0/B1 才按对应 exact-build 原生树做最小修复；普通 bound 直接从最新 durable anchor 续跑。
4. 自然死亡后继续等待琉焰卿 committed settlement；核对并记录
   `one_life_settlement.final_score == score == recorded_episode.score` 的人生分数，同时闭合 record persistence、cross-run record、
   零继承人 gameplay 与 cleanup 后才标 G1。
5. 对不阻塞首轮完成的完整 effects、reinforcement/join、forecast、智能宣战及通用经济/家庭/外交精度继续记为 B2/B3。
6. 首次 G1 后处理 `GEN-009` episode seed 债务并重复一轮验证 G2，再按账本恢复质量与覆盖面打磨。

## 本周 evidence 与提交

- Attempt4 artifact：`C:\Users\xenoa\AppData\Local\Temp\xar-current-event-window-context-cea30a0-live-attempt4.json`，
  size `130779`，SHA-256 `690EB5EA188B0903281E5F5DFDA343DA795117EE0FB1C83C3FCDC7F572170B7B`。
- 非空 Attempt1 artifact：`C:\Users\xenoa\AppData\Local\Temp\xar-current-event-nonempty-indicators-attempt1.json`，
  size `136947`，SHA-256 `1DE73B16CBD90FE05112D60A7F09274E95FE1BAC8D18D79C2AF8A8A2BC8249C3`；seed/cold PID
  `23632/35364`，三份 checkpoint SHA `9E846D2B...5B88`，独立审计 PASS。
- 收口验证：focused runner `17/17`、shared bridge `17/17`、ABI `57/57 + 32/32`、fresh native CTest `37/37`；
  宽回归 `1203 passed, 2 skipped, 817 subtests passed`。
- combined source `cea30a067b1e`、candidate freeze `fee93d57fb23`、runner contract fix `9e9ebbad6475`、Attempt4
  evidence/progress/demo `d44ea328063c` 均已 push。
- 双语自动录制器、旧版视频规范、日报/周报模板与阶段录像索引提交 `d46a129` 已 push；本周又按最新要求迁移为月报视频制度。
- 首份完整能力月报、月度视频制度、manifest/storyboard、通用录制器与合成器提交 `be7fc8b` 已 push。
- 午夜报告与日/周计划会制度 `d9bf823`、非空 event indicator runner/tests/live evidence `06bde70` 均已 push；后者 focused
  suites `49/49` GREEN，Python 编译与 scoped diff check GREEN。
- 一代人 100-turn report：
  `C:\Users\xenoa\AppData\Local\Temp\xar-one-generation-canary-20260827T1511-claim51fe8cf-state\runs\20260827T074858Z-one-generation-0e20ca35\report.json`，
  SHA-256 `784B6D17AC7F6C220C5E234914E03FA2539CD801E29FAC4F5C2ED4A91D8E827C`；最终 durable checkpoint SHA-256
  `71F23BB9F735AE118E4580AE62A9B3CE4C22AEB2AF22A91447A0484FCE38C1BF`。结果为可恢复的 `bounded_incomplete`，不是 G1。
- `aff784d` / `3bd8934` immutable RED 与 `8efa23f` 当前正式 run 的完整路径、hash、checkpoint 和测试口径见本文件 21:05 更新及
  [`../one-generation-blocker-ledger.md`](../one-generation-blocker-ledger.md)。本轮九个提交均已 push，正式 run 仍在进行，
  不能提前写 final report、cleanup 或人生分数。
- 长跑性能提交：`a8ff95f` first-safe route + adaptive speed gate；`7cb0b75` read-only persistence batching；`79b8d2a`、
  `e0688c7`、`9ff04ae` 依次去除 timeline/life/planning/termination transcript copy。三轮同 checkpoint live A/B、全量
  `1341 passed, 2 skipped, 900 subtests passed` 与独立审查均已闭合；这些提交不共同冒充 G1。
- 最新 A/B durable checkpoint：`date_raw=53209704`，SHA-256
  `39379D0224788198FECCCA82DA4B7B7257DB7E1AEE6B3750F62AA845E312678A`；driver state `D47DAA...BDA`。最新轮
  12/12 turns、6 gameplay、6 queries、2 checkpoints、paused/cleanup 全绿，但 gameplay 全为 speed 1，没有 speed-3 production sample。
- 本周仍在进行；后续重大里程碑继续更新本文件，不提前关闭周报。

## 月报素材贡献（非周报必需交付）

状态：**本周阶段素材已归档；2026-08 完整能力月报成片已通过抽检**

| 项 | 当前值 |
|---|---|
| 历史阶段成片 | `artifacts/demos/2026-08-27/ck3-autonomous-agent-event-window-bilingual-20260827-004648.mp4` |
| 语言 | `English primary + Simplified Chinese subtitles`；含双语游戏 lower-third 与证据卡 |
| 时长 / 规格 | `170.700s / 2560×1440 / H.264 + silent AAC` |
| size / SHA-256 | `28455097` / `873498C21BD2795ED185B00C0579C118D944E5CE4BE2592B49C4E232CCDF4A81` |
| 同次 live evidence | `9B560BEBD5455002642DEEDB7572BC0CCFB226186625A06EA45F87DDFAC36A21`；seed/cold PID `85368/121352`；GREEN |
| 当月正式成片 | 见 [`../monthly/2026-08.md`](../monthly/2026-08.md)；中文字幕越界版已废弃，语义分 cue 的重制版已通过抽检 |

周报只要求文字；上述录像只是可复用素材，不影响本周报完成或收口，也不冒充 2026-08 正式月报成片。
