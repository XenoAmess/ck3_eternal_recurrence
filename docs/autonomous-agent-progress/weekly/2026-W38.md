# 2026-W38 weekly report

Status: **in progress**

Period: 2026-09-14 through 2026-09-20

Formal close: 2026-09-21 00:00 (Asia/Shanghai)

Weekly planning meeting: [`../meetings/weekly/2026-W38.md`](../meetings/weekly/2026-W38.md)

Opening branch/HEAD: `root-rebase-r345-20260909` / `3327e91e747deda4c3f6b6c820bb7435e7bc3509`

## Opening readiness

- CoA frontend navigation through lobby and bounded native tree inspection are production-live; the remaining editor route and clipboard matrix are the active owner-directed task.
- G2 is `1/8`; GEN-034 is `2/4`. Its narrow projection, evaluator and runner are static-ready; broad termination preview remains disabled after a real crash RED.
- Current round R639 and old R638 are terminated; CK3=0. Tianchao is on owner manual-test hold and promotional video is out of scope.

## Work completed

- Closed W37 against W1-W7 and opened the combined W38 plan immediately.
- Three managed attempts disproved the first featured-bookmark/Pick-Any selection model without using OCR, keyboard or mouse. The replacement MCP now activates vanilla `SetRandomPlayableObserverCharacter` at the exact live-tree path `4/0/1/0/1`, then requires independent proof that the default designer target `3/0/2/3` became enabled.
- The corrected random-playable and lobby-to-designer package builds; focused native/Python/official-MCP contracts and reconfigured full CTest `106/106` are GREEN. The initial 9 environment REDs were isolated to a cached nonexistent in-repository CK3 EXE path and disappeared against the exact installed EXE. Readiness remains `mcp-static-ready / live=false`; current DLL SHA-256 is `5573805A...A24456C`.
- The managed acceptance runner now requires the corrected capability and performs the complete route through official MCP, ending with a ruler-designer-scoped native tree. The owner has since prohibited starting/occupying CK3 and the screen; CK3 is absent, and live work stays queued until explicit release.
- After the owner lifted that hold with a shared-slot requirement, the CoA
  frontend runner was connected to the installation-wide launch mutex and the
  isolated state mutex. Both locks now cover launch through cleanup, so a
  competing owner produces a no-launch RED instead of a second CK3 process;
  the focused source-order contract is GREEN.
- The first locked attempt is GREEN through Ruler Designer. Official MCP
  selected the fixed random playable, opened the fixed designer entry and
  captured a 512-node ruler-designer tree; cleanup and lock release are GREEN.
  That live tree plus vanilla GUI source fixes the dynasty CoA edit leaf, so a
  new closed zero-input CoA-page action is static-ready with independent route
  and visible-page postconditions. Python normal/optimized tests and equivalent
  native `106/106` coverage pass; only this newest final transition remains
  `live=false`. Its first launch preflight yielded without injection because
  the shared CK3 slot was occupied by the auto-upgrade acceptance runner.
- The editor companion now forwards that one fixed zero-input action and the
  Vue UI verifies its exact after-route before reporting success. Frontend
  tests/build and Quarkus `11/11` plus package are GREEN; no desktop fallback
  or caller-selected GUI path was introduced.
- The final shared-lock run then closed that fixed action as a production-live
  primitive. Artifact `mcp-frontend-route-coa-page-live5.json` is 1,185,843
  bytes with SHA-256
  `6BE30B3C356CCE22D279FD1906BAFE6474451D7FFCB5EF2229E5B538A0106103`.
  Official MCP independently observed the CoA route and visible/enabled page,
  input and finish widgets; all zero-OCR/keyboard/mouse checks, Steam offline,
  lock release and managed cleanup are GREEN. This does not prove the
  upper-level ruler-designer Finish or persistence.
- A checked-in 15-case syntax matrix and opt-in phase now prepare the next
  MCP-only evidence pass without changing the runner's default route mode. It
  captures detect, optional apply and native Copy/export on the same designer;
  known-negative cases must also preserve the before/after export hash. The
  manifest is `ADFBB02A...F6BA13F`, and focused normal/optimized tests pass
  `4/4` per mode. This package is static-ready only: CK3/screen use is currently
  prohibited, so none of the exploratory outcomes has been promoted.
- The editor's live source binding no longer assumes a gameplay snapshot.
  Quarkus validates the exact native capabilities/connection and supplies
  either a positive snapshot revision or the native frontend revision zero;
  Vue uses it for connect/probe/export while runtime features retain the
  positive-snapshot gate. Frontend `38/38`, Vite build, Quarkus `14/14` and
  package are GREEN, with no CK3 or screen use.
- Exact vanilla corpus inspection narrowed mask values to region indices 1-3,
  consistent with the shipped R/G/B shader and prior MCP detection. The Vue
  form no longer silently drops invalid tokens and now blocks non-integer or
  out-of-range masks. Frontend tests pass `39/39` and the build is GREEN; no
  CK3 or screen resource was used.
- The Vue editor now continuously enforces the proven deterministic export subset after structured edits: variable resolution, top-level shape, color literals, printable-ASCII resources, finite numbers and the native 128 KiB limit. Vitest is `32/32` and the production build is GREEN; no CK3 or screen was used.
- Correction on 2026-09-15: 128 KiB is the current project-authored MCP bridge safety contract, not a measured CK3 native limit. Browser copy is no longer blocked above it; the page warns that only development MCP verification is unavailable, and image fitting no longer stops at that threshold. A visible progress bar now consumes exact Worker counts for background, coarse and refinement phases. Vitest `54/54`, headless Playwright `2/2`, production build and complete-pack verification are GREEN.
- Added a machine-readable syntax evidence table to the Vue UI. Its exact examples and accept/warn/sanitize/reject policies are covered by tests against the parser and serializer; the frontend suite is now `36/36` and the build is GREEN.
- GEN-034-C current round R640 stopped before launch on a missing system-Python dependency. Current round R641 completed the exact two paused native reads and full cleanup, then exposed an idempotence defect: the options normalizer emitted `source=native` but rejected that same public shape when the projection consumed it. The minimal repair and typed unavailable-path retention pass focused normal/optimized tests `18/18` per mode.
- After one no-launch basename correction and one retained cold-load timeout, new round R644 reached the frozen frame and preserved the expected typed native/final-recipient white-peace blockers. Exact-build, same-frame binding, the two-command read-only history, cleanup and source invariants are GREEN; no utility or action was fabricated.

## Current work

- Keep the prepared clipboard syntax-matrix runner queued for official MCP
  execution; do not execute it while CK3/screen use is prohibited.
- Prepare any G2 checkpoint tooling offline only; do not start CK3 while the owner hold is active and do not repeat the zero-day negative frame.

## RED / blockers

- R640/R642 remain no-launch harness REDs, R641 the repaired consumer RED, and R643 a cold-load timeout. R644 is valid negative live evidence, not a successful utility comparison. The broad termination preview's two crashes at RVA `0x334C668` remain disabled.
- CoA ruler-designer `live1`-`live3` are retained navigation/workflow REDs with cleanup GREEN. They do not classify any clipboard syntax; the invalid state model has been removed rather than retried.

## Range boundary

- No CK3 launch or screen use until the owner explicitly lifts the current hold. No online Steam, Workshop action, OCR/mouse/keyboard fallback, arbitrary native pointer/path interface, Tianchao publication or promotional-video work.

## 2026-09-14: GEN-034-C 135-day boundary and horizon preparation

- Current round R645 reused the nearest existing pre-action checkpoint and
  established that Raiktor white peace remains natively unavailable at war day
  135. Its two-query same-frame evidence, exact build, cleanup and source
  invariants are valid; the typed option/final-response RED is preserved.
- A focused managed runner is now static-ready for bounded preparation to day
  365. It resumes once and polls read-only state. At a first event it pauses,
  captures exact native event context and saves the unresolved frame; at the
  date bound it queries the native option once and saves only when admission is
  proven. It cannot select events or submit surrender, white peace or demands.
  Focused normal/optimized tests pass `4/4` per mode.
- Readiness remains G2 `1/8`, GEN-034 `2/4`. R645 and all older rounds are
  terminated, CK3=0; no MCP wire, native ABI or open_kaishek consumer changed.

- Current round R646 exercised the stop path: day 158 produced active event
  instance `7`, after exactly one resume. The run preserved RED, performed no
  event selection or exit action, and cleaned the only CK3 process. Report
  SHA-256 is `47B9F94B...8AA9`. The generic wrapper's misleading outer error is
  now replaced by the runner's typed event stop reason. One capture rerun is
  required because the original lossy snapshot did not include the definition
  key needed by the vanilla-event SOP.

## 2026-09-14：自动升级建筑四期超直辖策略计划

- Completed the Phase 4 requirements analysis for choosing “pause while over domain limit” or “continue building” during the enable flow. The plan is `research / plan-ready`; no Mod runtime, CK3 process, Steam state or Workshop item changed.
- The exact threshold follows vanilla's `domain_limit_available < 0` alert. A pause skips the complete player build pass without stopping the 15-day global loop, so returning to the limit resumes automatically on the next scheduled tick.
- CK3 allows only one custom widget per decision and Phase 3 already uses it for funding. The primary UX is one native 3×2 combined list, with a one-time live usability gate and a native two-option follow-up event only as fallback. Absence of the new pause flag preserves 3.0.0 save behavior.
- The implementation plus sampled live acceptance estimate is 3.25–5.25 hours. Full scope, migration, static gates and live matrix: [Phase 4 plan](../../auto-upgrade-buildings-phase-4-plan.md).

- Current round R647 closed that identification step through the official
  current-event MCP and saved the unresolved frame. It is exact-build vanilla
  `chancellor_task.1104`, whose sole enabled option and five Character scopes
  match the existing shared registry record. Report SHA-256 is
  `B9EC5A96...5DA7`; checkpoint SHA-256 is `01CAACED...49B2`. Source review
  confirms the only continuation grants a neighboring ruler timed positive
  opinion of the player. The live RED now narrows to the planner's missing
  consumer support for the registry's role-alias and distinct-role fields;
  no new event definition, native ABI or public MCP wire is required.

- The focused consumer now validates those exact-player, non-player, alias and
  distinct-character relations and freezes the event's five scope names/types
  plus native `[0]` projection. Normal/optimized tests pass
  `24 passed / 200 subtests` per mode, and offline replay of the actual R647 MCP
  context recommends only option 1 with zero failed checks. The event remains
  live-unresolved until the frozen checkpoint is consumed once.

- A managed registered-event continuation runner is now static-ready. It
  performs one official event-context query, one registry-authorized option and
  one checkpoint save while prohibiting time advance, broad preview and every
  war exit. It checks the same event instance/revision and requires unchanged
  player, date and active war after closure. Focused normal/optimized tests pass
  `14 passed / 7 subtests` per mode.

- Its no-launch admission is GREEN for the R647 checkpoint/driver pair and all
  executable/DLL/injector/product hashes, with report SHA-256
  `228692C3...AD65`. The report explicitly proves no CK3 start and no profile
  preparation. The owner then prohibited CK3 use, so no new current round was
  created and live continuation remains queued while offline G2 work proceeds.
- GEN-034-D now has a static-ready v2 recommendation path. It combines the
  immediate white-peace/surrender utility result with a same-frame measured
  power certificate and applies the already-versioned relation tail penalty to
  the continue baseline. It can map a unique production-live winner to one
  typed semantic action while keeping submission, postcondition and closure
  false. Focused normal/optimized tests pass `7/7`; no CK3 was used. D remains
  blocked on the C horizon, a same-frame power refresh and the bounded action /
  postcondition / checkpoint / cold-restore lifecycle.
- A bounded managed recommendation runner now folds that same-frame power
  refresh into the C horizon consumption: one options read, one terms read and
  two stable power reads, with no mutation or time advance. It requires one
  production recommendation/action plan and rejects any extra history row.
  Focused normal/optimized tests pass `4/4`; CK3 remains unused.
- The runner now also applies a pure pre-action gate. The gate hash-binds the
  recommendation to the exact current paused snapshot and advertised typed
  capability/action step, returning one expected-revision authorization with
  submission and postconditions still false. Related normal/optimized tests
  pass `18/18`; no public MCP/native ABI changed.
- GEN-034-D's generic state snapshot now publishes additive signed Q100000
  `played_character_prestige`, reusing the exact-build resource leaf already
  used before termination. This makes the player's prestige balance observable
  after the old WarID disappears. Native fixture and focused normal/optimized
  Python tests are GREEN; live action, delta verification and cold restore stay
  pending. No managed CK3 round was created or used.
- The D recommendation now freezes route-bound gold/prestige pre, delta and
  post values plus WarID/truce/loss/cold-restore expectations. Its completion
  gate is the six-item milestone contract; decision-only claim, prisoner and
  favor inputs no longer expand live acceptance. Focused normal/optimized
  recommendation/action-gate tests pass `14/14`, with no CK3 use or public
  interface change.
- The six-item D gate is now executable as a pure postcondition verifier. It
  binds the action ACK, successor paused snapshot, source-loss/truce evidence,
  and native save/restore results to the authorization hash. Only all-six
  success closes GEN-034; a verified continue leaves it open. The focused
  normal/optimized recommendation/action-gate/verifier suite passes `20/20`.
  This is static-ready only and did not use CK3 or change public interfaces.

- The two native postwar domains now have a pure source-bound composer. It
  requires exact equality between the retained source capture and the current
  full-generation persistent/current/CArmy set, then checks that same set is
  destroyed and the directional truce is stable across two consecutive reads
  on the successor revision. Named REDs cover no-truce and surviving rows.
  Focused normal/optimized tests pass `26/26`; no CK3 or public interface was
  used, and GEN-034 remains `2/4` pending the bounded live lifecycle.

- GEN-034-D now has a bounded managed action runner. It validates the unchanged
  paused frame and source/current generation sets before its one action, then
  collects cleanup, two persisted-truce reads and a checkpoint/cold restore for
  the six-item verifier. The private default-OFF cleanup dispatch now recognizes
  either same-WarID white-peace or surrender termination ACK, fixing its mismatch
  with the production driver's white-peace path without changing public wire or
  enabling surrender. Focused normal/optimized tests pass `29/29`; the enabled
  MSVC Release candidate builds. Readiness remains `static-ready / live=false`,
  G2 `1/8`, GEN-034 `2/4`.
- The owner has lifted the CK3/screen hold. The next work is one bounded managed
  GEN-034 lifecycle; its initial start and cold-restore restart must use
  consecutive rounds and the historical broad preview remains disabled.
## 2026-09-14：《重整河山》三期静态需求分析

- 完成 `0.3.0` plan-ready 分析，没有修改 Mod 运行时代码，也没有启动 CK3、占用桌面或操作 Workshop。
- 将后朝死亡继承列为 P0：当前动态霸权缺少原版同类 titular title 明确添加的 `single_heir_succession_law`，且 `0.2.0` 实机矩阵没有真正执行死亡用例。计划同时覆盖新建头衔和既有存档的幂等迁移。
- 将前朝官署延续列为 P1：保留九席和仍在旧朝的合资格 incumbent，只清退实际叛离／失格者；多个后朝使用唯一的“最近前朝官署”归属，避免九个全局 `e_minister_*` 在朝廷间争抢。正式 Grand Secretariat 与 `h_china` 专属权力明确不在本期范围。
- 完整实现顺序、兼容风险、MCP-first 验收矩阵和退出标准见 [三期计划](../../reclaim-the-motherland-phase-3-plan.md)。

## 2026-09-14：《重整河山》四期外交附庸平衡计划

- 玩家反馈证明二期只控制裂解瞬间的留朝比例还不够；后朝作为天朝制霸权级，会被原版外交附庸公式误认成现任中华霸权，从而取得国王拒绝豁免、倍增等级差和霸权加成。旧天子的其他正常优势只是在此基础上进一步放大，并非唯一根因。
- 四期新增独立工作包：排除后朝的三项现任中华霸权优惠，并为本次实际独立诸侯的主头衔保存五年、跨双方继承的 `-50`“新近自立”关联；不降头衔、不清零合法性、不硬禁外交统一。
- MCP-first 验收增加接受度固定向量、三个自然样本、一个极端外交样本、首年和平领土增量和 `51%` 门槛检查。四期估计 `9..14` 小时，三期范围和排期保持不变；当前仍为 `research / plan-ready`，没有 CK3、Steam 或运行时代码变更。

## 2026-09-14：Auto Upgrade Buildings 4.0.0 release-complete

- R0032 closed the six-choice funding/domain-limit matrix and representative Phase 2/3 regressions; R0033 closed the later custom decision-art delta in CK3 1.19.0.6. The exact 16-file build, release localization and deterministic manifest/ZIP are GREEN.
- Existing Workshop item `3800124956` now serves the exact maintained title/description and full Change Notes entry `1789335499`. Anonymous body readback is exact at 17 lines / 492 normalized characters. The upstream item was untouched.
- A pre-submit gate detected a concurrent task's CK3 process and stopped. Publication resumed only after that task's managed cleanup and a fresh no-in-game/CK3=0 check; no game session was forced or taken over.
- An initial fresh-cache request returned definite `No Connection`; after automatic reconnect, one retry produced 16 files / 970,267 bytes matching the formal manifest exactly. Steam returned Offline Mode without exit/sign-out and CK3 is stopped. Full hashes, CI boundary and evidence are in [the 4.0.0 changelog](../../release-changelogs/auto-upgrade-buildings/4.0.0.md).

- The bounded GEN-034 lifecycle did not close. R657/R658 preserved the
  application-main power-query RED; a hash-bound offline join then produced a
  valid `continue` recommendation from immutable R471/R657 evidence. R659/R660
  each submitted exactly one resume, but no successor appeared. R661 made the
  resume the first post-restore gameplay command, used zero fresh exit/power
  reads and still remained paused for 45 observations. Its report SHA-256 is
  `BD6682AA...5DA2`; source inputs were unchanged and cleanup was GREEN.
- Successful R655/R656 used a different checkpoint/source frame, so R661
  disproves command order and wait length as repairs. The RED is now confined
  to R459 fixture map-control execution; unchanged retries are stopped. G2 stays
  `1/8`, GEN-034 stays `2/4`, and active delivery moves to G2-M2's three
  production event loops while the fixture seam remains on the ledger.

- G2-M2 now has its first production recommendation/action/material event loop.
  The reusable bounded runner was added in `77a7cdb`; concrete action-step
  admission was corrected in `3a30488`, and `b4542f2` replaced an R663
  application-main query timeout with a checkpoint-bound source-query replay.
- R664 selected `trait_specific.8001` native option 1 on unique PID `124060`.
  Event instance `1075` closed, gold raw increased by `1500000` at Q100000,
  the material comparator returned `verified_change`, and a successor
  checkpoint was saved without date advance. Report SHA-256 is
  `AB718B94...BC63`; cleanup and immutable-source checks are GREEN.
- G2-M2 is `1/3` production event loops and global G2 remains `1/8`. The next
  work is one of the two remaining already-defined material event paths, with
  no single-event long run.

## G2-M2 R665-R670 bounded horizon disposition (2026-09-14)

- The generic horizon runner was added and corrected in six small, separately pushed packages: `1a600b6`, `9119cda`, `33dc070`, `25ef1ba`, `f27b62e`, and `89ae41f`. It now advances only at speed one, waits for submitted state to materialize, limits each pulse to one game day, and gives only declared event dates a bounded settle window.
- R665-R670 all stopped before the target selection and remain RED. The last report is `Z:\ck3_mod_rewrite_process_assets\g2-m2-r670-tgp0030-material-89ae41f\report.json`, SHA-256 `79FB89A4...ED3B`; it records an unmaterialized speed-one command, no resume/query/selection, unchanged source, and GREEN cleanup. The old R555 trajectory will not be restarted again.
- R374 ordinary autosaves were rechecked. Its closest pre-`.1007` durable save is roughly 231 game days before the event, so it is not a direct material-delta source. The earlier event-instance advance remains valid, while the stress delta is deferred to a natural re-encounter. G2-M2 stays `1/3`; global G2 stays `1/8`; current round R670 is terminated and no CK3 process survives.

## G2 R671 war-entry mailbox RED (2026-09-14)

- R671 restored the R664 successor checkpoint, returned seven declarable wars, then stopped before mutation/time advance when the first one-target native power query produced only the old generic application-main failure. The run and final driver-state hashes are `9257FEB3...B1D0` and `453669C9...E4AB`; cleanup is GREEN.
- The adapter now aligns its finite queued wait with the other production main-thread queries (`8000 ms`, with `2000 ms` executing slices) and reports the exact wait/completion/reader stage instead of collapsing infrastructure states. Focused native mailbox and source-contract tests are GREEN; rebuilt DLL SHA-256 is `6562CD13...1994`.
- The patch changes neither capability/schema nor the fail-closed policy. G2-M2 stays `1/3` and global G2 stays `1/8`; one short R672 replay is the only proportional live check.

## G2 R672 war-entry differential GREEN (2026-09-14)

- With the rebuilt DLL and all other frozen inputs unchanged, R672 completed the declarable-war query, the exact one-target power query, and one `NO_DECLARE` 30-day advance. It ended `turn_limit / qualified / ok=true`, saved checkpoint `8D918606...CCB4`, and cleaned CK3/injector to zero.
- Run log SHA-256 is `A803C1E4...BA6D`; final driver-state SHA-256 is `76AF995D...B654`. The live differential closes R671's operational blocker while preserving R671 as RED. No old-DLL replay is planned.
- This unblocks normal campaign progression but does not itself satisfy another registered-event material loop. G2-M2 remains `1/3`; global G2 remains `1/8`.


## G2 R673 cross-frame declaration-cache RED (2026-09-14)

- R673 advanced the ordinary campaign five times, then retained target_not_declarable when it assessed target 38436 from the original declaration query on a sixth native frame. Run log and final driver-state SHA-256 are CF92D2D...4DE3 and 30902F5E...BA0B; the last durable checkpoint is F2E84CF1...D27B, and cleanup is GREEN.
- The Python driver had same-frame bindings for strategic-power rows but only a query sequence for declaration rows. The repair binds declaration rows and their generated actions to the exact paused frame, connection and episode; the next frame now forces a fresh native declaration query. Focused tests pass 14/14 in normal and optimized Python.
- No public MCP/native interface or dependency changed, so open_kaishek is unaffected. G2-M2 remains 1/3, global G2 remains 1/8, and only one short R674 replay is required before normal campaign progression resumes.


## G2 R674 declaration-cache differential GREEN (2026-09-14)

- R674 bound fix commit c1d91c0 and the complete R673 checkpoint, passed no-launch preflight 4E0BC602...F03BE, then executed exactly three successful turns: fresh declaration query, current target power query, and one NO_DECLARE 30-day advance.
- It ended qualified with no blocker and saved checkpoint 4F72E469...61686. Run log and final driver-state SHA-256 are C5B37439...1CC09 and A6033D85...4567E; managed cleanup leaves CK3/injector at zero.
- R673 remains historical RED while its operational cache blocker is closed. No public interface or open_kaishek change occurred. G2-M2 remains 1/3 and global G2 remains 1/8; campaign progression returns to encounter-driven event coverage.


## G2 R675 ordinary campaign segment GREEN (2026-09-14)

- R675 completed 12/12 turns with four fresh declaration/power-query/NO_DECLARE cycles and advanced date 53787072 to 53789952. The legal target changed from 38436 to 83395 and was refreshed before assessment, with no recurrence of R673.
- The run is qualified with no blocker. Log, driver-state and checkpoint SHA-256 are D42D7EF7...32A5A, 0AD53440...1F53 and 223E4C65...16092; cleanup leaves CK3/injector at zero.
- No registered event appeared. G2-M2 stays 1/3 and global G2 stays 1/8; the cache repair has enough live evidence and receives no further dedicated replay.


## G2-M3 succession transition contract static-ready

- Added a pure pre-death expectation and post-transition reconciliation
  contract over the production-live per-title partition. It binds the living
  episode ruler and exact paused frame, then admits only a paused
  `played_character_changed` successor frame with a same-frame turn bundle.
- The comparator checks successor identity and only the predecessor estate;
  the successor's pre-existing titles are informational. Missing holders,
  succession law and claims remain explicit gaps. Focused normal/optimized
  tests pass `5/5`; no CK3, native ABI, MCP interface or action changed.
- G2-M3 moves from `not_started` to `in_progress / static-ready`; global G2
  remains `1/8`. Next is driver retention, real-transition reconciliation and
  successor continuation integration.


## G2-M3 driver-state retention fixture-ready

- The native driver now persists the latest strict succession expectation in
  its existing private v2 state. Same-PID recovery preserves it; cold/seed
  restore, Phase 2 source staging and operator rebind clear it. A fixture
  completes prediction, same-PID recovery, real played-character change and
  successor-estate reconciliation.
- Contract/driver tests pass `7/7`; three existing persistence/rebind
  regressions pass `3/3`, normal and optimized. No CK3, public MCP/native ABI,
  game file, launch configuration or load order changed. M3 remains
  `in_progress / fixture-ready`; global G2 remains `1/8`.

## G2-M3 real-successor continuation static-ready

The planner service now refreshes the persisted succession expectation on each
eligible paused living frame and reconciles it on the first eligible
`played_character_changed` successor frame. After the predecessor's normal
death settlement completes, the strategy selects
`continue-as-reconciled-successor` only when both the successor identity and
predecessor-title distribution match. The driver keeps the live campaign and
process, sends no CK3 command, clears character-scoped caches, and binds a new
one-life run to CK3's already-played successor.

A missing or mismatched reconciliation blocks this path and cannot fall back to
immutable-seed replay. Focused contract/service/strategy/driver tests pass
`10/10` in normal and optimized Python, including the existing distinct seed
replay regression. Status remains `in_progress / static-ready`; one bounded
exact-build natural-death artifact is the remaining integration gate. No native
ABI, MCP tool, game file, dependency, launch configuration or load order
changed.

## G2-M3 bounded runner continuation proof static-ready

A review of the actual production owner found that `native-auto-run` still
stopped immediately after every `death-terminal`, so the previously completed
planner/driver action could not execute in an ordinary bounded campaign. The
runner now continues after a natural `played_character_changed` settlement,
verifies the matched predecessor/successor transition, unchanged PID,
connection, frame and date, zero CK3 command/restart, and the new episode
identity before gameplay resumes. Its report preserves these facts in
`natural_succession_transitions`.

Strict one-generation runs still end at death; immutable-seed next-episode runs
retain their separate restart contract. Focused normal/optimized boundary tests
pass `4/4`, including both existing terminal modes. This package changes the
private runner report shape but no native ABI, public MCP tool, DLL, game file,
dependency or load order. M3 remains `in_progress / static-ready` and global G2
remains `1/8` pending one bounded exact-build natural-death artifact.

## G2 R676 celestial council scope RED and static repair (2026-09-14)

- R676 restored CharacterID `32904` at date `53789952`, then its first
  campaign-root query returned `council_unavailable`; no gameplay command or
  date advance occurred and cleanup was GREEN.
- The standard five-seat council predicate wrongly admitted the ruler's
  celestial government. The focused native correction treats celestial
  ministry as outside that component's current scope, leaving council typed
  unavailable while preserving the remaining campaign-root and turn-bundle
  inputs.
- Reader/source-contract/mailbox fixtures are GREEN and candidate DLL
  `D3D6AB71...B065DF` links. The real RED remains open only until one short
  R677 corrected-DLL replay; no death/event long run is added.

## Reclaim the Motherland 0.4.0 release closure (2026-09-14)

- Combined Phase 3/4 is release-complete on Workshop item `3798404599`: exact public title and 5,720-character checked-in BBCode, exact 542-character / 11-line Change Notes entry `1789357628`, four live-image HTTP checks, and required All Under Heaven DLC visibility all pass.
- The first native MCP submit preserved an `EResult=8` receipt for the over-limit 11,017-byte description and did not update the item. The 7,946-byte corrected copy was pushed at `ffb60e53`; a new reviewed operation then returned `EResult=1`.
- A forced fresh download produced 35 files / 1,006,824 bytes matching formal manifest `34A950D3...DD935`. Exact-cache `R0026-workshop` passed 36 ordered markers, MCP readiness, zero project diagnostics, protected storage, and cleanup; wrapper SHA-256 is `8C5E80D7...F49E4`.
- `R0025-workshop` remains a no-launch environment preflight RED caused by an omitted explicit migrated CK3 path. Steam is back in Offline Mode, the client remains running, and CK3 is stopped.
- Permanent release facts are in `docs/release-changelogs/reclaim-motherland/0.4.0.md`. The controlled result does not claim the unexecuted three-natural-campaign first-year distribution proposed as an observational Phase 4 follow-up.

## G2 R677 optional game-rule component RED (2026-09-14)

- R677 restored CharacterID `32904` at date `53789952`. Its first root query
  progressed beyond the celestial council boundary and then returned
  `selected_game_rule_tokens_unavailable`; no turn bundle, gameplay command or
  date advance occurred. Cleanup is GREEN.
- Run/driver/error SHA-256 are
  `0FC0C00D7FDE01F032C17956D086B0DF076D157A424018D4327D955B2F33028D`,
  `74676EC7CAC08AC8A5F8EC82852A833E5A6F1BDE0A4AA3866D2AC63804D60E08`, and
  `107628E983A0A46044B5D2626B883ACFCE22BA3819EBF49E84F505917EF131AF`.
- The candidate preserves campaign-root `available` plus independent fields
  when optional rule tokens are unreadable, publishes empty token data with
  component and aggregate readiness false, and permits a `partial` bundle
  alongside typed-unavailable celestial council. Candidate DLL is
  `68E3746D35601C3197165A91ED85C5E5AA5C362C5EEF882123487E52B2ED2B76`.
- R677 stays RED pending exactly one three-turn R678 differential replay; no
  R678 success is claimed here. A naturally occurring death remains a later,
  independent M3 gate and is not part of this short replay.

## G2 R678 executor timeout and mailbox partial gate (2026-09-14)

- R678 launched the commit `764e1c4` / DLL `68E3746D...B2ED2B76` candidate
  after GREEN preflight. Command-history entry `621` was
  `timeout_cancelled_before_execution`, which means the application-main
  executor never took the campaign-root request. The bounded attempt therefore
  finished `0/1` turns with no gameplay, date still `53789952`, no expectation
  and no root payload. Cleanup is GREEN and the old round R678 process is
  terminated.
- Metadata/run/driver/cleanup hashes are `DF7401C0...016BD`,
  `51D66A8D...11456BF`, `2CE3FA57...DE79BFD`, and `D7007C3D...FA303`.
  R678 remains RED and provides no live proof for partial optional readiness.
- A distinct post-run source finding fixes the next deterministic gate without
  rewriting the timeout: the campaign-root mailbox currently requires
  aggregate `ready=true` for `typed_available`, while the candidate validly
  publishes an available root with rule-token and aggregate readiness false.
  A completed partial result would therefore become `internal_error`.
- The mailbox predicate and focused fixture must be repaired before one short
  new round R679 replay. Natural-death succession remains the independent M3
  integration gate; no dedicated long run is added.

## G2 R679 cold-restore paused pump-stall RED (2026-09-14)

- The one permitted R679 differential used commit `a4bb5a5` with FIX1 mailbox
  partial admission and pickup telemetry. Its campaign-root request was
  published but ended `timeout_cancelled_before_execution`; no typed root was
  returned, so the FIX1 partial path was not observed live.
- Probe evidence localizes this attempt before reader execution: paused pump
  epoch `11208 -> 11208`, executor starts `0 -> 0`, and executed requests
  `0 -> 0`, while the heartbeat described the mailbox as installed, ready and
  paused. This is a new cold-restore paused-pump stall RED, distinct from the
  previously repaired mailbox admission predicate.
- The run completed `0/1` turns with no gameplay, date still `53789952`, no
  succession expectation and GREEN cleanup. Run/driver/error/debug/cleanup
  hashes are `63155AEE...3A52B`, `3D303A29...D1914`,
  `33DECEE4...34E1C`, `7BE36B3C...6BF5A`, and `9E13CC1E...4DE3`; probe hash is
  `86012956...0FBC2`. The old round R679 process is terminated and no current
  CK3 instance exists.
- No further round starts before a focused FIX2 candidate. Its single short
  replay must first prove request pickup and only then assess FIX1 partial-root
  publication, partial bundle derivation and expectation capture. M3 remains
  `in_progress`; the natural-death transition gate is unchanged.

## G2 R680 RED to R681 bounded GREEN (2026-09-14)

- R680 exercised FIX2 commit `324dab573f48b9ab3439a9a0eb0eb1c8edb2478f`
  once. The campaign-root request timed out before execution: pump epoch stayed
  `9849 -> 9849`, executor starts and executed requests both stayed `0 -> 0`,
  and the attempt completed `0/1` turns without gameplay or date advance.
  Probe/run/driver/cleanup hashes are `A1E9B898...82B997`,
  `4C34D4FF...C18DD`, `FCDB79A8...619EE`, and `F27BCDE1...61F1F`.
- FIX3 commit `080bc507ee9c2e1c2fa1decd787a3927c15a12e4` delays cold
  readiness until a pump epoch is observed after the current binding baseline.
  Focused normal and optimized tests pass `2/2` each. Exact successful epoch
  values were not serialized; R681 behavior proves that the fresh-epoch gate
  passed and the executor completed the typed request.
- R681 completed `3/3` bounded turns. Campaign root was `available`; celestial
  council remained typed `unavailable`; selected game-rule tokens were empty
  with component and aggregate readiness false; and the planner consumed a
  `partial` bundle. It captured succession expectation `32904 -> 88187` for
  primary title `16761`, ran two queries plus one `life-advance`, and advanced
  date 30 days from `53789952` to `53790672`.
- Final checkpoint SHA-256 is
  `CDCC3771B1179667ECD017817781B2A93A412EA82EC9455B08EECE170F2FBC61`.
  Acceptance/run/driver/cleanup-inventory hashes are
  `2BED139B...6B14`, `EFBFF36A...5C866`, `19421B49...40734`, and
  `086138C3...B2D1`; cleanup is GREEN and CK3/injector are zero.
- This closes the mailbox/cold-readiness operational blocker. M3 remains
  `in_progress` and global G2 remains `1/8`; one bounded naturally encountered
  death, actual-distribution reconciliation and continuation as the real
  successor remain the sole M3 integration gate.

## G2-M4 BUILD2/MIL2 and R682 DEV3B disposition (2026-09-14)

- BUILD2 work commit `fca3fd` maps to remote-master commit
  `228d0423d169bcf6007ff10bfcaf35c606188a83`. Its default-off private
  construction-candidate observer captures bounded opaque `0x28` producer
  rows without adding a public bridge/MCP/schema surface. The retained focused
  native suite passes `4/4`; delivery is `static-ready / live-pending`.
- R682 did not reach that observer. A harness-readiness RED admitted the first
  paused/map-ready frame before played-character identity was populated, then
  correctly stopped on `episode_character_id=null`. There was no UI input,
  date advance or save mutation; source invariance and managed cleanup are
  GREEN and current round R682 is terminated. The artifact manifest at
  `Z:\ck3_mod_rewrite_process_assets\g2-m4-r682-devcounty-observer-r639-7429d84`
  has SHA-256
  `9FB6C13F6352BEC9A568D4DFBF60508DFCA727DAD0FFD9D66308AE8A41FC3470`.
  Observer install, callsite hit and row mapping therefore remain live-pending.
- MIL2 work commit `62ab2cd` maps to remote-master commit
  `303891e24d43ebfb32d681920e86abc69e9e5d07`. It freezes the exact
  Character-root receiver and named signed-Q100000 evaluator lifecycle for the
  ten `military-preparation-summary-v1` inputs. JSON parsing and
  `git diff --check` are GREEN. Status is
  `research / evaluator-static-ready`; no native reader, MCP tool, planner,
  action or live snapshot is claimed.
- These packages add no public interface/dependency, so `open_kaishek` and MCP
  general assets are unchanged. Integrated-branch cleanup has removed five
  `open_kaishek` remote work branches, five worktrees and ten local temporary
  refs; its remote now contains only `main@1643d03`. G2 has exact-tip deleted
  twelve integrated remote work branches including BUILD2. Remaining G2 local
  refs/worktrees/clones, MIL2 cleanup confirmation, BUILD2's residual `.git`
  directory and Realm2's reproducible build-output disposition remain in
  progress.
- Global G2 remains `1/8`. The next M4 gates are a corrected bounded DEV
  observer capture and production typed readers before planner/action/
  postcondition loops. M3's separate natural-succession live reconciliation
  gate is unchanged.

## G2-M4 COUNCIL3/DEV4/MIL3/FACTION1 and R683 disposition (2026-09-14)

- Final rebase mappings are
  `8187d94b00284362089957736277590c039b31d0 -> d402a6e7f2e85222f606d4ac5b84a8634f64e164`
  for COUNCIL3,
  `498aba3f2b28f079e7df353c21f4ea74fb6104dd -> fcf4a3dfd32409ea9af6ff673ef021307054fde7`
  for DEV4,
  `c2502b1c2252edad008974b9987f0f59ea9802e6 -> 50b69df3e40115520dcc49c6a63ac8634101907d`
  for MIL3, and
  `2132f112d7aa9533d77c81a510a03ffa75345f2e -> bb5911cedc7af70c43312d16a88c75ec50aac4f6`
  for FACTION1.
- COUNCIL3's default-off paused candidate observer remains
  `static-ready / live-pending`. DEV4's stable expected-character readiness
  gate is statically GREEN and received live positive evidence in R683. MIL3
  is `research / private-core-static-ready` for the ten-value military summary,
  without public native/MCP wiring or live values. FACTION1 is an exact-build
  research/contract package whose first implementation gate is targeting-
  faction/member observation; it does not claim a gift action loop.
- R683 ran fixed commit `50b69df` on unique PID `89132` with watchdog
  `215232`. The paused readiness binding was GREEN with played and episode
  identity both `29829`; the construction observer installed with failure
  flags zero. Its bounded 60-second window nevertheless observed zero producer
  calls and zero captures. The outcome is
  `NO-GO / no_producer_return_observed`, not RED. It used zero UI input, forced
  calls, date advance or save mutation, then terminated with all managed
  process inventories at zero. The current self-contained artifact manifest
  SHA-256 is
  `C3D2301E647BBDEF1A7CFB33CF2795EA9905BFBC98671371DD2C5D66055DFABC`
  under
  `Z:\ck3_mod_rewrite_process_assets\g2-m4-r683-construction-observer-live-50b69df`.
  The former
  `177F4F3EAB41570F005132E294ADEEBBEEA6DD7E1BF704CF004DB0F00F846DF4`
  is the historical seal from before the four exact candidate binaries were
  copied into the artifact; the final manifest retains it as
  `previous_manifest_sha256`.
- The four work packages have completed branch/workspace cleanup: corresponding
  remote work branches, local work/integration/tracking refs, registered
  worktrees and independent clones are absent, and COUNCIL3/MIL3 temporary
  build directories are removed. All four final commits are ancestors of
  remote master. No public interface/dependency changed, so `open_kaishek` and
  MCP general assets are unaffected.
- Global G2 remains `1/8`; M4 completion is unchanged. The next construction
  capture must use a naturally producer-eligible bounded scene or yield to
  independent typed-reader work, without repeating a zero-hit long run.

## CoA MCP fifteen-case live syntax matrix (2026-09-14)

- The frozen CoA matrix ran once on exact CK3 `1.19.0.6` through the official
  MCP SDK after the native route reached `coat_of_arms_designer`. Interaction
  policy was MCP-only with OCR/keyboard/mouse all false. Ten positive cases
  were detected and applied; five negative cases were not detected and left
  the current native Copy SHA unchanged.
- Exact Copy-back established last-wins duplicate scalars, first-wins multiple
  outer objects, static-variable materialization, preserved `parent`, dropped
  comments, HSV-to-RGB canonicalization, applied `_default.dds`
  textured-emblem, applied empty body, and all-channel-mask omission. Plain
  text, body-only, effect syntax, template `list` and unknown fields remained
  non-executable at this entrypoint.
- Evidence is
  `artifacts/coa-clipboard-probe-2026-09-08/mcp-frontend-route-coa-syntax-matrix-live6.json`
  (1,130,288 bytes; SHA-256
  `0C5F2F88765224219F7F824DD9F1215E4D2B9EBAB024F0677B5F7506D8F5F84A`).
  It is overall GREEN with cleanup and both shared-lock releases GREEN; CK3 is
  absent afterward and Steam remains offline.
- The exact-source Release bridge completed `618/618` compile/link steps.
  Full CTest remains recorded as `118/125`, not GREEN: seven failures are in
  existing combat-v3/ZhongGuo source contracts and a war-entry mailbox test;
  the CoA-relevant protocol, adapter and injection tests passed. The matrix
  itself is GREEN and bound to bridge SHA-256
  `412A9A869A1B4E43DFE6CCF521D43806FC10EDA24F9B934B76C242BA04B3582C`.
- The editor now preserves validated `parent`, follows native duplicate
  precedence with a warning, rejects lossy multiple roots, and publishes the
  new evidence levels. Frontend tests are `39/39` and production build is
  GREEN. The implementation/capability report package is pushed on master as
  `3b47f6d8aa8aa34e25db3ae9d75c2cb5e5f543d5`. This advances only the tested
  designer working-state primitive; Finish/persistence, runtime CoA registry
  winners, native-pixel parity and cross-build support remain pending.

## G2-M4 MIL12/FACTION7 and pre-launch R688 harness RED (2026-09-14)

- MIL12 is on remote `master` as `432545e7`. It replaces the military private
  probe's synchronous one-second mailbox wait with cross-heartbeat terminal
  observation and delayed reclaim. Focused debug/release/mailbox tests are
  GREEN; the capability remains `static-ready / live-pending`.
- FACTION7 was rebased from `778eaac5` to remote-master commit `9dea3de4`.
  Its private probe provides typed ready/known-empty/unavailable faction target
  rows with stable leader/member IDs. Normal/optimized native and source
  contracts are GREEN; shared bridge, MCP, planner and live evidence are still
  missing.
- Their remote/local temporary branches, clones and build trees were deleted
  immediately after integration. Root and remote `master` both point to
  `9dea3de4947ad70c4db0da484ee90cfcdf080d38` at this checkpoint.
- R688 did not start CK3. The frozen wrapper supplied a malformed named-pipe
  prefix and the driver rejected it before process creation, so no PID or raw
  military result exists. The preserved pre-launch harness artifact manifest
  is `ECAC52912F42D863671B3332AC79FABA1A80A7F774F28A9746FC5E38C98DFAC5`.
  CK3/helper inventory remains zero; the old candidate was not retried. A new
  reusable wrapper-argument contract and hash-bound candidate are in progress
  before the next actual current round R688 launch.
- Council9's no-launch R689 prep is sealed under
  `Z:\ck3_mod_rewrite_process_assets\g2-m4-council9-r689-prep-a04ee02` with
  manifest `E478BEDFBE97084C56FADBB71AE673573D2D4DE5EFFD7138A3C5F52CCDE81A66`.
  It has not acquired CK3 ownership. Public interfaces are unchanged, so
  `open_kaishek` remains unaffected. G2 stays `1/8` and the preserved R686
  military capability RED remains the immediate M4 live blocker.

## CoA dynasty Finish MCP round-trip (2026-09-14)

- The CoA editor now exposes a closed fixed action that commits the dynasty
  coat of arms from `coat_of_arms_designer` back to `ruler_designer`; native,
  Python MCP, Quarkus REST and Vue integrations are pushed at
  `8231b2f1139970d4c3a8eb8191309255b61784c3`.
- Focused gates are GREEN: Python 18/18, native CTest 3/3, Vue 40/40 and build,
  Quarkus 15/15 and Maven test. The live run used no OCR, keyboard, mouse or
  pixel interpretation.
- Exact CK3 `1.19.0.6` completed apply → Copy → dynasty Finish → reopen → Copy;
  the two 330-byte canonical results share SHA-256
  `4769FD42836E68FA35DC07FBA23BEABCC589E5A33087314F1D6287E5C53C305A`.
  The 1,594,848-byte GREEN artifact SHA-256 is
  `6569F652DB057520EB24A2DB3CC9FE2CD8F8C2A6BE8E76597F8F244BE6EFCEB8`.
  Cleanup, tree-gone, lock release and Steam offline are all proven.
- Scope remains ruler-designer submission only. Whole-ruler completion,
  campaign/save persistence, native pixel export and the effective runtime CoA
  registry winner remain open; no G2 capability count changes.

## R688 military GREEN, R689 council harness RED and G2 integration cutoff (2026-09-14)

- Current round R688 ran exact CK3 `1.19.0.6` with bridge commit
  `432545e7d65e56550d56cbdeda9d1f762f0ac71c` and canonical Python harness
  commit `5b2d487426ef04a53782e94b61f99ccd4915c243`. Its one paused private
  heartbeat returned `available`, `failure_flags=0`,
  `observation_ready=true` and one published execution for CharacterID
  `29829`. Q100000 current/max strength was
  `135600000/142200000`, knights `1200000/1500000`, and MAA gold
  expense/min/ideal/max was `20435/25000/50000/60000`. This is private live
  observation GREEN, without a public bridge/MCP/planner/action claim.
- R688 performed zero UI/gameplay actions, date advance or save mutation.
  Cleanup removed PID `85768` and watchdog `148360`; current round R688 is
  terminated and the final CK3/helper inventory is zero. Its sealed artifact
  at `Z:\ck3_mod_rewrite_process_assets\g2-m4-mil15-r688-live-5b2d487` has
  artifact-manifest SHA-256
  `09151A61FE8C5506579742EB34DFE48AC5692183F7F07D0D8F8EA1A9F92C7A85`.
- New round R689 launched candidate
  `a04ee02a04a3a86bb685395270d8cd23359b8ba2`, then stopped on
  `TypeError: _wait_for_readiness() got an unexpected keyword argument
  'expected_character_id'`. The RED is classified as `harness_red`: it occurred
  before readiness/private-heartbeat collection, no raw probe was persisted,
  and the council capability remains `not_observed`. It performed no input,
  gameplay, date advance or save mutation. Cleanup removed PID `211496` and
  watchdog `103512`; current round R689 is terminated with zero final
  CK3/helper inventory and no same-round retry. The sealed artifact-manifest at
  `Z:\ck3_mod_rewrite_process_assets\g2-m4-council9-r689-prep-a04ee02\live-r689`
  has SHA-256
  `C35876FDD3A817E872DF7987622C2B62FC56BEADF4E011A8A24CF98EE161939B`.
- Remote `master` reached the report cutoff at
  `e86d3fbb34d4e9f978cb3b9a95cfedab2131beec`, including the MIL14 harness,
  LIFE2 private observer core, MARRIAGE1/DIPLO1/SCHEME1/LAW1/CULTURE1 native
  trees and FACTION8 asynchronous private glue. They are research, static or
  private-observation inputs, not completed gameplay milestones. Global G2
  remains `1/8`; R688 closes only the private military observation blocker,
  while public military consumption and the R689 council harness/live gate
  remain open. Public MCP and cross-repository contracts are unchanged, so
  `open_kaishek` is unaffected.

## COUNCIL12 integration, R690 harness RED and R691 launch boundary (2026-09-15)

- COUNCIL12's two work commits map through rebase as
  `16697858 -> 0f9889288430b005eb5977e4072d7c9d04b3f15f` and
  `3e43a410 -> a905af184798439a41cdc6f63bfeb47d67f62608` on remote `master`.
  The following REPORT12 report close is master commit
  `295a684ba1b1b21fc62516615265e88ab4967502`.
- R690 used exact CK3 `1.19.0.6` for one paused, read-only council attempt. It
  stopped when the runner called missing driver method
  `take_internal_semantic_snapshot`. No raw private probe or council
  publication was captured, so the retained result is a **harness RED** and
  the council capability remains `not_observed`; it must not be reported as a
  capability RED.
- R690 made no UI/gameplay input, date advance or save mutation. Cleanup is
  GREEN: CK3 PID `199000` and watchdog PID `208880` are gone, process inventory
  is empty, and source/target save hashes are unchanged. Its preserved report
  SHA-256 is
  `E2D562A9BC88BFEA1752AAE55473B1148824B019917138FB3BF1B34D55C82F50`.
- DEV14's R691 construction candidate is static/no-launch and integrated as
  `15582df862bfe8179bed03db8e9b751059bc597f`. A compatibility check found the
  same unavailable driver method in its runner, so it was not started and no
  new round was allocated. No CK3 instance is current; the next actual launch
  identifier remains R691.
- The Council RED remains open. G2 stays `1/8`; no raw council capability,
  public MCP, cross-repository contract or `open_kaishek` change is claimed.

## G2 integration, CI recovery and branch cleanup (2026-09-15)

- Integrated the READY Activity2-5, DEV15-21, DIPLO2-6, GOV2-4,
  COUNCIL16, DECISION1-8, SCHEME2-10, MARRIAGE6-8, LAW8 and CULTURE2-4
  stacks through rebase-only, fast-forward master updates. The final code head
  before this report closeout is `b9f210f8830d25813baf1366dcab6eece40d1bd7`.
- CULTURE2-4 maps to `ea9038e7`, `95842ca0` and `26fe0f5d`; receipt
  `b9f210f8` records the rebuilt candidate hashes. Slot 40 is bound to the
  private culture/innovation query while slots 36-38 retain decision, law and
  marriage and slot 39 remains reserved. The source and evidence boundary is
  recorded in [culture-innovation-ai.md](../../ck3-native-ai/culture-innovation-ai.md).
- Focused validation is GREEN: Python-only and static validators, Debug and
  Release Culture-enabled DLLs, Release default-disabled DLL, the culture
  mailbox test, and the shared main-thread mailbox test. GitHub Actions run
  `34922343898` completed successfully on the exact code head `b9f210f8`.
- Closed the CI RED chain caused by obsolete Windows automation and stale
  static source contracts. No failure was suppressed or reclassified.
- Cleaned 144 exact temporary clone, build and linked-worktree paths after
  proving their patches were present in master and that they contained no
  unique live/candidate evidence. Remote heads fell from six to two and local
  heads from three to one. The remaining remote heads are `master` and the
  unrelated long-lived `codex/mod-shiren-import`, which has 15 unique patches
  and was therefore neither deleted nor integrated into G2.
- The two preserved matching asset directories are the formal Council and
  Faction R692 candidates. No CK3 process is current and this closeout allocated
  no new round. The Council candidate remains unexecuted; global G2 therefore
  remains honestly `1/8`, with most recent packages still static-ready or
  private-candidate primitives rather than complete production-live loops.
- Public MCP schema, protocol and dependency versions did not change, so
  `open_kaishek` needs no compatibility update for this integration.

## R692 sealed Council candidate RED (2026-09-15)

- Current round R692 executed the sealed Council candidate once on exact CK3
  `1.19.0.6`. The private result was
  `unavailable/application_main_thread_required`; counters froze at
  `submit=0 / wait=4 / executor_started_requests=0`, so no steward-candidate
  vector or council adjustment proof exists. The RED remains open.
- The paused read-only run made no UI/gameplay input, date advance or save
  mutation. Both save hashes remained
  `9104CCB8AE9D5776166FBBAEDA9B43BD08CBAA2CB5C057332EB8B7A1A212CC63`;
  managed cleanup proved CK3 and watchdog absent. Current round R692 is
  terminated.
- The immutable post-run manifest SHA-256 is
  `589FFE1526F3C98DFC5C04E5F0C8A48EC41D6DB893CC5497AE459781D4F2FFCB`.
  One-time helper `seal_r692_red.py` has `reason=现场封存`, impacts only the
  evidence manifest, and has no product reuse plan. Its migration deadline is
  this report package, where the manifest replaces it as the durable record.
- Formal strategy consumed nothing from this failed private result. G2-M4
  remains `in_progress`, with council action and independent postcondition
  still open; global G2 remains `1/8` (`12.5%`). No public MCP or
  `open_kaishek` compatibility surface changed.

## R693 private Council reader GREEN (2026-09-15)

- New round R693 ran sealed candidate `g2-m4-council18-r693-5e0c5d5` once on
  exact CK3 `1.19.0.6` from source commit
  `41bd2d5d0c159676876f21adc3845cf0afeeaf97`. One application-main request
  started and executed; the private steward reader returned `available` with a
  complete 11-character vector, released its temporary native vector and
  persisted no raw pointers. This is the first usable exact-build live result
  for this private reader and closes the R692 capability RED.
- The candidate remained paused and read-only: zero UI/gameplay actions, no
  date advance, unchanged source/target save hash
  `9104CCB8AE9D5776166FBBAEDA9B43BD08CBAA2CB5C057332EB8B7A1A212CC63`,
  and cleanup with no surviving CK3 or watchdog process. Current round R693 and
  old round R692 are terminated.
- The sealed post-run manifest SHA-256 is
  `FC59D8D8F001B6B6F6A5ACD041EFBA78596F7D837A54819A8B3F3A2E3EAFABC4`;
  raw probe and report SHA-256 are
  `C185D49F3DB17FF4CB3CE2129FF3DA611E6DC65F0E1DA33D8DA004B8A87FE40D`
  and `F5A9518885398B9503B587164F174E6E9F18345390BF022381A1DEE85B28D297`.
  The one-time sealing helper affects only this immutable evidence inventory,
  has no product reuse plan and is superseded by the manifest at package close.
- Formal strategy consumed nothing and no Council action/postcondition was
  attempted. G2-M4 remains `in_progress`; a public/formal candidate query,
  legal assignment, independent postcondition and next-cycle result
  consumption are still required. Construction and a real vassal/faction
  intervention also remain open, so global G2 stays `1/8`. This evidence-only
  package introduces no public interface and no `open_kaishek` adaptation.

## Council formal-loop implementation closeout and handoff (2026-09-15)

- The final in-flight Council implementation packages reached master linearly:
  Python formal action consumption at `a7756cd5`, and native application-main
  shared glue from work tip `0104b097` to rebased master `42acfb60`. The latter
  kept an equivalent patch across rebase according to `git range-diff`.
- Focused validation remained GREEN: Python normal/optimized tests, native
  Debug/Release 3/3 CTest, glue fixture 5/5, source verification and the
  Council22 exact-build ABI verifier. Validation was limited to the affected
  scope; no redundant full CI or long-running CK3 acceptance was started.
- This closes the static implementation handoff, not the product loop. Runtime
  bindings for already-councillor, guest, pending interaction and replacement
  fireability are still required before query/action advertisement and the next
  real formal-strategy candidate. No new CK3 evidence was created, so the fixed
  whole-game milestone total remains `1/8` (`12.5%`).
- `open_kaishek main@c88206e5` remains paired through passive, false-by-default
  profiles. All completed Council23/24 temporary branches, clones, build
  directories and tracking references were cleaned after integration. CK3
  process inventory is zero; current round R693 and old round R692 are
  terminated. The successor entry point is
  `docs/handover/2026-09-15-g2-real-council-loop-handoff.md`.

## Whole-campaign critical-path delivery plan (2026-09-15)

- Added a dated critical-path plan that freezes non-blocking work and sequences
  Council, war exit, natural events, succession, peace governance, joint
  family/diplomacy/war policy and long-campaign qualification.
- Planning targets are 2026-10-23 for the first conservative 1066→1453
  candidate, 2026-11-06 for a two-seed standard-feudal “reasonably normal”
  result, and 2026-11-20 for live-only B0/B1 risk. Broader government/DLC and
  formal `8/8` qualification remain later work.
- This was documentation-only: no CK3 launch, interface change, live evidence,
  `open_kaishek` adaptation or G2 milestone state change.

## Runnable-preview and real-Council handoff resumed (2026-09-15 16:36 Asia/Shanghai)

- The owner reprioritized delivery during the day. This section records the actual mid-day change and does not rewrite the 00:00 weekly plan. Runnable preview is not yet deliverable: the ordinary-production R675 evidence has date/query turns but no nonempty official action with independent result and later consumption, and no frozen cold restore. Council R693 is a paused private query primitive only. The fixed G2 contract remains `1/8`; GEN-034 remains `2/4`.
- The direct preview Ctrl+C/checkpoint B0 is integrated at master `1cfa2d7` with focused normal/optimized 63/63 tests. Bounded GEN-034-D pending white-peace handling maps `461fb31` to master `d8cfb7f`, focused 23/23 in each Python mode. Private Council26 application-main status/query transport maps `2643bbef` to master `76a3da2`, focused Debug/Release 3/3 each. All are static fixes; none substitutes for a gameplay result, cold restore or G2 gate.
- Council25 four exact-build predicates/replacement fireability work branch `b364e18` and independent ABI manifest `307946c` are pushed and awaiting integration/live acceptance. A bounded read-only private query against the known feudal R693 source save is being frozen as the next Council live candidate; /root alone will allocate and operate the sole CK3 round after whole-managed-environment process checks. Public Council capability remains unregistered and unadvertised.
- Exact focused M2/M3 tests passed, but natural event/heir evidence is still absent. Player construction producer ABI and M5 private marriage ranking are active distinct work packages; unhooked inventory is not a delivered policy. No CK3 round was launched in this update, so actual gameplay progression is zero and long-run throughput remains unknown.
- Integrated temporary remote work branches and root integration worktrees were cleared. Automatic approval review rejected recursive deletion of three clean independent clones (`blocked by policy`); their owners retain cleanup-blocked entries pending policy change. No public MCP interface or `open_kaishek` advertised behavior changed in these static integrations.

## R694 Council private query actual result (2026-09-15 17:21 Asia/Shanghai)

- R694 consumed a sealed, read-only native/Python candidate frozen at master `76a3da26` against exact CK3 1.19.0.6 and feudal `dev3b_r639.ck3`. The sole CK3 PID 137060 started at 17:17:46 Asia/Shanghai, completed a typed Council26 query/status inside its 60-second query window, and was tracked-cleaned with empty postflight inventory. Total wall time was 223.73 seconds; no game date advanced and both save hashes stayed `9104CC...212CC63`. Live report SHA-256 is `2C0241C0A87B5C486D9295CCACEEB9A4D6A3C5784A84E956417D1A89BBDCA71F` in `Z:\ck3_mod_rewrite_process_assets\g2-m4-council-r694-query-76a3da26-v2\live-r694`.
- Same paused frame returned 11 unique steward candidates, including Ivo full ID 33433 with stewardship 16 versus incumbent Roger ID 32716 stewardship 12, a real +4 candidate lead. Guest, candidate-specific pending and replacement fireability were not queried, and no helper assignment/formal next turn ran. Council25 exact static binding maps `b364e18→15cc5350`; ABI manifest maps `307946cf→ca8bb895` on master, but its four live gate classes remain open. A separate gate-only controlled query is being implemented to read all 11 native predicates without making a possibly legal submission. G2 remains `1/8`; preview remains unavailable because a same-source ordinary formal action/next-turn result and cold restore have not been established.
- The root ABI integration temporary branch/worktree was cleaned. Automatic approval review also rejected recursive deletion of the integrated Council25 root build directory (`blocked by policy`), leaving its local worktree/branch cleanup-blocked without a game dependency. Other rejected clean clones remain owned by their original workers. Public MCP/Open Kaishek advertised surfaces remain unchanged.

## Council27 gate-only acceptance path (2026-09-15 17:29 Asia/Shanghai)

- A default-OFF, unadvertised Council final-gate read-only step now evaluates all currently accepted candidate IDs on application-main and returns the full vector or typed unavailable without a helper action. The gate-only build leaves action runtime false; a separate default-OFF controlled submit switch remains closed until legal/deny/wait behavior is observed. Focused Release native tests passed 3/3, Debug 2/2; exact-build assignment verifier GREEN and Release DLL link GREEN. No Council27 DLL has yet run in CK3, so four live gates and formal next-turn consumption remain open.
- Initial isolated-worktree CTest failures were accounted separately: missing ignored EXE on a default relative path (`harness RED`), a test binary not built (`not run`), row-order assumption in the offline fixture (corrected and passed), and Cygwin Ninja choosing a broken MSVC path (`harness RED`, Visual Studio Debug passed). None was downgraded to a gameplay success. A distinct R695 gate-only candidate runner is in preparation. G2 remains `1/8`, preview still requires ordinary nonempty formal gameplay action and cold restore.

## R695 bounded Council gate observation and preview gap (2026-09-15 17:48 Asia/Shanghai)

- R695 used one frozen CK3 1.19.0.6 instance and the unadvertised gate-only Release artifact. Its independent paused query returned 11 complete native candidate rows: Ivo 33433 is not already a councillor or guest, has no pending interaction, and the current steward can be fired; 33435/34333/34867 are already councillors. No assignment helper ran, no date advanced and no save content changed. R695 terminated GREEN after 163.46 wall seconds with process inventory zero. Report: `Z:\ck3_mod_rewrite_process_assets\g2-m4-council-r695-final-gates-72882e69\live-r695\report.json`, SHA-256 `16428FCD73665A8E72670D7DCDF2E29328DF9C405AD8C13A3AE60054184F72F2`.
- A controlled typed replacement is now a ready-to-prepare bounded candidate, but guest-positive rejection, pending-positive wait, fireability denial, independent incumbent materialization, formal next-turn consumption and cold restore remain open. Public Council query/action stay unregistered and unadvertised. R694/R695 runner packages were rebased as equivalent patches and FF-delivered to master `eefc88e`/`9a23395`, with their verified-tip remote branches and clean integration worktrees/branches cleared. G2-M4 private construction view probe tip `f84629a` also rebased as four equivalent patches to master `bfa2fab`; exact-source and focused Debug/Release link GREEN, paused player view and actual construction still absent. Its remote work branch and root clean integration worktree/branch were cleared.
- The runnable preview remains blocked by a matching default Release DLL/injector, formal ordinary-feudal nonempty action/result/next-cycle consumption, and true stop/checkpoint/new-round cold restore. A paired G1 peaceful production seed and formal preflight exist but have not yet been played under the preview artifact. GEN-034 remains `2/4`; G2 remains `1/8`. No full-campaign game years or throughput were added by these read-only rounds. Recursive cleanup of several otherwise integrated source clones and build-bearing Council25/27 root worktrees was rejected by automatic approval review (`blocked by policy`); owners recorded these as `integrated, cleanup blocked` without blocking unrelated work. The default-OFF private probes have not changed the public `open_kaishek` or MCP capability advertisement.
## R696-R700 actual Council and runnable-preview gates (2026-09-15 18:34 Asia/Shanghai)

- Preview is still NO-GO until the new formal operator stop-request entry is verified with CK3 checkpoint, process recycle and same-goal cold restore. Frozen feudal agent eefc88e/default Release DLL 5143F1C6...FDF5 was eligible R697, then official native-auto-run R698 issued exactly one natural pay_ransom typed reject, saw pending clear and later consumed it without duplicate. First R698 detailed planner stdout was truncated, so this is command-history/official-route evidence. R699 true new-process cold restore from paired post-action checkpoint resumed same player/episode/goal for 5/5 turns and recycled CK3; full report SHA-256 003C95EC0223643B31B28502E08C9367152F5D308DAF4C969180CACAA98B84AB, final save SHA-256 13784F622609F0D8E33648F3B785562AE460346704A3308E936039E9D95F13E8. New stop-request CLI original c43bdb6 mapped to master d11268f, normal/-O focused tests 65/65 each GREEN. Python d112 and frozen DLL eefc are distinct candidate versions and live stop/restore are still pending.
- R696 private Council typed already-councillor rejection and one legal replacement led to incumbent33433 material in same process, but case-sensitive checkpoint SHA comparison produced retained RED; original report SHA-256 AAFDA2C3346A5A4F4E6966E77BD0C7CD15F4947CDD7C599CD64029D34C9950FB. Council29 fix old tips 25ed122/3b58c0c mapped to master 40cb49b/c31886f. R700 new-process gate-only read from physical checkpoint SHA-256 599B9EEE2E2C78A2AA341567FE5C52C36A1292AE1C5F8F00A231E2D7DCA27F64 independently saw incumbent33433 without action/helper/date/save change, GREEN and process recycle; report SHA-256 5D7E4034738A9A62A2D3C79DB623672F2BF88CD036EFF174FEEDFDBB5F4CFF11. Typed receipt/formal strategy next-turn/goal restore remain unverified. Guest-positive, pending-positive and fireability-denial scenes remain missing; public Council query/action unadvertised, G2 authority 1/8.
- Preview operator tools original c12ec1a maps to master b0ff511; stop patch c43bdb6 maps to d11268f. Both remote temporary work branches and root clean integration worktrees/branches/tracking refs were cleaned after FF delivery. Physical integrated clean tools clone deletion was automatically rejected as blocked by policy and stays assigned to preview_delivery for cleanup retry; no gameplay runtime depends on it. War terminal, natural events/heir, peaceful governance and two independent complete seeds remain active contract work, not satisfied by this preview.
## 2026-09-15 19:35 实际可运行预览包与战争 RED 增量

- 标准封建 exact CK3 1.19.0.6 冻结预览 ZIP SHA-256 0E233DCB024D1DF3E6EB1A3534F1D160CF27B4609CEC078CC2A1364551E44EB2 已可获取，外部交付清单 R706 后 GO、SHA-256 7F08E17C290F9E39D00F7145E7933D4FE54CBE3C09FED57FBA2E5E7BB240098D；启动/stop-file 控制暂停与停止/配对 checkpoint/新进程 cold restore 见 master@144956e 的 docs/ck3-native-ai/g2-preview-release.md。R701 同冻结 Python d112/native eefc 正式 20/20 自然 pending pay_ransom 唯一 typed reject、独立后置清空与下一 turn 消费；R702 正式受控 stop checkpoint 后 CK3 回收，R703 后时点 paused 公共查询证明 feudal，R704 同目标新进程 5/5；R706 实际 ZIP 解压正式冷恢复 5/5、date_raw 53190696→53192304、配对 checkpoint、回收。R706 无新增语义动作，复用 R701 而非把日期推进写成动作。完整 ZIP/轮次/证据和未支持范围在 g2-delivery-ledger.md 增量表；G2 仍 1/8，预览不代替首条/双种子整局。
- R705 战争 source 的真实 RED 为封建 steward/spouse 空席 task 导致 campaign-root council_unavailable，正式 turn1 0 战术动作；修复原 6769442 等价 rebase master f342232，Debug/Release 聚焦 fixture GREEN、远端及 root 临时 Git 引用/worktree即时清理。R705 游戏存档原 SHA 未变、driver 仅 restore/查询；gen034_war 正在用最终 master 新 DLL 与原安全配对 source 准备短实机复验，绝不把旧候选或白和平非法 R656 算 GEN-034-C/D 通过。GEN-034 仍 2/4。议会三个缺自然场景与正式消费、和平建设玩家 held holding 观测、自然事件/继承和家庭外交仍在关键链；R675 celestial 存档不属于标准封建议会取景。没有新增整局年份或长期吞吐观测，只有上述有界 date_raw/墙钟证据。open_kaishek 聚焦静态兼容未发生公开接口变化；被自动审批审查拒绝的独立 clone 物理递归清理按 blocked by policy 分列，不挡唯一 CK3 实机队列。

## R708-R710 实际关键链增量（2026-09-15 21:09 Asia/Shanghai）

- 已交付标准封建冻结预览 ZIP SHA-256 0E233DCB...4EB2；R706 后外部 GO 下载清单 SHA-256 C4AF9C...395E。R701-R706 正式非空 typed 互动、独立 pending 清空、下一 turn 消费、受控 stop、配对 checkpoint、ZIP 解压冷恢复的边界不变；整局首种子、双种子和正式 G2 仍待。
- R707 是旧 Windows-shell wrapper 的未启动游戏 harness RED；仅修改 wrapper 后 R708 从安全原 War 配对 checkpoint 用新 master f342 native DLL 6/6 关闭 R705 封建空席 campaign-root B0。R708 只有一条 typed 行军且下一 turn 消费，也自然出现 chancellor_task.1104 正式合法单选/事件窗口独立清空；原版调用链 a6535a4→master eb5bc32 文档集成，邻国好感物质 +30 未验。
- R709 同一 WarID/episode 真正新 CK3 进程 cold restore 6/6，date_raw 53187648→53188128，已生效行军目标 5604 继续 moving→embarked、没有重复 move 或事件选项，paired save 7D0C4059...DDC3/driver CD5A990C...AD76；War score0，白和 native false，GEN-034 C/D 未通过、2/4。R710 已从此真实配对源启动唯一 CK3 PID33020，40 turn/7200 秒有界 War 合同，物质状态/终局结果尚待。议会三个真实 final gate 场景仍缺、公共能力未广告。
- M4 closed holding_view typed read-only 补丁 7bf001d→master 64f6e59 等价 rebase/FF，并立即删远端/local 临时引用；default OFF，Python/native 聚焦双模式与 exact build 静态 GREEN，新 DLL 和 paused 实机未验。M5 标准封建独立合法候选只读场景已 no-launch READY，但五个未见。首条独立 1066 前缀 no-launch 检查已冻结，typed 新局/paused feudal/首次配对 checkpoint 仍是 B0，不能复制历史固定种子。G2 合同保持 1/8；无新整局年份/长期吞吐观测。M4 已集成独立 clean clone 递归物理删除自动审查 blocked by policy，temp refs 已清，其他关键链照常推进。
## R710-R718 连续交付增量（2026-09-15 22:08 Asia/Shanghai）

- 用户可取得 SHA 0E233DCB...4EB2 的冻结标准封建预览 ZIP，按正式入口自动游玩/受控 stop/配对 checkpoint/新进程 cold restore；R701-R706 是有界证据，预览不替代整局或 G2。R710 正式 War 40/40 在总墙钟 128.671 秒推进 176 游戏日，物质 war score -41/army 5604 siege；R713/R715 同帧公共只读 options/terms/两次兵力与三择一 continue 成功，未提交终局，GEN-034 C/D 仍 2/4。
- R712 slot42 M4 executor capability RED 经 master58e6f58 焦点双模式和 R717 同 paused private callback invocation/visibility receipt 真正关闭；closed holding-view cache0 不是零合法建设，m4_closed_view 补 exact read-only holdings 枚举。R716 是预建 evidence 目录的未启动游戏 harness RED。M5 五秒排队补丁 148bf0d→master8e07eac 焦点双模式后，R718 private callback 真执行却 native route_failure8、completion3，新的 capability RED 保留，feudal 源/目标 save 不变、30 原生 war rows/公共婚姻0、五合法联合候选未知，不盲重跑。
- GEN-034-D Robert 原版 event typed 消费 97b8292→master a5b954d normal/-O 41/41、临时 refs/worktree 即时清；特殊 CB 源捕获 observer DLL/event saved-scope 接线和自然 WarID 尚缺，D 未执行。Council guest/pending/fireability 三合法场景及正式任命消费仍缺，公共能力不广告；StartGame 1066 Bookmark 原生身份/首次 paired save 是首种子 B0。当前 CK3 进程0，最新实际 R718 RED，FEUDAL 编译原缓存继续，M4/M5/Council/War 独立任务并行；权威 G2 1/8、100年/首整局/第二种子均未验。default-OFF 变化目前不破坏已交预览与 open_kaishek 公开组合；旧 clean clone 物理递归清理自动审批 blocked by policy，已合入临时远端/local refs/root worktree 已清，不挡实机。

## R719-R720 并行关键链增量（2026-09-15 22:43 Asia/Shanghai）

- 已交可运行有界 preview 仍为 ZIP SHA 0E233DCB...4EB2、官方入口、stop/checkpoint/cold restore；R719 真正官方 NewGame→Bookmarks/native read-only tree 绿色、CLI0/194.984 秒，树512截断且 selected native identity 缺口仍阻止首个新 1066 种子。FEUDAL exact GameSetupView owner/view source 逆向施工；没有代点、角色猜测或 StartGame。
- R720 M5 新诊断制品同 paused native executor1 复现 ranked_source_unavailable/strategy_unavailable，CLI1 RED，222.779 秒，原/目标 save 未变、零动作/UI/日期、唯一 CK3 回收；五合法联合候选数未知。M4 player-model+slot42+候选 freezer 等价 rebase 后 master56bad/f729/054f，受控 paused 模型结果与正式建设仍待，remote/root temp refs/worktree 即时清。旧 R715 文字报告 root worktree/ref 本轮完整核验并清；独立 clean clone 物理目录自动审查 blocked by policy 保留清理阻塞。
- open_kaishek main64cd 当前公开 profile 与冻结 preview d112/eefc 兼容，三项 guarded/private OFF 增量无适配补丁；能力公开注册时重验。sole CK3 进程0，FEUDAL/M4/M5/War 四个非冲突 worker 在途，War 正准备下一有界正式候选；真实整局/第二种子无新进度，GEN-034 2/4，权威 G2 1/8。

## R721-R723 有界实机与关键缺口（2026-09-15 23:09 Asia/Shanghai）

- 用户可启动并冷恢复已交付 preview ZIP SHA0E233DCB...4EB2；R721 正式 War8/8 唯一 move8755/后续 turn 路线消费/配对 checkpoint，R723 新进程从其真实 cold restore4/4、同目标且历史前缀一致，旧/新 move 各仅一次，save31C34D24...4447/driver0A4DE82D...A25，CK3 回收。总推进 R721+R723 为3 游戏日/138 秒有界墙钟；这是功能窗，不外推整局吞吐。native route 首跳 ETA53192568，当前53192424，先前提前静止物质 RED 现场保留并更正到 ETA 前证据不足，不称物质 GREEN，C/D 仍2/4；围城强度0独立查 exact 原版。
- R722 M4 slot42 模型六条原生 barony 身份/绑定 true，closed-view definition0，CLI2 evidence_insufficient、0 动作/日期、公共 OFF；M4 查 world definition/native final legality。Council R695/R700 候选各11条无 guest-positive，三类最终合法自然场景与正式下一 turn 任命仍缺。自然 `.1007`/自然玩家死亡没有近点，现有正式健康/分封/事件窗口反应查询可用，继承人健康 unknown。FEUDAL selected model source 静态双模式 GREEN 但 key/政府/paused 互证和首个独立 1066 paired save 未闭合；M5 玩家族人 native marriage 查询仅开发中，五合法候选数仍未知。当前唯一 CK3 已回收，W12/A6 非冲突 worker 在途，权威 G2 1/8，百年/完整 campaign/第二种子未执行。

## R724-R726 关键链增量（2026-09-15 23:58 Asia/Shanghai）

- 可启动预览冻结 ZIP SHA0E233DCB...4EB2范围不变。R724 War 正式从 R723 配对点跨 native ETA，14/15、真实 army5604→5615、date+12游戏日；turn15 campaign-root read-only rejection RED，最后有效实存 save date53192568/driver history index135，未 claim新位置已保存。Python 合同 read retry 原 eb210355/cf47d80→master60144b8/1f117ab normal/-O6/6，R727 官方12turn/900s聚焦 cold 候选 READY等单实例。R725 M5 exact 玩家/首继承人 CanSend657、30 war rows同 paused真实，只读、source pair不变、公开候选仍0；answer raw0旧映射错误，raw0原版语义 B1修复/新paused核对待，五合法联合决策和行动未过。M4 exact world源39e25cd/freezer3c7beba私有R726当前唯一 CK3 PID83700，从R697/R722纯封建 pair只读定义与stock最终合法，尚无行动/广告。FEUDAL model source master0ecd静态，private wire链接/paused身份/新1066 paired起点待。G2权威1/8、GEN0342/4，百年/整局/双种子无新运行；受管 temp refs/worktrees已清，独立clean clone物理递归删除自动审批 blocked by policy标清理阻塞。

## 2026-09-16 00:06 rolling handoff

- 09-15日报按原D1-D7于00:04正式收口并立即写09-16早会，当前G2关键链优先。R726 M4 world private定义身份RED、原生legal未执行、报告SHA0A5C5545...35D7，源pair不变/CK3回收；M4 exact根因聚焦修。R727已从R724安全paired date53192568分配新轮次并启动 sole CK3 PID174812正式12turn/900s，结果待核；War root read RED仍开。冻结preview GO、GEN0342/4、权威G21/8，首独立种子/整局/第二种子未见。

## R727 正式短重放（2026-09-16 00:14 Asia/Shanghai）

- R727从R724安全pair新进程冷恢复官方12/12/196.704s、28游戏日、query9/gameplay3；Army50331653独立从5613→5615→5616、route缩短，下一正式turn选续已有目标，旧move一次/终局0。turn11 ArmyID从原生名单消失/score-41→-42，turn12原生终局查询available：真实因未知/B1 watch、目前非B0卡死。原root拒绝未复现且retry-live分支未执行，不宣称恢复了拒绝状态；新pair save51769DF4...3776/driverF1331051...BD90/index151/CK3回收。C终局休战与D仍缺、GEN0342/4、G21/8；预览GO边界不变。

## 2026-09-16 G2 incremental delivery (rolling, not formal W38 close)

- Bounded preview ZIP d112/eefc remains user-startable GO under its documented exact feudal range; formal G2 remains authority1/8 only M1, GEN0342/4. R728 six-turn official cold run ended predecision (evidence insufficient, no RED); R729 separate twelve-turn official cold run issued one new Army50331957→8755 typed move, independent province2629→2630 and next formal consumption, +8 game days/175.668s, paired save/driver index168, CK3 recycled. War terminal/truce and special D remain open; R724 read-only rejection retry-live branch did not trigger.
- FEUDAL private selected-model source final4223 and sentinel correction final8ea, M5 raw0 acceptance finalab, M4 `definition_identity` scalar diagnostic final4fc reached master by ordinary FF with focused checks; none is public advertised or same-final-source live. M4 exact short private candidate READY, FEUDAL and M5 final-source candidates under preparation. New 1066 seed, hundred-year prefix, full campaign, independent second seed still absent; natural `.1007`/same-campaign succession and Council three lawful scenes await normal paused anchors.
- Master mappings: FEUDAL f32fe858→4223a5cc and 0923b933→8ea06348; M5 2404647→ab7899c; M4 b21c08a→4fc2b9f. Reports and source temp refs/worktrees are cleaned per package; old physically clean isolated-clone deletion auto-review rejected blocked by policy and remains recorded cleanup-blocked. open_kaishek main64cd4e0 continues preview compatibility; gated private FEUDAL/M5 need public passive adapter only when typed contract and live gate close.

## 2026-09-16 01:19 rolling increment (not W38 formal close)

- Preview GO ZIP SHA0E233DCB...4EB2 remains user-startable within documented standard-feudal exact-build bounded range; G2 authority1/8 only M1 and GEN0342/4 unchanged. R730 actual M4 private county-definition identity RED, R731 FEUDAL selected Bookmarks owner-type RED and separate no-launch AllSigned harness RED were preserved. M4 county registry source fix masterac4402 and FEUDAL owner-chain source fix mastere655 passed focused builds, with same-final-source paused replay still pending and public capabilities OFF.
- R732 official War6/6,184.24s advanced7 game days and materially continued the already submitted Army50331957->8755 without duplicate action; pair date53193600/index178 saved, CK3 recycled. War end/truce/cold terminal remain evidence insufficient, so C/D remain open. R733 finalab M5 read-only five-native-family-candidate short gate is currently sole CK3 PID169796; score/action/postcondition have not yet occurred. M4 heavy Release paused for game resource; FEUDAL final-e655 read-only candidate queued. No first1066 seed, 100-year prefix, complete first campaign or second independent seed exists. This rolling update records actual state, not a new delivery date or formal week close.
- FEUDAL original5047->finale655 reached remote master by ordinary FF and temp original remote/root integration refs/worktree were cleaned in the same integration. M4 original267->finalac already integrated/cleaned. G2-REPORT-R730-R733 remains active until this increment plus R733 result is pushed/integrated/cleaned. open_kaishek main64cd4e0 stays compatible with frozen preview; private default-OFF fixes have no public consumer, public adaptation remains gated on typed contract/live proof.
## R733 M5 paused-pump RED (2026-09-16 01:22 rolling)

- R733 finalab private marriage read-only gate returned real RED before native candidate collection: its preliminary campaign-root mailbox request timed out before execution, pump epochs7851->7851/executor starts0/requests0. It is not a five-candidate legality result. Tracked CK3 PID169796/job/watchdog recycled after182.398s with both source/target save SHA9104CC...2CC63 unchanged, no gameplay/UI/date. Report SHA4C9F61B6...A81D is preserved; m5_legal_scene investigates only the actual paused-pump production-path failure. Public marriage remains OFF, preview GO/G21/8/GEN0342/4 unchanged. Next sole CK3 is final-e655 FEUDAL read-only gate after strict preflight; M4 resumed separate Release work after game recycle.

## 2026-09-16 01:38 rolling FEUDAL/M4 RED and external machine reservation

- Preview GO ZIP SHA0E233DCB...4EB2 remains deliverable in its exact bounded ordinary-feudal scope; g2-requirements-v1.json still 1/8 only M1, GEN0342/4. R734 verified official MCP NewGame->Bookmarks and native tree, but private selected model unavailable/frontend_setup_view_unverified; keys/date/government unobserved and no 1066 seed. PID9512/job/watchdog recycled after145.387s. FEUDAL owner is adding exact per-stage RTTI and unique named-root fallback before any new live gate/public advertisement.
- R735 M4 finalac same-source read-only replay proved prior county registry global also wrong: registry_count7/first vtable0x441EF38 exact RTTI CCourtTypeSetting, definition_identity RED; native final legality/action did not run. PID102076/job/watchdog recycled after177.299s, same actor29829/date53178312/revision3 and source seed. M4 owner traces true CBuildingType source in isolated code; no RED downgrade.
- M5 R733 actual paused-pump B0 repair original/final67cafd0 reached remote master by ordinary FF with normal/-O focused68/68; temporary remote/local work/integration refs were immediately deleted. New final67c read-only candidate SHADE3302BC...0CE9C strict no-launch READY, actual legal count still unobserved. War R732 +7 game days remains most recent material game progress; terminal/truce, Council three remaining scenes, natural events/heir, new seed and long-run remain pending.
- After R735 cleanup the user reserved this Windows machine CK3 and screen for manual use. All agents paused CK3 launch, UI and screen capture until explicit release; subsequent manual process state is unknown and must not be terminated. Isolated source/Git/report work continues; W12 nominal development slots excluding coordinator/outside unknown, A2 currently FEUDAL/M4 genuinely progressing, CK3 queue externally held. G2-REPORT-R734-R735 is an active narrow report package until master integration/cleanup. Private changes do not affect frozen preview or current open_kaishek main64cd4e0 pairing.

## 2026-09-16 static repair increment during user CK3 reservation (rolling)

- Frozen bounded preview remains obtainable/GO, formal G2 remains 1/8 (M1 only) and GEN034 2/4. User manual CK3/screen ownership means no new game round, gameplay, screen action or final DLL live seal; latest actual advance R732 +7 game days, no first 1066 seed/100-year gate/full campaign/second seed. R734 FEUDAL setup-view and R735 M4 definition_identity private REDs are preserved.
- FEUDAL exact GUI RTTI/named-root source repair original298c879→master0ab58c0 and M4 true CBuildingType getter repair original05e1e48→master1f5bc54 passed focused /Od,/O2 and M4 PE check, with final Release/game replay still pending. M4 minimal lifestyle policy originalf28a10f→master88430bc and faction budgeted-gift policy original150c0ed→masterda3fbed passed normal/-O focused tests; both private/unadvertised. Natural .1007 exact trigger boundary doc original1034a01→masterf87604f; no natural live encounter/heir proof. Lifestyle formal wire is the only active independent code worker (A1); LIFE6 episode-bound receipt fix /Od,/O2 7/7 each passed while shared bridge binding continues.
- /root ordinary-FF integrated mapped static source and cleaned temp remote/local refs/worktrees. An integrated clean faction clone's physical recursive removal was auto-review rejected `blocked by policy`; branch cleanup is clean, physical clone cleanup remains blocked. No public interface/open_kaishek incompatibility or new MCP gate was advertised. Real-game queue resumes only after explicit user release and fresh global single-instance check, then the shortest B0 read-only candidates; Council positive scenes, war terminal/truce, natural events/heir and two-year governance stay open.

## 2026-09-16 delivery integrity and Council entrance (rolling)

- Frozen ZIP recheck matched SHA0E233DCB...4EB2; external GO manifest actual SHAC4AF9C50...395E replaced one stale guide value, original ba1117d→masterabdd60a, temp refs cleaned. Full new worktree checkout hit actual Z: no-space; failed empty branch/partial checkout cleaned, existing clean clone reused, no CK3/screen touched.
- Council controlled guest/pending/fireability typed-reject entrance originalce628c4→mastereb586f7, normal/-O3/3; no-launch evidenceSHA D86BF857...0513C shows R695/R700 all three positive lists empty. R696 already-councillor remains separate, public Council/ad OFF, no formal next-turn or cold proof. Branch/worktree/refs immediately cleaned. M4 faction Python consumer stopped on deterministic native-route B0 (no complete paused member query/typed send-gift/receipt; private CMake integration gate omission); source contract recorded, no fake action or advertised capability. Sole shared bridge/CMake owner remains lifestyle formal wire until it releases files; manual machine reservation still holds all game gates.

## 2026-09-16 09:45 rolling G2 evidence and user-startable delivery

- Frozen bounded preview remains obtainable and GO (ZIP SHA `0E233DCB...4EB2`, Python d112/native eefc, standard-feudal CK3 exact 1.19.0.6 single-mod profile). This is a user-startable preview, not a first whole campaign. Authority `g2-requirements-v1.json` remains 1/8 only M1 and GEN034 2/4; no 1066 seed, hundred-year prefix, full campaign or independent second seed has run.
- R736 focused actual paused query closed R733 preliminary paused-pump RED and returned 30 native legal war+657 final-legal family candidates, 687 distinct; no M5 joint scorer/action. R737 formal War 12/12/+11 days/-43→-50, Army50331957 battle loss, terminal0; R741 from paired R737 checkpoint formal 12/12/+8 days/-50→-51, one new Army67108977→8755 typed move, independent route-target change and next same-War query, terminal0, valid save+driver pair. Old successful report omitted three-way decision evidence, so recommendation unknown. New additive trace originalaaa1808→master342ba818 is source/static only; separately frozen runtime/short real recheck pending. War C/D material end, truce and post-action cold remain open.
- R738 FEUDAL final0ab read-only attempt failed before game launch because archived runner had no Git head; exact Git source replacement R740 formal MCP NewGame→Bookmarks/private native model read bm_1066_rags_to_riches, 5 current keys and final feudal government in 104.568s, reclaimed CK3. Typed selection/StartGame and ordinary seed still need implementation/live. R739 M4 same-frame private native read 981 stock building types, 6 held barony-province rows and 512 legality checks, no definition_identity RED on master1f5; cost/action and two-year material build remain open. R739 targeting-faction count0, so this scene cannot pass real gift; private faction route original8c0c→master8d4094 static-only, public OFF. Council guest/pending/replacement positive0 and R737/R741 natural heir transition0 are evidence gaps, not success.
- M3 normal/optimized bounded static successor checks 9/9, R737 pair expects 29829→38822 and TitleID2141/2173 but ruler alive; use existing hundred-year production slice or a genuinely near-death paired anchor for natural same-campaign evidence, not a new permanent test. Current queues are War trace newly frozen candidate, FEUDAL typed 1066 start, M4 cost/gift, M5 current-supply readback and Council positive scenes; CK3 R741 positively recycled to single-instance inventory0. W12 nominal development slots excluding coordinator, A7 agents on independent READY work, outside occupancy unknown.
- Master deliveries/rebase mappings: M5 supply c7→c7, faction8c→8d, War trace aaa→342. Each ordinary FF master push was followed immediately by exact-tip temp remote/local refs/worktree cleanup; R740 source Git worktree/local branch similarly removed after game cleanup, evidence retained. open_kaishek frozen preview compatibility unchanged; read-only open_kaishek main64cd/current23e1 audit found no formal report consumer (manifest SHA D784B630...E177), so no adapter is required; native ABI/MCP/public behavior unchanged. Automatic approval rejected physical cleanup of failed Z: War runtime and failed C: zero-byte ZIP (`blocked by policy`), so those remain cleanup-blocked without stopping independent work.

## 2026-09-16 10:39 G2 bounded live update

- Frozen user-startable preview remains GO, global G2 stays1/8 (only M1), GEN034 stays2/4. R742/R744 officially cold-resumed War33554473: two same-frame three-way certificates selected legal continue; Combat402653200 loss/retreat changed score -52→-56 and the next production turn consumed it. No terminal/truce/post-terminal cold proof.
- R743 found three real already-councillor rows and zero guest/pending/replacement positives. R745 typed-rejected dynamically selected candidate before native helper; an independent later paused frame kept incumbent32716. One controlled gate passed, formal Council appointment and three natural scene gates remain OFF.
- R746 read six true stock construction cost tuples on the ordinary feudal paused save with native raw10/AI8 and player gold; no date/game action. cost_ready/action_ready remain false pending final affordability and typed material construction. Faction targeting was zero. LIFE2 state-only route and bounded formal consumer are static-ready without paused slot43 evidence or perk result.
- R747 NewGame reached valid 1066 Bookmarks model with five dynamic keys and Murchad feudal index0, but typed selector hit exact fa7275 bridge execute_step allowlist unsupported gameplay step before mailbox. StartGame/ordinary seed absent; RED/old frozen files retained, CK3 reclaimed. Source admission fix320 is integrated, new-version bounded live still pending.
- Rebase/ordinary FF master mappings and exact-tip temp ref cleanup: Council query9ea→b72, FEUDAL gluef903→fa, LIFE2 state0cfa→a074, Council reject8d68→4a7b, faction helperc101→840, LIFE2 dispatchfe70→39a9, LIFE bounded consumer0573→a9b4, FEUDAL B0 320→320. Public ABI/open_kaishek and frozen preview combination unchanged; private new steps remain OFF.

## 2026-09-16 10:57 R748 FEUDAL typed post-frames and new campaign-root RED

- The bounded preview ZIP remains user-startable with its pinned standard-feudal exact-build scope. Authority G2 stays 1/8 (M1 only), GEN034 2/4; no ordinary 1066 seed, hundred-year prefix, full campaign or independent second seed has run.
- R748 used new final320 Release DLL from the R747 allowlist fix. The typed selector was independently confirmed in a later Bookmarks model as Murchad index0; typed StartGame was independently followed by paused `map_ready=true`, player31853 and date53144328. Public campaign-root query then timed out before application-main execution (pump5390->5390; executor257->257), so the official run is RED. No paired save materialized, and CK3 PID18112/job/watchdog were recycled to machine-wide inventory0. Original report SHA14A29575...EE237E and round ledger SHA E9E5C153...A81 are retained.
- The prior R747 allowlist RED is verified only for selector and StartGame post-frames; this does not close the ordinary campaign start gate. FEUDAL owner is locating the narrow campaign-root paused-pump B0 while M4 construction action and LIFE2 read-only candidate prepare independently. Council indexed guest/pending/replacement positives remain zero on matching prior scenes; public ability stays OFF.

## 2026-09-16 11:05 R749 LIFE2 read RED and integration increments

- R749 cold-restored the exact R739 ordinary-feudal save/driver pair and reached paused actor29829/date53178312/native:3 with a ready mailbox. The private LIFE2 query returned `native_lifestyle_current_state_unavailable`; no focus/XP/points/perks state is claimed, no action occurred, and CK3 PID56056/job/watchdog were reclaimed. Report SHA E9117132...E832E and round ledger SHA E954D0A0...216AC are retained.
- Root integrated LIFE2 bounded read runner original69d324bb -> final9ffbd39b, construction typed material action original142ef080 -> final8a0383d7, and FEUDAL post-StartGame pump wait original9f2c75d2 -> final8ed2dae7 by rebase/ordinary FF, then deleted exact-tip temporary remote/local refs. Construction action remains unexecuted; FEUDAL R750 no-launch package is being frozen.
- MCP address/upgrade assessment final77706243 records exact EXE SHA admission, runtime module-base-plus-frozen-RVA binding, dynamic pointer chains/field offsets and offline signature scanning. It does not claim automatic support for a newer CK3 build. G2 remains1/8 and GEN0342/4.

## 2026-09-16 11:18 R750 ordinary 1066 root and checkpoint GREEN

- R750 closed the R748 post-StartGame lifecycle RED without resubmitting actions. A later application-main pump advanced5478->5480 on the same paused 1066 frame, then public campaign-root independently returned ready for player31853/date53144328/`feudal_government`. A material paired checkpoint was sealed: save SHA AEF2B79E...B837B and driver SHA6F58249E...C971; tracked CK3 PID184900/job/watchdog recycled. Report SHA69701165...A25720, ledger SHA3DC8633E...6D36D5.
- This is controlled start evidence, not yet an ordinary production strategy loop or campaign prefix. The next candidate must cold-start this exact pair through formal `native_auto_run`/`ck3_auto_turn`, produce a real non-empty strategy action, independent material result, next-turn consumption and checkpoint. The 100-year gate, first full campaign and second seed remain unexecuted; G2 remains1/8 and GEN0342/4.


## 2026-09-16 11:37 R751 production loop GREEN and R752 construction receipt RED

- The frozen bounded preview remains obtainable/GO at ZIP SHA `0E233DCB...4EB2`; its documented start/pause/stop/cold path and standard-feudal exact-build boundary are unchanged. R751 independently cold-restored the new ordinary 1066 pair through formal `native-auto-run`, completed 20/20 turns in 138.061 seconds, advanced date `53144328 -> 53146272` (81 game days), saved periodic and final paired checkpoints, and reclaimed CK3 PID50660. Formal report SHA `FE821718...E617C`; final save SHA `C8A1F1CC...C1C4F6A`, driver SHA `ECD3A430...B305E9`.
- R751 closes its minimum formal action gate: same-frame public event queries observed instances 1/2/3/4; four typed event selections each submitted once and independently advanced `1 -> 2 -> 3 -> 4 -> null`; the following successful strategy turn consumed the new instance or moved to marriage without replay. Gate sidecar SHA `2EABCCCC...C73BB7`. Event choices were explicitly degraded blocker-removal because complete effect/resource/relationship/native-weight inputs remain unavailable; this is continuity evidence, not event-quality completion. Turn10 marriage had ACK plus `no_semantic_delta`, so marriage remains open.
- The obsolete R751 non-Python wrapper was rejected before launch by the host execution policy. The exact sealed preflight and official `native-auto-run` argv were executed directly and produced the evidence above; a Python entry point is still required before this newer candidate can replace the current preview. R751 ledger SHA `AD1375DF...29690`; G2 authority remains 1/8 and GEN034 2/4. No 100-year, full first campaign or second seed claim.
- R752 then consumed the sole CK3 slot for the private M4 construction gate. A fresh same-frame stock read found a legal strict-affordable tuple (actor29829, barony2103/province2635, building24, slot1, raw gold cost15000000 against50035659). Validator/materializer/receiver each ran exactly once and returned receiver sequence1, but only `pending_receipt`; the immediate read-only receipt query failed `private construction probe paused frame unavailable`, with no independent active-construction postcondition. The runner preserved this as RED, performed no retry, did not start reserved R753, and reclaimed CK3 PID153044. Report SHA `31B88485...B19295`, action SHA `B2E672D4...5B2392`; source checkpoint stayed byte-identical SHA `D8BDC3C4...01474`.
- R752 root work is narrowed to waiting for a newer paused native revision/proof epoch before the read-only receipt query, while retaining one-submit and unknown-stop semantics. A corrected candidate must be resealed from the immutable source seed and query actual stock state before any new submit. Public construction remains unregistered/unadvertised; the two-year construction/lifestyle/faction governance gate remains open.

## 2026-09-16 12:12 rolling R753-R755 increment

- R753 completed one real construction action and independent material readback: exact active tuple actor29829/barony2103/province2635/building24/slot1, gold `50.035659m -> 35.035659m`, next official turn without repeat, then paired checkpoint SHA `F450F4E6...44EFA`. R754 cold restore initially retained the same construction; a later official turn exposed pending interaction855638016 and correctly blocked the construction read. Construction loss is not proven. The bounded official-auto-turn drain repair is now master `ec1a8fd5`; it preserves typed capability RED and never resubmits construction. A new cold candidate from the R753 safe checkpoint is being sealed.
- R755 LIFE2 read-only replay produced deterministic `native_lifestyle_current_state_invalid_request` with zero gameplay actions and full process reclamation. The production snapshot is `native:N`, but the lifestyle snapshot validator rejects `:`; a narrow validator-contract fix is in progress with focused normal/optimized tests. No retry will run before that source is resealed.
- The R751 direct Python formal operator candidate is no-launch READY and avoids the AllSigned wrapper failure, but lacks same-candidate live validation. The frozen preview remains the user-startable delivery. G2 stays 1/8, GEN034 2/4; no hundred-year/full-campaign/second-seed claim.

## 2026-09-16 12:18 rolling R756 construction recovery GREEN

- R756 cold-restored the R753 checkpoint, observed the exact active building, advanced seven days through one official turn, naturally received pay-ransom instance855638016, queried its typed context and formally rejected it in two official pending turns, then independently re-observed the same active construction. Construction submit count0, direct/private pending replies0, manual input0; CK3 fully reclaimed. Report SHA `3D16B5FB...5758E`. The R754 forced-pending construction uncertainty is closed, while LIFE and real faction/vassal intervention keep M4 and the two-year governance gate open. Public construction remains OFF; G2 stays1/8.

## 2026-09-16 12:25 rolling R757/R758 increment

- R757 closed LIFE2's snapshot-ID RED and observed real focus/progress/owned-perk state, including one unspent point; window-bound final legal candidates remain unavailable, so no action/public advertisement follows. Report SHA `794EDBE5...D88EB`, zero gameplay action and full reclaim.
- R758 live-validated the direct Python preview operator without a legacy non-Python wrapper or script-policy changes: cold restore, 20/20 formal turns, 14 query/6 gameplay/2 checkpoints, +192 game days, final save SHA `14B81513...1497`, full reclaim. Exact commands and limits are in `docs/ck3-native-ai/g2-preview-python-operator-r758.md`. This is a local live candidate while the existing ZIP remains the portable frozen preview. G2 stays1/8.


## 2026-09-16 12:55 rolling R759/R760 increment

- R759 added 5,155 game days (about 14.1 years) to the ordinary 1066 prefix through 497/497 formal turns in 1,157.126 wall seconds, with 167 gameplay advances, 330 queries and 55 checkpoints. An official stop request produced `operator_stop_checkpointed/operator_stopped`, save SHA `3A7DE04C...A4CB`, driver SHA `79DD4016...4E0`, report SHA `7DB94672...AABB`, and complete process reclamation. This is valid long-run progress, not a 100-year completion claim.
- R760 preserved a LIFE4 production-transport RED with zero action: the real paused frame had public revision4/native revision3/`native:3`, but the private Python gate still required both revisions to be equal and therefore never sent the query. Report SHA `C105C49A...75D36`; full reclaim. A narrow separate-revision transport fix is in progress with public/ad OFF, while R761 continues the same long-run pair until the higher-priority candidate is ready. G2 stays1/8 and GEN0342/4.


## 2026-09-16 13:08 rolling R761 pending-marriage RED

- R761 added 250 successful turns (84 gameplay, 166 query, 26 checkpoints) before natural inbound `arrange_marriage_interaction` instance-1040187385 blocked turn251. The player was legal recipient; reject was native-legal/reachable with 58 of 60 days remaining, but the existing degraded reject-only rule incorrectly required a same-day deadline and selected no reply. No reply was sent, so the safe checkpoint date53333040/index1116/save SHA `387C8417...DA3E` is recoverable without action uncertainty. Report SHA `0EF04877...A649`; full reclaim. The bounded pending-policy fix is now the sole CK3 queue blocker; public/ad remain unchanged and G2 stays1/8.
