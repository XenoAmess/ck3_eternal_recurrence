# YYYY-MM 月报：完整能力盘点

状态：**重制中 / 已交付**

报告截止：YYYY-MM-27 00:00（Asia/Shanghai）

统计窗口：`<上次月报截止>` 至 `<本次月报截止>`

证据基线：`<branch / commit / exact build / EXE SHA>`

## 本月结论

- `<最重要的可见能力增量>`
- `<当前正在闭合的主线及其目的>`
- `<为什么还不能称为完整自治>`

## 完整能力清单

| # | 能力 | 当前等级 | 已验证范围 | 诚实边界 / 下一门槛 |
|---:|---|---|---|---|
| 1 | `<能力>` | `<research/static-ready/fixture-live/production-live primitive/production-live loop/complete>` | `<证据>` | `<缺口>` |

## 本月新增与 readiness 变化

| 能力或 gate | 上次 | 当前 | 证据/限制 |
|---|---|---|---|
| `<name>` | `<status>` | `<status>` | `<source>` |

## 测试、live evidence 与 RED

- `<测试命令/结果、artifact 路径与 SHA、PID/checkpoint/cleanup>`
- `<保留所有 capability/harness RED，并写明下一可施工入口>`

## 本月完整能力 Show-off 视频

状态：**重制中 / 已通过抽检**

| 项 | 值 |
|---|---|
| 成片 | `<path>` |
| sidecar | `<path>` |
| 时长 / 分辨率 / 帧率 | `<seconds> / <WxH> / <fps>` |
| 编码 | `<video/audio>` |
| 语言 | `English primary + Simplified Chinese in-frame subtitles` |
| size / SHA-256 | `<bytes> / <sha256>` |
| evidence 索引 | `<关键 live artifacts / hashes>` |
| 诚实边界 | `<未覆盖能力与状态等级>` |

- 字幕抽检：`<语义断句、实际渲染宽度、安全区、无裁切、无乱码>`
- 内容抽检：`<关键实机场景、等级徽章、最终证据与边界卡>`
- 被废弃版本：`<路径/原因；没有则写无>`

## 下一月入口

1. `<最高优先级的可执行工作>`

## 文件与提交

- 文件：`<paths>`
- commit/push：`<sha/remote status；尚未提交则明确写 pending>`

## 范围边界

- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。
