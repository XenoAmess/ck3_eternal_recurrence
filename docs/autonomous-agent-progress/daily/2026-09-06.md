# 2026-09-06 日报

状态：**进行中**

正式收口：2026-09-07 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-06.md`](../meetings/daily/2026-09-06.md)

起始分支/HEAD：`master` / `7092fa5207bf7c743f0feb946bf199397e3f79d5`

## 今天完成了什么

- 跨日冻结 R98 的 9 条 compensation parser RED，并将 `Unknown effect: has_variable` 的 effect/trigger 语法边界写入权威坑表与晋升取证文档。
- 修复生成器：L/AE/AF portfolio dispatch 均在 character scope 内使用 `if = { limit = { has_variable = ... } }`，新增拒绝裸 trigger 形状的回归；补偿 29/29、effect boundary 4/4、静态校验和 937-file reproducible release check GREEN。
- 通过受管 stop 回收 R98 PID 46224，CK3=0。R98 非疾病死亡，病死连续失败计数重置为 `0/3`，未应用健康增益。
- 提交并推送 R98 parser 修复（`2d45678`），构建 R99 937-file release-identical product；R99 完成 303/303 database nodes、fatal=0、项目 loader match=0，R98 九条 parser 签名归零。
- R99 首客户端遇到同一精确 `spymaster_task.0381` 的第三次合法投递；将 harness 上限由 2 调整为三次实机观察值，并在同 PID 无重启续跑。
- R99 两客户端累计 966 游戏日、239 次 paused native 观测、12 次精确事件处理。fresh player B1 已处理 `.200/.201/.126`；续跑截止只距开案 217 日，尚未到 D+300，因此不把 `B1=true` 误判为卡死。
- 从 R99 runtime log 取证并修复四类实证回归：weak archive owner 写入、旧 compensation 缺 deferred payer-share/死亡付款人、死亡监察上司 opinion、promotion/PIP receipt revision 首次增量。聚焦 normal/`-O`、effect boundary、全量 1650/1650、静态和 937-file release reproducibility 均 GREEN。
- 更新 `zg361_readiness_data.py` 并重新生成长期未刷新的 `361-phase2-coverage-ledger.md`，最新快照现为 R99；逐号等级未被整树 reachability 虚假提升。
- 同步纠正 source registry 计数：`.146 → D+1 .147` 尚未形成有效 receipt，当前完成数是 `0/4`；旧日报中的 `1/4` 实为“正在攻第 1 类”而非已完成一类。
- 提交并推送 R99 四项 runtime 修复（`13504c3`），构建并排他运行 R100；937-file product 加载 303/303、fatal 0、project match 0，R99 四类签名在 548 日窗口中全部归零。
- R100 以默认 5 速完成 134 次 paused native 观测和 6 次 exact event drain；共同上级 bank close 的固定 `max=7` 在实际短列表上连续报错 3 次。四处 full-walk 已改用各自 live `list_size`，B1 `68/68 ×2`、effect boundary `4/4`、静态与 937-file release check GREEN；R100 受管回收，CK3=0。
- 提交并推送首轮 bank 修复（`2848dd1`），构建 R101；loader 303/303、fatal 0、project match 0。R101 在 549 日、144 次 paused native 观测、9 次 exact drain 后，最终以 `.200@53160192` 打开 fresh player cycle。
- R101 的 exact product 仍在带 `pending_grade=2` 内层过滤的 top walk 重现相同错误 3 次，证明 `max=list_size` 只解决容器长度，不能代替 filtered-range 处理。两个 filtered walk 已增加 `check_range_bounds=no`；R101 受管停止，CK3=0，病死计数保持 `0/3`。

## 早会承诺对照

| # | 早会承诺 | 结果 | 证据 / 未完成原因 | 是否顺延 |
|---:|---|---|---|---|
| D1 | 提交、推送 R98 parser 修复并构建 R99 fresh product | 完成 | `2d45678` 已推送；R99 projection 937 files | 否 |
| D2 | R99 排他实机验证 loader 与晋升链 | 部分完成 | R101 loader GREEN 且 fresh cycle 已在窗口末端打开；filtered-range RED 的 R102 fix 已 static-ready | 顺延 R102 |
| D3 | 推进 source registry | 未开始 | 当前有效完成数 `0/4`；正在攻第 1 类 promotion source | 当日继续 |
| D4 | registry 闭合后按 `1 → 8` 录制 clean spans | 未开始 | registry 未达 `4/4` | 依赖 D3 |
| D5 | 素材门闭合后更新宣传工具并制作双片 | 未开始 | footage `0/8` | 依赖 D4 |
| D6 | G2 与 open_kaishek 伴随收口 | 进行中 | 仅推进不占用 T0 CK3 槽的工作 | 当日继续 |
| D7 | 更新 coverage ledger 和日/周证据 | 完成 | readiness 数据已更新到 R99 并重生成 ledger；日报/forensics 同步 | 持续滚动 |

### 计划外完成

- 使用 retained-session 客户端在不重启 CK3 的情况下处理 harness-only 第三次 `.0381`。
- 将 R99 runtime error block 转化为四项最小产品修复，并补充 first-use/dead-character 语法经验。
- 将 R100 的三次 `ordered_in_list max bigger than list` 转化为 live `list_size` 修复及生成器回归；没有删除 full-walk `max`。
- R101 实机否定“仅 `list_size` 即足够”，把剩余条件收窄到两个带 mutable-grade 内层过滤的 walk，并补精确范围策略。

## 现在在做什么

- 提交并推送 R101 filtered-range 修复，构建 R102 release-identical projection，并以唯一 fresh CK3 实例继续默认 5 速业务链；fresh cycle 打开后同产品续跑，不重启。

## 为什么做这些

- R98 已把阻点缩到确定的 parser 形状；fresh R99 是把 static-ready 提升到 loader-live 并继续 source registry 的最短路径。

## 证据与验证

- R99 artifact：`Z:\b3r99_retry1` 与 `Z:\b3r99_resume1`；loader full log SHA-256 `AC6C1B7A0A18D646A6E7D97D866A6A75AE7867B0912018F0B91327EFF6EAD5AC`；product tree/manifest/ZIP 分别为 `1C69EC72…D689E`、`A27E3A87…F21C`、`7E3C4820…BF8B`。
- 本日修复验证：B1 `68/68 ×2`、compensation `29/29 ×2`、promotion/PIP `51/51 ×2`、manager governance `49/49`、effect boundary `4/4`、完整 discovery `1650/1650`、静态校验 GREEN、ZhongGuo release reproducibility `937 files` GREEN。
- R100 artifact：`Z:\b3r100_retry1`；product tree/manifest/ZIP 分别为 `613ABE65…2307C`、`0E44A891…52C72`、`AD160759…7550`；loader log SHA-256 `9F153031…185B`，entry receipt SHA-256 `A67A2A1F…0E6C`。
- R101 artifact：`Z:\b3r101_retry1`；product tree/manifest/ZIP 分别为 `F4EC20F0…79E81`、`A1154F6A…77056`、`4D20073D…0F7F`；loader log SHA-256 `B119D1F9…B8035`，entry receipt SHA-256 `5B51C009…E7AF`。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| R98 parser 修复 | static-ready | loader-live GREEN | R99 项目 loader match=0 |
| R99 runtime 四项修复 | static-ready | gameplay-live GREEN | R100 548 日窗口中四类签名全部为 0 |
| R100 bank allocator | product RED | R101 实机否定首轮充分性 | live `list_size` 后 filtered top walk 仍 RED |
| R101 filtered range | product RED | static-ready | 两个带 mutable-grade `limit` 的 walk 已加 `check_range_bounds=no`；需 R102 归零 |
| 病死连续失败 | `1/3` | `0/3` | R98 是非病死产品 RED；未应用 buff |
| Source registry | 旧口径误记 `1/4` | `0/4` | `.146/.147` 未闭合；“第 1 类进行中”不得计为完成 |
| Phase2 footage | `0/8` | `0/8` | 依赖 registry `4/4` |
| 最终 MP4 | `0/2` | `0/2` | 依赖 footage `8/8` |

## 失败、阻点与未知

- R101 filtered-range 修复必须在 fresh R102 同类窗口中归零，静态测试不能代替 gameplay-live。
- promotion source 后续三类 registry、八段 clean footage 和双片仍未交付。

## 文件与提交

- 文件：B1/compensation/promotion-PIP generator 与生成物、jingcha effect、四套回归、promotion harness、readiness/coverage ledger、坑表、promotion forensics、本日日报。
- commit/push：R98 parser 修复 `2d45678`、R99 runtime 修复 `13504c3`、R100 首轮 bank 修复 `2848dd1` 已推送；R101 filtered-range 修复 pending。

## 下一步

1. commit/push R101 filtered-range 修复并构建 R102。
2. 排他启动 R102，验证 bank 错误归零；stale cycle 退出、fresh `.200` 打开后复用同 PID 继续 5 速跑过 D+300 与 `.146/.147`。
3. source registry 闭合后按 `1→8` 录 clean spans，再执行宣传工具 fresh pull 与双片制作。

## 范围边界

- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。

## R102 incremental closeout (01:29 Asia/Shanghai)

- R102 loaded the committed 937-file product at 303/303 database nodes and
  fatal zero. Retained PID 38964 advanced 1,346 game days at speed 5 across
  307 paused native observations and 21 exact GREEN event drains.
- The R101 filtered `ordered_in_list` bank-range signature stayed at zero;
  that repair is now gameplay-live. Harness-only contracts for
  `sway_ongoing.5011`, `sway_outcome.1001`, and the dynamic
  `tgp_china_yearly.0020` roles were verified on the same PID without restart.
- The promotion source is still not complete: no real `zg361pp.146 -> D+1
  .147` receipt exists, so canonical registry remains `0/4` and footage/video
  remain `0/8` and `0/2`.
- The extended log exposed four product RED families for R103: missing legacy
  compensation funded shares, missing career-transfer consumer/receiver
  fields, missing B2 redundancy state, and elimination of a character without
  rank. R102 is therefore RED overall despite loader and bank-range GREEN.
- This was not an illness-death failure. The player is alive, the consecutive
  counter is `0/3`, and no health or survivability modifier was applied.

## R103 static repair candidate

- Reduced the R102 product log to four causes and fixed them at their owning
  boundaries: recipient-versus-payer compensation scope, absent HC transfer
  state, absent B2 redundancy eligibility, and absent annual rank during AI
  elimination. No missing character identity was fabricated.
- Corrected all optional-state checks to use lazy `trigger_if/trigger_else`;
  a flat `has_variable` plus `var:` comparison was explicitly rejected because
  CK3 trigger siblings do not short-circuit.
- Focused normal/optimized suites are GREEN: compensation `29/29`, HC `43/43`,
  Career/Learning `34/34`, B2 `40/40`; effect boundary `4/4`, complete discovery
  `1651/1651` twice, static validation GREEN, and the 937-file release check is
  reproducible (manifest `87C020D9...B07C`, ZIP `C53A6777...0F51`).
- Status is static-ready only. R102 PID 38964 remains retained until the R103
  source commit is made; the next action is managed stop followed by one fresh,
  serial, speed-5 CK3 run. Illness-death count remains `0/3`; no health or
  survivability modifier has been applied.

## R103 live closeout and R104 repair candidate (02:39 Asia/Shanghai)

- R103 commit `5b80006` loaded its 937-file release-identical projection at
  303/303 database nodes with fatal zero. One CK3 PID covered 2,345 game days
  at speed 5, 500 paused native observations and 22 exact event drains across
  the initial client plus eight same-PID continuations.
- All four R102 product signatures and the filtered bank-range error stayed at
  zero. The run instead exposed one B1 weak-subject calibration cascade and
  two absent optional settlement/sample reads in `zg361pp.2167/.2168`.
- The B1 generator now re-prunes through `is_alive = yes` immediately before
  the delayed calibration consumer group. Promotion/PIP audits now read the
  optional values only behind lazy presence guards and skip settlement when
  no underlying business object exists.
- B1 `68/68` and promotion/PIP `51/51` are GREEN in normal and optimized
  modes; generated effect shards stay within the existing purpose boundary.
  R104 remains static-ready until the fresh exact-product run.
- The player survived illness and recovered. Illness-death failures remain
  `0/3`; no health or survivability modifier was applied.
- Promotion source `.146 -> D+1 .147` is still absent, so registry/footage/
  final-video gates remain `0/4`, `0/8`, and `0/2`.

## R104/R105 full-product progress and R106 candidate (04:59 Asia/Shanghai)

- R104 completed the player B1 publication, central serial portfolio and summary
  traversal on the 937-file release-identical product. It then proved that the PP
  adapter reselected a top-stewardship employee 42 game days after central had
  frozen its subject. Frozen-subject handoff and the invalid visible-summary
  `ROOT.MakeScope` chain were fixed and pushed in `17e8005`.
- Commit `a298eea` repaired the first Simplified-Chinese audit batch, but its
  original blocker `0/12`, high `0/24` closure claim was invalidated by R106's
  first live `zg361cl.314` rendering. The audit remains RED until each player-visible
  family has an itemized generated-product review and fresh live evidence.
- R105 (commit `a298eea`) completed 303/303 loader nodes with fatal zero and no
  loading-performance RED. PID 10216 ran 550 game days at default speed 5, with
  141 paused native/MCP observations and eight exact event drains. The player
  stayed B1-active throughout while Central/PP stayed inactive, so registry remains
  `0/4`.
- R105's player closure snapshot showed target quota `2/4/1`, recount `0/0/0`, and
  no manager-owned `zg361_b1_processing_subjects`. The `.121` employee callback
  entered `var:<manager>` and called list pruning, but that helper rebuilt through
  the original employee `ROOT`. It now freezes current manager `THIS` at entry and
  explicitly owns all four scratch/rebuild writes. B1 `68/68`, readiness `10/10`,
  full discovery `1655/1655`, and `validate_local.py` are GREEN.
- R105 CK3 was cleanly stopped through the managed queue before changing mod bytes.
  The player survived; this was not an illness-death failure, the consecutive count
  is `0/3`, and no health/survivability fixture was applied. R106 awaits release
  reproducibility, commit/push and one fresh serial CK3 run.

## R106 same-process harness-contract recovery (05:15 Asia/Shanghai)

- R106 commit `7f17fed` loaded all 303/303 database nodes with fatal zero, then
  advanced 353 game days at speed 5 across 102 paused native/MCP observations.
  Seven exact event drains include `zg361b1.200` and `zg361b1.201`, proving the
  R105 manager-roster ownership fix on the release-identical product.
- The client stopped before clicking `zg361b1.126`: its old contract incorrectly
  required completed `.200/.201` event-local self/shadow scopes. All ticket,
  oversight, pending-watch and publication-notice checks passed, so this is a
  harness RED rather than a new mod RED.
- The contract now binds the actual independently scheduled `.126` context and
  has a focused normal/optimized regression. Healthy CK3 PID 30492 remains alive;
  the next client will reconnect to the same process without a game restart.
- Registry/footage/final-video remain `0/4`, `0/8`, and `0/2`. The player is alive,
  illness-death failures remain `0/3`, and no health fixture was applied.
- The corrected `.126` contract then drained GREEN on the same PID. B1 closed,
  Central activated, and the replacement client handled 22 more central/summary
  windows before fail-closing on valid player notice `zg361.50`, which was absent
  from the harness allowlist. Cumulative R106 evidence is now 414 game days, 134
  paused observations and 30 exact drains; `.50` has been source-reviewed and bound
  to its exact native context for the next same-PID client.
- `.50` then drained GREEN on that same PID. The immediate `zg361.4` reaction window
  is the next harness-only allowlist gap; option 1 was source-reviewed as the bounded
  path because it only reduces stress. R106 now totals 415 days, 136 observations and
  31 exact drains, with Central still active and the same CK3 process retained.
- `.4` then drained GREEN, followed by 25 additional central/compensation windows.
  R106 now covers 505 days, 168 paused observations and 57 exact drains. The current
  allowlist gap is `zg361.6`; only native options 1 and 3 are enabled, and source review
  selects option 1 because it is the only route that can preserve the player's career.
  PID 30492 remains retained and healthy.
- `.6` completed GREEN and its random appeal succeeded: the player remained landed,
  entered another annual B1 cycle, completed it and activated Central again. R106 now
  spans 887 days, 217 paused observations and 74 exact drains. The current harness gap
  is valid policy card `zg361m.2`; option A is the source-reviewed reference-charter path.
- The same PID then drained `zg361m.2` GREEN through option A. R106 now totals
  887 game days, 219 paused native/MCP observations and 75 exact drains. The next
  same-date `zg361.1` summary omitted the already-completed outer bank-ticket tuple;
  its source consumes only the four summary values, so this is a harness-contract RED,
  not a mod RED. The contract and focused normal/optimized regression now accept both
  exact first-cycle and later-cycle scope sets while still rejecting any extra name.
- That `.1` window then drained GREEN. The same PID advanced another 44 game days and
  handled 44 more exact Central/B1 windows before stopping at a recurring `zg361b1.126`.
  R106 now totals 931 days, 265 paused observations and 120 exact drains. The current
  notice preserves completed self/shadow ticket names instead of the first cycle's bank
  tuple; all scopes actually consumed by `.126` are correct. Its contract now accepts
  both full observed inheritance sets but still rejects partial or unrelated names.
- The recurring `.126` then drained GREEN. The process advanced another 61 days before
  recurring `zg361.50` exposed the same later-cycle self/shadow-versus-bank inheritance
  distinction; prompt/result values and manager aliases are all exact. R106 now totals
  992 days, 270 paused observations and 121 exact drains. The `.50` contract and regression
  now bind both complete observed cycle shapes without weakening its consumed-field checks.
- Recurring `.50` then drained GREEN and the immediate `.4` reaction exposed the same
  inherited-name distinction. Since `.4` consumes no saved scope and retained its exact
  four-option shape, this is another harness-only drift. R106 now totals 993 days,
  272 paused observations and 122 exact drains; both complete `.4` cycle shapes are bound.
- Recurring `.4` then drained GREEN and the retained PID advanced another 114 days before
  reaching the first live `zg361cl.314` player career-learning response. R106 now totals
  1,107 days, 279 paused observations and 123 exact drains. Source review selects option 2,
  which records a valid decline and resumes the stage without route-A transfer or resource
  charges. Its exact two-option/scope contract lives in a new one-item purpose module rather
  than enlarging the existing production-entry file.
- The player screenshot of that paused `.314` card exposed a product copy RED before any
  click: a policy-slogan title, a shared scope/permission template, and the phrases
  “有钱的调任包” and “绩效锅”. The earlier audit had already identified the family under
  H-01/H-04/H-05, but R105 fixed only part of the buttons/dead keys and then incorrectly
  closed all High clusters. Six subject cards and the one digest now have distinct Chinese/
  English scenes and concrete stakes; a separate five-test copy contract forbids the old
  template and developer language. Because localization bytes changed, retained R106 PID
  30492 cannot validate the repair and must be managed-stopped before a fresh R107.
- Generated Chinese/English output was read back directly. Career-learning runtime tests are
  34/34 in both modes, the new copy contract is 5/5 in both modes, promotion checkpoint tests
  are 46/46 in both modes, and the complete mod discovery is 1,660/1,660 in both modes.
  `validate_local.py` and both generator `--check` gates are GREEN. The 937-file release
  reproducibility check is GREEN with manifest SHA-256
  `4DF09E9AED1895DB9DC1880366FABA869B9DFCCCB2A9E14863B8004BCA8CF654` and ZIP SHA-256
  `C77DE4D24D71777A676826D0C51468CEBA7501DBD8BE7BE1E724FFE00779F981`.
- Fresh R107 (`275ee65`) loaded the 937-file release-identical product at 303/303, fatal 0.
  One retained PID advanced 918 game days at speed 5 with 221 paused native/MCP observations
  and 68 exact event drains. It traversed all 44 Career/HC decision cards and six receipts;
  the current pause is recurring `zg361.1` and the remaining RED is harness-only scope drift.
- This execution is not copy acceptance. The full Chinese audit remains RED: the six rewritten
  Career Learning cards have not yet been revisited visually, while the 44 Career/HC cards prove
  that still-open template-copy findings remain on the product path. Live progression is paused
  until the audit ledger is rechecked and the remaining copy is repaired.

## R108 copy re-audit and effect-boundary closure (09:00 Asia/Shanghai)

- Ten parallel ownership-isolated work packages re-audited the generated Simplified Chinese tree.
  The final ledger closes 59/59 static findings with zero known static remainder: punctuation or
  empty dynamic starts, title/body repetition, body-carried choices, generic A/B buttons and
  player-visible implementation language all scan at zero. This remains `STATIC GREEN / LIVE
  PENDING`; no visual-layout or dynamic-scope claim is inferred from static checks.
- R108 (`63f3a40`) had already completed 303/303 loader nodes with fatal zero. One CK3 process ran
  508 game days at speed 5, produced 206 paused native/MCP observations and drained 50 exact events
  without OCR or restart. It stopped at a harness-only `.9100` portfolio-selection mismatch; the
  focused source-checkpoint contract now selects route D so `.146 -> .147` remains reachable.
- Copy changes altered mod bytes, so the healthy R108 process was stopped through its managed queue.
  No CK3 process was reused against changed product bytes.
- The effect boundary is now a count-and-byte hard gate. HC, Compensation, Incident, PP, scoreboard
  and Workforce M360 were split from their authoritative generators. Final inventory is 713 files /
  3,813 top-level effects, maximum 10 effects and 130,221 bytes per file, with zero target misses,
  zero files above 20 effects and zero files above 200 KiB. Active runner/validator/preflight/CMake
  consumers were migrated; frozen historical manifests retain their original paths.
- Complete mod discovery is 1,755/1,755 GREEN in normal and optimized modes. Promotion-source
  runner is 50/50 GREEN in both modes; `validate_local.py`, root static validation and the 1,031-file
  reproducible release build are GREEN. Manifest/ZIP SHA-256 are
  `BBF50CB13EA4A8393C0321A3A14D775347F0C9EF4B4421C60AD867599E42E2E1` /
  `869B742480EB52C3893AA5D9DFC7652F6BF08B6A9AE8D8061D8101CDF53B88E7`.
  The implementation and evidence are commit `061bc1a`; push follows the reporting commit. R109
  fresh speed-5 CK3 validation is the next serial gate; registry/footage/final-video remain
  unchanged until live artifacts are produced.

## R109 loader RED and R110 repair candidate

- Fresh R109 loaded all 303/303 database nodes, but the exact product remained RED: `debug.log`
  recorded seven `Scripted effect should have no arguments` hits, collapsing to two unique
  Workforce M360 helpers. Because the failure occurred during product loading, R109 never entered
  gameplay and produced no gameplay-acceptance evidence.
- R109 used no OCR. The CK3 process was stopped through the managed queue after evidence capture;
  no process was retained or reused after the product bytes changed.
- The Workforce generator now derives each helper call's named arguments from the placeholders
  actually consumed by that helper definition. Parameterized helpers receive the exact consumed
  ticket set, while parameterless cleanup and route-B validation helpers are called with bare
  `= yes`; the same contract covers both routes and every generated M360 call site.
- This repair is static-ready only. R110 must use a fresh release-identical product and fresh CK3
  launch to prove the seven-hit/two-helper loader signature is zero before gameplay testing resumes.

## R110 loader GREEN, same-PID continuation and R111 repair candidate

- Fresh R110 completed all 303/303 database nodes with fatal zero; the R109 seven-hit/two-helper
  Workforce loader signature is zero. Gameplay therefore resumed at the default speed 5 without OCR.
- The first stop was harness-only saved-scope drift on `zg361.6`. Its exact observed self/shadow
  ticket set was added to the contract, then the replacement client continued the same healthy PID
  112096 with restart count zero rather than relaunching CK3.
- The retained run selected `.6.a`; its authored 40% appeal failed and the player lost the title.
  After the next summary, B1/Central/PP all remained inactive and review-now was ineligible, so this
  seed was invalid for further promotion-source acceptance. That outcome is a legitimate scenario
  branch, not evidence that the migration tree completed or stalled.
- R110's product runtime log recorded 41,958 entries across four actionable families. Parallel static
  repairs now (1) lazily guard absent B2 adverse-action state, (2) read Central summary numeric values
  through `MakeScope.Var(...).GetValue`, (3) compare a transfer title's holder with the enclosing
  character scope rather than the title scope, and (4) skip Career/Learning opinion writes when a
  frozen owner has died. Each repair is generator-owned and has focused normal/`-O` regression.
- Because those fixes change product bytes and the retained seed can no longer reach the target chain,
  R110 was stopped through the managed queue. The next serial gate is a fresh R111 release-identical
  run at speed 5; no R110 runtime repair is claimed gameplay-live until that run returns the four
  signatures to zero.

## R118 copy closure, loader evidence and invalid-seed closeout

- Commit `21c5764` completed eight parallel, ownership-isolated Simplified-Chinese copy review
  packages. The pass recovered and reviewed 1,542 generated-key lines that the earlier partial audit
  had not closed. All 59/59 historical finding clusters plus the newly discovered same-class items
  now have zero known static remainder. This is a static copy result only; no visual-layout or dynamic
  rendering GREEN is inferred from it.
- Complete discovery is GREEN at 1,780/1,780 in both normal and optimized (`-O`) modes. Root static
  validation and the reproducible release check are GREEN. The effect boundary remains compliant at
  713 files / 3,817 top-level effects, with a maximum of 10 effects and 130,221 bytes in any one file.
- R118 loaded all 303/303 database nodes. One retained CK3 PID continued at speed 5 without OCR, and
  runtime product diagnostics remained at zero. However, vanilla event
  `ep3_interactions_events.0630` removed the player's governor title and invalidated the canonical
  seed/scenario before the full migration tree could complete.
- After the title removal, the corrected product closed the now-empty B1 roster instead of consuming
  stale subjects. That empty-roster closure is the correct product behavior and supports the roster
  repair, but it is not a full-tree live GREEN and does not advance registry, footage or final-video
  completion.
- R118 has been stopped through the managed queue. The next serial gate is a dedicated player-manager
  seed followed by one fresh, release-identical R119 run; static copy closure will remain distinct from
  live acceptance until that run supplies a complete player-manager artifact.

## R119 dedicated manager-seed attempt

- Commit `ab4eca2dc28f26b9b98852fc2dced1b7c8d2bfed` added a dedicated
  player-manager fixture, request contract and materializer. The fixture only exposes
  `zga_phase2_manager_seed.1` after the played human ruler satisfies the manager gates, saves the
  played manager plus one real direct reviewable subject, and does not open B1 or manufacture product
  variables/receipts.
- The first `ab4eca2` candidate run at `Z:\p2r119_seed_live1` still used the historical
  player-subject checkpoint. Offline evidence then excluded that input from manager-seed eligibility,
  so PID `134408` was manually stopped. The surviving progress artifact reached 303 database nodes
  with fatal zero but still had `event_wait_authorized=false`; no final runner report, manager event,
  checkpoint or candidate contract exists. This interrupted attempt is not an R119 acceptance result.
- Commit `e760d63f0c507b7dc4be9d56fefe792bb201fbe1` switched the request contract to an
  offline-qualified manager candidate: played CharacterID `37884` is alive, landed, celestial, has
  prestige `7436.75919` and three direct alive landed celestial vassals. The same audit also proves
  that the save is a **five-player multiplayer save**. Local-player restoration, `zg361_on` and the
  dedicated fixture event therefore remained live-only facts; offline qualification did not make the
  contract ready.
- The formal release-projection run used `Z:\p2m119` / `Z:\p2m119_a`, projection
  `phase2-full-release-r119-e760d63`, final CK3 PID `89284` after Frontend warmup PID `131864`, and
  completed 303/303 database nodes in `54.117s` with fatal zero. Product was 1,031 files /
  31,183,422 bytes, tree `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1`,
  projection manifest SHA-256 `0f30b7ba332df2029ca7acf9180b56d4c5df740074fd1922702f1a56438eeb4c`,
  and fixture tree `2eb194c61ad1f781e05f8035e873305735a19e50d5288dd00ae8e54159ad4cf2`.
- Native readiness then timed out before CK3 reached the required paused application-main state:
  the main-thread mailbox was not installed/ready, application-state pointers were unavailable and
  `played_character` remained null. Accordingly `scenario_verdict=RED / seed_verdict=RED`; no manager
  event, product action or candidate materialization was evaluated. This is a scenario/native-readiness
  RED, **not a product RED** and not migration-tree evidence.
- Managed cleanup is GREEN with restart count zero and final inventory empty. The clean source tree
  remained `af4c5271e99b286d914d24572276261cdd7c8feea1099cae420c2b2b7a9f5276`; runtime product/fixture
  trees remained exactly the hashes above, and all external dependencies were unchanged. The frozen
  source ZIP is `322eb22f9deb776e50cc677537ae47703eb3fd3e1292e202dac1832f26039b7b`.
  `runner-report.json` SHA-256 is
  `c7ba9ac391d337d9e6a378326b869158d16567a2117ab51f2b7aca86b5ed24c9`; native-readiness and cleanup
  reports are `e436ad24e0c625c8153949553694359544cd3dd15ecefe5661533cc7cf7658e1` /
  `90ec9ec1115ba57c6f0ff3d052a964a5ee9409b62095eb2ed233cac09c5b77b8`.

## R120 single-player manager handoff seed attempt

- Commit `12405873745ea7df5cd399f4d25a4f91d4da2273` froze the acceptance-only typed handoff that
  switches the historical single-player subject CharacterID `29037` to its existing immediate
  liege/manager CharacterID `32904`, then exposes the dedicated seed event without creating any
  character, title, relation or product receipt. The formal `Z:\p2m120_a\preflight.json` is GREEN.
- The live run at `Z:\p2m120` / `Z:\p2m120_a` completed 303/303 loader nodes with fatal zero and
  reached paused application-main native readiness. Before the handoff fixture could run, the loaded
  save exposed its already-scheduled `zg361b2.40`. Event key, instance, date, root, manager/subject
  saved scopes and all three authored options matched the exact drain contract; the sole failed
  identity check was `source_save_sha256`. The runner still bound that event to historical SHA-256
  `233e70536d736c32efb9bbd20ef4bab9e0be8f96ee13524707b9ee31e319dc9c`, while the selected exact
  source is `8e6ceb97e97cd6b9185ebbcce38b42fc087e0b800cd5e321037c9f29a79e45b9`.
- The runner therefore failed closed before selecting any option. This is a **seed/harness identity
  contract RED, not a product RED**: `zga_phase2_manager_seed.10/.11/.1`, candidate materialization
  and downstream product acceptance were not evaluated. The next correction is limited to rebinding
  that known pre-bootstrap event contract to the exact selected source, followed by a fresh seed run.
- The release-identical projection `phase2-full-release-r120-1240587` stayed at 1,031 files /
  31,183,422 bytes, tree `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1`,
  manifest SHA-256 `b80bb8ebda083abe70acbeb17ae0faf2d269b089adfd78beb1b430a23bf2d55c`.
  Critical B2 byte equivalence is GREEN; runtime product/fixture trees remained
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` /
  `07228b54b3a3da71d648301e494068a3852d5c9f1ed3f64de9699eebd9b18428`, external dependencies
  were unchanged, and source before/after manifests both hash to
  `8b8b169b75e39bd672698b5cab7fc4e23924057782d3355511fdbf03815245f2`.
- Managed cleanup is GREEN: warmup PID `43576` and final PID `85688` are gone and the final CK3
  inventory is empty. Evidence SHA-256: runner report
  `deb968923ca92a9546ccafd5a11606d300bceedcf7f1270e41e7636a7bf3af55`; native readiness
  `32d37408d783c73826a86b7f99d565a3f530f90fec0c8b07db8845c9db5e9aef`; exact event identity
  `aae555d9b9e14c2c218e400c9a93670ae9381d63861649cb7f59fe6cb12ea52f`; cleanup
  `d362ce8ce064b4b3fa9675d701edd958570f93db4ccc1b462786cedcfe00b648`; relay
  `0fcd537f849c64fbcbadbd7f9342c1f6a78c66eacf4719dc745807a64a30873a`.

## R121 exact-drain seed attempt

- Commit `cf6063ba7f8b90e286262da96ca03735d0b2541f` rebounded the known pre-bootstrap B2 PIP event
  to the exact selected single-player source. The formal `Z:\p2m121_pre_a\preflight.json` is GREEN
  without a CK3 launch. Frozen source ZIP/tree SHA-256 are
  `dc15d01b79d5c9eb586ef8dfdddbdcfd5b170d961f9143256e9052028efe2493` /
  `7edb7d88a83df637d7ee7b2051e68dd11f7ed8f7a65dac4ed7c9a68654871f72`.
- The live run at `Z:\p2m121` / `Z:\p2m121_a` completed 303/303 loader nodes with fatal zero,
  zero project loader diagnostics and paused application-main/native readiness GREEN. The known
  `zg361b2.40` was then drained under its exact identity contract: source save SHA, event identity,
  date, root, manager/subject saved scopes, three authored options and pre-selection state all matched.
- The next visible event was vanilla `ep3_interactions_events.0630`, which was not in the seed
  runner's known prerequisite-event contract. The runner therefore failed closed before
  `zga_phase2_manager_seed.10/.11/.1`, manager handoff, final seed or candidate materialization.
  This is a **scenario/harness RED, not a product RED**, and provides no full migration-tree GREEN.
- Release-identical projection `phase2-full-release-r121-cf6063b` remained 1,031 files /
  31,183,422 bytes with product tree
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` and projection manifest
  SHA-256 `15edd426cb213e74b6d8eb41fb03c4e5b0e28e9996d8471ca3cae97f16c3e642`.
  Release manifest/ZIP SHA-256 are
  `1aec6b6a7c7ce8af42085d7fc0866e04ed255b9b3dca002d70fc5c8581077b33` /
  `12ebe06039167f1ac968916de56a4100a0281e933df112a7e2e1e2f60d8735b6`.
- Critical B2 byte equivalence is GREEN. Runtime product/fixture trees ended unchanged at
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` /
  `07228b54b3a3da71d648301e494068a3852d5c9f1ed3f64de9699eebd9b18428`; external dependencies
  were unchanged. Source before/after manifest files both hash to
  `ed1611e885761f7f563c72fa37ef77a8fa6f34411c211127d5b6ae4d188212b1`.
- Managed cleanup is GREEN with `restart_count=0`: warmup PID `182292` and final PID `135672` are
  gone, and final CK3 inventory is empty. Evidence SHA-256: preflight
  `f319a2d180ed9c8b2e04b7fce3d15b7d427d33d4efd040e656d89254062f41e4`; runner report
  `8ca41f5e86b06ad6ccf2bee0203c7845e4bc8f6c19ebfa623d9be29a0ed6d64a`; native readiness
  `08002b1b296b0804c0a635b6e8b49211e666b41dea6955e23f058f4bbcc81f05`; loader scan
  `dcc08459de97e74be3cd0ecf28dc3f656ddcdd472e5073a0912e2232c1ef04b7`; exact B2 PIP drain
  `cf2bac10ffbe2f764d4813c139b8ea41d3064146c3b52b11a7e9f52a9f6e53b7`; cleanup
  `5d91ec292e216edec6ac0b0985ae1f2af979a648f1235e8b2e9514bc15158ca2`; relay
  `fed8d98503928cabbfc9079d1a019aa8ad7c1a6c911194012a7af36301bc709e`.

## R122 immutable-date manager seed attempt

- Commit `30ad33788f949729f5412688b39603ccdf5c2bfa` froze the preemptive manager handoff fixture.
  `Z:\p2m122_pre_a\preflight.json` is GREEN without a CK3 launch. Frozen source ZIP/tree SHA-256 are
  `08fdb70659a3a7718866252b525ee5d78201eda0c2ebcc014faf07659bca28f7` /
  `3893e981ed607d8104487da0744865651ff9bded29a7d93bf00519e373344f96`.
- The live run at `Z:\p2m122` / `Z:\p2m122_a` completed 303/303 loader nodes with fatal zero,
  zero project loader diagnostics and paused application-main/native readiness GREEN. Repeated paused
  snapshots at immutable source date `53147016`, including the first two checks, had no active event;
  CK3 debug/error logs also contained no `ZGAP2MANAGERSEED` marker. The preemptive handoff therefore
  did not execute at the source date.
- The runner resumed at speed 5 under the immutable-date contract. The first later event was
  `zg361b2.40` at date `53147040`, so the required-date gate failed closed immediately with
  `known_visible_event_drain_allowed=false`. No option was selected, no event was drained, manager
  handoff/final seed were not reached and no candidate exists. This is a **scenario/harness RED, not
  a product RED**, and provides no full migration-tree GREEN or per-ID readiness promotion.
- Release-identical projection `phase2-full-release-r122-30ad337` remained 1,031 files /
  31,183,422 bytes with product tree
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` and projection manifest
  SHA-256 `ef8f00e27f0a8c2998a77030b4a8af36bfeb4846cf7b4903dd535b9c66364814`.
  Release manifest/ZIP SHA-256 are
  `c2fb8f13fcf9ae296b4fbd3228e5358a6282307c26f4261f47b9cfa07b190c06` /
  `12ebe06039167f1ac968916de56a4100a0281e933df112a7e2e1e2f60d8735b6`.
- Critical B2 byte equivalence is GREEN. Runtime product/fixture trees ended unchanged at
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` /
  `72accc5a975482790932714481cf3879604abdf83031ab3214b92eeff03f33d2`; external dependencies were
  unchanged. Source before/after manifest files both hash to
  `54e1dcb2e42b4a0ee8c329a8260615058125ad1d0a4279100a63083893d71474`.
- Managed cleanup is GREEN with `restart_count=0`: warmup PID `101784` and final PID `177256` are
  gone, and final CK3 inventory is empty. Evidence SHA-256: preflight
  `5a97b9a9b50058ccf1aa0a3f2ec1acf1d96b6ab42d52f116ac3154a4360bd728`; runner report
  `720f9dc6b6a45e2a2ebfbb62046cb812812e8886a18dc89a2f856f5b7605eb60`; native readiness
  `61c6134802e2f04f8b4253ab3a1a91bb6adc2aec05c344a2e5a78dbeb015679c`; loader scan
  `7d136c5f75b6e7c17ded971cd1bb07c7218c3ef3ffaad8e9dda8d56ba0fcd931`; preemptive contract
  `e837b96ea421f48cb181b4765c7c0a81f41b6c4c3c6fcf442bf8e186586df8b0`; bootstrap wait
  `be504ddb5b688bb073b2b737ffaac7d5b34668df14849ac8eefbbc3a5e327c1f`; cleanup
  `2aa007f5d12d15d22535ff38000b3c04b942b903c030b3c39cba2cf3b8eececb`; relay
  `ad58c138c07dc45fabbba899498e03d32b2705c7e64ae885251b3f03336ded9f`.

## R123 exact PIP-refusal manager seed attempt

- Commit `550d9ef1f12ee1fae8113b6a9d867ba5c7a3a147` froze the manager-seed route that permits only the
  exact `zg361b2.40` prerequisite and selects authored option 3/native index 2. The first no-launch
  preflight at `Z:\p2m123_pre_a\preflight.json` was a pipe-name configuration harness RED and did
  not cross the CK3 launch boundary; the corrected second preflight at
  `Z:\p2m123_pre2_a\preflight.json` was GREEN, also without launching CK3. Frozen source ZIP/tree
  SHA-256 are `228513b2b10567c86fd288773d3b0ef49516f091cd28a3017eab43eb716b6508` /
  `abc320466dd660106b6261300df64ec17ae467d60d328225600be557de2de4dc`.
- The live run at `Z:\p2m123` / `Z:\p2m123_a` completed 303/303 loader nodes with fatal zero,
  zero project loader blocking diagnostics and paused application-main/native readiness GREEN.
  Gameplay event recognition and option selection were MCP/native-only; top-level OCR/image were used
  only by the non-gameplay legal-consent gate, and coordinates remained disabled. The runner advanced
  at speed 5 to exact date `53147040`,
  verified `zg361b2.40` and all bound source/event/root/manager/subject/option identities, then selected
  option 3/native index 2 successfully. This closes the exact PIP-refusal drain operation itself.
- After the drain, repeated paused snapshots at `53147040` had no active event and CK3 debug/error
  logs contained zero `ZGAP2MANAGERSEED` markers. Resuming at speed 5 advanced to `53147136`, beyond
  the hard `53147040` manager-bootstrap ceiling, without reaching `zga_phase2_manager_seed.10/.11/.1`.
  The runner failed closed before manager handoff or candidate materialization; candidate is absent.
  This is a **scenario/harness RED, not a product RED**, and provides no full migration-tree GREEN or
  per-ID readiness promotion.
- Release-identical projection `phase2-full-release-r123-550d9ef` remained 1,031 files /
  31,183,422 bytes with product tree
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` and projection manifest
  SHA-256 `e0272b2307cfc40a25e635053403fc8ad532298a0b4b85ce83188ba44a7b7eec`.
  Release manifest/ZIP SHA-256 are
  `a94e9e6b72182a538c79ba45c1d53424bf2feef6e245a19933c1bfbc1681630c` /
  `12ebe06039167f1ac968916de56a4100a0281e933df112a7e2e1e2f60d8735b6`.
- Critical B2 byte equivalence is GREEN. Runtime product/fixture trees ended unchanged at
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` /
  `8cfa3a3cb30afe411bc802503383b76d6adadf14678198fa469b014b97974666`; external dependencies were
  unchanged. Source before/after manifest files both hash to
  `3a25e3077f1603fece951732bcdafe94648622970fa588fe09c4f5bfcf2371f7`.
- Managed cleanup is GREEN with `restart_count=0`: warmup PID `93696` and final PID `182168` are
  gone, and final CK3 inventory is empty. Evidence SHA-256: first/second preflight
  `6344b2884899546fa21ea90390e804ebcc2cbf81130c7daabb1d70047f83cedd` /
  `7d3de1995b02cbc42f2d1a3c36900f24f8c1205226940b21d01e901392cb7af9`; runner report
  `c1b129b57fbd1bc185836386c7a9e5f6eeb6ad95c609e7f950b2f6e3c870a241`; native readiness
  `8f6d73bf7a7717ac24dce40102a5a046728135cce773e6a3da5df4704555f72a`; loader scan
  `6d64a6f1b55ef89641b3752345ada59593f01e81203cdf966285cde481ffece2`; transition contract
  `864f84ef520ead5bd73e516d43243c22badc32b8f391b64273b4550b3bb083ed`; exact option-3 drain
  `dd0639f98842223a145c3fcd8d25343f2ffee99c3f77bfe38169057227a1521b`; bootstrap wait
  `d41b4cb91b89fa4c30a464fb2564a6bda3ee5b5d716f231b0bd583c14502779e`; cleanup
  `e33a55f61f14bac05146f682de3f4f78e6e4c8b579046bdebbf4984470d3f40c`; relay
  `731a09fbab60347aae753425cd942fe7de1a31ab7785208f3a8d6dc5cd7bc6b4`.

## R125 direct-manager seed attempt

- Commit `31f4163dbed5918962c422b57ad92932458c2b2b` froze the direct-already-player-manager
  fixture route against the exact source save. The no-launch preflight at
  `Z:\p2m125_pre_a\preflight.json` is GREEN with `ck3_launch_attempted=false` and
  `launch_boundary=not-crossed`. Frozen source ZIP/tree SHA-256 are
  `cf7ffb56dcfc1e413f290c168717a844433ef1599d1f0dbd6e87bfda82be5b82` /
  `ded6aa41ec3aeda8f0ca3d1850c1f51169684998c1949f74d22a9e6d67365d47`.
- The live run at `Z:\p2m125` / `Z:\p2m125_a` completed 303/303 loader nodes with fatal zero,
  zero project-attributed blocking matches and paused application-main/native readiness GREEN.
  The first paused typed snapshot was exact date `53164440`, played CharacterID `29037`, with no
  active event; the direct-manager source and entry-route contracts are GREEN, but fixture event
  `zga_phase2_manager_seed.1` did not appear on that source frame. The fixture and GUI assets loaded,
  but every `ZGAP2MANAGERSEED` success/RED marker remained absent. Direct `-load_save` does not traverse
  the lobby hook used by this route, while the load-safe GUI still accepted only the legacy terminal-PIP
  subject. Therefore R125 proves a missing direct activation carrier; it does **not** prove that any
  manager eligibility gate failed or was even evaluated.
- The runner then attempted the contract-required advancement. The next semantic snapshot was
  unpaused at `53164488`, already beyond the sole safe source date `53164440`, and still had no
  active event. The runner submitted the required MCP/native protective pause and failed closed;
  no event option was selected and no candidate was materialized. This is a **scenario/seed harness
  RED, not a product RED**, and provides no full migration-tree GREEN or per-ID readiness promotion.
- Top-level `ocr_used=true` and `image_used=true` belong only to the non-gameplay legal-consent
  gate; gameplay state, advancement attempt and protective pause used MCP/native only, and
  `coordinates_used=false`. Release-identical projection `phase2-full-release-r125-31f4163`
  remained 1,031 files / 31,183,422 bytes with product tree
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1`, projection manifest
  SHA-256 `6d8b5e55f41055da8dff7501073387da929d3e3f9a66835bb06775e786810a79`, and release
  manifest/ZIP SHA-256 `d012206a97a75730075a34278ecd438d76c5a01094a7e60d4ee06f92c6e6f413` /
  `12ebe06039167f1ac968916de56a4100a0281e933df112a7e2e1e2f60d8735b6`.
- Critical B2 byte equivalence is GREEN. Runtime product/fixture trees ended unchanged at
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` /
  `7eb72b0861d547d597b485fd2468972b724edfb22e77be67e7bca80d55f0ad86`; external
  dependencies were unchanged. Clean-source before/after manifest files both hash to
  `2aea4ad462a71125799c1f99631c0c14063512d6d9f5d7829b87e086f0dafbf1`.
- Managed cleanup is GREEN with `restart_count=0`: Frontend warmup PID `80116` and final PID
  `183912` are gone, and final CK3 inventory is empty. Evidence SHA-256: preflight
  `e3082dcfe8a0856bfb62b8ce9143158d5181159916698b127ef8bbd4a34d3df9`; runner report
  `ba6e10aac872f935fcd7380ae43bb63d8384287c4395c74941a43b8c61df1939`; native readiness
  `ba40300323f825aba86c82efd0e96b83991ef736994d26bc18c61b1fb194629d`; loader scan
  `246a6158dfeaf6b540de3ce0ad8e5d68d5c09662c614b95cb2bd328561ea1b53`; bootstrap wait
  `3b9f01903b382cb7ef8b604ee0f05101a1fc68895051329d73447c5eb9728afc`; direct source
  `b88d365ba5b782963241366f9577f90e418ff0de39da5b894c007cbb3b9c5bb0`; transition contract
  `e715a463326b96e34f395a604ff69a543c076414462038256deb9e9f37846ae1`; cleanup
  `4a973d653d1fb1c0b8daa230eda86375604144b5d6a5267dba3a055af6aa3305`; relay
  `2c6d703bfedc21cf2dbe09d3f957f2c7fc0b47e0aec27aa88e2dc6442fa607b0`.

## R126 load-safe direct-manager gate diagnosis

- Commit `e670e502461cc8f85218d028033cb3cdb1c61495` froze the R107 direct-player-manager
  source plus an acceptance-only load-safe carrier and one-shot gate diagnostics. The no-launch
  preflight at `Z:\p2m126_pre2_a\preflight.json` is GREEN with
  `ck3_launch_attempted=false` and `launch_boundary=not-crossed`. Frozen source ZIP/tree SHA-256
  are `d049d419a473af7af676534d6bb8cd704c6e62d690389291cce7fcb49a7decb7` /
  `71bea66709388396e355e03ff5c062fc54b546e5d9433391d1001bccb1bda30b`.
- The live run at `Z:\p2m126` / `Z:\p2m126_a` completed 303/303 loader nodes with fatal zero,
  zero project-attributed blocking matches and paused application-main/native readiness GREEN.
  The first paused typed snapshot was exact date `53164440`, revision 4, played CharacterID
  `29037`, with no active event and no gameplay input sent. The load-safe carrier then executed and
  logged all eight gates exactly once: `celestial=PASS`, `review_now=RED`, `prestige_150=PASS`,
  `b1_inactive=RED`, `review_in_progress_inactive=PASS`, `p2c_inactive=PASS`,
  `pp_inactive=PASS`, and `direct_reviewable_vassal=PASS`.
- Those markers prove the R126 carrier is live and the manager gate chain was evaluated. They also
  identify the source-state mismatch precisely: the frozen R107 source is currently inside an active
  B1, so both the review-now condition and the required B1-inactive condition fail. Consequently
  `zga_phase2_manager_seed.1` correctly did not fire. This is a **scenario/seed harness RED, not a
  product RED**; it neither closes the full migration tree nor changes any per-ID readiness tier.
- The runner's required advancement reached an unpaused semantic snapshot at `53164488`, beyond the
  sole safe source date `53164440`, with no active event. It immediately submitted the required
  MCP/native protective pause and failed closed. No event option was selected and no candidate was
  materialized. Top-level OCR/image use belongs only to the non-gameplay legal-consent gate;
  gameplay state, advancement and protective pause were MCP/native-only, with
  `coordinates_used=false`.
- Release-identical projection `phase2-full-release-r126-e670e50` remained 1,031 files /
  31,183,422 bytes with product tree
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1`, projection manifest
  SHA-256 `aadf48112920e7ac74a7fd8503bc13011e693a471daa324708646c7348ab99da`, and release
  manifest/ZIP SHA-256 `d3661baec68ebf28f43ca64fb75f6dad39aded77fc60a376d7024371a7cd5b9c` /
  `12ebe06039167f1ac968916de56a4100a0281e933df112a7e2e1e2f60d8735b6`.
  Critical B2 byte equivalence is GREEN. Runtime product/fixture trees ended unchanged at
  `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` /
  `ba4522d027a939f127b60fc764eb18d68779addb0c075e04aebc867aa00d3c0a`; external dependencies
  were unchanged. Clean-source before/after manifest files both hash to
  `faba9718db0ec46d698f1c576adf3f5f841e312d86ac99607b41d93dcc35efcc`.
- Managed cleanup is GREEN with `restart_count=0`: Frontend warmup PID `154964` and final PID
  `181508` are gone, and final CK3 inventory is empty. Evidence SHA-256: preflight
  `572f5f2c93ab819e86aec096bce6e21eb4e590b13cb834a2e918062b7f547bde`; runner report
  `7c7489ec658aeb8629282e77771f0326cd1b2fb46450ea939cae288ee48b7cc7`; native readiness
  `c86028f4a3e821dd6756da1a5494c324529b5240e66e66f7c17cc1afa37a5278`; loader scan
  `8be29b36233b007dd439eeaa8b5787c89394d93fe0b45756e96623eb51589926`; bootstrap wait
  `7e97f9c0d9dd54f7cf73d4ef4bfefeb820aec3886179a63d4c3d23699311c7f5`; direct source
  `b88d365ba5b782963241366f9577f90e418ff0de39da5b894c007cbb3b9c5bb0`; transition contract
  `39408cd0b34fc46a7f95ec8968a70e2712d2a9b048ff27ab39314885802282fd`; debug gate log
  `fe1c39e5adc1ef46a19e7c0d00f3f7087147feb79ab1e4778d1cbb47bc2e17be`; cleanup
  `dddc6dc8f294b16a9cfa911e7db0e01bc963910b314197c8c1261bef0734a78d`; relay
  `22eb83f820d5830b844a95f20685bbf556ba9288621295e6867f9e1df8ffb518`.

## R127 exact-PIP post-switch manager gate diagnosis

- Commit `4b8513e036f94563259c8f4ee0367e07fd48362d` restored the exact terminal-PIP
  route and added a read-only post-switch gate logger without changing the product or relaxing the
  final `.100` gate. The no-launch preflight at `Z:\p2m127_pre_a\preflight.json` is GREEN with
  `ck3_launch_attempted=false`. Frozen source ZIP/tree SHA-256 are
  `b6710b8e96ef52028f31444f58e5771b72e71f53fe9e1c371be9224595357157` /
  `61834c4b0659981ff771e9848ee0cfb8271045d30e09ea5df650a7712719eb78`.
- The live run at `Z:\p2m127` / `Z:\p2m127_a` completed 303/303 loader nodes, fatal zero,
  zero project-attributed blocking matches and paused application-main/native readiness GREEN. It
  advanced from the typed source frame `53147016` at speed 5 to the exact `zg361b2.40` instance at
  `53147040`, proved source `29037` and manager `32904`, then selected authored option 3 / native
  index 2 through MCP. The event closed with its postcondition verified while the map remained paused.
- The existing-liege handoff reached the post-switch scope, where 12/14 gates passed. The only RED
  gates were `review_now=RED` and `b1_inactive=RED`; human, alive, landed, owner binding, handoff
  pending, celestial, game rule, prestige, review-progress inactive, P2C inactive, PP inactive and
  subject binding all passed. Therefore `zga_phase2_manager_seed.1` correctly did not appear at the
  same paused date. This is a **fixture/seed target-state RED, not a loader, parser or product RED**;
  it does not change per-ID readiness.
- Release-identical projection `phase2-full-release-r127-4b8513e` remained 1,031 files with product
  tree `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1`; projection manifest,
  release manifest and release ZIP SHA-256 are
  `de3a83c3c81c9dd8554816c0111e85bcccb27bfd76ee4301b2d6705f9ebb29d3`,
  `8d6957762ae3d612e0a23d88d78929178a226eacc5fc16b2e5ca2ab8badfc564` and
  `12ebe06039167f1ac968916de56a4100a0281e933df112a7e2e1e2f60d8735b6`. Product, fixture and
  clean-source trees ended unchanged; the source before/after manifest files both hash to
  `4856a80e471b9baaada04b957c95ee5858e389d8a95d11f87627fd2f47637407`.
- Managed cleanup is GREEN with `restart_count=0`: Frontend warmup PID `170108` and final PID
  `85140` are gone. Gameplay was MCP-only and coordinate-free; OCR/image was limited to the
  non-gameplay legal-consent classifier and performed zero authorized clicks. Evidence SHA-256:
  preflight `32e9eb3451073d3a10005cf739e83bf55b462c0a48ce53d9ddeaf5abd5079d9f`;
  runner `43311d147adcd82f032a63543dfd9d6cf8103b77c60b2e3b6ab11f0aee06b786`;
  native readiness `b39425bfbe31fb9248c23590b77e881fd8ad76074f8944f9239784ead6595778`;
  loader scan `a6b39bb1d5953e0ddbed6816d6c837693f6018c30a95950dec8ac5a7d8b66374`;
  exact drain `5767434396318974ed8c1b802cf94091879ab47762cf9eea7a7ebfe2a41f2cdd`;
  manager route `f5b8f0094a1b36e26b886c2c94a324abaf2ed5830745b12b8587be1622c4cf40`;
  debug gate log `52cc1006df9ca2150eed156ccd9d67c14da1f7a99663ee03918c033640164f73`;
  cleanup `e306f17618fd9f3075c12253af0220656437fd8774dfbe7d73d7113b256205a3`;
  relay `0bd5e3f432f7606b560b6cadd25f5c2aff811ed0f92ee8334e5965ba6a023fbb`.

## R128 R119 multiplayer local-player restoration readiness RED

- Commit `2273c6061b4235c66a93eec026116523d670ba27` bound the clean R119
  player-manager save without changing product code. The no-launch preflight at
  `Z:\p2m128_pre_a\preflight.json` is GREEN with `ck3_launch_attempted=false`. The frozen source
  save is `26,325,927` bytes with SHA-256
  `bf5960b7194e1222029add884743c688fee0d86f95559670c587317461519e74`; source ZIP/tree SHA-256
  are `9c369c2cd3aa53bfc553efc9cf2ff2c39fb7147dfdbffb87d312d0a59d2f34cf` /
  `a8f89ab8ff15705da62fa945a65c1a89d0e1ebc4d76c318b2f288e8db2988717`.
- The live run at `Z:\p2m128` / `Z:\p2m128_a` completed all 303/303 loader nodes with fatal zero,
  reached the `In Game` idler, and connected the exact-build bridge with semantic state available.
  Native readiness nevertheless timed out after 180 seconds. Its last paused snapshot was
  `date_raw=51848904`, `speed=3`, `local_player_id=1`, `map_ready=true`, but
  `played_character=null`; the main-thread mailbox remained `installed=false`, `ready=false`,
  `pump_epochs=0`, with no application-state pointers or stable binding.
- This is a **multiplayer local-player restoration/native-readiness RED, not a product parser or
  loader RED**. The run never reached speed 5, an event option, the manager gate chain, or any
  business acceptance gate, and changes no per-ID readiness. Because readiness failed first, the
  runner did not reach its formal post-readiness loader-error batch scan; R128 therefore does not
  claim a zero blocking-match count.
- The release-identical product remained 1,031 files. Projection/release-manifest/release-ZIP
  SHA-256 are `508f1ff3950cd19032b49cc9e25014c4192bb14a4296db808e5fcd49a8ec1b72`,
  `57fcad53f65657fe1006b40ac8b3e7218d88c0ed9e0a8cd3b7ead2ba7cf71e6f` and
  `12ebe06039167f1ac968916de56a4100a0281e933df112a7e2e1e2f60d8735b6`. Runtime product/fixture
  trees ended unchanged at `55a08b950cb3fabd5628913a191214d60b2f8b973c72a4788fd8769536a35db1` /
  `cee41a7ce842d1e649637717d918961a314d6374f9b86fa5f3655801b4a7465d`; clean-source
  before/after manifest files are byte-identical at
  `96da308db88e6deeaede32fecc7f26c2d1a0fb68563ac2cd02135c3ce71846c6`.
- Managed cleanup is GREEN with `restart_count=0`: Frontend warmup PID `40864` and final PID
  `56312` are gone, artifact inventory and a follow-up process check both show no CK3 process.
  Gameplay transport remained MCP-only and coordinate-free; OCR/image was limited to the
  non-gameplay legal-consent classifier and performed zero clicks. Evidence SHA-256: preflight
  `1169dc20d7dc61b01a96ff6dd825977bdb0e692dc475f98b37e561330ca24b97`; runner
  `e012598262ef8625bc89b55fd2549a0252242256162de9d9897d1262430469f6`; native readiness
  `24ff1d90c84b6d5b336e7fe6cb9d208dd6996e343fc196409c5ef30d49e3c819`; critical-B2 equivalence
  `e971dd76e2660b81785a2d67de769b2e8ac8768329776685082a5c76a0b9f565`; cleanup
  `5de75f59e7f7690ea33249321407a990ffe7f8477952e48c942a1b5b57a88d0b`; relay
  `f20abffead4234eab9309a2607f5e44fabb9f1853808ebdc312e8e4cf568174c`.
