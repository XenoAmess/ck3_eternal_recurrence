# 2026-09-13 daily report

Status: **formally closed**

Formal close: 2026-09-14 00:00 (Asia/Shanghai)

Morning meeting: [`../meetings/daily/2026-09-13.md`](../meetings/daily/2026-09-13.md)

Starting branch/HEAD: `root-rebase-r345-20260909` / `a9415fb1e0d299df7acba2406b5288cc522ba9d9`

## Work completed

- Formally closed the 2026-09-12 daily report against D1-D6 and opened the 2026-09-13 meeting on time.
- Completed the off-screen G2-M2 same-character stress comparator. Exact `.0030` option 2 now carries a snapshot-bound expectation through
  planner, native action, service result and auto-run RED handling. Focused normal/optimized tests are `8/8` GREEN.
- Root `6da3ca847c8ce1ac4791050b49fe01318ab721f5` and open_kaishek
  `040a407fbaaaf8851d6ac731c31d6f3b46efdb58` are pushed and synchronized. The companion audit classifies the optional `ck3_auto_turn`
  fields as pass-through compatible; no Java API, profile, dependency or Operator MCP `1.1.0` version changed.
- Completed the first evidence-bounded option-variant consumer for R374 event `natural_disaster.7031`. Only the three source-reviewed native
  projections resolve to native 2; unknown shapes and all other variant event keys remain blocked. Policy tests pass normal/optimized `8/8`.
- Root `ff4cdf8fd2d9f5070c198ca891f1c69145894d46` and open_kaishek
  `5662dc70a77f8b823b0870ea34e0ad957568d118` are pushed and synchronized. The additive root planner metadata has no companion consumer;
  Operator MCP remains `1.1.0` with no Java, profile or dependency delta.
- Completed two exact selected-choice structured effect profiles. `.0030` publishes authored base stress loss `-30` while explicitly
  refusing an exact runtime delta; `.7031` separates its selected tooltip from the common-after variable write. The existing read-only
  vanilla-event knowledge MCP exposes both, and the `.0030` comparator now consumes its profile rather than a duplicate specification.
- Focused normal/optimized tests are `27/27` GREEN. This remains `source-structured / static-ready / live=false`; generic
  event-context-v2 effect extraction and all three production outcome loops remain open.
- Root `f4586847ec7d5a4201bde398a38137262e438d8c` and open_kaishek
  `3d78ff3fac8bfe03e5a5bd28454f873ffb50aa42` are pushed and synchronized. The additive analysis metadata and auto-turn decision field are
  pass-through compatible; Operator MCP stays `1.1.0` with no Java, profile, dependency or endpoint change.
- Completed the second G2-M2 material event path at static readiness. Exact `trait_specific.8001` authored option 2/native 1 now publishes
  `add_gold = minor_gold_value`; the state snapshot exposes signed Q100000 `played_character_gold`, and planner/action/service require a
  same-character strictly positive raw delta. The dynamic amount remains explicitly non-exact because monthly income, treasury and era
  participate in the vanilla value.
- Release DLL compile/link and the native `game_access` fixture are GREEN. Focused registry/profile/planner/action/comparator tests pass
  normal/optimized `30/30`. No CK3, recording, desktop input, mod tree or promotional asset was used; status remains
  `static-ready / live=false`, G2-M2 stays in progress and global G2 stays `0/8`.
- Root `0d7ddc5761b0997101806b179a52585332720beb` and open_kaishek
  `2351a184b96b1aed7bd2821cb2c8256e172d61a8` are pushed and synchronized. The companion has no closed state-snapshot or
  `ck3_auto_turn` consumer, so this is additive/pass-through compatible; Operator MCP remains `1.1.0`.
- Completed the third G2-M2 static material path for `death_management.1007`. The direct consumer admits only this key's distinct
  dead-character scope, the effect profile records authored stress base `+20`, and the comparator accepts a non-decreasing same-character
  result while counting only a positive delta as material evidence. R374's historical action/advance remains valid, but its old frame had no
  player-stress reading.
- Focused policy/profile/comparator tests pass normal/optimized `25/25`. Six stale registry-count assertions exposed by the earlier 188th
  record were synchronized to `188/334`; their affected modules pass normal/optimized `45/45`. This package launches no CK3, recorder,
  injector or desktop input and remains `static-ready / live=false`.
- Root `4dd7b74d7497980bf473da71bbe277b3e39f5027` and open_kaishek
  `53388093cf5dda77363f83f482d62493fdbaa307` are pushed and synchronized. Event-analysis content and the root planner/result objects remain
  additive to the companion; Operator MCP stays `1.1.0`.
- Completed bounded objective/utility profiles for all three G2-M2 target events. Each profile records the selected source-reviewed ordinal
  rank, discrete utility facts and alternative reasons; `.0030` and `.8001` both choose native1 rather than a fixed first row, while `.1007`
  is explicitly the sole legal route. Planner output now carries `event_campaign_utility`.
- Cross-event numeric score remains null, calibration remains unavailable and `semantic_optimal=false`; this closes the minimum static
  objective/utility explanation without inventing exchange rates between stress, gold, traits and time. Focused query/policy/planner tests pass
  normal/optimized `26/26`; no CK3 or screen resource was used.
- Root `dbd9a16b639c715dd95be999d35919477b3a3061` and open_kaishek
  `792ff6184619e919e231774e25aa49e92ea68ef0` are pushed and synchronized. The additive analysis/auto-turn fields have no companion consumer;
  Operator MCP remains `1.1.0`.
- Completed the first G2-M1 native observation subpackage at `static-ready / live=false`. The existing exact-build campaign-root query now
  enumerates sorted full-generation IDs for every alive, landed character whose native immediate liege is the player. Dead, landless,
  unrelated and generation-invalid rows cannot enter the vector; the result participates in the existing double observation and paused-frame
  equality gate.
- The Release DLL compiled and linked (`2,609,152` bytes; SHA-256 `7F2E7EE7...5429C`). The direct native reader and source-contract
  executables are GREEN, and the focused Python driver/service/MCP/live-harness tests pass normal/optimized `30/30` each. The inherited CTest
  directory still produces its known mixed Cygwin/Windows `BAD_COMMAND`; direct execution of the two newly built binaries is GREEN, so this is
  a harness-path issue rather than a product RED.
- G2-M1 moves from `not_started` to `in_progress`, while global G2 remains `0/8`. Existing 2026-08-26 campaign-root artifacts predate the new
  vector and do not prove it live. One future bounded paused read may close that field; it must share an already-required G2 session and must
  not become a dedicated long run. Realm-neighbor identity/adjacency and the turn bundle remain the next implementation gaps.
- Root implementation `c602d9a78d3d3e7b5bb84ce9f6dce40119fd5447` and open_kaishek compatibility record
  `c72b91e` are pushed. The companion has no campaign-root caller or closed response type, so its Operator MCP remains `1.1.0`; the public root
  field and static/live boundary are now recorded without inventing companion readiness.
- Completed the second G2-M1 native observation subpackage at `static-ready / live=false`. The campaign-root query now publishes sorted,
  duplicate-free `adjacent_external_province_holder_character_ids` by scanning exact Province/adjacency data and excluding every holder whose
  native immediate-liege chain reaches the current player. It deliberately reports boundary Province holders, not inferred independent rulers.
- Exact holder leaf `0x220C3F0..0x220C49F` and SHA-256 `531558C7...B8D8` are frozen in the reusable ABI asset. Release DLL build/link and direct
  native reader/source-contract fixtures are GREEN; focused Python driver/service/MCP/live-harness tests pass normal/optimized `30/30`.
  Candidate DLL is `2,615,808` bytes / `35079262...5B3834`.
- Historical campaign-root live artifacts predate both new identity vectors, so M1 stays in progress and global G2 stays `0/8`. A future
  already-required paused session may verify both vectors together without a dedicated long run. The next off-screen M1 input is canonical
  holder-to-primary-title/top-liege entity mapping and minimum alert aggregation.
- Root `0bc407df0d9e43d699cacde65c05034630647f50` and open_kaishek
  `f4a14dcea0dc64dc26cb8ab2cb8eb74e821ed5d3` are pushed and synchronized. The companion has no campaign-root caller or closed response
  type, so the additive public fields are pass-through compatible and Operator MCP remains `1.1.0`.
- Completed the first canonical `entity-directory-v1` identity-search slice at `static-ready / live=false`. The new read-only
  `ck3_search_entities_v1` MCP tool consumes exactly one existing campaign-root query, supports self/direct-vassal/adjacent-holder relation
  filters and stable CharacterID keyset pagination, and no longer requires planners to know those candidate IDs in advance.
- Components preserve their proof boundary: self values come from the source frame; direct-vassal immediate/top lieges follow from the proven
  native relationship; related title/capital and adjacent-holder lieges remain typed `unavailable`. Focused contract/service/official MCP SDK
  tests pass normal/optimized `19/19`; no DLL, CK3, recorder or desktop input was used.
- M1 remains in progress and global G2 remains `0/8`. The next off-screen input is per-related-character primary-title and liege-chain native
  batch design; one future already-required paused session will verify the two source vectors and directory together.
- Root implementation `e9670f40e75414a5b034fe62ffa62b5be7f66f41` and open_kaishek compatibility record
  `9d548bc594262657294a51e6c98604a377ab65c5` are pushed and synchronized. The companion has no entity-directory caller or closed response
  type, so Java/API/profile/dependency state and its independent Operator MCP `1.1.0` remain unchanged.
- Completed the related-character native context extension at `static-ready / live=false`. The existing campaign-root query now batch-resolves
  every direct-vassal and adjacent-holder full CharacterID to a required primary title, legal-null capital and native immediate/top-liege chain;
  adjacent holders retain their source role while top liege supplies canonical realm identity.
- The wire is sorted, duplicate-free and all-or-nothing across both source vectors. Any resolver, generation round-trip, relation invariant or
  second-sample failure returns `related_character_contexts_unavailable`; no partial row or stale frame is admitted. `ck3_search_entities_v1`
  now publishes complete current title/top-liege components and distinguishes legal independent/no-capital values as `not_applicable`.
- Release DLL build/link is GREEN (`2,634,752` bytes, `D86F78E9...5960B7`); direct native reader/source-contract executables are GREEN;
  focused Python normal/optimized tests pass `35/35`. M1 remains in progress and global G2 remains `0/8`; the next off-screen input is the
  minimum ruler/realm/succession alert aggregate and turn bundle.
- Root implementation `13d616baa7be6cadcd7b2c6fa5267d2ef6c65c01` and open_kaishek compatibility record
  `6234bec0c66c56a29fe3840839128a585477ea85` are pushed and synchronized. The companion has no campaign-root/entity-directory caller or
  closed response type; its Java/API/profile/dependency state and Operator MCP `1.1.0` remain unchanged.

## T0 P2 R614–R618 native pause-race RED and recovery

- R614–R618 completed the first four clean spans of another continuous FFmpeg take. In HC/Workforce span 5 the event-definition prequery gate
  stopped fail-closed, so the incomplete take remains uncounted: P1 is `9/9 GREEN`, P2 is `0/8`, and the final-video lock remains active.
- R618 proves a narrow harness race. The starting frame was running at revision 198; by the time `pause-map` reached the native bridge CK3 had
  auto-paused. The accepted `already_paused` response proved revision 199, unchanged date `53375616`, `ending_paused=true` and zero state-frame
  rejections. The runner incorrectly required only `status=submitted` and failed before its existing stability checks.
- Root commit `e642e39fae3dc84d5aee7fccedda49e4d04e65c3` accepts the idempotent native success, while still requiring a paused snapshot plus stable
  event instance, date, played character and valid revision. The focused promo-capture contract test and Python compile check are GREEN.
- All R614–R618 CK3 processes and FFmpeg are stopped; cleanup is GREEN and protected storage is unchanged. Raw MKV SHA-256 is
  `28490BA0...B9C05D`, report SHA-256 is `C9948ED3...D5C0AA`. No MCP/public interface changed, so open_kaishek and shared event assets required
  no update. Evidence: [R614–R618 recovery](../../phase2-promo/r614-r618-native-pause-race-recovery-2026-09-13.md).

## Current work

- The minimum alert aggregate, exact player monthly-income observation and domain capacity are now static-ready. The next off-screen input is a
  bounded exact-build council/faction assessment; implementation proceeds only if a frozen native binding can be closed without guessing.
  R558 remains deferred while the workstation is in human use.

## Opening readiness

- T0 P1 `9/9 GREEN`; P2 `0/8` clean spans.
- G2 `0/8`; GEN-034 `2/4`; G2-M1 and G2-M2 are both in progress. M1 has relationship discovery, minimum alerts, monthly income, health and
  domain capacity static-ready; M2 has three target material comparators and bounded utility profiles static-ready, with production loops pending.
- No product RED; current round R557 and old round R556 are terminated; CK3/FFmpeg/injector inventories are zero.

## Range boundary

- No CK3, screen input, recording or Workshop work while the workstation is occupied.
- Religion remains owner-deferred except for the narrow holy-war and marriage dependencies.

## Unplanned owner-priority Workshop correction

- At the owner's explicit reprioritization, corrected the existing Auto Upgrade Buildings 2.0.0 Steam Change Notes entry for item `3800124956`. The earlier 51-character summary was incomplete; anonymous public readback now matches the full 468-character, 15-line Chinese changelog exactly.
- Added the same player-visible `2.0.0 更新记录` section to the canonical Workshop description and published it. The public description now matches all 1,499 canonical characters; source commit `a879b5c1fcdc41afcc603ea62e655d24892b6f65` is pushed.
- Native metadata-only retries returned `EResult=1` but did not create or replace the existing public Change Notes entry. The owner-page edit was therefore required, and acceptance was based on anonymous public HTML readback rather than the submit receipt alone.
- Runtime content remained unchanged. The existing 15-file subscription cache still matches the formal ID-bound manifest exactly. Steam was restored to Offline Mode without client exit or sign-out; CK3 remained at zero processes.
- Permanent evidence: `D:\workspace\auto-upgrade-buildings-v2.0.0-publication\steam\steam_full_changelog_correction.json`, SHA-256 `6352B714B5CE1FCCF3B848FFF3FAED08AE484FC7B4B4CDB0EB585FBA556BCC73`. The release changelog records the correction and the uploader documentation now states the proven Change Notes boundary.
- Converted the correction into a mandatory future-release gate in `AGENTS.md` and `docs/workshop-publishing-mcp.md`: freeze the intended full note, treat submit receipts only as transport evidence, anonymously compare the exact public entry text and identity, and keep the release incomplete when that comparison is absent or fails.

## Auto Upgrade Buildings Phase 3 funding selector research

- Completed the owner-requested Phase 3 requirements analysis without changing product bytes or using CK3, Steam, or the desktop. CK3 1.19.0.6 natively supports an option list embedded directly in a decision through `decision_option_list_controller`; no custom GUI or follow-up event is required.
- Froze three modes: treasury only, personal gold only, and treasury first with whole-payment personal fallback. The modes never split one gold cost; prestige and piety remain personal resources. Two override character flags are sufficient, with no flag deliberately mapping old saves to the 2.0.0 treasury-first behavior.
- The checked-in plan specifies generator-centralized affordability/payment routing, exact decision UX, migration, non-goals, exhaustive 605-edge static regression and a sampled one-process live funding matrix. Estimate is 3.5–6 hours for implementation plus acceptance; publication remains a separate explicitly authorized package. Plan: [Phase 3 funding selector](../../auto-upgrade-buildings-phase-3-plan.md).

## G2-M1 primary-title succession observation static-ready

- Reused the exact-build `CLandedTitle+0x278/+0x280/+0x284` ordered succession layout already proven by the war-exit reader and published the complete vector as `primary_title_succession_character_ids` in `campaign-root-context-v1`.
- Every successor must be a distinct full-generation CharacterID, differ from the current holder and roundtrip through Character storage. The native order is preserved. Malformed spans, generation mismatch, duplicates and second-sample drift fail the whole frame as `primary_title_succession_unavailable`; landless and genuinely heirless states remain available with `[]`.
- Release DLL build/link is GREEN (`2,636,800` bytes, `8A4C8791A047F175241769A2DF8EB89B8D4116AA97C3ACC6A8E428DC409333B2`); direct native reader and source-contract executables are GREEN; focused Python normal/optimized tests pass `35/35`.
- This supplies real native input for a minimum primary-title succession alert but does not claim per-title partition, succession-law or post-death continuation coverage. Status remains `static-ready / live=false`, G2-M1 remains in progress and global G2 stays `0/8`. No CK3, recorder, injector or desktop input was used.
- Root implementation `ec967a14f4106b159b2c5ab8cc1b7cce239dd7ce` and open_kaishek compatibility record `0cc5515a8ac55d54d9f4cc43dc0de3f0f58e9d9b` are pushed and synchronized. The companion has no campaign-root/entity-directory caller or closed response type; its Java/API/profile/dependency state and Operator MCP `1.1.0` remain unchanged.

## Unplanned owner-priority Auto Upgrade Buildings 3.0.0 release

- Implemented the Phase 3 selector with CK3's native decision option list: treasury only, personal only and treasury first. The two strict modes have no fallback, no payment can split across balances, non-gold cost components remain personal, and missing new flags preserve the 2.0.0 treasury-first save meaning.
- Static validation covered all 165 chains／605 edges; translation caller tests were 26/26 GREEN, builder tests 7/7 GREEN and the deterministic release check passed. The formal 15-file manifest is `CFCF2A3...A3441B2`; ZIP is `933BA24...13A29`.
- Formal CK3 `desktop-3fevhd2-1c74096080--auto-upgrade-buildings--R0025` was GREEN in an isolated non-debug 1.19.0.6 session. All three native choices were clicked; no-fallback, no-split, old-save default and Phase 2 samples passed with zero project diagnostics. Evidence report SHA-256 is `1314C1AD...92B706`.
- Published existing Workshop item `3800124956` as 3.0.0. Anonymous readback matched the exact maintained title and all 1911 description characters. New Change Notes entry `1789236338` matches the frozen 413-character, 14-line text exactly.
- Moved the old cache aside and forced a new Steam download. The fresh 15-file, 676122-byte cache matches the formal manifest. Its exact canonical descriptor exposed a verifier false RED; commit `b85bd222` now accepts either exact bytes or one correct launcher ID injection, with 7/7 tests GREEN.
- Steam returned Offline Mode at 02:29:29 without exit or sign-out; `WantsOfflineMode=1`, local Steamworks reports `BLoggedOn=false`, the connection log did not reconnect, Steam stays running and CK3 is stopped. A later account-wide public presence readback was online without `inGameInfo`; it is not used as this machine's mode attestation and supplied no other-machine gameplay signal. Tag `auto-upgrade-buildings-v3.0.0`, full release changelog and evidence are linked from the Phase 3 plan.

## CK3 coat-of-arms MCP canonical round-trip

- Completed the first same-process native coat-of-arms apply → Copy/export → exact reapply → Copy/export closure on CK3 `1.19.0.6`.
  The tested payload covers pattern, three root colors, one colored emblem with three colors, and one instance with position, scale, rotation and depth.
- Both native probes returned `applied`; both native exports returned `exported`. The two engine-canonical outputs are byte-identical at 327 bytes,
  SHA-256 `9F84F667B413D2BA24FA101A28D6F455AD93B638F90D9C2A21A91EF264B93C93`, under PID `23008`, snapshot `native:3`, public revision `4`.
- Permanent process evidence remains outside Git at `artifacts/coa-clipboard-probe-2026-09-08/mcp-live-roundtrip3.json` (26,521 bytes,
  SHA-256 `9EB7DC0AE1F7EBF2F7D5F8518DE948AACD929104AAFF7447B0B8375B19D0DE84`). Shutdown is GREEN with `exit_reason=stop`,
  `cleanup_proven=true` and `tree_gone=true`; CK3 inventory is zero. `open_kaishek` preflight was `not-applicable` because it has no dedicated CoA reader domain.
- Readiness advances the tested native path to `mcp-roundtrip`. Browser → Quarkus → Java MCP live UI, upper-level Finish/persistence,
  runtime merged DLC/mod resources and native rendered pixels remain open and are not inferred from this result.

## G2-M1 first same-frame turn bundle static-ready

- Added public read-only MCP tool `ck3_query_turn_bundle_v1` with schema `xar.ck3.turn-bundle/v1`. It combines one cached normalized state snapshot with exactly one campaign-root query only when snapshot ID, public/native revision, date and pause state match.
- The bundle now exposes ruler identity/alive/title/capital/government and optional gold/stress; realm liege/direct-vassal/adjacent-holder summaries; ordered primary-title succession and first-heir/no-heir alerts; pending event/interaction; and compact sorted active-war summaries.
- Minimum ruler/realm/primary-title succession alerts are ready. Income, health, domain, council, factions and full partition remain explicit `unavailable`, so bundle status is `partial`, full readiness is false, G2-M1 remains in progress and global G2 remains `0/8`.
- Focused contract/service/official MCP SDK tests pass normal/optimized `26/26`. No native mailbox, DLL, mutation, CK3, recorder, injector or desktop input was used. The next off-screen package is the smallest exact-build income/domain observation slice.
- Root implementation `2dc2d4097fde8cdf0fbd7bd68daad72ab6dcc6cd` and open_kaishek compatibility record `8659ddad1717e26f54e7f68463985286c6ebc086` are pushed and synchronized. The companion has no turn-bundle/campaign-root/entity-directory caller or closed response type; Java/API/profile/dependency state and Operator MCP `1.1.0` remain unchanged.

## G2-M1 player monthly income observation static-ready

- Reused the exact-build `0x28DBE90` complete monthly-gold-income evaluator already proven by the war-exit observer. The campaign-root wire now
  publishes signed Q100000 `player_monthly_gold_income` and revalidates the full-generation Character pointer after each call; the complete value
  participates in the existing application-main double observation. The lagging `extension+0x2B0` cache remains excluded from readiness.
- Bad evaluator return, generation drift or inter-sample value drift fails the whole root frame as `player_monthly_gold_income_unavailable`.
  `ck3_query_turn_bundle_v1` now exposes the value and raises `ruler_resources_ready` only when current gold is also available. Full bundle
  readiness remains false because health, domain, council, faction and partition observations are still open.
- Release DLL build/link is GREEN (`2,636,800` bytes, `98FC243165EB0E136AE6160A6F665856C3AFF01E971E4F01B1D5BCD1FF0566C2`);
  native reader and source-contract executables are GREEN; focused campaign-root/live-harness/turn-bundle Python tests pass normal and optimized
  modes. The native-driver mirror now also preserves the existing primary-title succession vector.
- Status is `static-ready / live=false`. No CK3, recorder, injector, mouse, keyboard or foreground window was used. G2-M1 remains in progress,
  global G2 remains `0/8`, GEN-034 remains `2/4`, and domain capacity is the next off-screen observation candidate.
- Root implementation `e16d179db49d8613ff0576d7e7b1dff4001b8e4d` and open_kaishek compatibility record
  `e57df8284baac7a3151c75623a2c748bfa537860` are pushed and synchronized. The companion has no campaign-root/turn-bundle caller or closed
  response type; its Java/API/profile/dependency state and Operator MCP `1.1.0` remain unchanged.

## G2-M1 player domain capacity observation static-ready

- Froze the exact-build reflection chains for `Character.GetDomainSize`
  (`0x50BE60 -> 0x2622C40 -> 0x260BA50`) and `Character.GetDomainLimit`
  (`0x50C160 -> 0x2622CC0 -> 0x260BA20`). The existing campaign-root query now publishes
  `player_domain_size` and `player_domain_limit`, revalidates the full-generation Character after both calls and includes both values in its
  application-main double observation.
- `ck3_query_turn_bundle_v1` projects `{size, limit, available_capacity, over_limit_by}` and now reports
  `realm_domain_ready=true`. This minimum slice does not claim holding identities, buildings, construction or the adjacent
  `GetDomainSizeWithGracePeriod` penalty input; full bundle readiness remains false for health, council, faction and partition.
- MSVC Release reader/source-contract fixtures are GREEN. Focused campaign-root/live-harness/turn-bundle Python tests pass normal and optimized
  `39/39`. Release DLL is `2,637,824` bytes / `ACCC5E90...CF7ED1`, with all 27 candidate switches OFF.
- Extended the reusable `find_xrefs.py` helper to recognize all Capstone-decoded x64 RIP-relative memory operands and accept bounded source RVA
  ranges. This closed the reflection registration lookup without a one-off scanner.
- Status is `static-ready / live=false`; G2-M1 and global G2 remain `in_progress` / `0/8`, GEN-034 remains `2/4`. No CK3, recorder, injector,
  keyboard, mouse or foreground window was used. The same future paused G2 read will verify domain capacity with the other pending M1 fields.
- Root implementation `9ed271f667dc78e8a654d938ef554d658c2b9465` and open_kaishek compatibility record
  `11e5054b878892495b4bf7ab4be936e6924420ab` are pushed and synchronized. The companion has no affected caller or closed response type;
  Operator MCP remains `1.1.0`.

## Reclaim the Motherland 0.2.0 release-complete

- Delivered both Phase 2 goals: retained loyalists preserve the same title, realm name and subordinate tree, while Pro-Hegemon lords now resolve through hard-defect, hard-stay or one frozen scored roll under the default 【人心离散】 rule. The compatibility choice 【誓死尊王】 keeps the 0.1 behavior, and 【人心向背】 reports representative outcomes.
- The 32-file product passed 13/13 contract tests, 10/10 build tests, nine-language static validation and reproducible build. Source-tree R0004 and fresh Workshop-cache R0007 were both MCP-first GREEN with 20 ordered markers, zero project diagnostics, protected storage unchanged and native cleanup proven.
- Published existing Workshop item `3798404599` with exact public title, 5,446-character normalized BBCode and a separately verified 508-character／14-line Change Notes entry. Four checked-in screenshots from the GREEN run resolve publicly, and the public page retains the required `Crusader Kings III: All Under Heaven` DLC.
- Forced a fresh subscription download and verified all 32 files against the formal manifest. The previous 28-file cache was moved intact to a recoverable evidence directory. The fresh-cache descriptor exposed a tooling false RED; `52456f9f` now accepts Steam's exact canonical no-ID form as well as the launcher's one-ID form, with 10/10 focused tests GREEN.
- Release tag `reclaim-motherland-v0.2.0` targets `6486a0c72dda61c3b4e4466bc9db1193e83c74bc`; manifest / ZIP SHA-256 are `2d63b2c6...ccce73` / `5981535815...6c8550`. Steam is back in Offline Mode, the client remains running and CK3 is stopped. Full release identity and limitations are recorded in [the 0.2.0 changelog](../../release-changelogs/reclaim-motherland/0.2.0.md).

## T0 P2 R589–R593 four-span take and Central summary correction

- Ran one continuous FFmpeg P2 take. R589 warmup and R590–R593 gameplay replacements remained single-instance; four clean spans completed, then HC source stopped fail-closed. The incomplete take does not count, so P1 remains `9/9 GREEN`, P2 remains `0/8`, and the final promotional-video lock remains active.
- R593 proved the shared `ep3_story_cycle_admin_eunuch.8030` contract live: every exact identity/scope/option check passed and authored 4/native 3 advanced instance `621` without gold or stress drift.
- The next event was the product-owned Central summary `zg361p2c.2`. Its existing strict 1/0 contract matches the real frame completely; the RED was a runner routing gap because only reviewed vanilla contracts were tried while blind single-option clearing correctly remained disabled.
- Commit `e2dbf758ca5f735625a0f45caefd917205847f29` adds a reviewed Central product route limited to `CENTRAL_TIMELINE_CONTRACTS`. Unknown events and contract drift remain RED. Focused normal/optimized suites pass reviewed wait `4/4`, choreography `23/23`, and Central recovery `67/67`.
- Evidence report SHA-256 is `6D413E3D...B263A`; raw MKV is `290,584,880` bytes / `D4B3322B...286A8`. R589–R593 are terminated and final CK3/FFmpeg inventories are zero. No mod, DLL, MCP ABI or public interface changed; open_kaishek is unaffected. Full record: [R589–R593 recovery](../../phase2-promo/r589-r593-four-span-take-and-central-summary-recovery-2026-09-13.md).

## T0 P2 R594–R603 reviewed product interruption recovery

- R594–R598 again completed four clean spans. R598 proved Central `zg361p2c.2` live GREEN and stopped on product `zg361.40`; its existing two-option contract selected the source-reviewed activity-planner route. Commit `a44ca20f33f48e85456f8ab24eda85c651f5545a` added only that exact key to the P2 product route.
- R599–R603 again completed four clean spans. R603 proved `zg361.40` authored 1/native 0 production-live, then also cleared two reviewed vanilla events before stopping on the inert one-option annual summary `zg361.1`. Its real frame passes every existing manager-recovery contract check. Commit `5e8d77365a1fdda0510c32adba7d3b8750f9219f` adds only that key.
- Both failed takes remain uncounted: P1 is `9/9 GREEN`, P2 is `0/8`, and the final-video lock remains active. Generic single-option clearing is still disabled.
- R594–R603 and both FFmpeg processes are terminated; cleanup and protected-storage checks are GREEN. The newer report SHA is `8990E275...D7FA`, raw MKV is `350,673,985` bytes / `31D66FAE...B7A6`.
- No mod, DLL, MCP ABI, version, dependency or public interface changed; open_kaishek is unaffected and no MCP asset was added. Full record: [R594–R603 recovery](../../phase2-promo/r594-r603-reviewed-product-interrupt-recovery-2026-09-13.md).

## T0 P2 R604–R608 原版瘟疫事件作用域变体

- R604 warmup 与 R605–R608 gameplay replacements 保持 CK3 单实例；连续 FFmpeg take 完成前四个 clean spans 后，在 HC source 的原版 `epidemic_events.1060` fail-closed。失败 take 不计数：P1 `9/9 GREEN`、P2 `0/8`、最终宣传片继续硬锁。
- R608 frame 精确新增 `faith_to_blame: faith`。exact CK3 1.19.0.6 源码证明只有宗教少数群体随机分支导出该作用域；原有 authored 3/native 2 仍是终止且减缓 witch trials 的 reviewed route。
- `19828ebf312abe8c0504f34c1288e57eaf03d455` 只给共享合同增加精确四作用域变体，并把 exact-build 来源、调用链和 R608 artifact 写进通用 MCP 事件知识。错误类型继续 RED；普通/optimized 聚焦测试、13 个资产测试及公共 query 回放均 GREEN。
- R604–R608、FFmpeg 和 injector 已全部终止，protected storage 未变化。report SHA `8AF53648...E862`，raw MKV `326,805,944` bytes / `BD0AE9EB...60B6`。没有公共接口变化，open_kaishek 无需修改。完整证据：[R604–R608 恢复](../../phase2-promo/r604-r608-epidemic-1060-scope-variant-recovery-2026-09-13.md)。

## T0 P2 R609–R613 淘汰事件路由

- R609 warmup 与 R610–R613 gameplay replacements 保持单实例并完成前四个 clean spans；HC source 在 `zg361.5 options=3` 保留 RED。失败 take 不计数：P1 `9/9 GREEN`、P2 `0/8`、最终宣传片继续硬锁。
- R613 先依次 GREEN 清理 `.8030`、Central、京察、`.1060`、debate、emperor-yearly 与年度总览。`.1060` 这次是既有三作用域形态，authored3/native2 实机 postcondition GREEN；提交 `1b4f30312f1ce6467a0cbaf1db4082ec574cf733` 将其记入共享 MCP 资产。R608 的四作用域变体仍只标为实见 RED + 静态回放。
- `zg361.5` 已有源码审阅合同；R613 的 103-scope frame 对 manager-recovery 投影全部检查为 true。`3330f4c4202d62b5e0bbe043253578d203b0481a` 只把该 exact key 接入 P2 product route，固定 authored3/native2；普通/optimized 各 `7/7` GREEN。
- R609–R613 和 FFmpeg 已全部终止，cleanup GREEN、protected storage 未变。report SHA `4B487FEE...8736`，raw MKV `360,285,151` bytes / `13BA6A1A...0885`。open_kaishek 的非必需 full-root validator 仍明确 RED（parser GREEN；`175,310 UNKNOWN_OPCODE`）；本包无接口变化，不修改兼容层。完整证据：[R609–R613 恢复](../../phase2-promo/r609-r613-elimination-interrupt-recovery-2026-09-13.md)。

## T0 P2 R614–R618 原生暂停竞态恢复

- R614 warmup 与 R615–R618 gameplay replacements 完成前四个 clean spans；HC source 的原生 `pause-map` 返回已接受的 `already_paused`，旧 runner 将它误判为 RED。失败 take 不计数：P1 `9/9 GREEN`、P2 `0/8`、最终宣传片继续硬锁。
- `e642e39fae3dc84d5aee7fccedda49e4d04e65c3` 允许已接受的 `submitted` 或 `already_paused` 继续进入同一套事件、日期、玩家、revision 和真实暂停后置门；其他 ACK 仍失败。聚焦测试与编译检查 GREEN，没有扩大验收范围。
- R614–R618 与 FFmpeg 已全部终止，cleanup GREEN、protected storage 未变。本轮没有公共接口、DLL 或游戏文件变化，open_kaishek 无需修改。完整证据：[R614–R618 恢复](../../phase2-promo/r614-r618-native-pause-race-recovery-2026-09-13.md)。

## T0 P2 R619–R623 Career HC 连贯链路由

- R619 warmup 与 R620–R623 gameplay replacements 再次完成前四个 clean spans；HC source 在 `zg361ch.950 options=4` 保留 RED。失败 take 不拆分计数：P1 `9/9 GREEN`、P2 `0/8`、最终宣传片继续硬锁。
- R623 在新阻点前依次安全处理六个已审原版/产品事件。`.950` 的 104-scope 实机帧对现有 Career HC 合同 `17/17 GREEN`，固定选择 authored1/native0；根因是完整 51-key 合同链未注册进 capture runner 的受审产品路由，不是产品事件语义缺陷。
- `bfc19480e2844174fc1322761433a25632f6d663` 复用整个 `CAREER_HC_TIMELINE_CONTRACTS` key 集；注册表外事件仍保持 RED，通用单选自动清理仍禁用。普通/optimized 聚焦测试各 `8/8 GREEN`，没有逐事件长跑。
- R619–R623 与 FFmpeg 已全部终止，cleanup GREEN、protected storage 未变。report SHA `0B4AA93C...8844`，raw MKV `355,773,513` bytes / `89469150...7424`。本包没有接口变化或新 MCP 资产，open_kaishek 无需修改。完整证据：[R619–R623 恢复](../../phase2-promo/r619-r623-career-hc-chain-recovery-2026-09-13.md)。

## R629 人工体验：决议条件内部旗标文案修复

- R629 以 commit `c23eeb1649e9d288039a8967fd56822c1387fd12` 的 production staging 启动，仅加载天朝二期，交由项目所有者人工体验；PID `209948` 为唯一 CK3 实例，本工作包没有注入输入、切换前台或重启游戏。
- 人工实机发现决议条件把 `has_character_flag` 的内部名称直接显示为“……旗标”。根因是三处玩家可见 validity 路径没有用 `custom_description` 隔离技术 trigger；这是产品文案缺陷，不是预期措辞。
- 修复覆盖“立即开展绩效考核”“打开下一项制度评审”“颁行《三六一考功章程》”三处入口，分别改为考核季、待入卷裁定和待颁行章程的业务表述。底层旗标、资格、费用与 effect 均未改变；生成器是机制决议的权威来源。
- 聚焦验证包括生成器 `--check`、两项针对性单测、Python 编译与 `git diff --check`，均为 GREEN。当前 R629 仍加载修复前 staging，不能作为修复后画面证据；待人工体验结束后，用下一轮 fresh product 做一次决议面板抽检即可，不为该文案缺陷扩大成长跑。
- 本包不改变 DLL、MCP ABI、数据格式、依赖或对外接口，不影响 open_kaishek，也没有新增 MCP 通用资产。P1 仍为 `9/9 GREEN`，P2 仍为 `0/8`，最终宣传片硬锁未触碰。

## R629 人工体验：绩效榜长页面滚动视口修复

- 项目所有者在 R629 的“大朝绩效榜与制度账本 → 所辖官员 → 事实”页实见长字段列表越过窗口底边，且没有可用滚动条。原始截图保存在 `Z:\ck3_mod_rewrite\_runtime\manual-t0-r629-c23eeb1-20260913\r629-scoreboard-overflow-user.png`，SHA-256 `03123028B1893C28C3A7F9A21B6A5DD10BFB184A72D42318BE23383F2074FE16`。
- 根因不是缺少 `scrollbox` 声明，而是生成器没有约束固定窗口与页容器的 minimum-size 传播，也没有让互斥页容器忽略隐藏页；内容把滚动视口本身反向撑长。修复在权威生成器统一覆盖七个滚动面：两张榜单、四个详情页和制度账页。
- 固定面板根布局、榜单页、详情面板、四个详情子页和制度页现在均有明确的 bounded-parent 约束；两个互斥页布局层忽略隐藏页；七个 scrollbox 均显式裁切并保留横纵 `as_needed` 标准滚动条。生成结果只改变 `gui/zg361_scoreboard.gui`。
- 生成器 `--check` GREEN，针对性的响应式滚动结构测试 GREEN。R629 仍为唯一用户控制实例并加载旧 staging；修复后画面只待下一轮 fresh product 抽检一个长详情页和一个长名单页，不做长跑。
- 本包不改变脚本业务、存档、DLL、MCP ABI、数据格式、依赖或对外接口，不影响 open_kaishek，也没有新增 MCP 通用资产。P1 `9/9 GREEN`、P2 `0/8`，宣传片硬锁未触碰。

## 2026-09-13: R629 Career/HC effect-preview copy

- Manual play showed `EVENT_NOTHING_HAPPENS` on Career/HC choices whose real writes were hidden behind generated scripted effects. The generator now authors a concrete tooltip for every 44-by-3 individual route and all four portfolio-policy choices, including the #107 two-cycle readiness review, the #116 90-day release exception and all seven dual-ledger payments.
- Chinese and English copy is authored; the other seven development locales retain English placeholders. Business effects, guards, player/AI routing and public interfaces are unchanged. Generator reproducibility and the focused runtime suite pass `57/57`; status is `static-ready` pending one fresh-product UI spot check after R629.

## 2026-09-13: R629 bottom-quota selector consolidation

- Replaced the three duplicate 10%/5%/0% decision cards with one `zg361_ratio_policy_decision`. Its title shows the current setting and opens one three-option event; the active option is disabled, and a selection keeps the existing 75-prestige cost, one-year cooldown and next-cycle `zg361_ratio_override` semantics.
- Focused Phase 2 wiring passes `15/15`, localization and release-projection checks are GREEN. Status is `static-ready`; a single decision-list-to-variable spot check is pending in the next fresh-product CK3 round. No MCP/native interface or open_kaishek compatibility surface changed.

## 2026-09-13: R629 Career/HC click-load reduction

- R629 manual play exposed a real interaction-cost defect: the advertised 90/180-day limits governed case deadlines, while the standard Career/HC path still delivered one policy choice, 22 individual rulings and six filing-only completion cards on consecutive D+1 steps. The nominal streamlined path therefore required 29 clicks.
- The portfolio policy now applies evidence-led, expedient or defer treatment to all 44 original numbered manager/core/consumer effects. Guard or resource failures reopen only the exact affected card; the detailed fourth option still preserves all 44 individual rulings. Five intermediate completion popups are suppressed and one final receipt closes either the Q path or the lower-rank P terminal path, reducing the ordinary path to two required clicks without lengthening the central lane.
- All 44 generated background definitions, seven funded preflights, 90/150-day release semantics, player/AI boundaries and granular fallbacks are covered by the focused `57/57` suite. Central runtime `45/45`, Phase 2 wiring `15/15`, generator reproducibility and local static validation are GREEN. Status is `static-ready`; one fresh-product policy-to-final-receipt spot check remains after R629. No MCP/native interface or open_kaishek change is required.

## 2026-09-13: R629 persistent Career/HC custody control

- Added a player decision that keeps one of three Career/HC review routes as the default across future cycles: evidence-led, expedient or defer. Custody is off by default and can be disabled at any time; an already-open portfolio retains its frozen route.
- The central adapter still selects exactly one eligible direct official per cycle. For each future selected official, custody removes the opening policy card and final filing receipt, making a normal portfolio zero-click while preserving the exact numbered fallback card for a real identity, guard or resource failure.
- Focused Career/HC tests pass `58/58`; Phase 2 wiring `15/15`, local static validation, the generator reproducibility check and release-builder tests `9/9` are GREEN. Status is `static-ready`; R629 remains the sole user-controlled CK3 instance on older staging, so one decision-to-next-portfolio spot check is deferred to fresh R630.
- No public MCP/native interface, version or dependency changed; open_kaishek is unaffected. P1 remains `9/9 GREEN`, P2 remains `0/8`, and the final promotional-video lock remains active.

## 2026-09-13: R630 compensation option hover defect

- R630 manual play exposed `EVENT_NOTHING_HAPPENS` / ???????? on all three visible routes of the `zg361comp.1` L1 compensation docket. The buttons already stated the exact reward, bonus and treasury/personal-account result; the hover layer could not expand the internal portfolio scripted effect.
- The authoritative generator now binds each existing exact option text as its own `custom_tooltip` across all 14 stages and three routes, covering 42 variants without changing gates, accounts, deadlines, state transitions or AI routing. The owner screenshot is preserved at `Z:\ck3_mod_rewrite\_runtime\manual-t0-r630-6fe53dd-20260913\r630-compensation-no-direct-impact-user.png`, SHA-256 `C0537F8E...BAD54`.
- Focused compensation tests pass `35/35`; generator reproducibility, local static validation and `git diff --check` are GREEN. R630 remains the sole user-controlled process on pre-fix staging, so the RED is closed in source but the presentation fix remains `static-ready` until one fresh-product hover check.
- P1 remains `9/9 GREEN`, P2 remains `0/8`, and the final-video lock remains active. No MCP/native/public interface changed; open_kaishek is unaffected.

## R630 人工体验：绩效榜滚动补丁回归与修正

- R630 加载 `a57bcab76f7698cb0bfeb7f16aa3e9ad824ebadd` 之后的 production staging；项目所有者打开绩效榜时实见面板被内容最小尺寸撑出屏幕、头部与主体消失，只剩超大黑色背景。截图保存在 `Z:\ck3_mod_rewrite\_runtime\manual-t0-r630-6fe53dd-20260913\r630-scoreboard-scroll-regression-user.png`，SHA-256 `85DF2430154899F4215FA8A72C3A202D310BBC5AE97E8F399CD1389E1EB5D7DE`。
- 回归由上一包在 `90% × 90%` 百分比面板链上新增 `restrictparent_min = yes` 导致；它没有限制内容，反而把长内容的 minimum size 重新传给外层。该错误属性已从根布局、详情容器和七个页面全部移除。
- 修正采用原版固定窗口结构：面板直接子 `vbox` 明确占父级 `100% × 100%`，并忽略隐藏页；页面与七个 `scrollbox` 在这个固定视口内扩展，滚动面保留 `scissor = yes`、双轴 `as_needed` 和标准滚动条。
- 生成器重建/`--check` GREEN，绩效榜完整聚焦测试 `36/36 GREEN`，BOM 与 `git diff --check` GREEN。R630 仍由项目所有者控制并加载旧 staging；源修复为 `static-ready`，下一新轮次只抽检一个长详情页和一个长名单页。
- 本包不改变游戏业务、存档、DLL、MCP ABI、数据格式或依赖；open_kaishek 无需修改。P1 仍为 `9/9 GREEN`，P2 仍为 `0/8`，最终宣传片继续硬锁。

## R630 人工体验：提前考核与京察关系澄清

- 实现链确认“绩效考核”是冻结名册、自评、互评、校准、公示和结算的底层流程；“京察大计”是正常周期召集这条流程时附带的履责活动。京察召令下达当天已经开考，完成活动只算履责并给出活动收益。“立即开展绩效考核”则花费 150 威望提前启动同一流程，不举办京察，也不完成京察义务。
- 玩家界面将手动入口改名为“提前启动本年度考核”，并在说明、悬浮和确认按钮中明确上述边界。权威 feature freeze 同步修正了原先与实现相反的“真正完成活动才考核”条目。
- R630 截图还实见 `zg361_review_now_decision_valid_desc has no localization`。该键在源码与 R630 product staging 的九语文件中实际存在，运行日志也没有对应 missing-loc 记录；精确引擎内部原因未留下可读日志。为避开与决议自动派生说明同形的键名，两个 `custom_description` 改用普通业务键 `zg361_review_now_business_ready`，九语结构同步，旧键由聚焦测试强制禁止。
- 截图保存在 `Z:\ck3_mod_rewrite\_runtime\manual-t0-r630-6fe53dd-20260913\r630-review-now-jingcha-relationship-user.png`，SHA-256 `BB1E409CFFA1F5883A95E6962A5B5D6A8CC43D5CAEC6FB053EE8AD7EB4948AAB`。本地化聚焦测试与 `validate_local.py` GREEN；R630 加载旧 staging，下一新轮次只需抽检该决议卡和条件悬浮。
- 本包不改变考核、京察、费用、冷却、AI、存档、DLL、MCP ABI 或数据格式；open_kaishek 无需修改。P1 仍为 `9/9 GREEN`，P2 仍为 `0/8`，最终宣传片继续硬锁。

## R630 人工体验：薪酬案卷按钮与悬浮信息分层

- 项目所有者继续实见上一包虽补出了悬浮说明，却直接把原有详细结果全文同时留在选项按钮中；三条按钮过长，且金额、账户、账期等结果没有形成“短行动 → 详细悬浮”的信息层级。截图保存在 `Z:\ck3_mod_rewrite\_runtime\manual-t0-r630-6fe53dd-20260913\r630-compensation-button-overflow-user.png`，SHA-256 `A20D4C51CA5C2659F0CFCE468898C3CD735174B979E62FA0C53F56F6D5658CF4`。
- 权威生成器现为 14 个阶段的三条路线分别生成 42 个简短、阶段语义化的按钮标签；原有精确结果迁入对应的 42 个独立 `.tt` 键，事件选项只绑定该悬浮键。金额、冻结绩效、国库/私库、账期、欠付、后续阶段和离任结果没有丢失。
- 简中与英文完成日常开发精修，其余七语使用英文占位以保持结构可加载。trigger、effect、账本、结算、资源前置和 AI 路由均未改变。
- 生成器 `--check` GREEN，薪酬聚焦测试 `35/35 GREEN`，Python 编译、九语 BOM 与 `git diff --check` GREEN。R630 仍加载旧 staging；下一新轮次只抽检一张薪酬案卷的三条按钮和三条悬浮。
- 本包不改变 DLL、MCP ABI、数据格式、依赖或对外接口；open_kaishek 无需修改。P1 仍为 `9/9 GREEN`，P2 仍为 `0/8`，最终宣传片继续硬锁。

## R631–R632 绩效榜嵌套页面滚动实机修正

- R631 因启动准备脚本把路径替换写成无效 PowerShell 正则，生成了 2 字节空描述文件；`error.log` 明确记录 `Incorrect MOD descriptor`。该轮立即终止并保留为操作 RED，没有用于验收。R632 修正描述文件后以 commit `2ab2185c377c698e388415347a8b79a60cfc0bbf` 的 1,034 文件 production staging 启动，继承 R630 手动存档且保持暂停。
- R632 证明上一包已经消除整窗黑屏，榜单页也重新受窗口约束；但进入“事实”详情页后，底部字段仍被裁掉，鼠标滚轮不能移动内容。实机截图为 `Z:\ck3_mod_rewrite\_runtime\manual-t0-r632-2ab2185-20260913\scoreboard-detail.png`，SHA-256 `279272685FC9C3B1972514CE64444A76A93953564ADFBDE9466DAC85113B4CA0`；滚轮操作后的截图 SHA-256 为 `B2D6CAC831D29B90100CDAE7D4015783035B111D9C6E5948CE1C9B3B5EAD5703`。
- 当时的最小假设是：上一包移除 `restrictparent_min` 时同时移除了详情切换层原有的 `ignoreinvisible = yes`，四个互斥扩展页因此共同参与布局。权威生成器只恢复该属性，百分比面板链继续禁止 `restrictparent_min`；生成器重建与聚焦测试 `36/36 GREEN`。这个假设随后由 R633 实机否定，不能作为最终根因。
- R632 在这一步终止；下一轮仍只重验该详情页能否滚动及列表页是否回归，不扩大为长跑。
- 本包不改变业务逻辑、存档、DLL、MCP ABI、数据格式或依赖；open_kaishek 无需修改。P1 `9/9 GREEN`、P2 `0/8`，最终宣传片硬锁未触碰。

## R633 绩效榜 scrollwidget 最小尺寸传播修正

- R633 加载 commit `7e2c6471e5f6862529c01b1d0f268baed4fcc50b` 后重复同一三步：打开榜单、打开第一份案卷、在“事实”内容区滚轮向下。画面仍裁底且没有位移，证明单独恢复详情层 `ignoreinvisible` 不足。操作前后截图分别为 `scoreboard-detail-before-scroll.png` / `scoreboard-detail-after-scroll.png`，SHA-256 `08AAF1FA...C2C96` / `FCB9DECB...B6FA6`。
- 剩余共同异常是七个 `blockoverride "scrollbox_content"` 都在滚动模板包装层使用 `set_parent_size_to_minimum = yes`。这会让 scrollwidget 接受长内容的 minimum size，继续把内容高度当成可见高度；固定面板无法由此形成可滚动差值。原版普通固定视口 scrollbox 直接放置唯一内容子项，不设置这条反向传播。
- 权威生成器已从榜单、四个详情页和制度账页的共同结构移除 `set_parent_size_to_minimum`，保留内容自身 minimum width、`scissor`、双轴 `as_needed` 与标准滚动条；详情切换层继续忽略隐藏页。聚焦测试改为断言七个滚动面都禁止该属性。
- R633 仍加载修复前 staging；下一轮只复验“滚动前后字段位置变化、底部字段可到达、列表不回归”，再检查另外两项 UI。业务、存档、DLL、MCP ABI、数据格式和依赖均未变化，open_kaishek 无需修改；P1 `9/9 GREEN`、P2 `0/8`，视频硬锁未触碰。

## R634 绩效榜固定视口实机闭环

- R634 以 commit `d95ce36dcfe7dbf879ad6670f11ed4c64b2b0146` 的 1,034 文件 production staging、独立 `-userdir` 和继承的人工存档启动；当前只加载 `mod/zg361_manual.mod`，保持暂停。product manifest SHA-256 为 `29B60F34F7E0D337EDD2293712FF9586A017E125BC92DA8E49101CDA2C6D1170`。
- 所辖官员名单正常占满面板而未撑高窗口，截图 `scoreboard-list.png` SHA-256 为 `E298F21E037B4DB1C032AA3536C9B797AB59ED8CED57B8CC25DB2F92837DAD9A`。进入第一份案卷的“事实”页后，长字段被裁在固定视口内且右侧滚动条出现；鼠标停在内容区靠近滚动条处向下滚动，首行由“冻结绩效指标总分”移动到“证据：个人能力”，继续滚动后滑块抵达底部且末行“回应暂定评价的年份”可见。
- 滚动后的中段截图 `scoreboard-detail-wheel-near-scrollbar.png` SHA-256 为 `684705098B657D11D47BF48F8811B0ADDA1260C55634DA2DB671F01844D19013`；到底截图 `scoreboard-detail-bottom-reached.png` SHA-256 为 `B0FAC000FE22EC2724A3AF4E27D62AA9EF580AEB33459053A0958E3C3B9F383A`。用户报告的“高度失控 + 黑屏 + 无滚动”至此为 live GREEN。
- 验证保持在单页、单名单的最小范围，没有长跑。该包只改变 GUI 布局，不改变业务、存档、DLL、MCP ABI、数据格式或依赖；open_kaishek 不受影响。P1 仍为 `9/9 GREEN`、P2 仍为 `0/8`，视频硬锁未触碰。

## CK3 coat-of-arms browser-to-native MCP closure

- Completed a real browser path from the Vue editor through Quarkus REST, MCP Java SDK 2.0.1, the Python stdio MCP server and the named-pipe native bridge into CK3 `1.19.0.6`; no OCR was used for the syntax result.
- Headless Edge clicked the visible Connect, Apply-to-designer and Read-from-CK3 controls. Its own fetch trace captured three session GETs, one probe POST and one export POST, all HTTP 200. The probe returned `applied`; native Copy returned a 483-byte CRLF canonical source and the same Vue page parsed it with zero diagnostics.
- The payload adds live evidence for an `rgb` typed color, a one-value mask normalized by CK3 to `{ 1 0 0 }`, two repeated instances, decimal transforms/depth, rotations and a negative X scale. Binding was PID `17168`, snapshot `native:1`, revision `2`, native revision `1`, connection generation `1`.
- Browser evidence is `artifacts/coa-clipboard-probe-2026-09-08/browser-quarkus-mcp-live-e2e1.json` (54,897 bytes, SHA-256 `DB491DA4...07DAF6`). The host report is `mcp-rest-live-host1.json` (2,581 bytes, SHA-256 `F1C933AD...A38F0`) and records `cleanup_proven=true`, `tree_gone=true` after the controlled stop.
- Readiness advances to `browser-mcp-live`. Upper-level Finish/persistence, runtime DLC/mod effective resources and native pixel comparison remain open. Focused verification is Quarkus `9/9`, Vitest `24/24`, frontend production build GREEN and Quarkus package GREEN.

## R636 薪酬案卷按钮与悬浮实机闭环

- R636 加载 commit `98531f42fa7318e3315c23d8fb3f8861de1fa346` 的 1,034 文件 production staging，在隔离 profile 和继承的人工存档中只触发 `zg361comp.1`。14 阶段共享生成器的三条按钮在本轮显示为短动作：“核准绩效导向专项奖”“核准年功导向专项奖”“驳回专项奖”。
- 三条悬浮分别完整显示本阶段的支付总额、国库与私库分摊、年功/绩效账面拆分或零支付结论。R635 曾证明角色变量无法在 `custom_tooltip` 本地化上下文解析，导致当事人姓名留空并产生 loc data error；commit `1c04f7b1a3f83fc330792d8022ddc81fefb2857d` 将该处改为不依赖动态 scope 的“当事人”，业务账目和 effect 未改变。
- 实机证据为 `compensation-base.png`、`compensation-hover-performance.png`、`compensation-hover-merit.png`、`compensation-hover-reject.png`，SHA-256 依次为 `999160BFCA95D3999521CF191D70349BF839C2BE84937FD1C2023DEC3A75928F`、`C5560EBC9D4F57704C1BBC47FC2B3BD84B0F3F1979AEDCB9EFE1C3980A92D40F`、`86008D1F579C6F1739E794BC65AD4B5754A266C7378106763008CCB0F3D35259`、`F378B6A2DD94F28E995EB6D59BE4FB1C971E9EAA76C76516EDE77ED40F9669E7`，位于 `Z:\ck3_mod_rewrite\_runtime\manual-t0-r636-98531f4-20260913\`。
- 三次悬浮前后 `error.log` 均为 15,308 行，增量 0。验证止于一张案卷的三个选项，没有长跑。薪酬案卷的“按钮过长、结果未放入悬浮、动态当事人为空”三个连续 RED 至此为 live GREEN。R636 已终止，当前轮次 R637 用于另一项定点复验。
- 本包未改变 DLL、MCP ABI、数据格式、依赖或对外接口，open_kaishek 不受影响，也没有新增 MCP 通用资产。P1 保持 `9/9 GREEN`。

## R637 提前考核条件悬浮实机闭环

- “提前启动本年度考核”继续调用与京察相同的冻结、评价、公示和结算业务链，但不举办京察活动，也不完成京察履责；R637 的决议说明保持这一关系。按钮在当前存档中仍因同年已结算而不可用，真实业务门禁没有放宽。
- R635 证明给完整业务 trigger 套 `custom_description` 仍会在该决议悬浮中暴露自定义键；R636 移除包装后又实见紫色 `character_var_equal has no localization`。后者来自同年幂等门禁中的 `var:zg361_last_settled_year = current_year`，属于引擎内部谓词，不是可发布文案。
- 对照 exact-build 原版决议后，commit `b557e414d29e643e25461d1e8ebe7002094533bb` 保留 `is_valid` 中完整的共享业务 trigger，只将展示专用的 `is_valid_showing_failures_only` 改为 `always = yes`。这是原版用于分离完整合法性与玩家可读条件的既有范式；静态合同继续断言共享业务门禁、同年守卫和至少一名直属可评官员均存在。
- R636 RED 截图 `review-decision-hover.png` 的 SHA-256 为 `52B2C03FCA3FEB94DF1DC6DEDD1768B46BC069C0B025492397DB2EAB23205D1C`。R637 加载 `b557e41` 的 1,034 文件 production staging 后，同一决议悬浮只显示正常的“你未满足所有要求”和业务说明，紫色内部键消失；GREEN 截图位于 `Z:\ck3_mod_rewrite\_runtime\manual-t0-r637-b557e41-20260913\review-decision-hover.png`，SHA-256 `233A7861AC977E35171F196DECF74345E7160856DAC8CE42AE4EA05F03579801`。
- 悬浮前后 `error.log` 为 `15308 -> 15308`，增量 0。验证只覆盖该决议卡，没有运行整套验收或长跑。当前轮次 R637 是唯一 CK3 实例，旧轮次 R636 已终止。
- 本包未改变考核/京察业务、费用、冷却、AI、存档、DLL、MCP ABI、数据格式或依赖；open_kaishek 不受影响，也没有新增 MCP 通用资产。P1 保持 `9/9 GREEN`。

## 17:21 所有者优先级调整：T0 人工测试冻结，主线转 G2

- 项目所有者取消当前优先级周期的全部天朝二期宣传视频任务。旧 `P2 0/8`、source、失败 take 和双片计划继续保留为历史证据，但不再是待办或阻点；不得启动 FFmpeg、补拍、剪辑、审片、导出、发布或预热。
- P1 保持 `9/9 GREEN`。绩效榜、薪酬案卷和提前考核三组已知人工体验问题定点闭环后，T0 进入人工测试冻结，且不得发布。之后若所有者报告新的真实 bug，做最小修复和相称验证，完成后回到 G2。
- 当前主线改为 T1 G2，固定总进度仍按八项可见 OODA 里程碑计算。当天 00:00 早会的 D1 标记为被中途指令取消，不回写成早会当时已知；D2-D5 中与 G2、兼容和证据维护有关的部分继续执行。
- 权威执行口径落在 [`2026-09-13 T0 人工测试冻结与 G2 优先级指令`](../../handover/2026-09-13-t0-manual-test-hold-and-g2-priority.md)。本次只改变优先级和发布授权，不改变任何公共接口或数据格式；open_kaishek 不受影响，无 MCP 通用资产变化。

## G2-M1 玩家健康观测 static-ready

- 冻结 exact-build `Character.GetHealth` reflection/registration/thunk 链与 core `0x2619AD0..0x2619B18`；函数体 SHA-256 为
  `B6E3700766AD592A3E9B2C1F01D8AE9B8DA4774A6F009B5D30A0DA4380A89840`。`campaign-root-context-v1` 现在双采样并发布玩家 signed
  Q100000 `player_health`，调用或 generation 回读失败为 `player_health_unavailable`。
- `ck3_query_turn_bundle_v1` 保留 raw 值，并按原版 `death_chance_dying_health=1.5`、`fine_health=3.0` 发布
  `dying_or_worse / below_fine / fine_or_better` 与 `ruler_health_below_fine`。治疗、病因、预后、生育力和死亡概率未冒充完成。
- MSVC Release reader/source-contract fixtures 为 GREEN；campaign-root、live-harness、turn-bundle Python 聚焦测试普通与 optimized
  模式均为 `42/42 GREEN`。未启动 CK3、录制器、injector 或桌面输入；当前轮次 R637 保留给所有者人工测试，旧轮次 R636 已终止。
- 状态为 `static-ready / live=false`。该字段将和其它 campaign-root 扩展共用下一次有界 paused G2 读取，不安排单字段长跑。
  M1 仍缺 council、partition 和两场景 live 验收；G2 保持 `0/8`、GEN-034 保持 `2/4`。公共 MCP schema 已变化，open_kaishek
  兼容层同步是本包提交后的立即伴随动作。

## 2026-09-13: G2 health-schema companion compatibility closure

- The G2-M1 health field changed the public campaign-root and turn-bundle shape, so the companion audit ran immediately against a real, clean `Z:\workspace\open_kaishek` checkout. That repository was first rebased onto `origin/main` and then documented at commit `6492af9732742c0aae3b7ddea1efc624980f0aee`; no consumer, closed Java type, runtime binding or version bump was required.
- The first exact verifier run retained a real RED because the root compatibility fixture still pinned the earlier companion commit. The minimal root fix updates only the canonical companion pin in the public bridge constant, compatibility fixture and its unit expectation; historical ABI and audit pins remain frozen.
- The exact external verifier now reports `GREEN_STATIC` with every contract, profile, checkout, read-only and schema check true. The three focused compatibility suites pass `17/17` in normal and optimized modes. No CK3 process, desktop input, DLL, game file or save was touched; current round R637 remains owner-controlled and unique, and old round R636 remains terminated.

## CK3 coat-of-arms runtime-feature MCP projection

- A new isolated CoA syntax-matrix round reached the CK3 main menu, but three synthetic-input paths did not change the menu state. No CoA MCP
  request was issued, so this is retained as a navigation harness RED rather than syntax or capability evidence. The scoped report is
  `artifacts/coa-clipboard-probe-2026-09-08/mcp-live-matrix2.json` (2,685 bytes, SHA-256
  `50DB03A0589F0E3CA6736FE9CB9B92A43717EC32BDC65DF416B4383F7E233998`) and records `commands=[]`, `exit_reason=stop`,
  `cleanup_proven=true`, `tree_gone=true`; CK3 and the two accidental interactive Python children were terminated. Steam remained offline.
- Instead of repeating the unchanged navigation path, the editor now consumes the existing production-live
  `ck3_query_loaded_feature_manifest_v1` through its bounded Quarkus allowlist, a revision-bound REST endpoint, a typed Vue client and a separate
  “读取运行态” control. It reports the current process's complete 44-feature truth and script `has_dlc` key count while explicitly refusing to
  infer store entitlement or the effective CoA VFS/registry winner.
- This editor increment is `mcp-static-ready / UI-live=false`: Quarkus `10/10`, Vitest `25/25`, frontend production build and Quarkus package are
  GREEN. The underlying runtime primitive retains its independent production-live evidence. No native/public MCP schema changed, so
  open_kaishek compatibility work is not required.


## G2-M1 exact council research and held-title partition

- The bounded council research froze the 1.19.0.6
  `CCharacter* + position_key -> ActiveCouncilTask` entry, 15 stock position
  keys and progress/task-scope leaves. Incumbent identity, stable task key,
  typed target and effective-seat fallback remain unknown, so
  `realm_council_ready` truthfully stays false. Research package commit
  `7fe06681ca98a2581084a51be4af23ef82fb4364` is pushed.
- The partition implementation reads each generation-valid player-held title,
  excludes validated baronies from the realm view, and publishes current first
  heirs for every county-or-higher title. Turn bundle now derives split/no-heir
  state and `succession_partition_split`; laws, claims and post-death
  reconciliation remain open.
- MSVC Release reader/source-contract executables are GREEN. Focused Python is
  `42/42` normal and optimized. One CTest attempt retained a BAD_COMMAND because
  the reused build directory contains mixed Cygwin/Windows executable paths;
  both generated test executables were then run directly and passed. No CK3,
  DLL injection, game file, save or desktop input was touched. Current round
  R637 remains unique and owner-controlled; old round R636 remains terminated.
- Partition status is `static-ready / live=false`; M1 still needs council and
  one shared bounded two-scene live read. The public schema change requires the
  immediate open_kaishek companion audit after this root package is pushed.

## 2026-09-13: G2 held-title partition companion closure

- Root commit `6eaf0b40af541b09faf04b4d9f01f1de6a588abe` added the public held-title partition field and split-risk projection. open_kaishek has no caller or closed response type for either root tool, so its compatible documentation-only sync is pushed at `30b130416091eab150a9df75edfc631a226b5287` with no adapter, version or dependency change.
- The canonical companion pin is refreshed to that commit. Historical compatibility pins remain unchanged. No CK3 process, desktop input, DLL, game file or save was touched; current round R637 remains unique and owner-controlled, and old round R636 remains terminated.

## 2026-09-13: G2-M1 typed council observation and companion closure

- Root commit `e7e99e872dbfe3d4774d000b7d9702e122ce0041` completes the exact-build council reader and publishes it through the campaign-root and turn-bundle MCP surfaces. Standard landed, non-nomadic rulers receive five typed core seats plus occupied auxiliary rows, stable position/task keys, target type and identity, frozen state and progress; landless, nomadic and missing-primary-title contexts remain explicitly outside the current coverage contract.
- A fresh Release build compiled all 549 targets and linked the DLL. The first CTest pass was `103/106`; the three retained integration REDs were stale fixture/source-contract expectations exposed by the new field. Their minimal expectation fixes pass `3/3`, while focused Python campaign-root/turn-bundle tests pass normal and optimized `47/47`. The package is `static-ready / live=false`; M1 still needs only its shared bounded two-scene same-process live gate, and G2 remains `0/8` until that gate is GREEN.
- open_kaishek has no campaign-root/turn-bundle consumer or closed response type. Its documentation-only compatibility record is pushed at `a6542064546441f4c175c54f3dbf947d7385b768`; the exact clean-checkout verifier is `GREEN_STATIC`, and the three focused compatibility suites pass normal and optimized `17/17`. No Java adapter, Operator MCP version or dependency changed.
- The reusable exact-build ABI, source fixture, native reader, strict Python normalizer, MCP projection and council decision-tree documentation are updated. Current round R637 was exited by the owner and no CK3 process remains; old round R636 remains terminated. No new round was started in this package.

## CK3 家徽开局前导航改为原生 MCP

- `matrix3` 在前端 MCP 缺少开局前页面能力时错误复用了鼠标/键盘导航链，违反本任务的 MCP-first 指令。该 attempt 仅作为过程偏离保留：`mcp-live-matrix3.json` 为 2,686 bytes，SHA-256 `E529C7DF...2C4914`，且 `commands=[]`；它不构成任何家徽语法或能力证据。
- commit `4398783814b2c877b81f2f8d9d6292dd9c449eb4` 新增固定、零输入的 `ck3_query_frontend_gui_route_v1` 与 `ck3_activate_frontend_new_game_v1`。DLL 在 CK3 application-main pump 上解析原生 GUI owner tree，仅允许主菜单的 `new_game_button`，不接受调用方控件名、native pointer 或回调地址；driver 必须独立观察 `main_menu -> bookmarks` 才能报告 verified。
- 新合同明确声明 `uses_ocr=false`、`uses_keyboard=false`、`uses_mouse=false`，并保持原 gameplay mailbox 的 paused proof 不变。Release DLL 重新编译/链接、原生邮箱与 source-contract fixture 均 GREEN；前端路由、家徽 probe/export 和官方 MCP SDK 聚焦测试 `26/26 GREEN`。
- 本包状态为 `mcp-static-ready / live=false`。没有启动 CK3、使用桌面或改动 Steam；Steam 保持离线。`open_kaishek` 没有 frontend GUI/CoA domain，本包预验证为 `not-applicable`。下一步实机验收必须重启 CK3，已按所有者指令在重启前停止并通知。

## CK3 家徽开局前原生 MCP 导航实机闭环

- 首个 live attempt 暴露两处真实 MCP 缺口：前端 step 虽已公告却未映射进 `GameAdapter::supports_step()`；旧 mailbox 只观察下层 `PeekMessageW`，主菜单会跳过该 pump。commit `84e1f5f139ef4d299fe7635f44f01144083ac8e0` 补齐 step 路由，并在 exact `HandlePdxEvents → SDL_PollEvent` 槽返回处增加延迟解析后的原生主线程边界。
- Release DLL SHA-256 为 `8FE08D0FE6E2793866CB6C3164472CB1CAC10BD05A45B1996FCB321537A515F0`。adapter registry、mailbox/source-contract 原生测试 GREEN，前端/CoA/官方 MCP SDK 聚焦测试 `26/26 GREEN`。
- `mcp-frontend-route-live3.json` 在 CK3 `1.19.0.6` 中完成官方 MCP `query(main_menu) → activate → query(bookmarks)`；十项闭环检查全为 true，明确零 OCR、零键盘、零鼠标。artifact 为 408,936 bytes，SHA-256 `1EBBFA6052967E02F2C929CDD11A76A312B75F85487E9B65DE0158CA7C14DDD5`。
- Steam 全程保持离线；受管 runner 最终 `cleanup_proven=true`、`tree_gone=true`，CK3 进程树已退出。该固定前端动作从 `mcp-static-ready` 提升为 `production-live primitive`；`bookmarks → ruler designer → coat-of-arms designer` 的动作仍未实现，下一工作包继续补 MCP，不使用鼠标链。

## CK3 家徽书签页到选角大厅 MCP 静态包

- 原版 `frontend_bookmarks.gui` 证明固定入口为 `frontend_bookmarks/pick_any_character_button`，动画结束调用 `GameSetup.OnCustomStart`；`multiplayer_types.gui` 证明下一原生 root 为 `lobbyview`（56,911 bytes，SHA-256 `93912D008D2362955470E7F42029280C41E3E316E28D8D1055EE09DFBE8BC3A3`）。
- 新增零输入 `ck3_activate_frontend_pick_any_character_v1()`，只允许 `bookmarks → lobby`，仍通过 application-main mailbox 和同一 exact-build GUI semantic dispatcher；ACK 后必须独立观察 `lobby` 才报告 verified，且固定声明零 OCR、零键盘、零鼠标。
- Release DLL、adapter registry、mailbox/source-contract 与 Python contract/service/native-driver/官方 MCP 测试均 GREEN。状态为 `mcp-static-ready / live=false`；`open_kaishek` 无 frontend GUI/CoA domain，本包预验证为 `not-applicable`。下一步是一次受管 CK3 live 验收。
- 首次实机 attempt `mcp-frontend-route-lobby-live1.json` 为 RED：固定动作后 CK3 进入选角地图加载，GUI pump 暂停使单次 route command 超时；artifact 94,811 bytes，SHA-256 `B8A98F66B942B785629FED6255E276E313CB8E5870DF809D64A05D6A36039763`，并且 `cleanup_proven=true`。driver 已增加 120 秒有界 transition 重试，测试覆盖暂时失败后恢复，不把加载期不可观测误报为成功。
- 第二次 attempt `mcp-frontend-route-lobby-live2.json` 继续保留为 RED（94,795 bytes，SHA-256 `87650AC340C0049B3279816DE1D49DE14C9CCC79027BD0DCD158CBFEAD24C9D8`）：加载后 gameplay snapshot 已出现，route transport 却误用了 CoA 专用的 `snapshot=false` binding。现已拆出 GUI route exact-bridge binding，仍锁定 PID/connection/build/capability，但允许选角 lobby 与 snapshot 共存；聚焦测试 `6/6 GREEN`，cleanup 再次 GREEN。

## G2-M1 双场景 production-live 收口

- commit `411c5441d8f6a4d2e5b12f7e1e0c0528b8aaf301` 增加可复用的同进程双场景 runner；聚焦测试 normal/optimized `19/19 GREEN`。它只读冻结源存档，不推进日期、不保存，并在一个 CK3 PID 内查询独立 ruler、typed 切换玩家、再查询 vassal ruler。
- 旧轮次 R638 保留真实产品 RED：两场景都在 `direct_landed_vassals_unavailable` 停止。其 exact build、玩家切换、日期、源存档与 cleanup 均有效，根因收敛为新 Character storage 扫描器错误拒绝可读的旧代际/复用 slot。
- commit `700fae3fcca5af1ebfee26d4cbaf26d3c5f0a2a9` 采用 exact-build 原版枚举器语义跳过旧代际对象，同时继续对不可读 pointer 和已接纳成员的 generation round-trip fail closed；直接受影响 native fixture GREEN。
- 新轮次 R639 使用 fresh Release DLL `1F7DE4...48E`，在同一 PID/connection generation、冻结日期 `53178264` 上完成角色 `29829 → 36108`。两个 campaign root 和两个 turn bundle 均 `available/ready=true`；两场景分别观测 8/9 个直属有地封臣、6/15 个相邻 holder、14/24 个 related contexts，并各有 6 个 occupied council task。
- 完整 artifact 444,994 bytes / SHA-256 `CFF681146A344AE18FDEB36C20BDAEAFC2A30344023CC7827E9A77006C3530DB`；源存档 `9104CCB8...12CC63` 未变，runner 正常 stop、进程树消失、隔离 state 删除。旧轮次 R638 的 RED 已关闭，当前没有 CK3。G2-M1 由 `in_progress` 升为 `complete`，固定总进度为 `1/8`。
- 本次 live 收口没有新增或改变 MCP schema、ABI、版本和依赖；既有 council schema 已由 open_kaishek commit `a6542064546441f4c175c54f3dbf947d7385b768` 兼容记录，仓库保持 clean/synced，因此无需新的 companion 修改。

## CK3 家徽选角页原生树诊断 MCP

- setup-only `live3` 因 runner 使用了错误 Steam 配置路径，在 CK3 启动前即停止；690-byte artifact SHA-256 为 `ACAE42AD...B8D967`，不计作能力 attempt。修正后的 `live4` 取得 406 次官方 MCP 调用，固定动作进入 `In Game` idler，但 route 在完整 120 秒期限内保持 `unavailable`；94,822-byte artifact SHA-256 为 `7F7180A9...C839C0`，Steam offline 与 process cleanup 均 GREEN。
- 该证据将缺口收敛为“源码推定的 `lobbyview` 未被当前 runtime tree 证实”。新增零输入 `ck3_inspect_frontend_gui_tree_v1()`：在 application-main mailbox 上广度枚举至多 512 个命名节点，返回深度/visibility/enabled，不接受控件名或地址且不执行输入。Release DLL、mailbox source-contract 与 Python contract/service/official-MCP `7/7` GREEN；inspector 为 `mcp-static-ready / live=false`，下一次受管 run 用它取得真实页面身份。
- `live5` 因管道名缺少固定前缀而在 CK3 启动前停止（1,667 bytes，SHA-256 `F6827D48...0B8EAC`）。`live6` 原生 tree census 已直接证实 `lobbyview` 在全局 GUI root 深度 2 且 visible/enabled；352,192-byte artifact SHA-256 为 `E0E8FF03...1B4675F`，512 行输出按合同截断但浅层证据完整，Steam offline 与 cleanup GREEN。fixed route 漏报的根因是旧 resolver 深度优先遍历在大型局内窗口深子树中先耗尽 4,096 节点预算。解析器已改为相同预算的广度优先，不扩大允许名称或动作面，待下一受管 run 验证后置条件。
## GEN-034 versioned exit utility baseline

- Replaced the critical path's indefinite wait for an external owner-approved
  utility file with a versioned, replaceable repository strategy model. The
  legacy owner-artifact provider remains available, but its approval state no
  longer blocks the active G2 policy.
- `raiktor_exit_utility_v1.json` binds model `1.0.0` to the exact budget profile
  `1.0.0` bytes and declares coefficients plus hard-budget, uncertainty and
  measured-power tail-risk rule IDs. The provider accepts the repository
  default or a complete base-bound operator override and does not present this
  policy as CK3 observation, native-AI semantics or human approval.
- Focused normal/optimized tests are `7/7 GREEN` each. The 3,310-byte receipt at
  `Z:\ck3_mod_rewrite\_runtime\g2-gen034-exit-utility-model-20260913\repository-default-model.json`
  has SHA-256 `4BBFDA9E...3951C`. Status is `strategy-model static-ready`;
  same-frame comparison, decision and action remain false until the narrow
  white-peace observation and evaluator are complete.
- No public MCP/native schema, version or dependency changed, so open_kaishek
  is unaffected. No CK3 instance or desktop resource was used; current round
  R639 remains terminated and the next launch remains R640.

## CK3 家徽选角大厅原生 MCP 实机闭环

- `live7` 在 commit `294b2afe3b2a67d693ef369c5f1aa56732610402` 上闭环：官方 MCP 固定动作从 `bookmarks` 到 `lobby`，独立 route postcondition 和 tree census 均通过，零 OCR/键盘/鼠标。artifact 353,637 bytes / SHA-256 `FBD86144...347FCE9`，DLL `ACD88796...6D89EC7`；Steam 全程 offline，`cleanup_proven=true`、`tree_gone=true`。该动作与 inspector 均升为 `production-live primitive`，下一包继续 ruler-designer 入口。
- 为探索 lobby 内没有显式 `name` 的原版 `TryStartRulerDesigning(..., 'default')` 按钮，tree inspector 现会自动聚焦已识别的固定 route root，并为至多 512 个子树节点返回相对 child path、允许为空的 runtime name、child count 与模块相对 vtable RVA。调用方仍不能提供 root/name/path/pointer，功能只读；Release DLL、native source-contract 与 Python/official-MCP `7/7` GREEN，待受管 live 结构采样。
- `live8` 完成该结构采样：artifact 491,326 bytes、SHA-256 `46D167D7...EA073B`，聚焦 `lobbyview` 并返回 512 个有界节点。原版源文件与 native tree 共同锁定 `3/0/2/3` 为默认 `TryStartRulerDesigning` 按钮；它在本样本中 visible 但 disabled，且 `tab_character_unselected` visible，证明当前缺口是先选择有效 playable，而不是继续猜按钮。Steam offline、cleanup/tree-gone 均 GREEN；源码 `9e068764...03136`、DLL `6D0917BE...66A0E`。

## GEN-034-C white-peace narrow projection static-ready

- Added a Raiktor-only Python provider that binds the existing safe termination
  options and narrow terms queries to one paused process/connection/episode/
  snapshot/revision/date/WarID. It adds no native reader or mutation and keeps
  the crash-prone broad loaded-effect preview disabled.
- Exact-build rules now project retained/strengthened claims, zero primary gold
  transfer, `cb_prestige_factor * -5`, shared prisoner releases and favor
  conditional, shared truce duration, no title transfer and no hostages. Seven
  still-dynamic side effects remain explicit uncertainty for the versioned
  utility model rather than being treated as zero.
- Focused tests pass normal and optimized modes, `6/6` each, including direct
  integration with the existing same-frame comparator. Status is
  `static-ready / live=false`; GEN-034 stays `2/4` until one bounded real frame
  supplies the projection and utility evaluation. Global G2 remains `1/8`.

## GEN-034-C same-unit immediate-exit evaluator static-ready

- Added a pure Python evaluator for white peace versus surrender. It consumes the strict same-frame projection/session binding plus the versioned budget and utility-model providers; the two exits now share one nine-feature `strategy_utility_q100000` scale.
- Seven white-peace and nine surrender dynamic effects remain explicit and receive the versioned bounded uncertainty penalty. Hard budget eligibility is evaluated separately from utility. Generic current regiment strength is not presented as a proven surrender loss.
- The fixture certificate scores white peace at `-29,025,000` and surrender at `-74,725,000`; the default white-peace favor budget makes white peace ineligible on that frame, so the pairwise result is surrender. This is test evidence only and does not supply continue utility, a full recommendation, an action literal or submission authority.
- Focused projection/evaluator tests pass normal and optimized `10/10`. A source-evidence assertion caught and closed an insertion error before commit. Status is `static-ready / live=false`; GEN-034 remains `2/4` and G2 remains `1/8` pending one bounded production frame.
- This package changes no public MCP/native schema, ABI, version or dependency. open_kaishek is unaffected. Current round R639 and old round R638 remain terminated; no CK3 instance or desktop input was used.

## GEN-034-C bounded live runner static-ready

- Added the reusable R640 runner around the managed cold-checkpoint owner. It issues exactly one termination-options read and one narrow terms read at the same paused revision, then evaluates the production-marked projection in process.
- Native command history must equal those two successful reads. The broad preview family must be absent; time advance, termination action, OCR and visual input remain disabled. Focused normal/optimized tests pass `4/4`.
- No CK3 instance was started for this static package. Current round R639 and old round R638 remain terminated. No public MCP/native/schema/version/dependency changed, so open_kaishek is unaffected.

## Formal close at 2026-09-14 00:00 (Asia/Shanghai)

| # | Morning commitment | Result | Evidence / reason | Carry forward |
|---:|---|---|---|---|
| D1 | Resume one bounded T0 promotional take after desktop release | Cancelled | The owner cancelled all promotional-video work at 17:21; existing `0/8` footage remains historical evidence only | No |
| D2 | Add the G2-M2 registry-event material postcondition comparison | Complete | Same-character stress, gold and non-decreasing-stress comparators plus focused normal/optimized tests were delivered and pushed | No |
| D3 | Advance the next evidence-backed G2 static input | Complete | Three event utility profiles, G2-M1 native observation chain, bounded two-scene live closure, and GEN-034-C projection/evaluator/runner static packages were delivered | GEN-034-C live gate carries to 2026-09-14 |
| D4 | Keep open_kaishek synchronized | Complete | Every public shape delta received an immediate companion audit/pin update; root and companion were both clean and synchronized at close | Continue as standing rule |
| D5 | Maintain daily/weekly evidence | Complete | Daily, W37, native-topic and machine-readable G2 ledgers were updated throughout the day | Continue as standing rule |

### Plan-external completed work

- Tianchao Phase 2 P1 remained `9/9 GREEN`; owner-reported scoreboard, compensation-docket and early-review presentation issues received focused fixes and live checks. The product is held for further owner manual testing and must not be published.
- G2-M1 closed one bounded two-scene production loop at current round R639 evidence, advancing fixed G2 progress from `0/8` to `1/8`.
- GEN-034-C gained a safe exact-build white-peace projection, versioned same-unit immediate-exit evaluator, and a bounded two-read managed live runner. The broad preview crash RED remains open and its dispatch remains disabled.
- Native frontend MCP work replaced the earlier synthetic input chain and produced bounded production-live route/tree primitives; this does not change the current GEN-034 priority.

At close, root `3327e91e747deda4c3f6b6c820bb7435e7bc3509` and open_kaishek `a6542064546441f4c175c54f3dbf947d7385b768` are clean and synchronized. Global G2 is `1/8`; GEN-034 is `2/4`. Current round R639 and old round R638 are terminated, and CK3=0.

### Midnight carry-forward

- The fixed playable-selection plus ruler-designer MCP package was compiling at the boundary. It had not yet been committed or live-proven at 00:00, so it carries into 2026-09-14 as `mcp-static-ready / live=false` work rather than being backdated into this report.
