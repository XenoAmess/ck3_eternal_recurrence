# 2026-09-09 日报

状态：**进行中**

正式收口：2026-09-10 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-09.md`](../meetings/daily/2026-09-09.md)

起始分支/HEAD：`master` / `497a8cb7c7d8f10481ecf468b8412963683e89dc`

## 今天完成了什么

- R344 将 `seduce_outcome.4900` 从一次观测的固定 `owner/target` ID 改为
  原版源码保证的动态第三方关系合同，同时保留八个 saved scope、唯一
  native option `0` 与玩家 `target_liege=32904` 的精确约束。
- 回归 normal / `-O` 全绿：manager recovery 各 `40/40`，promotion source
  checkpoint runner 各 `83/83`，另有 `py_compile` 与 `git diff --check` GREEN。
- 代码 `dc3bdea3d767e9635781697bb9d9d8819397ba1c`、证据
  `bc30de3` 均已 ordinary fast-forward push 到 `origin/master`；未 merge、
  未 force-push，也未暂存用户已有 runtime dirty 文件。
- R345 在同一重放中实证 `ep3_decisions_event.2001` 可由三名不同总督封臣
  分别触发；前两次 terminal refusal GREEN，第三次被旧次数上限选择前
  fail-closed。合同按实证从 `2` 最小放宽到 `3`，runner normal / `-O`
  各 `83/83` GREEN。
- 并发远端出现 XQOL 提交 `a8acdc1` 后，没有 merge 或 force：在独立干净
  worktree 把次数修复 rebase 到新远端头，`071870e` 普通快进推送；R345
  证据提交 `d615528` 随后也已推送。
- 只读 P1 gate 审计确认 `strict 4/361` 与 `definitions 106/626` 是覆盖债务
  指标，不是要求逐项刷到 361/361 或 626/626 的发布硬门；361 项业务内容
  已是 `361/361 static-ready`。真实 P1 余门是完整 B1–B8、stage 9–11、
  四类业务后置条件、save/restore、错误清理和代表性简中 UI 实机验收。
- R346 对原版 `trait_specific_ongoing.2001` 的唯一实机变体补入精确
  三选项合同，选择只降低压力且没有后续事件的 native option `2`；代码
  `020a5e1bd2c731d6b32b3aa3c1a074e0ffcb9cea` 与证据 `1101013`
  已普通快进推送。
- 交付了复用既有 live-GREEN `ck3_set_played_character_v1` MCP 的按 Windows
  用户隔离安装器/doctor 与跨机器交接文档；17/17 normal、17/17 `-O`、
  plan/help GREEN。提交 `bcddf6f` 已普通快进推送；本轮没有改 Codex 配置、
  没有替任一账户安装，也没有启动 CK3。
- R347b 对原版 `ep1_flavor.1000` 来访阉人事件新增精确三 scope、三选项
  合同并采用 terminal refusal；专测 1/1、总 runner 83/83 normal / `-O`
  GREEN。代码 `548b5cb` 与证据 `423c704` 已普通快进推送。
- R348 安全停在新的原版大学学者中断 `major_decisions.2011`。未知帧未被
  选择，cleanup 无失败项，冻结输入 SHA-256 仍为
  `C7E5CA119F4DB4D9BFE738859DD5E32DEC6F9F1F804D91F9B26ED5D256E219DA`。
  已绑定动态非玩家 `new_courtier` 与精确两选项投影，选择把来客移回 pool
  的 native option `1`；专测 2/2、总 runner 83/83 normal / `-O` GREEN，
  修复 `1942cd1` 已普通快进推送。
- R349 到达产品自己的 Career/Learning 汇总事件 `zg361cl.390`。该单按钮
  只清除已完成 portfolio 的 `zg361_cl_digest_pending`，不支付资源、不改
  deadline、stage 或业务状态；精确 60-scope 合同及证据分别由
  `ff8dd16`、`3d75654` 普通快进推送。它证明 stage 9 的前置业务可达，
  不冒充 stage 9 已完成。
- Workforce endgame 的 12 条简中 tooltip 已把内部术语“本卡”改为
  “这项裁定”，M248 英文按钮缩至 43 字符；生成器、九语言结构投影和
  126/126 normal / `-O` 回归均 GREEN，提交 `754f215` 已推送。随后的
  copy sidecar 已重绑 1010 definitions、626 visible、384 hidden、4994
  final loc keys、0 machine failures，提交 `ec79eeb` 已推送。
- R350 实证原版 `ep1_flavor.2040` 的 armor + poor-quality 合法分支；合同
  现在精确覆盖 weapon/armor × normal/poor 四种互斥组合，仍选择无玩法
  effect 的 authored option 3 / native 2。修复经 normal / `-O` 回归后随
  `22b8f50` 普通快进推送。
- R351 实证 `zg361.5` 的 PIP deadline subject 与 3.25 notice deadline
  subject 来自两条独立业务链。旧合同把 R293 的偶然同人误写成恒等式；
  修复仅删除该伪关系，55-scope exact frame 与其余同源约束不变，8 项
  normal / `-O` 复核 GREEN，提交 `4831bd1` 已推送。
- 对 R349 的 136 个天朝窗口完成只读频率审计：A 类必须玩家裁决 74、
  B 类可合并摘要 12、C 类应后台 50。R349 是冻结产品树上的 full-tree
  itemized 轨迹，不能冒充默认玩法；当前生成器已经后台化 C 类，同分支
  约 87 张。若后续获准把 B 类 12 张合为 4 张，可约降到 79 张，并把
  24 小时内连发峰值从 36 降到约 5；本轮没有修改玩法。
- T1 的 G2 source-specific adapter 已支持显式 `game-root` / EXE / bookmark
  路径并保持 exact-build SHA 门，41/41 normal / `-O`、14/14 no-launch
  preflight GREEN，提交 `93ee3bf` 已推送，readiness 仍诚实为 90%。T2
  `open_kaishek` 在 `d1c0362` 上 156 tests、CLI smoke、兼容 preflight 与
  domain validator 全绿，当前兼容地平线为 100%。
- 当前 HEAD 的基础静态门已再次通过：`validate_static.py`、86 文件
  `build_release.py --check`、无继承人 GUI 生成测试和 release build 单测
  均 GREEN；完整 ZhongGuo CI-equivalent 静态 slice 另行并发执行中。

## 现在在做什么

- R352 已在未选择 `death_management.1001` 前 fail-closed 并完成进程清理；
  正在按当前真实 frame 与 CK3 1.19.0.6 原版事件源码闭合该死亡通知的动态
  child/成人 option variant，随后用唯一 CK3 实例启动 R353 重放。
- 非 CK3 工作并行修复 formal phase-two live runner 已确认的 promotion
  restore PID 断言与 scoreboard/workforce/promotion session lineage 漏记，
  并准备在下一干净 HEAD 从头执行完整 CI-equivalent 静态矩阵。

## Readiness / 能力变化

- 尚无业务计数变化：canonical source `3/4`，T0 `50%`、strict `4/361`、definitions `106/626`、stage `8/11`，T0-P2 `LOCKED`。

## 失败、阻点与未知

- R344 已闭合，属于已知中断合同过拟合而非产品 RED；其精确失败仅为
  `scope:owner` / `scope:target` 固定 ID 漂移，`selection_attempted=false`。
- R345 是 `ep3_decisions_event.2001` 第三次 occurrence 的旧上限 RED，不是
  产品 RED；第三帧 `selection_attempted=false`，cleanup GREEN、输入不变。
- R346、R347b、R348 均为随机原版事件第一次进入这条固定长时间线时触发
  的 fail-closed 合同缺口，不是天朝业务断言 RED；三轮都在未知帧选择前
  停止、清理进程并证明输入不变。
- R350 为原版 artifact helper 的 armor 分支未纳入旧合同；R351 为两个
  独立产品 deadline token 被旧合同误绑为同人。两轮均 selection 前停止、
  cleanup GREEN、冻结输入未变，且合同修复已推送。
- R352 当前 RED 是 `death_management.1001` 新的 one-option native-0 形态与
  dynamic `dead_character` 尚未纳入旧合同；`selection_attempted=false`、
  cleanup 无失败项、输入 SHA-256 仍为
  `C7E5CA119F4DB4D9BFE738859DD5E32DEC6F9F1F804D91F9B26ED5D256E219DA`。

## 下一步

1. 收口 R352 并启动 R353；任何新中断继续在选择前 fail-closed，按真实帧与冻结
   原版源码补最小合同并立即回归、提交、推送。
2. 修复 formal live runner 的真实 PID/lineage 记账阻点；第四类 source
   capture GREEN 后，按真实 P1 门运行完整 B1–B8、stage 9–11、三周期终局
   与四类强业务后置条件；不机械补刷全部 361/626 覆盖债务。

## 范围边界

- 宗教域继续 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。

## R353–R368：cross-cycle/endgame 长时间线与同 PID 热恢复

- 第四类 source 的真实推进已经从旧的单事件重启模式切换为同一健康 CK3 PID
  上的 fail-closed 热恢复：未知事件或已知合同漂移一律在选择前暂停，外部查阅
  1.19.0.6 原版定义并补精确合同，通过 normal / `-O` 回归后再连接同一
  event instance。合同修复不再触发 CK3 重启；截至本段记录，R368 的 PID
  `201320`、connection generation `1`、restart `0` 持续健康。
- 已闭合的新增/动态原版路径包括宦官人贡、重复辩论、宦官秘密与密谋目标、
  court/epidemic/EP3/birth/GHW/chaplain 等通知，以及本轮的
  `tribute_mission.1005` 重复投递、`stress_threshold_special.1001` 两种
  no-confidant 投影、`secrets.0108/.0112`、带 epidemic carry 的
  `health.2201` 和 `diarchy.8042`。每个新帧都保留原始 RED，且
  `selection_attempted=false` 后才补合同；已热重跑的帧均由 event-instance
  advancement 证明实际放行。
- `stress_threshold_special.1001` 的 `(1,4,7)` 双 scope 投影选择 native `4`
  的 inappetetic；新的 `(0,1,7)` 双 scope 投影选择 native `1` 的 drunkard，
  因其直接 health malus `-0.15` 小于 depressed/frozen-grief 的 `-0.5`。
  两个形状分别把 options 与 scopes 绑定在同一 variant 中，拒绝笛卡尔交叉。
- `diarchy.8042` 是共同皇帝替罪互动的结果信：当前 root 是 recipient，所以
  两个 authored option 只渲染 native `0`；该按钮为空效果确认，实际互动后果
  已在来信前执行。合同精确绑定 9 个 scope，并保留可重复投递语义。
- R368 的真实产品树还暴露两项产品脚本 RED：policy audit 在 reserve 后读取
  尚未设置的 `zg361_case_kernel_applied`，以及 Career/Learning digest 本地化
  使用了不受支持的 bare `ROOT.Var` 格式。生成器已分别改为 lazy 分支与
  `.GetValue`，九语言结构投影、manager governance `52/52`、career
  `38/38`、copy sidecar `6/6` 双模式及本地静态校验均 GREEN；因为 R368
  加载的是修复前产品树，这两项仍必须由 fresh R369 实机确认，尚不记 live。
- promotion progress 原生观测从 5 项扩展到 29 项，并补齐中央 stage 9、
  Workforce route/rehire fact 与 P3 tooltip 的只读守卫；native bridge
  `102/102` GREEN。当前批次的 secret `6/6`、health `12/12`、manager
  `54/54`、checkpoint `88/88` 均在 normal / `-O` 下 GREEN。
- 进度口径保持诚实不变：canonical source `3/4`、T0 `50%`、strict
  `4/361`、definitions `106/626`、stage `8/11`，T0-P2 `LOCKED`；当前
  cross-cycle/endgame 仍在实机推进，最终 source checkpoint 尚未交付。

## 当前下一步（15:21 后）

1. 继续 R368 同 PID 热恢复，直到自然 source 边界或新的真实 fail-closed 帧；
   对合同类修复不重启 CK3。
2. 受控回收 R368 后，用当前产品生成器构建唯一 fresh R369，实机复核
   `zg361cl.390` 与 policy audit 两类旧错误归零，再继续第四类 source capture。
3. 完成本批静态矩阵后按项目约定 rebase-only 提交并推送；禁止 merge。

## R368 收口、fresh R369 与 T1/T2 并行同步（16:30）

- R368 在同一 PID 上继续越过 accolade 与重复朝贡中断后，达到既定
  `10190` 天观察上限但没有抵达第四类 source；该轮按真实结果保留为
  bound-exhausted RED，不冒充 source capture。owner process 随后受控终止，
  Job object 回收 CK3 PID `201320`，串行槽清零后才启动 fresh R369。
- R369 从包含 policy-audit lazy guard 与 Career/Learning `.GetValue` 修复的
  当前 product tree 启动，CK3 PID `144008`、connection generation `1`、
  restart `0` 已通过 loader/map/mailbox 并进入长时间线。当前 `error.log`
  未再出现 `zg361_case_kernel_applied` 或 bare `ROOT.Var` 旧错误签名；最终
  source capture 尚未完成，因此 readiness 不提前提升。
- 同 PID 热恢复已闭合 `ep3_story_cycle_admin_eunuch.8010`、疫情麻疹
  `health.1015` 与其失败结果 `health.3104`。其中 `.3104` 将一按钮投影与
  epidemic/new-memory 的精确十 scope 绑在同一个 variant，继续选择无效果的
  native option `0`，且按原版多疾病复用途径修正为 observation-window 可重复。
  health focused `14/14`、checkpoint runner `88/88` 均在 normal / `-O`
  下 GREEN；提交 `e12d6f5`、`8be15a6`、`6dba45b` 已推送。当前停在未选择的
  `health.1112`，等待只读原版定义审计，CK3 不重启。
- T1 完成 9 份 G2 provenance DAG 刷新：focused `80/80`、G2 internal
  `111/111`、Raiktor `174/174` 均 normal / `-O` GREEN；no-launch preflight
  14 项依赖一致，artifact SHA-256 为
  `707C59A0214B58FBA80CFB75A9810A52F3C375593EFE7775F7E3A0DCAA67BDE8`。
  提交 `e7191c4` 已推送；下一实机包仍受 R369 的 CK3 串行槽约束，T1
  readiness 保持 `90%`。
- T2 发现并修复此前被 verifier 漏掉的真实漂移：父仓 promotion source 已从
  5 个固定 widget 扩展到 29 个，`open_kaishek` 旧 profile 未同步。伴随仓
  commit `8b0d1598d7d702a3b093675343d5fd20ed2883a1` 已普通 push；父仓同步
  29 项列表、三个内容哈希并增加无 checkout 也检查真实父仓 artifact 的
  drift-negative test。7/7 normal / `-O`、伴随仓全量 156/156、CLI smoke
  与 require-clean 双仓 audit 全部 GREEN；audit SHA-256 为
  `01C3D034381114C5F21C911C7CB252CFFC71DD4726F4B1A4200A91CDDB67FFC9`，
  父仓提交 `1e5ffd2` 已推送，T2 当前兼容地平线恢复为 `100%`。
- 本批父仓提交均先检查远端、保持线性 rebase-only 后普通 fast-forward push；
  `origin/master` 已核对到 `1e5ffd21fbecaac0f0e476ba2c9fda1995a228c3`，
  没有 merge、force-push 或 stash。T0 仍为 `50%`、source `3/4`、strict
  `4/361`、definitions `106/626`、stage `8/11`；T0-P2 继续 `LOCKED`。
