# 2026-09-15 daily report

Status: **in progress**

Morning meeting: [`../meetings/daily/2026-09-15.md`](../meetings/daily/2026-09-15.md)

Starting branch/HEAD: `master` / `096fa807003c8c1759bdade86fe94984012be4dc`

## Work in progress

- Completed the static vertical slice for a scoped CoA-page tree inspector and fixed custom-mode/background MCP action. The native Release DLL is 2,816,000 bytes, SHA-256 `D788710ACB7397DD70273E1136D50EE2A1CBA03F6BDB1E6711393C49D00E7CE4`; protocol/adapter/mailbox CTest passes `3/3`, Python contract/service/native-driver/official-MCP plus runner tests pass `20/20`, Vue passes `42/42` with a GREEN production build, and Quarkus REST passes `17/17`.
- The two zero-input endpoints are exposed through the 16-tool Java allowlist. The inspector can only root at live `coat_of_arms_page`; the action can only select one of the two source-owned `button_custom_mode` leaves and requires the background pattern panel/scrollbox as its postcondition. This is `mcp-static-ready`, not live runtime-registry evidence.
- Added and pushed an opt-in official-MCP custom-mode census runner at `d9de68b1`; focused contract tests pass 7/7.
- Ran one isolated live census with Steam offline and no OCR/keyboard/mouse. `mcp-frontend-coa-custom-mode-census-live8.json` is GREEN (2,331,467 bytes, SHA-256 `72542CE89CE33A76A05D35722CCF301994533D409E4AD59D4F497D58760735FD`), all ten custom-mode checks passed, and managed cleanup proved the CK3 process tree gone. The page-level tree reached a materialized fixed grid reporting 38 children, but the 512-widget cap truncated before enumerating those items; a narrower fixed-root MCP inspector remains necessary.
- Implemented that narrower zero-input pattern-grid inspector across the exact-build native bridge, Python native driver/service and official MCP server, Quarkus REST, and the TypeScript companion client. It resolves only the compile-time `ruler_designer` path, validates the four named ancestors plus the unnamed visible/enabled grid root, and requires every direct child path reported by the root to be present. A fresh MSVC build produced a 2,818,560-byte DLL with SHA-256 `2CEA58975B3ABF3B75453F4A85705B0BFC2DB999AD219E96F65E77B7BE47BB06`; all `127/127` CTest passed after synchronizing two stale source-contract expectations with the current 95-capability adapter and live-executor ABI. Python official-MCP tests, Quarkus `18/18`, Vue `43/43`, and production build are GREEN. Status is `mcp-static-ready / live=false`; no new CK3 run is claimed yet.
- The first live9 attempt stopped before launching CK3 because the cleanup watchdog could not call pywin32's current WMI method spelling. Its 2,040-byte RED artifact has SHA-256 `52C624709ABC2D7AC2ADF3B1BAA394CF746851C0CC48B8D84DE25FBBF8829C72`; Steam offline and both shared locks were proven, but no managed PID or MCP request exists. The Windows process helper now supports both legacy `ExecMethod_` and current `ExecMethod` dispatch names; focused unit tests pass `3/3` and an actual WMI process-create probe returned a PID. The RED artifact remains immutable and will not be overwritten.
- Added a project-wide legacy shell prohibition to `AGENTS.md`: no execution or new scripts for that shell; discovered historical paths must be migrated to Python without changing other languages.
- Completed the repository-wide Python-only migration: historical Windows-shell command blocks and structured launch fixtures now use direct Python argv/environment/cwd handling; repeated phase-two promo runbooks are backed by Python entrypoints instead of session variables. Added `tools/validate_python_only.py`, focused tests, and a Windows CI `cmd` default so the runner cannot silently select the prohibited shell. The validator is GREEN; no CK3 or desktop action was used.
- Steam remains offline. No CK3 or foreground input has been used for the new package yet.

## Range boundary

- This lane is the CK3 coat-of-arms editor and capability report. No G2 work, online Steam, Workshop write, OCR/mouse/keyboard fallback, arbitrary GUI pointer/path API or unproven runtime-registry claim is authorized.

## G2 carry-forward from the 2026-09-14 close

- Current round R688 completed a real paused private military observation GREEN
  on exact CK3 `1.19.0.6`: status `available`, failure flags zero, observation
  ready, one published execution, and zero UI/gameplay/date/save changes. Its
  sealed artifact-manifest SHA-256 is
  `09151A61FE8C5506579742EB34DFE48AC5692183F7F07D0D8F8EA1A9F92C7A85`.
  Cleanup terminated the process tree and released the CK3 slot.
- New round R689 retained a council harness RED before readiness collection:
  the Python runner passed unsupported `expected_character_id` to
  `_wait_for_readiness()`. No raw probe or council capability result was
  produced. The round terminated without input, gameplay, date advance or save
  mutation; final CK3/helper inventory was zero. Its sealed artifact-manifest
  SHA-256 is
  `C35876FDD3A817E872DF7987622C2B62FC56BEADF4E011A8A24CF98EE161939B`.
- The carried integration boundary is
  `master@e86d3fbb34d4e9f978cb3b9a95cfedab2131beec`. The added exact-build native
  trees and private observer/glue packages remain research/static primitives.
  Global G2 is still `1/8`; public military consumption and a corrected council
  harness/live observation remain open. Public MCP and cross-repository
  contracts did not change, so `open_kaishek` requires no adaptation.

## COUNCIL12 integration and R690 RED

- COUNCIL12's BOM-safe JSON input repair and sealed-candidate receipt reached
  remote `master` with rebase mappings `16697858 -> 0f9889288430b005eb5977e4072d7c9d04b3f15f`
  and `3e43a410 -> a905af184798439a41cdc6f63bfeb47d67f62608`.
  REPORT12 was then integrated as
  `295a684ba1b1b21fc62516615265e88ab4967502`.
- R690 launched the fixed, hash-bound council candidate for one
  paused read-only steward-candidate heartbeat. The runner stopped on
  `AttributeError: 'NativeHeadlessGameplayDriver' object has no attribute
  'take_internal_semantic_snapshot'` before it could persist a raw probe or a
  council private-heartbeat publication. The council capability therefore
  remains `not_observed`; this is a **harness RED**, not a capability RED and
  not evidence that the native council observer failed.
- R690 performed zero UI/gameplay input, date advance or save mutation. Managed
  cleanup removed CK3 PID `199000` and watchdog PID `208880`, reported
  `tree_gone=true / cleanup_proven=true`, and left final CK3/helper inventory
  empty. The source and target saves remained unchanged. The preserved report
  at
  `Z:\ck3_mod_rewrite_process_assets\g2-m4-council12-r690-bom-safe-1669785\live-r690\report.json`
  has SHA-256
  `E2D562A9BC88BFEA1752AAE55473B1148824B019917138FB3BF1B34D55C82F50`.
- The DEV14 construction one-day candidate was integrated as
  `15582df862bfe8179bed03db8e9b751059bc597f` and remains a no-launch static
  candidate. A subsequent compatibility check found that its runner also
  requires the unavailable `take_internal_semantic_snapshot` method, so it was
  not started and did not consume a round. There is no current CK3 instance;
  the next actual launch identifier remains R691.
- The Council RED stays open until a compatible runner/candidate obtains a raw
  private publication. Global G2 remains honestly `1/8`; no public MCP,
  cross-repository contract or `open_kaishek` surface changed.

## G2 integration, CI recovery and branch cleanup (2026-09-15)

- Integrated the READY Activity2-5, DEV15-21, DIPLO2-6, GOV2-4,
  COUNCIL16, DECISION1-8, SCHEME2-10, MARRIAGE6-8, LAW8 and CULTURE2-4
  stacks through rebase-only, fast-forward master updates. The final code head
  before this report closeout is `b9f210f8830d25813baf1366dcab6eece40d1bd7`.
- CULTURE2-4 maps to `ea9038e7`, `95842ca0` and `26fe0f5d`; receipt
  `b9f210f8` records the rebuilt candidate hashes. Slot 40 is bound to the
  private culture/innovation query while slots 36-38 retain decision, law and
  marriage and slot 39 remains reserved. The source and evidence boundary is
  recorded in [culture-innovation-ai.md](../../ck3-native-ai/culture-innovation-ai.md).
- Focused validation is GREEN: Python-only and static validators, Debug and
  Release Culture-enabled DLLs, Release default-disabled DLL, the culture
  mailbox test, and the shared main-thread mailbox test. GitHub Actions run
  `34922343898` completed successfully on the exact code head `b9f210f8`.
- Closed the CI RED chain caused by obsolete Windows automation and stale
  static source contracts. No failure was suppressed or reclassified.
- Cleaned 144 exact temporary clone, build and linked-worktree paths after
  proving their patches were present in master and that they contained no
  unique live/candidate evidence. Remote heads fell from six to two and local
  heads from three to one. The remaining remote heads are `master` and the
  unrelated long-lived `codex/mod-shiren-import`, which has 15 unique patches
  and was therefore neither deleted nor integrated into G2.
- The two preserved matching asset directories are the formal Council and
  Faction R692 candidates. No CK3 process is current and this closeout allocated
  no new round. The Council candidate remains unexecuted; global G2 therefore
  remains honestly `1/8`, with most recent packages still static-ready or
  private-candidate primitives rather than complete production-live loops.
- Public MCP schema, protocol and dependency versions did not change, so
  `open_kaishek` needs no compatibility update for this integration.

## R692 sealed Council candidate RED (2026-09-15)

- Current round R692 launched the sealed
  `g2-m4-council16-r692-9cdb430` candidate exactly once on exact CK3
  `1.19.0.6`. The private probe prepared and published a result, but returned
  `status=unavailable / unavailable_reason=application_main_thread_required`.
  Its decisive counters are `last_submit_result=0`, `last_wait_result=4`, and
  `executor_started_requests=0`; the expected steward-candidate vector was not
  collected. This is a retained capability RED.
- The run stayed paused and read-only: zero UI/gameplay actions, zero date
  advance, and no save mutation. Source and target save SHA-256 both remained
  `9104CCB8AE9D5776166FBBAEDA9B43BD08CBAA2CB5C057332EB8B7A1A212CC63`.
  Cleanup proved the CK3 process tree and watchdog gone and final inventory
  zero. Current round R692 is terminated.
- The sealed post-run evidence manifest is
  `Z:\ck3_mod_rewrite_process_assets\g2-m4-council16-r692-9cdb430\live-r692\postrun-evidence-manifest.json`,
  SHA-256
  `589FFE1526F3C98DFC5C04E5F0C8A48EC41D6DB893CC5497AE459781D4F2FFCB`.
  The one-time `seal_r692_red.py` has `reason=现场封存`, impacts only the
  evidence manifest, and has no product reuse plan. Its migration deadline is
  this work-package close, when the immutable manifest replaces the script as
  the durable record.
- No formal planner/strategy path consumed the result and no council action or
  postcondition was attempted. G2-M4 remains `in_progress`; global G2 remains
  `1/8` (`12.5%`). Public MCP/schema/dependency surfaces did not change, so
  `open_kaishek` remains unaffected.

## R693 private Council reader GREEN (2026-09-15)

- New round R693 executed sealed candidate
  `g2-m4-council18-r693-5e0c5d5` exactly once against CK3 `1.19.0.6`, executable
  SHA-256
  `2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86`.
  The candidate source was
  `41bd2d5d0c159676876f21adc3845cf0afeeaf97`. The application-main mailbox
  started and executed one request, and the private steward reader returned
  `status=available` with a complete 11-character candidate vector for owner
  `29829`. It released the temporary native vector and persisted no raw pointer
  fields. This closes the R692 private-reader capability RED.
- The run was a paused read-only observation: zero UI inputs, zero gameplay
  actions, no date advance and no save mutation. Source and target save SHA-256
  remained
  `9104CCB8AE9D5776166FBBAEDA9B43BD08CBAA2CB5C057332EB8B7A1A212CC63`.
  Managed cleanup proved the CK3 process tree and watchdog gone, with final
  process inventory empty. Current round R693 is terminated; old round R692
  remains terminated.
- Raw probe, report and round-ownership SHA-256 are respectively
  `C185D49F3DB17FF4CB3CE2129FF3DA611E6DC65F0E1DA33D8DA004B8A87FE40D`,
  `F5A9518885398B9503B587164F174E6E9F18345390BF022381A1DEE85B28D297`
  and `22CD29041B3C17B22A6B5D6C56E119C816CBEA0FD5AEABEEBEA1A873D852CC49`.
  The immutable post-run manifest is
  `Z:\ck3_mod_rewrite_process_assets\g2-m4-council18-r693-5e0c5d5\live-r693\postrun-evidence-manifest.json`,
  SHA-256
  `FC59D8D8F001B6B6F6A5ACD041EFBA78596F7D837A54819A8B3F3A2E3EAFABC4`.
- `seal_r693_green.py` is a one-time field-evidence helper. Its only effect is
  the post-run manifest and checksum, it has no product reuse plan, and its
  migration deadline is this work-package close, where the manifest replaces
  the helper as the durable record.
- Formal strategy consumed no Council observation and submitted no Council
  action in R693. G2-M4 only gains a `production-live primitive`: public/formal
  candidate semantics, legal assignment, independent postcondition and
  next-cycle consumption remain open, alongside construction and the real
  vassal/faction response. The fixed G2 total therefore remains `1/8`. This
  evidence package changes no public MCP/schema/dependency surface, so it does
  not itself require an `open_kaishek` adaptation.
