# 2026-09-11 日报

## 01:11 T0：R402 AF5 实机完成，剩余 P1 继续

- D2 的 AF5 修复已在实际游戏闭环：55 游戏日抵达 AF5，42/41 后 native 同 case state5→6、revision19→22、m299/m300 route3 consumed=true，终态原生存档已归档。
- 新 query 由 static-ready 升为 production-live primitive；AF5 操作单元已完成有界观察→决策→操作→验证。完整日志 byte0 扫描 blocking=0，canonical 与包装 cleanup GREEN，PID166928 已退出。
- 权威路径、版本与 hash 见[AF5 实机结果](../../phase2-promo/r400-af5-stage-order-and-terminal-observation-2026-09-11.md)。代码已普通推送 `262026d325e3d137e1cf03c46d723aad8bd57f3a`，旧 R400 RED 保留。
- 当前计数勘误：交接的 L0 `1/9` 沿用旧候选。已找到的最新完整差异基线为 `p1-current-head-l0-718a60d-evidence.json`（GREEN，产品8A260E2E），尚不能作为当前88076DBF的机器门对象；旧 assembler 还引用5fad候选。本轮已有可直接满足门字段的 AF5 receipt，完整 P1 仍未通过。正复用原21命令/103 native基线，仅补 Stage11 guard、B1 equality 及本轮实际变更的验证和候选绑定，不重新跑无变化矩阵。
- D3 准备并行完成：旧档迁移 job（normal / `-O` 各6项GREEN）只加载 `enabled_mods=[]` 并通过原生 save 重存 exact build；迁移收据明确不等于产品 seed。Stage9/10 续跑单元、Stage11 owner视角观测正在集成，避免为了读取下属终态而切换玩家。新 owner query 的定向 Python/MCP、native 三项与 serializer 对照均为静态证据，尚无新 live。
- Stage 9–11 动作单元现已集成 owner query：normal / `-O` 各12项GREEN，覆盖真实 paused 边界推进两天等待 Central 回调、N/A 无 `.360` 事件直接终止，以及 AF5 既有时序不回归；静态报告为 `Z:/ck3_mod_rewrite/_runtime/p1-terminal-stages-static-20260911/report.json`。Workforce owner query 的 C++/Python/MCP 与 serializer 对照通过，schema SHA `647C5252DFDC64D58F9FCF448DE8816B6B66EF6591485D7B7702ABD51A00A10C`，仍为 live pending。
- `open_kaishek` 已同步 owner query 的54字段只读兼容 profile，提交 `d4f7754f5979bb8fda8a330cfc40b340c7e84810` 并普通推送；Maven 离线依赖闭包和四帧 schema 对照 GREEN，profile 保持 native/runtime uncertified。
- 下一步：R403/R404 独立旧档迁移，再以新产品会话验收 B1 与 Stage9–11；最后做代表性四域冷恢复与最终装配。P2 继续 LOCKED，未进入任何媒体工作。

## 00:52 T0 执行增量（进行中）

- 用户已授权执行评估中的方案；D1 源码归因完成：compensation 是 Central stage 2，`.147` 属于后面的 stage 3，其后继为 `.148`。原交接路线错误，不以延长 timeout 或修改产品业务绕过。
- D2 正在实现独立 AF5 native/Python 查询、真实时间线 option 42 停靠与可复用 operator 包。前后态同 case，业务结果与存档/cleanup 分离，异常原始 evidence 留存。
- 输入改用历史 R185 autosave，历史 R192 记录给出约 55 游戏日的 AF5 后缀。现时结论来自源码/旧 artifact；新查询的测试只记 static-ready，尚未启动 R401/R402，不增加 live 或 P1 分子。
- 原因、权威源码、输入 hash 和身份口径集中记录在[AF5 路线勘误](../../phase2-promo/r400-af5-stage-order-and-terminal-observation-2026-09-11.md)。后续依次完成定向离线验证、冻结新执行包与短实机签收；P2 保持 LOCKED。

### 本轮离线验证与并行交接

- 生产时间线 2 项定向测试在 normal / `-O` 通过：真实 enter/drain/resume 逻辑驱动 AF4 → paused/null → D+1 AF5；三种 AF5 资源投影可零动作停泊。动作单元另 3 项 normal / `-O` 通过，覆盖真实驱动后选 42、ACK 无终态保持 RED、不同 case 的终态不能签收。
- Python query/transport/service/MCP 新增 6 项 normal / `-O` 通过，既有 MCP 工具清单/调用 1 项通过；C++ AF5 snapshot、adapter registry、main-thread mailbox 三项定向检查通过。均为合成或离线证据，不是 live。
- `open_kaishek` 预验：基线 `f7930d46b0cbf05fb6227d8b48c35f6fd9178784`，已提交并普通 push `e2aac63` 至 main；profile `ck3-1.19.0.6-zg361-compensation-af5-snapshot-v1`。命令 `mvn -o -pl kaishek-zg361-profile -am '-Dtest=CapabilityDescriptorTest' '-Dsurefire.failIfNoSpecifiedTests=false' test`，2/2 GREEN。最终 Java descriptor 编译、实例化及跨仓字段对照通过，53 个必需字段与 schema 一致。
- 对应 exact build 为 CK3 1.19.0.6 / EXE `2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86`；schema SHA `B8839712AE999E49C7B3EE5E20DBDA14C9DBD0BB8C0511CEA6F83FE903933EBB`；合成 corpus `zhongguo_compensation_af5_snapshot_v1` SHA `DA401EBD1691DD31594BD51189B2E9A885E74FDCF7C6C76C38B58EDCD33626C5`。该预验只覆盖公开合同兼容；Python 状态机、native ABI 读取、CK3 日期票据调度与真实终态在当前 open_kaishek 中 `not-applicable`，分别由定向测试与待执行的 live 验收覆盖。
- 并行调整：完成产品路线归因、时间线测试、Python 查询与 T2 同步包；native 构建与 operator 冻结准备服务于当前 AF5 关口。新增 stage 9/10/11 已绑定 service 的续跑单元准备，复用既有合同与 provider，不占 CK3 槽、不要求 AF5 与其处于同一切片。
- 可复用 operator 包 13 项 normal / `-O` GREEN，包括保留异常 evidence、业务/cleanup 分离、终态保存，以及选项 42 前从新冻结 Python checkout 复用原 PID 的热重试。原失败副本保留，热重试不重跑 loader 或启动游戏；选项 42 已提交后不会自动重复输入。
- 新 bridge 与三项 native 检查的冻结报告：`Z:/ck3_mod_rewrite/_runtime/r401-af5-bld1/af5-native-build-verification.json`。接口与 ABI 专题：[AF5 snapshot v1](../../ck3-native-ai/zhongguo-compensation-af5-snapshot-v1.md)。本工作包以 `Add standalone AF5 terminal observation and operator flow` 提交，精确 SHA 由该专题 Git 历史追溯；live receipt 将绑定新 commit 与 DLL hash。

状态：**进行中**

正式收口：2026-09-12 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-11.md`](../meetings/daily/2026-09-11.md)

起始分支/HEAD：`master` / `a0997e65e6b79d0d772383202317f2bf519a7de8`

## 今天完成了什么

- 按用户直接指令完成 `coat_of_arms_editer_of_ck3` 首个可运行前端基线；未启动 CK3，也未占用 T0 实机槽。
- Vue 3 + Element Plus + TypeScript 应用支持 CK3 纹章子集解析、结构化图层/实例编辑、CRLF 导出和近似构图预览。
- parser 会展开简单静态 `@变量`，并对多顶层、body-only、模板 DSL、未知字段和重复标量给出阻断诊断。
- 按 MCP-first 要求补完 `ck3_export_coat_of_arms_source_v1`：原生桥接在 designer 自然 UI-thread callback 内调用
  CK3 自己的 Copy/export RVA，读取其剪贴板输出，再经 closed schema、SHA-256 与 exact session binding 返回；hybrid 明确禁止视觉回退。
- 新增不启动游戏的 `ck3_query_coat_of_arms_resource_catalog_v1`：校验 exact EXE 后读取原版 designer manifest，
  按原版顺序分页返回 pattern、colored emblem 与背景色元数据、相对路径及 DDS 指纹；输出明确否认运行时注册和 DLC/mod override。

## 早会承诺对照

当前尚未到日终；D1–D6 的 T0 对照由对应 owner 在收口时填写，本补录不改变它们的状态。

00:23 起的 T0 评审工作包另在本文末尾追加滚动对照，未改写本节纹章编辑器工作包的记录。

### 计划外完成

- 用户指定的纹章编辑器基线：5 项 Vitest GREEN，`vue-tsc -b && vite build` GREEN；提交 `c537e172` 已推送。
- CoA Copy/export 静态能力：官方 MCP SDK 下 probe+export 聚焦回归 21/21 GREEN；普通 Python 环境同组 21 项 GREEN、
  2 项 SDK 集成按设计跳过；native protocol/adapter registry 2/2 GREEN；rebase 后 exact-master DLL 为 2,551,808 bytes，
  SHA-256 `F28BFFA0F82A6F9C51DC0E8491DE93578B443A00957F73E6B11BCB660C395FF8`。
- CoA resource catalog：官方 MCP SDK 5/5 GREEN；普通 Python 5 项 GREEN、1 项 SDK 测试按设计跳过；本机 exact
  1.19.0.6 基础游戏实读为 42/38 可见 pattern、1,578/1,576 可见 colored emblem、13 个背景色。

## 现在在做什么

- 纹章工作包继续静默推进；当前收口基础游戏资源索引 MCP，后续主线是编辑器 MCP 接入、DLC/mod 合并与 live round-trip。

## 为什么做这些

- 把已经由原版 MCP 实机验证的能力边界落实成可用编辑器，并避免第三方 parser 或 OCR 成为引擎真相来源。

## 证据与验证

- `pnpm test`：1 file / 5 tests GREEN。
- `pnpm build`：Vue typecheck 与 Vite production build GREEN；仅有 Element Plus 单包大于 500 KiB 的性能提示。
- CK3：本日该工作包未启动；09-10 attempt 8 已在前一日日报补录并证明 cleanup。
- 本轮遵守用户“不占用 CK3 或主屏幕”的要求，`ck3.exe` 进程数复核为 0；Copy/export 状态只记
  `mcp-static-ready`，未冒充 live。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| CoA Web 编辑器 | 未创建 | `static-ready` 基线 | parser/editor/serializer 可运行；真实资源预览与 MCP 会话尚未接入 |
| CoA native Copy/export | 未暴露 | `mcp-static-ready` | 原生构建、MCP schema/call 与 binding 回归 GREEN；真实 CK3 执行待用户重新开放桌面 |
| CoA base-game resource catalog | 未暴露 | `static-ready` | exact-build MCP 分页索引和本机实读 GREEN；明确不含 DLC/mod override 或运行时注册证明 |
| T0 P1/P2 | `1/9` / `LOCKED` | 不变 | 本工作包不改变天朝二期业务门 |

## 失败、阻点与未知

- 已有基础游戏 designer manifest 清单，但尚无 DLC/mod 合并及运行时注册表；Copy/export MCP primitive 已静态就绪但未 live，
  浏览器预览明确不是最终渲染。
- 浏览器不能直接连接本机 stdio/named-pipe MCP；真正接入时才评估是否需要 Quarkus 伴随服务。

## 文件与提交

- 文件：`coat_of_arms_editer_of_ck3/`、`docs/ck3-coat-of-arms-clipboard-import-capability.md`、
  `ck3_autonomous_player/src/xar_autoplayer/bridge/coat_of_arms_source_export_contract.py` 及对应 native/MCP/test 接线。
- 新增资源索引文件：`ck3_autonomous_player/src/xar_autoplayer/coat_of_arms_resources.py` 与对应 MCP/test 接线。
- commit/push：`c537e172` / `origin/master`。

## 下一步

1. 不占用 CK3/桌面期间把基础游戏资源 catalog 接入编辑器并设计 DLC/mod 合并边界；重新获准后再做 Copy/export live round-trip。

## 范围边界

- 宗教域默认 owner-deferred；本工作包不涉及宗教机制。

## 00:23 起：T0 方法论评审与交接勘误

本次记录时间：2026-09-11 00:23 起（Asia/Shanghai）；尚未正式收口。
本工作包起始分支/HEAD：`root-rebase-r345-20260909` / `afb45864cd3fc12f5e215475ad6ae91d9bbce875`。

### 完成项与原因

- 用户先要求只读评估前任交接，再要求把评估与改进建议写入 `docs`。
- 形成 [T0 方法论评审](../retrospectives/2026-09-11-t0-methodology-review.md)：覆盖故障归因、测试连接处、
  冻结版本粒度、独立 checkpoint 组合、关键路径、进度分层和排期/报告，并明确建议尚未实施。
- 原始 driver state 证明 R400 已读到 `.147`、执行源选项 1 并观察到事件关闭；失败位于之后等待 `.comp.1`。
  已在原交接追加带时间的勘误，保留原文和失败 artifact，让后续施工从真实失败阶段继续。
- 这是用户新增要求下的计划外文档交付，不把建议当成已完成修复。

### T0 早会承诺滚动对照

| # | 早会交付项 | 当前结果 | 证据 / 原因 | 后续 |
|---:|---|---|---|---|
| D1 | 只读归因 R400 source-event RED | 部分完成 | 已更正失败阶段；尚未闭合源选择后的业务条件与调度根因 | 从结果事件未到达处继续归因 |
| D2 | AF5 最小修复与专用复验 | 本工作包未执行 | 用户当前范围为评估与文档落盘 | 留待恢复施工 |
| D3 | 稳定谱系 stage 9/10/11 | 本工作包未执行 | 当前未启动 CK3 | 留待恢复施工 |
| D4 | cold restore 与 P1 assembly | 本工作包未执行 | 业务门尚未闭合 | 留待 D2/D3 后 |
| D5 | P1 后进入最终视频 | 本工作包未执行 | P1 未 GREEN，P2 继续 LOCKED | 按既定依赖推进 |
| D6 | T1/T2 伴随推进 | 本工作包无同步增量 | 只修改 docs，没有接口、ABI 或数据格式变化；其他工作包见本日日报各节 | 不制造兼容代码提交 |

这是当前时点的滚动对照，不是当日正式收口；没有倒改 00:02 的早会计划。

### 证据、验证与 readiness

- 复用 R400 driver state、AF5 RED、现有 loader/cleanup 与代码；精确路径、SHA 和可证明范围见评审正文。
- 本工作包执行文档 diff、空白及本地链接检查，不运行产品测试或 CK3，不产生新 live artifact。
- 文档所列复盘统计来自前任报告；本轮没有重新跑历史实机或大套件。

| 能力或 gate | 之前 | 现在 | 限制 |
|---|---|---|---|
| P1 正式签收 | `1/9` | `1/9` | 计数净增 0；不解释为整体工作量百分比 |
| AF5 | RED/PENDING | RED/PENDING | 纠正失败阶段，没有取得 terminal |
| P2 | LOCKED | LOCKED | 未进入宣传工具或媒体流程 |
| 方法论建议 | 会话评估 | 已落盘 | 实施与效果验收均待后续 |

### 未知、提交与下一步

- R400 的已确认失败边界为源选项关闭之后，结果事件未到达；是否缺时间推进、业务条件不满足或其他调度问题尚未定案。
- 评审、交接勘误与日/周报告更新属于同一文档工作包，提交信息为
  `Document T0 methodology review and AF5 handoff correction`；精确 SHA 从评审正文首次引入的 Git commit 追溯。
- 普通 push 的完成状态以远端 `master` 是否包含该提交为准；交付回复记录实际提交与推送结果。
- rebase 时保留主线已有纹章编辑器日报，追加本节解决 add/add 冲突，没有覆盖其他工作包成果。
- 恢复施工时先闭合 AF5 源选项之后的产品状态与调度归因，再决定最小修复；本次仅落盘建议。

## 01:48：CoA 编辑器 Quarkus/MCP 静默贯通

- `8da1a4c24781658fd16d0124e8f9d4d695631388` 已 rebase 后推送到 `origin/master`：新增 Java 17 + Maven +
  Quarkus 3.33.3.2 伴随服务，使用官方 MCP Java SDK 2.0.1 的 stdio client，仅允许 session snapshot、CoA probe、
  Copy/export 和资源 catalog 四项工具。浏览器不再需要直接启动 stdio 子进程，也没有引入 OCR/桌面自动化替代路径。
- Vue 编辑器已接入 MCP 状态、当前 revision、原生 detect/apply、从 CK3 Copy/export 载入，以及 exact 1.19.0.6
  基础游戏 pattern/emblem 选择器；catalog 仍明确标记不含 DLC/mod override、未观察运行时注册。
- 后台真实贯通 `REST → Java MCP SDK → Python stdio MCP → resource catalog` GREEN：返回 38 个可见 pattern，抽取的
  两项资源、EXE/manifest/DDS 字节与 SHA provenance 均完整。此贯通只读安装目录，没有向任何 CK3 进程调用 snapshot/probe/export。
- rebase 后验证：Vitest `2 files / 8 tests` GREEN，Vite production build GREEN，Quarkus REST 测试 `3/3` GREEN；
  Quarkus 与其 Python MCP 子进程随后均关闭。全局检查中另有早于该贯通、父进程属于 `ck3_xqol` 工作区验收 runner 的
  CK3 PID `18916`，本工作包没有连接、操作或关闭它，继续遵守“不占用 CK3 或主屏幕”。

| 能力 | 当前 readiness | 限制 |
|---|---|---|
| 基础游戏资源目录到 Web UI | `static-ready / backend-integrated` | 磁盘目录端到端 GREEN；不含 DLC/mod 与 runtime registry |
| Web → probe/apply | `mcp-static-ready` | UI、REST 与 typed MCP 接线完成；本轮禁止占用 CK3，未做 designer live |
| Web → Copy/export | `mcp-static-ready` | UI、REST 与 typed MCP 接线完成；原生 Copy primitive 仍待获准后的 live round-trip |

下一步静默工作转向 DLC/mod 资源合并模型、DDS 预览输入与不依赖 CK3 的测试夹具；真实 designer round-trip 继续等待用户重新开放 CK3/桌面。

## 02:48：CoA 离线渲染与启动配置 MCP

- `d0d8f12d` 新增 manifest-owned DDS asset MCP；`03fdf358` 在浏览器解码 DXT1 pattern、DXT5 colored emblem 和
  surface mask 顶层 mip；`740bebfd3d64b610086ef6440208623215cf7c25` 将 exact 1.19.0.6 随附的五份
  Clausewitz/Jomini/game shader 源、15 个命名颜色和 `coa_mask_texture.dds` 投影成离线渲染合同，并由 Vue 实现
  pattern/emblem 通道调色、mask、transform、surface detail 与 alpha blend。`FallbackColor` CPU 绑定和 GPU
  采样/色彩空间仍明确未闭合。
- `1f4ad6b0674965953e13c4ff4727e648efdae471` 新增
  `ck3_query_coat_of_arms_load_configuration_v1`：只读当前 `dlc_load.json`，按顺序解析启用的 `.mod` 描述符，
  为目录模组枚举九类 `common/gfx/coat_of_arms` 直接候选；archive、资源 merge 和引擎 mount 状态均不声称已支持。
  Quarkus 新增 `/load-configuration`，Vue 会显示当前配置模组数，但仍把基础游戏 catalog 与模组候选分开。
- `b9a923fb` 根据本机真实 `.mod` 形状修正描述符 parser：支持 `tags={ "Gameplay" }` 这种无 `key=` 的列表块；
  静默读取前 16 份已安装描述符均通过。`18f31d38` 继续新增 configured catalog/asset 两项 MCP：分页解析目录模组
  designer manifest、保留配置顺序、标记同名冲突，并用绑定配置/描述符/manifest 的 opaque ID 读取 DDS；不自行选胜者。
  Quarkus 和前端已接入候选数量与 archive 来源状态，但尚不把候选混进基础游戏选择器。
- `463ce744` 随后补齐 ZIP archive：通过 ZIP 中央目录有界直读同类 manifest/DDS，不向磁盘解压；catalog 报告
  archive 来源/成员/manifest 数，asset endpoint 复用 opaque ID 和当前配置绑定。基础游戏、load/configured 三组在官方
  MCP 环境合计 19/19；普通环境 19 项通过、3 个 SDK 用例按设计跳过；前端仍为 20/20 且 production build GREEN。
- `a26982bb` 把 configured pattern/emblem 放进独立候选表：显示来源 mod 与同名冲突，用户可显式选择某个 opaque
  candidate 的目录/ZIP DDS 做浏览器预览并写入当前结构；导出仍只含 CK3 资源名，UI 不静默推断 effective winner。
  前端 20/20、typecheck 与 production build GREEN。
- `c8ec7494` 为唯一已由原生 MCP 检测的
  `textured_emblem = { texture = "_default.dds" }` 增加独立受限面板，只保真解析/编辑/导出 `texture`；不生成
  未验证字段，也不把该层冒充已进入 shader 合成。前端增至 21/21，typecheck 与 production build GREEN。
- 官方 MCP SDK 2.0.0 对真实用户目录的后台调用列出 76 个工具，返回 `enabled_mod_count=0`、`disabled_dlcs=[]`、
  `dlc_load.json` 38 bytes、SHA-256
  `B28A99338A45655C4A25CFEE44602A56451960142C9DD9E767B78C21A08C91BB`，并明确
  `launcher_database_used=false`、`engine_mount_observed=false`、`resource_merge_applied=false`。启动器 SQLite 中
  可保留未 active 的旧 playset，因此没有被当作当前配置真相源。
- 验证：新 Python 聚焦测试在普通环境 5 项通过、1 项 SDK 用例按设计跳过；MCP 环境 5/5；Quarkus 6/6；
  前端 4 files / 19 tests；production build GREEN，仅保留既有大 chunk 提示。rebase 后再次运行 MCP 环境 5/5、
  Quarkus 6/6 和前端 19/19，再普通推送。
- configured 增量完成后，load/configured 两组在 MCP 环境 10/10、Quarkus 8/8、前端 20/20；production build
  再次 GREEN。真实目录的官方 MCP 工具总数为 78，当前零模组配置令 pattern catalog 返回 0 个候选、0 个 manifest，
  并继续声明未应用 merge/precedence。
- 全过程只读磁盘或调用离线 `vision-report` 路径；没有启动、连接、聚焦、查询或关闭 CK3，没有调用
  session/probe/export，也没有占用主屏幕。本工作包产生的 Quarkus/MCP 子进程均已随测试退出；不对其他并发工作的
  CK3 状态作全局声明。

| 能力 | 当前 readiness | 限制 |
|---|---|---|
| 原版 DDS 到浏览器预览 | `static-ready / backend-integrated` | 顶层 mip 解码和字节 provenance 已验证 |
| shader-grounded 浏览器合成 | `static-ready` | 公式来自随附源码；尚无原生 GPU 像素对照 |
| 当前模组配置候选 | `static-ready / MCP-integrated` | 证明 `dlc_load.json` 与描述符，不证明 VFS merge/mount |
| 目录/ZIP 模组 designer 资源候选/DDS | `static-ready / backend-integrated` | 同名冲突可见；不选 effective winner |
| 完整 DLC/mod 有效资源视图 | `research` | 覆盖顺序、replace/merge 与运行时注册尚未闭合 |

下一步静默工作：继续优先补 MCP 的 effective-resource 投影能力，先从目录模组的可审计覆盖/合并模型和冲突 provenance
入手；在用户重新允许前，不做任何 CK3 designer live 或主屏幕操作。

## 03:35：CoA `_default.dds` 离线 MCP 与浏览器预览

- `402bb4b3` 已普通推送：`ck3_read_coat_of_arms_render_support_v1` 现在同时返回原版
  `gfx/coat_of_arms/textured_emblems/_default.dds` 的 bounded bytes、SHA-256 和 DDS 元数据；configured asset MCP
  也统一投影可解码的 `format`，保留原始 FourCC。
- exact 1.19.0.6 安装经官方 Python MCP SDK 2.0.0 离线读取为 96×96、7 mip、49,272 bytes、无压缩 BGRA8，
  SHA-256 `697430F86ABD26B5056A8779E4BF78C7CB526A57B5AD13F02898BA64B89526CC`。
- Vue DDS decoder 新增该精确 BGRA8 mask 布局，并在受限 `textured_emblem={ texture="_default.dds" }` 行显示原始纹理；
  尚未把该层合成进最终家徽，也不扩张到未实机验证字段。
- 验证：官方 MCP 环境 Python 资源组 14/14、追加 configured 组 5/5、前端 22/22、production build GREEN；
  仅有既存的大 chunk 提示。全过程只读安装文件或运行离线测试，没有启动、连接、查询或关闭 CK3，也没有操作主屏幕；
  本工作包的 MCP/测试子进程均已退出，不声明其他工作的全局 CK3 状态。

Readiness：受限 textured-emblem 从“源码保真编辑”增至“源码保真编辑 + exact 原始素材预览”，仍为 `static-ready`；
完整字段、shader 合成和 native pixel 对照仍未闭合。

- `579eafbb` 随后补齐 Web 编辑器直接“从剪贴板粘贴”：使用浏览器 Clipboard API 读取文本并立即走同一 parser/诊断链，
  空剪贴板、权限或非安全上下文会明确报错。前端 22/22 与 production build GREEN；首次命令误在仓库根执行仅产生
  `package.json` 不存在的工作目录 RED，改到前端目录后一次通过，未触碰 CK3 或主屏幕。

## 03:47：CoA installed-DLC 来源 MCP

- `cbc6ae3c` 已普通推送：新增 `ck3_query_coat_of_arms_installed_dlc_sources_v1`，在 exact-build 门后读取
  `game/dlc/*/*.dlc`，按描述符内容根枚举九类 CoA 直接目录；结果固定区分物理安装、商店授权、`disabled_dlcs` 应用、
  引擎 mount 与资源 merge，不把前者冒充后四者。Quarkus `/dlc-sources`、前端 typed client 与资源状态说明同步接入。
- exact 1.19.0.6 官方 MCP 2.0.0 实读列出 79 个工具；29 份 `.dlc` 描述符中
  `dlc_with_coa_candidates=0`、CoA TXT/DDS 均为 0。EXE 内同一 `clausewitzlib/dlc.cpp` 诊断簇包含
  `dlc_load.json`/`enabled_mods`/`disabled_dlcs`，并有 `replace_path not implemented for MSGR_DLC`，说明 store DLC 与普通
  mod 至少有不同分支；静态字符串不足以推出完整 mount 顺序，故没有新增 effective-winner 结论。
- 验证：四组离线资源 MCP 21/21、Quarkus 9/9、前端 23/23、production build GREEN；仅保留既存大 chunk 提示。
  本工作包未启动、连接、查询或关闭 CK3，未操作主屏幕；Quarkus/MCP/测试子进程已退出，不声明其他工作的全局状态。

Readiness：DLC 物理 CoA 来源从 `research` 升为 `static-ready / MCP-integrated`；授权、disabled-list 生效、引擎 mount 与
VFS precedence 仍为 `unresolved`。
## 11:16：B1 零幸存者永久卡死关闭

- R424 在推进日期前遇到只读 revision 重绑竞态，只记 harness RED；R426 用 370 天窗口否定
  `yearly_global_pulse -> every_player`；R428 用 740 天窗口证明换到 `random_yearly_everyone_pulse` 后旧严格门仍拒绝该现场。
- 源码状态机审计把根因收窄到唯一未由 snapshot 暴露的 `zg361_b1_oversight_return_status`：`1` 才是在途回调，`2` 是
  `.124` 已消费回调、恢复 `state=7` 并解除 publication block 的完成态。旧恢复门只接受 `0`，因此完成态 `2` 永久卡住。
- `4553a429f69d1df15d31d59e14300067c7c33320` 已把恢复门最小改为接受 `0/2`、拒绝 `1`；生成器、B1 76 项、
  `validate_local.py` 与 1,031 文件 release reproducibility GREEN，提交已推送。
- R430 从原 cycle/case `8/8`、`state=7`、active、roster/processing `0/0` 的冻结现场开始，在最多 740 日的硬上限内
  实际只推进 148 游戏日便观察到同一周期 `state=8 / inactive`，agenda 清零、没有奖励；十项后置条件全部 GREEN 后立即停止。
- live artifact：`Z:/ck3_mod_rewrite/_runtime/p1-b1-oversight-r430-20260911/live-artifacts/b1-minimal-wakeup-live.json`，
  SHA-256 `4AB0EE070EB686DB8737AF5086516FF412E2C4E297B7F3BC17FF32774FEF782E`。canonical / operator cleanup
  SHA-256 为 `9D3FCCBE60B3929694587180E31808F47C23ED2AD76A95F8C7819DAF3BF9C028` /
  `5FF5F2BC4FE9A342FF047C08800A4F78F12C70FFC8C24E311B6CA757E79E311B`，CK3 与 operator 均已退出。
- readiness：B1 zero-survivor recovery 从 `static-ready` 升为 `production-live primitive`，真实 mod bug 已关闭。该收据只证明
  遗留空周期安全退役；正式 P1 计数等 evidence assembler 绑定本收据后再更新。按项目所有者要求，不再重复该 checkpoint，
  也不为这个单 bug 扩成长跑。P2 继续 `LOCKED`。

## 11:35：R430 收据接入与 P1 机器账本纠偏

- 全程 no-launch；没有重新启动 CK3，也没有重复 B1 测试。R430 B1 原始 artifact 经源 SHA、精确前置、148 日上限内终态和十项后置条件校验后，生成 `r430-b1-fix-live.json`，SHA-256 `2358BEAC4D965FFC40150965C50543D1424A0BFE683540E1CC9F56E0A8DD6308`。
- 同一已结束会话的 `error.log`、`game.log`、`debug.log` 从 offset 0 覆盖扫描，产品 blocking diagnostic 为 `0`；完整时间窗 error-scan 收据 SHA-256 为 `8DBC32267039408718309D1F896C68E9F7DD4EF11F6E92F19033D1E3A9AB6EC4`。13,957 条既有 static-liveness warning 保持 nonblocking 分类，没有被隐去或改写。
- canonical cleanup 的全部 checks、空 failed list、空 CK3 PID inventory 经只读校验，managed-cleanup 收据 SHA-256 为 `FB96E81954C3717C906C3446B960429338A745A9262CA2D9CB92D13345D3F0A6`。
- 候选 L0 不重跑：复用 `202760f` 的完整 21-command 基线，并确认其后到 `4553a42` 的 product delta 精确只有九个 B1 文件；绑定本轮已经执行的生成器、B1 `76/76`、`validate_local`、1,031-file reproducible build 与 R430 实际加载树。收据 SHA-256 `6E297E568D08D4C1E4EF96B49A7F71A5E60A29DD227DB69BE6E2B182B4C506C7`。
- P1 中间账本由陈旧候选纠正为 R430 tree `84443728…E58CE`，当前严格为 **`5/9 = 55.6%`**：READY 是 B1、AF5、runtime error scan、cleanup、L0；PENDING 是 stage 9/10/11 与终态 cold restore。尚未生成最终 P1 manifest，P2 继续 `LOCKED`。下一次实机只允许围绕最近 stage 9 边界的有限目标推进，不再运行无目标的长时间线。

## 12:08：R432/R433 有界归因与 Stage 9 复用收口

- R432 只以 `zg361cl.390` 为目标并设 370 游戏日硬上限；到上限即停，没有续跑 Stage 10/11。RED artifact SHA-256
  `2D33D87660BCC6D6EED0CFDDABD36687A88344CBD38C3398AB18D3EEAF557805`。日志证明该窗口先消费了 B1
  零幸存者恢复，不能据此认定 Stage 9 产品故障。
- R433 只做零游戏日查询：`date_raw=54487200` 前后不变，Central inactive、stage 1–11 全 false，CL cycle 不匹配且
  无 pending event；七项查询检查 GREEN 后立即清理。artifact SHA-256
  `C4FCFAEE4F77B9C1D715A965A7A5827EACA1A138A9B352F97233285D15692689`。旧 B1 helper 不适用于合法零幸存者退役态，
  不覆盖 R430 的权威 B1 GREEN。
- 冻结 R390 continuation 实际包含 `.390` instance `1201` 的 authored1/native0 选择、旧实例消失与
  `postcondition_verified=true`，并非只有 preselection。R390 启动 commit `7f21638` 的 23 个 Stage 9 provider/event
  产品文件与当前 `4553a42` 候选逐字节相同；唯一后续 Central 改动属于 Stage 11。
- no-launch 门收据 `r390-stage9-terminal-live.json` SHA-256
  `6C91ADF634F32406B3368C8DE5859D4D1D59B5B45CAE190C5F2466B55CD1CB9E`，21 项 checks 全 true。P1 严格更新为
  **`6/9 = 66.7%`**；余项为 Stage 10、Stage 11、代表性终态 cold restore。P2 继续 `LOCKED`。
- 方法论已落实：小改只做因果范围验证，单个 bug 不做永久长跑。完整边界和哈希见
  [R432–R433 Stage 9 有界准入](../../phase2-promo/r432-r433-bounded-stage9-intake-2026-09-11.md)。

## 12:40：R434–R437 source 有界资格核对（后续已纠正 Stage 10 解释）

- R434 只调用一次 manager/subordinate selector；`date_raw=54487200` 前后不变，结果为
  `NO_ELIGIBLE_MANAGER_PAIR_IN_CHECKPOINT / no_bounded_ai_direct_manager`。离线纠正后，该结果只排除 R432 的 B3 focused
  “玩家直属 AI manager → manager 直属下属”source；它不能判断 `.120` 所需的反向“AI 上级 → 玩家 manager subject”路线。
- R435 在同一 checkpoint 只调用一次 owner-view Workforce provider；日期仍零推进，返回
  `subject_projection_read_failed / terminal=false`。因此该存档不能直接关闭 Stage 11，也不再重跑。
- R437 离线确认旧 R398 与当前 R430 候选的 257 个 Stage11/Workforce 相关文件逐字节一致，但实机恢复后发现输入存档是
  `date_raw=53611200 / active_event=null`。operator 在要求 `.242` 的入口检查立即 RED；`actual_advance_days=0`，没有事件
  选择、没有 `.360` 动作、没有重试，managed cleanup GREEN。历史 R398 实际是从该存档继续约 189 日后才到 `.242`；
  “存档停在 `.242`”的旧描述已纠正。
- R434/R435 目标 artifact SHA-256 分别为 `9ADDB2D8...626625`、`402E91D1...988CA`；R437 report / canonical cleanup /
  operator cleanup 为 `B1F8A803...E4C11` / `341A333C...EECC` / `1932156F...D43F`。三轮清理后 CK3/injector 均为空。
- 没有 mod 代码变更，也没有新增产品故障。P1 保持 **`6/9 = 66.7%`**；Stage 10、Stage 11、代表性终态 cold restore
  继续 PENDING，P2 继续 `LOCKED`。后续只在离线证明 source 合格后启动，不从 R398 pre-entry save 重做 189 日单项长跑。
  详见 [R434–R439 source 核对与 Stage 10 路线纠正](../../phase2-promo/r434-r439-bounded-source-checks-and-stage10-route-correction-2026-09-11.md)。

## 12:53：R439 对 R159 manager seed 的零日重复查询

- 历史 R159 contract 证明 player-manager `32904` 与 direct reviewable subject `26347` 的真实绑定。R439 又执行了一次 B3 selector 原生查询；2026-09-07 日报已经明确写明 R159 不具备该 selector 所需的 AI manager/subordinate 层级，因此这次查询本可避免。它没有推进时间，也没有发送玩法动作。
- 查询前后 `date_raw=53154120`、paused/player/PID 均保持一致；typed 结果为 `NO_ELIGIBLE_MANAGER_PAIR_IN_CHECKPOINT / no_bounded_ai_direct_manager`，manager/subordinate 均为空。该结果只重复排除 R159 的 B3 focused 资格，不淘汰它的 player-manager 基线，也不构成产品 RED。
- artifact / canonical cleanup SHA-256 为 `D2CE38B5...33AD6` / `5BEC7631...02851`；cleanup GREEN，CK3/injector inventory 为空。没有 mod 代码变更，P1 保持 **`6/9 = 66.7%`**，P2 保持 `LOCKED`。
- Stage 10 `.120` 后续只接受已离线证明“AI Central owner → 玩家 F-case subject”身份与 near-boundary 状态的 source；不再用 B3 selector 代替 `.120` 资格，也不从玩家自己的 Central Stage 9 时间线长等 `.120`。完整记录见 [R434–R439 source 核对与 Stage 10 路线纠正](../../phase2-promo/r434-r439-bounded-source-checks-and-stage10-route-correction-2026-09-11.md)。

## 13:17：Stage 10 验收编排离线纠正

- 产品脚本确认：玩家作为 Central owner 时，Stage 10 冻结的是玩家直属的 AI manager；AI subject 到 F-case terminal 后静默完成，不触发玩家可见 `.120`。玩家可见 `.120` 属于另一条角色路线：AI 上级是 Central owner，玩家本人是 F-case subject。
- `zg361_phase2_terminal_stages_action_cell.py` 原先把 Stage 9、`.120`、Stage 11 串在同一 played-owner lineage，因果关系错误；R390 在 runner 等待 `.120` 时已进入 Stage 11，正是该编排问题的实机表现。
- 最小修正后，该 runner 只负责同一玩家 Central owner 路线上的 Stage 9 与 Stage 11，并在 artifact 中显式声明独立 Stage 10 `.120` 收据仍必需；P1 九项机器门及 `.120` 硬门均未降低。
- 只运行两个相关 Python 测试文件的 normal/`-O` 模式，共 `10 + 10 + 6 + 6` 项 GREEN；没有运行全量 L0，没有启动 CK3。P1 保持 **`6/9 = 66.7%`**，P2 保持 `LOCKED`。

## 13:26：Stage 10 历史证据离线收口

- 按事件实例字段精确检索现有可访问 `_runtime` JSON，没有发现真实 `zg361mg.100`–`.103` 或 `.120` 事件记录。旧 formal report 中的 `.120` 只是加载期本地化缺键日志；P1 assembler 仍明确记录没有合格 provider-observed terminal artifact。
- R390 `.390` 的 `date_raw=53615592` 前后没有 near-boundary save：最近前置存档相差 183 游戏日，最近后置存档已越过 182 游戏日。本轮因此停止，不启动 CK3，不为单个 Stage 10 门重放 183 日时间线，也不重跑 B1。
- P1 保持 **`6/9 = 66.7%`**，Stage 10、Stage 11 与代表性终态 cold restore 继续 PENDING；P2 保持 `LOCKED`。后续只复用合格近边界证据，或在其他有独立产品价值的流程自然抵达边界时即时冻结。

## 17:17：Stage 10 玩家 subject 有界 action cell

- 进一步读取现有 manager-governance provider 后确认它已支持 `subject_binding_kind=played_character` 的玩家 subject
  反向拓扑；无需新增 DLL、MCP、ABI 或 schema。Stage 10 可从真实 paused `.390` 边界选择一个 AI manager，确认 Stage 9
  后先由 owner-view provider 观测该 manager 的 F case 真正打开，再把玩家切到该 manager。
- 新增独立 action cell，共用一个 30 游戏日绝对截止等待 opening case 与真实 `.120`；`.120` saved scopes 必须回指原
  owner/当前玩家 subject，且现有 provider 必须回读 `state=5 / active=false` 后才保存终态并确认事件。未观测 opening
  case 时绝不切换；任一失败立即保留 RED，不原地重试。
- 聚焦测试 normal/`-O` 各 `5/5` GREEN，另通过 `py_compile` 与 UTF-8 BOM 检查。没有运行全量 L0，没有启动 CK3，
  没有 live artifact。状态为 `static-ready / live pending`；P1 仍为 **`6/9 = 66.7%`**，P2 仍为 `LOCKED`。
- 详细合同：[Stage 10 玩家 subject 有界 action cell](../../phase2-promo/stage10-player-subject-bounded-action-cell-2026-09-11.md)。

## 17:32：Stage 10 受管 operator 静态就绪

- 新增 `job_role=stage10-player-subject` 的独立 operator，复用既有 AF5 frozen-input admission、唯一 CK3 生命周期与
  managed cleanup；控制面只有 `status / run-stage10 / cleanup`，没有原地 retry。GREEN 时归档精确 `.390` source 和
  `.120` terminal；RED 时尽力先保留 source，再停车等待清理。
- operator 聚焦测试 normal/`-O` 各 `3/3`，`py_compile` 与 UTF-8 BOM 检查 GREEN；新增用例确认业务门 GREEN 后的
  checkpoint 归档故障保持 `product_result=GREEN`、外层为 harness RED。没有修改 mod、DLL、公共 MCP、ABI、schema 或依赖。
- T2 已同步 open_kaishek：通用 1.1 adapter 不需生产代码修改，commit
  `bab3efe9883ed730637b4aeac059b228779d0ce2` 增加目标自有 control 的兼容测试与记录，聚焦测试 `13/13` GREEN，
  `HEAD == origin/main`。
- 当前轮次 R439 已结束，CK3 存活数为 0；本工作包没有启动 CK3 或新建轮次。精确 `.390` source 尚不存在，不能仅为
  Stage 10 从 183 日前重放；新轮次 R440 必须先绑定独立的有界 P1 机器门，若自然抵达 `.390` 再即时冻结。P1 保持
  **`6/9 = 66.7%`**，视频锁未触碰，P2 保持 `LOCKED`。

## 17:53：Stage 10 source 捕获与启动前准入

- 离线核对确认 R390 只留下距 `.242` 约 189 游戏日的前置 autosave 和已经历产品 RED/谱系失效的后置 autosave；后续
  terminal partial checkpoints 均仍在 Stage 9。没有合格 Stage 11 near-boundary source，因此没有启动 R440。
- Stage 9/11 action cell 现在自然抵达 `.390` 时只读检查 manager selector；正向时在选择 Stage 9 前保存并以流式 SHA-256
  立即复制 source，负向只记 `INELIGIBLE` 且不阻断原路线。terminal-stages operator 把产品树、commit、PID/generation
  补入可复用 source 收据。
- Stage 10 operator activation 强制校验该收据与 event/owner/player/manager/selector/product tree/checkpoint 的完整绑定；
  缺失或漂移会在 CK3 启动前 RED，不能再用 R159/R432 一类未合格存档消耗新轮次。
- 与改动匹配的聚焦测试：terminal action normal/`-O` 各 `12/12`，terminal operator 各 `7/7`，Stage 10 operator 各
  `4/4`，均 GREEN；另通过 `py_compile`、BOM 与 diff 检查，不运行全量 L0。
- activation 数据合同变化已触发 T2。open_kaishek 通用 Operator MCP 1.1 无需生产代码修改；兼容记录 commit
  `8b68c63f1453b9da2907b9e2afd5825949ff93f9` 已推送，最终 source hash 刷新 commit
  `16d9e8e100e120738086c62a25525646f7098d02` 已推送并与 `origin/main` 一致；既有 `bab3efe...` 的 13/13 control 测试
  继续适用，没有重复运行。
- 当前轮次 R439 已结束，CK3 存活数为 0，旧实例均已终止；本工作包未创建 R440。P1 保持 **`6/9 = 66.7%`**，
  Stage 10/11 与代表性 cold restore 仍 PENDING，P2 视频锁未触碰。

## 18:09：G2 source-specific R440 启动门刷新

- T0 当前没有合格 Stage 10/11 near-boundary 输入，因此未用 CK3 槽重放 183/189 日前缀；在保持 P1 `6/9` 与
  P2 `LOCKED` 的前提下，转入既有 T1 G2 限界实机包。
- 第一次 no-launch preflight 在启动 CK3 前因 `profile_settings_guard` 整文件哈希陈旧而 RED。将当前文件与冻结提交
  `7dec66d` 做 AST 对照后，适配器实际消费的七个定义全部一致；其余 13 项冻结依赖也逐项匹配。
- 只刷新这一项 manifest 哈希。新 manifest SHA-256 为
  `4A370EEAE10B588ABD6730D2F4E0FC0998A73D80ADECE1A8CF778F2F54D73079`；第二次 preflight 返回
  `READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE`。收据路径为
  `Z:\ck3_mod_rewrite\_runtime\g2-source-specific-r440-20260911\preflight-green.json`，20,184 bytes，SHA-256
  `81A9F1D38FF1ED1F86FC89652A3D268D9F1ADCC336FFCE91ED5C729CEDCFAE4C`。
- 两次均未启动 CK3，当前轮次仍为 R439、存活实例 0；失败记录保留。T1 仍为 90%，等待唯一 R440 live 命令；
  source-specific loss/comparison input 尚未因 preflight 提升。

## 18:25：R440 原版单选事件 harness RED 与最小修复

- R440 以 root `c7a4cf2`、CK3 1.19.0.6、冻结 G2 DLL `4D8395…E238B` 启动唯一 PID `182552`；旧轮次 R439 已终止。
  它进入 Robert 1066、处理 `bookmark.1070.c` 后在原版单选事件“埃玛成年”暂停。旧 fallback 连续发送 `Shift+1`，22 次
  均未关闭事件；目标 `.1071.a`、六次 source capture、bridge 注入和 surrender 均未发生。
- 重复操作不再产生新信息，因此没有等满 520 秒。PTY 控制字符未传到 Python child 后，按已记录的 driver `78760`、observer
  `167192` 与 CK3 `182552` 精确清理；最终 CK3 inventory 为 0。R440 harness RED 收据 SHA-256 为
  `4016A49E72071DCA176224C5AE1283DBBC2293685F384EDB00023DFE7520B441`，不构成产品 RED。
- 最小修复改为复用既有 `quick_stall_and_recover`：只有 OCR 选项及点击后消失均验证通过才继续，否则立即 RED；删除盲按键。
  R440 截图离线回放命中 `[930,1043] / classic_event_option`；聚焦 adapter 测试 normal/`-O` 各 `20/20`，`py_compile`、
  两个 Python 文件 UTF-8 BOM 与修复后 no-launch preflight GREEN。
- 最终 adapter / manifest SHA-256 为 `CE9FF6D9…24848` / `29549DFC…113DE`。当前轮次 R440 已结束、旧实例均终止，
  T1 仍为 90%；T0 P1 保持 **`6/9 = 66.7%`**，P2 视频锁保持 `LOCKED`。

## 18:38：R441 有界实机验证与后继存档保留

- R441 以 root `a911c25` 启动唯一 CK3 PID `140912`；旧轮次 R440 已结束。本轮没有 DLL、游戏文件、加载顺序或启动环境变化，
  仅验证已提交的 Python 编排修复，并严格使用既定 520 秒 target window。
- 可验证 OCR recovery 连续处理 18 个原版事件，每次都确认选项消失并恢复日期推进；其中一次还通过既有时间轴恢复分支继续。
  因而 R440 的真实 harness bug 已在实机闭合，不再是待验证修复。
- `bookmark.1071.a` 在截止前仍未出现；source capture、bridge 注入和 surrender 均未发生。exact-build 原版定义显示该事件虽在
  开局后 1–7 年排程，但还要求 `gold >= 100`、`is_at_war = no` 及非玩家拜占庭持有者，失败后每 25 日重试。持久截图已到
  `1073-10-31` 且玩家 694 金，说明继续单纯扩大 timeout 没有依据。
- RED 报告为 `live-artifacts/report.json`，2,979 bytes，SHA-256 `FDE8FB0A…B128`；聚合收据
  `r441-bounded-attempt.json` 为 3,716 bytes，SHA-256 `E8EE5755…F88F`。cleanup GREEN，当前 CK3 与 driver 均为 0。
- 后继 `last_save.ck3` 已保留，85,561,556 bytes，SHA-256 `A0E122CA…51A98`；目前只是未验收 resume candidate。下一包为
  adapter 增加逐字节绑定的新 userdir 接续入口，避免重放已完成前缀。本轮未改变公共 MCP/ABI/schema，不触发 open_kaishek
  代码同步。T1 保持 90%；T0 P1 保持 **`6/9 = 66.7%`**，P2 视频锁未触碰。

## 18:55：G2 hash-bound 续跑入口 static-ready

- adapter 新增成对参数 `--resume-save / --resume-save-sha256`。启动前验证非空 `SAV0101`、exact `1.19.0.6` 与声明哈希；live
  路径复制到 fresh userdir 的 `save games/autosave.ck3` 后再次核对大小和哈希，全部完成后才允许 `Popen`。无参数时仍走原新开局路径。
- outer-owner 兼容字段 `startup_mode=normal-event` 保持不变，新增 `startup_source=resume-checkpoint`；resume 路径只点击一次可见
  `继续游戏`，source/war/readiness 事实仍完全来自 native observer 与 MCP。
- 聚焦测试为 adapter normal/`-O` 各 `23/23`、outer-owner normal/`-O` 各 `8/8`，另有 `py_compile` 与 Python UTF-8 BOM GREEN。
  R441 实档 no-launch admission 返回 `READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE`；收据
  `Z:\ck3_mod_rewrite\_runtime\g2-source-specific-resume-admission-20260911\preflight-final.json` 为 20,613 bytes，SHA-256
  `1D5B0B77…88B`，前后 CK3 inventory 均为空。
- adapter / manifest SHA-256 为 `13202ADA…33C` / `A2C2A93F…9763`。本包没有 DLL/游戏文件变化，没有创建新轮次；当前轮次
  R441 已结束，旧轮次 R440 已终止，CK3=0。它只把续跑入口提升到 static-ready，source capture、comparison、decision/action 和
  `GEN-034` 均未提升，T1 仍为 90%。该 CLI/preflight 数据变化触发 T2 open_kaishek 同步；T0 P1 仍为 **`6/9 = 66.7%`**，
  P2 视频锁保持 `LOCKED`。

## 19:05：R442 resume 实机与非 modal 暂停修复

- 启动前先出现一次目录形态 harness RED：fresh userdir 尚未创建。它发生在 `Popen` 前，CK3=0，已冻结为
  `r442-prelaunch-input-red.json`；创建空目录后沿用同一 R442 标识，没有虚增轮次。
- R442 随后启动唯一 CK3 PID `43852`。R441 的 85,561,556-byte 存档通过源/目标哈希核对，主菜单稳定识别并仅点击一次
  `继续游戏`，地图从 1079-01-01 恢复并进入速度 5，故 hash-bound copy/load 路径获得 live 证据。
- runner 后续因 8 秒 HUD 日期 OCR 空窗进入恢复；画面没有可验证事件选项，旧逻辑直接 harness RED。debug runtime 已推进到
  `1082.4.23`，且产生三份递进 autosave，证明不是加载帧永久冻结。source capture、bridge 注入和 surrender 均未发生。
- 冻结聚合收据 `r442-bounded-attempt.json` 为 3,760 bytes、SHA-256 `366E5B7E…C62D`；runner report 为
  `2AB08965…7385`。cleanup GREEN，CK3=0；error/debug 的 `XAR:` 命中均为 0，没有产品 RED。
- 最小修复保留已有 verified event-option 点击；仅当不存在验证后的 modal 选项时，重做已有速度/时间轴动作，并且必须读到严格更晚
  的 HUD 游戏日才能继续，否则仍 RED。adapter normal/`-O` 各 `23/23`、`py_compile` 与 BOM GREEN。
- 最新 R442 `autosave.ck3` 为 90,582,861 bytes、SHA-256 `3D8755AE…6E07`；新代码 no-launch admission GREEN，收据
  `C10FDE71…5391`。adapter / manifest 为 `94E4C574…736C` / `90D1B308…C28B`。当前轮次 R442 已结束，旧轮次 R441 已终止，
  T1 保持 90%；变化触发 T2 open_kaishek 同步。T0 P1 保持 **`6/9 = 66.7%`**，P2 仍 `LOCKED`。

## 19:15：R443 死亡终态与场景耗尽

- R443 以 root `e073216` 启动唯一 CK3 PID `187992`，继续使用 CK3 1.19.0.6、冻结 DLL `4D8395…E238B` 与相同加载顺序，
  从 R442 的 `3D8755AE…6E07` 存档接续。resume copy/load 与地图恢复再次 GREEN。
- 无 modal 恢复分支已在实机触发：重设速度 5、点击时间轴后仍读不到更晚日期，因而正确 RED 并保存 stall 帧。该帧显示真实原因是
  罗贝尔 69 岁于 1084-05-06 死亡，游戏因继承暂停；runner 没有点击“继续扮演公爵罗杰”。
- exact `game_start.txt`（SHA-256 `84C0101F…136C`）将 `.1071` 排程到 `character:1128`，事件失败重试也保留同一 root；改玩继承人会
  破坏 expected CharacterID/source identity。因此这条存档链分类为 `SCENARIO_EXHAUSTED`，不启动 R444 继续死档。
- 冻结 receipt `r443-scenario-exhaustion.json` 为 4,188 bytes、SHA-256 `CFBB1AFF…0DB6`；终态截图/report 为
  `A2C78B8D…63CF` / `28EF8764…DFD7`。cleanup GREEN、CK3=0、error/debug `XAR:` 均为 0；无产品 RED。
- source capture、bridge、surrender 均未发生，T1 维持 90%。下一次 CK3 前先设计能让新场景中的罗贝尔满足 `is_at_war=no` 的
  可执行前置方案，不用 console 冒充自然 source，也不再被动跑完整寿命。没有接口/ABI/schema 变化，本包不触发 open_kaishek
  代码同步。T0 P1 仍为 **`6/9 = 66.7%`**，P2 保持 `LOCKED`。

## 19:36：R444 native snapshot 纠错与目标事件去向闭合

- R444 首次调用因 fresh userdir 未预建在 `Popen` 前 RED，未启动 CK3；补空目录后沿用同一轮次。实际运行启动唯一 PID
  `70988`，从 R442 哈希绑定存档一次进入地图并连接 exact-build bridge，没有事件点击或战争 mutation。
- canonical snapshot 已明确给出 `date_raw=53278320`、played character `29829`、`active_wars=[]`。adapter 却仍要求旧的顶层
  `played_character_id`，因此错误等待 60 秒。最小修复兼容 canonical `played_character.character_id`，并保留旧形状兼容。
- adapter normal/`-O` 各 `23/23`、`py_compile`、BOM 与 real-save no-launch admission GREEN。adapter / manifest / admission
  SHA-256 为 `9F88417E…08A9` / `AF5533CA…165B` / `979DCD95…0489`；没有扩大验证范围。
- R444 snapshot 同时否定“战争阻塞”假设。R441 successor 的同一序列化角色记录相距 59 bytes 保存
  `show_historical_gui + raiktor`，与 exact `.1071:immediate` 一致，证明目标曾自然打开；结合 observer 未 arm，根因为标题漏识别后
  generic modal recovery 吞掉目标。下一包只加 target-option 识别与 arm-before-click 门，不再跑年份。
- R444 report/classification SHA-256 为 `111D3CFA…3438` / `3827A3E1…E5D4`；cleanup GREEN、CK3=0、`XAR:`=0。当前轮次
  R444 已结束，旧轮次 R443 已终止。T1 仍为 90%；T0 P1 仍 **`6/9 = 66.7%`**，P2 视频锁未触碰。
## 19:47：`.1071` target-option arm guard static-ready

- source loop 每帧先匹配 `.1071.a` 完整文案；OCR 拆行时必须在 event-option 区域同时命中“扶上 / 君士坦丁堡 / 皇位”，
  才选择覆盖 token 最多的文本行。单一城市词不会误判，命中后顺序固定为 `atomic_arm → deliberate_click`，不会落入 generic recovery。
- 聚焦 adapter normal/`-O` 各 `24/24`，`py_compile`、BOM、diff 与 no-launch preflight GREEN；adapter / manifest 为
  `6CB7E3B9…079AB` / `C271267F…10648`，未启动 CK3。
- 离线筛完 R440/R441 存档：R441 三份 autosave 均已有同记录 `raiktor`，不能重用；R440 `last_save.ck3` 尚无 marker，画面日期
  1068-01-03，58,320,404 bytes，SHA-256 `D1E469D0…CDA9D`。其 R445 input-specific admission 返回 READY，收据
  `5E6A0A00…AACD`，下一轮从这里接续而非从 1066 重跑。
- 当前轮次 R444 已结束、CK3=0。此包只到 static-ready，不提升 source/comparison/action/`GEN-034`，T1 仍 90%；T0 P1
  仍 **`6/9 = 66.7%`**，P2 `LOCKED`。

## 20:12：R445 原版信件 RED 关闭、通用资产同步与 R446 入场

- **实机结果**：当前轮次 R445 以唯一 PID `44836` 从 R440 的 `1068-01-03` pre-target 存档继续，推进至
  `1069-08-16` 后停在掌玺大臣外交失败信件。exact-build 定义确认它是 `chancellor_task.1004`，由
  `chancellor_task.1003 -> task_foreign_affairs_side_effects` 调用；唯一选项只施加限时好感惩罚。因此本次是
  harness RED，产品 RED 为 false。R445 已结束，旧轮次 R444 已终止，cleanup GREEN，CK3 当前为 0。
- **最小修复**：source adapter 仅在正文和选项区同时识别 `掌玺大臣 / 外交行为 / 可怕的误会` 时点击唯一选项并恢复时间；
  未改共享 runner、DLL、游戏文件、启动参数或加载顺序。normal/`-O` 各 `25/25`，实际截图离线回放命中
  `(1266,984)`，`py_compile`、BOM、diff 与 no-launch admission GREEN。提交并推送
  `3b632641ab839a6b9d569208e762fc39ad9fa052`。
- **证据**：R445 report SHA-256 `42084B46AC8B3E03963552C97FDEF04BB5D7E62F11C5886C9F8F3933FA279D86`；
  classification SHA-256 `FF1E7018E106C2CF9D11F31C39340953320FC32E9DBE3BFAFC0EB3E6BDFE554E`；
  successor save SHA-256 `431320AAC5094501BE48005C3A13E7FF0B4C75A56639A47C96352D04B1AFDBFF`，无 `raiktor`；
  R446 final admission SHA-256 `8A3C4FFA6E3C3A68738B6B06184F053C855B53196C896111B405A92971CBBE0F`。
- **MCP 通用资产**：`dacc1d759d349ff142f167e265f09077c51da27d` 将 `.1004` exact-build definition、caller、
  安全选项合同和诚实 R445 observation 接入默认 registry；source-index 现为 183 definitions，dataset SHA-256
  `265EBCE989627D68C69DDEF178A7BC0EBE1DE846721E42584D2B8271D14E9CFD`。聚焦 registry 测试
  `13 passed / 63 subtests`，generator `--check` GREEN。
- **T2**：`open_kaishek` 已同步 provider commit、dataset identity、计数和文档；聚焦 Maven `4 tests` GREEN，
  提交并推送 `2a558f6317551ba5f04f7d95009071d7f96bc90c`，本地与远端一致。首次 Maven 调用因 PowerShell 未引用
  `-D` 参数而在解析期失败，已按真实 CLI 失误保留，引用参数后的同一测试通过。
- **readiness**：T0 P1 仍为 `6/9 (66.7%)`，Stage10 `.120`、Stage11 terminal、代表性终局冷恢复待完成；
  P2 宣传视频继续硬锁。T1 G2 保持 90%，本轮未捕获 `.1071` source，不提升 comparison、decision/action 或 `GEN-034`。
- **下一步**：只从上述 admitted successor 启动新轮次 R446，进行一次有界 natural-source continuation；不重复旧前缀，
  不为单一事件扩大验证范围。

## 20:31：R446 自然命中 `.1071`，点击验收缺口最小修复

- **目标真实到达**：当前轮次 R446 以唯一 PID `207976` 从 R445 successor 推进，在 `1070-09-02` 自然打开
  `bookmark.1071`；exact `.1071.a` 位于 `(931,934)`，observer attach、断点安装与 arm SHA 均成功。
- **RED**：adapter 发出点击后直接等待 observer，没有验证选项从画面消失；capture 为 `source_execution_count=0`、
  `debugger-detach-failed`，原始断点字节已恢复。没有 post-click UI 帧，因此不能把该轮解释为 source 已执行，也不能判定 native seam
  失效。状态为 harness RED，source/comparison/decision/action readiness 不提升。外层 cleanup GREEN，R446 已结束，CK3=0。
- **原版日志**：`20:23:35` 保留 exact-build `bookmark.1071:immediate` line 1477 的
  `scope:raiktor invalid while building tooltip/description`。它发生在 `20:23:40` 点击前，故不能证明 option mutation 成败；
  按 RED 保留并留给下一 action-bound run 互证。
- **最小修复**：`85b7c8b49802a981f78e2e285f515f12e52a5812` 只对已高置信识别并已 arm 的 `.1071.a`
  最多重试三次，必须由同一识别器确认选项消失；保持可见即 RED。未改 generic recovery、observer、bridge、DLL、游戏文件或加载顺序。
  normal/`-O` 各 `27/27`，`py_compile`、BOM、diff 与 manifest binding GREEN；提交已推送且本地/远端一致。
- **证据**：report SHA-256 `536C590B10E67DFC0B362D418FFC534636FD2CFF2CC837C62AC1C78FFF2600FD`；
  capture `808DE68DD45D965E21BAC24F7D69D848B03C982E0295BC24F272FDA75433E453`；armed screenshot
  `EE3803364AC9465161C943E115F253B15F2547C09305F28341A9B4CA453850A6`；classification
  `FEDFC309F84F2B7ECB66B52CA99F98D15768360FC90BAD7AFF8F5EE6181E7F59`。
- **R447 输入**：R446 latest save 64,952,354 bytes，SHA-256
  `523D365EC6E566EE7432C99B04AD682C99BFCA92D26FDA7EDE340AACCCA38709`，无 `raiktor`；当前 adapter 的
  no-launch admission 为 READY，receipt SHA-256
  `BCF0467F59E1BEEFD02B2868BF4F159980E595137F58790A4A93860097475112`。
- **门禁**：T0 P1 仍为 `6/9 (66.7%)`，P2 视频硬锁；T1 G2 仍为 90%，`GEN-034` unresolved。
  本包没有接口/协议/数据格式变化，不影响 `open_kaishek`，也没有新增 MCP 通用资产。下一步只从上述近边界输入启动一轮 R447。

## 20:52：R447 目标点击已确认，native 断点命中后暴露证据门错误

- R447 唯一 CK3 PID `58396`。`.1071.a` 在 `(931,934)` 首次点击后由同一识别器确认消失；observer 随即以 `armed-hit-evaluated-name-mismatch` RED 结束，断点已安装和恢复，debugger detach 成功。由此确认 native `spawn_army` 已命中，旧采集器是在追加 evidence row 前错误地硬筛 `evaluated_name`。
- 原版 tooltip scope RED 仍保留，但真实 mutation breakpoint 命中已经证明它不阻断选项执行。report/capture/classification SHA-256 分别为 `C3EA19C27F8C4AF0DDE05AC0DD73BDF1D19F74CF63FC46A0C244B66DD4A6D841`、`BAAA2D4A2CE463F414E3FD6D50AF96D2AB94BE0ACFD15059C7DD2BD2F38E7D91`、`4F9E9DD3E8EB19DFD7B9BC88A308F39E7D43A0721185B77630D14096BE34C767`；cleanup GREEN、CK3=0。
- `8e2a8917143e261ccac589436b44baafdb1b9d14` 让私有 observer 先保留真实名称，同时维持最终六行 identity validator 的 RED 门禁。新 executable SHA-256 `B05E0B6D3CA8DBEC41C8C5107AB8F9AACD4E99981E442AC1DBF3077868241007`，self-test GREEN，normal/`-O` 聚焦矩阵各 `53/53`。
- R447 最新 pre-target save SHA-256 `89D15B8ACB0E69E6C439D658582B63DC1F8AD11EC687089E5021A5961607D4DD`；R448 no-launch admission receipt SHA-256 `5FF8771F9CCCA853FA4C4FE8FA7B7BE0787C3EAB5EEF25B18FD8FD5A9601E3EB`。T2 记录提交 `1d67a5e9567e83af2875c5122681c3b35bc92278` 已推送，公开 MCP/Java/profile 无变化。
- T0 P1 仍为 **`6/9 = 66.7%`**，P2 `LOCKED`；T1 G2 仍为 90%，`GEN-034` unresolved。下一步只运行一轮 R448 近边界取证，不重放旧前缀、不做永久长跑。

## 21:07：R448 六行 source 捕获与本地化合同修复

- 第一次命令因漏传外部 `--game-root` 在 CK3 创建前 fail-closed；修正后当前轮次 R448 以唯一 PID `26020` 启动。`.1071.a` 首次点击后确认消失，observer 捕获六行、恢复断点并 detach；外层 cleanup GREEN，R448 已结束，旧轮次 R447 已终止，CK3=0。
- 六行均绑定 exact WarID `33554473`，loaded node/CArmy generation 各自唯一，共 24 条 current/persistent regiment 映射，实测创建兵力 `6×500=3000`。运行时 `evaluated_name` 全为 `诺曼路匪`；旧合同把它与 authored key `norman_highwaymen` 比较，故保留 `six-execution-identity-mismatch` harness RED，产品 RED 为 false。
- artifact SHA-256：capture `B819D4C94B3BD25EC1B505368801FE5EC2BD09CBCEFB934543B687CB1A984A1D`；report `F42E36EA27E7A2CA099A49729AAE673C5A393900B3195080F7B6995BF80BA720`；classification `852DDB667BDEC287450441BC95A5032065F9BB77AA3FF67FFF6F315A2E300ABF`。
- `0235a50241f3dd6c37d375ff00bf56d76620d3a9` 将 C++/Python 名称门改为非空且六行一致，保留其他 identity 硬校验；新 capture executable SHA-256 `020F051DDE034CBBC67C5A308F8E035FFA3E224844AC413261AA257466B0F185`。self-test、normal/`-O` 聚焦矩阵各 `53/53` GREEN；提交已 rebase、push 并与远端一致。
- T2 提交 `880888cb130cbf2d7002ff02c9047d3e15e5f45a` 已同步私有数据合同；公开 MCP/Java/profile、DLL、游戏文件和加载顺序不变。R448 autosave 与输入 SHA-256 同为 `89D15B8ACB0E69E6C439D658582B63DC1F8AD11EC687089E5021A5961607D4DD`，无 `raiktor`。
- T0 P1 仍为 **`6/9 = 66.7%`**，P2 `LOCKED`；T1 G2 仍为 90%，`GEN-034` unresolved。下一步仅一轮 R449 从同一近边界输入通过修正后的 capture，并继续既定 same-PID lifecycle；不重放旧前缀。

## 21:16：R449 启动前消除动态 WarID 必然失败

- R448 的真实 source WarID 为 `33554473`；旧 live seam 却要求历史 fixture WarID `50331699`，且 concrete continuation 未接收 outer owner 传入的参数。该问题在 R449 启动前由静态调用链复核发现，因此没有浪费新 CK3 轮次。
- `5743466d1074af68ff12930bbe299becc12fef8c` 让 outer owner 从已验证 source capture 派生 full-generation WarID，并把它传给 current、唯一 termination 与 postwar；可选 CLI WarID 只做相等断言。normal/`-O` 聚焦矩阵各 `53/53`，提交已推送并与远端一致。
- T2 提交 `5aec42436035e870a63c29ece582d41fc909163a` 已推送；公开 MCP/Java/profile、DLL、游戏文件和加载顺序不变。当前 R448 已结束、CK3=0，T0 P1=`6/9`、P2=`LOCKED`、T1=90%、`GEN-034` unresolved。

## 21:26：R449 六行校验通过后 debugger detach RED

- R449 以唯一 PID `31936` 启动，处理已知信件后 `.1071.a` 首次点击确认消失。六行 locale-neutral 名称与动态 WarID `33554473` 校验均通过；`DebugActiveProcessStop` 在 final debug event continue 后失败，observer 改写为 `debugger-detach-failed` RED。断点已恢复，outer cleanup GREEN，R449 已结束、旧轮次 R448 已结束、CK3=0。
- capture/report/classification SHA-256 分别为 `E382E079DC7A6124A9961E174A3E802F8E67B0C95403325FB16485FD51A5E178`、`F200F6744242EED27D7ABAF8EA66DBE36ECF7FAEAF0F41E6960DDC4794F3864D`、`CAB2A4A1A8AF07FCB494F5ED52A02B49A05916F37575510A26A634CA00398B2C`。未启动 bridge、未提交 termination，readiness 不提升。
- `454f515d8ef55ddf6e3cd5eccfa0a8cfb26e7630` 增加 20×25 ms 的有限 detach retry（最多 475 ms sleep）及 attempts/last-error telemetry，耗尽仍 RED。新 executable SHA-256 `EEE39F858E941E1500DA13FB11906814FA4D70EE42DED894CFDEB03ACEF709B8`；self-test 与 normal/`-O` 聚焦矩阵各 `54/54` GREEN。
- T2 提交 `36009e7994f1db80f4e100eac7894dd357802b3a` 已推送；公开 MCP/Java/profile、DLL、游戏和加载顺序不变。T0 P1=`6/9`、P2=`LOCKED`；T1=90%、`GEN-034` unresolved。

## 21:48：GEN-034 R450 停战期限观测 RED

- 当前最高优先级仍为 T0 P1；由于没有合格的 Stage 10/11 边界输入，本轮使用空闲 CK3 串行槽推进 T1。P1 保持 `6/9 = 66.7%`，Stage 10、Stage 11、代表性终态 cold restore 待完成；P2 继续 `LOCKED`。
- R450 唯一 PID `105244` 从 SHA-256 `89D15B8…D4DD` 的近边界存档启动。目标选项首击接受，六行 capture GREEN，动态 WarID `33554473`，实测当前兵力 3000；debugger detach 有限重试在实机以 `attempts=1 / last_error=0` 成功。
- bridge 在同一暂停帧完成两次公开 termination-term 查询；两次 transport 均 GREEN，gold/prestige/PoW/favor/generic war-bound 均有值，但当前真实战争的 `evaluated_days` 均不可观测，唯一缺失域为 `truce`。正常 lifecycle 因此在 checkpoint/surrender 之前 RED，没有动作或存档变更。
- capture/report/driver/classification/launch-record SHA-256 分别为 `4C377E36…DA3`、`41D07532…F4C`、`752D03EB…894`、`4ACA6BC7…047`、`CBC4A1C0…62C`。cleanup GREEN，当前轮次 R450 已结束，旧轮次 R449 已终止，CK3=0。
- 分类：mod 产品 RED=false；G2 观测能力 RED=true。T1 保持 90%，`GEN-034` unresolved。下一工作包是可复用的只读 pre-termination 诊断模式和 exact-build default truce reader 取证；不会用相同配置重复长跑，也不会放宽动作门。
- 本轮复用了通用 public MCP termination-term 与 generic war-bound 查询资产，没有新增接口/ABI/schema，因此 open_kaishek 无代码变化，仍为 `36009e7994f1db80f4e100eac7894dd357802b3a`。

## 22:11：R451 只读停战诊断 static-ready

- 为 R450 已实证的唯一缺失域 `truce` 增加显式 pre-termination probe；它复用同 PID 双查询，但禁止创建 checkpoint、提交 action 或启动 postwar。`PROBE_COMPLETE` 与 `terms_ready` 分开，不能把诊断完成冒充能力 GREEN。
- default-OFF 诊断宏只记录现有 default reader 的阶段、callback、failure enum 与 context；诊断 ON 和默认 OFF 两套 bridge 目标均编译 GREEN。诊断 DLL/injector SHA-256 为 `AB1BF87A…1705` / `B5353527…39EC`。
- 聚焦 normal/`-O` 各 `57/57`，`py_compile` 与 `git diff --check` GREEN。no-launch receipt SHA-256 `E10DA2DBF2DDD3F7B427972223F4162BA886875F1445F47C4320BD375FC6B56D`，明确写盘 `requested_live_mode=read-only-pre-termination`，source save 仍为 `89D15B8A…D4DD`。
- 本包未启动 CK3；当前轮次 R450 已结束、旧轮次 R449 已终止、CK3=0。T0 P1 仍 `6/9`、P2 视频 `LOCKED`；T1=90%、`GEN-034` unresolved。open_kaishek 私有兼容记录已由 `89ea4218151e2b340463c85d682ebd6b765cb651` 推送并与远端一致；下一步只启动一次 R451 有界只读探针。
