# 2026-09-07 日报

状态：**已收口；D1 完成，D2/D5/D6/D7 部分完成，D3/D4 未完成并继续受 T0-P1 hard gate 约束。R280 已把 recovery startup RED 收敛为 `pdx_paths` task 已运行、两个 settings lookup 均返回 NULL、parser/insert 未进入；它仍不是产品场景 RED。T0 `50%`、strict per-ID `4/361`、完整阶段 `8/11`、T1 `90%`、T2 当前 horizon `100%`，T0-P2 继续 `LOCKED`**

正式收口：2026-09-08 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-07.md`](../meetings/daily/2026-09-07.md)

起始分支/HEAD：`master` / `e06f34c3edbfce04fab5623575a716c5ee00bcac`

## 00:38 滚动增量

### 今天完成了什么

- `ed3b8caef3d2ebdac375a84db15bca21f307148d` 闭合 Workforce/Endgame 文案复审的残余 effect/按钮语义错配：
  最新绑定覆盖两个 localization shard、66 条记录，修复 14 项且 `unfixed=0`。简中文案静态人工语义复核保持完成；
  `live_render_validation_status=pending` 继续保留，未把动态 scope、换行或实机语境写成 GREEN。
- 以同一 `ed3b8ca` 冻结 R151 正式天朝二期投影：`phase2-full-release-r151-ed3b8ca`，产品 `1,031` 文件、
  `32,278,871` bytes，产品树 SHA-256 `196e557e06ffa09ef488c819168f16237f5466089ab0d8087ac3f977398e4205`，
  projection manifest SHA-256 `dae4f8e6f3b4a4d0ac903164b9abaa594ecde6cebeef67eb7dca69bf2dce2b75`。
- R151 no-launch preflight GREEN：冻结 source 共 3,194 个 tracked files，source/archive、关键 B2 产品字节、外部依赖和
  clean-source before/after 均通过；`ck3_launch_attempted=false`。preflight SHA-256 为
  `537da667172c9110f4730c2d424c86bf6b6100c32e639beddfcaabea67e2a8e8`。
- R151 实机完成 loader `303/303`、fatal `0`，loader error batch scan GREEN；随后在默认 5 速、玩家
  CharacterID `32904`、`date_raw=53148360` 暂停于新原版事件 `epidemic_events.1100`。snapshot 对应的原生事件定义有
  3 个 authored option，当前窗口实际呈现 2 个可见且可用按钮，分别绑定 `native_option_index=0`（“我会留意的。”）和
  `native_option_index=2`（“叫医生来！”）；原生 option 1 未呈现。该精确帧尚无合同，runner 按 strict unknown-interrupt
  规则 fail-closed，未选择按钮、未到 clean review boundary、
  未物化 manager seed。因此 R151 总体为 `scenario_verdict=RED / seed_verdict=RED`，不是产品 loader RED，也不是完整迁移树 GREEN。
- R151 使用 final PID `116360`、connection generation `1`，没有同产品重启；cleanup GREEN、`restart_count=0`，
  warmup PID `86040` 和 final PID 均已回收，最终 CK3 inventory 为空。runner / manager recovery / cleanup SHA-256 分别为
  `a1cedc38887c32e14e5052169bb47ea2ce18ed639b7c67ebd1a7f122d5b7de62`、
  `38f2be0a6c9eadbbbb098e31fa6d9b4b66d1726c62e6afdfc9b0071bced45c0c`、
  `fbaa96b71d450d0a927b4bd97b212cc754796ee6d28b4f9c616638333d97937d`。
- 宣传侧 `28e7dd0` 修正 09:30/09:40 长片的 post-candidate storyboard 章节权重，并把两份环境 preflight 更新为 GREEN；
  final readiness 仍为 `RED [footage_pending, publish_target_pending]`。`2fc9cb6` 只生成待填的 source-review receipt 和
  16 个 context/action 原片槽，所有 source 字段仍为 null、`PENDING/template_only`。两项均未启动 CK3/TTS/FFmpeg，
  真实素材严格保持 `0/8`，不能算 source review、候选片或最终视频完成。

### 今日承诺滚动状态

| # | 状态 | 当前证据 / 下一门槛 |
|---:|---|---|
| D1 manager seed | R159 GREEN | clean same-frame seed 已由 MCP-only gameplay 精确验收并生成 candidate；cleanup、runtime/source unchanged 与进程回收全部 GREEN |
| D2 full-tree acceptance | 进行中 / recovery startup-blocked | R248–R250 已将四个 exact interrupt GREEN，并于 `ep3_emperor_yearly.2211` 输入前 fail-closed；随后外层 turn abort 造成 session loss。R251a/b、R252、R253、R254-Z 与 R255-Z 已完成 no-bridge/bridge、C:/Z:、Default direct-continue 等有差异启动单元；都在 loader 前重复 `ck3+0x1DABD89`，均为 startup/harness RED，不是 product/scenario RED。`natural_disaster.8001` 无重放/live proof，T0-P1/full-tree 未完成 |
| D3 source registry / footage | T0-P2 `LOCKED` | 用户新增 hard gate 要求 T0-P1 完成后才解锁；宣传 prep 仅执行 preflight，source registry `0/4`、真实 footage `0/8` |
| D4 最终双片 | T0-P2 `LOCKED` | `28e7dd0` / `2fc9cb6` 与本轮 prep 均只属 static/preflight；未制作、审阅、导出或发布任何媒体，最终 MP4 `0/2` |
| D5 文案闭合 | static 完成 / live pending | `ed3b8ca` 修复 14 项、未修 0；随后续实机会话验证动态呈现 |

### 15:50 统一进度口径与计划窗口

| 项目 | 当前进度 | 尚未闭合 |
|---|---:|---|
| T0 天朝二期 | **50%** | T0-P1 的完整迁移树实机验收、AF5 terminal route 3、stage 9–11、full-tree `7` 个 action/postcondition cells + `4` 个 observation cells + restore consistency；随后才可解锁 T0-P2 |
| T1 G2 | **90%** | GEN-034 source-specific lifecycle 实机与余下 provider/decision/action 闭环；完整 settings + warm shadercache 与 CIM→Toolhelp target identity 已 static GREEN，但 r3 仍在 observer/checkpoint/surrender 前命中同一 CK3 启动崩溃，不把 preflight 当 live |
| T2 open_kaishek | **100%（当前同步 horizon）** | 当前无公共 API/ABI/架构漂移；T0/T1 后续若产生公共变更则立即重开同步 |
| 固定交付积分 | **67.5%** | 本轮只增加 harness/失败证据，没有跨越既定计分门，因此不虚增 |

“还有多少场景没测”存在两个互不替代的分母：逐 ID 的 strict bounded CK3-live 为 `4/361 = 1.11%`，尚余 **357** 个 ID 未逐一拿到该等级；选定全树路线的 distinct event-definition 采样为 `106/626 = 16.9%`，尚余 **520** 个定义未在该路线采样。二者均不表示内容未开发：Python L0、CK3-static-ready 与 central-wired 都已是 `361/361`。

计划节点按当前真实阻点给出窗口而非伪精确承诺：

- **09-07 16:00–19:00**：已冻结 R252/R253、闭合双桥 recovery/Default-desktop 静态回归，并完成 G2 的 settings + warm shadercache 启动资产门。
- **09-07 晚间至 09-08**：R254-Z 与 R255-Z 已依次排除 Z: backing volume 以及 Z:/无 bridge 的缺失 2×2 单元；继续只读收敛 Sep-03/R248 GREEN 与当前 RED 的尚未解释差异，只有出现新的确定输入才再开一次启动。
- **09-08 至 09-09（以 recovery GREEN 为前提）**：先重抵 AF5，再完成 stage 9–11 与 full-tree `7+4+restore`；若启动环境仍无可执行差异，这个窗口顺延而不虚报业务进度。
- **最早 09-09 至 09-10（以 T0-P1 全 GREEN 为前提）**：解锁 source registry `0/4→4/4`、footage `0/8→8/8`，随后双片制作、审阅、导出、哈希与发布。
- **09-11+ 预备窗口**：承接尚未解除的 startup/product RED；T0-P1 未签署前，T0-P2 始终保持 `LOCKED`。

### 失败、阻点与未知

- R151 的 `epidemic_events.1100` snapshot 证明原生定义含 3 个 authored option，但 exact live frame 实际只呈现
  `native_option_index=0/2` 两个可用按钮；随后完成的精确处理器已静态入库，但 R152 没有重现该事件，
  因而仍不具备该处理器的 live 验证证据，也不得扩成 epidemic namespace 通配合同。
- R152 从 `date_raw=53147040` 开始推进，本轮 `epidemic_events.1100` 出现次数为 `0`；因此只能确认其精确处理器已静态入库，
  不能宣称该处理器通过了 R152 实机验证。R152 先按精确合同 GREEN 消化 `tribute_mission.1002`（选择 authored option 4 /
  native index 3）与 `tribute_mission.1005`（选择 authored option 6 / native index 5），随后在 `date_raw=53150712`
  遇到尚未签约的 `tgp_movement_events.0150`。该帧 root 为玩家 `32904`，saved scopes 精确为
  `other_ruler=29646`、`monk=16783528`；原生定义含 4 个 authored option，实机只呈现
  `native_option_index=1/2/3` 三个可见且可用按钮。runner 继续按 strict unknown-interrupt fail-closed，未点击、未物化 seed。
  R152 因此仍为 `scenario_verdict=RED / seed_verdict=RED`，但 loader `303/303`、fatal `0` 与 error scan 均 GREEN。
  final PID `192464`、connection generation `1`、`restart_count=0`；cleanup GREEN，最终 CK3 inventory 为空。runner /
  manager recovery / cleanup SHA-256 分别为 `06f4cb6c97fd48579efe5f5b6a92a4f143c90b7792b1cc8c86b8bd4e28874622`、
  `36284973e6ae2f451ffc1fb39bf3afa5a0823b6e7cb05fb57b50ef2af5a4b776`、
  `4ba446528eb8cba690741b2b0fec50e415b2e327347a1faf47cbfac7add50919`。
- manager seed、完整迁移树、source registry、`8/8` 素材、双片审阅/导出/发布均未完成。

### 下一步

1. exact `ep3_emperor_yearly.2211` strict 合同与未知事件耐久 checkpoint 已静态闭合；R251–R255 的 no-bridge/bridge、C:/Z: 与 Default direct-continue 形态均不再原样重试。只有尚未解释的启动输入出现新实证后，才从同一 SHA-256 绑定 autosave fresh-recover；不得把 startup RED 冒充产品/场景 RED，也不得宣称 `natural_disaster.8001`、T0-P1 或 stage 9 `.390` 已完成。
2. 先重抵 AF5 并用实机证明 `f9580ca` 的 deterministic terminal route 3（authored/native `42/41`）确实终止循环，再继续 full-tree 其余 domain；不以 ACK 冒充状态。
3. 宣传非 CK3 准备可以并行，但素材计数保持 `0/8`，不得提前执行最终媒体链或签署 source review。
4. T0 的下一个有差异 recovery 工作包释放 CK3 串行槽后，按 `BC439E85...210B` 的 `READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE` preflight 执行 G2 `GEN-034` source-specific lifecycle live；唯一显式 mutation 为 `surrender-war-50331699`，且 mutation 前必须先落盘并同帧 hash-bind durable checkpoint。当前槽位虽为空，仍由 T0 恢复保持优先级。
5. `open_kaishek` 只在后续 T0/T1 出现公共 API 或架构变化时再次同步；无公共合同变化时不重复制造 companion delta。

## 01:08 R153 滚动增量

- `4f17d3aaa6e48cb43594095f19ed9d60becde422` 冻结 `phase2-full-release-r153-4f17d3a`：产品仍为
  `1,031` 文件、`32,278,871` bytes，产品树 SHA-256 仍为
  `196e557e06ffa09ef488c819168f16237f5466089ab0d8087ac3f977398e4205`；no-launch preflight GREEN。
- R153 manager recovery 首次 GREEN，readiness 为 `paused-clean-review-boundary`：从 `date_raw=53147040` 推进，
  `tribute_mission.1002/.1005` 两个精确 interrupt drain 均 GREEN，`unexpected_event=null`，在
  `date_raw=53154144` 暂停于 clean review boundary。本轮没有生成 `tgp_movement_events.0150`，所以其静态合同尚未获得实机触发证据。
- native date-only sentinel 在默认 5 速只提交一次 resume，精确从 `53154144` 推进一日到 `53154168`；
  `completed_daily_ticks=1`、`overshoot_days=0`、deadline reason 与 pause observed 全部 GREEN，且 watched army count 为 `0`。
  但期望的 `zga_phase2_manager_seed.1` 没有在 paused continuation 中物化，故 R153 仍为
  `scenario_verdict=RED / seed_verdict=RED`。这是 fixture activation/harness RED，不是产品 RED；不得把 manager recovery GREEN
  或 date sentinel GREEN 冒充 manager seed 已完成。
- R153 cleanup GREEN：final PID `161112`、connection generation `1`、`restart_count=0`，最终 CK3 inventory 为空。
  runner / manager recovery / cleanup / bootstrap event wait SHA-256 分别为
  `3837b422c758a41baeaa4d5f4c62116f679548db81f604c0091680bf62ebbd67`、
  `f6bae82fb3723817a606ab141a86ce5a276feebcea2a64341565ec3ab2184190`、
  `ab6d884b4c55046af75a04c737dc6d0c775a6a23f94f1a9de76ab3263ad1b132`、
  `345defc4e3312dd6d33533ca8d97833395c329d3d5b2b9f3af524ee310ceac4a`。
- `04d3c959ad8b160abeabccf9bc122eef0ae66b1c` 已提交并推送 fixture daily hidden retry 修复：从已证明执行的载入
  diagnostic edge 只 arm 一个 `days=1` 隐藏 carrier；完整业务 gate 尚未满足时只调度一个 successor，seed 启动后永久停止。
  改动共 9 个文件；fixture scripted-effect 文件现含 5 个顶层 effect，仍满足每文件 `1–10` 的强制边界；fixture、runner 与静态校验
  normal/`-O` 均 GREEN。该 acceptance-only carrier 不进入 release staging，尚待 R154 实机证明其会物化 seed。

## 01:23 R154 滚动增量

- R154 冻结 `04d3c959ad8b160abeabccf9bc122eef0ae66b1c`，产品投影为
  `phase2-full-release-r154-04d3c95`；no-launch preflight GREEN。实机使用 PID `41284`、connection generation `1`，
  loader `303/303`、fatal `0`，按默认 5 速推进。
- 在 `date_raw=53150712` 遇到未签约的原版事件 `tgp_movement_events.0050`，runner 按 strict unknown-interrupt
  fail-closed，未点击。exact frame 的 root 为玩家 `32904`；saved scopes 恰为
  `my_movement:situation_participant_group` 与 `new_elder:character 27275`，不存在 `old_elder`。原生定义含 3 个 authored option，
  实机仅呈现 `native_option_index=0/2` 两个可见且可用按钮。
- 源码审计给出的最小确定路线是 authored option 3 / native index 2：仅产生 lifestyle major XP 与 trait-based stress，
  无 RNG、无后续事件；避开 native index 0 所建立的永久师徒关系与 merit 副作用。该合同已入工作树；新增独立用途测试文件仅含
  1 个场景，normal/`-O` 均 GREEN，旧 interrupt 测试文件保持 10 场景封顶，`validate_static` / `validate_local` 均 GREEN。
  合同随后以 `8810c3f82d8af4c9a41858c47c6529be01cfc9fb` 提交并推送；R155 本轮未生成 `.0050`，故仍无 exact live 触发证据。
- R154 未到 clean review boundary，因此没有进入 R153 后新增的 daily retry carrier；不能把“carrier 未触发”写成 carrier RED。
  本轮是新的原版 exact interrupt RED，不是产品 loader RED。manager recovery / runner / cleanup SHA-256 分别为
  `c2b7bc120276d7fd955d88fd324c3d69741ce7a54d809da26ed91cd59b5768dc`、
  `c1fd83ab325ae16b771d910f88061d6b5df1db4c637873e8c45b7fabed23d884`、
  `7c1050163847716e4da1d53d8a0861514d6a48ba688f54c1a7227bd69ef160f1`。
  cleanup GREEN、`restart_count=0`，最终 CK3 inventory 为空。

## 01:38 R155 滚动增量

- R155 冻结 `8810c3f82d8af4c9a41858c47c6529be01cfc9fb`，产品投影
  `phase2-full-release-r155-8810c3f` 保持 `1,031` 文件、`32,278,871` bytes，产品树 SHA-256 仍为
  `196e557e06ffa09ef488c819168f16237f5466089ab0d8087ac3f977398e4205`；no-launch preflight GREEN，
  preflight SHA-256 为 `e9b99866ecb2df32dc3df79f4564c7da88b8e430816ef145960e383ec7fa471b`。
- 实机 loader `303/303`、fatal `0`、error scan GREEN，默认 5 速推进。在 `date_raw=53150352`、root 玩家 `32904`、
  event instance `14` 遇到 `tribute_mission.1005` 的合法非人物贡品 13-scope 形态；现有 strict contract 未覆盖该 exact shape，
  因此 fail-closed RED、未点击。实机可见/native indices 为 `(0,1,2,3,5,6)`；源码审计后仍保持最小选择
  authored option 6 / native index 5。
- R155 未到 clean review boundary，也未进入 daily hidden retry carrier。修复边界是新增这个 exact saved-scope variant；
  不全局放松依赖 human tribute / concubine 等人物关系的既有合同。manager recovery / runner / cleanup SHA-256 分别为
  `24f899863d69eb2f1e4dbb2fb7428793275adb01bd3cc0e5ad1381ec33048a9a`、
  `023b5881012c4170fd0e338d3740d5f926d65f2031434aeed33f21e8363435cd`、
  `6f8f9934f317c20fa5aa2dd62fc4b674899bf3beca977aeb3490c40c95fc6b54`。
  cleanup GREEN、final PID `192116` 已回收、`restart_count=0`，最终 CK3 inventory 为空。

## 01:40 T0 宣传工具前置

- 权威仓库为 `Z:\workspace\xar_promo_toolchain`。更新前后 HEAD 均为
  `57c42fca13ea459432c1caf76e069a1fbccf602c`；`git pull --ff-only origin main` 返回 `Already up to date`，
  且 `HEAD == origin/main`。`git describe` 为 `v0.2.1-1-g57c42fc`，CLI 报告版本 `0.2.1`，worktree clean。
- 宣传工具回归 normal/`-O` 各为 `263 passed / 2 skipped`。本项只闭合“检查版本 → 更新远端 main → 验证更新成功”三项前置，
  未启动 CK3、未采集素材、未修改项目；真实天朝二期素材仍为 `0/8`，最终 MP4 仍为 `0/2`。

## 01:54 R156 滚动增量

- R156 冻结 `eb09b272988bc695244613c118c58feed64a6920`，产品投影
  `phase2-full-release-r156-eb09b27` 仍为 `1,031` 文件，产品树 SHA-256 保持
  `196e557e06ffa09ef488c819168f16237f5466089ab0d8087ac3f977398e4205`。source ZIP / projection receipt /
  product manifest SHA-256 分别为 `e1a38542f17ba7ff5ab1ff7cdc2224f552fb1604f58be4fb3394b0a7705f7829`、
  `b41133e12b45a0491246ede9ac8f402838bd04ba1f85888bbcbcc4c4c80ced15a`、
  `e13c3f5eda18127d239c93a6a838988113da60bf2b34d76ae27939cf6242595c`。no-launch preflight 16 commands 全 GREEN，
  `ck3_launch_attempted=false`。
- 实机使用唯一 final PID `113216` / connection generation `1`，loader `303/303`、fatal `0`、error scan GREEN，
  默认 5 速且没有重启。R155 暴露的 `.1005` 非人物贡品 scope variant 已被精确合同消化；本轮
  `tribute_mission.1002`、`tribute_mission.1005` 与 `tgp_interaction_event.0015` 三次 interrupt drain 均 GREEN，
  `unexpected_event=null`。manager recovery GREEN，并在 `date_raw=53154120` 达到 `paused-clean-review-boundary`。
- daily hidden retry carrier 首次实机物化 `zga_phase2_manager_seed.1`（event instance `17`），且就在暂停的 clean boundary
  日期 `53154120`；runner 错误地只接受预计算的下一日期 `53154144`，遂按 exact-date strict gate RED、未点击事件。
  这证明 carrier 本身 live GREEN，同时把故障收窄为 fixture/runner 的 exact-date harness RED；不是产品 RED，也不能算 seed 完成。
- 最小修复新增动态 activation contract：若 `target_binding` 存在，只有 event key、同日 `date_raw`、paused、map ready、
  玩家、connection generation、正 event instance 及唯一 option 全部精确匹配时，activation target 才绑定 clean boundary，且禁止
  再 arm sentinel；仅在 `target_binding=null` 时才允许 `+24` 的一次 date-only sentinel。任何字段漂移继续 fail-closed。
  seed runner normal/`-O`、manager bootstrap normal/`-O`、`validate_static`、`validate_local` 与 generator `--check` 均 GREEN；
  修复已以 `e3f525c5a9ae073d15d94b6a4a28d405e0e0e998` 提交并推送；后续 R157 已完成 live 验证。
- R156 manager recovery / runner / cleanup SHA-256 分别为
  `2adde4ff54ec5714a4a1fdd3dd375aa1952c3c9f947d5d987617745f2195ee39`、
  `aa3e19b9f417437c33f8aa2615aa6725af5352e2d4ed29c034df0f395d620971`、
  `b39fc4a138efae9130a07205b8f81451cba7708dc065f34525e63ebd01528be6`。cleanup GREEN，final PID 已回收，
  `restart_count=0`，最终 CK3 inventory 为空。

## 02:10 R157 滚动增量

- R157 冻结 `e3f525c5a9ae073d15d94b6a4a28d405e0e0e998`。本轮 manager seed capture GREEN：默认 5 速推进，
  same-frame `zga_phase2_manager_seed.1` 在 `date_raw=53154120`、event instance `16` 被精确接受；manager/player 为
  `32904`，subject 为 `26347`。玩法观察与操作全程 MCP-only，未使用 OCR/image 判断玩法状态。
- seed candidate 已物化于 `Z:\p2m157_a\candidate`。cleanup GREEN：final PID `205844` / connection generation `1`，
  `restart_count=0`，最终 CK3 inventory 为空；`runtime_unchanged=true`、`clean_source_unchanged=true`。

## 02:25 R158 full-tree 首轮

- full-tree no-launch preflight 最终 GREEN；product install、R157 seed install 与 native readiness 均 GREEN。实机随后在
  loader/capability gate fail-closed：`b3probe-msvc4` 的默认构建未启用 promotion compensation candidate，缺少
  `game.command.query-zhongguo-promotion-compensation-postcondition-v1` /
  `zhongguo_promotion_compensation_v1_query_supported`，因此没有进入 gameplay acceptance。
- 同轮还从 seed 存档读到 acceptance-only `zga_phase2_manager_seed_survivability_modifier` missing-key；该 modifier 属于 fixture，
  不应泄漏进用于 full-tree 的保存点。健康 PID `129672` 曾按 harness RED 保留以便诊断，但 DLL 不能在进程内热升级，且 seed
  必须重制，随后已由 native-session 受管停止；最终全局 CK3 inventory 为空。
- 已完成下一轮所需的最小静态修复：fixture `.1` option 在保存前移除 survivability modifier；manager candidate 增补
  `domain_query_matrix`；CMake source contract 从已拆除的 `zg361_effects` 改绑现有用途分片
  `zg361_core_result_delivery`。fresh `b3probe-msvc6` 以 promotion candidate ON 构建，原生 CTest `94/94` GREEN。
- R158 是 harness/capability RED，不是产品 loader 或内容 RED；尚未产出 full-tree gameplay GREEN，也没有新增可计入宣传的实机素材。
  R157 的 `runner-report.json` / `manager-cycle-recovery.json` / `09_phase2_native_session_cleanup.json` SHA-256 分别为
  `95783ac1b35ae30c46a2dc52ac2a9febf77b8a623180586efd13e978f92a3b07`、
  `691285c4f62162231a784400ce160cda873d9b70e27a3173f0e509184e9cd972`、
  `30e333dd2a114280b8d56ed685470db33eff10a33d7ae9b2010860969d12a70e`。其中 `691285c4...` 明确是
  `manager-cycle-recovery.json`；candidate contract `zg361_phase2_manager_seed_contract.candidate.json` 的 SHA-256 另为
  `34d3985e70525fe750838859d8c907f5327b9539849398651daf06fc87c8c505`。
  R158 `report.json` / capability gate / retained-session receipt SHA-256 分别为
  `7c6b8da98835b9041293f95b977be46910cf4dfff049877da609d1c61a41e0ec`、
  `56794ec7c82adc2d2a15d7a3258503c17f2355a58ab0eff44f44bee5616c2862`、
  `973d82230b9cb235965dd3005203d9e91d4abe524b0a546ecc0f8d402f8c9b56`。R157 完整闭合 manager seed capture；
  R158 已开始 full-tree，但停在 gameplay 前的能力门，不提前写成完整迁移树 GREEN。

## 02:42 R159 干净 manager/promotion 上游 seed

- R159 冻结 `8a0482c`，产品投影 `phase2-full-release-r159-8a0482c` 的产品树 SHA-256 继续保持
  `196e557e06ffa09ef488c819168f16237f5466089ab0d8087ac3f977398e4205`。no-launch preflight GREEN：
  `Z:\p2m159_pre_a\preflight.json` SHA-256 为
  `33add54a5ad4c01a3c375c13debaca56cb32699c6bb6ade178f4c8607ef63ef3`。
- R159 manager seed capture 再次 GREEN，且移除了 R158 暴露的 acceptance-only modifier 存档污染；manager/player `32904`、
  subject `26347`，默认 5 速、MCP-only gameplay。`runner-report.json` SHA-256 为
  `0247fc2130e02b5e45c5099b9ff8b39acc1f62964ca0d74d87670689fdcc5e4f`；candidate contract SHA-256 为
  `bad92e689fedf460f5e194e55ae1d56be881d0d83ac091113593150182131033`，immutable save SHA-256 为
  `cb08fce64ced7f3540b68c067aeb6c81bd740a03b92f6fd416cf03c6d0a669ec`。
- cleanup GREEN：final PID `34736` / connection generation `1`，`restart_count=0`，最终 CK3 inventory 为空；cleanup receipt
  SHA-256 为 `3030ed9d87f3bf0399492673f95fcf065859419015abc46ecf837903132e2928`。
- 这份 seed 只闭合 player-manager 及 promotion 的上游输入，不具备“AI 直属经理 → 经理直属下属”的 B3 两层关系，也没有执行
  scoreboard 实机界面动作；因此不得记为 B3 或 scoreboard GREEN，更不得记为 full-tree GREEN。
- 随后的 promotion no-launch preflight 因 R159 candidate 新增 `domain_query_matrix`、而 runner 仍使用旧 root-key 白名单而 RED；
  该轮在启动 CK3 前即停止。validator 已按新 candidate schema 修复，下一步先复验 preflight，再进入 promotion live。

## 03:01 R160 promotion action-dispatch RED

- R160 冻结 `bf314be`；source ZIP SHA-256 为
  `3a31b71c537588da5ffdc6fb967236fb008211d7828235284ea6976addf694a0`。冻结副本与当前工作树的
  `tools/run_zhongguo_acceptance.py` 字节完全一致，SHA-256 均为
  `9a4a7ccd4db4b1d9bcd9a5979435c662d52c24ffb52255ba2f15bda181584457`。
- 直接从 frozen path 执行 no-launch preflight 时，因被 Git 忽略的 `Crusader Kings III` junction 不存在而 RED；该失败发生在
  CK3 启动前。改从字节一致的 current runner 执行后 preflight GREEN，未用内容差异绕过门禁。
- live artifact 位于 `Z:\p2r160promo_a`，PID `126732` / connection generation `1` 当前按健康 RED 会话保留。
  promotion progress query 真实证明 `review_now_eligible=true`，且 B1/Central/PP 全 inactive；但 review action 返回
  `accepted=false`、`status=unavailable`、`rejection_reason=action_dispatch_rejected`，同时
  `production_capability_advertised=false`。因此未进入 B1、未捕获 `zg361pp.147`，产品业务状态未被修改。
- R160 严格归类为 native action-dispatch/harness RED，不是产品状态 RED；动作 ACK 未出现，也没有被冒充为业务后置。
  后续必须修复 dispatch 并用 progress/event/postcondition 重新观测，不能因 query GREEN 提升 promotion 或 full-tree readiness。

## 03:18 R160 根因闭合与 R161 输入

- 对 retained PID `126732` 做只读内存取证后，已排除 hidden/disabled、vtable、callback 与 modal admission：动作目标的
  GUI context 是 global chain 的第三对象，而共享 dispatcher 错误地再次解引用 `third+0x3D0` 的只读 owner-lookup host，
  因 `target_context != context` 在调用原生 binder 前确定性拒绝。这是 exact-build ABI 混用，不是产品状态或文案 RED。
- 最小修复保留全部既有 target/vtable/callback/modal/shortcut admission，只把 action dispatch context 绑定到第三对象；
  双对象布局与 review-now wrapper 回归均 GREEN。修复 commit 为 `01a1251`；fresh candidate
  `Z:\b3probe-msvc7\xar_ck3_bridge.dll` SHA-256 为
  `16fa6143130b1ac8eb70b4d6b0458f07ece3615121b319b9c52b0f7d360dffe9`，promotion provider ON，
  projects/career/scoreboard production OFF。旧 PID 尚未停止，新 DLL 尚未冒充 live 验证。
- full-tree runner 已删除“无条件抛 RED”的旧终点，改为七个 gameplay action、四个 observation cell、restore consistency、
  MCP-only/no-OCR 与无未实现域的 exact-set 门禁；每项还必须持有自己的 provider-observed 产品证据，ACK 或仅填写 completed label
  均不能过门。normal/`-O` focused gate 各 `3/3`、promotion seam `3/3`、完整 promo contract normal/`-O` 均 GREEN；
  commit 为 `f3b3ecf`。这只闭合验收工具，不提升尚未取得的实机 readiness。

## 08:34 R193–R198 AF5 修复后的同会话回归

- `f9580ca0ae94076dc4732521a976af12d27c6d08` 修复了 R192 暴露的 AF5 产品循环：对需要 M300 回购条件的可见路线 1/2
  加真实可执行门槛，保留资源无关路线 3，并把 exact live 合同固定为终止路线 authored/native `42/41`。补偿生成器 `--check`、
  35 个 compensation runtime tests 与 72 个 promotion-source checkpoint runner tests 均 GREEN。新的 1,031 文件产品投影为
  `phase2-full-release-r193-f9580ca`，source/product tree SHA-256
  `3adf4297df0699f903d152561d2a9b5231d7934e15bc2255dbb88d0541161bcd`。
- R193 fresh launch 为 PID `44264`；R194–R198 均连接同一 retained PID，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，全程保持默认 5 速。所有已签约事件都用 exact current-event identity 与独立的
  event-instance-advanced 后置验收，ACK 未作为状态证据。最后观测仍为 Central active、B1/PP inactive，产品运行时 blocking diagnostics 为 `0`。
- 连续推进边界如下：R193 在四个已签约外部事件后于未知 `lifestyle_nicknames.1000` fail-closed；R194 将该事件按 authored/native
  `2/1` 消化并继续 41 次既有 drain，随后暴露 `debate_event.5110` host-win scope variant；R195 消化 debate 与两次 tribute 后暴露
  `spymaster_task.0359`；R196 消化 spymaster、adultery、debate、movement 及 AE1–AF4，随后暴露无 physician 的 `health.1001`；
  R197 对该 `health.1001` 以 authored/native `1/0` 精确 GREEN，随后停在未知 `health.3001`；R198 已将 `health.3001`
  精确 GREEN，继而在 `health.3101` strict fail-closed。后续这些 RED 均是逐个 exact interrupt 的 harness-contract 缺口，
  不覆盖 `f9580ca` 之前已确认的产品故障事实。
- 对应合同继续按用途拆分：nickname 为 `tools/zg361_phase2_promotion_manager_nickname_contracts.py`（`ce0476d`），
  spymaster 为 `tools/zg361_phase2_promotion_manager_spymaster_contracts.py`（`bfa32c2`），health 为
  `tools/zg361_phase2_promotion_manager_health_contracts.py`（`bdeec72`、`3e1a25c`）；debate host-win variant 由
  `b894723` 补入既有 debate 用途文件。补偿 effect 家族仍为 29 个用途文件、每文件 1–9 个顶层 effect，没有超过目标 `1–10`
  或硬上限 `20`。
- R193–R198 report SHA-256 依次为
  `2cac613ec788f2b61dfbd085141a68206a583c60618a818bee4147352ed295fc`、
  `bf94f48d14946dfa0d1e02b1328522d4f19a251a5875f0f4683108c6d56c2780`、
  `9da351ff2092bc561c225e52aee578a0e05fe370c8179cd3543853496c3714c2`、
  `f53b1015fa39ff01f804340ebb696a269aab53483e8ede6758dce8588dafb675`、
  `ceddae32788abe73a6326b499990025b849f0d36fdaa191d45fd09d2fb883e7e`、
  `d1c3418b53bc038255615205d6a75d082f052861c42c6dc61e6ece2a433eab26`。R198 当时保留的是可继续连接的健康暂停会话，
  后续 R199/R200 已继续复用；
  这些 artifact 只证明已列出的 interrupt/action 边界。
- 诚实 readiness：R196 只推进到 AF4，R197/R198 又被 health 事件链截住，尚未重新抵达 AF5；因此 `f9580ca` 仍是
  `static-ready-live-pending`，不能写成 AF5 修复 live GREEN，更不能写成 promotion 或 full-tree GREEN。完整迁移树、`.147` 后续证据、
  source registry、`8/8` 素材与最终双片仍待完成。
- G2 的非 CK3 并行增量为 `ed26ac35349b0557d1b817c5dda3afed1aa439f0` 与
  `ca677d3c01f7f7c56dc15a4dd5af358d53a97110`：前者把三方 assessment/candidate/six-domain terms 投影为独立
  `surrender_execution_readiness`，后者为 hash-bound file intake 新增带 aggregate session binding 的 v2 manifest；focused normal/`-O`
  回归 GREEN，但 `action.ready=false`、`postcondition.ready=false`、literal `null` 均保持，未提升 production/action readiness。
  open_kaishek 对两项增量的兼容性审计均为 `NO-CODE-CHANGE`，29 项 focused checks 与 `GREEN_STATIC` 通过；该静态同步结论
  不冒充 G2 production/action live readiness。

## 08:43 R199–R200 health 链继续推进

- `8e51a0b3770154a68a4b1066ae8187db4d4bcdb9` 为 R198 的 `health.3101` 精确帧加入保守治疗路线：原版 authored 四路中
  mystic 路线未呈现，实机 native indices 为 `0/1/3`，合同按源码选择风险较低的 safe treatment native `0`。同一提交继续
  执行用途拆分，把 health aging 4 个合同移入 `tools/zg361_phase2_promotion_manager_health_aging_contracts.py`，原 disease/treatment
  文件当时保留 7 个合同；随后加入 `health.3103` 后增至 8 个，两份始终低于每文件 10 个的目标。
- R199 继续连接 PID `44264`，将 `health.3101` safe route 精确消化为 GREEN，随后新出现的 safe-treatment success result
  `health.3103` 因尚无合同而 strict fail-closed。`5be0bc4702e810a69b678d247cc79df8a344281c` 随后将该单选确认事件补入
  disease/treatment 用途文件，没有重新合并 aging 分片。
- R200 在同一 PID 上依次将 `health.3103`、`zg361p2c.2`、`zg361.40` 与 `spymaster_task.0359` 精确消化为 GREEN；
  最新 progress observation 为 B1 active、Central inactive、PP inactive，blocking diagnostics 仍为 `0`。随后出现 `health.1101`
  的无 physician scope 变体，既有合同 shape 不匹配，runner 按 harness-contract RED 停止而未选择按钮。
- R199/R200 均为 retained reconnect：`launch_performed=false`、`restart_performed=false`、`restart_count=0`，沿用 PID `44264`
  与默认 5 速。report artifact 分别为 `Z:\p2r199promo_resume\report.json`（SHA-256
  `26f144ffc3ff1c0f007fb1811c67b22bae12c21158bc4ae268cee7c6eb06b2db`）和 `Z:\p2r200promo_resume\report.json`（SHA-256
  `29c706f3656ab1b9617093932e5f60a7b953727ef68aebda549972a38931fb91`）。
- R200 后只读 query 以 connection generation `11` 冻结 `health.1101` 当前窗口：saved scopes 恰为
  `sick_character` 与 `disease_type`，不存在 physician scope。该 query 没有提交事件选择，也没有改变产品状态；它只是下一份
  exact 合同的输入证据。
- 诚实 readiness 保持不变：R199/R200 闭合的是 health 与已列出的 interrupt 路径，不是 AF5 terminal route 的实机后置；
  当前仍未重抵 AF5，promotion/full-tree 均为 pending，未新增 per-ID GREEN。

## 09:05 R201–R204 B1 转入 Central

- R201 以 connection generation `12` 继续 PID `44264`：无 physician 的 `health.1101` 与
  `spymaster_task.0359` instance `91` 均按 exact contract GREEN；紧接着出现第二个 `.0359` instance `92`，既有单次上限
  使 runner fail-closed。generation `13` 只读 query 精确冻结 instance `92`，没有选择事件；
  `1bd045deca5bb2dd2c78fd020079130f0ccf93e0` 随后只把该 exact spymaster discovery 的上限扩为两次。
- R202 以 generation `14` 将第二个 `.0359` instance `92` 精确 GREEN，随后在 `befriend_outcome.0002` 的 failure shape
  上 fail-closed。`a4983bf5d71cfb3c680eb67198887fb0813cd00c` 补入该精确失败变体，并将 befriend 合同迁入独立用途文件；
  该文件只有 1 个合同。
- R203 以 generation `15` 连续完成 6 个 exact interrupt drains，期间产品 progress 从 B1 active 转为 Central active；
  随后 `zg361.1` 出现第七套、共 46 个 saved scopes 的合法年度总结 shape，旧合同因 exact scope-set 不一致而停止。
  `07b06eb76b4f2aba7dedec4ccb9e5a19369f73f9` 将 annual-summary 从主 entry 拆入只有 1 个合同的独立用途文件，
  同时从主文件删除 268 行；`e380ddd234c285a2d75e94f2b5e0e9c009db55d6` 再清理该分片 EOF，保持边界清晰。
- R204 以 generation `16` 将该 `zg361.1` exact frame 验收为 GREEN，随后遇到新的产品事件 `zg361.5`：Central active，
  当前窗口有 3 个可见选项，runner 因尚无精确合同而未点击。源码审计后的预定路线是最温和的 authored option 3 / native index 2；
  `.5` 与其后继 `.6` 将拆入同一个只有 2 个合同的专用文件，不继续堆入主 entry。
- R201–R204 全部复用同一 PID `44264`，默认 5 速、`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，产品运行时 blocking diagnostics 始终为 `0`。各轮 report artifact 与 SHA-256 为：
  `Z:\p2r201promo_resume\report.json` / `1cd356c6af126e305e680d4f5ca27f655a3b0167b6b643e96c9f511fec210a09`，
  `Z:\p2r202promo_resume\report.json` / `4732ea95e5c446537fc955432d5cf46704667c0e0dd76eeaa864a0bfd0fa08db`，
  `Z:\p2r203promo_resume\report.json` / `417785701a602c64f5e5e0bbd6c8614307a8d48ead7219db3d0d78e66ebfa7dc`，
  `Z:\p2r204promo_resume\report.json` / `24b5aa92827ec20fb8f723b2456d511ecdb62f40ab718b394b22e36aacbdefa6`。
- 诚实 readiness：R203/R204 证明 B1 已转入 Central 并闭合上述 interrupt，但本条线仍未重新抵达 AF5，也未完成
  Central 后续、promotion 或 full-tree；`f9580ca` 的 AF5 terminal route 仍待 live 后置验收。

## 09:17 R205 Central/PP 连续推进

- `4807587cad2b808832106bb11d7b3d2ff2abf883` 已提交并推送 `zg361.5/.6` 的 elimination 专用合同分片；该文件仅含
  2 个用途一致的合同，`zg361.5` 选择源码审计所得最温和路线 authored/native `3/2`，不再把两项合同堆回主 entry。
- R205 以 connection generation `17` 继续 retained PID `44264`。`zg361.5` 首先 GREEN；随后 26 个 Central/vanilla drains
  全部 GREEN：`zg361ch.950`、`zg361.30`、`debate_event.5110`、`zg361ch.21/.24/.25/.901/.93/.902/.101/.103/.104/.105`、
  `zg361ch.903/.107/.108/.111/.112/.113/.904/.114/.115/.116/.119/.120/.905`；最后 `zg361pp.9100` 也 GREEN。
  本轮因此共有 28 个 exact drains，每次都要求事件身份与独立后置，而不是把 ACK 当成状态。
- 28 次 GREEN 后的首个 RED 是产品事件 `zg361pp.149`：`date_raw=53186856`、event instance `128`，exact frame 含
  48 个 saved scopes（17 个 character scopes + 31 个 value scopes）以及三个可见 native options `0/1/2`。runner 未点击，
  当前正在把它补入专用 PP compensation contract，而不是放宽 namespace 或堆回主 runner。
- 最后 progress observation 仍为 Central active，PP 已转为 active；B1 inactive。产品运行时 blocking diagnostics `0`。
  本轮 `launch_performed=false`、`restart_performed=false`、`restart_count=0`，默认 5 速且继续复用 PID `44264`。
  report artifact 为 `Z:\p2r205promo_resume\report.json`，SHA-256
  `201bbb139560d08084b952b1db79cbcbe99d84bf095f517777317d2f4371929d`。
- 诚实 readiness：R205 证明了列出的 Central/PP 事件路径，但 `zg361pp.149` 尚未验收，AF5 仍未重新抵达；
  promotion/full-tree 继续 pending，未新增 per-ID GREEN。

## 09:24 R206 PP bargaining 继续推进

- `ffa7152ff9c70b4c58a801798a6f24e296c07f12` 已提交并推送 `zg361pp.149` 的 PP bargaining 专用合同；
  R206 以 connection generation `18` 将其最温和、无资源消耗的 authored option 3 / native index 2 精确验收为 GREEN。
- 下一帧立即出现 `zg361pp.150`：`date_raw=53186880`、event instance `129`，同样含 48 个 saved scopes
  （17 character + 31 value）和三个可见 native options `0/1/2`；其 alias 集与 scope types 和 `.149` 完全一致。
  runner 未点击。当前正把 `.150` 并入同一个 PP bargaining 用途文件，使该分片最终只有 2 个合同；预定继续选择
  无资源消耗的 authored/native `3/2`。
- 本轮继续 retained PID `44264`、默认 5 速，`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`、blocking diagnostics `0`。report artifact 为 `Z:\p2r206promo_resume\report.json`，SHA-256
  `5da38b3f05d92722bccb685d538f4f02d9c5fc0468f162fa4b7f33cfd5bc651d`。
- R206 只闭合 `.149`；`.150`、AF5 terminal route 与 full-tree 均仍 pending，未提升 promotion/per-ID readiness。

## R207 PP bargaining 继续推进

- `4c7476f` 已提交并推送 `.150` 的 exact PP bargaining 合同。R207 以 connection generation `19` 继续连接
  retained PID `44264`，默认 5 速，`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361pp.150` instance `129` 在 `date_raw=53186880` 的 exact frame 仍为 48 个 saved scopes
  （17 character + 31 value）和 native options `0/1/2`；R207 选择无资源消耗的 authored option 3 /
  native index 2，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361pp.151` instance `130` 在 `date_raw=53186904` 出现，exact saved-scope 集增至 53 项；
  新增 subject prompt 恰带入 2 个 character scopes 与 3 个 value scopes，native options 为 `0/1/2`。
  该 shape 尚未签约，runner 按 strict harness gate fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r207promo_resume\report.json`，SHA-256
  `BC1EB388686709C0E9FC9CB2438D77423BCDDA9E6118E5073268EE0C3515C80C`。
  R207 只闭合 `.150`；`.151`、AF5 terminal route 与 full-tree 均仍 pending，未提升 promotion/per-ID readiness。

## R208 PP bargaining 继续推进

- `7cbb3a4` 已提交并推送 `.151` 的 exact PP bargaining 合同。R208 以 connection generation `20` 继续连接
  retained PID `44264`，默认 5 速，`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361pp.151` instance `130` 在 `date_raw=53186904` 的 exact frame 含 53 个 saved scopes
  （19 character + 34 value）和 native options `0/1/2`；R208 选择 authored option 3 / native index 2，
  并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361pp.153` instance `131` 在 `date_raw=53186928` 出现，exact frame 同为 53 个 saved scopes，
  alias/type shape 与 `.151` 相同，native options 为 `0/1/2`。该事件尚未签约，runner 按 strict harness gate
  fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r208promo_resume\report.json`，SHA-256
  `F9B38744B7DF03661BE6672059664F0428B5B64C852AFC7E4506311A64455748`。
  R208 只闭合 `.151`；`.153`、AF5 terminal route 与 full-tree 均仍 pending，未提升 promotion/per-ID readiness。

## R209 PP bargaining 继续推进

- `f5204ad` 已提交并推送 `.153` 的 exact PP bargaining 合同。R209 以 connection generation `21` 继续连接
  retained PID `44264`，默认 5 速，`launch_performed=false`、`restart_performed=false`、
  `restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361pp.153` instance `131` 在 `date_raw=53186928` 的 exact frame 含 53 个 saved scopes
  （19 character + 34 value）和 native options `0/1/2`；R209 选择 authored option 3 / native index 2，
  并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361pp.155` instance `132` 在 `date_raw=53186952` 出现，exact frame 同为 53 个 saved scopes，
  alias/type shape 与 `.153` 相同，native options 为 `0/1/2`。该事件尚未签约，runner 按 strict harness gate
  fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r209promo_resume\report.json`，SHA-256
  `97E6C901AE185850BAE0D5140883F8C30073F8D8A5DBF06DE181665A4FF6CEE5`。
  R209 只闭合 `.153`；`.155`、AF5 terminal route 与 full-tree 均仍 pending，未提升 promotion/per-ID readiness。

## R210 PP public feedback 继续推进

- pushed commit `3a5b624`（`Handle PP public feedback prompt`）已加入 `.155` 的 exact contract。R210 以
  connection generation `22` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361pp.155` instance `132` 在 `date_raw=53186952` 的 exact frame 含 53 个 saved scopes
  （19 character + 34 value）和 native options `0/1/2`；R210 选择 authored option 3 / native index 2，
  并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361pp.9001` instance `133` 在 `date_raw=53186976` 出现，exact frame 含 54 个 saved scopes
  （20 character + 34 value），且只有 native option `0`。该 PP completion prompt 尚未签约，runner 按 strict
  harness gate fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r210promo_resume\report.json`，SHA-256
  `96433412CC6B9D055CEC114C21110C261EE0347E5FE06BE68144AA7044EC9DBD`。
  R210 只闭合 `.155`；PP completion prompt、AF5 terminal route 与 full-tree 均仍 pending，未提升 readiness。

## T1/T2 非 CK3 并行增量

### T1：G2 provenance-only contract refresh

- `1f5e310` 已提交并推送。11 份 G2 contract/fixture JSON 只更新 source hash 与 provenance DAG；生产行为、
  readiness、authorization 和 command 均未改变，不能把 provenance 对齐写成新的生产动作能力。
- 静态回归全绿：Raiktor normal `157/157` + `-O 157/157`，G2 normal `176/176` + `-O 176/176`，
  5-module normal `29/29` + `-O 29/29`，合计 `724/724`；targeted `57/57`，JSON parse 与 diff-check GREEN。
  本工作包未启动 CK3。
- `GEN-034` 仍需 source-specific lifecycle live。由于 T0 retained PID `44264` 正占用 CK3 串行槽，本轮按排他门禁暂缓，
  不是以离线矩阵替代 live。campaign dominance、owner-approved utility、white-peace exact terms 与 typed postcondition
  边界均保持未闭合，不虚报 readiness。

### T2：open_kaishek companion compatibility

- `open_kaishek` commit `80fb3a4f5816ea4fd2c4d2b2a81db176078831dd` 与父仓 pin commit `661226e` 均已推送。
  open metadata、Maven `156/156`、domain `8/8`、CLI/corpus/compat verifier 全部 GREEN；父仓 compatibility units
  normal `16/16` + `-O 16/16`。
- 相对审计基线 `804ef3d`，公共 `game.command`、`game.contract`、8 个字段与 5 条 invariant 均无变化；结论为
  `NO-CODE-CHANGE` companion delta，不提升 G2/open_kaishek 的 runtime 或 live readiness。
- 后续仅当 T0/T1 发生公共 API 或架构变化时再次同步；当前不追加无事实变化的兼容性改动。

## R211 PP queue completion card 闭合

- pushed commit `f857b23`（`Handle PP feedback completion`）已加入 `.9001` 的 exact contract。R211 以
  connection generation `23` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361pp.9001` instance `133` 在 `date_raw=53186976` 的 exact frame 含 54 个 saved scopes
  （20 character + 34 value），且只有 authored option 1 / native index 0；R211 选择该唯一选项，并以独立的
  `event_instance_advanced` 后置验收为 GREEN。PP queue completion card 至此闭合。
- 下一帧 `zg361cp.9050` instance `134` 在 `date_raw=53187000` 出现，exact frame 含 50 个 saved scopes
  （21 character + 29 value）和 native options `0/1/2/3`。该事件尚未签约，runner 按 strict harness gate
  fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r211promo_resume\report.json`，SHA-256
  `E048061FD3200B19E9EA3064593D0DDC794F8DF13F2750898EAC0EF95EEC478F`。
  完整 promotion/full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R211 不提升最终 readiness。

## R212 credit project portfolio mode 继续推进

- pushed commit `f990808`（`Handle credit project portfolio mode`）已加入 `.9050` 的 exact contract。R212 以
  connection generation `24` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.9050` instance `134` 在 `date_raw=53187000` 的 exact frame 含 50 个 saved scopes
  （21 character + 29 value）和 native options `0/1/2/3`；R212 选择 itemized full-tree authored option 4 /
  native index 3，并以独立的 `event_instance_advanced` 后置验收为 GREEN。该模式保留完整 27 项项目树，
  但仅闭合本事件路线，不等于完整 full-tree acceptance。
- 下一帧 `zg361cp.30` instance `135` 在 `date_raw=53187024` 出现，exact frame 同为 50 个 saved scopes
  （21 character + 29 value），native options 为 `0/1/2`。该事件尚未签约，runner 按 strict harness gate
  fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r212promo_resume\report.json`，SHA-256
  `1207C4F0D4A9579904C6F510C76267817423320285AB7966A5820DB40D14FB05`。
  完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R212 不提升最终 readiness。

## T1：G2 source-specific lifecycle no-launch preflight

- T1 commit `bdf5324` 对应的 no-launch preflight 已达到
  `READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE`。artifact 为
  `Z:\ck3_mod_rewrite_process_assets\zg361\g2-source-live-adapter-static-20260907\preflight-master-5ffbc1d-r4.json`，
  `7,067` bytes，SHA-256 `6BB2090ADEB83D99906F5D88682CF602A69BD48E14EAA12F98C4F2592EA2CCC6`；
  manifest SHA-256 为 `D888FA2DF6839C5A424D9947EE7BCE746ABF930E45B2384169DD23488ABB5FB0`。
- focused preflight normal `20/20` + `-O 20/20` GREEN。CK3 before/after inventory 完全相同，preflight 未启动 CK3，
  也未连接、暂停、恢复或以任何方式操作 T0 retained PID `44264`。
- typed surrender 与 private receipt 已具备；但没有任何额外离线实现能解除 campaign dominance 或 owner-approved utility
  缺口。下一项有效证据只能是 T0 释放独占 CK3 槽后的 source-specific lifecycle live，且唯一显式 mutation 固定为
  `surrender-war-50331699`；在此之前不提升 `GEN-034` readiness。
- 父仓推进至 `e4398bf` 后再次执行 committed `--verify-only` preflight，状态仍为
  `READY_TO_RUN_G2_SOURCE_SPECIFIC_LIFECYCLE`，13 项依赖全部匹配，CK3 inventory 前后均为空，未执行 surrender。
  新 artifact 为 `Z:\ck3_mod_rewrite_process_assets\zg361\g2-source-live-adapter-static-20260907\preflight-master-e4398bf-r1.json`，
  `5,111` bytes，SHA-256 `1347C3C426DA8F507561FB777FE1D916E891A2079CFB65A7C48E89EC0C2F2198`；
  `1f5e310..e4398bf` 无 G2 public code、native API/ABI 或 research implementation 漂移，故无需再次修改 manifest 或同步 open_kaishek。

## R213 credit project resource race 继续推进

- pushed commit `a5ba41f`（`Handle credit project resource race`）已加入 `.30` 的 exact contract。R213 以
  connection generation `25` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.30` instance `135` 在 `date_raw=53187024` 的 exact frame 含 50 个 saved scopes
  （21 character + 29 value）和 native options `0/1/2`；R213 选择 subject-led reserve `40h` 的 authored option 1 /
  native index 0，保留 itemized 项目树，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.26` instance `136` 在 `date_raw=53187048` 出现，exact frame 同为 50 个 saved scopes
  （21 character + 29 value），native options 为 `0/1/2`。该事件尚未签约，runner 按 strict harness gate
  fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r213promo_resume\report.json`，SHA-256
  `8A9AA71F378AD4A2BF8A4F4C9DA8755A28A7A3744836B38ECE6DF3FD172B66A5`。
  完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R213 不提升最终 readiness。

## R214 credit project visibility ledger 继续推进

- pushed commit `ca2bed3`（`Handle credit project visibility ledger`）已加入 `.26` 的 exact contract。R214 以
  connection generation `26` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.26` instance `136` 在 `date_raw=53187048` 的 exact frame 含 50 个 saved scopes
  （21 character + 29 value）和 native options `0/1/2`；R214 选择 authored option 1 / native index 0，
  并以独立的 `event_instance_advanced` 后置验收为 GREEN。itemized 项目树当前仍有 25 张可见卡。
- 下一帧 `zg361cp.27` instance `137` 在 `date_raw=53187072` 出现，alias/type shape 与 `.26` 相同，
  exact frame 仍为 50 个 saved scopes（21 character + 29 value），native options 为 `0/1/2`。该事件尚未签约，
  runner 按 strict harness gate fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r214promo_resume\report.json`，SHA-256
  `E3AC5A9855D83C2058C1BDE2254FFBFFC57B94EE2E17F1F35504E7EA249A234B`。
  E→I→J→R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R214 不提升最终 readiness。

## R215 credit project contribution shares 继续推进

- pushed commit `6e85772`（`Handle credit project contribution shares`）已加入 `.27` 的 exact contract。R215 以
  connection generation `27` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.27` instance `137` 在 `date_raw=53187072` 的 exact frame 含 50 个 saved scopes
  （21 character + 29 value）和 native options `0/1/2`；R215 选择 authored option 1 / native index 0，
  contribution shares 为 `7000/2000/1000`、总计 `10000`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- hidden `.9231` 没有暴露 UI；下一可见帧 `zg361cp.31` instance `138` 在 `date_raw=53187096` 出现，
  exact frame 仍为 50 个 saved scopes（21 character + 29 value），alias/type shape 与 `.27` 相同，native options 为
  `0/1/2`。该事件尚未签约，runner 按 strict harness gate fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r215promo_resume\report.json`，SHA-256
  `8CFCF8EB5702CD63B886C907F017D7C2C00E155BDCE6B8A76CDFEC5BECCBD138`。
  完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R215 不提升最终 readiness。

## R216 credit project capacity grant 继续推进

- pushed commit `a672dc5`（`Handle credit project capacity grant`）已加入 `.31` 的 exact contract。R216 以
  connection generation `28` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.31` instance `138` 在 `date_raw=53187096` 的 exact frame 含 50 个 saved scopes
  （21 character + 29 value）和 native options `0/1/2`；R216 选择 authored option 1 / native index 0，
  取得 `grant=20 / spend=0 / balance=20 / visibility=5`，E 从 `2` 推进到 `3`，并以独立的
  `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.28` instance `139` 在 `date_raw=53187120` 出现，exact frame 仍为 50 个 saved scopes
  （21 character + 29 value），alias/type shape 与 `.31` 相同，native options 为 `0/1/2`。该事件尚未签约，
  runner 按 strict harness gate fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r216promo_resume\report.json`，SHA-256
  `17AA7DCFCAECEDDA857A0697706E060DE20F3BAB9B8324315B770B1F320BF232`。
  完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R216 不提升最终 readiness。

## R217 credit project attribution audit 继续推进

- pushed commit `eff54a2`（`Handle credit project attribution audit`）已加入 `.28` 的 exact contract。R217 以
  connection generation `29` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.28` instance `139` 在 `date_raw=53187120` 的 exact frame 含 50 个 saved scopes
  （21 character + 29 value）和 native options `0/1/2`；R217 选择 authored option 2 / native index 1。
  审计回拨后 contribution shares 仍为 `7000/2000/1000`，E 从 `3` 推进到 `4`，并以独立的
  `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.29` instance `140` 在 `date_raw=53187144` 出现，exact frame 仍为 50 个 saved scopes
  （21 character + 29 value），alias/type shape 与 `.28` 相同，native options 为 `0/1/2`。该事件尚未签约，
  runner 按 strict harness gate fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r217promo_resume\report.json`，SHA-256
  `C830D145A332639AFA2A23041AEB19928D49A21DB1BD2722B1B78851E9A9B08D`。
  完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R217 不提升最终 readiness。

## R218 credit project value audit：E domain live 完成

- pushed commit `42afe5f`（`Handle credit project value audit`）已加入 `.29` 的 exact contract。R218 以
  connection generation `30` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.29` instance `140` 在 `date_raw=53187144` 的 exact frame 含 50 个 saved scopes
  （21 character + 29 value）和 native options `0/1/2`；R218 选择 authored option 1 / native index 0，
  取得 `gain=10 / cost=0`，E 从 `4` 推进到 `5` 且 `active=0`，并以独立的 `event_instance_advanced`
  后置验收为 GREEN。E domain 至此完成本条 live 路径。
- hidden `.9001/.9261` 均未暴露 UI；下一可见帧进入 I-domain：`zg361cp.61` instance `141` 在
  `date_raw=53187168` 出现，exact frame 含 58 个 saved scopes（27 character + 31 value），native options 为
  `0/1/2`。该事件尚未签约，runner 按 strict harness gate fail-closed 且未点击；这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r218promo_resume\report.json`，SHA-256
  `8B17B5C967690DD066783150EB4C5E110BD0BB0D90839A1081E227ADBC3F86A9`。
  E domain live 完成不等于完整 full-tree；I/J/R、AF5 terminal route 3、source registry 与最终视频仍 pending。

## R219 credit project reporting policy：I-domain 首卡 GREEN

- pushed commit `2987950`（`Handle credit project reporting policy`）已加入 `.61` 的 exact contract。R219 以
  connection generation `31` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.61` instance `141` 在 `date_raw=53187168` 的 exact frame 含 58 个 saved scopes
  （27 character + 31 value）和 native options `0/1/2`；R219 选择 authored option 1 / native index 0，
  得到 reporting policy `1/1h`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
- 下一帧 `zg361cp.54` instance `142` 在 `date_raw=53187192` 出现，保持相同的 exact 58-scope split
  （27 character + 31 value）。policy `1` 正确隐藏 native index `1`，实机仅渲染 native indices `0/2`；
  该事件尚未签约，runner 按 strict harness gate fail-closed 且未点击。这是条件渲染下的新精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r219promo_resume\report.json`，SHA-256
  `5C22FAB4F4472B704BCAFA33B500424B66396AAEAEE4F782C2B343B220F83B0C`。
  I-domain 其余路径、J/R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R219 不提升最终 readiness。

## R220 credit project report creation：I `1→2`

- pushed commit `2cf9b2e`（`Handle credit project report creation`）已加入 `.54` 的 exact contract。R220 以
  connection generation `32` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.54` instance `142` 在 `date_raw=53187192` 的 exact frame 含 58 个 saved scopes
  （27 character + 31 value）；完整 option snapshot 为 `3`，policy 条件下仅有 2 个可见按钮，对应 native indices
  `0,2`。R220 选择 authored-visible option 1 / native index 0，创建 `1h` report、消耗 capacity `1`，I 从 `1`
  推进到 `2`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。hidden `.9256` 未暴露 UI。
- 下一帧 `zg361cp.56` instance `143` 在 `date_raw=53187216` 出现，保持相同的 exact 58-scope split
  （27 character + 31 value），native options `0/1/2`、snapshot `3`；该事件尚未签约，runner 按 strict harness gate
  fail-closed 且未点击。这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r220promo_resume\report.json`，SHA-256
  `1DF19E9E4446C3B5650C2834A011C29A14D157E3900607C7F0FBD26B0F12AE41`。
  I-domain 其余路径、J/R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R220 不提升最终 readiness。

## R221 credit project forwarding：I `2→3`

- pushed commit `83c5c52`（`Handle credit project forwarding`）已加入 `.56` 的 exact contract。R221 以
  connection generation `33` 继续连接 retained PID `44264`，默认 5 速，`launch_performed=false`、
  `restart_performed=false`、`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.56` instance `143` 在 `date_raw=53187216` 的 exact frame 含 58 个 saved scopes
  （27 character + 31 value）和 native options `0/1/2`；R221 选择 authored option 1 / native index 0，
  `forwarded=1`、股比保持不变、I 从 `2` 推进到 `3`，并以独立的 `event_instance_advanced` 后置验收为 GREEN。
  hidden `.9257` 未暴露 UI。
- 下一帧 `zg361cp.57` instance `144` 在 `date_raw=53187240` 出现，保持相同的 exact 58-scope split
  （27 character + 31 value），native options `0/1/2`、snapshot `3`；该事件尚未签约，runner 按 strict harness gate
  fail-closed 且未点击。这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r221promo_resume\report.json`，SHA-256
  `5D0DF4B38BEA4EF3290FFBB4BC45A791E30F17FD4B50F0369BAB4BC5D9085337`。
  I-domain 其余路径、J/R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R221 不提升最终 readiness。

## R222 credit project freeze signature

- pushed commit `7037bcf` 已加入 `.57` 的 exact contract。R222 以 connection generation `34` 继续连接 retained
  PID `44264`，默认 5 速，`launch_performed=false`、`restart_performed=false`、`restart_count=0`，产品运行时
  blocking diagnostics `0`。
- `zg361cp.57` instance `144` 在 `date_raw=53187240` 的 exact frame 含 58 个 saved scopes
  （27 character + 31 value）；R222 选择 authored option 1 / native index 0，以 author=subject 冻结
  version/signature `1`，该路径 GREEN。hidden `.9258` 未暴露 UI。
- 下一帧 `zg361cp.58` instance `145` 在 `date_raw=53187264` 出现，保持相同的 exact 58-scope split，
  native options `0/1/2`、snapshot `3`；R222 未点击并停在 strict harness gate。这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r222promo_resume\report.json`，SHA-256
  `48466E88710E5EBA1ED72E7676673491A1346EAD4136C3422F2552B6164D147F`。该 run 独立收口，不把下一帧计作 GREEN。

## R223 credit project direct-root delivery

- pushed commit `1a85ee7` 已加入 `.58` 的 exact contract。R223 以 connection generation `35` 继续连接 retained
  PID `44264`，默认 5 速，`launch_performed=false`、`restart_performed=false`、`restart_count=0`，产品运行时
  blocking diagnostics `0`。
- `zg361cp.58` instance `145` 在 `date_raw=53187264` 的 exact frame 含 58 个 saved scopes
  （27 character + 31 value）；R223 选择 authored option 1 / native index 0，证明 recipient count `1` / direct root
  路径 GREEN。
- 下一帧 `zg361cp.59` instance `146` 在 `date_raw=53187288` 出现，保持相同的 exact 58-scope split，
  native options `0/1/2`、snapshot `3`；该事件尚未签约，runner 按 strict harness gate fail-closed 且未点击。
  这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r223promo_resume\report.json`，SHA-256
  `BAA3B4E09AA302FE19F0774B5671953CC13F059E4DA0494F2AAA7CB557742E15`。
  I-domain 余下路径、J/R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R223 不提升最终 readiness。

## R224 credit project integrity closure：I `3→4`

- pushed commit `4216f8c` 已加入 `.59` 的 exact contract。R224 以 connection generation `36` 继续连接 retained
  PID `44264`，默认 5 速，`launch_performed=false`、`restart_performed=false`、`restart_count=0`，产品运行时
  blocking diagnostics `0`。
- `zg361cp.59` instance `146` 在 `date_raw=53187288` 的 exact frame 含 58 个 saved scopes
  （27 character + 31 value）；R224 选择 authored option 1 / native index 0，记录 remaining loss `5`、integrity
  `+1`，I 从 `3` 推进到 `4`，该路径 GREEN。
- 下一帧 `zg361cp.55` instance `147` 在 `date_raw=53187312` 出现，保持相同的 exact 58-scope split，
  native options `0/1/2`；R224 未点击并停在 strict harness gate。这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r224promo_resume\report.json`，SHA-256
  `FC349FEE10CA361117779EB63E760C939E39B8AE51D2674CE467B208D9B08679`。该 run 独立收口，不把下一帧计作 GREEN。

## R225 credit project attention accounting

- pushed commit `a27d9b6` 已加入 `.55` 的 exact contract。R225 以 connection generation `37` 继续连接 retained
  PID `44264`，`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.55` instance `147` 在 `date_raw=53187312` 的 exact frame 含 58 个 saved scopes
  （27 character + 31 value）；R225 选择 authored option 1 / native index 0，attention free `2→1`、used `0→1`、
  read `1`、visibility `+5`，该路径 GREEN。
- 下一帧 `zg361cp.60` instance `148` 在 `date_raw=53187336` 出现，保持相同的 exact 58-scope split，
  native options `0/1/2`；该事件尚未签约，runner 按 strict harness gate fail-closed 且未点击。这是 I-domain
  最后一张卡的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r225promo_resume\report.json`，SHA-256
  `06D83C44967DBE627660330D28764002A882B5C11B669ADC09B0809DF02D038B`。
  I-domain 最后一张 `.60`、J/R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R225 不提升最终 readiness。

## R226 credit project I completion：I domain live 完成

- pushed commit `50535bc` 已加入 `.60` 的 exact contract。R226 以 connection generation `38` 继续连接 retained
  PID `44264`，默认 5 速，`launch_performed=false`、`restart_performed=false`、`restart_count=0`，产品运行时
  blocking diagnostics `0`。
- `zg361cp.60` instance `148` 在 `date_raw=53187336` 的 exact frame 含 58 个 saved scopes
  （27 character + 31 value）；R226 选择 authored option 1 / native index 0，保留 subject `idea_owner` / `theft_upheld=1`，
  I 从 `4` 推进到 `5` 且 `active=0`，该路径 GREEN。hidden `.9002/.9263` 均未暴露 UI，I domain live 至此完成。
- 下一可见帧进入 J domain：`zg361cp.63` instance `149` 在 `date_raw=53187360` 出现，exact frame 含 66 个
  saved scopes（33 character + 33 value），native options `0/1/2`；R226 未点击并停在 strict harness gate。
  这是新的精确合同边界，不是产品 RED。
- report artifact 为 `Z:\p2r226promo_resume\report.json`，SHA-256
  `CF9355E25263FE838C91B700E9DB56C9C2A14263BFE71E367B1025E7523A5896`。该 run 独立收口，不把下一帧计作 GREEN。

## R227 credit project J weights：J `1→2`

- pushed commit `54c698a` 已加入 `.63` 的 exact contract。R227 以 connection generation `39` 继续连接同一 retained
  PID `44264`，`restart_count=0`，产品运行时 blocking diagnostics `0`。
- `zg361cp.63` instance `149` 在 `date_raw=53187360` 的 exact frame 含 66 个 saved scopes
  （33 character + 33 value）；R227 选择 authored option 1 / native index 0，weights `70/30`、total `100`，
  J 从 `1` 推进到 `2`，该路径 GREEN。hidden `.9262` 未暴露 UI。
- 下一帧 `zg361cp.62` instance `150` 在 `date_raw=53187384` 出现，保持相同的 exact 66-scope split，
  native options `0/1/2`；该事件尚未签约，runner 按 strict harness gate fail-closed 且未点击。这是新的精确合同边界，
  不是产品 RED。
- report artifact 为 `Z:\p2r227promo_resume\report.json`，SHA-256
  `749798B33F765F6AA226C11EC9CEB3D190D970540E020F3E39DC8EEB54B0D310`。
  I domain live 已完成，但 J-domain 余下路径、R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R227 不提升最终 readiness。

## R228 credit project J continuation：`.62` GREEN

- `.62` 静态 exact contract 位于 commit `a7f7bf0`。R228 以 connection generation `40` 继续连接 retained
  PID `44264`，默认 5 速，`launch_performed=false`、`restart_performed=false`、`restart_count=0`，产品运行时
  blocking diagnostics `0`。
- `zg361cp.62` instance `150` 在 `date_raw=53187384` 的 exact frame 含 66 个 saved scopes
  （33 character + 33 value）；R228 选择 authored option 1 / native index 0，并以独立的 `event_instance_advanced`
  后置验收为 GREEN。
- 下一帧 `zg361cp.65` instance `151` 在 `date_raw=53187408` 出现，保持相同的 exact 66-scope split，
  native options `0/1/2`、snapshot `3`；R228 未点击并按 strict harness gate fail-closed。`.65` exact contract
  commit `6075e2e` 已 push，但本轮没有执行它，因此仍是 live-pending，不是产品 RED，也不是 `.65` GREEN。
- report artifact `Z:\p2r228promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `4129AD0A359F29CEB05229358D72DB5009D79A88AFE0C4BC887D819B81B82281`。
  J-domain 余下路径、R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R228 不提升最终 readiness。

## R229 credit project J continuation：`.65` target GREEN / sentinel RED

- `.65` exact contract 位于 commit `6075e2e`。R229 以 connection generation `41` 继续连接 retained PID
  `44264`，`launch_performed=false`、`restart_performed=false`、`restart_count=0`；runtime diagnostics GREEN，
  blocking diagnostics `0`。
- `zg361cp.65` instance `151` 在 `date_raw=53187408` 的 exact frame 含 66 个 saved scopes
  （33 character + 33 value）；R229 选择 authored option 1 / native index 0，并以独立的 `event_instance_advanced`
  后置验收为 GREEN。本轮目标事件与合同 live 验收已经完成。
- 下一帧 `zg361cp.64` instance `152` 在 `date_raw=53187432` 出现，保持相同的 exact 66-scope split，
  native options `0/1/2`、snapshot `3`；R229 当时 `.64` 静态包尚在进行，runner 按 strict sentinel fail-closed 且未点击。
  因此 top-level RED 仅表示当时的 next-event 合同缺口，不否定 `.65` 目标 GREEN，也不是产品 runtime RED。
- `.64` route B authored/native `2/1` exact contract 随后已由 commit `f1518e4` push；focused normal/`-O` 各
  `12/12`、aggregate normal/`-O` 各 `73/73`、real exact `79`、`validate_static` 与 diff-check 均 GREEN。
  这些只闭合静态准备，R229 没有补做点击，`.64` 仍是 live-pending。
- report artifact `Z:\p2r229promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `4888ADAAACA34C6E0910A159614E9F3373BF5DB3E0193AA44BA1269C102A0E1F`。
  J-domain 余下路径、R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R229 不提升最终 readiness。

## R230 credit project J route B：`.64` target GREEN / sentinel RED

- `.64` route B exact contract 位于 commit `f1518e4`。R230 以 connection generation `42` 继续连接 retained PID
  `44264`，`launch_performed=false`、`restart_performed=false`、`restart_count=0`；runtime diagnostics GREEN，
  blocking diagnostics `0`。
- `zg361cp.64` instance `152` 在 `date_raw=53187432` 的 exact frame 含 66 个 saved scopes
  （33 character + 33 value）；R230 选择 authored option 2 / native index 1，并以独立的 `event_instance_advanced`
  后置验收为 GREEN。
- 下一帧 `zg361cp.66` instance `153` 在 `date_raw=53187456` 出现，保持相同的 exact 66-scope split，
  native options `0/1/2`、snapshot `3`；`.66` contract 仍在施工，runner 按 strict sentinel fail-closed 且未点击。
  top-level RED 仅表示 next-event unknown，不否定 `.64` 目标 GREEN，也不是产品 runtime RED。
- report artifact `Z:\p2r230promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `132FC018A60A9E22A019DF5D0E672D6F498F31D36FDE8153BA684851CC8607EA`。
  J-domain 余下路径、R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R230 不提升最终 readiness。

## R231 credit project J capacity preserve：`.66` target GREEN / secret sentinel RED

- `.66` exact contract 位于 commit `833fd50`；focused normal/`-O` 各 `15/15`、aggregate normal/`-O` 各
  `73/73`、real exact `79`、`validate_static` 与 diff-check 均 GREEN。R231 以 connection generation `43` 继续连接
  retained PID `44264`，`launch_performed=false`、`restart_performed=false`、`restart_count=0`；runtime diagnostics
  GREEN，blocking diagnostics `0`。
- `zg361cp.66` instance `153` 在 `date_raw=53187456` 的 exact frame 含 66 个 saved scopes；R231 选择
  authored option 2 / native index 1，保留 capacity 供后续 R-domain `.132` 使用，该路径 GREEN。
- 下一帧为原版 `secrets.0122` instance `155`，在 `date_raw=53187480` 的 exact frame 含 13 个 saved scopes
  （9 character + 1 secret + 2 value + 1 flag），完整 snapshot `7`，实机仅渲染 native indices `1,2`。
  R231 未点击并停在 strict sentinel；top-level RED 仅来自 next-event 合同缺口，不否定 `.66` target GREEN。
- `secrets.0122` exact contract commit `fb2a7e9` 随后确定 Forgive 为 authored option 3 / native index 2，
  不是“rendered option 2”；focused normal/`-O` 各 `3/3`、aggregate normal/`-O` 各 `73/73`、exact `26`、
  `validate_static` 与 diff-check 均 GREEN，但这些静态证据不回填 R231 点击。
- report `Z:\p2r231promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `C14816EA716EC5AA9BDC3E5598E153CDCFBA7601335390457A6DDC0F1E73C793`。

## R232 native secret interrupt：Forgive target GREEN / `.67` sentinel RED

- R232 以 connection generation `44` 继续连接同一 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- 原版 `secrets.0122` instance `155` 在 `date_raw=53187480` 按 `fb2a7e9` 选择 Forgive authored option 3 /
  native index 2，该目标路径 GREEN。这里的 `3/2` 是 authored/native 映射，不以仅两个 rendered button 的序号替代。
- 下一帧回到先前已排队的 `zg361cp.67` instance `154`，同在 `date_raw=53187480`；exact frame 含 66 个
  saved scopes（33 character + 33 value），native options `0/1/2`、snapshot `3`。`.67` contract 仍在施工，runner
  strict fail-closed 且未点击；top-level RED 仅来自 next-event sentinel，不否定 secret target GREEN。
- report `Z:\p2r232promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `73D94802BD063FECBF6E72738E7ED5500640722CF03CF8B7E8020F69A5EDBCB3`。
  J-domain 余下路径、R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R232 不提升最终 readiness。

## R233 credit project J continuation：`.67` target GREEN / `.68` sentinel RED

- `.67` exact contract 位于 commit `390aad0`；focused normal/`-O` 各 `18/18`、aggregate normal/`-O` 各
  `73/73`、real exact `79`、`validate_static` 与 diff-check 均 GREEN。R233 以 connection generation `45` 继续连接
  retained PID `44264`；runtime diagnostics GREEN，blocking diagnostics `0`。
- `zg361cp.67` instance `154` 在 `date_raw=53187480` 的 exact frame 含 66 个 saved scopes；R233 选择
  authored option 1 / native index 0，该目标路径 GREEN。
- 下一帧 `zg361cp.68` instance `156` 在 `date_raw=53187504` 出现，exact frame 含 66 个 saved scopes，
  native options `0/1/2`、snapshot `3`；R233 strict no-click。top-level RED 仅来自 next-event sentinel，
  不否定 `.67` target GREEN，也不是 runtime RED。
- report `Z:\p2r233promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `191280BED635FECE431C42EB59B87B3F7D5335206035E30F929D286B13BC5683`。

## R234 credit project J completion：J domain live complete

- `.68` exact contract 位于 commit `d47c077`；focused normal/`-O` 各 `21/21`、aggregate normal/`-O` 各
  `73/73`、real exact `79`、`validate_static` 与 diff-check 均 GREEN。R234 以 connection generation `46` 继续连接
  同一 retained PID `44264`；runtime diagnostics GREEN，blocking diagnostics `0`。
- `zg361cp.68` instance `156` 在 `date_raw=53187504` 按 authored option 2 / native index 1 选择 preserve history /
  no PIP 路径，该目标 GREEN；J state `4→5` 且 `active=0`。hidden `.9003/.9331` 未暴露 UI，J domain live complete。
- 下一帧进入 R domain：`zg361cp.131` instance `157` 在 `date_raw=53187528` 出现，exact frame 含 74 个
  saved scopes（39 character + 35 value），native options `0/1/2`、snapshot `3`；`.131` contract 仍在施工，
  runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 `.68` target GREEN。
- report `Z:\p2r234promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `7DB45A511366461CAF854F374AADC26526D084FA4CC0DF839BB1F98F7DBC061F`。
  J domain live complete 不等于 full-tree；R、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending，R234 不提升最终 readiness。

## R235 credit project R entry：`.131` target GREEN / `.129` sentinel RED

- `.131` exact contract 位于 commit `ec5ab47`；focused normal/`-O` 各 `3/3`、aggregate normal/`-O` 各
  `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。R235 以 connection generation `47` 继续连接
  retained PID `44264`；runtime diagnostics GREEN，blocking diagnostics `0`。
- R-domain `zg361cp.131` instance `157` 在 `date_raw=53187528` 的 exact frame 含 74 个 saved scopes
  （39 character + 35 value）；R235 选择 authored option 1 / native index 0，该目标路径 GREEN。
- 下一帧 `zg361cp.129` instance `158` 在 `date_raw=53187552` 出现，保持相同的 exact 74-scope shape，
  native options `0/1/2`、snapshot `3`；R235 strict no-click。top-level RED 仅来自 next-event sentinel，
  不否定 `.131` target GREEN，也不是 runtime RED。
- report `Z:\p2r235promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `E4F011558A01982DE3F73B2EE45CB943688A4874453BC7B106B859ECE266D3CC`。

## R236 credit project R queue：`.129` target GREEN / `.134` sentinel RED

- `.129` exact contract 位于 commit `69e8ce5`；focused normal/`-O` 各 `6/6`、aggregate normal/`-O` 各
  `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。R236 以 connection generation `48` 继续连接
  同一 retained PID `44264`；runtime diagnostics GREEN，blocking diagnostics `0`。
- `zg361cp.129` instance `158` 在 `date_raw=53187552` 选择 authored option 1 / native index 0，queue 路径 GREEN；
  awarded `0`，promotion slot 保持 `free=1 / used=0`。
- 下一帧 `zg361cp.134` instance `159` 在 `date_raw=53187576` 出现，保持相同的 exact 74-scope shape，
  native options `0/1/2`、snapshot `3`；`.134` contract 仍在施工，runner strict no-click。top-level RED 仅来自
  next-event sentinel，不否定 `.129` target GREEN。
- report `Z:\p2r236promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `379B34E58DDDC08F6E194F28C19AEE9DF8F9BC23C0EBF80DA88E10E809E519ED`。
  R-domain 余下路径、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R236 不提升最终 readiness。

## R237 credit project R continuation：`.134` target GREEN / `.130` sentinel RED

- `.134` exact contract 位于 commit `d8ef85b`；focused normal/`-O` 各 `9/9`、aggregate normal/`-O` 各
  `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。R237 以 connection generation `49` 继续连接
  retained PID `44264`；runtime diagnostics GREEN，blocking diagnostics `0`。
- `zg361cp.134` instance `159` 在 `date_raw=53187576` 的 exact frame 含 74 个 saved scopes；R237 选择
  authored option 1 / native index 0，该目标路径 GREEN。
- 下一帧 `zg361cp.130` instance `160` 在 `date_raw=53187600` 出现，保持相同的 exact 74-scope shape，
  native options `0/1/2`、snapshot `3`；R237 strict no-click。top-level RED 仅来自 next-event sentinel，
  不否定 `.134` target GREEN，也不是 runtime RED。
- report `Z:\p2r237promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `4BE5F047ECE822DDA2F78D094D91B7B2A51612B25801F3E2B07EED76400003FA`。

## R238 credit project R outcome：`.130` target GREEN / `.132` sentinel RED

- `.130` exact contract 位于 commit `84fe28a`；focused normal/`-O` 各 `12/12`、aggregate normal/`-O` 各
  `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。R238 以 connection generation `50` 继续连接
  同一 retained PID `44264`；runtime diagnostics GREEN，blocking diagnostics `0`。
- `zg361cp.130` instance `160` 在 `date_raw=53187600` 选择 authored option 1 / native index 0，目标路径 GREEN；
  `wrong_role=0 / outcome=3`，R 从 `3` 推进到 `4`。
- 下一帧 `zg361cp.132` instance `161` 在 `date_raw=53187624` 出现，保持相同的 exact 74-scope shape，
  native options `0/1/2`、snapshot `3`；`.132` contract 仍在施工，runner strict no-click。top-level RED 仅来自
  next-event sentinel，不否定 `.130` target GREEN。
- report `Z:\p2r238promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `7534B22FDB901715A944C353EC8EA7E7F33708D7A60FF9AE32B6BE81BE223AF4`。
  R-domain 余下路径、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R238 不提升最终 readiness。

## R239 credit project R settlement：`.132` target GREEN / `.133` sentinel RED

- `.132` exact contract 位于 commit `a90c12a`；focused postmortem normal/`-O` 各 `3/3`、R regression
  normal/`-O` 各 `15/15`、aggregate normal/`-O` 各 `73/73`、real exact `87`、`validate_static` 与 diff-check
  均 GREEN。R239 以 connection generation `51` 继续连接 retained PID `44264`；runtime diagnostics GREEN，
  blocking diagnostics `0`。
- `zg361cp.132` instance `161` 在 `date_raw=53187624` 的 exact frame 含 74 个 saved scopes；R239 选择
  authored option 1 / native index 0，该目标路径 GREEN。实机结算 release `16`，结算后
  `available=76 / rem=0 / reserved=24 / spent=24`，project closed。
- 下一帧 `zg361cp.133` instance `162` 在 `date_raw=53187648` 出现，保持相同的 exact 74-scope shape，
  native options `0/1/2`、snapshot `3`；R239 strict no-click。top-level RED 仅来自 next-event sentinel，
  不否定 `.132` target GREEN，也不是 runtime RED。
- report `Z:\p2r239promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `D04663E90BD693049A50FEEFBC25756E97D01374DAAB2D6F2F04BF14291F1B19`。

## R240 credit project R completion：R domain live complete

- `.133` exact contract 位于 commit `584cbe6`；focused normal/`-O` 各 `5/5`、R regression normal/`-O` 各
  `17/17`、aggregate normal/`-O` 各 `73/73`、real exact `87`、`validate_static` 与 diff-check 均 GREEN。
  R240 以 connection generation `52` 继续连接同一 retained PID `44264`；runtime diagnostics GREEN，
  blocking diagnostics `0`。
- `zg361cp.133` instance `162` 在 `date_raw=53187648` 选择 authored option 1 / native index 0，目标路径 GREEN；
  R 从 `5` 推进到 `6` 且 `active=0`，portfolio finalize，R domain live complete。
- 中央 poll 后，下一帧进入 P3：`zg361cp.9000` instance `163` 在 `date_raw=53187720` 出现，exact frame 含
  54 个 saved scopes（23 character + 31 typed non-character），native options `0/1/2/3`、snapshot `4`；`.9000`
  contract 仍在施工，runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 `.133` target GREEN。
- report `Z:\p2r240promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `BA090BA6DD8E39706182ADAC1BB50237795403A9C393D4F4C5D5718143A47361`。
  R domain live complete 不等于 full-tree；P3 35 卡、stage 9–11、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending，R240 不提升最终 readiness。

## R241 P3 entry：`.9000` route D target GREEN / `.229` sentinel RED

- `.9000` exact contract 位于 commit `9aa3a85`；focused normal/`-O` 各 `3/3`、aggregate normal/`-O` 各
  `73/73`、real exact `67`、`validate_static` 与 diff-check 均 GREEN。R241 以 connection generation `53` 继续连接
  retained PID `44264`；runtime diagnostics GREEN，blocking diagnostics `0`。
- `zg361cp.9000` instance `163` 在 `date_raw=53187720` 的 exact frame 含 54 个 saved scopes
  （23 character + 31 value）；R241 选择 route D authored option 4 / native index 3，该目标路径 GREEN。
- 下一帧 `zg361cp.229` instance `164` 在 `date_raw=53187744` 出现，保持相同的 exact 54-scope shape，
  native options `0/1/2`、snapshot `3`；R241 strict no-click。top-level RED 仅来自 next-event sentinel，
  不否定 `.9000` target GREEN，也不是 runtime RED。
- report `Z:\p2r241promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `B6E92CF23CAA583B711EDC2CD7D500D022F1C7D7047407341AD45B532235CFB2`。

## R242 P3 AA batch：13 张逐张 live proven，AA close

- AA batch exact contract 位于 commit `5517db1`；focused normal/`-O` 各 `7/7`、aggregate normal/`-O` 各
  `73/73`、direct `.229` exact verification 与 exact coverage `67`、`validate_static` 与 diff-check 均 GREEN。
  R242 以 connection generation `54` 继续连接同一 retained PID `44264`；runtime diagnostics GREEN，
  blocking diagnostics `0`。
- R242 按执行顺序逐张闭合 AA 13 张：`.229,.230,.231,.232,.233,.234,.235,.236,.237,.240,.238,.239,.241`，
  对应 instances `164–176`、`date_raw=53187744..53188032`。每张均选择 authored option 1 / native index 0，
  且各自都有独立的 `event_instance_advanced` 后置验收；AA close，hidden `.9001` 未暴露 UI。
- 除直接 `.229` 外的后 12 张在本轮前只是 source-predicted；R242 现已逐张提供 live proof，不能再把它们仅标为预测。
- 下一帧进入 AG：`zg361cp.301` instance `177` 在 `date_raw=53188056` 出现，exact frame 含 58 个 saved scopes
  （25 character + 33 typed non-character），native options `0/1/2`、snapshot `3`；AG contract 仍在施工，
  runner strict no-click。top-level RED 仅来自 next-event sentinel，不否定 AA batch target GREEN。
- report `Z:\p2r242promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `06A44E2641D4BB576A90DBE6CDDD1735ABC1EB86E4101BEF7FBB32020378F128`。
  AA close 不等于 P3/full-tree 完成；AG、AJ、stage 9–11、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending，R242 不提升最终 readiness。

## Stage 11 Workforce Endgame 源码路由预审（research）

- 只读 source audit 确认 itemized/full-tree 应选 player batch mode `zg361ch.950.d`；中央先完成 stage 9/10，stage 11 才打开 Workforce。最短 non-debt 合同对适用的 owner-visible 卡选择 A，不能让 timeout 自动落到 route C debt。
- AB 可见序列为 `.242→.243→.244→.245→.246→.247→.248→.249→.250→.251→.252→.253`。AC 为 `.254→.255→.260→.261→.256→.258→.259→.257→.262`；随后必须跨周期通过 `.5262` 才出现 `.263`，再走可见 handoff `.5264→.5265→.5266`（其间 hidden D+30 `.5267/.5268`）及 `.264→.265`。
- 当前 central-owner / AI-subject 的 accepted AD 生产路径为 `.266→.273→WAD referral callback→.271→WAD panel callback→.267→.268→.270→.272→.274→.269→future real probation/outcome→.276→.277`；只有 player 承担对应 actor 时才额外显示 `zg361wad.1` 或 `.11/.12/.13` 之一，accepted 路径的 `.275` 自动记 N/A、不显示。AL 为 `.355→.356→外部真实 357–359 receipts→.360`；冷启动/第一个 eligible history cycle 在 `.360` 后以 status `8`、`operation_used=39/40` 合法关闭，`.361` 仅同 subject/tuple 第三个 eligible cycle 且 owner 为 top celestial liege 时可见，并以 status `6`、`40/40` 关闭。
- `.262` 的硬前置是 `owner.review_serial >= ticket_cycle+1`，所以不存在同一 review cycle 内抵达 summary 的 non-debt 路线；只有 Workforce status `6/8` 关闭且 central stage 11 记录 status `2` 后，才可等待 aggregate `zg361p2c.2`。以上仅为 `source-audited` / `research` 施工边界，本次没有点击或实机观测任何 Stage 11 事件，不提升 live/full-tree readiness。

## R243 P3 AG batch：11 张逐张 live proven，AG close

- AG batch contract 已由 commit `006da51` 提交；R243 前的报告滚动状态已在 commit `c9a5bb1` 提交。
  R243 以 connection generation `55` 继续连接 retained PID `44264`；runtime diagnostics GREEN，blocking `0`。
- R243 按正常顺序逐张闭合 AG 11 张 `.301–.311`，对应 instances `177–187`、
  `date_raw=53188056..53188296`。每张均选择 authored option 1 / native index 0，且各自都有独立的
  `event_instance_advanced` 后置验收；AG close，hidden `.9002` 未暴露 UI，流程同步进入 AJ。
- 下一帧为 AJ `zg361cp.334` instance `188`，在 `date_raw=53188320`、root `32904` 出现；exact frame 含
  62 个 saved scopes（27 character + 35 value），native options `0/1/2`、snapshot `3`。R243 strict no-click，
  runner 整体 RED 仅来自这个新哨兵，不否定 AG batch target GREEN，也不是 runtime RED。
- report `Z:\p2r243promo_resume_r1\report.json` 已通过 JSON parse，SHA-256 实算为
  `3D15FBCB042D4C8D98C625599FEDDB70DF96FD4F2CEFA197E102576F4F26607B`。
  AG close 不等于 P3/full-tree 完成；AJ、stage 9–11、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending，R243 不提升最终 readiness。

## R244 P3 AJ batch：AJ / Phase 3 close，后续 harness RED

- AJ batch contract 位于 commit `3926006`。R244 以 connection generation `56` 继续连接 retained PID `44264`；
  runtime diagnostics GREEN、blocking `0`。另有 1 条无关 situation nonblocking warning，不构成产品阻断。
- R244 按执行顺序逐张闭合 AJ 11 张：`.334,.335,.336,.338,.339,.340,.342,.337,.341,.343,.344`，
  对应 instances `188–198`、`date_raw=53188320..53188560`。`.336` 选择 authored option 2 / native index 1，
  其余均为 authored/native `1/0`；所有卡逐张 GREEN，AJ / Phase 3 正常 close。
- runner 随后自然推进到 `date_raw=53191392`。pause-map pre-submission revision rebind 连续 4 次失败，
  最终为 expected revision `78` / current revision `80`，因此整体 verdict 为 harness RED。该故障不否定 AJ/Phase 3
  close，也不是 product RED；最小修复正在进行。
- 下一玩家事件尚未冻结；stage 9 `.390` 仍 pending，不能据此推定它就是下一帧、推定日期或提升 readiness。
- report `Z:\p2r244promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `E5E710B1B51BBA06F7AFB43743F0E33C15C4BA63EFA2F36926CEA6CBA28D3933`。
  stage 9–11、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending；R244 不提升最终 readiness。

## R245 旧 absolute bound 输入前拒绝

- R245 connection generation `57` 继续连接 retained PID `44264`，从 `date_raw=53194104` 开始；本轮 drain 为 `0`。
  旧 absolute 1900-day bound 在任何输入前拒绝，attached snapshot 为 `paused=false`，产品仍在继续运行。
- runtime diagnostics GREEN、blocking `0`。本轮整体 RED 是 input-free harness RED，不是 product RED；旧 bound 单独提供的证据不足，
  也不能据此提升 full-tree readiness。
- report `Z:\p2r245promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `CBCD5711B76A6253B42EB601EC7A7AF8EA6192E5BAAF3FF76B52C8518A9426D2`。

## R246 pause mismatch：仅增加重试被实机否定

- R246 connection generation `58` 继续连接同一 PID。`tribute_mission.1005` instance `199` / `date_raw=53194104`
  以 authored option 6 / native index 5 GREEN；`spymaster_task.0359` instance `200` / `date_raw=53194896`
  以 authored/native `1/0` GREEN。
- 随后连续 8 次 pause mismatch 全部保持 input-free；runner 因此为 harness RED，而 runtime diagnostics 仍 GREEN、blocking `0`。
  该实证否定“单纯增加重试次数”足以修复，不否定前述两个事件目标 GREEN，也不是 product RED。
- report `Z:\p2r246promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `756943C014E5830A740EE93305FA09265767E40D2AF8144FFDB3202114A2CFED`。

## R247 native-fresh-idempotent pause fallback 实机成功

- R247 connection generation `59` 继续连接 retained PID `44264`；native-fresh-idempotent pause fallback 已在实机成功，作为
  pause-map harness 修复的 live audit item。随后 `tgp_china_ministry.0100` instance `201` / `date_raw=53198208`、
  `debate_event.5110` instance `202` / `date_raw=53203368`、`ep3_decisions_event.2001` instance `203` /
  `date_raw=53203440` 均以 authored option 2 / native index 1 GREEN；runtime diagnostics GREEN、blocking `0`。
- 下一帧为 `natural_disaster.8001` instance `204` / `date_raw=53204688` / root `32904`，exact 8 scopes：
  2 character，以及 situation、situation_sub_region、situation_participant_group、province、great_project、landed_title 各 1；
  仅呈现 native option 0、snapshot `1`。runner 严格未点击，当前边界仍是 harness sentinel，不是 product RED。
- `c0b42cb` 已闭合有限 5000-day absolute bound 的静态证明及 `natural_disaster.8001` exact 合同；该事件的 live continuation 仍待恢复后继续。stage 9 `.390`
  尚未出现，不能推断其状态；stage 9–11、完整 full-tree、AF5 terminal route 3、source registry 与最终视频仍 pending，
  R247 不提升最终 readiness。
- report `Z:\p2r247promo_resume\report.json` 已通过 JSON parse，SHA-256 实算为
  `517B41DB465D1C8A48F70CE2FADAAE6802C9D2647C5D549DAFF041579F387E5A`。

## `c0b42cb` 5000-day absolute bound 证明与 R247 后续 incident

- `c0b42cb` 将 promotion continuation 的时间门槛固定为单一 absolute 5000-day bound。R245 已消耗 `1962` 天；
  Stage 11 nominal 预算 `3234` 天，加两次 `D+90` 与 `60` 天余量后为 `3234 + 2×90 + 60 = 3474` 天，再加后续 B1 `400` 天及
  compensation `1060` 天，审计上界合计 `4934` 天，距 5000-day bound 仍余 `66` 天。
- absolute raw deadline 固定为 `53267016`，reconnect 不重置、不延长该 deadline；因此修复旧 1900-day bound 不等于取消有限门槛。
- R247 报告生成后，retained PID `44264` 于约 13:48:46 无序退出；最新 autosave 时间为 13:48:10。
  未发现新的 CK3 crash artifact 或 Windows Error Reporting artifact，但缺少 artifact 不能证明退出原因。
- 该退出同时暴露 report generation 的 liveness race。退出前最后一份产品证据仍是 runtime diagnostics GREEN、blocking `0`；
  当前只标记为 `unexplained abrupt exit` / harness incident，不回溯改写既有事件 GREEN，也不宣称 product RED。
  恢复结论保持 pending，待从 autosave 重新建立可核验现场；不猜测退出原因或 stage 9 `.390` 状态。

## `df5ab3c` late-save recovery 竞态修复与 R248

- commit `df5ab3c9110e07ea2501c2aaecf87a5790196ea0`（`Recover late promotion sessions safely`）已推送，当前
  `HEAD == origin/master`。恢复器将 late save 按字节复制到 fresh isolated product-only profile，保留源 profile/save 只读；
  final handoff 在 `driver.close()` 后重新检查 managed supervisor，若会话已退出则撤销 retention sidecar、禁止 reconnect，
  不再把已死亡 PID 报为可续跑。
- 直接覆盖该修复的纯离线单元测试 normal `79/79`、`-O 79/79` GREEN；测试包含 supervisor 在 driver close
  期间退出时撤销 retention 的竞态回归。本报告代理未启动 CK3。
- R248 late-save recovery report 位于 `Z:\p2r248restore\report.json`，JSON parse GREEN，SHA-256
  `DAFADC56F489226BD5C4FBC57C9C236DB119FD54ED663B083ABCCBDBE3FF968D`。源 autosave 为
  `Z:\p2r193promo_a_native_state\profile\save games\autosave.ck3`，`83,449,160` bytes，SHA-256
  `c7e5ca119f4db4d9bfe738859dd5e32dec6f9f1f804d91f9b26ed5d256e219da`。
- 当前产品 projection 为 `phase2-full-release-r248-df5ab3c`；source tree SHA-256
  `3adf4297df0699f903d152561d2a9b5231d7934e15bc2255dbb88d0541161bcd`，formal overlay/product projection tree
  SHA-256 `644dbc006d15bb9cbb193a31323e313682464855c5b8e863b59e7e4fa0e8e197`。projection manifest
  `Z:\p2p248restore_current\phase2-product-projection.json` SHA-256
  `5846F60D99AE6E040E99E65DB96938B4C66B14174479D975D07C20EB6036EBD7`。
- R248 loader `303/303` 与 loader error scan GREEN。产品推进在 `death_management.1001` instance `202` /
  `date_raw=53201424` / root `32904` 输入前 strict fail-closed，因此 report 为 RED；没有点击事件。会话以
  `RETAINED` 保留 PID `113932`、connection generation `1`、paused/map-ready、reconnect authorized。
- 该 late save 从 `date_raw=53199480` 继续并直接遇到新的原版事件边界；它没有重放 R247 的
  `natural_disaster.8001`，不能证明该事件 live continuation，也不能证明 T0-P1/full-tree 完成或提升 readiness。
- 宣传工具本轮仅做 prep preflight。按用户新增 hard gate，T0-P2 继续 `LOCKED`；未执行 source intake、素材采集、
  TTS、FFmpeg、候选构建或媒体审阅/导出，footage 保持 `0/8`、最终 MP4 `0/2`。

## R249 retained continuation

- R249 report `Z:\p2r249promo_resume\report.json` 已通过 JSON parse，SHA-256
  `4F2B70AC510157DC8DFD978A3A0418B9A28597D5B88EE50460FD10F4BB6964F1`。connection generation `2`
  复用同一 PID `113932`，`launch_performed=false`、`restart_performed=false`、restart count `0`；runtime diagnostics
  GREEN，blocking `0`，结束后会话继续 `RETAINED`。
- `death_management.1001` instance `202` / `date_raw=53201424` 以 authored/native `4/3` GREEN；随后
  `debate_event.5110` instance `203` / `53203368` 与 `ep3_decisions_event.2001` instance `204` / `53203440`
  均以 authored/native `2/1` GREEN，三者都具备事件实例已推进的独立后置条件。
- 下一帧 `parent.1005` instance `205` / `date_raw=53205336` / root `32904`，原生 options `0/1`；runner
  在输入前 strict fail-closed、未点击，因此整体 report RED 仅是新的 harness-contract sentinel，不是 product/runtime RED。
- 本轮没有 `natural_disaster.8001` drain 或重放证据，不把日期推进冒充该事件 live proof；T0-P1/full-tree 仍未完成，
  T0-P2 hard gate 继续 `LOCKED`，宣传媒体仍未制作。

## R250 retained continuation

- R250 report `Z:\p2r250promo_resume\report.json` 已通过 JSON parse，SHA-256
  `E7EE5D62BF4998F1C082849CC9602C8DA4D9796E6396F1C640145F4BEE0A1061`。connection generation `3`
  复用同一 PID `113932`，`launch_performed=false`、`restart_performed=false`、restart count `0`；runtime diagnostics
  GREEN，blocking `0`，结束后会话继续 `RETAINED`。
- `parent.1005` instance `205` / `date_raw=53205336` 以 authored/native `2/1` exact GREEN，并具备
  `event_instance_advanced` 后置条件。
- 下一帧 `ep3_emperor_yearly.2211` instance `206` / `date_raw=53206512`：snapshot option count `4`，event-context
  实际渲染 native indices `1/2/3`；exact saved scopes 为 `potential_title`（`landed_title`）、`liege`（character `32904`）
  与 `vassal`（character `27275`）。runner 输入前 strict fail-closed、未点击，整体 report RED 仅是新的
  harness-contract sentinel，不是 product/runtime RED。
- T0-P1/full-tree 仍未完成；T0-P2 hard gate 继续 `LOCKED`，宣传 prep-only、媒体未制作边界不变。

## R250 后外层 turn abort 会话丢失

- R250 报告写入时的 `RETAINED` handoff 是有效的 artifact-time 证据；随后外层 turn abort 后，OS liveness check 发现 bridge PID `113932` 与 owner PID `151820` 均已不存在，`owner.json` 只剩陈旧记录。
- 该事件严格定性为 `post-turn-abort session loss`：不推断 product fault，不撤销 R250 已完成的 `parent.1005` GREEN；但 `.2211` 的未输入 paused frame 已丢失，不能再声称从同一 PID 续跑。
- 源 autosave 仍为 `83,449,160` bytes，SHA-256 `C7E5CA119F4DB4D9BFE738859DD5E32DEC6F9F1F804D91F9B26ED5D256E219DA`。下一步从该冻结存档 fresh-recover 到新的隔离 userdir，并重放已签约事件至 `.2211`。

## R251 recovery 启动边界 RED

- 两个配置拒绝均发生在 CK3 启动前：首个 pipe 名不满足严格的 32 位十六进制后缀；第二个 userdir 位于当前工作树内，被既有隔离门禁拒绝。两次均未创建 CK3 进程。
- R251a 使用系统临时目录内完整复制的 `pdx_settings.txt`、account、DLC 与 4,944 文件 / 215,620,408 bytes warm shadercache；产品投影、83,449,160 bytes autosave 与双空槽检查均 GREEN。无 bridge 的 Frontend warm-up PID `64324` 在约 2.32 秒、任何 debug log 或 Frontend 证据前以 code `1` 退出。crash bundle 的异常为 `C0000005` at VA `0x00007FF6FC2FBD89`；minidump 证明 CK3 image base `0x00007FF6FA550000`，故 exact RVA 为 `ck3+0x1DABD89`。report SHA-256 `325F18550571D0B6ED4A5AD44C66BEF1026A5F86194C24A9FBC2FEDAD099DD5A`，exception/minidump SHA-256 分别为 `5B26A227134CBDAE0C8432A8E3C8B8FA3B2EDADDF46F2905595CBB8ED3D82E9A` / `76C6A151F36C0FFA3B8E4C98BF5AA1B9D39797090146F67AE1A2405A821577FC`。
- 依据既有一次性冷启动复验规则，R251b 只改变为更短的全新 userdir；同一无 bridge warm-up 又在约 1.66 秒以相同 VA/RVA 崩溃。report SHA-256 `3DCC64B237C0382466C61D61F73C0441FC69B58246265C94B3BDDBA4FC7817AD`，exception/minidump SHA-256 为 `CFE6EB2A93FFCE34F55430508EFEEFBFC966651FCC1BE644DA197F7B8697C0FC` / `54E1D14B1D20DB09FFA7D07251996E73D8977F1288D9AA09002C2A8953E907E3`。
- 两轮都没有进入 loader、bridge、存档或 gameplay，不能推断产品 RED，也没有新增已测业务场景。CK3/injector 最终 inventory 为 `0`；相同 direct-launch 分支停止重试，T0 的 CK3-dependent recovery 标记为 startup/harness RED。T0-P2 继续 `LOCKED`，素材仍 `0/8`、最终 MP4 `0/2`。

## R252 Default-desktop `continue-last-save` 启动边界 RED

- 新增的 Default-desktop relay 固定 child 为同工作树内 `recover_zg361_phase2_promotion_source_session.py`，默认 no-launch，只有显式 `--execute` 才创建 child；同时强制且只允许 `--startup-mode continue-last-save`。共享 Win32 primitive 由既有 seed relay 与本 recovery relay 共用。focused normal/`-O` 各 `5/5`，seed relay normal/`-O` 各 `4/4` GREEN。
- R252 no-launch preflight 固定 `WinSta0\Default`、83,449,160 bytes / `C7E5...19DA` autosave、R248 产品投影和 bridge `30A134...9349`，`child_process_started=false / ck3_launch_attempted=false`；preflight SHA-256 为 `26A563E4FEB7F0359A836826CCFDB82D58164CFA74FEB3AB95F29A2BFAB5AE2A`。
- 唯一一次 execute 通过 suspended pre-resume bridge injection 启动 PID `171184`；bridge generation `1` 已连接，随后进程在 `14.371s` 内、任何 loader/save/gameplay 证据前以 exit code `1` 退出。没有发送任何玩法输入，loader gate 为 RED，cleanup 证明 Job active process `1→0`、tree gone、watchdog absent、control files absent，最终 CK3 inventory 为空。
- crash exception VA `0x00007FF7D823BD89`；minidump ModuleListStream 证明 CK3 image base `0x00007FF7D6490000`，故仍为 exact `ck3+0x1DABD89 / C0000005`。report / exception / minidump SHA-256 分别为 `6E88F5A9D5F79F97A3A7BAAF696A720B8D5CBDD50F209858E369CE31DD1C7A2B`、`57AA603055D2AAD3EDA97C95E93E7FE3E9AE86E63EA0B1E1A70B35B53CBD8EDA`、`851FE1D2EADAE466DC624AEEB310823677D1CD2771A2544B8DE9129138B943B8`。
- 该轮只证明“Default desktop + direct managed bridge + `-continuelastsave`”仍会命中已知启动签名；它不是 product/scenario RED，不增加 scene coverage，也不推翻 R248–R250 的 exact GREEN。相同形态停止重试，T0 仍为 `50%`，T0-P2 仍 `LOCKED`。

## T1 GEN-034 settings/cache 启动资产门

- G2 concrete adapter 现要求显式 `--profile-settings-template`，并在 fresh repo-external userdir 验证之后、任何 `Popen` 之前复用已实证的完整 `pdx_settings.txt` + warm DX11 shadercache 校验复制；缺失、lane 不完整或复制/hash 漂移均写 `startup-profile-assets.json` 并以零启动收口。
- 保留既有顺序：正常无 bridge 启动 → private observer 捕获并 detach → 同 PID pause → bridge attach。固定 `expected-war-id=50331699`，并在唯一 surrender mutation 前先生成 `xar_checkpoint.ck3`，校验路径、大小、SHA、日期、角色、episode、PID/generation、snapshot/revision/native_revision 与 active war，再把 checkpoint/ticket/source hash-bind；checkpoint 失败时 surrender 零调用。
- focused lifecycle + adapter 合计 normal/`-O` 各 `19/19` GREEN；上游 profile settings guard normal/`-O` 各 `1/1` GREEN。实际 no-launch preflight 校验 `14/14` 依赖、前后 CK3 inventory `0/0`，settings SHA-256 `592AB6C6...F5244`，shadercache `4,908` files / `213,198,746` bytes / tree `373B8C76...B202`，preflight SHA-256 `BC439E8576AC6DF211E7BE68EDE549BAE95BDA3E06EA4EFBC53973B10FC3210B`。
- 状态保持 T1 `90%`：该结果解除旧的静态启动编排 NO-GO，但没有执行 CK3、observer、bridge 或 surrender，也不能冒充 GEN-034 live closure。此次变化只在 G2 内部 runner/manifest，不改变 open_kaishek 公共 API/ABI，T2 当前 horizon 保持 `100%`。

## R253 freeze165b bridge-backed Frontend warm-up RED

- 新增 opt-in `bridge-frontend-first` recovery：Sep-03 freeze165b bridge 只负责独立 Frontend warm-up，warm-up 进程与生命周期必须清理后，当前 `30A134...9349` bridge 才能加载冻结 autosave。两桥 bundle/SHA 与管道均显式提供；相同管道会在 launch 前 fail-closed。默认 `frontend-first` / `continue-last-save` 行为不变。
- 最终 focused recovery normal/`-O` 各 `13/13`、Default-desktop relay 各 `6/6`、R253 native lifecycle 选定用例各 `3/3` GREEN；`py_compile`、`validate_static` 与 `git diff --check` GREEN。no-launch preflight 固定 `WinSta0\Default`、83,449,160 bytes / `C7E5...19DA` autosave、R248 projection、freeze165b warm-up bridge与当前 final bridge；preflight SHA-256 为 `3914C7CD37BC10BB0019B800C002C259AB6F9A274C514667B69FF6BAFFC5793B`。
- 唯一 execute 启动 warm-up PID `42716`；它在 marker wait 1.411 秒内以 code `1` 退出，未创建 debug log、未到 loader/save/gameplay，当前 final bridge 进程从未启动，且没有玩法输入。异常 VA 与 R252 完全相同，为 `0x00007FF7D823BD89`，即已绑定的 exact `ck3+0x1DABD89 / C0000005`。report / exception / minidump SHA-256 分别为 `1F91F30CD5DBFF6757A27FC6C335F42C69311BD3DCA69341D727FA92C3F12E39`、`B4C4F21EF5524BE22705811986E745D5543557BE5C55B0F1D0D7C9EC7376560D`、`562C26615B0567EA48CFEB516B20C6BCACEE64FCCBE7D51C5E06425E93A6A79F`；最终 CK3/injector inventory 为空。
- 这轮排除了“仅把历史 bridge 放进当前 warm-profile/Default-desktop 编排即可恢复 Frontend”的假设，仍是 startup/harness RED，不是 product/scenario RED。scene/readiness 计数不变：T0 `50%`、逐 ID `4/361`、全树定义 `106/626`，T0-P2 继续 `LOCKED`；该 exact 形态不再重试。

## R254-Z envelope volume A/B 仍为 startup RED

- 既有证据显示 Sep-03 freeze165b Frontend GREEN 与 R248 full-product recovery GREEN 都使用 Z: userdir，而 R251–R253 使用 C: Temp；R251b 已排除单纯路径长度。R254-Z 因此只改变 fresh state/artifacts/relay logs 的 backing volume/location 为 Z:，保持 R253 的 Default desktop、p2r193 资产、R248 product、autosave、freeze165b warm-up bridge、当前 final bridge和双桥 choreography 不变。
- no-launch preflight GREEN，SHA-256 `B9081CB2232DFC99A4A2017B1FCE0DFDCBD6C672C6AEF503D0325DF7C5294E64`。唯一 execute 的 warm-up PID `166664` 在 marker wait 1.361 秒内、debug log/loader/save/gameplay 前 code `1` 退出；final bridge 未启动且没有玩法输入。异常仍为同一 VA `0x00007FF7D823BD89` / exact `ck3+0x1DABD89 / C0000005`；report / exception / minidump SHA-256 为 `DC31413D20D0DDC18D58AF69F37B64066227B90B0DA448077856D7677B77608D`、`5651953D61B317448E2539F837B9919563B4D596F85A268287FF91A6B72DCD91`、`599648ED0180B0A40FF2AA7A7A3C6B36F312017BAAE729A601420B29D31EE6F6`，最终进程槽为空。
- 该 A/B 排除了 backing volume/location 是充分恢复条件；它仍不是 product/scenario RED，不增加场景或 readiness。T0 保持 `50%`、逐 ID `4/361`、全树定义 `106/626`，T0-P2 `LOCKED`。C:→Z: 的同形态不再重试。

## R255-Z no-bridge + Z: 缺失单元仍为 startup RED

- R255-Z 复用 R248 同日 GREEN 对照的核心形态：p2r193 warm profile、R248 full product、相同 autosave、Z: fresh userdir 与无 bridge Frontend warm-up；保持当前 final bridge 只用于后续 autosave 进程。它补齐 R251 的 C:/no-bridge 与 R254-Z 的 Z:/bridge 之间尚未执行的 2×2 最后一格。首次错误 pipe 后缀在输入校验时拒绝，未创建目录或 CK3，不计启动 attempt；修正为严格 32 位 lowercase hex 后只执行一次。
- 唯一 execute 的 warm-up PID `143132` 在 `1.311` 秒内、debug log/loader/save/gameplay 前以 code `1` 退出；final bridge 未启动、无玩法输入。异常仍为 VA `0x00007FF7D823BD89` / exact `ck3+0x1DABD89 / C0000005`。report/startup/warm-up/exception/minidump SHA-256 分别为 `5BD27572B78BF3B281608B45726399B824BEB9A209FE6A2C9926F08DE751B201`、`5F224C6D3D3DB1FA74A57BF2A96EDB4D53F29E480D0646EEF9E638B5E49C521E`、`8EA5A89FE680AC3438158451E063FEDEC875241CA99B3A691BCFD40B454CDA18`、`4422784DE31B1EC7CBED40F1471FDFF44F4C42C8CEF424E2F45F2C24A44AC50A`、`32EB5D32D5DAC9F77998146AFFB3F3FA1634518E485F5C9A480A31699CA92FF1`；事后 CK3/injector 槽为空。
- 该轮只关闭 Z:/no-bridge 缺失单元，仍是 startup/harness RED，不增加 scene/readiness，也不撤销 R248–R250 GREEN。T0 保持 `50%`、逐 ID `4/361`、全树定义 `106/626`，T0-P2 继续 `LOCKED`；此 exact 形态不再重试。
- R248 GREEN 与 R255 RED 的 artifact 差分只剩一个可执行定义清楚的变量：R248 使用 `Z:\p2r248restore_state\profile` 这一 Z: 根目录短路径，而 R255 使用 `Z:\ck3_mod_rewrite\_runtime\...\profile`。R251b 仅排除了 C: 短路径，R254 也仍在仓库嵌套路径。候选 R256 必须使用新鲜 `Z:\p2r256restore_state` / `Z:\p2r256restore`，其余严格保持 R255；当前 sandbox 对 Z: 根目录无写权限，因此 CK3 gate 为 `NO-GO`。除该精确外部状态变化外，没有 artifact 支撑的新启动变量。
- 六份 R251a/b–R255 dump 的 exact-build 反汇编已闭合直接故障：同一 `ck3+0x1DABD89` 指令 `movsxd rdi,[r14+0x4c]`、同一 `R14=0x1f8`、同一非法读地址 `0x244`。全局 manager 有效，但 `[manager+0xA8]` 的 index-0 slot 为 null，caller 第一项即消费它。当前没有 PDB/source，不能命名该子系统；已转入 slot writer/xref 与只读启动时序探针，目标是区分初始化未发生与发生过晚，不修改 CK3 内存。
- writer/xref 已证明构造器在发布 manager global 前同步调用 `0x3A88920` 构建 slot0，并在 `0x39C70B8` 把返回值写入 `manager+0xA8`；相关资源字符串明确为 `ParticleTexture` / `gfx/FX/cw/particle2.shader`。因此真实条件是 factory 返回 null，而非发布后的异步时序。已新增 exact-build、默认关闭、只读的 startup slot0 probe：读取 `module+0x570F908`、`manager+0xA8` 与 graphics global `module+0x570FC60`，记录首次非零/末值/退出/错误；模块、native 接线、recovery normal/`-O` 分别各 `5/5`、`3/3`、`16/16` GREEN。不写 CK3 内存、不输入玩法，显式启用时不完整 capture 必须 fail-closed。
- factory 已收敛为 graphics global、source lookup、variant lookup、backend creation 四类 null 出口。三层 `particle2.shader` 都存在并声明 `ParticleTexture`；R248 启动前与 R252–R255 的 4,908-file DX11 cache/tree 完全一致，R251a 复用 R248 成功后 4,944-file cache 仍崩，settings 也同哈希。因此缺文件、缺 lane、cache 复制损坏与 settings/backend 漂移均无证据，不执行清 cache/改设置；graphics-global 采样只能区分第一类，不能冒充后三类分支执行证明。
- R257 仅增加上述只读 probe，保持 R255 启动输入不变；PID `212584` 仍在 `1.312` 秒、任何业务证据前命中同一 RVA。probe 在刚恢复进程的首次 module snapshot 命中 `ERROR_PARTIAL_COPY (299)`，零样本、`capture_ok=false`，所以它是 harness attach RED，尚未判断四类 factory 分支。report/probe/exception/minidump SHA 为 `99FFF31D...EBB0E` / `7B09CBD7...3ADE` / `2D7C3244...AF7E` / `3FAE6781...6150`；已限定只修 24/299 有界重试后做一次 R258 诊断，计数保持不变。
- R258 的有界 retry 后 probe `capture_ok=true`：PID `135392`，root/graphics 各 `252` 次读取、slot0 `109` 次读取、零错误；graphics global 于 timeline `12.538463s` 非零，manager 于 `12.681426s` 非零，slot0 直至 `13.254901s` 进程退出始终为零。故 graphics-global-null 分支已排除，只剩 source/variant/backend 三类。CK3 仍为约 1.361 秒同 RVA startup RED；report/probe/exception/minidump SHA 为 `63FB0671...8361` / `2C1E8B8C...A334` / `DA59C0DD...AF98` / `DC2BFC0...A5EB`。这是 diagnostic evidence GREEN，不增加 scene/readiness，且同一 probe 不再重跑。

## R264–R271 Particle2 resolver / backend 诊断收敛

- R264 与 R265 用 probe SHA-256 `227CDB851BC58208BD7685ED52BCBCC85DE6C50671ADC913DE1EB788C9D656A9` 两次独立闭合 exact tuple `gfx/FX/cw/particle2.shader`（26 bytes）/ `ParticleTexture`（15 bytes）的 source-detail 路径。两轮都观测到 graphics global 非零、source cache 为 `0`、规范化成功且文本逐字节不变、resolver list head 非零，但 search/source/final output 均为 `0`，分类为 `source-lookup-null / source-resolver-null / resolver-candidates-all-reject`。R264 PID `159248`、`1.7703586s`，artifact `Z:\ck3_mod_rewrite\_runtime\p2r264diag-artifacts\particle2-resolver-detail-capture.json`，SHA-256 `B1677D1CC015C15E63B370E76A29549D8C1202F9A8B02926DE5E2FCA88C7C32F`；R265 PID `169736`、`1.4725841s`，artifact `Z:\ck3_mod_rewrite\_runtime\p2r265diag-artifacts\particle2-resolver-detail-capture.json`，SHA-256 `21C985201824ABA45BF4845502CB47801E372506C751485A05C6E951B8027D93`。两份均为 `GREEN_DIAGNOSTIC / complete-valid / diagnostic-termination-clean`，不是产品 live。
- R266 把 resolver list 展开为唯一候选：空 prefix、`next=0`，primary 与 secondary 都返回 `0`，最终 search 仍为 `0`，从而把“all reject”落到确定的一项候选而非空链。PID `71632`、`1.5059928s`，probe SHA-256 `F974682AF7A8CFC61B71A551EB6FF4593C053A4691296B826C145CD45619BA77`；artifact `Z:\ck3_mod_rewrite\_runtime\p2r266diag-artifacts\particle2-resolver-candidates-capture.json`，SHA-256 `825DEEF2F9CD6B7D9C37B39DCF09B75FD3F495849CB1FBFFB396DCE7447DA986`。
- R267 继续进入该候选的 secondary：backend object、dispatch table 与 callback 均为非零，rewrite address/offset 与 state `f9/fa` 均为 `0`，callback 和 secondary 最终都返回 `0`；source/final output 仍为 `0`。PID `87188`、`1.5367991s`，probe SHA-256 `9710AD3A1D0A9D6167109FF7D0C774167B6D9DBABC670B6074E0963AF394177F`；artifact `Z:\ck3_mod_rewrite\_runtime\p2r267diag-artifacts\particle2-secondary-capture.json`，SHA-256 `B63A0224B3DF9244937E21A30B902F9419786C1A6C5CAE325B668C9BF08CA0AE`。
- R268 把 secondary callback 的实际文件解析闭合：backend root 为 `Z:/ck3_mod_rewrite/_runtime/p2r267diag-state/profile\`，requested text 为 `gfx/FX/cw/particle2.shader`，组合路径精确为 `Z:\ck3_mod_rewrite\_runtime\p2r267diag-state\profile\gfx\FX\cw\particle2.shader`，backend read 返回 `0`，随后 callback/search/source/final output 全为 `0`。PID `90140`、`2.3235644s`，probe SHA-256 `6EF81E91401A75C3C1DAD5A3BC8085E638B4F9E3DFF69E88F0633B93C16F12FF`；artifact `Z:\ck3_mod_rewrite\_runtime\p2r268diag-artifacts\particle2-backend-path-capture.json`，SHA-256 `A30DE8C7D09D7214015D0BD3326D00FB363AA59F9D5CD4EC6EE09FD57F9426DD`。该证据只证明这条候选读取 profile-local loose file 失败，不能外推所有 VFS/backend 语义。
- R269 仅向该精确 profile 路径投影单个 4,329-byte `particle2.shader`（SHA-256 `9F25E03134E24EF0490EA0E1537BFE5BF070FF9CE567678CC636A18FAA84F075`）后，source cache 与 source/variant output 首次全部非零，但 backend/final output 仍为 `0`，分类从 `source-lookup-null` 前移为 `backend-creation-null`。PID `97608`、`2.0599121s`；artifact `Z:\ck3_mod_rewrite\_runtime\p2r269diag-artifacts\particle2-single-file-overlay-capture.json`，SHA-256 `DEEA6C82E76B8A5DBC1CD5488C7A093B2A8B3F8CCCB451BAB41140F2BB4C659A`。
- R270 在同一诊断 profile 补入该 shader 的 10 个最小 `.fxh` include 后，source/variant 继续非零，backend/final 仍为 `0`，再次分类为 `backend-creation-null`；所以单文件缺失是 source lookup 的直接条件，但已知最小 include 集仍不足以恢复 backend 创建。PID `202708`、`1.737294s`；artifact `Z:\ck3_mod_rewrite\_runtime\p2r270diag-artifacts\particle2-minimal-include-overlay-capture.json`，SHA-256 `25AE4EE9B1ADD868EDB656A823172510A16FCB193AFB30AD46F965273C34A3D7`。
- R271 对 backend 创建分支继续只读下钻：source 与 variant 均非零，backend entry 已命中，device object、dispatch table 与 callback（exact RVA `0x3AD4C30`）均非零；backend cache result 为 `0`，随后 virtual-call wrapper output、backend output 与 final output 仍全部为 `0`。PID `56452`、`1.5450722s`，probe SHA-256 `41580D1B8D02EB7F59C347C4325EC8D474B4276D9E5F1438FF3880CE771541A6`；artifact `Z:\ck3_mod_rewrite\_runtime\p2r271diag-artifacts\particle2-backend-detail-capture.json`，SHA-256 `3A230624A5DDF5BDC71B9CA11941DB096964BF0BCA4ABB97318F6B48A79A7FC5`。
- R264–R271 全部使用 exact CK3 `1.19.0.6` / EXE SHA-256 `2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86`，`input_sent=false`；artifact 内的 bridge/map/mailbox 均为 false、query/gameplay input 均为 `0`、`readiness_promoted=false`，并采用 capture 后诊断终止。它们只提高 startup fault 的归因精度，不是 loader、save、场景或 production-live 验收。计分保持 T0 `50%`、逐 ID `4/361`、全树定义 `106/626`、完整迁移阶段 `8/11`、总体 `67.5%`；T0-P2 继续 `LOCKED`。
- 下一步只沿 R271 已观测的 backend virtual callback `module+0x3AD4C30` 做 exact-build 静态逆向与一次性、默认关闭的只读分支采样，区分 callback 内部 cache/input/编译或设备创建的确切 null 出口；不再凭猜测扩大 loose-file overlay，也不把任何诊断 capture 当作恢复 GREEN。只有得到最小可验证修复并重新穿过 Frontend、loader、存档和 paused gameplay 后，才恢复 `.2211` / `natural_disaster.8001` 与 full-tree 验收，计数方可变化。

## R272–R273 Particle2 HLSL 隐式前导闭合与 harness-only 投影

- R272 复用 exact EXE 与 probe SHA-256 `359CA19FA71F234CC317EA9DB1E04A0489708F0280CF8DBDC92C7D44841770F9`，保持 R270 的 11 文件显式 shader/include closure 不变。PID `14816` 中 source cache、source output、`ParticleTexture` variant 及 backend config/device 均非零，但首个 active stage `VertexParticle` 的 HLSL generation 返回 false，initializer、backend callback/vcall、outer backend 与 caller final output 随后均为零；首个失败点因此从笼统 `backend-creation-null` 收敛为 `backend-hlsl-generation-null`。artifact `Z:\ck3_mod_rewrite\_runtime\p2r272diag-artifacts\particle2-initializer-detail-capture.json` 为 `7,002` bytes，SHA-256 `C528AA12E463543AE2BF81CF46201109DEDFE65F0978C235678033D2DF0D840D`。
- R273 保持上述输入、断点合同和 userdir 不变，只增加 Clausewitz 在 HLSL 生成阶段隐式注入的 `cw/defines_common.fxh`（`880` bytes，SHA-256 `CAAC6CB12CE9FD820C35581076A642538F822CB86CE27148B4E55E2A167781EC`）与 `cw/defines_hlsl.fxh`（`8,074` bytes，SHA-256 `A92B73EE9969B2C61BA83C86D705C95D5E264641D758BDBAD13D241A4DF04374`）。PID `147896` 中 `VertexParticle` / `PixelTexture` 两个 active stage 均为 `hlsl_ok=true`，各自 shader vcall/getter/stage output、initializer、backend callback/vcall、outer backend 与 caller final output 全部非零，分类为 `all-nonnull / backend-initializer-success`；shader cache lookup 仍为零，不影响本次非缓存创建成功。artifact `Z:\ck3_mod_rewrite\_runtime\p2r273diag-artifacts\particle2-implicit-defines-overlay-capture.json` 为 `13,577` bytes，SHA-256 `06328E125181343AF5D9F69D4644EBDCDFDF052970BD964A56FA0E012A024A5D`。
- 由此冻结 13 文件 startup bundle：`game:cw/particle2.shader`=`9F25E03134E24EF0490EA0E1537BFE5BF070FF9CE567678CC636A18FAA84F075`、`game:cw/particle2.fxh`=`5388B201A41F8606195DC48BFD7F784DC7E18DB3BF1F25AEF91C68FE56E9F35D`、`game:jomini/jomini_fog.fxh`=`D77842C81E8896EFB7BDBECBE8A10C9A3C9B2E2CC3AC378F069863CECFF0AF72`、`game:jomini/jomini_fog_of_war.fxh`=`8D6DD77ECA2C20AEB4F5C13975603ADBFC7010548C7B52464F41C55AB45D882F`、`game:cw/camera.fxh`=`C1B8FC8B61C08CA73A0B8F48EF85680B9973F1253E9549464370AD21F7275684`、`clausewitz:cw/random.fxh`=`20FF4A25C9860028AD4AE9688AAFE97DF79BA1B69563C03F7797F25853C6865D`、`clausewitz:cw/pdxterrain.fxh`=`DEDCD87D156B19B00958844D7734D21EE1A0B22C98D27657827CE6FAE166C696`、`jomini:jomini/jomini.fxh`=`8377A1E9D2D9E732449F22E0FBC21B787A42CFD69AB272922239CE4BD3DA6026`、`clausewitz:cw/heightmap.fxh`=`231B687443A2DC4D9B7C8606DA9CF65EF43B0CE1AB53CCD7F5F6BEC6B576E3E7`、`clausewitz:cw/utility.fxh`=`ABD382499457D6616597E41647983B982A44AEA7A0A3392927693827201CAB1B`、`clausewitz:cw/upscale_utils.fxh`=`A48DCA74818B4B69E020B0CE5567BD9EC87527CB686871C0F069849A98C62079`，以及上述两份隐式 prelude。权威逐项表同时记录在 `docs/testing-workflow.md`。
- `tools/run_zhongguo_acceptance.py::project_particle2_startup_shader_bundle` 已把该 bundle 接入 `bootstrap_userdir`：只从 exact CK3 `1.19.0.6` 的 `game` / `clausewitz` / `jomini` 层读取，先绑定 EXE SHA-256 `2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86`，再逐项核对来源与复制后哈希、拒绝目标越出隔离 profile 或覆盖已有文件；receipt 固定记录 `GREEN_STATIC`、`projected=true`、`harness_only=true`、`release_staging_affected=false` 及 13 项明细。它只写一次性 `<userdir>/gfx/FX/`，不写 mod 源树、Workshop cache、`mod-content/zhongguo_361` 或 release staging/manifest/ZIP，不能当作产品资源改动。
- 离线验证已通过：debug-capture 单测 normal/`-O` 各 `7/7`，MSVC `/W4 /WX /O2` build GREEN，exact EXE self-test GREEN，focused recovery normal/`-O` 各 `16/16`，从正确 cwd 执行的 workshop acceptance normal `10/10`，`py_compile` 与 `git diff --check` 均 GREEN。13 文件 helper 直接投影 normal/`-O` 各显示 `13/13`；其中 `-O` 外层使用 Python `assert`、会被优化掉，因此只把 normal `13/13` 作为强验证证据，不夸大 optimized 轮。
- R272/R273 均为 `GREEN_DIAGNOSTIC / complete-valid / diagnostic-termination-clean`，`input_sent=false`，bridge/map/mailbox/readiness 均为 false，query 与 gameplay input 均为 `0`；R273 的 `all-nonnull` 与 bundle 的 `GREEN_STATIC` 只关闭 direct-startup harness 的 factory 分支，不证明正常 launcher continuation、loader、存档、地图或 gameplay。计数保持 T0 `50%`、逐 ID `4/361`、全树定义 `106/626`、完整迁移阶段 `8/11`、总体 `67.5%`，T0-P2 继续 `LOCKED`。

## R274 正式 relay：越过 Particle2 后命中新 DX11 draw fault

- R274 no-launch preflight 为 `READY_TO_RUN`；artifact `Z:\ck3_mod_rewrite\_runtime\p2r274relay\preflight.json` SHA-256 `C6AD369D060FB646509A42DAA338CC263AFC43C9FB2DF42B2CE60985E171AEEA`。唯一 execute 通过 `WinSta0\Default` relay 启动 fixed late-save recovery，relay child PID `116104`；`Z:\ck3_mod_rewrite\_runtime\p2r274relay\execution.json` 最终为 `RED`、`child_process_started=true`、`ck3_launch_attempted=true`、child exit code `1`，SHA-256 `DAAF183871AD1958583B195FA5DB019722CE565B4A503D471E33EFDAB7725275`。
- CK3 PID `89336` 的进程创建时间为 `2026-09-07T13:13:04.264874Z`，crash exception 时间为本地 `2026-09-07 21:13:05`，因此真实 CK3 生存期约 `1.3s`。父 native-session 从 `13:12:51.747724Z` 到 `13:13:06.419994Z` 报出的 `14.672s` 包含 CK3 创建前的编排及退出后的会话收口，**不是 CK3 运行了 14.672 秒**；loader artifact 的 `299.753s` 同样是进程退出后的 harness 等待，不是游戏存活时间。
- 13 文件 profile-local bundle 已使本轮越过旧 `ck3+0x1DABD89` / read `0x244` 的 Particle2 null-slot 故障；bridge generation `1` 也已连接，但在 map/mailbox、loader database node、save/gameplay 和任何玩法输入之前，进程命中新 `C0000005`：VA `0x00007FF6BE4CAE5F`，按 exact image base 解析为 `ck3+0x3B0AE5F`，read address `0x90C`。minidump context 为 `R14=0`，故障读取是 null child 的 `+0x90C`；其非空 outer render-context object 满足 `[outer+0x1938] bit0=true`，但 `[outer+0x1940]` shader-state child 为 null。这里的准确结论是 Particle2 旧 fault 已越过、下一个独立 DX11 pre-draw binding fault 已暴露，不能把它写成 CK3/loader 恢复 GREEN。
- recovery report `Z:\ck3_mod_rewrite\_runtime\p2r274restore\report.json` 为 RED，SHA-256 `A25E7A5DB702262FED20CE0EDEC101ED1AA17F4EBC6F39CB91D259E37FA0A16F`；loader gate `Z:\ck3_mod_rewrite\_runtime\p2r274restore\03_loader_gate.json` 为 `no_loader_terminal`、database nodes `0`、gameplay 未执行，SHA-256 `2A6865C8208950ABD43AD511290CEE7415CC0EDF1CC034E55E9B4D519D348325`。crash bundle 位于 `Z:\ck3_mod_rewrite\_runtime\p2r274restore_state\profile\crashes\ck3_20260907_211305\`：`exception.txt` SHA-256 `558A8003241842FECEE1C9775CC16D88FB43814AC5BE41C494C64C88736355B3`；`minidump.dmp`（`50,489,896` bytes）SHA-256 `E5005A73A62905335F918851A87E87A007FF63D9F9E3C1CE97DD5BC512982C0F`。
- 受管清理已闭合：CK3 exit code `1`，Job active processes `2 -> 0`、`tree_gone=true`、`cleanup_proven=true`，最终 tasklist/WMI CK3 inventory 均为空，watchdog 从 `running` 到 `absent`，control files 全部 absent，cleanup contract errors 为空且 `ok=true`。R274 是新的 startup/runtime fault evidence，不是场景验收；没有 scene、逐 ID、full-tree、stage 或 readiness 增量。计数继续保持 T0 `50%`、逐 ID `4/361`、全树定义 `106/626`、完整迁移阶段 `8/11`、总体 `67.5%`，T0-P2 继续 `LOCKED`；该 exact failure shape 不原样重跑。

## R275–R276：guarded startup 越过 R274 后收敛到历史 cold-map VFS RED

- R275 保持 13 文件 harness-only startup overlay，并把 exact-build guarded candidate（DLL SHA-256 `CB7E0B0989CEA3D8B799915A57C80801DE0626A90D3EAA74C8D9EAA1395EB950`；injector SHA-256 `550FC2C5655868A40D0F309DA60C0D52213C0BA05D89C09105A1A8D87B5FDC2D`）注入唯一 `continue-last-save` PID `115468`。该进程不再命中旧 Particle2 `ck3+0x1DABD89`，也越过 R274 的 `ck3+0x3B0AE5F` DX11 pre-draw fault，随后在 loader/map/mailbox/save/gameplay 前命中历史 exact `ck3+0x3BE33A9` cold-map VFS `C0000005`（VA `0x00007FF6BE5A33A9`）。这证明 overlay 与 guard candidate 实际跨过前两处故障，但只暴露下一条既知 startup 分支，不等于 Frontend 或 loader GREEN。
- R275 权威 relay/artifact：`Z:\ck3_mod_rewrite\_runtime\p2r275relay\preflight.json` SHA-256 `A3440D5F30C1CCAA7399DEC3ED6F6E41612477BA52D1661411F4988CA85E918C`；`Z:\ck3_mod_rewrite\_runtime\p2r275relay\execution.json` 为 RED、SHA-256 `6FA7A5C600EF3F8A768B56FC851BEFF62F44710C48FA743CCD91C458D80DFD75`；`Z:\ck3_mod_rewrite\_runtime\p2r275restore\report.json` 为 RED、SHA-256 `042DC1E0E8BC0BCA1D85DE530B4E099FB0BF52E9B75B953356F370CA7AAA38E9`；session-start `Z:\ck3_mod_rewrite\_runtime\p2r275restore\00_phase2_native_session_start.json` SHA-256 `C99D75B195453AB2A8DAEDF14272A644ED34919CF748D5B4B7157A29F9DDCAC4`；loader gate `Z:\ck3_mod_rewrite\_runtime\p2r275restore\03_loader_gate.json` SHA-256 `C5C244E1E2DC9C954CF5DF5BF247AAB7C29C590FEF06E85FA540E4BAF2A0AB3A`；session state `Z:\ck3_mod_rewrite\_runtime\p2r275restore\10_phase2_native_session_state.json` SHA-256 `A9EB4EE4E25C622F575CE63390960B70CA86AB706DB2E5174648B1843484B11B`。crash bundle `Z:\ck3_mod_rewrite\_runtime\p2r275restore_state\profile\crashes\ck3_20260907_213439\` 内 `exception.txt` SHA-256 `5E36173202EDA97641B5A25004ED3EA9788FD3F08F7C850356572220158223E0`，`minidump.dmp` SHA-256 `B3911F7C1846279E2DBF3A0D5F1C9FBE80275FDF16C5CC8E4CE1A53AE042677F`。本轮受管 session report 完整，cleanup 为 `tree_gone=true / cleanup_proven=true / job 1->0 / final inventory empty / watchdog absent / ok=true`。
- R276 将同一 13 文件投影与同一 guarded candidate 改走 `bridge-frontend-first`：warm-up PID `56652` 使用独立 guarded bridge/pipe，但在 Frontend marker、debug log 与任何 map/mailbox 证据前，以 code `1` 再次命中相同 VA `0x00007FF6BE5A33A9` / exact `ck3+0x3BE33A9`。warm-up evidence 为 `status=failed`、Frontend marker `seen=false`、log `0` bytes；为 autosave 配置的 final bridge/process 从未启动，故没有 final binding、存档恢复或 gameplay。
- R276 权威 artifact：recovery report `Z:\ck3_mod_rewrite\_runtime\p2r276restore\report.json` SHA-256 `FD50EEB07DDBC3C7897082A0A67D5F6ACAFBBF9E8A6A5957FEE6800DA9DBECA9`；session-start `Z:\ck3_mod_rewrite\_runtime\p2r276restore\00_phase2_native_session_start.json` SHA-256 `28D02C15388005C1525AAB4868FE35C786808A5788750B92CD6A0FD4966A87A5`；formal cleanup `Z:\ck3_mod_rewrite\_runtime\p2r276restore\09_phase2_native_session_cleanup.json` SHA-256 `984498177CD3810E19A54B7A7366C6381770A489815AB56140FAE3C210E9D0FA`；session state `Z:\ck3_mod_rewrite\_runtime\p2r276restore\10_phase2_native_session_state.json` SHA-256 `308C214BA24DC2283CE8AA5530683E4AFBA0BAA6A836EDD91525DD558C46B7E5`；warm-up evidence `Z:\ck3_mod_rewrite\_runtime\p2r276restore_state\native-session\frontend-first-warmup.json` SHA-256 `206476EF40EC75A710DE1FA4AD8DFD1BEE78E64CD9C0639F0EDF797A1370181B`。crash bundle `Z:\ck3_mod_rewrite\_runtime\p2r276restore_state\profile\crashes\ck3_20260907_214331\` 内 `exception.txt` SHA-256 `F182D0EB087846D0133464A75B4FC8E3BB737E567BD5D73A8542EC5A050E97B6`，`minidump.dmp` SHA-256 `2BC1DA1629EC2F057F2AACFAAB8866D9F3D182B3A56B327B049A7F3E647C7DEE`。
- R276 收口后的实时只读 process inventory 为 `ck3=0 / xar_ck3_bridge_injector=0`，说明现场没有遗留进程；但 formal cleanup 必须保持 **RED**。原因是 warm-up 在 binding 前失败，`binding=null`、native session `report=null`，因此 cleanup artifact 无 initial PID/generation/session report 与对应 managed shutdown proof，多项合同检查按设计失败；不能以事后实时空槽把 formal cleanup 改写成 GREEN。
- R275/R276 都是 startup/harness RED：没有新增事件动作、postcondition、loader database node、paused snapshot 或场景验收。全部口径冻结为 T0 `50%`、逐 ID `4/361`、全树定义 `106/626`、完整迁移阶段 `8/11`、T1 `90%`、T2 当前同步 horizon `100%`、固定总体 `67.5%`；source registry `0/4`、footage `0/8`、最终 MP4 `0/2`，T0-P2 继续 `LOCKED`。`0x3BE33A9` 两种已执行形态不原样重跑。

## R277–R278：正确 no-bridge warm-up 与 typed terminal/cleanup 合同

- R277 是配置错误而不是 CK3 attempt：命令选择 `bridge-frontend-first`，却未提供强制的完整 warm-up bridge DLL/injector/pipe/expected-SHA bundle。child preflight 以 exit code `2` 拒绝，stdout 明确为 `bridge-frontend-first requires the complete explicit warm-up bridge bundle, pipe, and expected SHA-256 pair`；`Z:\ck3_mod_rewrite\_runtime\p2r277restore_state` 与 `...\p2r277restore` 均未创建，CK3 进程为零。relay `execution.json` 的 `ck3_launch_attempted=true` 只表示 relay 已调用 child 的粗粒度意图字段，不覆盖 child 在真正创建游戏/目录前的 fail-closed 事实，所以 R277 不计实机启动次数。权威 artifact 为 `Z:\ck3_mod_rewrite\_runtime\p2r277relay\preflight.json`，SHA-256 `3025501906039A2B96F6510C418C77AD03514FD245220B879A56F084C287E997`；`Z:\ck3_mod_rewrite\_runtime\p2r277relay\execution.json`，SHA-256 `FF1852B808F78820B7D654A6AF8B241DC8894E68ACE9E3628E27BD493291EA02`。
- R278 使用正确的 `bridge-frontend-first` 语义：Frontend warm-up 明确 `native_bridge_mode=disabled`、无 warm-up bridge，只在 fresh profile 投影 13-file startup shader bundle；final autosave process 才配置正式 bridge。唯一 warm-up PID `151976` 在 `2.169s` 内、Frontend marker/debug log/map/mailbox 前以 code `1` 退出，exception VA `0x00007FF6D733AE5F` 按 exact image base 绑定 R274 的 `ck3+0x3B0AE5F` DX11 pre-draw `C0000005`。final process/bridge 从未启动；故本轮证明 no-bridge warm-up 加 13-file overlay 仍命中 R274 边界，不是存档或产品恢复。
- 新 typed terminal/cleanup 合同首次经该真实 pre-binding failure 验收：native session 总结果保持 RED，且 `crash_accepted_as_success=false`；同时写出 `native_session_terminal{state=frontend_warmup_process_exit, stage=frontend_warmup, pre_binding=true, warmup_pid=151976, process_exit_code=1, cleanup_proven=true, tree_gone=true}`。cleanup artifact 独立为 `GREEN / acceptance_scope=cleanup_only / session_result=RED`，所有检查 GREEN，包括 `supervisor_stopped`、terminal/session RED 匹配、warm-up PID/exit code 匹配、final PID absent、binding absent、Job empty、global inventory empty、watchdog/control files absent 与 contract errors empty。这是清理合同 GREEN，不把崩溃或 session RED 改写成业务成功。
- R278 权威 artifact：`Z:\ck3_mod_rewrite\_runtime\p2r278restore\report.json` SHA-256 `D25DC41B5B5986EE87675A7E8753BC56821B3C0D05E9863D928F5BDC22034E6C`；`00_phase2_native_session_start.json` SHA-256 `2657EE81AD1369448214C55284BF018694B18050439C881D915D6F44BDD66110`；`09_phase2_native_session_cleanup.json` SHA-256 `6B37594D5270315B814897CF7B3CA5BE62C914039791B52D95E56BB03FBA1764`；`10_phase2_native_session_state.json` SHA-256 `A18F8A3A31745ADF41C86108A7C27EACD9C6CEA27207A2ACA4B34C045D9A559A`；`Z:\ck3_mod_rewrite\_runtime\p2r278restore_state\native-session\frontend-first-warmup.json` SHA-256 `68C77909F4783D888B67B1FBCEFD884BB17E1C1B5E438897CE744E92B645C373`。crash bundle `Z:\ck3_mod_rewrite\_runtime\p2r278restore_state\profile\crashes\ck3_20260907_220720\` 内 `exception.txt` SHA-256 `4DDE8E042BE63F459D2234AF017388EFC17D79EA6C33FD1E0914056308916802`，`minidump.dmp` SHA-256 `B1EA380B8004D4D645F235D350DC993F8016EDD895AC5E1594A046683AD2435D`。
- 收口时实时只读 inventory 为 `ck3=0 / xar_ck3_bridge_injector=0`，与 typed cleanup proof 一致。R277 无启动，R278 未进入 Frontend/loader/save/gameplay；没有 scene、逐 ID、full-tree、stage 或 readiness 增量。全部计数继续冻结为 T0 `50%`、`4/361`、`106/626`、`8/11`、T1 `90%`、T2 `100%`、固定总体 `67.5%`，source registry `0/4`、footage `0/8`、最终 MP4 `0/2`，T0-P2 继续 `LOCKED`；R278 exact shape 不原样重跑。

## R279：短 Z state 路径 A/B

- R279 只把 R278 的 disposable state/profile 缩短到 `Z:\ck3_mod_rewrite\s279\profile`（约 29 字符级），其余保持正确的 no-bridge Frontend warm-up、13-file startup shader bundle 与 final-only 正式 bridge 配置。唯一 warm-up PID `57688` 在 `2.067s` 内、Frontend marker/debug log/map/mailbox 前以 code `1` 命中 VA `0x00007FF6D733AE5F` / exact R274 DX11 `ck3+0x3B0AE5F`；final process/bridge 从未启动。因此短 Z 路径不是跨过该 pre-draw fault 的充分条件，停止继续做同类路径长度 A/B，也不原样重跑 R279。
- typed terminal 保持 `frontend_warmup_process_exit / frontend_warmup / pre_binding=true / warmup_pid=57688 / process_exit_code=1 / cleanup_proven=true / tree_gone=true`。session/report 为 RED、`crash_accepted_as_success=false`；formal cleanup 为 `GREEN / acceptance_scope=cleanup_only / session_result=RED`，且 `supervisor_stopped`、warm-up Job/global inventory empty、watchdog/control files absent、零 contract error 均闭合。收口时实时只读 process slots 仍为 `ck3=0 / xar_ck3_bridge_injector=0`；该 GREEN 只证明失败后回收，不证明 startup、Frontend 或产品。
- 权威 artifact：`Z:\ck3_mod_rewrite\a279\report.json` SHA-256 `4742B12ED6135A1CA93B109B990A0C14A933176E400705011BC52D96F06E11C4`；`Z:\ck3_mod_rewrite\a279\00_phase2_native_session_start.json` `A5FE1DFD8B4F5DC5822414431EBE892A12643733C7CA6BA4E30FA0A30832DA1C`；`Z:\ck3_mod_rewrite\a279\09_phase2_native_session_cleanup.json` `F22D08C98BB51E94956CBB603DE9D26226642D67E7099EED0307443B8E3C4EA5`；`Z:\ck3_mod_rewrite\a279\10_phase2_native_session_state.json` `03187B0D99EEF12A56C2B3258A87CFAFB522F258544D336F2F953B43DE8A8B58`；`Z:\ck3_mod_rewrite\s279\native-session\frontend-first-warmup.json` `0763E32574FF7AFF1DDA1A6AF6CFF9162B4496488B0840AD2B2A27195CC5403C`；crash bundle `Z:\ck3_mod_rewrite\s279\profile\crashes\ck3_20260907_221635\` 内 `exception.txt` / `minidump.dmp` SHA-256 为 `B8E7C8476D7D057E38A76023714A823A658555711C97BF6A922D87DCC61122E8` / `1BFB693AFC0447FC8EF5FC8BA7941FBBC19DEEF7CEA5E17201C46B639CE53DBA`。
- R279 没有 Frontend、loader、save、map/mailbox 或 gameplay，因此所有场景与 readiness 计数继续冻结：T0 `50%`、逐 ID `4/361`、全树定义 `106/626`、完整迁移阶段 `8/11`、T1 `90%`、T2 `100%`、总体 `67.5%`、source registry `0/4`、footage `0/8`、最终 MP4 `0/2`；T0-P2 继续 `LOCKED`。

## T1 GEN-034 r2/r3 live startup boundary

- r2 新 verify-only receipt 对 14/14 当前依赖与 4,944 files / 215,620,408 bytes warm shadercache GREEN，SHA-256 `3894C49DBB303623339F6468E32150B1BA0FD26ACF58DC1D8947E54BAE946942`。两次纯路径前置 RED 都在 Popen 前结束并保留；真正 execute 启动 ordinary CK3 后，concrete operation 的三处 target identity 仍绕过既有 Toolhelp fallback，因 `Get-CimInstance Access denied` 未能识别唯一进程并立即回收。report SHA-256 `26CC1A3E0692D2E9ABA16283EE072864DA3964B727719D28A4DC68B82CDC20CD`；未到 observer、checkpoint 或 mutation。
- 依据该实证故障完成最小修复：三处 target identity 统一使用 CIM→Toolhelp32 inventory；Toolhelp row 通过 `QueryFullProcessImageNameW` 保留 exact executable path，再严格验证唯一 PID/name/path/EXE SHA，路径查询失败仍 fail-closed。adapter focused normal/`-O` 各 `14/14` GREEN；新 adapter/manifest SHA-256 为 `24C835E6637ED2CF0099704064F99F3F5C2635ED1FC7CB73870C93BA7C28A630` / `087EE3BF8EF711049D02F44A40F8FB22690AF1BD39485363C8879A717C3721DF`。
- 修后 r3 verify-only receipt SHA-256 `F9705D8D02323C2E271CD827003EC44A30D0943B04C8030FD5B80290C41B4486`。execute 已通过目标识别并启动 PID `62524`，但进程在任何日志/窗口/observer 前命中 `C0000005` at `0x00007FF7D823BD89`，随后前台操作只观察到 invalid window handle；因此前台错误是进程先崩溃的后果。cleanup `ok=true`、remaining CK3 `[]`。report / exception / minidump SHA-256 为 `2C590C763759500C1CDB2B99FCD4920FC0F230BAA5E283661510AE6B33C9DF9E`、`3CFC1E79F2A940645A4A5BA551AEC336EF20DB3B4A9E8A8C85AA0D23E6B3B504`、`654FF0BCE2B2EADBA1713CE21B3DD3306CA597F7F455F658F6574B9D3EB0FEC9`。
- r2 的实际 CIM harness fault 已修复；r3 则与 T0 共用同一 startup/environment blocker。两轮均未观察 source-specific loss、未生成 durable checkpoint、未执行 `surrender-war-50331699`，所以 T1 保持 `90%`。本轮仅扩展父仓内部 runner/keyword-only API，open_kaishek 无消费者且 verifier `GREEN_STATIC`，T2 当前 horizon 维持 `100% / NO-CODE-CHANGE`。

## T1 GEN-034 owner utility 与 same-frame white-peace 静态合同

- 新增 owner exit utility model provider 与全空 draft fixture。provider 对缺源、draft、部分填写、budget ID/SHA、非线性/uncertainty/tail-risk rule 或审批元数据不完整全部 fail-closed；不内置默认权重。即使 synthetic approved 输入通过结构校验，`production_live`、same-frame evaluation、comparison、decision/action 与 `gen034_closed` 仍保持 false。draft SHA-256 为 `0C13AE4B9A94319AC5215F03516313AB17DA5C873FB259574566522B3E6AF235`；owner-model focused normal/`-O` 各 `10/10` GREEN。
- 新增 same-frame white-peace comparator、hash-bound prepare CLI、synthetic contract fixture 与专题文档。以 durable pre-mutation source checkpoint 为 snapshot anchor，严格合取 source/white/surrender 的 `snapshot_id`、`revision`、`native_revision`、WarID、日期、PID、primary attacker 与 coarse frame；只输出静态条款差异，production-live/recommendation/action/automatic-surrender/GEN-034 门恒为 false。focused normal/`-O` 各 `39/39`，专项 comparator 各 `7/7` GREEN。
- 集成审计发现 lifecycle 新增 `expected_war_id` 后，outer-owner 默认 continuation 未透传参数；已将默认与注入 continuation 统一显式传递冻结 WarID，并把 manifest 契约纳入 preflight。outer-owner、live adapter、lifecycle normal/`-O` 分别为 `8/8`、`14/14`、`7/7` GREEN。
- 这些交付关闭的是 provider/binding 静态缺口，不是 owner 实际审批、paused/live evaluator、comparison、typed submit 或六域 postcondition。T1 因此保持 `90%`，不能把 synthetic fixture 写成 GEN-034 live closure。
- T2 同步复核为 `NO-CODE-CHANGE`：两个新模块未导出包级公共 API，open_kaishek 对 schema/函数/模块名均为零消费者；WarID 透传只在父仓 research runner 内向后兼容。外仓保持 clean `HEAD == origin/main == 80fb3a4f...`，现有跨仓 verifier `GREEN_STATIC / ok=true`，因此当前同步 horizon 仍为 `100%`。
- 本轮提交门当前为环境阻塞：`git add` 在创建 `.git/worktrees/_root-promo-split-20260902/index.lock` 时返回 `Permission denied`；没有文件被 staged，也未伪称 commit/push 成功。工作树改动与验证证据均保留，待 Git 元数据恢复可写后一次性提交推送。

## T0 R259–R261 ParticleTexture factory 精确诊断

- R259 与 R261 两次独立绑定 exact caller/tuple，均观察到 graphics global 非零、source output `0`、final output `0`，有效分支分类稳定为 `source-lookup-null`。两份 capture artifact SHA-256 分别为 `0E269C37BDE80380AA66AA8D41D68C2933C4AEB699B20E0393026A08A71629BA` 与 `229A98C02B9BDB63053D31824FFAAA29575B2C8F4E6594E08A58EDBD28B7DE6C`。
- 两轮 v1 都在清除 debug registers、continue capture event 后遇 `DebugActiveProcessStop` error 5；进程已受控回收，但 cleanup gate RED，所以只保留“capture-valid / cleanup-red”的诊断边界。R260 是错误 suspend-count 期望导致的 resume harness RED，未观察 tuple 或 factory，不提供业务结论；其 artifact SHA-256 为 `405C36A1E5A1A309ABC1BB32CE08E15E0B09A156B99827F5402D8295A364515E`。
- 已停止重复 v1 detach。v2 静态状态机新增 `0x3AAE920 → 0x3AAE9A8 →（initial output 为零时）0x3AAE9C1 → 0x3A86761 → 0x39C70A6`，分别读取 lookup/population helper 的 `[RSI]` 与 fallback resolver raw `RAX`，并保留 outer/final 交叉核验。成功后采用显式 `terminate-after-capture`，不宣称 native continuation；artifact 新增 probe SHA、capture/cleanup/exit kind 与全 false product-evidence 边界。
- R259–R261 均未到 bridge/map/mailbox/save/gameplay；没有新场景或 readiness 增量。T0 保持 `50%`，strict per-ID `4/361`、full-tree distinct definitions `106/626`、complete stages `8/11`、总体 `67.5%`，T0-P2 继续 `LOCKED`。
- R262 首次完整观察 helper output 与 fallback resolver raw result 均为零，但 exit-event 后零等待检查触发 cleanup-proof RED；capture artifact SHA-256 为 `0E66C3500764B11F5E41973C8B893D5F3869FA6FAD6498D214C5DE945353CB42`。R263 只修该已实证等待竞态，使用 probe SHA-256 `748938B059EDA60AFB8C7C7B0265305482EC4BF08E3A1E6772DF586258EE9E70`，得到 `GREEN_DIAGNOSTIC / source-resolver-null`；artifact SHA-256 为 `87A7213F9B632B96FDD8A28434918B49EE7BBCF801486F3F8DCBB23D250D0C58`。七个同线程 checkpoint、debug-register clear、event continue、意图性终止、exit event/process signal 与最终空进程槽均闭合。
- R263 没有 bridge/map/mailbox/query/input，全部 product-evidence 字段均为 false；它只把 source-null 细化成 fallback resolver null，不证明文件缺失或修复启动。因此上述 T0/coverage/readiness/T0-P2 口径继续不变；下一步先做 resolver 内部 null 分支的静态逆向，不再重跑 R263。

## T0 R280：`pdx_paths` 双 lookup NULL 的实机闭环

- 新增并冻结两组 default-OFF、exact-build、initial-suspended、只读观测器：named-path `0x583` consumer 绑定 resolver `0x3B96C70` 与 `CMap+0x18` move；producer 绑定 `pdx_paths` task、两次 settings lookup、parser entry 与 insert。producer 的五个物理挂点使用单一 near RX allocation、五锚点预验和 all-or-none 回滚，lookup/insert 原函数各只调用一次；第六参数 ABI 已冻结为 caller `[RSP+0x28]` → shim entry `[RSP+0x30]` → callee `[RBP+0x58]`。两组 observer 仅进入 private heartbeat，不发布公共 capability/API/ABI。
- R280 专用 MSVC Release bridge 使用 `/Z7 + /DEBUG:FULL + /MAP` 构建；focused cold-map、named-path 与 producer CTest `5/5` GREEN。DLL 为 `2,493,952` bytes、SHA-256 `50794254F851A78E0AC5522E91F6B7A37A54EA6FD523F92898D28B9D9E0747A4`；injector SHA-256 `B878A07046EFCBD54EB641DCBD61232D0F20375AEC31CFDF9202000C33A7AF58`；PDB / MAP SHA-256 为 `78482CA06009022D08FD5F0C10317577578E20D14D5DB94974BE22FEAD5BCD27` / `F377BBE7A4E7A42E7D780C199F62B9F743700F5E1194D56677B83EA8A7F4E07D`。MAP 直接给出三组 private state RVA `0x6A2518 / 0x6A26C0 / 0x6A3B28`。
- no-launch preflight `READY_TO_RUN` 后只执行一次 guarded `continue-last-save`。PID `91980`、bridge generation `1` 已连接；进程在 `16.143s`、loader database node、日志、map/mailbox、save continuation 与任何玩法输入前以 code `1` 命中 VA `0x00007FF6ACC333A9`。dump module base `0x00007FF6A9050000`，故仍是 exact `ck3+0x3BE33A9 / C0000005`。受管 shutdown 证明 Job active processes `1→0`、`tree_gone=true`、`cleanup_proven=true`、watchdog/control files absent、contract errors 空，事后 CK3/injector inventory 也为零。
- `/Z7` dump 直读闭合了上游因果分支：producer `installed=true / mask=31 / failure=0`；`pdx_paths` task 进入一次，同一 TID `41812` 上 `paths.settings` 与 `paths_checksummed.settings` lookup 都各进入/返回一次且 `raw_result=0 / null=true`；三个 parser count、insert count 与 `0x583` insert pre/post 全为 `0`；task table 为真实空表且无 read fault。因此 R275 留下的三分支已收敛为：**task 已进入，但两个 settings 查询均返回 NULL，所以 parser 根本未启动，named-path 表保持空**。named-path consumer 尚未到达；cold-map 仍记录空 ctor、tag-1 `VFSOpen Error:  not found` 和 state `0` poll，构成上下游交叉证据。
- 权威 artifact：preflight / execution / report / session-start / loader gate / session-state SHA-256 分别为 `46CA98289F073F54C7606B79DC672610AAC0B1D6005D8B803EFBD734F852B95D`、`BAC230D1707ED80BD8882EE69E5E9D4C06419B70D2C515FC9F609A5F88E9BB83`、`9230A6ABB2A27E74211360555A973AC623A6063B21D8A257D11C26711672A2A2`、`7C2333FA216FE60738251675FF29E1EBF498CC70841527783C1470FC19687D74`、`A9B03B9974ED9E6D47A0C78A9C9210BEC2DBD012C11E0DD908DC84FA3109694A`、`D00DC4FA295C4F4B60E6652B9B6CEADF8B34EFDBA408B04C90A343942E1BAE84`。exception / dump / 离线三 observer 解码 JSON SHA-256 为 `4D8B3D138DE1076CB111CCA2F9DF6AC84E6E891C93F5B9A4267EB50F36DC0D8C`、`8CAD7C1D200E565BA69AF3F07C4F28C93A694B743527071EFF5EA993EBEBB296`、`0E95E67496F86285394D78CCFD10F45A84152482247D812EBA1446E9A597C4F6`。
- R280 只缩小 startup/VFS 根因，不新增产品场景证据。T0 `50%`、strict per-ID `4/361`、full-tree definitions `106/626`、完整阶段 `8/11`、T1 `90%`、T2 当前 horizon `100%`、总体 `67.5%`、source registry `0/4`、footage `0/8`、MP4 `0/2` 全部冻结，T0-P2 继续 `LOCKED`。下一步只读逆向 lookup `0x3BE2340` 的输入与 NULL 早退，并追踪 `pdx_paths` 之前的 VFS 初始化，不原样重跑 R280。

## 2026-09-08 00:00 正式收口

### 早会承诺对照

| # | 早会承诺 | 结果 | 证据 / 未完成原因 | 是否顺延 |
|---:|---|---|---|---|
| D1 | 继续 manager-seed 串行验收 | **完成** | R159 已形成 canonical clean same-frame manager seed，candidate/save/receipt/cleanup 均闭合；当日后续不撤销该 GREEN | 否 |
| D2 | 从 canonical manager seed 完成完整迁移树验收 | **部分完成** | R160–R250 将完整阶段推进至 `8/11` 并闭合多帧原版 interrupt；R251–R280 的恢复链仍在 loader 前 startup RED。R280 已证明 `pdx_paths` task 运行但两次 settings lookup 均 NULL，parser/insert 为零；尚无 full-tree GREEN | 是，顺延至 2026-09-08，先解除 VFS 初始化 blocker |
| D3 | 闭合 source registry 与 `8/8` clean footage | **未完成** | T0-P1 未完成，hard gate 禁止进入素材采集；registry `0/4`、footage `0/8` | 是，依赖 D2/full-tree GREEN |
| D4 | 制作并验收两条最终宣传片 | **未完成** | 宣传工具仅完成版本更新与 preflight；未启动 TTS/render/source intake，MP4 `0/2` | 是，依赖 D3 与 T0-P1 hard gate 解锁 |
| D5 | 保持文案修复闭合 | **部分完成** | 简中文案机器规则与人工静态语义审阅保持闭合；动态 scope、换行/截断、正文/按钮职责仍缺 full-tree 实机渲染证据 | 是，搭载后续 T0 实机会话验收 |
| D6 | G2 与 open_kaishek 伴随推进 | **部分完成** | G2 完成 GEN-034 WarID/checkpoint/settings、owner utility provider 与 same-frame white-peace 静态缝，保持 `90%`；下一有效入口是排他 CK3 source-specific lifecycle。open_kaishek clean/pinned、63/63 verifier GREEN，当前 horizon `100%` | 是，G2 live 等待 T0 CK3 槽释放；T2 按公共 delta 触发 |
| D7 | 同步日报、周报与权威证据 | **部分完成** | R151–R280 的 GREEN/RED、artifact/hash、能力边界与 cleanup 已持续落盘；但 `.git` worktree metadata 不可写，`index.lock` Permission denied，无法完成本日 commit/push | 是，Git 元数据恢复可写后一次性提交推送 |

### 计划外完成

- 完成 exact-build Particle2 → DX11 → cold-map VFS 启动故障的逐层只读诊断链；R273 的 13-file harness-only shader closure 只用于越过首故障，不进入产品或 release staging。
- 新增 named-path `0x583` consumer、`pdx_paths` producer 两个 default-OFF private observer，以及 `/Z7 + /DEBUG:FULL + /MAP` dump state 提取器；R280 以一次排他启动把“task 未进入 / lookup NULL / parser 无 insert”收敛为第二项。
- 完成 T1 owner utility 模型 provider、same-frame white-peace comparator 与 WarID/耐久 checkpoint 绑定；所有 synthetic 结果仍明确保持非 production-live。

### 收口时真实状态

- T0：`50%`；central-wired/static-ready `361/361`，strict CK3-live `4/361 = 1.11%`，尚余 `357`；full-tree distinct definitions `106/626 = 16.9%`，完整阶段 `8/11`。
- T1：`90%`；下一项有效工作为 CK3 GEN-034 source-specific lifecycle，或 owner 实际批准 utility model；没有可继续堆叠的高价值非 CK3 包。
- T2：`100%（当前同步 horizon）`；open_kaishek 与已发布父仓边界兼容，R280 observer 仍为 private/default-OFF，无外部消费者。
- 固定加权总体：`67.5%`。T0-P2 `LOCKED`；source registry `0/4`、footage `0/8`、MP4 `0/2`。
- 收口后的首个可施工入口是 exact lookup `0x3BE2340` 的 mount-list/prefix/resolve NULL 分支和 `pdx_paths` 前置 VFS publisher；不得原样重跑 R280 或盲拷 settings 文件。

## 范围边界

- effect 文件继续按用途拆分，目标每文件 `1–10` 个、原则不超过 `20`；不讨论单文件过大是否有问题。
- 玩法观测与操作使用 MCP/native；OCR/image 仅限非玩法 legal-consent/front-end gate。
- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。
