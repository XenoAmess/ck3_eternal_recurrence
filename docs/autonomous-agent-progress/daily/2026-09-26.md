# 2026-09-26 日报（本包滚动增量）

状态：进行中；记录时间 2026-09-26 13:40（Asia/Shanghai）。仅覆盖非战争交接包，其他团队当日工作未在此核实。对应[真实时间补录计划](../meetings/daily/2026-09-26.md)，不据本记录宣布全天或 09-25 已收口。

## 本轮交付与原因

用户将战争研究分配给其他同事。本轮从实际 master `1b57b3301b87231b2b71eeae9edc44e20ee01a33` 核对合同、专题、生活方式及联合调度代码，形成[非战争优先交接](../2026-09-26-g2-nonwar-priority-handoff.md)。后续首项为生活方式机会消费，接着建设经济、婚姻继承与非战争资源分配；战争定期 fetch/rebase 后消费。

| # | 计划对照 | 状态 / 证据 |
| --- | --- | --- |
| N1 | 非战争现状及有界产物 | 完成文档：复用 R0081 建设、R0186/R0187 生活方式、R0126 exact `.1007`；指出 M5 已有分析核，尚缺真实估值及正式消费者 |
| N2 | 战争同步与交接导航 | 完成文档：明确同步节点、运行候选冻结、独立非战争进度；未安装后台同步任务 |
| N3 | commit/push/集成/清理 | 随本包交付执行；最终 exact SHA、CI 和清理记录以交付回执为准，不在提交前预填成功 |

## 证据层级与状态

- 新增的是源码/文档核查与队列调整；没有新增 no-launch、只读实机、typed 动作、下一 turn 或日期。
- G2 **3/8**；最近文档记录 Robert **2,983/36,524 天**，百年 **0/1**、首整局 **0/1**、独立种子 **0/2**。本轮未提升 readiness，未重新盘点当前运行状态。
- PRV008 历史窄范围 GO 保留；09-24 核验的 ZIP/说明/配对继续原地保留，本轮未验证其他机器下载渠道。
- 文档完成 diff 与 24 个本地链接校验。首提交 `db3927d6cb4e6ce34722be5a440520151147770c` 已推送 PR #244；官方 CI `36221771531` / `36221787656` 在 checkout 因主线已有文件路径过长失败。计划外追加 `static-ci.yml` 作业级 `core.longpaths=true` 环境修复，保留原失败并由官方 CI 验证；游戏代码无修改，不重跑实机或全仓本地测试。

## 14:40 非战争接班滚动增量

接班从已集成交接 `db0b862` 开始，按生活方式、建设、家庭及联合调度拆成独立 Z 盘源码工作区。代码与证据范围如下；**本次仍为源码、聚焦测试与官方 CI，没有新 no-launch、CK3 查询、typed 动作、日期或冷恢复**。

| 包 | 真实缺口与变更 | 截至本次记录的交付层级 |
| --- | --- | --- |
| NW-LIFE | 正式战时 `query-combat-simulation-inputs-v3` 只读计划会绕过已 opt-in 的可用 perk 消费；`service.py` 现在先检查原生 final-legal perk，保留战争动作优先级与 pending receipt 消费。已核 R0186/R0197 实际取得 `professional_workforce_perk` / `centralization_perk`；当前 h2134 为点数 0/7，本包不冒充新增实机 perk。 | 聚焦 19/19；原 `2f63acb` 经 rebase 内容不变，以 `fb8989c` 进入 master，exact master CI `36224120002` 成功。PR #245 因同 patch 已在线性 master，核实内容后关闭；远端/本地临时分支及源码 worktree 已清理。 |
| NW-ECON | 已核 applied receipt 曾永久屏蔽同 episode 后续日期的新建设机会；仅在同 PID、后续游戏日/native revision 且前一 receipt 已验证时重新评估，pending、同帧及冷恢复守卫保留。 | 聚焦 14/14；`c0c7c52` 已进入 master，PR #246 合并，exact master CI `36224014643` 成功；远端/本地临时分支及源码 worktree 已清理。开工后的完工/收入仍无本轮新证据。 |
| NW-FAMILY | 五个 final-legal 婚配候选新增同一原生 finalized context 的 `predicted_outcome_if_accepted`，区分结婚/订婚与父系/母系；无结果时给独立失败原因，不把 8 个 `would_attempt_if_accepted=false` 潜在联盟记成价值。 | 聚焦 Python normal/`-O` 各 6/6、C++ 1/1、exact ABI verifier 两项及 Release DLL 构建通过，DLL SHA-256 `63B4A53AC3943F4751A993CE70A68B8DB4C7B66B1A211594A4FACE2EBC27BE5F`；PR #247 经保护检查以 rebase 进入 master `7d17ffc`，exact master CI `36224157313` 成功。旧分支已删除，原源码工作区已转给 FAMILY-VALUE。无实机 proposal。 |
| NW-JOINT | 联合 collector 曾把已核 applied 建设 receipt 当成整 episode 永久消费；后日新 revision 的建设 proposal 现可重新进入既有选择器，pending/冷恢复不重提。 | 聚焦 11/11；候选 `7b32f37` 的 PR #248 经 CLA/签名/双 static 成功后以受保护 rebase 进入 master `356d356`，exact master CI `36224481358` 当时仍在运行。`selected_step=null`/`formal_action_ready=false` 未被强改，仍无正式 M5 动作。 |

第一批并行由本执行者负责 LIFE、独立 worker 负责 ECON 与 FAMILY；其后 ECON worker 承接 JOINT。当前追加 FAMILY-VALUE 首继承人关系观测与 ECON-VALUE 建筑收益字段的定向工作，二者共享 native bridge 写入范围须串行。任务总线 `Z:\\.codex-task-bus` 已登记 LIFE/JOINT；等待官方检查或实机的任务不计有效占用。战争维护者身份和受管全局实例 owner 仍未从正式 live source 核实；本机未启动 CK3、未操作桌面，窗口最小化状态不适用/未知，未占用唯一实机队列。

此次 LIFE 线性 `git push HEAD:master` 被 GitHub 明确报告为管理员例外绕过 `CLA / signed` 分支保护。PR 分支本身的 CLA/签名及双 static 在推送前均成功，随后 exact master CI 成功，但这**不抹去流程偏差**。发现后已停止直接推 master；FAMILY 改用 `gh pr merge --rebase` 的受保护路径，后续同样执行。

PRV008 冻结 ZIP、说明、配对及 `PRV008-FROZEN-EARLY-PAIR` 在本机核实存在，资格仍限旧 actor 31853 有界普通封建路径；其他机器实际可获取性未测。新 Robert/非战争候选不复用 PRV008 资格。现有 WarID16777231 的 Robert RED 尚未解除；最近合法文档基线仍为 h2134/raw53215920，持久 **2,983/36,524 游戏日**，G2 **3/8**，百年 **0/1**、首整局 **0/1**、独立种子 **0/2**。当前轮次/PID/owner、其他团队是否有新 live 与本地新候选的正式配对均未知，不能据本包源码/CI 提升 readiness。

## 阻点与下一步

生活方式仍是有限私有策略；建设收益与婚姻物质结果不完整；M5 缺真实统一估值/消费；议会、派系与部分 M2 门缺自然场景。交接为每项指定了复用入口和下一个可交付结果。接班先做 `NW-LIFE` 正式入口/触发追踪，有独立容量再推进建设和家庭投影；自然场景等待不计活动 worker。

本包隔离分支 `wip/nonwar-priorities-handoff-20260926`，源码与临时目录在 `Z:\ck3_mod_rewrite\.task-tmp\G2-NONWAR-HANDOFF-20260926`。根目录历史脏现场、其他包清理阻塞和全部运行资产均保留。
