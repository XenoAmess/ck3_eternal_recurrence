# 2026-09-14 daily report

Status: **in progress**

Formal close: 2026-09-15 00:00 (Asia/Shanghai)

Morning meeting: [`../meetings/daily/2026-09-14.md`](../meetings/daily/2026-09-14.md)

Starting branch/HEAD: `root-rebase-r345-20260909` / `3327e91e747deda4c3f6b6c820bb7435e7bc3509`

## Work completed

- Formally closed the 2026-09-13 daily and W37 reports at 00:01, then opened the 2026-09-14 and W38 planning records without backdating the in-flight build.
- The first static playable-selection package was disproved by three managed live attempts: selecting a featured bookmark character and then invoking `Pick Any` does not carry that selection into the free-pick lobby. All three attempts retained Steam-offline, zero-desktop-input and cleanup GREEN evidence; none is a capability GREEN.
- Replaced that invalid state model with the vanilla lobby's fixed `SetRandomPlayableObserverCharacter` button at live-tree path `4/0/1/0/1`. The composite remains zero-input, verifies that exact unnamed target before activation, and independently waits for the default ruler-designer button at `3/0/2/3` to become enabled.
- Release build/link, adapter registry, exact-EXE mailbox/source-contract, focused contract/service/native-driver/official-MCP and reconfigured full CTest `106/106` are GREEN. The initial full run's 9 environment REDs came from a cached nonexistent in-repository EXE path and disappeared when the same build was configured against the exact installed EXE. Current DLL: 2,701,312 bytes, SHA-256 `5573805A227AE86683FD7C846B8E4BA621F60CF10725067ADC55D1055A24456C`.
- `open_kaishek` prevalidation is `not-applicable`: it has no frontend GUI/CoA domain and this additive route has no companion consumer shape.
- Updated the managed frontend acceptance runner to require all six closed tools and the corrected random-playable bridge capability, then execute `main_menu -> bookmarks -> lobby -> selected playable -> ruler_designer` exclusively through official MCP. Its final inspector must be scoped to `ruler_designer`.
- The owner has now prohibited starting or occupying CK3 and the screen until explicitly lifted. CK3 was already absent and remains absent; only source, tests, docs, Git and retained-artifact analysis continue.
- Hardened the Vue editor's deterministic export boundary without CK3 or screen use. It now rejects unresolved/cyclic/duplicate static variables, wrapper-external values, unsupported color tagged blocks, non-ASCII resource names, non-finite structured values and output beyond the native 128 KiB limit even after form edits. Frontend tests pass `32/32`; production build is GREEN.
- Added a machine-readable, user-visible syntax capability matrix sourced from the report's exact-build MCP evidence. It distinguishes importable, ambiguous and non-executable families; each row carries an example, native result and editor policy. Matrix/parser/API/DDS/renderer tests pass `36/36`; production build remains GREEN, with no CK3 or screen use.
- Ran the bounded GEN-034-C admission and first production attempt. Current round R640 stopped before launch because the system Python lacked `win32api`; current round R641 used the shared project environment, completed both allowed native queries in one paused frame, and then retained a contract RED because the options normalizer rejected its own normalized `source=native` output.
- Repaired that idempotence defect without changing the native wire: raw producer payloads and already-normalized public payloads are both accepted, while any other source remains invalid. The live helper now preserves a typed `evidence_required` projection without invoking the utility evaluator. Focused normal and optimized tests pass `18/18` in each mode.
- Current round R642 stopped in no-launch preflight on an incorrect injector basename. Current round R643 then proved launch and cleanup but timed out while CK3 was still loading. New round R644 reached the exact paused frame, completed the two allowed official-MCP reads, preserved both typed white-peace blockers, and performed no evaluation or action. The same frozen frame will not be rerun again.

## Current work

- Continue the CoA capability report, corrected MCP contracts and Vue editor using source files and retained artifacts only. The next CK3 live attempt and every screen-dependent action are paused by owner instruction.
- Offline-only preparation for the bounded Raiktor checkpoint may continue, but no CK3 evaluation or war-termination action may run during the hold.

## Why this work

- The frontend package replaces a desktop-input chain with semantic MCP. The GEN-034 work now distinguishes a valid unavailable option from a consumer failure and moves to the first frame where the option can actually participate in a three-way decision.

## Evidence and validation

- Accepted build: `C:\xb\coa-rd-20260913-3`; source fingerprint `BB40E68A...94C07`; DLL `38BDED23...DA591`; injector `D51CBFA7...3FEBB`.
- Earlier long-path and missing-executable build failures remain retained environment diagnostics. The corrected fresh build is the accepted static validation.
- R644 exact-build proof passed all `13/13` checks. Its two query results are non-error, the before/after frame binding is unchanged, the only native-history delta is the two read-only commands, and cleanup/source invariants are GREEN.

## 01:40: CoA runner shared CK3-slot ownership

- The owner lifted the no-CK3 hold but required coordination through the shared
  CK3 slot. The CoA frontend live runner now acquires the installation-wide
  `exclusive_launch_lock` and its isolated-profile `exclusive_state_lock`
  before launch, retains both through managed shutdown, and records acquisition
  and release in the live artifact. A busy slot therefore fails closed without
  starting CK3.
- The focused runner contract test passes `1/1`; Python compilation and
  `git diff --check` are GREEN. A broader environment suite reached 47 passing
  tests and one skip, while 10 unrelated prepared-profile tests remained
  environment RED because their ignored in-repository `Crusader Kings III/`
  fixture is absent. No CK3 process or screen was used for this work package.

## 02:08: CoA ruler-designer live GREEN and fixed CoA-page MCP

- The managed official-MCP route is GREEN through
  `main_menu -> bookmarks -> lobby random playable -> ruler_designer`.
  Artifact `artifacts/coa-clipboard-probe-2026-09-08/mcp-frontend-route-ruler-designer-live4.json`
  is 911,495 bytes, SHA-256
  `23F752FF7FF37A5354BD35DE92777EFDA746225DC4FDE980A1FD6DA064666CAE`.
  Every route/action/no-input check is true; Steam stayed offline. Shared launch
  and state locks were acquired and released, and PID 4848 plus its watchdog
  were proven absent after cleanup.
- The live `ruler_designer` tree fixes `dynasty_house` at
  `0/0/0/0/0/0/0/3` and its two-child Hub row at
  `0/0/0/0/0/0/0/3/1/0`. Vanilla
  `window_ruler_designer.gui:514-548` identifies the second child as the
  unnamed button that calls `OpenDynastyCoatOfArmsDesigner` and sets the
  dynasty CoA page variable.
- Added the closed zero-input capability/tool
  `game.command.activate-frontend-coat-of-arms-designer-v1` /
  `ck3_activate_frontend_coat_of_arms_designer_v1`. It accepts only the fixed
  leaf `0/0/0/0/0/0/0/3/1/0/1`, requires visible/enabled runtime state, and
  independently proves the `coat_of_arms_designer` route plus visible
  `coat_of_arms_page` before reporting verified.
- Exact-master Release DLL is 2,702,848 bytes, SHA-256
  `973B9EB1A4BAA926811CD06237A8B8173CC4459C7F9140221AFD45242E537095`.
  Python normal/optimized suites pass `11/11` and runner contracts `2/2` in
  both modes. Native CTest first passed 100/106; all six REDs were existing
  source-contract pins for capability count 88. After updating the exact pin
  to 89, those six pass 6/6, yielding equivalent `106/106` coverage without
  rerunning unchanged tests.
- The first live attempt for this final transition did not start: preflight
  found PID 10560 already owned by
  `tools/run_auto_upgrade_buildings_acceptance.py`. CoA performed no injection,
  input or cleanup against that process and yielded the shared slot. Live
  remains queued while offline/editor work continues.
- The Vue/Quarkus editor now exposes the same fixed action as
  `POST /api/ck3/coat-of-arms/open-native-designer`; the Java MCP allowlist
  grew by exactly that one zero-input tool. The UI accepts only the verified
  action with the `coat_of_arms_designer` after-route. Vitest passes `37/37`,
  the Vite build is GREEN, and Quarkus REST tests pass `11/11` inside a GREEN
  Maven package.
- The final managed official-MCP run is GREEN through the native CoA page.
  `mcp-frontend-route-coa-page-live5.json` is 1,185,843 bytes, SHA-256
  `6BE30B3C356CCE22D279FD1906BAFE6474451D7FFCB5EF2229E5B538A0106103`,
  and binds source commit `c3f074a8f8f6717dd0a40f23b796c6d8a2d23881` to the
  2,702,848-byte DLL above. The independent postcondition observed visible,
  enabled `coat_of_arms_page`, `dynasty_detail_input` and
  `dynasty_finish_button`; every route, native-backend and zero-input check is
  true. Steam stayed offline, both shared locks were released, and PID 19424
  plus its watchdog were absent after cleanup. This closes page entry only;
  upper-level Finish and persistence remain outside the proof.

## 02:54: queued MCP-only CoA syntax expansion

- Added an opt-in syntax collection phase to the same managed frontend runner;
  the default route-only behavior remains unchanged. After the independently
  verified CoA page, it resolves either the live snapshot revision or the
  exact frontend revision-zero binding, then uses only the official probe and
  Copy/export MCP tools.
- The checked-in v1 matrix freezes 15 exact ASCII sources: two applied
  regression controls, eight detect/apply/Copy observations for the unresolved
  precedence/inheritance/canonicalization edges, and five rejected controls.
  A negative case is complete only if native Copy returns the same source hash
  before and after it. Matrix SHA-256 is
  `ADFBB02A097A5B13452743761D693A78FBB988570DEE3A80A5EFEC764F6BA13F`.
- Runner compilation and normal/optimized focused tests pass `4/4` in each
  mode. No CK3 process or foreground screen was used. Live collection remains
  queued under the owner's current prohibition and no syntax claim was
  upgraded from static preparation.

## 03:04: frontend-safe CoA source binding

- Closed an editor integration gap exposed by the native contract: probe and
  export accept exact frontend revision zero, but the UI previously required a
  gameplay snapshot before every request. The companion now derives a typed
  source binding from `ck3_get_capabilities`, returning a positive live
  snapshot revision when present or revision zero only for a connected exact
  frontend bridge.
- The endpoint validates native-headless/named-pipe provenance, CK3
  `1.19.0.6` and EXE SHA, connection generation/PID, build match and both CoA
  capabilities. Disconnected or malformed states fail closed. Runtime-feature
  reads still require the separate positive gameplay snapshot path.
- Vue now uses this binding for connect/probe/export. Vitest passes `38/38`,
  Vite production build is GREEN, Quarkus REST passes `14/14`, and the Quarkus
  package builds. No CK3 or foreground screen was used.

## 03:08: deterministic mask input boundary

- The editor previously filtered invalid mask tokens out of direct form input,
  which could silently change user intent, and accepted arbitrary finite mask
  numbers. Exact vanilla CoA corpus inspection finds only `{ 1 }`, `{ 2 }` and
  `{ 2 3 }`; the shipped shader exposes three R/G/B regions, while prior MCP
  evidence also detects `{ 1 2 3 }`.
- Form parsing now retains invalid numeric results so validation can block
  output, and the deterministic model accepts only integer region indices
  1-3. Focused coverage includes 0, 4, decimal and NaN rejection. Vitest passes
  `39/39` and the Vite production build is GREEN. This was installation-file
  inspection plus offline frontend work only; CK3 and the screen were unused.

## Readiness change

| Capability | Before | Now | Remaining gate |
|---|---|---|---|
| Fixed playable selection | Invalid featured-bookmark/Pick-Any model | `production-live primitive` | No primitive gate; broader workflow remains separate |
| Lobby to ruler designer | Native path identified only | `production-live primitive` | No primitive gate; upper-level Finish remains separate |
| Ruler designer to CoA page | Live tree identified the fixed leaf | `production-live primitive` | Syntax expansion and upper-level Finish remain separate |
| G2 / GEN-034 | `1/8` / `2/4` | C has production-live negative evidence; count unchanged | One available same-frame evaluation, then decision/action work |

## RED / blockers

- R640 and R642 are retained no-launch harness REDs. R641 is the repaired consumer-contract RED. R643 is a retained cold-load timeout with cleanup GREEN.
- R644 is the authoritative negative live result: the frozen zero-day, zero-score frame returns `white_peace_native_option_unavailable` and `white_peace_final_recipient_response_unavailable`. The broad termination-preview crash RED remains open and dispatch remains disabled.
- CoA ruler-designer `live1` entered an unselected lobby; `live2` and `live3` failed closed later in the invalid featured-bookmark/Pick-Any chain. Their SHA-256 values are `CDCCD36E...3C92E`, `01FFD583...DEAF` and `F2C61695...6636`; cleanup is GREEN and they do not classify any clipboard syntax.

## Files and commits

- CoA implementation: `ck3_autonomous_player/native_bridge/`, `ck3_autonomous_player/src/xar_autoplayer/bridge/`, focused unit tests and `docs/ck3-native-ai/frontend-gui-route-v1.md`.
- R640 report: `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r640-dc42838\report.json`, 8,057 bytes, SHA-256 `656C26958FF36D3FEB0FF90623F15D9324B9BEEAEAD90578C753CF78F240658C`.
- R641 report: `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r641-dc42838\report.json`, 94,506 bytes, SHA-256 `AAF02DFB7B678C1889FB14D6C37DCBE7A48A907FE68DB0D23272F4BA33D15370`; retained driver state SHA-256 `10792DC261D8D51083FF2A4C570696CF70AA703A1998902551EB63E45FCC0807`.
- R643 report: `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r643-63bb714\report.json`, 9,589 bytes, SHA-256 `BC1F0F89C05734CA56AB7B5FFC2E0CD46D2B03A0E28E516069E817B7155B6F54`.
- R644 report: `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r644-63bb714\report.json`, 3,509,196 bytes, SHA-256 `9844B817995A876C1B48C7BDDF42826B4866F8E8C9680EB79F0EA53D4FECBA0B`; retained driver state SHA-256 `7D92159CC19E988600A713A2BA0217A4B99133BD18183ED4FD1D9765EEB9845E`.

## Next step

1. Continue non-CK3 CoA work: consolidate the production-live MCP route contract, capability report and Vue editor against the already proven parser/resource subset.
2. Keep the prepared syntax-matrix MCP runner queued; do not start CK3 or use the screen until the owner explicitly lifts the hold.
3. Reuse an existing eligible Raiktor checkpoint if one exists; otherwise prepare its bounded offline harness without launching CK3.

## Range boundary

- Steam remains offline. No Workshop operation, arbitrary GUI API, Tianchao publication, promotional video or general religion expansion is in scope.

## 01:04：GEN-034-C R645 and bounded horizon runner

- Current round R645 reused the existing R456 pre-action checkpoint at date
  `53187096`, 135 war days after the zero-day source. It completed the same two
  official-MCP reads on CharacterID `29829` and WarID `33554473`; white peace
  still returned `native_validator_passed=false`, `available=false` and an
  unavailable final recipient response. Exact-build, same-frame history,
  cleanup and unchanged-source checks are GREEN. Its 3,738,267-byte retained
  RED report is
  `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r645-3c7e79c\report.json`,
  SHA-256 `C6858122FCC4120E4715E220D4B711BF05180B741C294185A69C4BFE51A201FE`.
- This proves that the nearest existing 135-day checkpoint is insufficient;
  it will not be replayed. A reusable managed horizon runner now advances that
  immutable source once toward the already-frozen 365-day admission boundary.
  It allows one resume, at most one pause and read-only snapshot polling. At
  the horizon it queries options once and saves only after admission. At an
  interrupting event it instead captures the exact native event context and
  saves the still-unresolved event frame. It never selects an event or submits
  a war-exit action. Focused normal and optimized tests pass `4/4` in each mode.
- GEN-034 remains `2/4` and global G2 remains `1/8`; current round R645 and all
  older rounds are terminated, CK3=0. The next launch is one bounded horizon
  preparation attempt from the R456 source, not a repeated evaluation frame.

## 01:20：GEN-034-C R646 event interrupt RED

- Current round R646 was the first bounded horizon preparation attempt. It
  issued exactly one `resume-map` and advanced the same war from day 135 to day
  158 (`date_raw=53187648`), where `active_event instance_id=7` appeared. The
  runner stopped without selecting the sole visible option, ending the war or
  saving over the immutable source. Exact-build checks, source invariance and
  process cleanup are GREEN; CK3=0.
- The 112,046-byte report is
  `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r646-a1455d9\report.json`,
  SHA-256 `47B9F94BFDAA12DB47435351F958C3A6C608745B82AE942D5A3782AC17808AA9`.
  Its outer `paused double-sample MCP proof failed` label came from the shared
  acceptance wrapper; the retained snapshot samples prove the actual stop was
  the event interrupt. The runner now emits the typed stop reason directly.
- Because the lossy snapshot exposes no event definition key, the next launch
  is a single capture pass to the same deterministic interrupt. It will pause,
  query `ck3_query_current_event_window_context_v1`, save the unresolved event
  checkpoint and exit RED. Only after that exact-build identity is reviewed may
  a safe option be selected under the vanilla-event SOP.

## 01:29：自动升级建筑四期超直辖策略预研

- 完成“启用自动建造时选择超直辖暂停或继续修建”的需求分析，状态为 `research / plan-ready`；本工作包没有修改 Mod、启动 CK3、占用屏幕、切换 Steam 或执行 Workshop 写入。
- 原版 1.19.0.6 的超直辖警报使用 `domain_limit_available < 0`，因此四期冻结 `0` 为正常、`-1` 起暂停；暂停只跳过本轮 `.0004`，15 日全局循环继续运行，回到上限后自动恢复。
- 决议 schema 每个决议最多一个 custom widget，而三期资金选择已占用该位置。首选方案是在同一原生滚动列表中提供 3×2 六个资金/超直辖组合；若一次真实 UI 门禁证明不可清楚使用，才退到确认后的原生二选一事件，不开发自制双选择器 GUI。
- 用单一 `aub_pause_when_over_domain_limit` 覆盖 flag 表示暂停；无 flag 继续修建，保证 3.0.0 已启用旧存档不改变行为。实现与抽样验收预计 3.25–5.25 小时，计划见 [自动升级建筑四期](../../auto-upgrade-buildings-phase-4-plan.md)。

## 01:32：GEN-034-C R647 exact event capture

- Current round R647 repeated only the bounded preparation route needed to
  identify the interrupt. After the sole `resume-map`, it paused at war day 158
  (`date_raw=53187648`), queried the official current-event MCP and saved the
  still-unresolved frame. The event is `chancellor_task.1104`, instance `7`,
  with root CharacterID `29829`, five saved Character scopes and one shown,
  enabled native option `0`. No option or war-exit action was submitted.
- The exact source is
  `events/councillor_task_events/chancellor_task_events.txt:765-783`, SHA-256
  `EAF95612E4AEC6BF0CEDBC1ACA1C66C8087DD280BC42A60296F824437A5A46EB`;
  `.1103` calls `.1104` at line 727. Its only authored continuation grants the
  selected neighboring ruler a timed positive opinion of the root. The shared
  exact-build registry already contains this event and the same role-alias
  contract, so no new event record is needed.
- The unresolved checkpoint is 69,333,480 bytes, SHA-256
  `01CAACED503F803E201CA4646D598DA0AAA152452E198D8CE719A77A48DB49B2`.
  The 120,058-byte RED report is
  `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r647-1975271\report.json`,
  SHA-256 `B9EC5A960EDBD96A4B82E88F530FAF9E4362DB8C94FD43AA99D70481EFE35DA7`.
  Cleanup and source invariants are GREEN; CK3=0.
- The remaining blocker is the direct registry consumer: it currently rejects
  the already-recorded `character_scopes`, `character_scope_matches_any` and
  `character_scope_differs_from` fields as requiring an extended consumer.
  The next package is a focused, fail-closed implementation of those three
  relationship checks, followed by one checkpoint continuation. GEN-034 stays
  `2/4` and global G2 stays `1/8`.

## 01:46：GEN-034 event relationship consumer

- The shared `chancellor_task.1104` contract now explicitly publishes its five
  saved-scope names/types and native `[0]` option projection. The direct policy
  validates exact player binding, non-player exclusions, the two chancellor
  aliases and the distinct neighbor relation. Admission is limited to this
  reviewed event key; any missing identity or relationship drift stays blocked.
- Focused registry, embedded-analysis and migration-parity tests pass
  `24 passed / 200 subtests` under normal and optimized Python. An offline replay
  of R647's actual MCP event context returns exactly
  `recommended / option 1 / native 0 / failed_checks=[]`.
- This is a reusable Python registry improvement with no public schema, native
  ABI or game-file change. It does not itself resolve the live event or advance
  time. GEN-034 remains `2/4`; CK3 remains absent.

## 02:03：bounded registered-event checkpoint continuation

- Added a reusable managed runner that cold-restores any caller-supplied paused
  war/event checkpoint, queries the exact event through official MCP, requires
  a `recommended` result from the shared exact-build registry, selects exactly
  that bound option and saves one successor checkpoint. It cannot resume time,
  invoke the broad preview or submit any war-exit action.
- The runner binds event instance, public revision, event key, player, war and
  date before mutation; afterward it requires the event to be closed while the
  same paused war identity remains. Missing or drifted registry relationships
  stop before selection. Focused normal/optimized tests pass
  `14 passed / 7 subtests` in each mode; Python compilation is GREEN.
- This package adds no MCP wire or native ABI. The next step is a no-launch
  identity preflight and one current round from the immutable R647 unresolved
  checkpoint. CK3 remains absent and GEN-034 remains `2/4`.

## 02:10：registered-event continuation no-launch admission

- The no-launch preflight is GREEN against the immutable R647 bundle: event
  checkpoint SHA-256 `01CAACED...49B2`, driver-state SHA-256
  `FDAA47C6...48D9`, exact CK3 executable `2D00FF31...DB86`, bridge DLL
  `C70BC0B6...0E2C`, injector `65830F17...F9F7`, CharacterID `29829`, WarID
  `33554473` and date `53187648` all match their internal anchors.
- The 3,462-byte report is
  `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-event-continuation-preflight-5ef1cc7.json`,
  SHA-256 `228692C3518AAA37A1F14CB2AF75A669A45A533C30F0C3CB6EDAB80DCEE3AD65`.
  It confirms `ck3_started=false` and `profile_prepared=false`.
- The owner has temporarily prohibited CK3 use. No new CK3 round is created;
  CK3 remains absent. Live continuation stays queued while offline G2 work
  proceeds, and GEN-034 remains `2/4`.

## 02:04：GEN-034-D v2 three-way recommendation

- Added a bounded recommendation provider that joins the existing same-frame
  white-peace/surrender utility result with the v2 measured strategic-power
  certificate. Continue is valued as zero baseline minus the versioned
  power-relation tail penalty; the provider does not claim a campaign forecast.
- A unique margin winner maps to exactly one semantic action. Static inputs
  emit no action; production-live same-frame inputs can emit only `resume-map`,
  `offer-white-peace-{WarID}` or `surrender-war-{WarID}`. Submission and all
  route-specific postconditions remain separately unverified.
- Focused normal and optimized tests pass `7/7` in each mode, covering all
  winners, underdetermination, missing dominance, cross-frame rejection and
  action suppression. No CK3 or desktop resource was used.
- GEN-034 remains `2/4` and G2 remains `1/8`. D is static-ready but blocked on
  C plus one same-frame power refresh, one action, postconditions, checkpoint
  and cold restore. No public MCP/native ABI or open_kaishek shape changed.

## 02:08：GEN-034-D same-frame recommendation runner

- Added a managed read-only runner for the future white-peace horizon
  checkpoint. It performs one options read, one terms read and two stable power
  reads on one paused frame, then builds the v2 recommendation in process.
- The exact history delta contains only those four queries. A GREEN report must
  contain one production recommendation and one typed planned action, while
  mutation commands, time advance and action submission remain absent.
- Focused normal/optimized tests pass `4/4`; Python compilation and CLI loading
  are GREEN. No CK3 or desktop resource was used. GEN-034 remains `2/4`, G2
  remains `1/8`, and the next offline package is the bounded action and
  postcondition executor.

## 02:13：GEN-034-D exact action admission

- Added a pure action gate between the recommendation and managed executor. It
  binds the certificate hash to the current paused snapshot's revisions, date,
  connection, episode, PID, WarID, player and opponent, requires no active
  event, and checks the exact advertised step plus typed exit capability.
- The gate emits one expected-revision authorization only for production-live
  inputs. It never executes the command and leaves ACK, postconditions, cold
  restore and GEN-034 closure false. The recommendation runner now records this
  gate output alongside the read-only plan.
- Related recommendation/runner/gate tests pass `18/18` under normal and
  optimized Python; compilation is GREEN. No CK3 or desktop resource was used.
  GEN-034 remains `2/4`, G2 remains `1/8`, and open_kaishek is unaffected.

## 02:27：GEN-034-D post-action prestige observation

- Added additive signed Q100000 `played_character_prestige` to the generic
  native state snapshot and strict Python normalization. It reuses the same
  exact-build `extension+0x130` leaf already used by the war-exit terms reader.
- This removes the concrete post-action visibility gap for comparing the
  frozen attacker prestige delta after the old WarID disappears. It does not
  claim an action or postcondition.
- Native fixture is GREEN; focused normal/optimized Python tests pass `2/2`
  each, compilation and diff checks are GREEN. No managed CK3 round was
  created or used. One externally started CK3 process was observed and left
  untouched under the owner's hold.
- The change is an additive public snapshot field, so open_kaishek requires a
  companion compatibility record. GEN-034 remains `2/4`, G2 remains `1/8`.

## 02:39：GEN-034-D frozen postcondition expectations

- Replaced the recommendation certificate's names-only termination checklist
  with concrete route-bound expectations: exact gold/prestige pre, delta and
  post values; WarID/opponent; truce days; destroyed cleanup; and cold restore.
- The live gate is now exactly the milestone requirement of
  `WarID/loss/truce/resources -> checkpoint/cold restore`. Claims, prisoners
  and favor remain utility inputs without expanding D's postcondition scope.
- Recommendation/action-gate tests pass `14/14` under normal and optimized
  Python; compilation and diff checks are GREEN. No CK3 process was started,
  attached to or read, and no public MCP/native interface changed.
- Root prestige snapshot commit `c17e3d8c0ae9c91d9c056f6f6fb0186c1bbba4fb`
  and open_kaishek compatibility commit
  `0180ca3d3140b54f82fdca8de00cd26b121f42a1` are pushed and synchronized.
  GEN-034 remains `2/4`, G2 remains `1/8`.

## 02:59：GEN-034-D pure postcondition verifier

- Implemented the pure verifier for the six route-bound D expectations. It
  consumes standard action/snapshot/save/restore results plus one
  authorization-bound loss/truce evidence packet; it performs no command or
  CK3 access.
- ACK is submission evidence only. Termination closes only after WarID,
  gold, prestige, directional persisted truce, source-specific cleanup and
  cold-restored identity all pass. Continue can verify its successor frame but
  cannot close GEN-034.
- Focused recommendation/action-gate/verifier tests pass `20/20` in normal and
  optimized Python; compilation is GREEN. No MCP/native schema changed, so
  open_kaishek is unaffected. GEN-034 remains `2/4`, G2 remains `1/8`, and
  live C/D execution stays queued under the owner's CK3 prohibition.
## 03:17: GEN-034-D source-bound postwar evidence composer

- Added a pure composer for the action-bound source-loss and persisted-truce
  evidence consumed by the six-check verifier. It binds the retained source
  capture to the exact full-generation regiment/army set on the authorized
  action frame, then validates the same set through the postwar cleanup read.
- Two consecutive same-revision truce reads must be identical and carry the
  frozen player-to-opponent duration and exact persisted expiry. A native
  `no_truce`, surviving generation, identity drift or unstable read remains a
  typed RED; an ACK is never accepted as a substitute.
- Focused recommendation/action-gate/evidence/verifier tests pass `26/26` in
  normal and optimized Python. Compilation, requirements validation and diff
  checks are GREEN. No CK3 process, desktop, filesystem artifact or MCP/native
  interface was used or changed, so open_kaishek is unaffected. GEN-034 stays
  `2/4`, G2 stays `1/8`, and live execution remains queued under the owner hold.

## 03:51: GEN-034-D bounded action lifecycle

- Added the managed D executor: four same-frame recommendation reads, one
  unchanged-frame check, pre-action source-generation binding, exactly one
  semantic action, and the frozen postwar evidence/checkpoint sequence. A
  continue result is verified but cannot close GEN-034.
- Fixed the default-OFF cleanup candidate's real route mismatch. Its retained
  lifecycle now accepts a successful same-WarID white-peace or surrender ACK;
  public surrender remains disabled and no capability ID or wire shape changed.
- Focused normal/optimized Python tests pass `29/29` in each mode. The enabled
  MSVC Release candidate DLL builds successfully. No CK3 process was launched
  for this package; status is `static-ready / live=false`, GEN-034 is `2/4` and
  G2 is `1/8`.
- The owner then lifted the CK3/screen hold. The next step is a single bounded
  managed live lifecycle under the installation-wide single-instance lock;
  the initial start and cold-restore restart will receive consecutive rounds.
## 03:55：《重整河山》三期静态需求分析

- 完成后朝死亡裂解与前朝三省六部延续的静态分析，状态为 `research / plan-ready`；本工作包没有修改 Mod 运行时代码、启动 CK3、占用主屏幕或写入 Steam。
- 后朝目前只有 `always_follows_primary_heir` 而没有显式 title succession law；原版同类动态礼仪头衔会添加 `single_heir_succession_law`。这被列为三期 P0 高概率根因，同时明确需要受控死亡实机证明，不能用静态对照冒充闭环。
- 当前割据 effect 会统一销毁九个部院头衔，原版 ministry access 又硬性要求 `h_china`。三期推荐保留九席及合资格原班大臣、只补真实空缺，并用单一“最近前朝官署”归属解决多个后朝争用全局官职；正式 Grand Secretariat 和 `h_china` 专属大工程不纳入本期。
- 计划、旧存档迁移、MCP-first L0-L3 矩阵和退出标准已落地到 [三期计划](../../reclaim-the-motherland-phase-3-plan.md)，并修正 README 中把死亡继承写成已完成事实的过度声明。

## 04:20：《重整河山》四期外交附庸平衡计划

- 根据玩家“割据后可成片立即附庸”的真实反馈完成静态根因链：空法理后朝仍是天朝制霸权级，因而错误取得现任中华霸权的国王拒绝豁免、每级 `+20` 等级差和霸权 `+10`；再叠加旧天子的法理、合法性、好感、军力等正常因素即可让大量对象同时转正，并非单一角色数值异常。
- 冻结双层最小调整：后朝不再享受上述三项 `h_china` 身份优惠；本次实际独立诸侯的主头衔保存五年“新近自立”关联，对确切后朝持有者提供公开的 `-50` 接受度。方案不降后朝头衔等级、不清零个人合法性、不设领土硬上限，也不影响战争与其他军阀。
- 将普通受控样本、双方继承、五年到期、无关角色负面对照、三个自然样本和一个极端外交样本写入 MCP-first L0-L3；自然样本首年和平回收上限为 `10` 个中华法理领土百分点，且不得仅靠即时归附达到 `51%`。
- 计划已独立落地到 [四期计划](../../reclaim-the-motherland-phase-4-plan.md)。四期预计 `9..14` 个有效工时；三期范围和排期保持不变。本轮仅修改计划文档，未启动 CK3、占用桌面或操作 Steam。

## 06:17：自动升级建筑 4.0.0 release-complete

- 四期六组合策略、超直辖 `0/-1/恢复` 状态机、旧存档语义、三种资金与代表性建筑／成本回归由 R0032 全量 GREEN；用户选定的悲伤忙碌人物、图纸、施工现场与赛博噪声专属插图由 R0033 聚焦 GREEN。正式构建为 16 文件，manifest／ZIP SHA-256 为 `55B91CB5...680F`／`67C88612...074E`。
- 原生 Steamworks 只更新维护版物品 `3800124956`，公开标题与描述逐字一致，完整 4.0.0 Change Notes 条目 `1789335499` 匿名回读 17 行／492 规范化字符完全一致；原作 `3596580780` 未修改。
- 上传门禁曾检测到另一任务刚启动 CK3，因而在 Submit 前停止并等待对方正常清理；账号恢复无 `inGameInfo` 且 CK3=0 后才提交。没有强启、接管或终止游戏。
- 旧 15 文件缓存已可恢复移动；第一次空路径下载明确 `No Connection`，Steam 自行重连后单次重试得到 16 文件／970,267 字节并严格匹配正式 manifest。Steam 随后恢复 Offline Mode，`WantsOfflineMode=1`、本地 `BLoggedOn=false`、客户端仍运行、CK3=0。
- 完整发布身份、CI 边界与证据哈希见 [4.0.0 changelog](../../release-changelogs/auto-upgrade-buildings/4.0.0.md)。整仓 CI 的无关天朝本地化计数断言失败被如实保留，没有冒充本产品 RED 或 CI GREEN。

## 07:04?GEN-034 immutable replay ?????

- ?????????????checkpoint replay dominance
  `d0db9d3`??? recommendation composer `bef0527`???? power read ?
  action runner `9096d87`??? successor observation `6629dec`?first-command
  continue replay `ed1ebb0`??? open_kaishek ????? `2c7d6ff`?
  `e2f95ec`?`f10a2e3`?`d5b2fe5`?`cf1cf0f`????????
- R657/R658 ?? application-main power-query RED?R659/R660 ??????
  `resume-map`???????? 42 ? snapshot ?????R661 ?????
  ??? gameplay command ?? `resume-map`?fresh exit/power query ????
  ?? 45 ?????????? paused frame?R661 report SHA-256
  `BD6682AA...5DA2`?final driver-state SHA-256 `C9D503E9...ED84`?
- R655/R656 ??? checkpoint/source frame ???? first-command shape
  ??????? R661 ??????????????? RED ????? R459
  ????? map-control execution seam?????? checkpoint???????
  submission ?????????? continue ??? GEN-034???? `2/4`?
  G2 ?? `1/8`?
- ???? R661 ??????????????CK3=0?source checkpoint ?
  driver state ???cleanup GREEN??????? G2-M2 ??? production
  event recommendation-action-material outcome loops?

## 07:44?G2-M2 ??? production material event loop

- ?????????????? `77a7cdb`???? immutable checkpoint ? source event query?registry recommendation?
  ?????material comparator ? successor checkpoint?normal/optimized ????? `38/38`?????????? MCP
  consumer???? public MCP/native ABI?
- R662 ? mutation ??? concrete action-step capability ????report SHA-256 `C29D2289...5248`?cleanup/source GREEN?
  ???? `3a30488` ????R663 ???? application-main event-window query timeout RED???????report
  SHA-256 `548A064C...534C`?checkpoint-bound replay ?? `b4542f2` ????
- R664 ??? PID `124060` ? GREEN?`trait_specific.8001` registry objective ?? native 1?instance `1075 -> null`?
  ???? Q100000 raw `2042495773 -> 2043995773`?delta `+1500000`?material status `verified_change`?????
  `select-event-option-2` ? `save-checkpoint`??? `53783472` ???
- R664 report SHA-256 `AB718B94...BC63`?successor checkpoint SHA-256 `8506B83C...2616`???????cleanup GREEN?
  ???? R664 ?????????????CK3=0?G2-M2 ? `1/3`??? G2 ?? `1/8`?

## 08:32: G2-M2 bounded event-horizon attempts stopped

- Added and incrementally corrected the reusable registered-event horizon runner in commits `1a600b6`, `9119cda`, `33dc070`, `25ef1ba`, `f27b62e`, and `89ae41f`. Focused normal and optimized tests remain GREEN. The runner now accepts an event-free source, waits for command state materialization, advances at speed one in exact `+24 raw` steps, and gives declared event dates one bounded paused-frame settle window.
- R665-R670 all stopped before the target action and retain `status=red`. Their report SHA-256 values are `EEB32A7A...5B88`, `205A5384...3900`, `30DE561F...C674`, `511368EB...0300`, `415F8998...C782`, and `79FB89A4...ED3B`. R670 observed no materialized speed-one state within ten seconds, submitted no resume/query/selection, preserved the source, and cleaned up GREEN. Repeating the old R555 trajectory is now stopped under the proportional-validation rule.
- The R374 archive does contain ordinary autosaves, but the closest reusable file before `death_management.1007@53590464` is `autosave_2.ck3@53584920`, SHA-256 `F1188E04...2CBB`, about 231 game days earlier. The historical command remains a valid production-live primitive (`authored1/native0`, instance `1046 -> null`), but it is not an event-boundary material checkpoint. `.1007` therefore remains encounter-driven rather than triggering a dedicated replay.
- G2-M2 remains `1/3` production material loops and global G2 remains `1/8`. Current round R670 is terminated; CK3 and the injector are absent. No public MCP/native ABI or open_kaishek surface changed.

## 09:29: R671 war-entry mailbox RED bounded fix

- Current round R671 restored the R664 successor checkpoint and completed `query-declarable-wars`, but the first target power query `query-war-entry-assessments-v1-1-38436` failed before mutation or date advance. Run log SHA-256 is `9257FEB3...B1D0`; final driver-state SHA-256 is `453669C9...E4AB`. Current round R671 then terminated with cleanup GREEN; no CK3 or injector survives.
- The missing `:<stage>` proves the strict reader did not report a business-data failure. The dispatch layer had collapsed queued timeout, executor failure, boundary drift and completion rejection into one generic error, while using a 2-second queued wait rather than the bounded 8-second budget used by the other production application-main adapters.
- The war-entry adapter now uses `8000 ms` queued / `2000 ms` executing waits and preserves exact terminal diagnostics. Query-specific unavailable results remain RED and retain their reader stage. The rebuilt Release DLL SHA-256 is `6562CD13...1994`; focused mailbox fixture and source-contract executables are GREEN.
- No capability name, command literal or success payload schema changed, so the MCP consumer and open_kaishek require no compatibility edit. G2-M2 remains `1/3`, global G2 remains `1/8`, and the next action is one short R672 replay with the rebuilt DLL.

## 09:39: R672 war-entry short replay GREEN

- New round R672 changed only the bridge DLL (`ED39D5FD...EB86 -> 6562CD13...1994`) against the same frozen R664 successor input. Its three bounded turns completed `query-declarable-wars`, `query-war-entry-assessments-v1-1-38436`, and one explicit `NO_DECLARE` `life-advance`; the exact assessment returned available on the original paused frame.
- The gameplay turn advanced 30 days from `53783472` to `53784192`, remained at peace, repaused, and saved successor checkpoint `8D918606...CCB4`. The run is `turn_limit / qualified / ok=true`, with no first blocker. Run log and final driver-state SHA-256 are `A803C1E4...BA6D` and `76AF995D...B654`.
- Cleanup is GREEN and current CK3/injector counts are zero. R671 remains a preserved historical RED; the same-source differential closes its operational blocker without another old-DLL replay. This does not add a registered event outcome, so G2-M2 remains `1/3` and global G2 remains `1/8`.


## 10:14: R673 stale declaration cache RED and bounded repair

- Current round R673 continued the ordinary G2-M2 campaign for 12 attempted turns. Eleven completed: six queries, five explicit NO_DECLARE advances and one periodic checkpoint. Date advanced from 53784192 to 53787792; the last durable checkpoint is F2E84CF1...D27B at date 53786352.
- The twelfth step preserved a real RED: query-war-entry-assessments-v1-1-38436 returned target_not_declarable. The log SHA-256 is CF92D2D...4DE3, final driver-state SHA-256 is 30902F5E...BA0B, and cleanup left CK3/injector at zero.
- The command sequence isolates the cause. query-declarable-wars ran only on the first frame, but its seven rows remained projected through five later native revisions. The same-frame power cache expired correctly; the declaration cache did not. Python therefore reused an obsolete legal target.
- The bounded repair gives declaration rows the same exact paused-frame ownership used by other query caches. A different snapshot/revision/native revision/connection/episode removes both rows and generated action literals; a query crossing frames is rejected before cache admission. Focused declaration and war-entry tests pass 14/14 under normal and optimized Python.
- This is an internal Python cache correction. It changes no native DLL, public MCP schema, capability, command literal, version or dependency, so open_kaishek needs no companion edit. G2-M2 remains 1/3, global G2 remains 1/8; the next proportional check is a short R674 checkpoint replay.


## 10:18: R674 declaration-cache replay GREEN

- New round R674 used fix commit c1d91c0a08c3cf0b520c881ea8727a6b99d79351 and cold-restored R673 checkpoint F2E84CF1...D27B at history 599/date 53786352. Its no-launch preflight was GREEN with SHA-256 4E0BC602...F03BE.
- All three bounded turns passed in the required order: a fresh query-declarable-wars, one current-target query-war-entry-assessments-v1-1-38436, then one explicit NO_DECLARE life-advance to 53787072. The run ended turn_limit / qualified / ok=true with query=2, gameplay=1, checkpoint=1 and no first blocker.
- Run log SHA-256 is C5B374397C1F45E633F0B193ADC0B1C0B8DD33183ED83E647DD370828871CC09. Final driver-state and checkpoint SHA-256 are A6033D856F61BFA331A4F8BFE0251625426773C28088341CDAF4CA9C4E14567E and 4F72E469331A4BF09AE85FF2D12D4520751387A710257DFE2C25E2E350561686. Cleanup is GREEN; current round R674 and old round R673 are terminated, with CK3/injector at zero.
- R673 remains historical RED, but the stale-declaration operational blocker is closed. No public interface changed and open_kaishek remains clean/synchronized at cf1cf0f5c1a4ce4ed1ab70ceba6316c1f6c3d58f. G2-M2 remains 1/3 and global G2 remains 1/8; ordinary encounter-driven campaign work resumes without a dedicated event long run.
