# 2026-09-14 daily report

Status: **in progress**

Formal close: 2026-09-15 00:00 (Asia/Shanghai)

Morning meeting: [`../meetings/daily/2026-09-14.md`](../meetings/daily/2026-09-14.md)

Starting branch/HEAD: `root-rebase-r345-20260909` / `3327e91e747deda4c3f6b6c820bb7435e7bc3509`

## Work completed

- Formally closed the 2026-09-13 daily and W37 reports at 00:01, then opened the 2026-09-14 and W38 planning records without backdating the in-flight build.
- Completed the static native MCP package for deterministic playable selection and lobby to ruler-designer activation. Both public tools are zero-input and reject caller-supplied GUI names, paths, pointers, coordinates and callbacks.
- Fresh Release compiled `549/549`; CTest passed `106/106` against exact CK3 `1.19.0.6`; focused contract/service/native-driver/official-MCP passed `7/7`. Candidate DLL SHA-256 is `38BDED238B182542C27BFCD860FB848EE9DEAD79F71E26998C9FDE8179DDA591`.
- `open_kaishek` prevalidation is `not-applicable`: it has no frontend GUI/CoA domain and this additive route has no companion consumer shape.
- Updated the managed frontend acceptance runner to require all six closed tools and both new bridge capabilities, then execute `main_menu -> bookmarks -> ready lobby -> ruler_designer` exclusively through official MCP. Its final inspector must be scoped to `ruler_designer`; `py_compile` and the argument contract are GREEN.
- Ran the bounded GEN-034-C admission and first production attempt. Current round R640 stopped before launch because the system Python lacked `win32api`; current round R641 used the shared project environment, completed both allowed native queries in one paused frame, and then retained a contract RED because the options normalizer rejected its own normalized `source=native` output.
- Repaired that idempotence defect without changing the native wire: raw producer payloads and already-normalized public payloads are both accepted, while any other source remains invalid. The live helper now preserves a typed `evidence_required` projection without invoking the utility evaluator. Focused normal and optimized tests pass `18/18` in each mode.
- Current round R642 stopped in no-launch preflight on an incorrect injector basename. Current round R643 then proved launch and cleanup but timed out while CK3 was still loading. New round R644 reached the exact paused frame, completed the two allowed official-MCP reads, preserved both typed white-peace blockers, and performed no evaluation or action. The same frozen frame will not be rerun again.

## Current work

- Find or produce the smallest bounded Raiktor checkpoint that satisfies the already-frozen white-peace admission horizon, then run the same two-read C evaluation once. This work must not use the disabled broad preview or submit a war-termination action.

## Why this work

- The frontend package replaces a desktop-input chain with semantic MCP. The GEN-034 work now distinguishes a valid unavailable option from a consumer failure and moves to the first frame where the option can actually participate in a three-way decision.

## Evidence and validation

- Accepted build: `C:\xb\coa-rd-20260913-3`; source fingerprint `BB40E68A...94C07`; DLL `38BDED23...DA591`; injector `D51CBFA7...3FEBB`.
- Earlier long-path and missing-executable build failures remain retained environment diagnostics. The corrected fresh build is the accepted static validation.
- R644 exact-build proof passed all `13/13` checks. Its two query results are non-error, the before/after frame binding is unchanged, the only native-history delta is the two read-only commands, and cleanup/source invariants are GREEN.

## Readiness change

| Capability | Before | Now | Remaining gate |
|---|---|---|---|
| Fixed playable selection | Not implemented | `mcp-static-ready / live=false` | Official MCP ACK plus enabled lobby postcondition |
| Lobby to ruler designer | Native path identified only | `mcp-static-ready / live=false` | Independent `ruler_designer` route/tree observation |
| G2 / GEN-034 | `1/8` / `2/4` | C has production-live negative evidence; count unchanged | One available same-frame evaluation, then decision/action work |

## RED / blockers

- R640 and R642 are retained no-launch harness REDs. R641 is the repaired consumer-contract RED. R643 is a retained cold-load timeout with cleanup GREEN.
- R644 is the authoritative negative live result: the frozen zero-day, zero-score frame returns `white_peace_native_option_unavailable` and `white_peace_final_recipient_response_unavailable`. The broad termination-preview crash RED remains open and dispatch remains disabled.

## Files and commits

- CoA implementation: `ck3_autonomous_player/native_bridge/`, `ck3_autonomous_player/src/xar_autoplayer/bridge/`, focused unit tests and `docs/ck3-native-ai/frontend-gui-route-v1.md`.
- R640 report: `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r640-dc42838\report.json`, 8,057 bytes, SHA-256 `656C26958FF36D3FEB0FF90623F15D9324B9BEEAEAD90578C753CF78F240658C`.
- R641 report: `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r641-dc42838\report.json`, 94,506 bytes, SHA-256 `AAF02DFB7B678C1889FB14D6C37DCBE7A48A907FE68DB0D23272F4BA33D15370`; retained driver state SHA-256 `10792DC261D8D51083FF2A4C570696CF70AA703A1998902551EB63E45FCC0807`.
- R643 report: `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r643-63bb714\report.json`, 9,589 bytes, SHA-256 `BC1F0F89C05734CA56AB7B5FFC2E0CD46D2B03A0E28E516069E817B7155B6F54`.
- R644 report: `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r644-63bb714\report.json`, 3,509,196 bytes, SHA-256 `9844B817995A876C1B48C7BDDF42826B4866F8E8C9680EB79F0EA53D4FECBA0B`; retained driver state SHA-256 `7D92159CC19E988600A713A2BA0217A4B99133BD18183ED4FD1D9765EEB9845E`.

## Next step

1. Reuse an existing eligible Raiktor checkpoint if one exists; otherwise add a bounded checkpoint-preparation runner that advances only to the frozen white-peace admission horizon and saves once.

## Range boundary

- Steam remains offline. No Workshop operation, arbitrary GUI API, Tianchao publication, promotional video or general religion expansion is in scope.

## 01:04：GEN-034-C R645 and bounded horizon runner

- Current round R645 reused the existing R456 pre-action checkpoint at date
  `53187096`, 135 war days after the zero-day source. It completed the same two
  official-MCP reads on CharacterID `29829` and WarID `33554473`; white peace
  still returned `native_validator_passed=false`, `available=false` and an
  unavailable final recipient response. Exact-build, same-frame history,
  cleanup and unchanged-source checks are GREEN. Its 3,738,267-byte retained
  RED report is
  `Z:\ck3_mod_rewrite_process_assets\g2-gen034c-r645-3c7e79c\report.json`,
  SHA-256 `C6858122FCC4120E4715E220D4B711BF05180B741C294185A69C4BFE51A201FE`.
- This proves that the nearest existing 135-day checkpoint is insufficient;
  it will not be replayed. A reusable managed horizon runner now advances that
  immutable source once toward the already-frozen 365-day admission boundary.
  It allows one resume, at most one pause, read-only snapshot polling, one
  options query and one checkpoint save only after the native option and final
  recipient response are both available. It stops RED on an event, identity
  drift, early war end, unexpected pause or bounded date overshoot, and never
  selects events or submits a war-exit action. Focused normal and optimized
  tests pass `4/4` in each mode.
- GEN-034 remains `2/4` and global G2 remains `1/8`; current round R645 and all
  older rounds are terminated, CK3=0. The next launch is one bounded horizon
  preparation attempt from the R456 source, not a repeated evaluation frame.
