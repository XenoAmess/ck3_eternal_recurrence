# 2026-09-18 daily report

Status: **in progress**

Formal close: 2026-09-19 00:00 (Asia/Shanghai)

Morning meeting: [`../meetings/daily/2026-09-18.md`](../meetings/daily/2026-09-18.md)

Opening branch/HEAD: `origin/master` / `463a21593a4a392ec627c8de77d63b32fe824e55`

## Completed so far

- R860 returned after midnight as a formal capability RED after 58 attempted / 57 successful turns: 29 queries, 28 gameplay actions, eight checkpoints and +849 game days. Natural `befriend_outcome.0002` instance5 matched the registered failure shape but stopped before action at `registered_contract_requires_extended_consumer`; action uncertainty is none.
- The safe recovery anchor is h961/date `53235912`, checkpoint `70C2423F...A2808`. Formal report/driver/operator receipt/allocation SHA-256 are `4BA536C7...38F2`, `33A16D8C...D5F3F8`, `A45FA230...E815`, `6B8D3A4F...1ECB3`. PID119616 and injector were reclaimed; inventory returned to zero.
- Exact-source review confirmed the conservative befriend route as authored option3/native2 for both success and failure projections. The direct consumer implementation now permits only that event's source-bound variant fields and relationship checks, adds `target: character` to every projection, and replaces the false observation-only `max_occurrences=2` with repeatable source semantics.
- Focused befriend policy tests cover the exact R860 failure frame, success projection, owner/target/branch drift and formal planner step `select-event-option-3`. Normal mode is GREEN for 62 isolated tests; optimized mode is GREEN for the same 62 with only expected pytest `-O` warnings. The broad legacy checkpoint file was not rerun; only its exact befriend method passed normal and optimized.
- Read-only audit identified R860 turn17/18 as natural `fervor.1002`. The old unregistered path selected native0 through `active_event_degraded_minimal_choice`, although exact source gives all three options distinct resource/relationship consequences. The exact consumer is now on master `d5fba52c`: it binds the five-scope/three-option projection and selects only authored3/native2, the only source route without resource, opinion or rival effects. R862 did not naturally reproduce the event, so action-live proof remains open and unadvertised.
- R864 continued the ordinary campaign from h1146 for 17 successful turns and +207 game days before preserving a capability RED in a natural day-0 `vassalization_cb` defensive war. The last durable h1167 checkpoint is 196 days after h1146 and predates the war plus all tail actions; no command was submitted on the failing turn.

## Morning commitments: rolling comparison

| # | Morning commitment | Current result | Evidence / remaining gate |
|---:|---|---|---|
| D1 | Close `befriend_outcome.0002` consumer B0 | **Complete for static consumer and success live loop; failure live branch open** | `d3133d18`; R861 natural success variant chose authored3/native2, independent disappearance, stress result, later consumption and checkpoint. |
| D2 | Cold-restore from safe h961 | **Complete** | R861 new process discarded failed tail, completed 12/12 and saved h981; cleanup proven. |
| D3 | Remove observed `fervor.1002` blind-choice B1 | **Static implementation complete; live recurrence open** | `d5fba52c`; exact source contract and option3/native2 pass normal/`-O`; R862 had no recurrence. |
| D4 | Continue the 100-year prefix | Blocked on one scoped war B0 | R864 durably advanced h1146 -> h1167 (+196d), then stopped at a natural primary-defender capital-hold scene. The h1167 pair is the recovery anchor; 100-year gate remains open. |
| D5 | Opportunistic named gates | Partial, no authority gate closed | R862 naturally drained `court_chaplain_task.0311/.0312`; `.0030`, `.1007` and succession did not appear. |
| D6 | Keep delivery/Git/evidence current | In progress | Preview remains GO; fervor code is on master, R861/R862 evidence is sealed, and implementation temp refs are clean. |

## Readiness change

| Capability or gate | Before | Now | Evidence / limitation |
|---|---|---|---|
| User-startable preview | GO | GO, unchanged | R802 immutable ZIP `E32D2057...A1273`; new source is not silently added. |
| `befriend_outcome.0002` | Natural pre-action RED | Success projection production-live; failure projection live-open | R861 closed the success path; R860 failure shape remains post-fix action/result/next-turn open. |
| `fervor.1002` | Unregistered degraded consumption | Exact static-ready consumer; live recurrence open | Old native0 choice is not accepted as safe coverage; `d5fba52c` selects typed native2 only on the frozen projection. |
| Ordinary prefix | h878 GREEN | h1167 durable prefix; R864 tail RED | R861/R862 plus R864's last durable checkpoint extend the safe campaign prefix. The later natural defensive-war frame is a preserved capability RED, not a GREEN continuation. |
| GEN-034 / Council / G2 | 3/4 / 1/4 / 1/8 | Unchanged | No complete new authority gate. |

## Failures, blockers and unknowns

- R860 `befriend_outcome.0002` was a capability RED, not a harness failure. It stopped before action; R861 safely recovered h961 and closed only the success projection.
- The earlier `fervor.1002` action did advance the event and later turns, but material piety/opinion/rival results are unobserved and the generic lowest-index choice is not accepted as safe coverage. The exact consumer is implemented; its live action/result/next-turn branch remains evidence-not-observed after R862.
- R864 is a production capability RED, not a harness timeout. `vassalization_cb` legitimately supplied no targeted title or exact objective province; the player was primary defender at score 0/day 0, and surrender would have made the player the attacker's vassal. The raised army became regular at the same-frame exact player capital while the enemy remained gathering with no route. The planner then had no narrow defensive-capital hold branch and stopped rather than infer combat power or chase the visible enemy.
- Independent recovery review proved h1167/date `53280600` unpolluted. Restore must truncate h1168-h1176, including the confirmed but uncheckpointed raise/advance tail, and fresh-query the new timeline; no prior unconfirmed action exists and old WarID/ArmyID/termination rows must not be replayed.
- The befriend fix changes no public schema, native ABI, MCP command or capability advertisement. No open_kaishek adapter release is required.

## Files and commits

- Current implementation files include the befriend direct consumer plus `records_fervor.py`, exact source/portable evidence, focused fervor tests and `docs/ck3-native-ai/fervor-1002.md`.
- Commit/push: befriend source `d3133d18`; R861 evidence `395a35cf`; fervor original `395c1acf` rebased to final master `d5fba52c`. Its remote/local branch and worktree are deleted.

## Next steps

1. Integrate the narrow R864 primary-defender exact-capital hold policy, then cold-restore only h1167 and require a fresh war observation, one bounded typed advance, changed-route/contact consumption and a new paired checkpoint without replaying the old raise tail.
2. Continue the same ordinary prefix after that focused GREEN and capture only real succession/governance/named-event scenes.
3. If `fervor.1002` recurs, require typed authored3/native2, independent stress/window result, later turn and paired checkpoint; do not expand religion strategy.
4. Wait for a materially newer natural Council scene; h961 is retired and public Council remains OFF until all four gates close.
5. Refresh the frozen user preview once the cumulative ordinary changes have their exact-ZIP qualification; keep R802 available meanwhile.

## Scope boundary

- The preview is runnable now, but this work does not claim the 100-year prefix, first 1066–1453 campaign, second seed or a new G2 milestone.
- Religion remains owner-deferred; the `fervor.1002` work is only a necessary exact modal drain and is not advertised as religion capability.

## R861 befriend live recovery

- Master `d3133d18` was prepared against the exact R860 h961 pair. No-launch rebind changed only lifecycle environment bindings; the save remained `70C2423F...A2808`, while rebound driver became `5D076D0B...D65AA`. The failed h962-h971 tail contained no submitted action and was discarded by cold restore.
- R861 completed 12/12 turns: six queries, six gameplay turns, two checkpoints and +144 game days (`53235912 -> 53239368`). A natural `befriend_outcome.0002` success projection matched variant0; formal policy selected only `select-event-option-3` (authored3/native2/rendered1). Instance `5 -> null`, stress `20 -> 60`, gold raw remained `49949715`, and turn9 plus later gameplay consumed the absence.
- Final h981 checkpoint/driver are `6A8720C6...2D20` / `488A52B4...A5FC`. Report/operator/close-ledger are `623BAB4F...47A7`, `53613A7A...D866`, `890EA1CC...D7F4`. PID121692, injector and managed tree were fully reclaimed.
- This closes the success projection as a production-live loop. The exact failure projection seen in R860 remains post-fix live-open; static consumer coverage and the natural pre-action frame are retained, but they do not substitute for a failure-branch action result.

## R862 ordinary prefix and fervor verification window

- The fervor implementation was rebased linearly from original `395c1acf` to final `d5fba52c` and fast-forwarded to remote master. Focused normal and optimized suites each passed 36 tests plus 71 subtests. The temporary remote branch, worktree and local branch were removed immediately after integration.
- R862 cold-restored the R861 h981 pair through the formal production entry and completed 100/100 turns in 736.769 seconds: 50 queries, 50 gameplay turns, 16 checkpoints and +1,522 game days (`53239368 -> 53275896`). Final h1146 checkpoint/driver are `442FC751...92AE2` / `B5C18328...2FE01`; report/operator/close-ledger are `EB6FF99B...A3233`, `5C8D105E...BD3F`, and the external `R862-closed-green.json`.
- Two natural one-option notices, `court_chaplain_task.0311` instance6 and `.0312` instance7, each followed public current-event query -> the sole shown+enabled option -> independent instance disappearance -> later formal consumption. Stress and gold were unchanged across both choices.
- `fervor.1002`, `death_management.1007` and natural succession did not occur in this bounded window. Their live gates remain open; absence is not a pass. Actor31853 and the same episode/high-level objective continued, with expected successor36403 still observed. PID113072 and the entire managed tree were reclaimed.
- Preview remains the immutable R802 GO package. GEN-034 stays 3/4, Council 1/4 and G2 1/8; this bounded prefix does not complete the 100-year or whole-campaign gate.

## R863 Council h961 query-only scene

- After R862 released the singleton, the existing private Council materializer created a no-launch candidate from R860 h961. The candidate froze source save `70C2423F...A2808`, projected driver `2E89C27F...4B488`, private query ON, private action OFF and public registration/advertisement OFF; preflight returned `ready-no-launch`.
- R863 used one CK3 process for one private paused query. It observed 10 candidates: already-councillor isolated positives `28925/30909/36567`, seven ordinary candidates, and zero isolated guest, candidate-pending or replacement-fireability-denial rows. Gameplay actions and native helper invocation delta were both zero; date `53235912`, native revision3 and save SHA were unchanged.
- Report/raw-terminal/close-ledger are `AC7ECA23...6F5D8`, `55A6C2D4...C0E24` and external `R863-closed-green.json`. PID125360, job and watchdog were reclaimed. The h961 scene is retired after this one materially different query; Council remains 1/4 and public query/action/ad stay OFF.

## R864 natural primary-defender capital-hold RED

- R864 started from the exact R862 h1146 pair under master `ee03bad5` and completed 17/18 formal turns: eight queries, nine gameplay turns, two checkpoints and +207 game days in 296.458 seconds. The last durable h1167 checkpoint is date `53280600`, size `94,278,189`, SHA-256 `01980CE6...9E5D`; it is before the new war and every later write action.
- A natural `vassalization_cb` created WarID150994969 with the player as primary defender, score 0 and duration 0. The public termination query correctly showed surrender as the only currently legal terminal action, but its material outcome was attacker victory/player vassalization; white peace and defender victory were unavailable. Formal policy therefore continued tactically rather than surrendering on day 0.
- One typed `raise-troops-default` produced Army234881216 at the same-frame exact player capital Province45; a five-day bounded advance independently changed it from gathering to regular. Enemy Army184549393 appeared at Province28 still gathering, without target or route. Because this character-target CB has no targeted title, both `war_objective_province_ids` and rich objective state were legitimately empty. The planner returned `native_war_counterpolicy_hold` without submitting a command.
- Exact-build native-tree review confirms defender stances include own-capital objectives. The scoped repair is therefore Python-only: a primary defender with an empty exact-objective set may hold a fresh same-frame exact capital and take one bounded `life-advance` only while the selected regular army is stationary, all controllable armies are safe, no combat/retreat/event/interaction/write is pending, and the current termination assessment selects no terminal action. It does not declare the capital a wargoal, chase the enemy, infer power from soldiers or advertise a combat forecast; native/MCP interfaces are unchanged.
- Formal report/operator/final-driver/RED seal are `53970EED...9624E`, `DD50F71E...6F349`, `12686AFD...9D553` and `785B7850...6B7525`. Cleanup is GREEN and PID117108/injector inventory returned to zero. Recovery must use h1167 plus the final driver only to truncate h1168-h1176; the old WarID, ArmyID, termination row and confirmed-but-uncheckpointed raise are not facts on the restored timeline.
- Preview R802 remains GO. GEN-034 stays 3/4, Council 1/4 and G2 1/8; neither the R864 RED nor its static repair closes an authority gate.
