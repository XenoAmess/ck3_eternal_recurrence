# 2026-08-28 日报

状态：**滚动日报；普通战 speed 3 已 production-live、全监视 terminal speed 5 已零中间暂停实测，GEN-023/024 已解除，G1 未完成**

当前滚动更新时间：2026-08-28 12:47（Asia/Shanghai）

正式收口：2026-08-29 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-08-28.md`](../meetings/daily/2026-08-28.md)（00:02 真实补录）

起始分支/HEAD：`agent-mainline-20260827` / `79b8d2acfbe1f80a7a7f88da3fed7cda1791017c`

当前实现基线：`42601a5`；本轮证据收口随本次提交同步 `origin/master`。普通 active combat 已由 exact-build decision sentinel
正式选择 speed 3，全 6 支可控 army 的 terminal watch 又在 speed 5 实机取得零中间暂停。GEN-023/024 已由正式 continuation
穿过，上一场战争也已在 `53224776` 原生结束；最新 durable checkpoint 为
`53224848 / E317CB7F...C2EE`。角色仍活、cleanup 全绿，当前没有未解除的 gameplay B1；下一步直接恢复 G1 长跑。

## 今天完成了什么

- 从同一个 immutable checkpoint `date_raw=53209560 / SHA-256
  A8DD4034C32856B8D1E05D6B834BBBF3C51AA74DA038BB22A0CA23A998AD76CF` 连续完成 `79b8d2a`、`e0688c7`、
  `9ff04ae` 三轮 CK3 live A/B。三轮逐步移除 timeline wait、life history 与 planning/termination query 内部的完整 transcript
  复制，没有更改 gameplay policy 或放宽动作后置条件。
- `79b8d2a` 运行段为 `48.134s`：首/尾 query 约 `3.398s / 2.579s`，首/尾 life-advance 约
  `5.065s / 4.600s`。`e0688c7` 运行段为 `44.875s`：query `3.317s / 2.516s`，life-advance
  `4.583s / 4.111s`。
- `9ff04ae` 运行段降为 `24.684s`；六次 query 约 `0.050–0.068s`，首/尾 life-advance 为
  `4.569s / 3.643s`。该轮 `12/12` turns 全部成功，包含 `6` gameplay、`6` queries、`2` checkpoints；六次推进均为
  `timeline_policy=player_tactical / speed=1`、各推进 `1` 游戏日，动作后全部真实 `paused=true`，managed cleanup 全绿。
- 最终 durable checkpoint 为 `date_raw=53209704`，SHA-256
  `39379D0224788198FECCCA82DA4B7B7257DB7E1AEE6B3750F62AA845E312678A`；配套 driver-state SHA-256 记录为
  `D47DAA...BDA`。它是继续同一 CharacterID `29829` / episode 的最新恢复入口，不是 terminal 或 G1。
- 确定性计数/计时与 live A/B 互相闭合：life history 完整复制 `9 → 1 → 0`；planning transcript 复制
  `1 → 0`，局部计时约 `600.637ms → 5.813ms`；termination query 内部复制 `3 → 0`，局部计时约
  `1815.527ms → 1.852ms`。这解释了 query 降到几十毫秒，但不把仍需约 3.6–4.6 秒的 native life-advance 冒充已消除。
- 最新实现全量 unit 为 `1341 passed, 2 skipped, 900 subtests passed`；独立审查 `PASS`。`e0688c7` 与
  `9ff04ae` 已提交并推送。
- 正式 `9ff04ae` 长跑已真实恢复，但在 `85/86` turns、42 gameplay、14 checkpoints 后命中新的 GEN-014：一次
  `pause-map` ACK 后没有在原 10 秒窗口观察到 paused。RED artifact、`date_raw=53210712 / F15D383B...35559` checkpoint 与
  matching driver state 均已冻结，CharacterID `29829` 仍活且 cleanup 全绿。
- 未修改 `9ff04ae` 从同一个 F15D checkpoint 的独立 4-turn 重放没有复现；两次 life 均逐日推进并回到 paused，产生更新
  checkpoint `date_raw=53210760 / 79B71103...85F2`。最小 blocker-removal 只允许同 owner 在原 deadline 内补交一次幂等 pause；
  聚焦 `12 passed, 10 subtests passed`、完整 driver `153 passed, 128 subtests passed`、全 unit
  `1344 passed, 2 skipped, 908 subtests passed`；独立复审 PASS，尚待 live revalidation。

## 早会承诺对照（滚动）

| # | 早会承诺 | 当前结果 | 证据 / 未完成原因 | 是否顺延 |
|---:|---|---|---|---|
| D1 | 对 `79b8d2a` 做同 checkpoint lean snapshot live A/B | 完成并继续优化 | 三轮均从 `A8DD4034...AD76CF` 起跑；最终 `9ff04ae` 运行段 `24.684s`、`12/12`、2 checkpoints、paused 与 cleanup 全绿 | 否 |
| D2 | 继续 adaptive speed-3 production gate | 完成；继续收集 crush 胜局样本 | ordinary active combat speed 3 已 production-live；全 6 army terminal speed 5 已零中间暂停实测。双 `4x` crush 与 player-won pursuit 仍等自然 qualifying checkpoint | 否；后两项作为不阻断 G1 的 live calibration |
| D3 | 恢复正式一代人 G1 长跑 | 部分完成 / 当前可继续 | GEN-023/024 已 production-live continuation；战争于 `53224776` 结束，最新 checkpoint `53224848 / E317CB7F...C2EE`，角色存活 | 是；用当前 speed/sentinel 基线从最新 anchor 续跑 |
| D4 | 完成死亡后的琉焰卿人生分数结算 | 未开始 | CharacterID `29829` 尚无自然死亡 terminal/committed settlement；不能记录分数 | 是，等待真实 terminal |
| D5 | 次日午夜逐项收口并滚动更新 W35 | 等待 08-29 00:00 | 当前仅为滚动日报；W35 仍进行中 | 否 |

### 计划外完成

- `79b8d2a` live profiling 证明剩余成本不只在 timeline wait；因此按真实计时追加 `e0688c7`，把 life history copy 从 `1` 降为
  `0`，再追加 `9ff04ae`，移除 planning 与 termination query 的 transcript copy。两项都是 D1 暴露后的最小性能修复，不是
  理论安全扩张。

## 现在在做什么

- 将 `42601a5` 的 speed-3/full-watch/sparse-stop 代码与 live evidence 提交推送，然后从
  `53224848 / E317CB7F...C2EE` 冷恢复同一 CharacterID `29829` / episode，继续正式 `50000`-turn 长跑。
- 普通战直接使用 production speed 3；sentinel 只在语义决策点停。真实 player-won pursuit 或双 `4x` crush 出现时冻结样本并完成
  `1,5,5,1` 校准；不暂停 G1 去等待随机场景。
- 角色死亡后继续等待琉焰卿 Mod 的 committed settlement；在权威人生分数与全部 terminal gates 形成前不结束 runner。

## 为什么做这些

- 用户指出 121 条查询约耗时 26 分钟。旧实现的 driver-state 全量重写、timeline/history 深复制以及 planning/termination query
  内部 transcript 深复制都在真实 production history 或同 checkpoint live A/B 中被测到；三轮修复直接减少这些已发生的成本。
- 战斗速度是用户明确提出的吞吐瓶颈。五档 STOP、1/2/3 ongoing frame 与五档 terminal journal 已分别实测；本次 formal
  run 的 163 个推进日达到 `12.07 游戏日/分钟`，推进事务约 `4.555 秒/日`，已经不是“每游戏日半分钟”。随后又将 state
  压缩 `78.946%` 并上线 native decision sentinel：普通战正式 speed 3；已决定的 terminal 可 speed 5、零中间暂停。

## 证据与验证

| runtime | 同 checkpoint 运行段 | query 首 / 尾 | life 首 / 尾 | 结论 |
|---|---:|---:|---:|---|
| `79b8d2a` | `48.134s` | `3.398s / 2.579s` | `5.065s / 4.600s` | timeline wait 复制已减，但 query/life 仍有完整 transcript copy |
| `e0688c7` | `44.875s` | `3.317s / 2.516s` | `4.583s / 4.111s` | life history copy 已为零；planning/termination query 仍主导 query 延迟 |
| `9ff04ae` | `24.684s` | 约 `0.050s / 0.068s` | `4.569s / 3.643s` | planning/termination copy 清零；12-turn live loop 与 cleanup GREEN |

- 三轮使用同一 `A8DD4034...AD76CF` 起点，因而可比较运行段；这里只报告观察值，不把单次 12-turn 样本外推成长跑稳定倍数。
- 最新轮为 `12/12`、6 gameplay、6 queries、2 checkpoints；每次 gameplay 只推进 1 天并回到 paused。
- 最终 checkpoint `39379D0224788198FECCCA82DA4B7B7257DB7E1AEE6B3750F62AA845E312678A`；driver state
  `D47DAA...BDA`；cleanup 全绿。
- 全量 unit：`1341 passed, 2 skipped, 900 subtests passed`；独立审查 `PASS`。
- 正式 RED：`20260827T163217Z-one-generation-ace7cbcf`，report SHA-256 `49D2A8BB...B50808`，first-blocker SHA-256
  `A0BD8C79...A484`；`85/86`、42 gameplay、14 checkpoints、cleanup GREEN。
- GEN-014 non-repro：同 F15D checkpoint 的未修改 4-turn replay 为 `4/4`，两次 life 后 checkpoint
  `53210760 / 79B71103...85F2`，cleanup GREEN。
- 最新 formal RED `20260828T000753Z-one-generation-a09470a0`：report / first-blocker SHA-256
  `AFEECF331F298F73F41D62A4CA78AE1C69C5BD09850EF4E692394645FDA12809 /`
  `52C46EA6AA0335A7A7086021EC5B3FD1BF2AC373AFE1673C9B8E8659C0AAAD50`；`380/381` successful、
  211 query、169 gameplay、54 checkpoint、166 visible gameplay、elapsed `810.217s`。最新 durable checkpoint
  `53220288 / history=4087 / size=79345233 / C8C2926F...67D5`；角色存活、cleanup 全绿、CK3 已退出。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| read-only persistence batching | `static-ready` | `production-live loop` | 三轮真实 query/action/checkpoint/close；最终 driver state 已持久化且 cleanup GREEN |
| lean life snapshot/history | `static-ready` | `production-live loop` | life 完整 history copy `9 → 1 → 0`；三轮同 checkpoint A/B |
| planning/termination query transcript | 每次仍复制完整 transcript | `production-live loop` | planning `1 → 0`、termination `3 → 0`；最新 query 约 50–68ms |
| battle speed 1–5 | 五档 `static-confirmed / live pending` | ordinary speed 3 `production-live loop`；full-watch terminal speed 5 `production-live primitive` | production `30C247B6...CEC`：6-army watch、连续 4 日、零中间暂停；speed-5 terminal `AE8D8EC2...FEF4`：同终点/核心 outcome、约 `5.01×`。player-won pursuit 与双 `4x` crush 样本待补 |
| GEN-014 bounded pause retry | `static-ready / live pending` | 一般路径 `production-live`；rare retry B3 pending | `0ceb7d8` cold revalidation `12/12`、6 gameplay、2 checkpoints、cleanup GREEN；未命中第二次 retry |
| GEN-015 state delivery | `static-ready / B1 open` | `production-live / resolved` | `f1230f6` 诊断拒收原因；`cf98648` 与 `c21c096` 连续 cold replay 已收到并消费 fresh frames |
| signed pending interaction ID | 负值被判 malformed | `production-live primitive/loop` | live ID `-2013265918` typed query 与 reject lifecycle 全程保真；仅 `-1` 为 sentinel |
| arrange-marriage reply | opaque special / planner blocked | definition-bound reject-only `production-live loop` | `c21c096` query→reject→旧 ID null→继续推进/checkpoint；婚姻效用仍为 B2 |
| GEN-018 multi-army horizon | planner blocked | `production-live / resolved` | `e619219` 复用同帧 hostile timelines；`12/12` canary 跨过旧帧，正式 run 随后继续 38 日 |
| GEN-019 tactical army identity | CFleet carrier 被当成独立 ArmySnapshot | `production-live / resolved` | `816442e` 只发布 raw-kind 0 + CArmy self-backlink；fresh canary 尾段对 `150995278` 的出现和 preview 均为零 |
| GEN-020/021/022 contact endpoint | 所有 objective route 在当前省端点冲突，exact-day 后置状态不足 | `production-live loop / resolved for continuation` | `4a7d7ce` bounded adjacent-day follow-up 已由 formal run 穿过，并继续至 `53220312` 的独立 GEN-023。该结论只关闭当前 continuation blocker，不是通用接敌质量完成 |
| GEN-023/024 route advance scope | required literal 未广告，随后 sibling proof 不完整 | `production-live / resolved for continuation` | `b4a1cc4 / 9be9571` formal continuation 穿过旧帧并推进至战争结束；逐 moving army 的 exact proof 只授权一个全局日 |
| one-generation G1 | `production-live loop / incomplete` | 不变 | 最新 durable checkpoint `53224848 / history=4561 / E317CB7F...C2EE`；角色仍活、cleanup GREEN，待自然死亡与琉焰卿 committed 人生分数 |

## 失败、阻点与未知

- 三轮没有 capability RED；最新轮 action、checkpoint、paused postcondition 与 cleanup 全绿。
- 五档 STOP 与核心终局结果已有 live research；strict warscore parity 因同档也漂移而保持 RED-inconclusive，不能把核心结果一致
  写成完整确定性，也不能把差异归因于速度。
- query 复制热点已经显著下降；life-advance 首/尾仍为 `4.569s / 3.643s`，其剩余 native wait/进程成本尚未由本轮拆分，不能把
  24.684 秒运行段写成全部性能问题已解决。
- GEN-014/015、signed ID 与本次婚姻回复 blocker 已有最窄实现和 live continuation，不再是当前 B1；原 RED 与 diagnostic
  artifacts 继续保留，不能被后续 GREEN 改写。
- GEN-018 与随后暴露的 GEN-019 都已有最窄实现及 fresh live continuation；两轮 RED 继续保留，不能被后续 GREEN 改写。
- GEN-020/021 的 immutable RED 与 GEN-022 same-date diagnostic 继续保留；`4a7d7ce` formal run 已真实穿过端点，故 GEN-022
  blocker-removal 保持 `production-live loop / resolved for continuation`。该 run 最终因独立 GEN-023 B1 停止，不能倒退 GEN-022 状态。
- GEN-023/024 的原始 RED 均保留；`b4a1cc4 / 9be9571` formal continuation 已真实穿过对应帧并继续到战争结束，故不再是当前 B1。
- G1 的真实 blocker 仍是尚未完成一代自然寿命与 committed settlement，不是测试覆盖或性能提交数量。

## 文件与提交

- 实现提交：`79b8d2a`、`e0688c7`、`9ff04ae`，均已 push 到 `origin/master`。
- 本轮报告/专题：`docs/autonomous-agent-progress/daily/2026-08-28.md`、`weekly/2026-W35.md`、
  `one-generation-blocker-ledger.md`、`docs/testing-workflow.md`、`docs/ck3-native-ai/player-counterpolicy.md`。
- 后续实现提交：`36d6262`、`f1230f6`、`5f8687a`、`cf98648`、`c21c096`、`1048a45`、`e619219`、
  `ae8da4f`、`9c5066f`、`d1b11b9`、`816442e`，均已 push 到 `origin/master`；
  本次进度文档正在滚动更新，尚未单独提交。
- 五档 parity 与 contact endpoint 提交：`3c2a9c7`、`0ec7e2f`、`b5865f3`、`9b7d254`、`76cae78`、`4a7d7ce`，
  均已 push 到 `origin/master`。
- 后续吞吐与 sentinel 提交：`b4a1cc4`、`9be9571`、`82817df`、`212ce21`、`a1607ca`、`4b6764e`、
  `b0d7782`、`052825f`、`23e0efa`、`31a219a`、`f4ddc3d`、`42a813d`、`d75d10c`、`dcf7f16`、`289c85f`、`42601a5`。

## 下一步

1. 从 `53224848 / E317CB7F...C2EE` 冷恢复同一角色/episode，按正式合同继续 `50000`-turn run；普通 bound 且角色仍活时续跑。
2. 普通 active combat 使用 speed 3 decision sentinel；出现 player-won pursuit 或双 `4x` crush 时冻结 checkpoint，补
   `1,5,5,1` 校准并让 qualifying terminal 生产路径自动 speed 5、零中间暂停。
3. 自然死亡后等待琉焰卿 `ready=true / commit_serial=1` committed settlement，记录
   `one_life_settlement.final_score` 人生分数，并核对顶层 `score`、`recorded_episode.score`、record persistence、cross-run record、
   零继承人 gameplay 与 cleanup 全绿后才标 G1。

## 范围边界

- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。
- 不为未发生的安全形状新增门禁；继续优先处理实际功能、性能和 G1 运营结果。

## 01:47 计划外工具链插单：Edge TTS / Sonia

状态：**完成；不改变上述 G1/readiness 结论**

- 按所有者 01:27 批准的新增事项，未来完整能力 show-off 构建器已从 Windows SAPI / `Microsoft David Desktop`
  迁移到 `edge-tts==7.2.8`，默认女声为 `en-GB-SoniaNeural`。SAPI helper、PowerShell 与 `System.Speech`
  路径已移除；manifest `voice` 与 `--voice` 均改为 Edge voice short name。
- `narration.en.mp3` 与 `narration.edge-tts.json` 使用 staged metadata + media rollback 事务；缓存指纹覆盖 provider、
  包版本、声音、旁白文本和中性 rate/volume/pitch。有效缓存可离线复用，clean synthesis 与 `--force` 需要联网，
  没有 SAPI fallback。
- 9/9 离线 mock、Python compile、`validate_static.py`、正式 27 章 `--validate-only` 全绿；后者验证 27 chapters /
  36 source references，且不会构造 Edge TTS 网络客户端。
- 在线短句为 `31968 bytes / 5.328s / MP3 24 kHz mono`，SHA-256
  `40441557531320CC17F9EE5B81400F09C249A768E39706032F999B7A775FDC03`。单章 title-card E2E 为
  `216057 bytes / 6.099333s / 2560x1440 / H.264 yuv420p + AAC 48 kHz stereo`，SHA-256
  `3A64988BC284D607E910C0C03B927E9A0B02F5B91563BB4AA7E39CCB5BF6EFAE`；sidecar 记录
  `edge-tts / 7.2.8 / en-GB-SoniaNeural`，抽帧已确认中英文字在安全区内。
- 初版缓存提交可能在 metadata 失败时留下新媒体/旧 metadata；独立复审后已补齐普通异常与 `KeyboardInterrupt`
  的旧媒体回滚，并用不同新旧 payload 回归。当前无遗留 blocker。
- 2026-08 canonical 成片未重制，其 David/SAPI 声音、路径、时长、媒体 SHA 与 sidecar 均不变；历史 manifest 继续绑定
  提交 `be7fc8b481d2551483c80d6e68bb586a652f3bbd` 和冻结 SHA-256
  `BFC4F38B3F47882888EB7B5AE754CD67125D8CE81D4BAA2649C302D688A72B32`。现行同名 manifest 只作为未来 Sonia 输入。
- 核心提交 `e205a28` 已 push 到 `origin/master`。本项只提升未来展示旁白构建合同，不提升任何 CK3 gameplay capability；
  下一次月报完整视频使用 Sonia，历史月报不替换。

## 01:19 增量：GEN-015 与持久化降本

- `0ceb7d8` 的 12-turn cold revalidation 为 `12/12`、6 gameplay、2 checkpoints、cleanup GREEN，checkpoint 推进到
  `53210904 / 367967CD...C3221`。六次 pause 都只提交一次，因此没有冒充 rare retry live coverage。
- 正式 run `20260827T171107Z-one-generation-ee8ac4b9` 又完成 25 gameplay 与 8 checkpoints，随后在 resume ACK 后没有收到
  running frame；游戏日志确认 CK3 实际已 running。报告/阻点 SHA 分别为 `52CED5F7...C8419 / C9BFA9E6...82114`。
- 未修改 cold replay 从有效 `53211480 / FBC40774...D9E9C` 恢复同一角色/episode，首日推进成功；随后 pause ACK
  `submitted -> already_paused`，日志确认 CK3 已 paused，但 Python 仍为 `paused=false`。这把根因收窄为真实 state frame
  publication/consumption 缺口，而不是命令未执行。
- GEN-015 最小实现：只有 `already_paused/already_running` 绕过 bridge 内部 previous-snapshot 去重并强制 fresh publish；resume 与
  pause 均只在同 owner、原 10 秒 deadline 内最多两次，且最终仍必须收到真实 semantic frame。
- 性能子线程在冻结 80.7 MB 状态上把持久 barrier 中位数从 `0.944s` 降到 `0.234s`，单日预计节省 `0.710s`；compact JSON
  反序列化语义与旧格式完全相等，无 schema/version/API 变化。当前正移植到最新 HEAD，live A/B 前不写 production-live。
- 用户新增独立研究项：战争/战斗推进的 `1/2/3/4/5` 全速度矩阵，按行军、接触、交战、围城/突击、撤退阶段分别给出实时观测、
  超调与战力悬殊准入/退出条件；研究线程不占用 CK3，不会把静态结论直接改成正式策略。

## 01:48 增量：GEN-015 static GREEN 与全速战斗矩阵

- GEN-015、对称 bounded resume retry 与 compact driver-state barrier 已合入 clean mainline；两个独立只读审阅均为 PASS。
  `native_driver` 为 `158 passed, 128 subtests passed`，全量 Python 为
  `1350 passed, 2 skipped, 908 subtests passed`。
- fresh Release 构建 `xar-native-gen015-20260828T0145Z` 完成 `37/37` CTest，公共 header dependency gate GREEN。
  DLL/injector SHA-256 分别为 `50227D28...831F2` / `2F6CEB43...35B5C`。新 CMake 已直接保存正确 UTF-8
  `/showIncludes` 前缀，fresh helper 只新增 direct-exact 分支，旧 mojibake 确定性修复与 Ninja dependency gate 均保留。
- 战斗速度研究已覆盖全部 `1/2/3/4/5`。原生证据确认各速执行同一逐日 movement/contact/combat 链；高倍速不会减少
  游戏计算，只缩短外部介入窗口。每日暂停不是引擎要求，近期先施工 speed-2 的有界 early-battle tranche；speed 3 待同
  checkpoint A/B，speed 4 只保留 native deadline guard 下的 capped cruise，speed 5 战斗待 tactical sentinel 与 crush
  forecast。权威矩阵写入 `docs/ck3-native-ai/battle-speed-control.md`，当前仍为 static-confirmed / live pending。
- 以上仍未构成 GEN-015 production-live 或 G1。下一动作仍是从 immutable
  `53211480 / FBC40774...D9E9C` 使用 fresh DLL 冷恢复，先跑短 revalidation，再继续同一角色正式长跑。
- 实现与技术文档提交在合并同时发生的 Edge TTS 远端增量后 rebase 为 `36d6262`，已 fast-forward push 到
  `origin/master`；没有覆盖远端提交。

## 03:23 增量：负 ID 与婚姻 blocker 实机解除，正式 G1 再启动

- 五档战斗速度专题随后以独立提交 `5f8687a` 收口：CK3 在 `1/2/3/4/5` 速都执行相同的逐日
  movement/contact/combat 链，高速只缩短外部观测窗口。该结论仍是 `static-confirmed / live pending`；没有将 speed 2–5
  擅自上线，也没有用研究替代 G1 长跑。
- `f1230f6` 将 native snapshot publish/delivery rejection 暴露给 driver。diagnostic report：
  `C:\Users\xenoa\AppData\Local\Temp\xar-delivery-diag-f1230f6-state\runs\20260827T181439Z-one-generation-a991f39a\report.json`，
  SHA-256 `A6827430C6B37D1BFA7F11F08E10831B92023C34F53F489C8F803EE87E52A3AB`。artifact 保留 8 个 consumer
  rejected frames；CK3 最后状态仍为 `date_raw=53211600 / speed=1 / paused=false`，拒绝原因是
  `native pending_character_interaction is malformed`。GEN-015 因此被收窄为 wire consumer 问题，而不是 timeline 命令未执行。
- `cf98648` 贯通 pending full ID 的 signed int32 合同，`-1` 保持唯一 invalid sentinel。全量 Python 为
  `1352 passed, 2 skipped, 913 subtests passed`，fresh native 为 `37/37` CTest；DLL SHA-256
  `67B7231B55FB55788D1069C984589B91ABC0D25F8540B7E42FF7BFC4703CB535`。fresh live report：
  `C:\Users\xenoa\AppData\Local\Temp\xar-signed-pending-cf98648-state\runs\20260827T184232Z-one-generation-25fe58db\report.json`，
  SHA-256 `0D2BBFA638ED2BEC3F27D754DE5B64AE931F2627D4EB51FBC3F03EF125CC77D0`；实际负 ID
  `-2013265918` 已通过 snapshot 与 typed query，无 malformed frame。run 随后只因婚姻 special 尚未分类而 planner-blocked，
  `first-blocker.json` SHA-256 `A8DF58EBF6AB25EDF633BFA6596A33ECBB8B6901E9A156F8A79FF5A6951F64D3`，cleanup GREEN。
- exact-build 原生树确认该请求是 stock `arrange_marriage_interaction`。当前没有发送时 `ai_accept` 数值、secondary pair 或
  alliance 后置状态，因此 `c21c096` 只匹配本次实见的 direct/四角色/无 intermediary/六 option 全未选/reject legal 合同并拒绝。
  该动作会留下五年 `player_declined_marriage`，明确是非最优 G1 blocker-removal，不是完整婚姻 AI。
- `c21c096` canary report：
  `C:\Users\xenoa\AppData\Local\Temp\xar-marriage-reject-c21c096-state\runs\20260827T191804Z-one-generation-8c116e3e\report.json`，
  SHA-256 `3980E4A2CD7F140A98488184C2095B3B41EF92EC80505B837177200705DD3973`。结果为 `12/12` turns，
  negative-ID query→reject→旧 ID null，5 个 visible gameplay，2 个 checkpoints；最终 checkpoint 为
  `date_raw=53211576 / EEBE541E5C6CA8372E95F294FA3C93B9E7A423D8E8281EE7E0FE9BC4CFB0B57B`，角色 `29829`
  存活、cleanup 全绿。它是可恢复的 `bounded_incomplete`，不是 G1。
- 正式 run `20260827T192055Z-one-generation-1d8c0f50` 已从 EEBE checkpoint 启动，参数为
  `--max-turns 50000 --timeout 604800 --readiness-timeout 300 --checkpoint-every-advances 3`。当前 startup report 仅有
  `seed_archived / in_progress / finalized=false`；正式 run 尚未完成，不记录虚构的 final turns、cleanup、terminal 或人生分数。
  CharacterID `29829` 自然死亡后仍必须额外等到琉焰卿 committed scoring，并记录三处一致的人生分数，才可标 G1。

## 03:26 增量：正式 run 命中 GEN-018

- 上述正式 run 已于 03:25 finalize 为 `blocked/failed`。report：
  `C:\Users\xenoa\AppData\Local\Temp\xar-marriage-reject-c21c096-state\runs\20260827T192055Z-one-generation-1d8c0f50\report.json`，
  SHA-256 `FC7D4E5069C84A4D10A3E5359A387C3F9BD5CD422FFE20D45ACCEB0ADDD4DF90`；`first-blocker.json` SHA-256
  `AB9FDD76D0251070F1A12AAE8CAE51C2CB23B0CA675EF229DF42724C35500AB0`。
- run 完成 `104/105` successful turns：54 query、50 gameplay，其中 49 visible gameplay；16 个 periodic checkpoints 全部成功。
  最新 durable checkpoint 为 `date_raw=53212728 / history=3172 / SHA-256
  ED3675867A2780CCD0FD9B77AE80E3BDDFC40EEA47879224914470001732E2A7`，可恢复。角色 `29829` 仍活、settlement
  `not_terminal`，cleanup 全绿。
- GEN-018 发生在 WarID `33554527`。ArmyID `33554818` 到 Province `5715` 已有 `safe_one_day_contact_horizon`；另一支
  可控 ArmyID `150995278` 驻 Province `8658`，非战斗/非撤退、无 route、move target 不可观测，却仍被列为
  `threatened_stationary_armies`。planner phase 为 `native_war_route_contact_horizon_global_blocked`，无 selected step，拒绝让
  第一支军队的安全窗口替整个玩家军队集合授权时间。
- 这是新的真实 planner B1，不是 turn/wall bound、cleanup、signed ID 或婚姻回归。下一步先补 exact-build 多军队/驻军原生树，
  再做最小可验证 continuation 并从 ED367586 checkpoint 冷恢复；G1 与人生分数仍未完成。

## 05:11 增量：GEN-018/019 解除、五档 STOP 实机与最新恢复点

- `1048a45` 首先为 GEN-018 尝试 subject-bound stationary horizon；live 证明 `target=current` 会在 native reader 返回
  `route_unavailable`。`e619219` 随后复用同一 fresh moving horizon 已发布的完整 hostile timelines，按原生闭区间语义重投影
  stationary hold。canary `20260827T201025Z-one-generation-03f57fda` 为 `12/12` turns、4 gameplay、2 checkpoints、
  cleanup GREEN；report / bound SHA-256 为 `95B33802...3E9CB / 5150DF83...2783`，最终 checkpoint
  `53212824 / 44C98F...6FCDF`。正式 run 随后继续推进，证明 GEN-018 已 production-live 关闭。
- 后续正式 run `20260827T201247Z-one-generation-c1cdfbc7` 完成 `301/302` successful turns：77 query、224 gameplay、
  38 visible gameplay、12 checkpoints；elapsed `940.429s`，角色与 episode 不变，cleanup 全绿。report / blocker SHA-256
  为 `C011A2B6...226 / 1BF4F666...A87`；最后 durable checkpoint 为
  `date_raw=53213688 / history=3308 / b60348da...324f1`。
- 该 run 暴露 GEN-019：CUnit `150995278` 自 canonical CUnit `33554818` 登船后连续 59 游戏日逐省同步，却被旧 reader 投影为
  `regular / target=null / route=[]`。planner 对它做 186 次 preview，全部 `army_not_move_ready`，耗时 `572.765s`
  （约占全 run `60.9%`）。exact-build 原生链确认 raw-kind `1` 通过 `CFleet+0x1C` 间接连接 CArmy，原生 move/contact gate
  只接受 raw-kind `0`；`GetUnitState=regular` 不代表可独立下令。
- `816442e` 在 `ReadArmies` 源头只发布 raw-kind `0`、有效 `CUnit+0x178 → CArmy` 且
  `CArmy+0x124 == 当前完整 CUnitID` 的 canonical tactical row；并在未知 non-orderable row 首次 preview 拒绝后停止扫描其它目标。
  full Python 为 `1374 passed, 2 skipped, 915 subtests passed`；fresh native build `37/37` CTest。fresh DLL / injector SHA-256
  为 `E66E9235...8573 / 0295D6EA...CDD`；独立 exact-build 复核未发现反例。
- fresh cold canary `20260827T210853Z-one-generation-19717952` 从 `53213688 / b60348da...324f1` 启动，`12/12`
  turns 全成功：7 query、5 gameplay、5 visible gameplay、2 checkpoints，推进到 `53213808`；report / bound SHA-256
  为 `7BB967D2...AC5 / 7A7C6BD6...233`。增量 history `3309..3323` 对 `150995278` 的出现次数与 preview 次数均为零，
  `33554818` 仍保留并完成两次 proof-bound route advance；最终 checkpoint SHA-256
  `467CB119404AAB2FA401DE95DBE17600A3E269B444D68CBCD7A1D48B8D50A441`，paused/map-ready/角色存活/cleanup 全绿。
- 五档活跃战斗 STOP matrix artifact：
  `C:\Users\xenoa\AppData\Local\Temp\xar-smx-stop-ae8-20260828-01\artifacts\stop-envelope-active-battle-1to5.json`，
  SHA-256 `0B6818DA2630786A31EE28729694CA911A27781C9E8A60F1955B4F07F0DE6FEA`。调度
  `1,2,3,4,5,5,4,3,2,1` 的 10/10 arms 都恰好推进一日，观察过冲与暂停收敛过冲均为零，checkpoint 不变且 cleanup GREEN。
  端到端吞吐分别为 `0.317 / 0.575 / 0.799 / 0.998 / 1.669` 游戏日/秒，即相对一速约
  `1.00× / 1.81× / 2.52× / 3.15× / 5.26×`。样本仅 `n=2/档`，只证明该 checkpoint 的 stop envelope；五档
  ongoing/terminal 结果 parity 继续由隔离线程验收，不据此上线 selector。

## 06:24 增量：1–5 速 parity research 实机收口

- `3c2a9c7` 将全部 `1/2/3/4/5` 速度纳入 research matrix。上述 active-battle STOP 仍只证明 stop envelope；
  另从同一 immutable battle checkpoint 对 1/2/3 速做一日与三日 ongoing frame 配对。artifact 分别为
  `C:\Users\xenoa\AppData\Local\Temp\xar-smx-parity-ae8-20260828-01\artifacts\ongoing-parity-1to3-day1-v4.json`
  （SHA-256 `1148E8DF2904D70C9BF0E1DE574E99295614C8A25CBA42F1306D90B32BEE269A`）与
  `ongoing-parity-1to3-day3-v5.json`
  （SHA-256 `C7A9E7658C65977523E6586212BC4A563CDC4DE18F73831E3F6D8DD7910EACD5`）。两轮均为
  `1,2,3,3,2,1`、12/12 operational，完整 normalized battle frame 严格一致；三日吞吐相对一速为
  `1.000× / 1.610× / 2.360×`，达到 2/3 速 research 门。
- 五档 terminal 平衡复测 artifact 为
  `C:\Users\xenoa\AppData\Local\Temp\xar-smx-parity-ae8-20260828-01\artifacts\terminal-parity-1to5-balanced-v3.json`，
  SHA-256 `1C19372B51CDE8A66380F556B51495476A668DD028C705CADC7EE7D51E731103`。十笔 exact terminal date、
  phase/day、winner、Result/wipe、ordered sides、removal 全等；只有 battle warscore 漂移，且合并重复样本后每档都能看到
  同档漂移。因此核心终局一致，但 strict parity 仍为 RED-inconclusive，当前证据不能把 warscore 差异归因于速度。
- 这是 research-only live evidence：2/3 速成为受控多日 tranche 候选；4/5 速任意中途决策仍缺 native same-day sentinel，
  5 速 crush 仍缺 overwhelming checkpoint 与 calibrated forecast。production selector 继续 speed 1。

## 08:05 增量：GEN-020/021 failure evidence 与 GEN-022 bounded follow-up

- formal run `20260827T221605Z-one-generation-f02c81cb` 为 `676/677` successful turns、296 gameplay、
  110 visible gameplay、36 checkpoints；report / first-blocker SHA-256 为
  `231FAF47A6F6F4B29EE5F508D36F50D0B6EAF0DED426614E051C274C9963A924 /`
  `4B93EE56EC680C130F1D28351598E2D5AB842C5EBA75A2B0CFE3967756AADED4`。它推进到 immutable
  `date_raw=53216400 / history=3661 / 26298014...4383` 后，在 `53216424` 命中 GEN-020：185 条 exact objective
  route 都先于玩家首跳在当前 Province `5692` 与 `117440838` 的 `53216448` point overlap 冲突。
- `0ec7e2f` 先冻结 exact-build 原生账本；`b5865f3` 只加入 proof-bound unavoidable exact-day。live
  `20260827T225828Z-one-generation-e74fb9df` 为 `5/6`，精确 `53216400→53216424→53216448` 后仍无
  combat/retreat/war transition；report / blocker SHA-256 为
  `0CAA13221A470D71496ACE49B6C1C43E0260F002C13ACAB510C05F64EF06791E /`
  `A4EE8434F37293F8C96F9DB1C69C7CEF8866EBCC4CB45EB3EDE440F01F62925A`，cleanup GREEN。
- `9b7d254` 增加 conflict hostile 实际入省观测；live `20260827T231856Z-one-generation-2318df2a` 仍 RED，
  report / blocker SHA-256 为
  `5E67925F38D85D068131914254495603480156F4FCB2F7ED899313471DA4A079 /`
  `F495A83251BD346C96481668C6E007C5C90D753D0121492E59146FEB4536B7EED`。这实证否定“最后已发布帧中敌军已经进入
  `5692`”；闭区间 ETA endpoint 不能冒充 movement/contact lifecycle 已完成。
- `76cae78` 增加一次幂等 same-date paused refresh 与完整失败证据。formal run
  `20260827T234150Z-one-generation-46069983` 中 ACK 为 `already_paused`，public/native revision
  `10/9→11/10`，但 subject 仍 `33554818@5692` moving、hostile 仍 `117440838@5693` moving，均非 combat/retreat。
  report / blocker SHA-256 为
  `4D21FD1662906C094544E80D15B840B7B31BB745EF8F1F9DBDD974054B64085C /`
  `39CA98290B6B595800CB51FD7D67045DDBD4B7D69C5DF0B6BC8E0309D0BDDE66`，cleanup GREEN。这排除了单纯 stale cache。
- `4a7d7ce` 只允许 point-overlap marker 后再推进一个相邻日；第二日仅接受 episode terminal、active-war set change、
  subject removed、active combat 或 retreat。敌军入省/改 intent 不能冒充成功；仍无强 transition 时记录
  `exhausted_without_strong_transition` 并禁止第三日。独立复审发现并修复 Province drift 绕过第三日限制；相关回归
  `376 passed / 191 subtests`，driver `165 passed / 141 subtests`，全量
  `1394 passed, 2 skipped, 928 subtests passed`，`git diff --check` GREEN。

## 08:28 增量：`4a7d7ce` formal production continuation 与 GEN-023

- formal run 路径为
  `C:\Users\xenoa\AppData\Local\Temp\xar-marriage-reject-c21c096-state\runs\20260828T000753Z-one-generation-a09470a0`；
  从冻结 `53216400 / 26298014...4383` 按
  `--max-turns 50000 --timeout 604800 --readiness-timeout 300 --checkpoint-every-advances 3` 冷恢复。
- 该 run 已真实穿过 GEN-022 严格相邻日 follow-up，并继续到 `53220312`；因此 GEN-022 的 blocker-removal 保持
  `production-live loop / resolved for continuation`。run 随后在独立 GEN-023 finalize RED：`380/381` successful turns，
  counts 为 `query=211 / gameplay=169 / checkpoint=54 / recovery=0 / terminal=1`，visible gameplay `166`，elapsed
  `810.217s`。report / first-blocker SHA-256 为
  `AFEECF331F298F73F41D62A4CA78AE1C69C5BD09850EF4E692394645FDA12809 /`
  `52C46EA6AA0335A7A7086021EC5B3FD1BF2AC373AFE1673C9B8E8659C0AAAD50`。
- 最新 durable checkpoint 为 `date_raw=53220288 / history=4087 / size=79345233 / SHA-256
  C8C2926F63451725ABE9C496B7966B5C3971FD0B06CC5223B832A657176567D5`。CharacterID `29829`、episode
  `native-29829-ee172aa720db` 均未改变且角色存活；cleanup 全绿、CK3 已退出。该 run 的 163 个推进日折合
  `12.07 游戏日/分钟`，推进事务约 `4.555 秒/日`，不是 30 秒/日。
- GEN-023 发生在 `53220312`：ArmyID `117440751→3610` 的 exact horizon 已证明
  `53220312→53220336` contact-free，但 backend 没有广告 required advance literal，错误原文为
  `the exact route is contact-free for one day but this backend cannot advance it`。下一施工仅修复 capability/subject-scope
  映射，然后从 `53220288` 冷恢复；G1、terminal 与人生分数仍未完成。
- 五档速度都已纳入研究矩阵，但当前交叉路线形状只把 speed 2 作为下一 research A/B 候选；selector 继续 speed 1。

## 10:23 增量：战争吞吐 P0、持久化降本与零暂停预研

- `b4a1cc4` 先让“fresh timed subject + 其它 moving/stationary 安全 row”的全局 conjunction 正确广告已有一日推进；
  cold replay 从 `53220312` 推进到 `53220384`，并把 durable anchor 更新为
  `53220360 / history=4104 / 94FBEFF444F18303CAE9BF369EA33AA26DA704ADB759402F480DAD8EB77B26AA`。
  随后 GEN-024 证明另一条 moving sibling 的 current Province 恰在 closed horizon endpoint 被敌军占用，不能继续借主 subject
  的 proof 放行。`9be9571` 改为逐 moving Army 收集同 snapshot/date/native revision、同 hostile scope 的 fresh exact proof，
  多份 proof 只共同授权一个全局日；正式续跑已跨过旧帧，GEN-023/024 均为 production-live continuation。
- `9be9571` 正式 run 路径为
  `C:\Users\xenoa\AppData\Local\Temp\xar-marriage-reject-c21c096-state\runs\20260828T005943Z-one-generation-151eb143`。
  为优先解决用户明确指出的战争速度工期问题，runner 在安全 checkpoint 后被人工 Ctrl+C 中止；其 `report.json` 保持
  `seed_archived / in_progress / finalized=false`，SHA-256
  `D2F4BD16ADBF46BCC39714603E96F960B2FCF244FC02D0C96FCA650026F5CF1A`。这是有意 harness interruption，不是 capability RED，
  CK3/受管进程树已经退出。seed `53220360` 后的 driver history 新增 459 行，其中 153 个 life advance、53 个 save checkpoint；
  durable 边界以前为 152 个 advance，尾部另有一日 `53224872`，恢复时按 checkpoint history 截断。
- WarID `33554527` 在 1317 日时仍为 `-100`；`53224752→53224776` 的下一次原生日结算后 WarID 消失，随后四支玩家军
  `67109252 / 83886358 / 117440751 / 218103933` 全部成功解散。最新 durable checkpoint 为
  `date_raw=53224848 / history=4561 / size=81626939 / SHA-256
  E317CB7F79508D1518E5B27B36173757A7E9926B8284BE3DE8EF0BBC0762C2EE`；CharacterID `29829`、episode
  `native-29829-ee172aa720db` 不变且角色存活。不可变停机备份位于
  `C:\Users\xenoa\AppData\Local\Temp\xar-g1-pre-speed-p0-20260828T091744`；配套 driver-state 为
  `93957625 bytes / SHA-256 01CDD4B97FF51B7F38520EDB4574F6E37399940B69FE245F8A081A8C9D3A066C`。
- `82817df / 212ce21` 将 fresh `one_day_contact_free` route 的正式默认档提升到 speed 3；unavoidable-contact endpoint 仍为
  speed 1，speed 4/5 只有显式 research A/B 才能使用。相同 frozen seed `53220360` 的 production-path 1–5 矩阵全部
  `6/6` turns、严格 `53220360→53220384`、`elapsed_days=1`、最终 paused、checkpoint/cleanup GREEN。route turn 实测为
  `3.184053 / 2.250474 / 2.172263 / 1.811434 / 1.380542s`；report SHA-256 依次为
  `9979B9A1...E9700 / DEF5511F...0697 / 8AEEC3BD...0963 / 7DF10F02...B79E / 0200B030...321`。
  这证明 3 速正式路径可用，也证明固定往返成本使 2/3 速收益被压缩。
- `a1607ca` 对已有长期 episode 做 load-time compaction，并让未来 `war_progress_before/after` 只保留 active-siege objective
  detail；完整 objective identity 仍留在 `war_objective_province_ids`，动作/失败/checkpoint/close durable barrier 不变。
  冻结 state 离线从 `79,884,717→16,818,672 bytes`（`-78.946%`），encode+write
  `0.4759→0.1098s`，两次 planner + deferred query + action barrier 端到端 `0.4936→0.1343s`，且每组仍严格一次
  encode/atomic write。集成后同 seed 实机 A/B：speed 1 `3.184053→2.597862s`（`-18.4%`），speed 3
  `2.172263→1.607000s`（`-26.0%`）；两组 driver-state 都落为 `16,872,234 bytes`，严格一日、`6/6`、paused、
  checkpoint/cleanup GREEN。report / checkpoint SHA-256 分别为
  `E4AFB464...F1A8 / AF70DD8A...3711` 与 `91BF27AA...D3D1 / F9468086...A931`。
- 查询本身不是剩余瓶颈：有效矩阵的 route query 通常 `0.0487–0.1015s`，完整 planner 约 `10ms`；一次 `2.946s`
  是冷异常。下一项因此不是继续猜测性重构查询，而是 native decision-epoch sentinel，把“每天 pause/RQ/barrier”缩为真正策略输入变化才停。
- `4b6764e` 已提交 research-only 零中途暂停 gate：普通战按 army/CombatID、route/contact、retreat、ordered roster、终局或原生
  auto-pause 等 decision epoch 停；terminal mode 忽略普通 phase/winner 变化。候选、research-run、production 三层分开；明确碾压
  暂以同一 paused frame 的 current fighting 与 native side strength 双重 `4x` 作为可复现实测定义，另允许 winner 已定的 pursuit
  或敌 fighting pool 归零直接跑到 finalizer。正常 speed-5 臂要求 external pause 与 rich RQ 都为零，production 仍必须有 native
  sentinel live 与真实 overwhelming checkpoint 的 `1,5,5,1` 矩阵。
- 只读筛查覆盖 47 个 live artifacts 的 310 帧与 50 个唯一 driver-state hash 的 1654 帧，没有已经满足双重 `4x` 的
  checkpoint。最小可复现入口是 `xar-owner-subset-reopen-seed-v1\canonical` 的 `53178624` cold-bound checkpoint：
  `67353788 bytes / SHA-256 81034D76...F2DB`，CharacterID `36108`、CombatID `335544325`、main day 12；current fighting
  比为 `4.71985x`，side strength 为 `3.52177x`，只差后者门槛。可从此用 decision sentinel 运行至首次双重 `4x` 或 decided
  pursuit，只在 qualifying 帧停一次并立即冻结 checkpoint/driver/sidecar，无需等待 G1 随机再开战。现有
  `xar-smx-parity-ae8-20260828-01` 是明显劣势 seed，只用于 decision-epoch primitive；另有
  `xar-one-generation-resume-20260827T1847-0848d61-state` 的 `53195952` 已决定败退 pursuit seed，可验证 terminal mode，不能冒充
  碾压胜局。
- 三次 launch-only 失败 artifact 继续保留但排除在性能样本外：`014559/b350db8f` 与 `014630/8587ea02` 缺显式 game-dir，
  `020137/b2b7ac6b` 是新 runtime 尚未 prepare-profile；report SHA-256 分别为
  `C48339D3...EA88 / BE6E6A5F...F42C3 / C6A59A3C...A930E`。三次均 0 turn、CK3 未启动、seed 未推进。
- 当前已推送 HEAD 为 `3be9520`，包含 speed-3 证据、历史 compact 与零暂停 research gate。native sentinel 正在修复独立复审发现的
  四个真实合同问题并重跑 build/tests；完成 exact-build live 前不写 production-live。G1 仍未完成；恢复正式 run 后仍必须等
  CharacterID `29829` 自然死亡，再额外等待琉焰卿 committed settlement，并记录三处一致的人生分数。

## 12:47 增量：普通战 3 速 production-live、全监视终局 5 速零暂停

- `f4ddc3d / 42a813d` 将 exact-build decision-epoch sentinel 接入正式战斗 selector；`dcf7f16 / 42601a5` 将 watch 集合扩为
  全部可控 canonical army，并修复实机暴露的 command 长度与 idle army 非正 direct-target 读取；`289c85f` 用绝对日 gate
  取代固定逐日 tactical stop。普通 active combat 现在默认 speed 3，不再把 speed 2 当最终候选，也不再每日 pause/RQ/barrier。
- 最终 production report 位于
  `C:\Users\xenoa\AppData\Local\Temp\xar-full-watch-production-56eac58-state\runs\20260828T043404Z-one-generation-ce48a71c\report.json`，
  SHA-256 `30C247B6C470BB1B867D90456282A25B6D30CC85E49C805263A2427AB32A7CEC`。immutable seed 为
  `53195952 / 08964AFA6D6CD56C6F7ACB9B24A79E30FC7C125936FD88E6635E4008B6203686`；正式 planner 选择 speed 3，
  watch IDs 为 `33554797 / 33554818 / 67109252 / 83886358 / 117440751 / 218103933`，连续 4 日抵达
  `53196048` terminal。`3/3` turns、零中间/外部暂停、零 running rich query、零过冲，wrapper/observed terminal 与 cleanup 全绿。
- 终局 speed-5 全监视 primitive 位于
  `C:\Users\xenoa\AppData\Local\Temp\xar-full-watch-terminal-speed5first-56eac58.json`，SHA-256
  `AE8D8EC25B4CF38BB864212099F9F9251C0BD37E0362BE22EE60B45C60DEFEF4`。同 seed 的 `[5,1]` 两臂终点与核心 outcome 完全一致，
  speed 5 `2.000491s` 对 speed 1 `10.030168s`（约 `5.01×`）；两臂均零中间暂停、零外部暂停、零 running rich query、零过冲。
  这是玩家败退 pursuit，只证明 terminal primitive；未把它夸报为玩家碾压胜局。
- 预研结论已经落到可执行合同：ordinary maneuver→terminal 通常只需约 3 个语义停点，main→terminal 约 2 个，pursuit→terminal
  约 1 个；若原生 legality 只差日期，则直接跑到最早合法日，最多 45 日。decided/crush terminal mode 忽略普通 phase/winner 变化，
  以 speed 5、零中间暂停直接到 terminal。player-won pursuit selector 已写入正式策略；双 `4x` crush 仍等真实 qualifying checkpoint
  做 `1,5,5,1` 矩阵，不因缺少随机样本阻断 G1。
- 保留两类真实 RED：首次全监视 arm 超过旧 128-byte action step 上限；放宽后又因 idle army 的 direct target 指向
  `ProvinceID <= 0` 被判 unavailable。最小修复后 full watch 固定为 6；没有引入额外诊断 API。fresh native `39/39` CTest；
  主线 canonical Python 为 `1402 tests / 3 skipped / 948 subtests` 全绿。12:51 首次复跑因临时 worktree 未向子进程传入
  Git `safe.directory`，6 个 environment tests 全在同一 `git status` 前置处报 harness error；按该 worktree 的标准 Git 环境重跑后
  `Ran 1402 tests in 155.345s / OK (skipped=3)`，没有代码失败。
- readiness 变化：ordinary battle speed 3 为 `production-live loop`；full-watch terminal speed 5 为
  `production-live primitive`；player-won pursuit 与双 `4x` crush 的对应胜局样本仍为 `live pending`。下一步恢复 CharacterID `29829`
  的正式一生长跑；自然死亡后额外等待琉焰卿 committed 算分并记录人生分数，不能在 mod 算分之前结算 G1。

## 13:18 增量：和平态 war-entry 单目标查询热路径收窄

- 正式 G1 run
  `C:\Users\xenoa\AppData\Local\Temp\xar-marriage-reject-c21c096-state\runs\20260828T045914Z-one-generation-f3d7241c`
  在 13:00 后实证了与此前 route query 不同的隐藏热点：截至 history `4749`，显式 `query-declarable-wars` 只有 6 次，
  但单目标 `query-war-entry-assessments-v1-1-29097` 已执行 573 次。`debug.log` 的 `13:00:15–13:08:53` 区间有 77 组
  `allowed_against_character` burst，每组 12 条，平均间隔 `6.816s`、中位 `6s`；同期每轮只推进一个游戏日。
- exact-build 调用链已闭合：planner 每个新 native revision 会重新请求同帧 assessment；bridge 明确只接收一个 target，旧实现却先调用
  全局 `ReadDeclarableWars`；后者遍历 Character storage 全部 slot，并对每个 live character 遍历全部 enabled CB。故单目标请求的
  实际成本是 `O(all characters × enabled CB types)`，同时解释了无 faith/culture target 产生的大量错误日志。
- 最小修复给 `GameAdapter` 增加 target-specific 读取入口，exact adapter 直接复用既有
  `ck3_11906::ReadDeclarableWarsForTarget`。`query-war-entry-assessments-v1` 仍在同一 paused expected snapshot 上复核请求的唯一
  target，复杂度降为 `O(enabled CB types)`；没有缓存旧合法性，没有改变 declaration legality、策略 cadence、pause 语义或持久化合同。
- 独立 fresh MSVC/Ninja build 完成，native `39/39` CTest 全绿；fixture 额外断言 target-only 读取只执行一遍 2 个 CB，source
  contract 拒绝 assessment 分支回退到全局扫描。fresh DLL SHA-256
  `66C1D10DEBFD17345520436F40A323290F921C6ED9D418EC7BFF153B646B5401`，source fingerprint
  `AEFF1C212BB001D765D26BF4DA98A85E207B27C9D72D9EBE6B3D20499398DD12`。当前 readiness 为 `static-ready / live pending`；
  本施工没有操作或停止当前 CK3/runner，也不以 CTest 冒充实机吞吐收益。
