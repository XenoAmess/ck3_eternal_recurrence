# 2026-08-28 日报

状态：**滚动日报；GEN-015 timeline state publication blocker-removal 实现与 fresh-DLL 复验中**

当前滚动更新时间：2026-08-28 01:52（Asia/Shanghai）

正式收口：2026-08-29 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-08-28.md`](../meetings/daily/2026-08-28.md)（00:02 真实补录）

起始分支/HEAD：`agent-mainline-20260827` / `79b8d2acfbe1f80a7a7f88da3fed7cda1791017c`

当前已推送 HEAD：`36d626227fe93e46bcce437159251f41340e9629`；`origin/master` 同步。GEN-015、compact persistence、
fresh-build compatibility 与五档战斗速度专题已提交并推送。

## 今天完成了什么

- 从同一个 immutable checkpoint `date_raw=53209560 / SHA-256
  A8DD4034C32856B8D1E05D6B834BBBF3C51AA74DA038BB22A0CA23A998AD76CF` 连续完成 `79b8d2a`、`e0688c7`、
  `9ff04ae` 三轮 CK3 live A/B。三轮逐步移除 timeline wait、life history 与 planning/termination query 内部的完整 transcript
  复制，没有更改 gameplay policy 或放宽动作后置条件。
- `79b8d2a` 运行段为 `48.134s`：首/尾 query 约 `3.398s / 2.579s`，首/尾 life-advance 约
  `5.065s / 4.600s`。`e0688c7` 运行段为 `44.875s`：query `3.317s / 2.516s`，life-advance
  `4.583s / 4.111s`。
- `9ff04ae` 运行段降为 `24.684s`；六次 query 约 `0.050–0.068s`，首/尾 life-advance 为
  `4.569s / 3.643s`。该轮 `12/12` turns 全部成功，包含 `6` gameplay、`6` queries、`2` checkpoints；六次推进均为
  `timeline_policy=player_tactical / speed=1`、各推进 `1` 游戏日，动作后全部真实 `paused=true`，managed cleanup 全绿。
- 最终 durable checkpoint 为 `date_raw=53209704`，SHA-256
  `39379D0224788198FECCCA82DA4B7B7257DB7E1AEE6B3750F62AA845E312678A`；配套 driver-state SHA-256 记录为
  `D47DAA...BDA`。它是继续同一 CharacterID `29829` / episode 的最新恢复入口，不是 terminal 或 G1。
- 确定性计数/计时与 live A/B 互相闭合：life history 完整复制 `9 → 1 → 0`；planning transcript 复制
  `1 → 0`，局部计时约 `600.637ms → 5.813ms`；termination query 内部复制 `3 → 0`，局部计时约
  `1815.527ms → 1.852ms`。这解释了 query 降到几十毫秒，但不把仍需约 3.6–4.6 秒的 native life-advance 冒充已消除。
- 最新实现全量 unit 为 `1341 passed, 2 skipped, 900 subtests passed`；独立审查 `PASS`。`e0688c7` 与
  `9ff04ae` 已提交并推送。
- 正式 `9ff04ae` 长跑已真实恢复，但在 `85/86` turns、42 gameplay、14 checkpoints 后命中新的 GEN-014：一次
  `pause-map` ACK 后没有在原 10 秒窗口观察到 paused。RED artifact、`date_raw=53210712 / F15D383B...35559` checkpoint 与
  matching driver state 均已冻结，CharacterID `29829` 仍活且 cleanup 全绿。
- 未修改 `9ff04ae` 从同一个 F15D checkpoint 的独立 4-turn 重放没有复现；两次 life 均逐日推进并回到 paused，产生更新
  checkpoint `date_raw=53210760 / 79B71103...85F2`。最小 blocker-removal 只允许同 owner 在原 deadline 内补交一次幂等 pause；
  聚焦 `12 passed, 10 subtests passed`、完整 driver `153 passed, 128 subtests passed`、全 unit
  `1344 passed, 2 skipped, 908 subtests passed`；独立复审 PASS，尚待 live revalidation。

## 早会承诺对照（滚动）

| # | 早会承诺 | 当前结果 | 证据 / 未完成原因 | 是否顺延 |
|---:|---|---|---|---|
| D1 | 对 `79b8d2a` 做同 checkpoint lean snapshot live A/B | 完成并继续优化 | 三轮均从 `A8DD4034...AD76CF` 起跑；最终 `9ff04ae` 运行段 `24.684s`、`12/12`、2 checkpoints、paused 与 cleanup 全绿 | 否 |
| D2 | 继续 adaptive speed-3 production gate | 部分完成 / 未命中 | 六次 gameplay 全为 `player_tactical / speed 1 / elapsed_days=1`；相同玩家 12-hop route 与敌军追尾门仍成立，没有任何 speed-3 live sample | 是；只等自然出现合法帧 |
| D3 | 恢复正式一代人 G1 长跑 | 部分完成 / 新 B1 | 正式 run 已完成 42 个新 gameplay turns 后命中 GEN-014；RED 已冻结，未修改重放又将可恢复进度推进到 `53210760 / 79B71103...85F2`。最小修复 static-ready，待 live revalidation 后续跑 | 是，立即继续 |
| D4 | 完成死亡后的琉焰卿人生分数结算 | 未开始 | CharacterID `29829` 尚无自然死亡 terminal/committed settlement；不能记录分数 | 是，等待真实 terminal |
| D5 | 次日午夜逐项收口并滚动更新 W35 | 等待 08-29 00:00 | 当前仅为滚动日报；W35 仍进行中 | 否 |

### 计划外完成

- `79b8d2a` live profiling 证明剩余成本不只在 timeline wait；因此按真实计时追加 `e0688c7`，把 life history copy 从 `1` 降为
  `0`，再追加 `9ff04ae`，移除 planning 与 termination query 的 transcript copy。两项都是 D1 暴露后的最小性能修复，不是
  理论安全扩张。

## 现在在做什么

- 正在关闭 GEN-014 并以 `53210760 / 79B71103...85F2` 作为最新 durable anchor 做 cold revalidation；通过后立刻恢复正式 G1。
  自然出现 `remote_enemy_route` 合法帧时继续收集 speed-3 elapsed/safety 证据，当前 `player_tactical` 帧继续使用 speed 1。
- 角色死亡后继续等待琉焰卿 Mod 的 committed settlement；在权威人生分数与全部 terminal gates 形成前不结束 runner。

## 为什么做这些

- 用户指出 121 条查询约耗时 26 分钟。旧实现的 driver-state 全量重写、timeline/history 深复制以及 planning/termination query
  内部 transcript 深复制都在真实 production history 或同 checkpoint live A/B 中被测到；三轮修复直接减少这些已发生的成本。
- speed 3 是另一条吞吐路径，但当前玩家已提交战术路线且敌军追尾。保持 speed 1 是现有策略的正确结果；强行制造 speed-3 样本会
  改变战争风险合同，也不能证明真实可用。

## 证据与验证

| runtime | 同 checkpoint 运行段 | query 首 / 尾 | life 首 / 尾 | 结论 |
|---|---:|---:|---:|---|
| `79b8d2a` | `48.134s` | `3.398s / 2.579s` | `5.065s / 4.600s` | timeline wait 复制已减，但 query/life 仍有完整 transcript copy |
| `e0688c7` | `44.875s` | `3.317s / 2.516s` | `4.583s / 4.111s` | life history copy 已为零；planning/termination query 仍主导 query 延迟 |
| `9ff04ae` | `24.684s` | 约 `0.050s / 0.068s` | `4.569s / 3.643s` | planning/termination copy 清零；12-turn live loop 与 cleanup GREEN |

- 三轮使用同一 `A8DD4034...AD76CF` 起点，因而可比较运行段；这里只报告观察值，不把单次 12-turn 样本外推成长跑稳定倍数。
- 最新轮为 `12/12`、6 gameplay、6 queries、2 checkpoints；每次 gameplay 只推进 1 天并回到 paused。
- 最终 checkpoint `39379D0224788198FECCCA82DA4B7B7257DB7E1AEE6B3750F62AA845E312678A`；driver state
  `D47DAA...BDA`；cleanup 全绿。
- 全量 unit：`1341 passed, 2 skipped, 900 subtests passed`；独立审查 `PASS`。
- 正式 RED：`20260827T163217Z-one-generation-ace7cbcf`，report SHA-256 `49D2A8BB...B50808`，first-blocker SHA-256
  `A0BD8C79...A484`；`85/86`、42 gameplay、14 checkpoints、cleanup GREEN。
- GEN-014 non-repro：同 F15D checkpoint 的未修改 4-turn replay 为 `4/4`，两次 life 后 checkpoint
  `53210760 / 79B71103...85F2`，cleanup GREEN。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| read-only persistence batching | `static-ready` | `production-live loop` | 三轮真实 query/action/checkpoint/close；最终 driver state 已持久化且 cleanup GREEN |
| lean life snapshot/history | `static-ready` | `production-live loop` | life 完整 history copy `9 → 1 → 0`；三轮同 checkpoint A/B |
| planning/termination query transcript | 每次仍复制完整 transcript | `production-live loop` | planning `1 → 0`、termination `3 → 0`；最新 query 约 50–68ms |
| adaptive speed 3 | `static-ready / live pending` | 不变 | 最新六次均是合法 `player_tactical / speed 1`；仍无 speed-3 elapsed sample |
| GEN-014 bounded pause retry | absent | `static-ready / live pending` | 最多两次幂等 pause、原 10 秒 deadline、同 owner gate；全 unit `1344 passed, 2 skipped, 908 subtests passed`，独立复审 PASS |
| one-generation G1 | `production-live loop / incomplete` | 不变 | durable anchor 推进到 `53210760 / 79B71103...85F2`；仍待 GEN-014 live 关闭、自然死亡、琉焰卿人生分数与 cleanup |

## 失败、阻点与未知

- 三轮没有 capability RED；最新轮 action、checkpoint、paused postcondition 与 cleanup 全绿。
- speed 3 仍未 production-live。当前相同玩家路线/敌军追尾形状持续命中 `player_tactical`，这是一项未命中，不是失败。
- query 复制热点已经显著下降；life-advance 首/尾仍为 `4.569s / 3.643s`，其剩余 native wait/进程成本尚未由本轮拆分，不能把
  24.684 秒运行段写成全部性能问题已解决。
- GEN-014 是真实 B1：首次 pause ACK 后未观察到 paused。它不是 GEN-012 的 pre-submit revision starvation，也没有证据证明为
  去复制优化回归；同 checkpoint 重放未复现，只能标为瞬时 pause application/publication 时序边，具体侧未查明。
- G1 的真实 blocker 仍是尚未完成一代自然寿命与 committed settlement，不是测试覆盖或性能提交数量。

## 文件与提交

- 实现提交：`79b8d2a`、`e0688c7`、`9ff04ae`，均已 push 到 `origin/master`。
- 本轮报告/专题：`docs/autonomous-agent-progress/daily/2026-08-28.md`、`weekly/2026-W35.md`、
  `one-generation-blocker-ledger.md`、`docs/testing-workflow.md`、`docs/ck3-native-ai/player-counterpolicy.md`。
- GEN-014 blocker-removal 与当前文档修改尚未 commit/push；不得把 static-ready 误写成 production-live。

## 下一步

1. 构建 fresh exact DLL，完成 GEN-015 idempotent timeline state republish、bounded resume retry 与 compact persistence 全量测试。
2. 从 immutable `53211480 / FBC40774...D9E9C` cold revalidate；观察真实 pause/resume state frame、逐日进度、compact barrier 耗时和 cleanup 后，立即恢复正式 G1。
3. 战斗速度研究扩为 `1/2/3/4/5` 全档；只有 exact-build 原生树、live A/B 与阶段化准入/退出 gate 闭合后才改 production selector。
4. 自然死亡后等待琉焰卿 `ready=true / commit_serial=1` committed settlement，记录
   `one_life_settlement.final_score` 人生分数，并核对顶层 `score`、`recorded_episode.score`、record persistence、cross-run record、
   零继承人 gameplay 与 cleanup 全绿后才标 G1。

## 范围边界

- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。
- 不为未发生的安全形状新增门禁；继续优先处理实际功能、性能和 G1 运营结果。

## 01:47 计划外工具链插单：Edge TTS / Sonia

状态：**完成；不改变上述 G1/readiness 结论**

- 按所有者 01:27 批准的新增事项，未来完整能力 show-off 构建器已从 Windows SAPI / `Microsoft David Desktop`
  迁移到 `edge-tts==7.2.8`，默认女声为 `en-GB-SoniaNeural`。SAPI helper、PowerShell 与 `System.Speech`
  路径已移除；manifest `voice` 与 `--voice` 均改为 Edge voice short name。
- `narration.en.mp3` 与 `narration.edge-tts.json` 使用 staged metadata + media rollback 事务；缓存指纹覆盖 provider、
  包版本、声音、旁白文本和中性 rate/volume/pitch。有效缓存可离线复用，clean synthesis 与 `--force` 需要联网，
  没有 SAPI fallback。
- 9/9 离线 mock、Python compile、`validate_static.py`、正式 27 章 `--validate-only` 全绿；后者验证 27 chapters /
  36 source references，且不会构造 Edge TTS 网络客户端。
- 在线短句为 `31968 bytes / 5.328s / MP3 24 kHz mono`，SHA-256
  `40441557531320CC17F9EE5B81400F09C249A768E39706032F999B7A775FDC03`。单章 title-card E2E 为
  `216057 bytes / 6.099333s / 2560x1440 / H.264 yuv420p + AAC 48 kHz stereo`，SHA-256
  `3A64988BC284D607E910C0C03B927E9A0B02F5B91563BB4AA7E39CCB5BF6EFAE`；sidecar 记录
  `edge-tts / 7.2.8 / en-GB-SoniaNeural`，抽帧已确认中英文字在安全区内。
- 初版缓存提交可能在 metadata 失败时留下新媒体/旧 metadata；独立复审后已补齐普通异常与 `KeyboardInterrupt`
  的旧媒体回滚，并用不同新旧 payload 回归。当前无遗留 blocker。
- 2026-08 canonical 成片未重制，其 David/SAPI 声音、路径、时长、媒体 SHA 与 sidecar 均不变；历史 manifest 继续绑定
  提交 `be7fc8b481d2551483c80d6e68bb586a652f3bbd` 和冻结 SHA-256
  `BFC4F38B3F47882888EB7B5AE754CD67125D8CE81D4BAA2649C302D688A72B32`。现行同名 manifest 只作为未来 Sonia 输入。
- 核心提交 `e205a28` 已 push 到 `origin/master`。本项只提升未来展示旁白构建合同，不提升任何 CK3 gameplay capability；
  下一次月报完整视频使用 Sonia，历史月报不替换。

## 01:19 增量：GEN-015 与持久化降本

- `0ceb7d8` 的 12-turn cold revalidation 为 `12/12`、6 gameplay、2 checkpoints、cleanup GREEN，checkpoint 推进到
  `53210904 / 367967CD...C3221`。六次 pause 都只提交一次，因此没有冒充 rare retry live coverage。
- 正式 run `20260827T171107Z-one-generation-ee8ac4b9` 又完成 25 gameplay 与 8 checkpoints，随后在 resume ACK 后没有收到
  running frame；游戏日志确认 CK3 实际已 running。报告/阻点 SHA 分别为 `52CED5F7...C8419 / C9BFA9E6...82114`。
- 未修改 cold replay 从有效 `53211480 / FBC40774...D9E9C` 恢复同一角色/episode，首日推进成功；随后 pause ACK
  `submitted -> already_paused`，日志确认 CK3 已 paused，但 Python 仍为 `paused=false`。这把根因收窄为真实 state frame
  publication/consumption 缺口，而不是命令未执行。
- GEN-015 最小实现：只有 `already_paused/already_running` 绕过 bridge 内部 previous-snapshot 去重并强制 fresh publish；resume 与
  pause 均只在同 owner、原 10 秒 deadline 内最多两次，且最终仍必须收到真实 semantic frame。
- 性能子线程在冻结 80.7 MB 状态上把持久 barrier 中位数从 `0.944s` 降到 `0.234s`，单日预计节省 `0.710s`；compact JSON
  反序列化语义与旧格式完全相等，无 schema/version/API 变化。当前正移植到最新 HEAD，live A/B 前不写 production-live。
- 用户新增独立研究项：战争/战斗推进的 `1/2/3/4/5` 全速度矩阵，按行军、接触、交战、围城/突击、撤退阶段分别给出实时观测、
  超调与战力悬殊准入/退出条件；研究线程不占用 CK3，不会把静态结论直接改成正式策略。

## 01:48 增量：GEN-015 static GREEN 与全速战斗矩阵

- GEN-015、对称 bounded resume retry 与 compact driver-state barrier 已合入 clean mainline；两个独立只读审阅均为 PASS。
  `native_driver` 为 `158 passed, 128 subtests passed`，全量 Python 为
  `1350 passed, 2 skipped, 908 subtests passed`。
- fresh Release 构建 `xar-native-gen015-20260828T0145Z` 完成 `37/37` CTest，公共 header dependency gate GREEN。
  DLL/injector SHA-256 分别为 `50227D28...831F2` / `2F6CEB43...35B5C`。新 CMake 已直接保存正确 UTF-8
  `/showIncludes` 前缀，fresh helper 只新增 direct-exact 分支，旧 mojibake 确定性修复与 Ninja dependency gate 均保留。
- 战斗速度研究已覆盖全部 `1/2/3/4/5`。原生证据确认各速执行同一逐日 movement/contact/combat 链；高倍速不会减少
  游戏计算，只缩短外部介入窗口。每日暂停不是引擎要求，近期先施工 speed-2 的有界 early-battle tranche；speed 3 待同
  checkpoint A/B，speed 4 只保留 native deadline guard 下的 capped cruise，speed 5 战斗待 tactical sentinel 与 crush
  forecast。权威矩阵写入 `docs/ck3-native-ai/battle-speed-control.md`，当前仍为 static-confirmed / live pending。
- 以上仍未构成 GEN-015 production-live 或 G1。下一动作仍是从 immutable
  `53211480 / FBC40774...D9E9C` 使用 fresh DLL 冷恢复，先跑短 revalidation，再继续同一角色正式长跑。
- 实现与技术文档提交在合并同时发生的 Edge TTS 远端增量后 rebase 为 `36d6262`，已 fast-forward push 到
  `origin/master`；没有覆盖远端提交。
