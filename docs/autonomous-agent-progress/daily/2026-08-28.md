# 2026-08-28 日报

状态：**进行中；Edge TTS 插单已完成**

当前滚动更新时间：2026-08-28 01:47（Asia/Shanghai）

正式收口：2026-08-29 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-08-28.md`](../meetings/daily/2026-08-28.md)（01:27 补录，不冒充 00:00 已知计划）

起始分支/HEAD：`master` / `5bc54852cd87d71012fbc25e6f8d451b4439ddd9`

## 今天完成了什么

- 按所有者批准的范围，把后续完整能力 show-off 构建器从 Windows SAPI / `Microsoft David Desktop` 迁移为
  `edge-tts==7.2.8`，默认女声固定为 `en-GB-SoniaNeural`；SAPI helper、PowerShell 和 `System.Speech`
  路径已移除。
- `voice` manifest 字段与 `--voice` 现在均表示 Edge voice short name；缺省、`null` 或空白值均回落 Sonia。
  中性 `rate / volume / pitch` 固定进入缓存指纹和 sidecar，构建格式版本已提升，旧 SAPI 缓存不会被复用。
- 新旁白先写临时 MP3，经 FFprobe 验证为有效 MP3 后再提交 `narration.en.mp3` 与
  `narration.edge-tts.json`。媒体与 metadata 提交失败或中断时恢复旧缓存，不留下半成品。
- 两份依赖锁定、Windows 静态 CI、静态 validator、离线 mock 回归、展示 manifest 与说明文档均已同步；
  核心提交 `e205a28` 已 push 到 `origin/master`。
- 未重制 2026-08 canonical 成片。历史 David/SAPI manifest 已用 commit-pinned URL 与冻结 SHA-256
  `BFC4F38B3F47882888EB7B5AE754CD67125D8CE81D4BAA2649C302D688A72B32` 单独保留 provenance；
  现行同名 manifest 只作为未来 Sonia 构建输入。

## 早会承诺对照

| # | 早会承诺 | 结果 | 证据 / 未完成原因 | 是否顺延 |
|---:|---|---|---|---|
| D1 | 完成 Edge TTS / Sonia 展示视频构建迁移 | 完成 | `e205a28` 已 push；9/9 离线回归、静态校验、在线短句与单章 title-card E2E 均 GREEN；历史成片未改 | 否 |

### 计划外完成

- 本项是所有者在既有一代人自动游玩主线之外批准的工具链插单；它只改变未来月报展示层旁白，不改变任何 CK3 gameplay capability。
- 独立复审发现并促成两项收口：缓存媒体/metadata 的事务回滚覆盖普通异常与 `KeyboardInterrupt`；历史 David manifest
  与现行 Sonia manifest 的 provenance 明确分离。

## 现在在做什么

- 本插单已完成；等待 2026-08-29 00:00 按早会 D1 做正式日报收口。自动游玩主线仍按既有周计划推进。

## 为什么做这些

- 所有者要求未来能力展示使用英国英语女声 Sonia。迁移使旁白声音跨 Windows 机器一致，并把 provider、版本、声音与
  韵律参数纳入可复现缓存和成片 sidecar；代价是 clean synthesis 与 `--force` 重合成需要 Edge 在线服务。

## 证据与验证

- 最新远端组合树：`python -m compileall -q tools`、`python tools/test_build_full_agent_showcase.py`
  （`9/9 OK`）、`python tools/validate_static.py`（`STATIC VALIDATION OK`）与 scoped `git diff --check` 全绿。
- `--validate-only` 对正式 27 章 manifest 完成 `27 chapters / 36 source references` 校验，并由离线 mock 证明不会构造
  Edge TTS 网络客户端。
- 在线 Sonia 短句：`artifacts/demos/2026-08-28/edge-tts-sonia-smoke.mp3`，31968 bytes、5.328s、MP3
  24 kHz mono，SHA-256 `40441557531320CC17F9EE5B81400F09C249A768E39706032F999B7A775FDC03`。
- 单章 E2E：`artifacts/demos/2026-08-28/edge-tts-title-card-smoke.mp4`，216057 bytes、6.099333s、
  2560x1440、H.264 yuv420p + AAC 48 kHz stereo，SHA-256
  `3A64988BC284D607E910C0C03B927E9A0B02F5B91563BB4AA7E39CCB5BF6EFAE`。
- E2E sidecar：`artifacts/demos/2026-08-28/edge-tts-title-card-smoke.video.json`，`3988 bytes`，SHA-256
  `9517CFAC15A6E0C71454D4EB6897366305C2649EE4CF00D921416AB92339D955`；记录 provider `edge-tts`、版本
  `7.2.8`、声音 `en-GB-SoniaNeural`、中性设置、旁白 `5.328s` 与镜头 `6.078s`。
- 抽帧 `artifacts/demos/2026-08-28/edge-tts-title-card-smoke-frame.png` 已目检：双语标题和中文字幕均在安全区内，
  无缺字或裁切；SHA-256 `B974CBA9A2E5521CD5F08CE929ACEB04EC98EE37F836B0328D74745C80335C5A`。

## 月报素材线索

- 上述单章是构建验收 smoke，不是新的 canonical 月报成片，也不替换 2026-08 的 20 分钟完整能力视频。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| 未来 show-off 英语旁白 | Windows SAPI / David，本机 voice 依赖 | Edge TTS 7.2.8 / Sonia，构建与缓存合同完成 | 9/9 离线回归 + 在线短句 + 单章 E2E；clean/force 合成需联网 |
| 2026-08 canonical 视频 | `complete`，David/SAPI | `complete`，字节不变 | 未重制；路径、时长、媒体 SHA 与 sidecar 保持历史值 |
| CK3 自动游玩能力 | 既有周报状态 | 无变化 | 本任务只改展示工具链，不得据此提升 gameplay readiness |

## 失败、阻点与未知

- 初版缓存更新先替换媒体、后写 metadata；复审确认 metadata 提交失败可能造成不一致。现已改为 staged metadata +
  media rollback 事务，并用不同的新旧音频 payload 覆盖普通异常和 `KeyboardInterrupt`。
- Edge TTS 是有意接受的在线依赖；无有效缓存且服务不可达时构建会明确失败，不提供 SAPI fallback。当前无遗留 blocker。

## 文件与提交

- 核心文件：`tools/build_full_agent_showcase.py`、`tools/test_build_full_agent_showcase.py`、
  `tools/requirements*.txt`、`.github/workflows/static-ci.yml`、`tools/validate_static.py`。
- 展示合同与 provenance：`docs/autonomous-agent-progress/demos/2026-08-27-full-capability-showcase.manifest.json`、
  `docs/autonomous-agent-progress/demos/README.md`、`docs/autonomous-agent-progress/monthly/2026-08.md`。
- commit/push：`e205a28`（`Migrate showcase narration to Edge TTS`）已 push 到 `origin/master`。

## 下一步

1. 下一次月报完整能力视频直接使用 Sonia；只有需要重新合成的章节才访问在线服务，有效逐章缓存继续复用。
2. 2026-08-29 00:00 正式收口本日报；本插单不占用或改写既有一代人主线的 capability 状态。

## 范围边界

- 未运行完整 27 章重制，未替换历史月报视频，未新增 SAPI fallback、离线模式或韵律配置面。
- 宗教域与其它 gameplay 范围均未触碰。
