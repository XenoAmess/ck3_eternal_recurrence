# 2026-09-10 日报

状态：**进行中**

正式收口：2026-09-11 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-10.md`](../meetings/daily/2026-09-10.md)

起始分支/HEAD：`root-rebase-r345-20260909` / `34cdaa7fa4384bb5340fc972b1a87df4f1b07ef2`

## 今天完成了什么

- 00:00 已接续 R372 的 harness RED，冻结 report、driver-state 与 cleanup sidecar；CK3 串行槽已可靠释放。
- D1 timeout/cleanup 合同修复已由 commit `f766a58` 推送；Official Runner CI run `34375813001` / job
  `102548196817` 约 4 分钟完成，结论 `GREEN`、失败步骤为空。
- 使用新逻辑回放既有 R372 实证报告后，cleanup 结论为 `cleanup_only GREEN`、`failed_checks=0`；会话结论仍为
  `TIMEOUT`，产品结论仍为 `INCOMPLETE`，并明确 `resume_required=true`。这是分类修正，不是产品完成度提升。
- `natural_disaster.7031` 的通用合同、analysis 与第八条 observation 已由 commit `d7435cf` 推送；共享资产增至
  `160 contracts / 160 analysis / 8 observations`，production runtime 增至 `299`。Official Runner CI run
  `34383321634` / job `102573260244` 在约 4 分 13 秒内 `completed/success`，失败步骤为空。
- R374 在 PID `51852` / connection generation `1` 上同 PID 热恢复 `.7031`：选择 authored `3` / native `2`，
  instance `978` 已验证 advance；随后长跑继续推进到新的原版事件 RED。
- `ep3_story_cycle_admin_eunuch.1001` 的通用 package 已由 commit `a472965` 按 rebase-only 推送；共享 inventory 保持
  `160 contracts / 160 analysis`，observation 增至 `9`，production runtime 保持 `299`，MCP schema 不变。Official Runner
  CI run `34386079583` / job `102582565979` 约 4 分 28 秒 completed/success，失败步骤为空。
- R374 同一 PID `51852` / generation `1` 选择 `.1001` authored `2` / native `1`，旧 instance `988 -> null` 的独立
  postcondition GREEN；这是第七条 production-live event primitive。

## 现在在做什么

- `.1005` package commit `ed25bd4` 已按 rebase-only 推送并通过 Official Runner；R374 已在 PID `51852` /
  generation `1` 原位热恢复，选择 authored `6` / native `5`，instance `1007 -> null` 且 postcondition GREEN。
- park4 已闭合：`.p2c.2` 有限产品观察窗口内可重复合同由 commit `2ba288b` 推送并通过
  Official Runner；R374 在同一 PID/generation 选择 authored `1` / native `0`，instance `1035 -> null`。本轮 typed-RED
  central 结果仍按失败保留，未被 summary drain 冒充为成功周期。
- `.0040` 可移植 package 已由 commit `eeea8a6` 推送并通过 Official Runner。R374 在同一 PID /
  generation 选择 authored `1` / native `0`，instance `1038 -> null`，成为第九条 production-live primitive。
- `.4001` package commit `75a611e` 已按 rebase-only push 并通过 Official Runner。R374 同 PID `51852` /
  generation `1` 选择 authored `2` / native `1`，instance `1040 -> null` 且 postcondition GREEN，成为第十条
  production-live primitive；选择前 RED observation 继续保留。
- 随后 `tgp_decision_events.0101` #1041、`tgp_interaction_event.0010` #1042、`epidemic_events.1100` #1043、
  `tribute_mission.1002` #1044 -> #1045 与 `tribute_mission.1005` #1045 均安全 drain。
- `.1007` 的 `on_death -> .0001 -> delayed .0002 -> .1007` exact-build 调用链与唯一按钮完整效果已审阅；
  严格无 killer 三 scope 合同、analysis、observation、MCP/runtime 与计数测试由 commit `c666335` 按 rebase-only
  推送并通过 Official Runner run `34401932801` / job `102635654978`。R374 同 PID/generation 选择
  authored1/native0，instance `1046 -> null`、snapshot `native:1779 -> native:1780`、revision `1780 -> 1781`、
  postcondition GREEN，成为第十一条 production-live primitive。
- 随后 `tgp_interaction_event.0010` #1047 authored2/native1、`debate_event.5110` #1048 authored2/native1 与
  `epidemic_events.1100` #1049 authored1/native0 均安全 drain。`faction_demand.2001` #1050 的五 scope、三选项中
  native1 disabled 的精确形状与 authored3/native2 安全路线已完成共享迁移；commit
  `3bb5169ca2b81701a8ea49e9d842de36f39fd87b` 已按 rebase-only push，Official Runner run `34404896747` / job
  `102645406781` 约 4 分 26 秒 completed/success、失败步骤为空。R374 在原 PID/generation 热恢复 authored3/native2，
  instance `1050 -> null`、snapshot `native:1847 -> native:1848`、revision `1848 -> 1849`、postcondition GREEN，
  成为第十二条 production-live primitive；T2 `.2001` 审计为 `NO-CODE-CHANGE`。
- `faction_demand.1101` 通用包已由 commit `0880b4d579b92685be0696df5a5a32d701db5ed2` 按 rebase-only
  推送并通过 Official Runner run `34408112586` / job `102655870657`。R374 在原 PID `51852` / generation `1`
  选择 authored1/native0，old instance `1055 -> null`、snapshot `native:2057 -> native:2058`、revision
  `2058 -> 2059`、`postcondition_verified=true`，成为第十三条 production-live primitive。
- 选择后 CK3 前景出现原版 `tgp_china_ministry.0100`“宋国库”三选项窗口，但 native publisher 仍返回
  `paused=false / active_event=null`；runner 随后把幂等 `pause-map=already_paused` 与陈旧帧循环组合成自旋 RED。
  后续仅为解开前景遮挡而执行一次明确标注的视觉兜底：按已审 source 选择 authored2/native1“维持原有分配方案”，再以
  坐标关闭宣战通知、activity detail 和唯一选项的胜利窗，并用 Windows `SendInput` 发送空格。这些都不是 MCP 动作，不能给
  `.0100` 记 production-live；可信 native instance/date/root/scopes/option-state 仍然缺失。命令 `#3200` 随后返回
  `pause-map=submitted`，`#3201`--`#3355` 共 `155` 次均返回 `already_paused`，但旧 runner 仍未读到新 paused frame。
- R375 已从 R374 `autosave@53602440` 受管重启为 PID `180544` / generation `1`。R374 final drain list 实际为
  `456` 条而非 `455`；按存档日期严格裁切只保留 `453` 条 durable seed，丢弃并重放 indices `453..455` 的
  `travel_danger_events.3002`、`debate_event.5110` 与 `faction_demand.1101`。R375 随后新增五条全 GREEN drain，至
  `.0100` 共 `458` 条：两次 `.3002`、`.5110`、`.1101` 和 `.0100`。
- `.0100` 已在新 DLL 下取得可信 native 同帧并纯 MCP 选择 authored2/native1：instance `1057 -> null`、snapshot
  `native:356 -> native:357`、revision `357 -> 358`、`postcondition_verified=true`，成为第十四条
  production-live primitive；R374 的 UI-only RED 继续保留。
- `tgp_dynastic_cycle.0081` exact-build 通用包已由 commit `862fc7fc89e61a290295a25dcdf2ca31434509be` 推送；R375
  在同一 PID/generation 原位热恢复唯一 authored1/native0，instance `1058 -> null`、snapshot
  `native:426 -> native:427`、revision `427 -> 428`、postcondition GREEN，成为第十五条 live。
- `epidemic_events.0110` instance `1059` 的选择前 scope-drift RED 已冻结；commit
  `6fc2ef2167f4af1dbb2ff4bb1be7fa70825203ea` 收口 exact-build 最小合同后，R375 在同一 PID/generation 原位热恢复
  authored3/native2，instance `1059 -> null`、snapshot `native:432 -> native:433`、revision `433 -> 434`、
  postcondition GREEN，成为第十六条 live。
- `stress_threshold.1721` instance `1060` 的 option-shape RED 已冻结；commit
  `27921a9b96c40043ddb70c89b1614f170e4ba192` 收口 exact-build 最小 variant 后，R375 在同一 PID/generation
  原位热恢复 authored10/native9，instance `1060 -> null`、snapshot `native:440 -> native:441`、revision
  `441 -> 442`、postcondition GREEN，成为第十七条 live；query `#371`、select `#372` 的不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-017-stress-threshold-1721-green.json` SHA-256 为
  `869BFE72B6FF2ABEF55FF3F4191F13D3A9F57E696F02B011367527256AD57EF3`，并由 commit `76f9a83` 接入 MCP
  observations。
- `ep3_landless_admin.1000` instance `1062` 的选择前 RED 已冻结；commit `5c8a526` 收口 exact-build 最小合同后，R375
  在同一 PID/generation 以 query `#602`、select `#603` 原位热恢复 authored3/native2，instance `1062 -> null`、snapshot
  `native:720 -> native:721`、revision `721 -> 722`、postcondition GREEN，成为第十八条 live。不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-018-ep3-landless-admin-1000-green.json` SHA-256 为
  `F545AC8E85102FBFE83B6EFEC23AEB47978844B9EF3F8FE4538C720369BAA8C4`。
- R375 最终从 `453` durable seed 累计新增 `11` 条 GREEN drains，rolling report 实际 list length 为 `464`；尾段还包括
  `death_management.1001` #1061 与 `tgp_movement_events.0080` #1063。sidecar 的 retained count `462` 是尾段写入前的
  滞后字段，不能作 final ledger。随后在 `date_raw=53635920`、无 active event 时超过 exact bound `53635896`，冻结为
  关键产品 liveness RED：并非随机/穷举门禁或 harness 故障；第三次 `.356` 是三周期首次解锁 `.361` 的必要边界，当前待
  B1 周期/名单只读诊断与 unavailable 名单的 liveness 修复。

## 为什么做这些

- 第四 source 只差第三次真实 `.356`。D1 将“按租约到期且完成清理”与“产品已完成”分开记账，避免 cleanup 健康被误报为
  harness 失败，同时继续诚实保留产品 `INCOMPLETE` 与恢复义务。
- `.7031`、`.1001`、`.1005`、`.0040`、`.4001`、`.1007` 与 `.2001` 的共享化及同 PID 热恢复证明新合同可以由 registry/MCP 复用并进入
  production runner；选择前 RED 继续保留为 observation，修复没有放宽到未知变体。
- `.2001` 首次提供了真实的 shown-but-disabled native row；把它建模为通用 `disabled_native_option_indices`，可让 CK3
  自动玩家、天朝二期与其它 mod 精确复用，而不是为当前事件写私有特判。

## 证据与验证

- R372 final report SHA-256：`62D2D0F79B11F928DBB1DA3AE05C630F05B9BAA351921CA15B399552627FCAD0`。
- Final driver-state SHA-256：`C154CA44A69AE5211C36F4EC9F05D002B850C67AA845A36CD891FA848C630633`。
- Cleanup sidecar SHA-256：`2EFF46B12030748D5ACC88291BFD2C8A6B20C3F9F1E8FD10F8C3D1525E383704`；进程树、watchdog、job 与全局 CK3 inventory 均已清空。
- 新逻辑对同一冻结报告的结果：`cleanup_only=GREEN`、`session=TIMEOUT`、`product=INCOMPLETE`、
  `resume_required=true`、`failed_checks=0`；该 D1 离线回放本身没有重跑 CK3。
- `.7031` 动作前 RED 的 immutable report / driver-state / park SHA-256 分别为
  `7B1DB1CB63CD309288FAA7774E122D74FE16E11CF3B0D6E51EB112949C21D4E9`、
  `8A63BBB6A611508B782335B4384A6F95EC189A0D7353E5C3E21EC22F8940011D`、
  `86F39D782D159FB58278726255E0862254C221E54882D2B0C8A69BACA964FE57`。
- `.1001` park2 的 immutable report / driver-state / park SHA-256 分别为
  `FAEFEF7A2F09099A697CE4A0215AECF6CCE080013D2822BBFD65BBB8B38B7116`、
  `446605FC4AB05D1DDED330929E57B2D04A73E91A601E58B4320D6E2037ED9548`、
  `F2265333D99B4E996CBAAFD5F31C616291E03438A976E3D85CD4262D4C9889D0`。
- natural-disaster 专项 normal / `-O` 各 `12/12`；registry/MCP selected tests normal / `-O` 各 `29/29`，均 GREEN。
- `.1001` package 回归：primary admin normal / `-O` 各 `7/7`，recovery normal / `-O` 各 `60/60`，registry/MCP
  selected normal / `-O` 各 `28/28`，均 GREEN。
- `tribute_mission.1005` park3 的 immutable report / driver-state / park SHA-256 分别为
  `65D685D64EC4016E952A8F439A40F0406A9AFBFF459340CA71AD8564B90063DD`、
  `02CCAA03AD0549CD69708209A39E63E92CB1F57CD342AA428AD41C9E04BEDB1F`、
  `381858FACBAD07092D4876F59D85CAC69607B497A108872F361AC0C078724FCE`。
- `.1005` focused normal / `-O` 各 `8/8`，manager recovery normal / `-O` 各 `60/60`，registry/MCP selected
  normal / `-O` 各 `37/37 + 42 subtests`，均 GREEN；共享 inventory 为 `160/160/10`，runtime `299`。
- T2 兼容复验：MCP 仍为 schema v1，open_kaishek 模块 29 tests / 0 failure；没有 API、schema 或 native ABI 变化。
- live driver command `732`：`select-event-option-6` accepted，authored/native `6/5`，old/new instance `1007/null`，
  snapshot `native:998 -> native:999`、revision `999 -> 1000`、`postcondition_verified=true`，PID `51852` / generation `1`。
- commit `ed25bd4` 的 Official Runner CI run `34388489589` / job `102590688933` 在约 4 分 10 秒内
  completed/success，无失败步骤。
- park4 immutable report / driver-state / park SHA-256 分别为
  `21E317707214B52D787226F31B01A26C1372D924AD12E40389C22FDA61A4AF71`、
  `FBAF8DBB359C73F76D01F54C4FFB37B834E51DC4643B7C64B4DCDA6778910BB6`、
  `49E8714A20465392B3F498D1D05F1C59DA9365B1185648E9813E0142FD94F22B`。
- `.p2c.2` 历史两次 GREEN 与本次 active instance 均通过相同 root/type/option 合同；manager recovery normal /
  `-O` 各 `60/60` GREEN。当前只修 occurrence policy，不改产品脚本或 MCP schema。
- commit `2ba288b` 的 Official Runner CI run `34391028529` / job `102599080015` 在约 4 分 16 秒内
  completed/success，无失败步骤。`.p2c.2` 热恢复后 old/new instance `1035/null`，
  snapshot `native:1597 -> native:1598`、revision `1598 -> 1599`，`postcondition_verified=true`。
- park5 immutable report / driver-state / park SHA-256 分别为
  `E6E1D565AA74A004B5F995457D4DB52AF5E6829430B2DCB14B16A69F4B7FB529`、
  `D0137F8780BEB0E6A18A4A4E25ABE9E1436FD683A4473CC025CE6258B22FA1E1`、
  `D1CA6DA488048CF7F77A81F53290F57AF4BA36D15ABCD6E502A9B080B2627293`。动作前 snapshot 为
  `native:1655/revision:1656`，距离产品绝对截止仍有 `2223` game days。
- `.0040` 通用包与 registry/MCP/codex-setup 联合回归 normal / `-O` 各 `37/37` GREEN；原版事件、
  interaction 与 effect 三份 exact-build source SHA-256 已在当前 1.19.0.6 游戏树重新计算并与 analysis 一致。
- commit `eeea8a6` 已普通 fast-forward push；Official Runner CI run `34395299023` / job `102613407044`
  在约 4 分 16 秒内 completed/success，无失败步骤。`.0040` 热恢复后 old/new instance
  `1038/null`，snapshot `native:1655 -> native:1656`、revision `1656 -> 1657`，`postcondition_verified=true`。
- park6 immutable report / driver-state / park SHA-256 分别为
  `9FDF93D053E001C73D9DC469DDE37BC44C3DA8F1872AC3E587C10241EA27FB51`、
  `750EEEB9AC789F075CCA2C9F5AC2E2619D3F6DABE9E12744FE26D25E3C1292ED`、
  `A94103B7788591FFD3379BEB418E6EC7DE675AA0FC4BB252DDB658086BED6A49`。`.4001` 动作前 snapshot
  `native:1669/revision:1670`，root/player `32904`，`created_witch=witch=94245`，`witch_secret` 为 secret，
  距离产品绝对截止仍有 `2196` game days。
- `.4001` exact-build 审阅冻结 11 份原版源码哈希；事件是 yearly pulse candidate，不是每日事件。native0 会添加五年
  reading modifier 并启动秘密女巫转化 scheme，native1 的完整直接效果仅为 `medium_piety_gain=100`。通用合同与
  registry/MCP/runtime 联合回归 normal / `-O` 各 `40/40` GREEN；共享 inventory 现为
  `162 contracts / 162 analysis / 12 observations`，production runtime `301`。
- commit `75a611e` 的 Official Runner CI run `34398909398` / job `102625565721` completed/success，job 约
  4 分 07 秒，无失败步骤。`.4001` live selection 为 old/new instance `1040/null`、snapshot
  `native:1669 -> native:1670`、revision `1670 -> 1671`、`postcondition_verified=true`。
- park7 immutable report / driver-state / park SHA-256 分别为
  `B69243825168B01F11C0CBDEBC321C277E3AC22CEEC2F50650ABB9C0994D1D64`、
  `8702894C65AAF57E0D943C7BAA2FC4D613E4D8C5BDDFC2E4C70DB165C774A5C6`、
  `A2E63CF1EBB67ADB0987F9360B023E1F0F4659242EE048D51A8CA1219674DEE6`。当前 snapshot
  `native:1779/revision:1780`，PID `51852` / generation `1`、paused、`selection_attempted=false`、
  `process_restart_required=false`，产品绝对截止前尚余 `1893` game days。
- `.1007` analysis 冻结 6 份 exact-build 原版源文件哈希。它由继承人死亡触发，不是 daily/yearly pulse；唯一
  authored1/native0 为不可避免的确认，完整 authored effect 是基础压力 `20`，`after` 仅 tooltip。当前合同只接受
  `new_memory`/`dead_character`/`deceased_character_stress` 严格三 scope 无 killer shape。`.1007` package 时共享 inventory 为
  `163 contracts / 163 analysis / 13 observations`，production runtime `302`。
- T2 `.4001` 兼容审计为 `NO-CODE-CHANGE`：MCP schema/API/ABI 仍是 v1，open_kaishek 没有逐事件或库存数量
  消费者；其 capability profile `3/3` GREEN，`.4001` 与 MCP setup 普通及 `-O` 模式亦全部 GREEN。
- T2 `.1007` 兼容审计同为 `NO-CODE-CHANGE`：实际 MCP 调用返回 `available` 且 contract/analysis/observations
  齐全；Python 最小测试 `7/7`、open_kaishek capability profile `3/3` GREEN，公共 schema/API/ABI 未变。
- T2 `.2001` 审计同为 `NO-CODE-CHANGE`：实际 MCP 调用返回 `available`，三投影齐全，
  `disabled_native_option_indices=[1]` 可严格 JSON 往返；Python normal `23/23`、`-O` `7/7` 与 open_kaishek
  capability profile `3/3` GREEN，MCP v1 schema/API/ABI 与固定实现哈希均未变。
- 当前已推送 `.2001` package commit `3bb5169ca2b81701a8ea49e9d842de36f39fd87b`；Official Runner run
  `34404896747` / job `102645406781` 在约 4 分 26 秒内 completed/success，失败步骤为空。
- `.1007` 热恢复 GREEN：authored/native `1/0`，old/new instance `1046/null`，snapshot
  `native:1779 -> native:1780`、revision `1780 -> 1781`、`postcondition_verified=true`；PID `51852` /
  generation `1` 未变。
- park8 immutable report / driver-state / hot-recovery SHA-256 分别为
  `AC7EF0A37844A7F0B252917DAB0922B77721F0CAE6FB2A7416BC0F4420BCF9CA`、
  `FDFD7C0B2AB7DF6DC936B9FC01D611F1F5425BA6E571CBB74942BF08A68A9F28`、
  `89B4F2F8F6ADD2243C0CAD803766EB6B82491D0EF8E3C3BCCC6B55E5BC41B561`。
- `.2001` park8 为 PID `51852` / generation `1`、date `53595360`、instance `1050`、player/root `32904`、
  snapshot `native:1847`、revision `1848`、paused、`selection_attempted=false`、`process_restart_required=false`。
  五个 scope 为 faction/raw25、faction leader `50355542`、target `32904`、claimant `39232`、targeted title/raw5；
  native0/native2 enabled，native1 shown/disabled。deadline `53635896`，尚余 `40536` 小时 / `1689` game days。
- `.2001` live selection 为 authored/native `3/2`，old/new instance `1050/null`、snapshot
  `native:1847 -> native:1848`、revision `1848 -> 1849`、`postcondition_verified=true`；PID `51852` /
  generation `1` 未变。
- `.2001` exact-build analysis 冻结 11 份 source SHA-256：faction demands
  `B06241E67B6692F51FCFC6021E4DBE085C9E25A50B9CD08957CBCC9E25AEBEA9`、00_factions
  `0A47171476811DD16EBD44A7335EAEBD17376FD87E41290F72B5C6785365B276`、_factions.info
  `FB47457AABE7C7DF78555B4DFBA74B8932DAE468C45EBB2D1381CADDC2B7E019`、00_defines
  `C1ECA141C71EC1E741CA5336E01BB538EEFAEC05B0684EDEC477CFC9053C3807`、00_faction_modifiers
  `CD3DAA9DA33C3DFD15934CA30C1B2D7381E0B3968337BF52A7CBC2F9F2237102`、00_faction_values
  `4EE4098080B8C4536E74B047801F8715DD5EB0BCDCBAC04A8E3BF563D6555932`、faction_on_actions
  `A4E3EDA2F31CB08D29DEAE1A1CDBD89256973A81FC1A1D4DF00D9CF5BBCB68FB`、00_faction_effects
  `C8E0B3C57665F775973BC8559166CD1F6414471CA25017800777C3C6466DC5D3`、legitimacy
  `DEE9D48221B49EF41490D04451ACD6DBFD4994A50EAD9D006F831F41A6247A83`、00_war_effects
  `A936E09F448EF715580A918165EAB89A9368AD2D3014E425C998CD9D4F0E8D7D`、00_civil_war
  `CFF84009E58F5D6386A6D7501CFF084CB206A542E7D0CFA221ED2CC050A9DA68`。
- park9 `.1101` immutable report / driver-state / hot-recovery SHA-256 分别为
  `BA39B64ACC3224E8F1F5D5C04719A04106430D7F2F2551DEC52CCEC514FC3AA6`、
  `78C84C846ED9D2F05AF153EC9302C4C3A85C645FD0B33316CD212B10E342C494`、
  `4E3B59B3EB6328939D2008BD2DD4138546C0224F76D62297D19415138DF577E6`。
- `.1101` park9 为 PID `51852` / generation `1`、date `53607792`、instance `1055`、player/root `32904`、
  snapshot `native:2057`、revision `2058`、paused、`selection_attempted=false`、`process_restart_required=false`。
  四个 scope 为 faction/raw25、peasant county landed_title/raw5、peasant leader character `33633057`/raw4、
  new title landed_title/raw5；native0/native1 均 shown/enabled、非 fallback/cancel。deadline `53635896`，
  尚余 `28104` 小时 / `1171` game days。原版按 eligible 月度检查而非 daily pulse；普通不满积累约 50 个月，
  高加成约 7 个月，另有 90 eligible days 更新上界。安全路线 authored1/native0 接受有明确 legitimacy、县控制与
  十年 modifier 代价，但避免 authored2/native1 立即开农民战争；宗教只作 source-authored 附带边界。
- Git 双账户整改已把三个仓库的 repo-local identity 固定为 `XenoAmess <xenoamess@gmail.com>`，并给
  `CodexSandboxOffline` 对三把仓库专用 SSH key 增加只读 ACL；xenoa 全局 `safe.directory` 已覆盖主根、
  `open_kaishek`、宣传工具和主根下工作树。机器级 gitconfig 因 Program Files 无写权被拒绝且无部分修改。
  `open_kaishek` 已通过纯 `git rebase origin/main` 前进到 `890b32d`；宣传工具因 T0-P2 硬锁未执行 pull。
  真实 CodexSandboxOffline token 下的 `status/ls-remote/push --dry-run` 仍待该账户会话验证，不能把 ACL 就绪写成 push 身份已验证。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| T0 canonical source | `3/4`, target `.356` `2/3` | 不变 | R372 timeout 前未出现第三次目标 |
| T0 overall | `50%`, stage `8/11` | 不变 | harness RED 不改变产品完成度 |
| T0-P2 | `LOCKED` | `LOCKED` | P1 尚未签收 |
| 共享事件资产 | `159/159/7`, runtime `298` | 当前 package `167/167/29`、runtime `306`；严格 portable `29/167 = 17.4%` | 10 个既有 shard 与 holy-war notice 已去除固定日期/角色绑定；legacy live binding 保留为 observation。production-live 仍为 18，不能把迁移 observation 冒充新增实机样本 |
| T2 open_kaishek | 当前兼容 horizon | `NO-CODE-CHANGE` | `.1101` 实际 MCP 查询与 JSON 往返 GREEN；公共 schema/API/ABI 无变化，open_kaishek `3/3` GREEN |
| T1 G2 | `90%` | 不变 | 当前实机工作包等待唯一 CK3 串行槽，非 CK3 静态项没有新的诚实完成量 |

## 失败、阻点与未知

- 已查明：native-session 固定 6 小时 timeout 主动结束 R372；新合同已将健康清理归类为 `cleanup_only GREEN`，同时保持
  `TIMEOUT / INCOMPLETE / resume_required=true`，没有把超时误写成产品 GREEN。
- commit `f766a58` 的 Official Runner CI 已 GREEN，无失败步骤。
- `.p2c.2` occurrence-bound 已闭合，但其上游 typed-RED terminal 仍是本周期真实失败结果。
- `.1101` 已热恢复 GREEN 并成为第十三条 live；其后 R374 retained RED 是实际可见的
  `tgp_china_ministry.0100`“宋国库”窗口与旧 native publisher 陈旧状态之间的不一致。该缺口后来已由 R375 关闭。
- “宋国库”截图位于 `_runtime/p2r374-active-boundary-continuation-live/park9-post-selection-ck3-screen.png`，
  SHA-256 为 `B2E57E4B90EC82EBA96500B7D0B1F963A6EE46F521E4E5250C1A3BE113C7047B`。标题与三个简中选项只证明
  前景 UI identity；因为 `active_event=null`，没有可信 instance/date/root/scope/option-state，不能登记为 paused live observation。
- 该事件 exact-build source 已冻结五份 SHA-256：event definition
  `87358436D60431BC80DA0376DD1BB2C47696EFCDE813148C69883768439980EE`、yearly caller
  `0FC85A284224A68D1CA0A4EF071D4F4A4F49896753AEC463975A12EE4E1116FA`、budget effects
  `AEF36B884DC5E315DD5C655BC96012FF9FA8BB46BB0AF2C18FA878C890907747`、TGP triggers
  `8294C1D72ECC909428ABFBA27D6F10B127D796D2BEE350D1BB95024F36D60C99`、简中 localization
  `6AD38C15CAE1CD3EAC713EDBC1566C7D42D8CE3B94291139F976C439020F8210`。source-reviewed 安全路线为
  authored2/native1“维持原有分配方案”。该路线后来只通过上述 visual-coordinate fallback 执行；这不是 MCP/native-live
  验证，不能补造同帧上下文。通用合同已由远端 commit `b51ccd9` 推送；它是从既有 manager key 抽取，不增加
  contract/analysis 总数，只把 observation keys 增至 `16`。
- visual fallback RED sidecar 为
  `_runtime/p2r374-active-boundary-continuation-live/r374-visual-recovery-red-evidence.json`，SHA-256
  `003875E75BB05A8CF74AF27760DDC311B9F504C3A9FA51F5E3FDBD01831AB4EC`；其冻结 driver snapshot SHA-256 为
  `B83EFAAA2574ACCDEF533C5F2B8A38BAC20255237D18D32C35B87D3F42F48F43`。sidecar 明确
  `mcp_only=false / coordinates_used=true / send_input_used=true / production_live_credit=false`，因此它只保留 R374 RED 与恢复过程，
  本身不增加第十四条 live；第十四条 live 只来自后续 R375 MCP submission/advance。
- R375 `.0100` query command `#291` 在 `native:356 / revision 357` 取得唯一窗口、root `32904`、
  `treasury_ruler=32904 / steward=38076 / military_budget=32904` 与三个 enabled native options；command `#292`
  的 authored2/native1 submission/advance 独立 GREEN。不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-014-tgp-china-ministry-0100-green.json` SHA-256 为
  `6C1407AF00D2E767FA201DA2411619D5724C86951BB5CEFF006DAB50ABC6C779`。该条只提升 R375 MCP 切片，
  不删除 R374 visual RED。
- `.0081` 已由 commit `862fc7f` 同 PID热恢复并成为第十五条 live；不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-015-tgp-dynastic-cycle-0081-green.json` SHA-256 为
  `573992BD13E2373DB3A827698B770D703263EC678ECDEE98595C0D48C6671789`。`.0110` 随后由 commit `6fc2ef2`
  同 PID 热恢复并成为第十六条 live；其不可变 GREEN sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-live-016-epidemic-events-0110-green.json` SHA-256 为
  `1C96D13A88D9F96575CA6DD78E5E02C0483ECB1ADC09505949868EC387994EEF`，并由 commit `0a65adf` 接入 MCP
  observations。`.1721` 随后由 commit `27921a9` 同 PID 热恢复并成为第十七条 live；`.1000` 又由 commit `5c8a526`
  同 PID 热恢复并成为第十八条 live。当前阻点是关键产品 liveness bound，不再把此前任一选择前 RED 写成 current RED。
- CodexSandboxOffline 已取得三把仓库专用 SSH key 的只读 ACL，三个仓库也已有 repo-local identity；但其 GitHub
  push 身份仍未在真实 Codex token 下端到端验证。xenoa 当前工作树已能 status/rebase/push，双方可读写工作树的
  文件 ACL 与 GitHub 认证是两层不同结论。
- commit `a472965` 的 Official Runner CI 已 completed/success，无失败步骤。

## 文件与提交

- D1 commit `f766a58` 已按 rebase-only 流程 push；Official Runner CI run `34375813001` / job `102548196817`
  GREEN，约 4 分钟，失败步骤为空。
- `.7031` 共享合同/analysis/observation commit `d7435cf` 已 push；Official Runner CI run `34383321634` / job
  `102573260244` completed/success，约 4 分 13 秒，失败步骤为空。
- `.1001` package commit `a472965` 已按 rebase-only push；Official Runner CI run `34386079583` / job
  `102582565979` completed/success，约 4 分 28 秒，失败步骤为空。
- `.1005` package commit `ed25bd4` 已按 rebase-only push；Official Runner CI run `34388489589` / job
  `102590688933` completed/success，约 4 分 10 秒，失败步骤为空。
- `.p2c.2` repeatable 合同 commit `2ba288b` 已按 rebase-only push；Official Runner CI run `34391028529` /
  job `102599080015` completed/success，约 4 分 16 秒，失败步骤为空。
- `.0040` 通用 package commit `eeea8a6` 已按 rebase-only push；Official Runner CI run `34395299023` /
  job `102613407044` completed/success，约 4 分 16 秒，失败步骤为空。
- `.4001` 通用 package commit `75a611e` 已按 rebase-only push；Official Runner CI run `34398909398` /
  job `102625565721` completed/success，约 4 分 07 秒，失败步骤为空。
- `.1007` 通用 package commit `c666335` 已按 rebase-only push；Official Runner CI run `34401932801` /
  job `102635654978` completed/success，约 4 分 03 秒，失败步骤为空。
- `.2001` / disabled-native 通用 package commit `3bb5169ca2b81701a8ea49e9d842de36f39fd87b` 已按
  rebase-only push；Official Runner CI run `34404896747` / job `102645406781` completed/success，约 4 分 26 秒，
  失败步骤为空。
- `.1101` 通用 package commit `0880b4d579b92685be0696df5a5a32d701db5ed2` 已按 rebase-only push；Official
  Runner CI run `34408112586` / job `102655870657` completed/success，约 3 分 31 秒，失败步骤为空。
- T2 当前版本 pin commit `720ff9b` 已推送；Official Runner CI run `34411520520` GREEN，本轮没有公共 MCP
  schema/API/ABI 变化。
- “宋国库”通用合同远端 commit `b51ccd9` 已经 rebase-only 推送。Python bounded no-spin 修复也已由
  commit `ac6f63c` rebase-only 推送：direct `pause-map=already_paused` / `resume-map=already_running` 必须在同一命令
  deadline 内取得相符 semantic frame，否则只提交一次并转为可进入 hot-recovery park 的 typed RED；它不伪造 paused
  状态，也不声称已修复 native publisher。normal/`-O` driver 与 production-entry 定向测试均 GREEN；Official Runner run
  `34413044308` / job `102671610904` completed/success。
- 根因已经缩到 native bridge 的同命令双读竞态：`already_paused` / `already_running` 的 ACK 由一次可信 native snapshot 判定，
  bridge 却丢弃该帧并立即做第二次全量读取；自动暂停边界的第二读可回到陈旧 `paused=false / active_event=null`，使 ACK 与
  publisher 脱节。commit `9ccd3995418549b18f46190c3061ede5714c81e5` 已按 rebase-only push，把判定 ACK 的同一
  snapshot 原样交给 timeline publisher，幂等 ACK 时强制发布而不是再读。MSVC Release 定向 CTest `2/2` GREEN；新 DLL
  SHA-256 为 `18787F7F7F0A9BAF85C7D6979AB8AD20C8CF80D553AA6CA8D2AB87CF18DB0840`；Official Runner run
  `34415670708` / static job `102679886593` completed/success，约 4 分 36 秒、失败步骤为空。
- R375 新 DLL 同帧实证：driver command `#343` 的 `already_paused` 后置从 `native:424 / revision 425` 前进到
  `native:425 / revision 426`，同一 `date_raw=53611224` 观察 `paused=true` 且 rejected state frames 为 `0`；随后
  `.0081` 事件帧为 `native:426 / revision 427`。旧 ACK/publisher 分裂已在该 exact-build 实机切片中关闭。
- `.0110` 选择前 RED 的不可变 freeze
  `_runtime/p2r375-post-publisher-fix-live/r375-epidemic-events-0110-red-freeze.json` SHA-256 为
  `1CD3DEC1ED7DB6F3C7DBE8D485DC2B38753F1D7E1D90BD9E8FA99A8ADADC7448`。`.1721` 选择前的不可变 RED freeze
  `_runtime/p2r375-post-publisher-fix-live/r375-stress-threshold-1721-red-freeze.json` SHA-256 为
  `6F0F849C88EC48D78803B27B6BDE94C334FDD94F61024AA1BAA48EC88E2F4508`；其同 PID GREEN sidecar SHA-256 为
  `869BFE72B6FF2ABEF55FF3F4191F13D3A9F57E696F02B011367527256AD57EF3`。`.1000` 的不可变 RED / GREEN sidecar
  SHA-256 分别为 `F74447FE01DDB9BC890C757578DBA477373167EA0DA862BB987D26A61904DFA1` /
  `F545AC8E85102FBFE83B6EFEC23AEB47978844B9EF3F8FE4538C720369BAA8C4`。终态 product-observation-bound RED sidecar
  SHA-256 为 `C3D4503117ED687D3289AD612B25C1AA36A0FA1329572D607E604E0CE7EAA9F8`；final report / driver / park5 为
  `19969B3802F038C9AA7EFFF65BE78C8E0FEC73C2FE7914F86930C09618CCE4FF` /
  `78BC8A47A1F874D56194846C00FB8055FD65274B8A5B801A513362C1AC555914` /
  `3338D5779E51D8C75B705475C91F408E9755EA13D41AB31A12D91D036417BD6D`。`.0081` 选择前 RED freeze SHA-256 为
  `19860E8323136E2EED1209428BA56D09F1C4175E4250E90B43FB4AA27D0A3C35`。
- commit `f4e2d8b`、`95f6824`、`d0f16f7`、`9caf735`、`734ddbc`、`2fa9034` 均已按 rebase-only 推送；
  Official Runner 对 `f4e2d8b`、`95f6824`、`9caf735`、`2fa9034` 的 runs `34424658413`、`34425836613`、
  `34426038180`、`34426834671` 均 completed/success。当前 HEAD 与 `origin/master` 同为 `2fa9034`。
- B1 当前真实 RED 已收窄到同 tick 两次 quota rebuild 复用 temporary list：52 与 80 人累计为 132 人，target
  `40/79/13` 与 80 人 roster recount `25/46/9` 不守恒；`30938` 不在名单中。commit `95f6824` 已做最小静态修复，
  fresh replay 仍待验证。
- 可自然重放的最晚 active-B1 checkpoint 为 R374 `autosave_2.ck3`，SHA-256
  `F1188E048188AA37FAF42168E64F5E56E8413358E26D627BC6EFEA09FC452CBB`，`date_raw=53584920`，
  cycle/case/state=`8/8/3`，当前守恒配额 `23/46/7=76`，距离 D340 还有 93 天。
- `.0081` 热重跑已正确提前归类 `SCENARIO_INVALID / NOT_EVALUATED` 且不发送新输入；rolling report 新 SHA-256
  `EE1C4F4D2696CB2D9B380EABE265043E40C8049A23D2863431DA5BE18B943381`。旧一次性 wrapper 随后用陈旧 instance
  park 并触发 cleanup，故当前 CK3 PID 数为 `0`；下一次只在 B1 MCP/DLL 与脚本齐备后独占启动一个新 PID。
- 既有事件迁移审计基线为 `167/167` contract/analysis；manager A/B 与 embedded A/B/C 已完成并行迁移后，
  campaign-neutral 合同和对应 migration observation 已达 `158/167 = 94.6%`，带 exact-build source hash 的合同为
  `79/167 = 47.3%`。最后 9 条固定身份合同与其 source hash 正在独立收口。旧日期/动态 CharacterID 只留在
  `legacy-live-binding / migration-only` observation，安全选项语义未变。
- MCP 通用化已新增纯 Python discovery index：167 个 key 的稳定分页、搜索、namespace/evidence/observation 过滤、
  dataset hash 与 typed unavailable，由 commit `b1bedad` 推送；raw/source evidence 的内容寻址 gzip 包与读取层正在并行生成。
  现有单事件 lookup 保持兼容，不把路径字符串冒充跨机可读取的 evidence。
- R375 cleanup freeze 已把旧 PID `180544` 的终态唯一分类为 ad-hoc wrapper 的 stale park identity 后受控清理，
  不是 CK3 崩溃、也不是产品 RED。不可变 sidecar
  `_runtime/p2r375-post-publisher-fix-live/r375-scenario-invalid-cleanup-freeze.json` SHA-256 为
  `9558CECB5B32EEBEBF66058893FA0662416E71E7D5F2A5B3FB758CE859EF3387`，16 项绑定自检全真；canonical runner 已在
  typed terminal 后停止后续动作且默认不停止 CK3。
- Git 运维新增 `tools/setup_git_operator.ps1` 与 `tools/invoke_git_operator.ps1`，按当前 Windows SID 使用隔离 SSH key，
  不依赖 operator profile，并拒绝 merge、非 rebase pull 与 force push。xenoa 的三仓 status/ls-remote、主仓与
  open_kaishek push dry-run 已通过；CodexSandboxOffline 的 ACL/key 已就绪，但缺真实登录 token，仍须在该 token 下执行
  `setup_git_operator.ps1 -VerifyOnly -VerifyPushDryRun`，不能虚报端到端通过。宣传仓只读发现本地落后远端，P2 锁下未 pull。
- 本轮追加 rebase-only commits：`26b8dcc`（B1/迁移进度）、`1314845`（陈旧 inventory 测试）、`46da571`
  （embedded 27/26/26 纯拆分）、`3061eb8` / `aa0a578`（manager A/B）、`0512425`（manager observations MCP 聚合）、
  `3da165c` / `d6a9703` / `60a308d`（embedded A/C/B）、`6d0a37a`（通用 Git operator）、`1334517`
  （embedded observations MCP 聚合）、`b1bedad`（事件 discovery）。Official Runner 对 `26b8dcc`、`1314845`、
  `0512425` 已 GREEN；后续提交仍以最新 CI 为准。

## 11:35 通用事件证据 MCP 收口与 P1 口径校正

- 原版事件通用资产已经完成剩余迁移，不再沿用上文的 `158/167`、`79/167` 中间态：当前
  `167/167` 事件 key 全部 campaign-neutral、`167/167` 具 exact-build definition index，并封装
  `213` 份 evidence（`157` 个唯一原版 source 文件、`56` 份 observation artifact）。source index 含
  `167` 条 definition 与 `497` 条明确标为“lexical candidate、非已证 runtime caller”的候选调用引用。
- 新只读 MCP 面包括事件知识分页/搜索、evidence 分页、按内容哈希读取 evidence，以及 source provenance 查询；
  单事件 lookup 保持向后兼容。内容包 manifest SHA-256 为
  `BC843955BF8EAC8E0133386EFDD0D9DEF4DE794C50559E4F8C93EA6DFF9A5545`，source index dataset SHA-256 为
  `50DAE4938EF8D2D7677C10C55CB89FB34189B1E11F6788DCB00A4C8FA248759B`。正常模式与 `-O` 均为
  `423 passed + 839 subtests`；真实 MCP list/call/read smoke `5/5` GREEN。
- 上述通用 MCP 由 commit `df1ed7c` 收口。既有共享 SSH key 已通过 Git for Windows 的 HTTPS proxy 通道完成
  严格主机密钥校验下的 rebase-only 推送；当前主仓 `HEAD == origin/master == df1ed7c`，未 merge、未 force push、
  未修改凭据。此前的 Git 同步 RED 已关闭，通用 operator 路径仍继续抽象，避免依赖固定账号或机器路径。
- P1 验收正在按项目所有者裁剪后的范围落盘：硬门只保留 B1 fresh live、AF5 `42/41` terminal、stage 9/10/11
  各一条真实终态、一次代表性 terminal save/cold-restore readback、error scan/managed cleanup 与 final-candidate L0。
  旧 exact `7 action + 4 observation`、source `4/4`、第三次 `.356`、`.361` 三周期、`10190-day` 与 `8/8 footage`
  只作历史覆盖证据，不再阻断 P1；宣传视频仍保持硬锁定。
- B1 fresh replay 尚未启动，当前 CK3 PID 数为 `0`。T0 仍为 `50% / stage 8/11`，当前产品 RED 仍是重复 quota
  rebuild 修复缺 fresh live 证明；通用资产完成不等于提高产品完成度。

## 11:46 R376 启动环境 RED 与 T2 同步

- 在启动前确认 CK3 为 `0` 个进程后，以唯一实例 R376 / PID `43984` 从冻结 B1 存档准备 fresh replay；使用
  exact product tree、B1 DLL SHA-256 `539A1BC1D737A303EFCA61918D867540529D230F7BDF22D8BFBD47A2BA58707B`
  与 exact-build CK3 `1.19.0.6`。进程在 `3.293s` 后以 `C0000005` 退出，尚未生成 `debug.log` 或进入产品 MCP
  query；崩溃地址为 CK3 RVA `0x1DABD89`，与历史 no-DLL 控制组也出现过的 CodexSandboxOffline 交互桌面启动
  分歧相同。因此该次明确记为 startup/environment RED，不把它冒充 B1 产品失败或 fresh-live 结论。
- R376 已死亡，当前 CK3 仍为 `0` 个进程；旧 R375 早已终止。下一次启动只能在再次确认零实例后创建新轮次
  R377，并优先交由 Windows 交互操作者或通用 operator MCP 执行，不在相同沙箱路径无意义复跑。
- R376 crash evidence 位于 `_runtime/p2r376-b1-replay-state/profile/crashes/ck3_20260910_114633/`；`exception.txt` /
  `minidump.dmp` / `meta.yml` SHA-256 分别为 `045F8647FD9285E7DC63838F48148729C6AA9121F009F6B2587E82D83F04BFB2` /
  `3F4459D9141983713C5AC614CF5EC9BC6F1A1DA6EC019DD942573DEFDBDF3D2C` /
  `11BD147FB616EDF644582910794C4F36DD69F801D46E10040F08EB673ECC9E06`。选择前 RED 与唯一进程约束均保留。
- open_kaishek 已同步四个 portable vanilla-event MCP、请求/响应 schema 与 provider/source/manifest hash；commit
  `58d513b0b4a94c3e24f758841c43c475c050edf4` 已 rebase-only 推送，`origin/main` 精确一致。Java `167/167`、
  Python domain `8/8`、metadata/domain validator 与 diff-check 全部 GREEN；B1 能力保持不变。
- T0 仍为 `50% / stage 8/11`：B1 fresh-live RED 尚未关闭，P1 未签收，P2 宣传视频保持硬锁定。本轮没有检查、
  更新或预热宣传工具，也没有制作视频物料。

## 12:35 P1 范围门、通用 Git 操作者与 operator MCP

- P1 裁剪后的成功门已由 commit `d7a7c40` 按 rebase-only 推送：主体工程只以关键链、代表性高风险路径与真实
  RED 为验收范围；旧 361 场景与 626 definitions 仍是非阻塞覆盖账本。当前 T0 保持 `50% / stage 8/11`，
  B1 fresh-live 证明尚缺，P1 未签收，P2 最终宣传视频继续硬锁定。
- 通用 Git 操作者传输由 commit `252c579` 按 rebase-only 推送，支持严格主机密钥校验的 SSH-over-HTTPS-proxy，
  不绑定固定 Windows 用户、机器路径或凭据；定向回归 `17/17` GREEN。
- 通用 operator MCP 由 commit `e684fd0` 按 rebase-only 推送，提供 capabilities/status/preflight/handoff 四个工具，
  用 profile 冻结目标身份、交互桌面、命令、路径/hash、输出缺席条件与 CK3 排他进程门。normal 与 `-O` 测试均
  `6/6` GREEN；在当前沙箱实际启动 Streamable HTTP server 后，MCP list/call 与 JSON roundtrip GREEN。
- 当前沙箱 HTTP preflight 只在 `target_identity_exact` 返回 RED：实际身份仍是 CodexSandboxOffline 的 sandbox
  desktop，不是目标 Windows 交互操作者。随后 Shell bootstrap probe 也只落在同一沙箱身份，按 RED 保留；没有调用
  handoff、没有启动 CK3。R376 已死亡，CK3 当前为 `0`，R377 尚未启动。
- `e684fd0` 新增了跨仓接口/协议面，已触发 T2；open_kaishek 的 operator MCP 兼容层正在同步，完成前不把该新增
  horizon 记为兼容完成。该包是可切换操作者/机器的 MCP 通用资产，不依赖固定 R 轮次。

## 12:55 T2 同步、final L0 与 P1 pending assembly

- open_kaishek 已以 rebase-only commit `82d03cf81eb82b5ef5c65f93b92ea03b7617d170` 同步 operator MCP
  capability/profile/client adapter 并推送，`HEAD == origin/main`、ahead/behind=`0/0`、单父提交。provider normal/`-O`
  各 `6/6`、focused Java `9/9`、Maven 全量 `176/176`、domain normal/`-O` 各 `8/8`，当前接口 horizon 恢复一致。
- 固定候选 `e684fd0` 的完整静态 L0 为 `9/9` GREEN。Phase2 `broad` 产品投影共 1,031 文件、32,372,128 bytes，
  `candidate_sha256 == tested_candidate_sha256 == 5fad3533cd9296ebe03222e16bf06dee1b3231f36dda08f03f28b80139bf5d01`；
  live `loaded_candidate_tree_sha256` 仍保持未证明，不能由静态结果代填。
- runtime P1 assembler 已用 path、SHA-256 与 JSON Pointer 冻结九项关键门；当前只把 final L0 记为 `1/9 GREEN`，
  B1、AF5、stage 9/10/11、cold restore、runtime error scan 与 managed cleanup 共 `8/9 PENDING`，预检退出码 `2`，
  没有生成 acceptance manifest，也没有恢复 361/626 的非阻塞覆盖门。
- pipe owner 静态审计确认 native endpoint 为单客户端，断开重连会递增 `connection_generation`。同 generation 的
  B1 -> AF5/stage 9-11 热续跑必须由当前 owner 复用 service 原位执行；该生命周期约束已同步到
  `docs/testing-workflow.md`。目标 operator MCP 仍未监听，CK3=0，R377 未启动。

## 13:41 operator MCP 1.1.0 与 R377 静态准备

- operator MCP `1.1.0` 已由 commit `5610de6` 按 rebase-only 推送；现有五个工具以 profile 冻结 job 与控制合同，
  状态变更控制具备同 request/job 的幂等语义。normal/`-O` 各 `9/9`、官方 MCP client smoke 与 `py_compile` 均 GREEN。
- `5610de6` 再次改变 operator MCP 接口/协议面并重新触发 T2；open_kaishek 已由单父提交
  `193551f148f2786a6baf91d85b63eadb80b1793d`（parent `82d03cf`）完成同步，按 rebase-only 推送后
  `HEAD == origin/main == remote`、ahead/behind=`0/0`，未 merge、未 force push。provider normal/`-O` 各
  `9/9`、focused `15/15`、Maven `182/182`、domain normal/`-O` 各 `8/8`，metadata/validator/portability 均 PASS。
  1.0 四工具保持兼容，1.1 五工具及 `job_controls` / control 幂等语义已同步；T2 当前接口 horizon 恢复一致。
- T1 非 CK3 复查结论为 `NO-ACTION`，仍为 `90%`；当前剩余项必须取得 CK3 实机输入，或属于需 owner 明确授权的
  utility，不为制造静态增量而扩项。
- T0 runtime prep 的双段日志合同与 cold-cleanup lineage 已通过静态验证，但尚未产生 fresh-live、恢复或 cleanup
  实机 artifact，因此不增加 P1 stage 或 T0 总百分比。CK3 当前为 `0`，R377 尚未启动；P2 硬锁未触碰。

## 14:37 R377 冻结包终审与目标 MCP 等待

- R377 no-launch 冻结 profile 为
  `Z:\ck3_mod_rewrite\_runtime\operator-mcp-r377\target-profile.json`，5,465 bytes，SHA-256
  `CE35940E9C7D4212BABB29EEF97F47B15211B30241199EE18D41F606F369B498`；job spec SHA-256 为
  `C52BF39912BB44C94AAFCD49AE6C8928DCCEC21659C5B6A0B296E246526B8E90`。15/15 个必需输入路径及哈希匹配，
  7/7 个 live 输出在启动前保持缺失，冻结控制仅为 `resume-auto` 与 `cleanup`。
- 终审确认 B1 同 PID 热恢复、五段 continuation receipt、R377/R378 双段日志、一次 cold restore、两 PID lineage、
  managed cleanup 后离线重证明与九项 gate 接线没有剩余代码级 blocker。owner/continuation/cold/postlive/assembler/operator/client
  的 normal 与 `-O` 测试分别为 `9/9`、`4/4`、`9/9`、`27/27`、`6/6`、`9/9`、`9/9`，均 GREEN。
- 目标侧 `127.0.0.1:8766` 仍未监听；官方客户端保持 `RED/PENDING`，没有 fallback local start，也没有调用 handoff/control。
  当前 CK3=0，历史 R376 已终止，R377 尚未启动。该 RED 是目标交互操作者 MCP bootstrap 未完成，不是 B1 产品判决；
  仅在五工具、profile SHA、`xenoa / WinSta0\Default / XENOAMESS-FULL-TOWER` 身份及 job preflight 全 GREEN 后才可 handoff。
- P1 仍为 `1/9 GREEN + 8/9 PENDING`，T0 仍为 `50% / stage 8/11`；最终宣传视频继续硬锁，未检查或更新宣传工具，
  未制作、预热或发布视频物料。T2 当前兼容 horizon 为 `100%`；T1 仍为 `90%`。

## 下一步

1. 由目标 Windows 交互操作者启动通用 operator MCP；仅在 `status` 与 R377 `preflight` 全 GREEN 后执行幂等 handoff，并在启动前再次确认 CK3=0。
2. R377 先验证 B1 第二次 rebuild、generation=2 与同一 80 人域的 target/recount 守恒；GREEN 后在同一 PID 热续跑 AF5 authored 42/native 41、stage 9 `.390`、stage 10 manager 与 stage 11 Workforce terminal，再保存 checkpoint 并暂停。
3. 依照单实例规则执行代表性终态 cold restore，验证 PID 与 connection generation 递增、B1/AF5/Central/Workforce 前后同一读回；随后收口运行期错误扫描、managed cleanup、最终 candidate L0 与九项 P1 manifest。
4. open_kaishek 已完成 operator MCP 1.1 同步并恢复当前接口 horizon；后续若 T0/T1 再改变接口、协议、数据格式或版本，立即重新触发 T2。
5. 若再撞到新原版变体，执行“保留 RED -> exact-build 定义 -> 最小可复用合同 -> 双模式测试 -> rebase/push -> 同 PID 热恢复（仅无 DLL/游戏文件变化时）”。

当前 R375 `autosave.ck3` / `last_save.ck3` 仅到 `date_raw=53611200`、`144950977` bytes、SHA-256
`F67B86E41A566EB0622A4108B44634DBDFBD2F9277A85B5B04487B1515D4EB43`；它早于 `.0081@53611224`、
`.0110@53611320`、`.1721@53611536` 与 `.1000@53619912`，不得冒充任一事件或终态 bound 的同帧存档。事件边界与
终态分别由上述不可变 GREEN/RED sidecar 和 product-bound sidecar 证明。

## 范围边界

- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。

## 独立产品《重整河山》宣传片候选与自动审计

- 用户锁定 A03、现稿、片尾口号及音乐权利说明后，完成 `zh-CN-XiaoxiaoNeural` 旁白、88 秒主体与 8 秒自然尾音的 96 秒候选片；
  精确成片 SHA-256 为 `3E30299AAB75944D2A1C54E7044576816CEE5B61ED5032C5A1A7A917C577BE0B`。
- MCP-first 实机 capture a02 为 GREEN：11/11 抽样帧非黑且互异，地图锚定 `b_kaifeng` / province `9822`；CK3 排他槽已释放。
- 废弃并保留 a01（GOP 解码乱序）与 a02（割据段误用印度地图）后，visual a07 和 render a03 通过 14 个准确时间点的语义抽检；
  术语固定为“溥天之下 / All Under Heaven”，片尾保留“一朝失鹿，尚可再兴”。
- `xar-promo` 自动审计已对 18/18 个边界样本完成 SHA-256 绑定并通过；新增 `rmtm_promo.audit_evidence`，把 review 提取帧转换为
  evidence-plan v2 / bundle，同时拒绝任何已声称 approval 的 review package。项目集成测试 `16/16` GREEN。
- 人工审片包已生成并永久保留，状态明确为 `pending-human-review`、`approval_granted=false`、`signoffs=0`。等待用户对精确 MP4
  连续 1× 完整观看后再执行 signoff；本轮没有冒充人工签核，也没有向外部视频平台发布。
- 制作与验收报告：[`../../reclaim-the-motherland-promo-production-report-2026-09-10.md`](../../reclaim-the-motherland-promo-production-report-2026-09-10.md)。
- 用户随后回复“通过”；`signoff-000001` 已以 `repository-owner / approved` 绑定精确成片 SHA-256，离线 export
  `reclaim-promo-release-20260910-a01` 为 GREEN，交付片字节与签核对象一致。未指定外部视频目标，因此没有执行网络上传。

## 15:46 R380 B1 双归一化 RED 与 Python 热修

- R378 已受控清理且旧 PID `156280` 消失；新 wrapper 的 warmup 为 R379，已清理。当前唯一 CK3 是 R380 / PID
  `175936` / generation `1`，paused/map-ready；没有其他 CK3 实例存活。
- 第 29 个 executor 槽已生效：R380 原生命令成功返回 `status=available` 的完整 B1 frame。随后 Python native driver
  首次归一化并派生 `invariants`、`anomalies`，service 又用只接受 raw 18-field frame 的同一 normalizer 二次校验，
  因多出的两个派生字段保留 RED：`b1_cycle_snapshot must contain exactly the v1 fields`。这发生在 B1 业务推进前，
  所以 `product_result` 仍为 `NOT_EVALUATED`，不冒充产品验收。
- normalizer 现只接受 raw 18-field 或 canonical normalized 20-field 两种精确形状；对后者重新计算并逐类型核对派生
  invariants/anomalies，任何漂移继续 fail-closed。原生 wire、公共 JSON schema、68 个逻辑字段、38-variable allowlist、
  MCP tool/入参均未变化。合同文件 SHA-256 为
  `247729B653D2ECCA49A894024C9AB6E6450FB74FE17BD559174ECA166291F06D`，公共 schema SHA-256 仍为
  `42E5692943123AC71C8629C2A8A6C0656F6AAB20EA2A8546A193707BA932C23B`。
- B1 合同 normal/`-O` 各 `13/13`，Gameplay bridge normal/`-O` 各 `219/219`，`py_compile` 与 `git diff --check`
  均 GREEN；新增组合回归直接覆盖 production `NativeHeadlessGameplayDriver -> GameplayBridgeService` 双层路径。
- 此修复仅为 Python，R380 不应重启或新建 R 轮次。当前 outer RED park 的既有 `resume-auto` 没有绑定回调，发送它
  只会返回 unavailable；在补齐可核验的同 PID consumer 重连/热恢复路径前不发送无效控制，也不清理 CK3。
- T2 业务字段和接口无需变化，但 open_kaishek 固定了父仓 Python 合同哈希及 provider commit，必须在本工作包推送后
  做 provenance-only 同步；不得提升 runtime/native certification。T0 仍为 `50% / stage 8/11 / P1 未签收 / P2 LOCKED`，
  最终视频锁未触碰。

## 15:19 operator MCP 自启动、R378 B1 executor RED 与静态修复

- 当前执行会话已实测为 `xenoa / WinSta0\Default / XENOAMESS-FULL-TOWER`，因此不再等待外部 shell；通用 operator MCP
  已由当前执行者自行启动，五工具、profile identity、15/15 required hashes 与 7/7 启动前输出缺席条件均 GREEN，随后
  幂等 handoff 成功。
- wrapper 的 frontend warmup 是 R377 / PID `205788`，已受控清理且进程树消失；loaded-save 产品实例按“每次启动递增”规则
  记为 R378 / PID `156280`，它是当前唯一 CK3，paused/map-ready，旧实例均已终止。冻结目录名中的 `r377` 只是历史包名，
  不覆盖真实动态轮次。
- R378 在任何 B1 业务判定前保留真实 RED：`application-main ZhongGuo B1-cycle executor is unavailable`，因此 B1
  `product_result=NOT_EVALUATED`。根因是既有 B1 executor 已实现并被 capability 广告，但未注册进 application-main
  mailbox 的固定 callback allowlist；Python 重试无法改变已加载 DLL，本轮没有发送 `resume-auto`。
- commit `9033732930d10293a3e1c6b37942611b55b324f6` 新增第 29 个固定 typed-executor 槽并只绑定
  `ExecuteZhongguoB1CycleSnapshotMailboxQueryV1`，同步 ABI/fixture/source-contract 账本。短路径 MSVC Release 全量构建
  `524/524`、原生 CTest `103/103` GREEN；新 DLL 为 2,493,440 bytes，SHA-256
  `54DD192F21936D4B4C4D721D9D3D7900EE40FE8AB94DF1848EF806B6000E1C7D`。提交已按 rebase-only 普通推送，
  `HEAD == origin/master == remote master`、ahead/behind=`0/0`、单父、无 merge/force。
- T2 架构触发审计为 `NO-CODE-CHANGE`：变更只在父仓内部 C++ executor 准入表，对外 B1 MCP tool/command/step/schema
  与 68-field/38-variable 业务合同均未变，open_kaishek 没有该 mailbox/DLL 消费者。T0 仍为 `50% / stage 8/11 / P1 未签收`，
  P2 最终视频保持 `LOCKED`，未检查/更新宣传工具，也未制作、预热或发布视频物料。
- 下一动作：完成新轮次 wrapper/profile 的 no-launch 冻结后，以 `cleanup` 受控回收 R378；再次确认 CK3=0 后才能用新 DLL
  启动下一实例。若 wrapper 包含 warmup 与 loaded-save 两次启动，动态轮次必须分别记为 R379 与 R380，不能合并成同一轮。

## 16:35 R384 B1 只读探针发布竞态与同 PID 热修

- R380 已由 operator MCP 受控清理，cleanup/report SHA-256 分别为
  `9A5980792269AE1590CE9328B0272EFB4515E3280F3582A625839FB31D51E9AE` 与
  `DBC630870AC4660665CB8A7B9C700C002B09842DB325C96393E79D346F4EA6C4`。R381 warmup 已清理；R382 因 wrapper
  误把空 progression 当作全项目起点而在业务输入前触发 10190-day harness RED，已受控清理，cleanup/report SHA-256 为
  `2C44F9048548459753CE873389FD77A889C7A10F5FF8B1BF7DEBEA9332E0F092` 与
  `9E3757E6F3551FE6EC2E0E41ABBD44CF56C2A948CBF30E1A75BC30D9B18AB5E0`；未把该失败写成产品 RED。
- R383 warmup 已清理；当前唯一 CK3 为 R384 / PID `38864` / generation `1`。初始 B1 查询 `7/7` GREEN，legacy
  target/recount 为 `23/46/7`；新鲜 B1 回放边界固定为 `53584920 -> 53594520`，不得重置或续期。
- 日期推进至 `53584944`、`53584968` 后，第三次 `resume-map` 已返回 submitted，但下一心跳尚未替换旧 paused frame；
  只读 B1 probe 由旧帧发起并被 native direct-read gate 以 `snapshot changed or is not ready` 正确拒绝。拒绝发生在 mailbox
  查询执行前，严格日期只定位于 `53584968..53585040`；`product_result=NOT_EVALUATED`，没有事件选项、review action 或 B1
  写操作被提交。RED park 在 `53585040` 看到 instance `1041`/3 options；历史 ledger 映射为
  `tgp_decision_events.0101`，但本轮仍须以 live definition query 为准。
- 最小 Python 修复只对上述精确 probe 错误做最多 4 次 settle/rebind，并记录 before/after、query submitted 与
  `state_mutation_submitted=false`；未知错误及超限继续 RED。每次 probe 后重新绑定完整 snapshot，确保新 modal 仍先查询原版定义
  和共享合同，绝不吞事件。定向 normal/`-O` 各 `2/2`，既有 map-control/progress 回归 normal/`-O` 各 `1/1`，
  `py_compile` 与 diff-check GREEN；整文件另有 5 个既存事件日期合同失败，未冒充通过，也不由本修复扩张处理。
- 此变更不修改 DLL、游戏文件、B1 schema/wire/readiness 或 MCP 接口，open_kaishek 审计为 `NO-CODE-CHANGE`。
  R384 应通过现有 `resume-auto` 回调原位 reload promotion module；不重启、不新建轮次。T0 仍为
  `50% / stage 8/11 / P1 未签收 / P2 LOCKED`，宣传工具和最终视频物料均未触碰。
