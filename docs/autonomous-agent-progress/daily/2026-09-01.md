# 2026-09-01 日报

状态：**进行中；宣传工具链抽离 P0，ZhongGuo 二期与 G2 既有线程继续自然收口**

当前滚动更新时间：2026-09-01 00:00（Asia/Shanghai）

正式收口：2026-09-02 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-01.md`](../meetings/daily/2026-09-01.md)

本周计划会：[`../meetings/weekly/2026-W36.md`](../meetings/weekly/2026-W36.md)

开场分支/HEAD：`master` / `adc70c7ec705cbb3a9fc04f43e3c1c93f9eed68f`

## Opening baseline

- 宣传工具链已有 Skill 初稿和提取边界，核心/adapter/audit/TTS 正在并行落盘；最高已验证状态仅为 Skill quick validation，
  尚不能称可安装工具链。
- 二期 static/central-wired 361/361、complete 0/361、bounded live 4/361；四域 observation matrix 与 B2/AI action helper
  已完成，current-runtime seed、Incident/Workforce action、scoreboard safe invoke 和多周期 live 尚缺。
- G2 第二寿命已完成；favor-hook 只到 static/fixture-ready。

## 正在做什么

- P0：`xar_promo` core CLI、CK3 adapter、audit/evidence、TTS、Skill 与旧脚本兼容迁移。
- P1：让跨日已启动的二期 seed/action/Workforce/scoreboard 与 G2 工作自然结束并逐批收口。

## Readiness 边界

- 工具链：`design/skill-static-ready`，未达到可安装/可渲染/可审计 complete。
- 二期：`ck3-static + central-wired 361/361`，不是 `production-live` 或 `complete`。
- G2 favor-hook：`fixture-confirmed/static-ready`，不是 unified MCP/live。

## 02:30 计划外完成：宣传工具链 P0 可安装闭环与二期入口诚实 RED

本节是 00:00 opening baseline 之后的新增事实，不倒改当时状态。宣传工具链已从 Skill 初稿推进到
`static-ready + installed fixture-live`，但没有二期真实成片、真人签核或外部发布，因此仍不记 `production-live` 或项目发布
`complete`。

### 功能与边界

- 通用 `xar_promo_toolchain` 已形成 Python package、冻结的十命令 CLI
  `init/start-run/validate/preserve/signoff/plan/build/audit/review/export`、ProjectConfig 与 append-only RunManifest、
  配置快照、内容寻址素材保全、typed phase/audit 账本、真实 FFmpeg/Pillow pipeline、adapter/preset registry、TTS、字幕布局、
  pending review、人工 signoff 记录与只做离线 allowlist bundle 的 export。`plan` 与 review `--plan-only` 均为零生产进程、零写；
  GREEN build 与 RED partial/stdout/stderr/command audit 都保留。
- 自动 audit、`pending-human-review` 与真人 signoff 是三个独立状态。audit/review 不会批准；`signoff` 只记录已由具名真人明确
  给出的决定；offline export 不联网、不上传，也不授权 Steam/其他平台发布。
- 集成审查复现了旧 review 接受未绑定 raw ffprobe 的真实 RED：3.021333 秒成片可被伪造成只审 1 秒。现已改为只接受 v1
  `xar-promo-bound-media-probe`，公开 `probe_and_write_bound_media(...)` 必须现场运行 ffprobe、保全 command audit，并把 probe
  绑定到成片 bytes/SHA-256；bare/stale/mismatch 一律在零写条件下 RED。Skill、README 与专题文档已同步该合同。
- ZhongGuo 二期新增独立宣传 builder/preset 接线：只接受真实历史角色、MCP hash/clean-span 与测试 UI 缺席证据，全程不用 OCR；
  只消费已缓存的 `zh-CN-XiaoxiaoNeural` 音频，以真实 ffprobe 旁白/终片时长执行 `<1200s`，并拒绝把其他成片的旧 approval
  复活到当前目标。当前配置十章仍全为 `planned`，所以正式入口按设计返回 2。

### 验证与 RED→GREEN

- 初次冻结树在 Python 3.11.9 下普通/`-O` 各 `182/182 GREEN`；发现并修复 byte-bound probe blocker 后为各
  `185/185 GREEN`；补齐跨机器 human install guide 与 machine-readable install contract 后，最终 frozen source 全包普通/`-O`
  各 `194/194 GREEN`。十命令 help `10/10 GREEN`，Skill quick validation、安装合同 JSON、Markdown links 与
  `git diff --check` GREEN。这里的单测只证明 static 合同，不冒充二期 live。
- 真实 FFmpeg/Pillow smoke 首次在末帧 review partial 上 RED，失败 workdir 完整保留；随后形成可复现 GREEN，最终又经
  bound-probe 修复后的真实 ffprobe→exact envelope→review smoke 验证。最终 subject 为 `80353` bytes、SHA-256
  `FE01E2CE1FAB254DBC6564422DDFB24A1C4B21B9945766D2987F5169510C02F2`，3.021333 秒；plan-only 零写、末帧
  2.958333 秒，full review 仍为 pending、`approval=false/signoff=false`。
- 独立 bound-probe 回归普通/`-O` 各 `32/32 GREEN`；bare raw、stale envelope 和把旧 raw 重新包装到当前文件三路均 RED、
  零进程零写；live producer→bound envelope→2 帧 pending review GREEN。
- 安装隔离 E2E 在 Python 3.13.2 / Pillow 12.3.0 下从已安装 `xar-promo.exe` 实跑十命令，真实 FFmpeg/ffprobe build、
  exact-bound probe、4 帧 review 与自动 audit GREEN。它只写入 non-human fixture 的 `rejected` 记录以覆盖拒绝分支；没有真人
  approval，export 按预期返回 2 且没有创建发布目录。
- 跨机器安装材料现覆盖 Windows/Linux/macOS、Python ≥3.11、wheel/sdist/source/editable、可选依赖、FFmpeg/ffprobe、离线
  wheelhouse、SHA-256 来源核验、升级卸载、CK3 adapter/preset 边界与常见错误；明确 0.1.0 尚未发布到 PyPI。machine-readable
  contract 已随包并用 13 项合同命令验证，未把某台机器路径硬编码成通用事实。
- 最终 package 基于安装材料冻结后的 exact source 重建：wheel `43` members、runtime-only；sdist `110` members，包含
  docs/examples/scripts/Codex Skill/tests，只允许构建 backend 自动生成的 `src/xar_promo_toolchain.egg-info/SOURCES.txt` 控制
  元数据。fresh wheel/sdist install、pip check、import、console/module 十命令、八个顶层 bound-probe exports、13/13
  contract-driven commands、roundtrip/stale rejection、普通/`-O` contracts 各 `13/13`、全包各 `194/194` 与 exact snapshot
  `88/88` 均 GREEN。它们仍只是本地分发物，尚未上传包索引。
- Legacy 最终回归在 Python 3.13.2 / Pillow 12.3.0 / edge-tts 7.2.8 下普通/`-O` 各 `42/42 GREEN`：通用 legacy adapter、
  原有 full-agent showcase 与 ZhongGuo 一期 promo builder 均未回退。早期 `014117/014423` 两次 harness RED/superseded 现场保留。
- 二期 `--validate-only` 实跑为预期 RED `exit=2`：准确列出缺失的十章 live 素材；workdir、candidate run、capture root、TTS
  cache 与 FFmpeg/ffprobe/font sentinel 均未产生，配置前后 SHA 一致，无 traceback。第一次带 PowerShell stderr 包装的捕获和
  后续 raw 权威捕获都保留。

### Artifact 账本

| 状态 | Artifact | SHA-256 / 说明 |
|---|---|---|
| RED，保留 | `Z:\ck3_mod_rewrite_process_assets\promo-toolchain-full-smoke-20260901-012036\smoke-failure.json` | `62BA5477FE97063D438AC25DCF4FE00AE4E88E66F61B806D45FEF55BABFCA3D0` |
| GREEN，superseded | `Z:\ck3_mod_rewrite_process_assets\promo-toolchain-full-smoke-20260901-012420\smoke-report.json` | `A5A659CE2B67B96E26D91D774ED435781184C4E00EFE03F6B8E3BC78192833FF` |
| GREEN，pre-bound-probe 基线 | `Z:\ck3_mod_rewrite_process_assets\promo-toolchain-full-smoke-20260901-012638\smoke-report.json` | `FE834C14B4D0A0D2D37E6F21DA234979C4A9F3E65EBA756683AA64A98479F541` |
| GREEN，最终 bound-probe smoke | `Z:\ck3_mod_rewrite_process_assets\promo-toolchain-bound-probe-smoke-20260901-021500\smoke-report.json` | `93C29429917BF210D23A7C5B9C427792292B63036B824B5BD97E37CD9CE4901A` |
| GREEN，独立 probe 回归 | `Z:\ck3_mod_rewrite_process_assets\promo-bound-probe-independent-regression-20260901-021349\evidence-index.json` | `03398444765B23B0AF3C384804745514FC3845DF2202B774E7F6DA5EF06FD31B` |
| GREEN，独立 probe 全目录校验 | `Z:\ck3_mod_rewrite_process_assets\promo-bound-probe-independent-regression-20260901-021349\SHA256SUMS.txt` | `ABEF0132F3FCD439D583A3C8DF1E63374E689E3AF8D1FA49B9B7CEBFC816F426` |
| GREEN，installed-console E2E | `Z:\ck3_mod_rewrite_process_assets\promo-cli-e2e-bound-20260901-021314\cli-e2e-report.json` | `93DA18F4AED0DC5FDA72ABF23439F4D73C8029988B059B4E284BB5299473C0DF` |
| GREEN，E2E artifact index | `Z:\ck3_mod_rewrite_process_assets\promo-cli-e2e-bound-20260901-021314\artifact-index.json` | `380356532BE923A45618C8D2BD30894678A2EEE3C5D5F9C80B1436D976DC6079` |
| GREEN，E2E 全目录校验 | `Z:\ck3_mod_rewrite_process_assets\promo-cli-e2e-bound-20260901-021314\SHA256SUMS.txt` | `8DD1DB0E03FAC784A55C47BDA2FA4E0CA5D4129B2264BEA6CAA14D94D387F44D` |
| GREEN，历史安装合同（已被 `2628da0` supersede） | `xar_promo_toolchain/src/xar_promo/schemas/install-contract-v1.json` | `211B8CEBC69F9AD7925DF08BB7E1E03A3B097E44A8704E9751DB3103D32C5B4E`；8368 bytes |
| GREEN，历史 package summary（已被 `2628da0` supersede） | `Z:\ck3_mod_rewrite_process_assets\promo-package-authoritative-20260901-025705\acceptance-summary.json` | `DA715BD1A8D5449DA2E4FB8AF873834035F77F68ABE0FD8D957828CC290CD583`；5164 bytes |
| GREEN，历史 wheel（已被 `2628da0` supersede） | `Z:\ck3_mod_rewrite_process_assets\promo-package-authoritative-20260901-025705\dist\xar_promo_toolchain-0.1.0-py3-none-any.whl` | `4E3FB201A1BD3903767FBE2CB80DFA8FB13CEC1235C97AE4BB4DC7135D61643C`；145185 bytes |
| GREEN，历史 sdist（已被 `2628da0` supersede） | `Z:\ck3_mod_rewrite_process_assets\promo-package-authoritative-20260901-025705\dist\xar_promo_toolchain-0.1.0.tar.gz` | `5C10DDAC55F84964B8548FD277FCB271BE4AF348C970932C38A253CC95E06482`；241043 bytes |
| GREEN，legacy 最终回归 | `Z:\ck3_mod_rewrite_process_assets\promo-legacy-final-regression-20260901-014557\SHA256SUMS.txt` | `92262053D78FAC261E74E530236A9369AFD65F934E50D3237FBF9954CDA2EAF6` |
| RED，首次二期零写捕获 | `Z:\ck3_mod_rewrite_process_assets\zg361\promo-validation-artifacts\phase2-validate-20260901T014142\result.json` | `1A8F5604CA72AFF293AA01D9EA57E9F444E80AC3B68B73D2A37EEE952E2C38E3`；目录 `sha256.txt` 为 `274F881DBCF448526CAB26C768056DEC1B0D136165E04996A658FFDB939ABC0F` |
| RED，权威 raw 二期零写捕获 | `Z:\ck3_mod_rewrite_process_assets\zg361\promo-validation-artifacts\phase2-validate-20260901T014447-raw\result.json` | `2DA44F5606280B805A54EDB2153FA612475A6E41D6BC14F5119DEDF53828BA32`；目录 `sha256.txt` 为 `4B9F8168559B01870D3D7A27F3C14891C4C4101C4467FB53CA65D581787AE71F` |
| fixture-only 生成卡 | `C:\Users\xenoa\AppData\Local\Temp\zg-phase2-card-smoke-nnnwntr5\visuals\smoke.png` | `D54B451BFF74B0ABEBCD3A1E4ACA827662982E9935BBD19BB1337FCAA0F28052`；不是二期 live/promo 证据 |

旧 package roots `Z:\ck3_mod_rewrite_process_assets\promo-package-acceptance-20260901-013545`、
`promo-package-authoritative-20260901-021525/023203/023657`，旧 CLI E2E root
`Z:\ck3_mod_rewrite_process_assets\promo-cli-e2e-20260901-013838`、旧 legacy `014117/014423` 与全部失败 workdir 均按素材保留规则
留存并明确标为 superseded，不能引用其旧 hash 作为最终分发结论。

### Readiness / 未闭合项

| 能力 | 00:00 | 02:30 | 诚实限制 |
|---|---|---|---|
| 通用工具链 core/package/Skill | `design/skill-static-ready` | `static-ready + installed fixture-live` | 合成源与 fixture E2E 已闭合；未做真实项目发布 |
| 自动 audit / pending review | 施工中 | `fixture-live` | 不等于真人看完或批准 |
| 二期宣传入口 | 未形成 | `static-ready；planned RED` | 十章真实 live capture/matrix 未齐，无二期终片 |
| 人工签核 / 发布 | 未开始 | 未开始 | 无真人 1× 完整审阅、无 approval、无包索引/Steam 上传 |

- 本轮 P0 没有二期 `production-live`；合成视频、fixture rejection、自动 audit 和卡片实渲染均不得冒充真实 CK3 二期宣传片。

### Commit / push / CI

- `0b59e63`（通用宣传视频工具链）、`c82caa3`（Windows 路径别名回归）和 `3166c80`（跨机器安装指南与
  machine-readable install contract）均已进入并推送到 `origin/master`；最终 GitHub Actions run `33427860154` 在
  `3166c80` 上 GREEN。
- 上述提交只冻结通用工具链、兼容修复和安装材料；不代表二期 live 素材、成片、真人签核、Steam 更新或 PyPI 发布已完成。
- 本次日报/周报的滚动文字追加由主线程随本轮收口提交并推送；记录代理未改动其他并行工作现场。

## 03:50 计划外完成：G2 Raiktor 四域实机原语与二期 B2 action runner 接线

### G2：四域 terms 为 production-live read-only primitive

- `1e84e4b` 已推送到 `origin/master`：正式 `query-war-termination-terms-v1-50331699` 在同一暂停帧中统一发布 actual gold、
  F/prestige、PoW release pairs 与 favor-hook 四域。冻结 CharacterID `29829` / WarID `50331699` / `date_raw=53223936`
  经一次非 debug 冷恢复、两次正式 MCP query，`native:3 / revision=4 / native_revision=3` 与完整 normalized payload 保持一致；
  全程没有 OCR、时间推进、投降或其他 mutation command。
- fresh Release `Z:\xar-g2-gen034-fourdomain-20260901-02` 的 native CTest `63/63 GREEN`；terms contract、native driver、
  gameplay/MCP 回归普通/`-O` 各 `426/426 GREEN`。Official Runner run `33432357069` 已 terminal SUCCESS。
- 这只把四域策略输入提升为 `production-live read-only primitive`。`truce_ready=false`、
  `war_bound_armies_ready=false`，所以 `dynamic_deltas_ready / decision_ready / automatic_surrender_ready / ready` 仍全假；
  truce、Raiktor source-specific regiment/loss 与最终 continue-vs-surrender decision 继续 RED，未开放
  `surrender-war-50331699`，也不是完整投降 OODA。

| G2 live artifact | Bytes | SHA-256 |
|---|---:|---|
| `C:\Users\xenoa\AppData\Local\Temp\xar-g2-gen034-fourdomain-live-20260831T1932Z\report.json` | 121254615 | `1187D0BD129DA9188B7EBC0C389B035B5C4B1383CE1FF4895481678BCB4371E5` |
| `C:\Users\xenoa\AppData\Local\Temp\xar-g2-gen034-fourdomain-live-20260831T1932Z\evidence-index.json` | 4742 | `D1AF7A3C7F06FE8BFBF7F028C8B294B64381F276DBA612FAE07216EF615DE292` |
| `C:\Users\xenoa\AppData\Local\Temp\xar-g2-gen034-fourdomain-live-20260831T1932Z-hkl\hkl\ck3-hkl-summary.json` | 2409 | `2EDA954ED88EB2FE6116A0B138BCE0F8FE47C23EC04833A54EF4C1716831638C` |

HKL watcher 对 CK3 PID `51268` 的 `79` 次轮询均为 US English `0x04090409`，layout action 与 unresolved error 均为 0；
PID cleanup/process-tree 回收 GREEN。前三次 harness RED attempt 继续保留，未倒写成 gameplay RED。

### ZhongGuo 二期：B2 gameplay action runner static/fixture-ready

- `48fbe07` 已推送到 `origin/master`：batch runner 现按 MCP-only
  `只读 domain matrix → checkpoint → 等待 exact zg361b2.40 → accept → 同暂停日 provider 后置条件 → restore → post-restore matrix`
  接入 B2/PIP 本人回应；ACK 仍只代表 submitted，只有同案五元身份和 `state 1→2 / response=1` 的 provider 后置条件才可使 cell
  GREEN。失败 sidecar 与 save/restore lineage 分别落在约定的 `05`/`06` artifact。
- 独立 helper 普通/`-O` 各 `12/12 GREEN`，runner 的 static/fake-MCP 回归普通/`-O` 也均 GREEN；Official Runner run
  `33432445911` 已 terminal SUCCESS。当前只记 `static/fixture-ready`，不是 `fixture-live`：尚未启动 CK3，也没有真实 paused
  `zg361b2.40` action、postcondition 或 save/restore artifact。
- 本批只接 `accept`；Workforce collective、AI-owned case 与 scoreboard named-widget 三项 gameplay action 仍缺失，故二期整体继续
  `RED/incomplete`，不能因 B2 接线而升级 live readiness 或启动发布。

## 04:34 计划外增量：二期动作/Workforce 静态闭合，seed attempt 07 仍在运行

本节是 04:34 的滚动快照；以下提交和离线回归已经完成，但 seed 仍未形成权威 paused CK3 artifact，不能把 CI、fixture 或正在运行的
attempt 提前记为 live。

### 已合入能力与诚实 readiness

- `ec853a9` 已把 AI-owned case action 接入二期 batch。它继续坚持“真实 `life-advance` 后由 paused provider 回读新的
  mechanism 039 / roster-lock receipt”，ACK 不作为业务后置条件；Official Runner CI `33434175014` GREEN。当前仅为
  `static-ready / fixture-ready / runner-wired`，尚无真实 seed 上的 producer transition，因此不是 `production-live`。
- `c0455b6` 新增 Raiktor attacker→defender truce pointer observer core。冻结 source contract
  `ck3_autonomous_player/native_bridge/research/fixtures/raiktor_surrender_truce_v1_source_contract.json` 的 SHA-256 为
  `AE6C5569402FF448A3BB028EC3B7F525553075AC5C8FAC7005791FF2EB8ECABE`；Python `5/5` 在普通与 `-O` 两种模式均 GREEN，MSVC
  两套构建均 GREEN。它只证明 exact-build pointer shape、双求值稳定与 fixture 合同；尚未接到真实 paused MCP shape probe，状态严格为
  `static-ready + fixture-ready / not-live`，也没有把 expiry 推算成已观测事实。
- `038385b` 闭合 Workforce 正常离职 `occupied → frozen`、D+1 receipt 封存及再录用复制的产品脚本生命周期；相关生成/产品回归普通与
  `-O` 各 `82/82 GREEN`。这是 CK3 script `core-wired/static-ready/not-live`，不是正常离职或再录用全链实机完成。
- `1f083a0` 接入 Workforce normal-exit lifecycle 的 94-key received-self 只读 provider，包括 exact-build reader、serializer、
  application-main mailbox、driver/service/MCP、schema 与严格 Python 合同；native CTest `5/5 GREEN`，Python 普通与 `-O` 各
  `20/20 GREEN`。它只到 `static-ready + fixture-ready / not-live`：尚无真实 CK3 paused 查询，也没有证明
  `pre → migrating → sealed → rehire_captured` 的实机全链。
- `e1d0b9f` 没有伪造 Workforce #360 A/B/C 的实机动作，而是在 batch 中显式暴露
  `exact_owner_subject_player_transition` 与 `same_checkpoint_three_route_restore_lineage` 两个前置阻塞；Official Runner CI
  `33435902003` GREEN。该 CI 证明 fail-fast 接线，不证明任一路线 live。

### phase-two seed attempts 03–07

- attempt 03（`phase2_seed_20260901_035314_head_48fbe07_attempt03`）是 harness RED：clean export 未包含被 `.gitignore` 排除的原版
  CK3 路径，`00_game_rules.txt` 无法找到。经验是产品/runtime 可冻结导出，但原版游戏目录必须作为显式、只读外部依赖注入。
- attempt 04（`phase2_seed_20260901_040306_head_48fbe07_attempt04`）补上外部原版路径后仍为 harness RED：首次读取赶上 map/native
  readiness race，`native game state is not available yet` 被当成终局错误。经验是启动绑定后必须有界轮询 typed readiness，而不能假设首帧已可读。
- attempt 05（`phase2_seed_20260901_040550_head_48fbe07_attempt05`）增加轮询后又暴露 Windows 现场保全干扰：反复原子替换
  `bootstrap-event-wait.json.tmp → .json` 时文件正被读取，触发 `WinError 5`。这仍是 harness 证据写入 RED，不是产品或 provider RED；
  后续改为 append-only JSONL，避免读者阻塞 replace。
- attempt 06（`phase2_seed_20260901_041032_head_48fbe07_attempt06`）以 append-only JSONL 完整等待 `300s`，但 exact
  `zga_phase2_seed.1` 仍未出现，观测持续为 temporary unavailable，最终按超时 RED 收口。它证明等待/现场保全路径工作，不证明 seed、
  action cell 或四域 provider live。
- attempt 07（`phase2_seed_20260901_042407_head_48fbe07_attempt07`）在本节记录时仍在运行，只有 partial report 和持续追加的等待证据，
  尚无 final runner report、candidate save、paused MCP matrix 或 GREEN 结论；因此明确记为 `in progress / not-live`，结果须待 runner
  正式收口后另行更新，不能用运行时长或 partial artifact 提前宣称验收完成。

## 09:07 计划外完成：G2 war-bound 与六域聚合离线收口

- `94d367e` 已把 Raiktor generic war-bound current/cleanup observer 作为独立 core 合入 master。active 侧绑定 exact paused
  `raiktor_claim_cb`、primary attacker、full-generation WarID、`bound_war_id` 与 `keep=false`，按 generation 汇总当前 soldiers；postwar
  侧只按冻结 persistent/current/CArmy IDs 给出 `destroyed/still_alive`。它不把 generic rows 称作 `norman_highwaymen`，不把 authored
  `6×500=3000` 当作 measured pre soldiers，也不从 current/cleanup 推导 loss；source attribution、pre soldiers、proven loss 全为 false。
  Python 普通与 `-O` 各 `6/6 GREEN`，MSVC Debug/Release `/W4 /WX` 编译与运行均 GREEN；source contract SHA-256 为
  `26652A0FCB9D0C185272E7C3B2721A9EC93BC5C5CDC5003D2B9D9EE16B105241`。当前严格是
  `static-ready + fixture-ready / not-live`，没有 public wire 或 paused CK3 artifact。
- `5e2dfea` 已合入完全独立的 claims-base + six-domain same-frame aggregate。claims 是投降基础语义；六个 dynamic domains 是 gold、
  prestige、PoW、favor、truce 与 generic war-bound current。所有 present child 必须绑定相同 paused snapshot/native revision、date、
  WarID、CB、attacker/defender/claimant；任一 child 缺失时显式列入 `missing_domains` 且 `action_terms_ready=false`，present child 跨帧或
  错形时 aggregate unavailable。完整 fixture 的 `action_terms_ready=true` 只表示聚合条款齐全，`automatic_surrender_ready=false`；
  truce 不推算 expiry，cleanup 使用独立 postwar frame，source attribution/pre/loss 仍全假。Python 普通与 `-O` 各 `8/8 GREEN`，
  MSVC Debug/Release `/W4 /WX` 均 GREEN；source contract SHA-256 为
  `4D9A67F1C8DBD0528823E11C320DB1CFA57C4C30DF3B377595B26F7281A08500`。
- readiness 没有被提前升级：`GEN-034` **仍未 resolved**。`94d367e` 与 `5e2dfea` 都是
  `static/fixture-ready/not-live`；truce/generic aggregate 的 shared/public terms wire、MCP action surface、continue-vs-surrender policy、
  typed surrender 与六域 postcondition live matrix 仍 pending。已有四域 production-live read-only primitive 不等于完整投降 OODA。

## 10:08 计划外增量：seed attempt 07 正式 RED，二期静态修复与 G2 保守策略合入

### attempt 07 最终结论与现场

- `phase2_seed_20260901_042407_head_48fbe07_attempt07` 已在 `900s` 有界等待后正式收口为 **RED**，不再是 04:34 的
  `in progress`。全部 `8,916` 次 typed semantic observation 都是 temporary unavailable；bridge transport 已连接、CK3 进程仍响应，
  但 debug/error 日志停在 database/script 初始化，始终没有到达 Frontend、Load Save 或 In Game。因此没有
  `zga_phase2_seed.1`、candidate save、paused provider matrix 或动作后置条件，phase-two seed 继续 `RED/not-live`。
- 本次仍严格为 `mcp_only=true / ocr_used=false / coordinates_used=false / test_decision_used=false`。product 与 seed fixture 各只挂载一次，
  已排除重复 mount；日志中的 duplicate-registration 是非法事件 ID 后的恢复噪声。cleanup 与 runtime-unchanged 检查均 GREEN，结束后没有
  CK3/Python 残留进程。
- 保存日志的根因分类为：三处旧式 scalar `revoke_court_position`；manager governance 的 24 个 computed-equality 错形；五个
  Career-HC adapter 未消费显式 `TICKET_SUBJECT`；Workforce 17 个大于 9999 的 event suffix；以及缺失合法 theme 的事件 warning。
  这解释了“进程活着但永远没有 semantic snapshot”，不能再用更长超时或盲跑下一局代替 parser 修复。

| attempt 07 artifact | SHA-256 |
|---|---|
| `artifacts/runner-report.json` | `319A3A3419DB77D1349F12CA8450281597612F15E23856268E1ED17E678D2ECE` |
| `artifacts/09_phase2_native_session_cleanup.json` | `C656193A80A7A7E5AAA8CB77EC5C0EC7B18BA3DC1258EBF5FE055413E214011F` |
| `artifacts/bootstrap-event-wait.jsonl` | `2452104F92E75406ABAB3524AF6955D5292F3B15E8F583F959A5C9B01D838DE9` |
| `artifacts/attempt07-loader-stage-replay.jsonl` | `7687E389A75F0800B52AA64693C5375ED17FC926813CCF49A7042F28EEAAFCAE` |
| `artifacts/ck3-logs/debug.log` | `50F1D64E14684AE13AE7497C2061601BB781281062762684BCC4EAFDF05C3112` |
| `artifacts/ck3-logs/error.log` | `C59845A20BA424211DF3187D497256B559B667329BEB8023AAB1FB9B3F4D030A` |

权威目录为
`Z:\ck3_mod_rewrite_process_assets\zg361\phase2-seed-attempts\phase2_seed_20260901_042407_head_48fbe07_attempt07`；原始日志、append-only
等待记录和失败 runner report 全部保留。

### 二期 parser、loader、Workforce 与跨周期事实

- `641e862` 将三处撤任改成 owner-scope compound `revoke_court_position`，并补齐正常离职、probation、rehire 事件 theme；四组
  Workforce 专测普通/`-O` 分别为 `23/23、27/27、32/32、24/24 GREEN`。`c79057c` 让五个 Career-HC adapter 在目标 scope
  显式消费 `TICKET_SUBJECT`，Career-HC `41/41` 与 learning `33/33` 普通/`-O` 均 GREEN。`e1d8ebf` 把 24 个 computed equality
  改为同一表达式的 `>=` 与 `<=` 精确合取，manager 专测普通/`-O` 各 `48/48 GREEN`。`42a511a` 已把这三类 parser 失败及解法
  永久记入 grammar pitfalls。以上均为静态修复；尚未用新 HEAD 重跑 CK3 loader，不能称 loader-live。
- `962c442` 新增 append-only loader-stage detector：只对当前启动阶段的已知 product parser error 在 45 秒无进展后给出
  `loader_parse_red`，Frontend-only 与 Load Save/In Game 分路处理，theme warning 不冒充 parser fatal。`f0ffdb2` 已把该 gate 接到正式
  phase-two runner、且放在 native readiness 与四域 query 之前；`loader_smoke_only` 不调用它。两次提交只证明 deterministic/static runner
  合同，尚无新 CK3 attempt。
- `b8d9483` 把 AD 的 referral→panel→offer 三格真实人物 source 接入 Workforce core：A/B 只在 operation、业务对象与 consumer 成功后
  消费，C 只按 exact tuple 退役；#271 B 的 5 金奖励延迟到 #267 证明 referrer 确实投票后才付。该提交同时把 17 个非法五位 event suffix
  迁移到 `<10000` 的冻结 ID，并补齐五个 AD event theme。状态为 `core-wired static-ready / not-live`。
- `42e08e3` 交付独立 acceptance-only player-transition fixture 与正式 batch 接线：从同一 pre-#360 checkpoint 分别恢复 A/B/C，使用
  exact current-event MCP 在 subject→owner→subject 间切换，由 owner carrier 排队真实 `zg361we.360`，再以 played CharacterID 和 provider
  后置条件判定。helper 普通/`-O` 各 `10/10`、transition runner 各 `4/4`，fixture isolation 与全量 runner 回归均 GREEN；它仍只是
  `static-ready + fixture-ready + runner-wired / not-live`，没有真实 A/B/C paused artifact。
- `16fd6a0` 让 B1 #142 pending 改为逐 subject 局部公示：稳定人员先发布，pending 本人与其预留 peer 等单案终态后再逐行 revision；
  最终 modifier、奖惩与固定考核榜仍在 cohort barrier 后一次结算。quota model `69/69`、runtime `51/51` GREEN，但这只是
  `static-ready/not-live`。
- `7a28967` 为 B2 #016 冻结并在 D+180 重读真实 governance/capability/collaboration KPI 分项，且为 #017 冻结一名互异、无关系冲突、
  具天朝制公爵以上考核资格的真实 reviewer，由其在 D+365 签署结论后才允许唯一 settlement。伯爵/男爵仍只能被考核；资源交付不冒充
  任务进步、直属经理也不能代签。状态为 CK3 script `static-ready/not-live`。
- `d69262b` 补齐 Workforce canonical quality 3/4：#075 已封存真实正常离职与 HC 守恒后发布 quality 3 attrition；没有退出 intent 的
  native slot invalidation 以独立 receipt 发布 quality 4 role/strategy exclusion，formal HC 保持 1 且不冒充离职。两路都绑定 source
  receipt/hash 与 attribution，仍需新 loader、paused snapshot、存读档和多周期实机，当前只记 `core-wired static-ready/not-live`。
- 考核榜 shared native/MCP action wiring 仍是未提交 WIP；本节不记录其实现细节，也不把它写成 static-ready、fixture-live 或 live。
  phase-two seed 仍 RED，故没有启动发布、Steam 上传、二期实机宣传素材或真人审阅。

### G2 GEN-034 保守策略边界

- `630ff35` 新增 Raiktor continue-vs-surrender 的 conservative pairwise policy core 与冻结合同。它要求同帧六域、完整 campaign dominance
  certificate、显式 owner budget profile 和风险上下界；任一缺失都返回 underdetermined。当前 checkpoint 仍缺 public truce/
  generic-war-bound 输入、campaign certificate 与 budget profile，且 white peace 未评价，所以
  `recommendation_ready=false / full_exit_decision_ready=false / automatic_surrender_ready=false`，不产生 action literal。
- 因而 `GEN-034` 仍为 **unresolved**：`630ff35` 只是 `static-fixture-policy-core / not-live`。四域 read-only production-live 原语保持有效，
  但不能与离线六域 aggregate 或 policy fixture 拼接成可执行 surrender OODA。

### 提交与 CI

- 本节涉及的已提交 SHA 均已推送到 `origin/master`，且已知 Official Runner CI 全部 terminal SUCCESS：
  `641e862→33456512073`、`c79057c→33456535013`、`e1d8ebf→33456632187`、`42a511a→33456693910`、
  `962c442→33457400357`、`b8d9483→33458186500`、`42e08e3→33458400780`、`f0ffdb2→33458789247`、
  `630ff35→33460057790`、`16fd6a0→33460089129`、`7a28967→33460507162`、`d69262b→33460624848`。
- CI GREEN 只证明 L0、生成可复现和 runner/fixture 合同；它不改变 attempt 07 的 RED，也不授权把二期或 `GEN-034` 写成 live、complete 或已发布。

## 20:21 当前收口增量（真实时间）

### 视频宣传工具（P0）

- `d086a2d` 修复候选 run 持久化异常边界：`_persist_candidate_run` 失败时写入不可覆盖的
  `phase2-entry-failure.json`，保留已经建立的 `candidate-run` 半成品并记录 `retained_paths`，
  收据写失败也不会掩盖原始异常。相关二期 entry 测试普通/`-O` 各 `17/17`。
- `67b8f1d` 接入显式 `--phase2-promo-capture` 合同：一期 `--promo-capture` 的 span、流程和
  timeline 结构保持原行为；二期固定八个 `phase2_*` span、producer-key map 和 v1 JSON schema，
  preset 拒绝 legacy、乱序、缺失或额外 span。真实 choreography producer 尚未注册，因此命令实测
  在任何 preflight、CK3 或 FFmpeg 之前 typed RED：`phase-two promo capture producer hook is
  unavailable`，没有产生录屏或伪造素材。
- 整合回归：二期 builder 普通/`-O` 各 `17/17`，preset 各 `14/14`，`xar_promo_toolchain`
  全套各 `200/200`；promo runner、release manifest、visual-prep/audit、静态校验和双构建检查均 GREEN。
  当前 readiness 为 `static-ready`，不是 `fixture-live`、`production-live` 或 `complete`。
- 尚未开展：真实二期 gameplay choreography/八段 HUD 后 clean capture、十章 cues/TTS/字幕与媒体
  构建、runtime claim matrix、完整内容/画面抽检、真人签核、导出和发布。旧一期原片不得通过改名充当二期。

### 天朝二期

- `5a66036` 已把 loader 接入 native-session 终止探针；未来进程提前退出会立即落 typed
  `native_session_process_exit`，并沿用原 cleanup，不再把进程死等到超时。普通/`-O` loader、seed
  runner、bootstrap、fixture 回归均通过；该修复尚未启动新的 CK3。
- 冻结输入 no-launch preflight 的权威报告为
  `Z:\ck3_mod_rewrite\.worktrees\phase2-preflight-fix-20260901_183757_2622cbb\attempt-correct-pipe\artifacts\preflight.json`，
  SHA-256 `53C8A09DC0A0C07165626CB902A23EF218B20879D625E468D7B62E26B1B7A888`；字段为
  `result=GREEN/status=preflight-ready/seed_ready=false/seed_contract_status=blocked_seed_generation_required`，
  且 `ck3_launch_attempted=false`、`launch_boundary=not-crossed`、`native_session_started=false`。
- 唯一有界 CK3 attempt 仍保留为 RED：
  `Z:\ck3_mod_rewrite\.worktrees\phase2-preflight-fix-20260901_183757_2622cbb\attempt-bounded-2622cbb\artifacts\runner-report.json`
  SHA-256 `130CAD215F53EC72E1ED4B504D39EADB446ABA1FF9C7EE49149433D5B765709F`；CK3 启动阶段复现
  已知 particle2 null-slot `C0000005`，没有到 Frontend/Load Save/In Game，也没有 seed/candidate
  save/provider matrix。crash `exception.txt` SHA-256 为
  `CFBFF96E1144EE868ECC592A5BD56431DC037AEF6ED1DD4202BBFC9C8C5B7E66`，cleanup 收据
  `09_phase2_native_session_cleanup.json` SHA-256 为
  `10F9FC9ECF964389A8D599041B82B240938BA9035C2AB92AC92FCDB872E7F52A`；失败现场继续保留。
- readiness 保持 `preflight-ready/static`、`seed generation blocked`、live=false。未取得 owner 授权
  前不启用 startup guard，也不重复该已知崩溃 attempt；下一门是 parser/compiler/theme 静态清零后
  再做一次新的有界 MCP-first 验收。

### G2 演进

- `67b8f1d` 所在整合线保留 `16aef31` 的 typed `call_ally` observer；其 allowlist 只接受
  `call_ally_interaction` 的 `war:<id>`，generation-safe resolver/full-ID 复核，不扩展 action/ABI/MCP。
  `61cdcd4` 注册三个 Raiktor native CTest，`820accb` 补齐原生决策树/GEN-034 文档索引。
- Python pending-bridge `24/24`、live acceptance `18/18`、gameplay `call_ally` `8/8` 均在普通/`-O`
  通过；fresh MSVC/Ninja 全构建 `338/338`，observer CTest `3/3`、Raiktor 新增 CTest `3/3`。
  全 CTest 记录为 `56/71`，其余 `15` 项是路径/旧 source-contract harness 失败，不能误报全绿。
- 当前 readiness 为 `static/query-ready`，paused live=false；`GEN-034` 仍 unresolved。下一窄入口是
  已有 exact-build 证据支持的 Raiktor attacker→defender truce duration 只读 wrapper，仍不得推导
  expiry、自动投降或提交 action。

### RED、依赖与下一步

- P0 依赖真实二期 producer 和十章同源 capture；二期 builder/静态合同通过不代表宣传片已完成。
- 二期 seed 依赖 CK3 启动故障之外的 parser/compiler/theme 清零和新的有界实机尝试；保留所有失败
  artifact，不盲目重跑。
- G2 truce wrapper 继续在独立 worktree 施工；没有 paused artifact 前不提升为 production-live。
- 本轮不执行垃圾清理，按项目所有者指示保留现有现场。

## 21:14 当前收口增量（真实时间）

### 视频宣传工具（P0）

- `79ce523` 收紧二期 producer hand-off：producer 必须显式返回 canonical `capture_mode`、版本和完整合同，禁止
  `setdefault` 或把一期结果改名伪装；`2628da0` 将 phase2 capture schema 纳入安装合同、资源清单和安装验证。
- `c688fb9` 新增 side-effect-free producer scaffold：只做依赖注入、运行时 readiness 和八段合同校验；不自动注册，
  不调用 `PromoRecorder.start/clean_hold/stop`，不启动 CK3/FFmpeg，也不写 artifact。缺真实 probe/choreography 时返回
  typed RED，普通/`-O` 纯测试均 `12/12`。
- 基于 `6be8831`（其后 `c39f09c`/`c688fb9` 未改动 package 内容）重建的外部包：wheel
  `C:\Users\xenoa\AppData\Local\Temp\xar-promo-package-6be8831-20260901\artifacts\xar_promo_toolchain-0.1.0-py3-none-any.whl`
  为 `147394` bytes、SHA-256 `D343CFF474415A8E5ADA92287C86482F9C57B7C0BE913F7451A31C2A0D995059`；sdist
  `C:\Users\xenoa\AppData\Local\Temp\xar-promo-package-6be8831-20260901\artifacts\xar_promo_toolchain-0.1.0.tar.gz`
  为 `243608` bytes、SHA-256 `4E4A4E8A6CA1A251AEDA31EDEB59CFED485423F496A5C2559E01637F2846C5F0`。两包均含四个 schema，外部 wheel 安装资源读取通过；install-contract `9/9`、packaging `4/4`，普通/`-O` 均 GREEN。
- readiness 仍为 `static-ready`。八段真实 gameplay choreography、clean capture、十章媒体/TTS/字幕、runtime claim matrix、
  内容/画面审阅、真人签核、export/publish 尚未开展；没有宣传片成片或可发布视频。

### 天朝二期

- 针对当前整合头 `6be883137ca4fdad2562e9d2c0afc241073aa105` 的 clean export + `--preflight-only` 已重新完成。
  权威报告为
  `Z:\ck3_mod_rewrite\_phase2-fresh-preflight-6be8831-20260901-210357\attempt-valid-pipe\artifacts\preflight.json`，
  `34470` bytes、SHA-256 `5185B4D57E29D378A985C199FEC24F275045531100F6A710C06822F660C5A702`；source tree `1637` files，
  tree SHA `D2800B21EDC20A52E5A9FDBC9B83D3CB5EC71ED564A53DFD95FEA2D5E042021F`，source ZIP SHA
  `1C9EC0F4ACA1DCD9E2AB436C030B694423F8E1FBB00BCC466808073203B696F0`。
- 该报告所有冻结输入、source/ZIP 等价、projection、进程清零和静态命令（普通/`-O` loader/bootstrap/fixture/capture、
  `validate_static/local` 共 `10/10`）均 GREEN，但边界明确为 `preflight-ready`：`seed_ready=false`、
  `seed_contract_status=blocked_seed_generation_required`、`ck3_launch_attempted=false`、`launch_boundary=not-crossed`。
  首次非法 pipe 的配置 RED 也保留在同一外部目录：报告 SHA `D68BA9AE6434622DBA1861FDA4BAB5C68661200646A50C4EB6C9657927ED1A36`。
- 旧 `attempt-correct-pipe` 报告（SHA `53C8A09DC0A0C07165626CB902A23EF218B20879D625E468D7B62E26B1B7A888`）冻结在 `2622cbb`，已由上述当前头报告取代，不能继续作为当前候选绑定。
  新报告使用既有 four-domain bridge 依赖（DLL `B018FB8C55E99E915E728166BBF1934518095CF4E4981114970FC0CEE69026F0`、
  injector `8F4E9C6E36BA50B976909E3AC54B3B6A655F39EBED81F42993E517F0E40BF65D`），不声称包含 G2 truce fresh build。
- 唯一有界 CK3 attempt 仍为已知 particle2 null-slot `C0000005` RED（runner SHA
  `130CAD215F53EC72E1ED4B504D39EADB446ABA1FF9C7EE49149433D5B765709F`，exception SHA
  `CFBFF96E1144EE868ECC592A5BD56431DC037AEF6ED1DD4202BBFC9C8C5B7E66`，cleanup SHA
  `10F9FC9ECF964389A8D599041B82B240938BA9035C2AB92AC92FCDB872E7F52A`）。early-exit 修复已覆盖进程提前退出，但未再启动 CK3；
  seed/candidate/paused provider matrix 仍不存在，状态保持 `seed blocked/not-live`。

### G2 演进

- `6be8831` 将 Raiktor `raiktor_surrender_truce_v1` pointer/evaluator core 接入正式 terms/MCP wire，只公开稳定的非负
  `evaluated_days`；`actual_expiry_observable=false`、`expiry_date_raw=null` 固定保留。`c39f09c` 同步 fixture、GEN-034
  ledger 和 live-acceptance harness，明确区分 duration observed 与 expiry unobserved；相关 source-contract SHA 为 truce
  `E168CA7B6382B2CCBD8D0619220ADF82A8AE79E525D9CE6E149CCFF9C0F86368`、six-domain
  `05C3C40180FCFEC6E33C5DB627287C7FCF361C5B5228374D953BD5119DAD3C79`、policy
  `9BDFE4EEB1A1CB9C57F66D67325656FEBF73FD31101904310D92F0D1E628B2C5`。
- Python terms/truce/policy 等相关单测普通/`-O` `72/72`；fresh MSVC/Ninja 编译链接 `360/360`，针对性 CTest `6/6`；全 CTest
  `56/71`，其余 `15` 项是既有 source-contract/path/harness RED，不能误报全绿。fresh DLL SHA
  `CFDA6FF379F25A250696CAD2497AE5FDDB78559E924E787F6B86B13D7608B578`，injector SHA
  `92F54D2701C5A64BB3735E74C3B869360DEA89CCE0B47EAE4481B475166A0E13`。
- readiness 为 `static/query-ready`、paused/live=false；没有 truce paused shape artifact，generic war-bound/source-loss、
  campaign/budget/white-peace、typed action 和 action-boundary postcondition 仍未闭合，`GEN-034` 继续 unresolved。旧四域 live
  artifact（SHA `1187D0BD129DA9188B7EBC0C389B035B5C4B1383CE1FF4895481678BCB4371E5`）是 `6be8831` 之前的 wire，不能当作当前 truce live 证据。

### RED、依赖与下一步

- P0 下一步是接入真实 CK3 gameplay producer，完成八段同源 clean capture，再做候选构建、媒体与人审；scaffold 和包测试不改变
  `static-ready`。
- 二期下一次 CK3 尝试必须先解决/明确授权已知启动崩溃，并使用当前头的新 preflight；不重跑旧 attempt，不以延长 timeout 替代修复。
- G2 继续只做窄 truce paused/live probe，随后才评估 generic war-bound 与完整六域/策略；不开放 surrender action。
- 不执行垃圾清理，保留当前构建目录与所有失败现场。

## 21:29 当前收口增量（真实时间）

### 视频宣传工具（P0）

- `b274a36` 将 phase2 handoff 的 producer 与 runner 合同统一收紧：mode 必须是精确字符串、版本必须是精确整数，
  嵌套八段合同采用递归类型感知比较；显式 `result` 只能为 `GREEN`，scaffold 内部合同快照不再暴露可变引用。
  普通/`-O` scaffold 纯合同测试均 `18/18`，promo capture runner 普通/`-O` 也均通过；未启动 CK3/FFmpeg，二期真实
  producer 与成片仍未产生。

### 其他两线与纪律

- 天朝二期和 G2 的 readiness、RED 现场及下一步不变，沿用本文件 21:14 节的权威 artifact 索引；本次只补合同门，
  不重复已知崩溃 attempt 或 truce live probe。
- 不执行垃圾清理，保留构建目录、失败 attempt、crash dump 和外部包现场。

## 21:40 当前收口增量（真实时间）

### 视频宣传工具（P0）

- `770973b` 修复二期 seed-preflight 绑定边界：即使 timeline identity 已匹配，只要 capture root 的 GREEN
  `report.json` 缺失，`capture_identity_status` 仍为 `unbound`，并保留明确的
  `capture_identity_unbound` blocker；缺失报告不会在后续 `verify_unchanged` 被误当作已绑定。二期 builder
  普通/`-O -B` 均 `17/17`，静态校验继续 GREEN。
- 只读审计用 fake runner 复现的 raw/evidence source 漂移 seam 已由 `03940d8` 收口：full build 在最终时长
  校验后、候选持久化前重验 report/timeline/evidence-index/raw/全部 clean-span evidence 的 load-time bytes/SHA；
  raw 与 clean-frame mutation 均保留 typed `capture-source-immutability` RED attempt。phase2 builder 普通/`-O -B`
  `18/18`，CK3 adapter `11/11`，toolchain 全套 `203/203`（普通/`-O -B`），未启动 CK3/FFmpeg。

### 纪律与现场

- P0 仍为 `static-ready`，真实二期 producer、八段 clean capture、媒体/人审/发布均未开展。
- 不执行垃圾清理，继续保留构建目录、失败 attempt、crash dump 和外部包现场。

## 22:05 当前收口增量（真实时间）

### 视频宣传工具（P0）

- `03940d8` 将已复现的 capture-source 漂移问题接入 phase2 full execute：候选写入前会重新校验 report、timeline、
  evidence index、raw recording 及全部 clean-span evidence 的字节数和 SHA；fake runner 修改 raw 或 clean-frame
  evidence 时均保留 `phase2-entry-failure.json`，阶段为 `capture-source-immutability`。
- `12f0b91` 合入天朝二期 loader 的另一个实证修复（见下节），并完成整合头回归：xar_promo_toolchain
  `203/203`（普通、`-O -B`），phase2 builder `18/18`（普通、`-O -B`），CK3 adapter `11/11`（普通、`-O -B`），
  producer scaffold `18/18`（普通、`-O -B`），promo runner 与 `validate_static.py` 均 GREEN。未启动 CK3/FFmpeg。
- 当前头外部包已完成可安装复核：
  `C:\Users\xenoa\AppData\Local\Temp\xar-promo-package-12f0b91-20260901\artifacts`；wheel `148000` bytes，
  SHA-256 `D7FEC8E98C6606F61252D320C8451274442011009E5F409E1DACA6799CB0B33B`；sdist `244106` bytes，
  SHA-256 `F5B758151A432B4C7D7449B9414A2BEA5D8C84BF230325D4B69703B53531CC75`。两包均含四个 schema，外部安装与
  `xar-promo --version`（`0.1.0`）通过；install-contract `9/9`、packaging-contract `4/4`（普通/`-O`）通过。
  generic/Vivhite/ZhongGuo/Ox release checks 分别为 `86/27/279/22` 文件 GREEN。工具本身没有 publish 命令；
  PyPI 上架仍需维护者配置 token 或 Trusted Publishing，当前未声称已上架。

### 天朝二期

- `12f0b91` 修复 loader 顺序：即使日志已有 `Load Save/In Game` 授权标记，也先调用
  `native_session_probe`；terminal/non-zero process exit 优先产生 typed `native_session_process_exit` RED，只有
  probe 返回 `None` 才继续等待并允许 GREEN。新增 authorized-marker fake regression；loader 与 seed runner 普通/`-O`
  均 GREEN，未重跑 CK3。
- current-head `12f0b91` no-launch preflight 已完成（无 CK3/FFmpeg 启动）：权威报告
  `Z:\ck3_mod_rewrite\_phase2-preflight-12f0b91-20260901-220303-077-961bab98\attempt-valid-uuid\artifacts\preflight.json`，
  `34626` bytes，SHA-256 `6EF53565AD120CDB2F3AE7F3AC6D8877E81E75E8F64B123D300F1DC47E445D8D`；source tree `1641` files，
  canonical tree SHA `4254FF0146C909464BDCADABBA0B816E12EA147672E39A3796EF053848356B0F`，source ZIP `80422158` bytes，
  SHA `71CF8C8750102412CF76C406793A68C990A80B47A039CD27F192B9D21F35CCF0`。结果 `GREEN/preflight-ready/ok=true`，
  frozen commit 精确为 `12f0b91193d2ea5d37919e28032d0cc2d67a3e2e`，但 `seed_ready=false`、
  `seed_contract_status=blocked_seed_generation_required`、`ck3_launch_attempted=false`、
  `launch_boundary=not-crossed`、`native_session_started=false`、`driver_opened=false`；静态矩阵 `10/10` GREEN。
  非法 pipe 等 no-launch harness RED controls 也保留（valid CLI 错误报告 SHA `5CEDAC1C...25DB8F`、invalid pipe SHA
  `4ABECFF7...0F1DF7`），不归类为能力 RED。既有 particle2 `C0000005` attempt 不重复运行。

### G2 演进

- 对 fresh truce build 的一次有界 paused probe 已收口为 **harness RED before launch**：
  `C:\Users\xenoa\AppData\Local\Temp\xar-g2-truce-paused-live-20260901T215831\report.json`，`4337` bytes，
  SHA-256 `5396A78148C6197922AC885C60E3C1699B44009F12A9F1F03ADF4C30691A1719`。失败发生在
  `prepare_profile → agent_runtime_fingerprint` 的 `git status`，CK3 未启动、无 PID/paused snapshot/MCP terms；
  cleanup `ok=false` 仅因 session 从未启动，checkpoint/driver-state source invariant 保持 true。按一次尝试约束不重跑，
  不把它归类为 CK3 capability RED，也不声称 `truce_ready` 或 `evaluated_days` live。

### 当前 readiness、依赖与纪律

- P0 仍为 `static-ready`：真实二期 producer、八段 CK3 clean capture、十章媒体/TTS/字幕、runtime claim matrix、
  人审、export/publish 尚未开展。producer 审计确认下一项实际工程依赖是先生成 ready seed，再把
  `phase2_promo_capture` 接入 seed install + managed supervisor；不制造 synthetic capture。
- 天朝二期保持 `preflight-ready/static + seed blocked + not-live`，等待 current-head preflight 和后续真实 seed gate。
- G2 保持 `static/query-ready + paused/live=false + GEN-034 unresolved`；上述 harness RED 不改变该状态。
- 不执行垃圾清理，保留构建目录、失败 attempt、crash dump、probe report 和外部包现场。

## 22:45 宣传工具独立拆分评估（仅评估）

- 按项目负责人要求，v0.1.0 继续在本仓库孵化与发布；本次没有创建 `Z:\\workspace` 目录、移动文件、拆分 Git 历史或创建 GitHub 远端。
- 只读盘点确认 `xar_promo_toolchain/` 有独立 `pyproject.toml`、LICENSE、MANIFEST、CLI、schema、Codex skill、90 个 tracked files；当前头 wheel/sdist 可外部安装，包测试普通与 `-O -B` 均 `203/203`。
- 仍存在主仓耦合：两个 builder 的源码路径注入、二期 preset 测试读取主仓配置/fixture、根 CI 直接发现 subtree 测试，以及少量 legacy/packaging 测试反向导入主仓。直接复制目录会留下这些假设。
- 迁移门槛定为：v0.1.0 已公开发布并记录产物哈希；再从该 tag 的干净 clone 做 path-filter/subtree 历史抽取，先保留二期配置/producer/runner 在主仓，主仓以固定版本依赖和兼容 wrapper 过一轮完整回归，最后才移除 subtree。预估核心抽取半天到一天，含 CI/二期解耦约 1–2 个工作日。
- GitHub 建仓仍需维护者重新认证（当前 `gh` token invalid）；该外部前置不影响今晚的单仓 v0.1 工作。大型成片和 CK3/Steam 现场不进入独立代码仓。
## 23:21 三线收口与伪 Runtime/CLI 集成

- 宣传工具：合并 `1157883`、`84c60b7`、`6138dfd`、`b28c8e8`、`e9edd85`，完成 phase2 capture typed-RED 接线、候选失败留痕、项目配置相对路径门禁与 artifact-only 发布工作流。代表性回归为 `204/204` Python 测试、loc/static validation GREEN、`git diff --check` GREEN；readiness 仍为 `static-ready`，未声称 PyPI、CK3/FFmpeg 或成片已发布。另修正校验器对 `upload-artifact@v6` 的兼容性。
- 天朝二期：当前 HEAD 的 no-launch preflight 保持 `GREEN/preflight-ready`，但 `seed_ready=false`、`blocked_seed_generation_required`，且既有 particle2 `C0000005` attempt 仍为唯一 CK3 RED；不重复启动同一崩溃。后续入口仍是 ready seed 与授权后的新 paused 验收。
- G2：保留 `6e27221` 的 profile Git-status race 修复及 `4283724` readiness 边界说明；paused truce probe 仍是 launch 前 harness RED，`GEN-034` 未闭合，不把 duration wrapper 冒充 expiry/live。
- open_kaishek：临时集成线已合入 Runtime scoped/global store（`1942a752`）、`ROOT/THIS/PREV` scope chain（`549ef0a`）、014 appeal fixture（`308d2ba`）、JSONL batch/replay CLI 与 synthetic runtime 入口（`1e4c432`），并保留 corpus gate 修复（`a7a27c9`）。完整 Maven reactor 一次性 GREEN（11 modules，0 failures）；readiness 为 `runtime-fixture/static`，不是 CK3 live。CLI 分支已成功 push；其余集成线待一次性推送。
- 按项目所有者指示，本轮不清理垃圾、不重复核对、不扩展理论安全审计；所有既有失败现场与外部构建目录继续保留。
