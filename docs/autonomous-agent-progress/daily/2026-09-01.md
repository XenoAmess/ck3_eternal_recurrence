# 2026-09-01 日报

状态：**进行中（本次工作包已完成，待次日正式收口）**

正式收口：次日 2026-09-02 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-01.md`](../meetings/daily/2026-09-01.md)

起始分支/HEAD：`detached` / `9c978b3`

## 今天完成了什么

- 按授权启动并落地 `open_kaishek` Phase 0 静态工程基线：Maven 多模块纯 Java
  parser、profile API/CK3 profile、validator、IR、runtime、diff contracts、361
  schema 和 CLI（证据：[`phase0-verification.md`](../../../open_kaishek/docs/phase0-verification.md)）。
- 冻结当前验证环境记录：Temurin `21.0.10+7`、Maven `3.6.3+`、Apache-2.0 和
  [`THIRD_PARTY_LOCK`](../../../open_kaishek/THIRD_PARTY_LOCK)。
- 目标 corpus 27 个 `.txt/.gui` 文件完成 byte-for-byte round-trip（2,677,440
  bytes、0 parser errors）；manifest 重新生成无 diff。
- 补齐 361 domain schema 的 Java/Python structural validator、profile/IR/diff
  合同不变量测试，以及 CLI fail-closed/选项解析 smoke。

## 早会承诺对照

| # | 早会承诺 | 结果 | 证据 / 未完成原因 | 是否顺延 |
|---:|---|---|---|---|
| D1 | 启动 Phase 0 合同冻结工作并审阅 ADR/manifest | 部分完成 | 合同、构建、许可证和 manifest 已落盘并通过离线探针；正式维护者 M0 评审、平台矩阵和 CI 镜像仍 pending | 是 |
| D2 | 维护进度报告与证据索引 | 完成 | 本日报、早会、周报、验证记录与 manifest 已互链；无 live artifact | 否 |

### 计划外完成

- 在用户继续授权后，提前建立 M1/M2 的静态入口、M3 runtime 原语和一个明确标注
  synthetic 的 014 纵向夹具；没有把这些结果写成 CK3 live、differential-certified
  或 product-live。

## 现在在做什么

- M0 静态基线已形成，正在等待正式合同评审；M1/M2 为 `static-ready`，M3 为
  runtime 原语 + synthetic 014 夹具的 `static-ready`；完整 361 exact-build 范围仍未开始。

## 为什么做这些

- 先闭合版本、许可和 schema 合同，再用可重复的 parser/validator/runtime 合同
  探针推进纵向切片；同时保持 CK3 语义边界，避免把静态夹具误报为 live 能力。

## 证据与验证

- 已执行 `mvn -o ... clean test`（11 个模块成功；profile-api 4、CK3 profile 5、
  validator 11、IR 7、diff 2、runtime 19、zg361 synthetic 4 个 JUnit 测试通过，合计 52）；
  syntax/CLI dependency-free smoke 另行显式通过。
- Parser corpus：27/27 parsed、0 errors；CLI corpus SHA-256
  `30d63aad6adbe60a5df610dca4dcb2592f4e8d6dc5edb9a1d411744571cd687c`。
- 361 Python validator：8 tests passed；manifest SHA-256（显式 LF）
  `b219a2ee0ef3c9b77fbc0d28a7c99714e6e8b000b84bf4403d352999e3896ac3`。
- 参数合同：对显式登记参数集合，profile/validator 同时校验参数数量与参数名，未知参数返回
  `INVALID_PARAMETERS`；空集合保留多态/未查明形状。CST/validator 层与嵌套 executable
  sequence 保留有序重复字段，文件根声明层仍检查重复定义；当前 Map 形状的 StrictIrCompiler
  对重复命名参数显式返回 `DUPLICATE_PARAMETER`/`INVALID_INPUT` 且不发射指令，不会静默覆盖。
  非法 UTF-8 返回 `INVALID_BYTE`，CLI 语法错误返回 `INVALID`/exit 1。
- 根据目标 corpus 实证校正 `change_variable { name, add }`、`trigger_event { id, days }`
  与资源表达式 block；vanilla 更广泛多态签名仍标为未认证。
- Synthetic 014：生成 BOM `.txt` 经 Parser → Validator → Strict IR → VM，状态为
  `delivered → appeal_open → closed`；未知 opcode、解析错误和 CK3 未认证语义均
  fail-closed。该证据只属于离线 synthetic fixture。
- 未运行 CK3、MCP live 或生产 Runtime；无 live artifact、PID、checkpoint 或
  differential certification 可登记。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| Phase 0 / M0 合同冻结 | research | `static-ready` | 工程、依赖、manifest 和合同已落盘；正式 M0 审查仍待完成 |
| Lossless parser / M1 | not-started | `static-ready` | 27 个目标文件逐字节 round-trip；property/fuzz 矩阵未完成 |
| Schema validator / M2 | not-started | `static-ready` | CK3 profile 与 361 schema fixture 通过；历史故障覆盖有限 |
| Runtime / M3 | not-started | `static-ready`（原语 + synthetic 014） | stale/draw/receipt/queue 合同与有限状态夹具通过；不代表 CK3 语义 |
| CK3 live / product-live | not-available | `not-available` | 没有 paused/live artifact |

## 失败、阻点与未知

- 阻点：正式 M0 合同评审、平台矩阵/CI 镜像与完整 B2/Workforce exact-build 361
  范围尚未闭合；synthetic 014 子集已完成。
- 环境提示：Windows 本机 javac 在关闭缓存 JUnit jar 时会输出非致命
  `AccessDeniedException`，Maven 进程仍以 `BUILD SUCCESS` 结束；CI 需在干净缓存复核。
- 静态校验 RED：`tools/.venv/Scripts/python.exe tools/validate_static.py` 仅因仓库中既有
  `.idea/` 不在 release allowlist 而失败；未修改该无关目录。
- 未知：CK3 scheduler、native port、完整 361 运行语义尚未认证；不得推断其行为。

## 文件与提交

- 文件：`open_kaishek/` 全部 Phase 0 源码/合同、[`phase0-verification.md`](../../../open_kaishek/docs/phase0-verification.md)、corpus manifest、本次进度报告文件。
- commit/push：Phase 0 集成提交为 `dcbcba7`（`Start Kaishek Phase 0 engine baseline`）。已尝试
  `git push`，但当前环境安全审查以配置远端未验证为由拒绝了网络推送；未绕过策略，远端仍未更新。
  随后提交 `065eab5`（`Harden parser encoding and CLI validation`）纳入非法 UTF-8 与错误码
  修复；`3771ae6`（`Clarify Kaishek baseline status and boundaries`）同步状态边界；本次参数
  合同、重复字段语义和 IR fail-closed 边界提交为 `b1fc809`（`Align CK3 parameter contracts
  and IR boundaries`）。最终报告提交与 push 尝试在本次收口后进行。上述提交均未声称远端同步；
  工作区仍为 detached HEAD，并保留其他工作包的未提交修改。

## 下一步

1. 完成 M0 合同实例评审和平台/CI 决策。
2. 扩展 B2 或 Workforce 361 exact-build 范围，继续走通“实际脚本 → parser → validator → IR → synthetic VM”。
3. 在真实 paused artifact 出现前保持 `static-ready`，不升级为 `fixture-live` 或 `product-live`。

## 范围边界

- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。
