# 2026-09-17 daily report

Status: **in progress**

Formal close: 2026-09-18 00:00 (Asia/Shanghai)

Morning meeting: [`../meetings/daily/2026-09-17.md`](../meetings/daily/2026-09-17.md)

Opening branch/HEAD: `origin/master` / `1e8462be24c9ce941aa5943dba38aa89ab94e222`

## Completed so far

- The ordinary succession production consumer entered master as `1e8462be24c9ce941aa5943dba38aa89ab94e222`. It clears an exact-bound ordinary succession timeline through the existing typed path before accepting the successor checkpoint; its natural-death live gate remains open.
- The R793 siege recovery changes entered master as `e19dd9c26dc7009e41a86f7802ef737ad50faf26` and `c513aa21404e9da4abc5eee55141f61df2ffffaf`: combat now resets stale siege-stall state, while a rejected siege retains the alternate reroute instead of immediately returning the old blocker.
- R794 first rejected an old prepared environment at no-launch preflight because its runtime fingerprint differed. A fresh current-source environment plus `ordinary-seed-rebind-v1` resolved that blocker without changing the save bytes; the formal run then started a new CK3 process and cold-restored the R793 history245 checkpoint.
- The R793 fix was exercised live. R794 replayed the battle transition, submitted typed `start-assault-6` on turn5, independently advanced one game day on turn6, submitted typed stop-assault on turn7, observed seven more days of siege progress on turn8 and saved a new durable checkpoint at history260/date `53149872`, SHA-256 `55A51F5D6329C2682C9108FB190BDE10592BA19EBC4AF518671290E17B1FDE82`.
- The independent R794 post-run seal confirms that checkpoint260/date `53149872` is unpolluted and is the only recoverable state. History261-264 must be abandoned; history263's six-day advance was never persisted. Turn11 action uncertainty is zero.

## Morning commitments: rolling comparison

| # | Morning commitment | Current result | Evidence / remaining gate |
|---:|---|---|---|
| D1 | Seal R793 and fix rejected-siege exit | Complete for the observed R793 branch | R793 seal `98CCF93E...BFF7`; commits `e19dd9c` + `c513aa2`; R794 exercised battle, assault, stop and siege progress. |
| D2 | Focused R794 cold replay, then resume campaign | Partially complete | Cold replay and repaired branch passed through a new checkpoint; turn11 exposed a different `native_war_no_safe_exact_route` RED. |
| D3 | Live-validate ordinary succession consumer | Static integrated, live pending | `1e8462b` is on master; no new natural-death scene in this increment. |
| D4 | Exercise governance faction refresh | Pending | No compatible real faction scene consumed yet. |
| D5 | Continue Council and natural-event gates | Pending | Council remains 1/4; no new natural `.0030` / `.1007` evidence. |
| D6 | Resume the 100-year prefix | Blocked by current War RED | Continue only after the new route state receives a minimal deterministic correction and focused cold replay. |

## Current work and reason

- The current critical path is the R794 turn11 route state. The planner reported that every remaining exact objective route was blocked or observably intersected a non-retreating enemy. This is a campaign-continuity B0 because time cannot safely advance, while submitting a guessed movement or terminal action would create an uncontrolled side effect.
- The next code slice is limited to the exact route-rejection/threat state, followed by a focused cold restore from history260. Unrelated policy refinement, broad matrices and full CI remain outside this slice.

## Evidence and validation

- R794 formal report: `D:\ck3_mod_rewrite_process_assets\g2-r794-war-siege-recovery-c114759\formal-replay-02\formal-report.txt`, SHA-256 `F155E2DD2D0AB6B0932B5C46EEE0903FBA140C9E24CA6EFB8B667F1F5A6C1A6D`.
- Operator receipt SHA-256: `ED609F6B81B744243A11EAE6247F880D03FD279AAEC8DC74C0A0B206C0D61CE4`; 11 attempted turns, 10 successful, 4 queries, 6 gameplay steps and one durable checkpoint.
- Fresh environment SHA-256: `527591E7A7FBFBE34AEDE119D7425CE563AFD853F9A8A44BBB591D6475EC408C`; rebind receipt SHA-256 `865AA16AF8D8BE47CCB697EF4281F9964F69D633B49FDAC82B32E1D34F851F26`. The initial stale-environment preflight launched no CK3.
- The new RED is turn11/date `53150016`, WarID5/ArmyID33, phase `native_war_no_safe_exact_route`, required step `safe-exact-war-route`. `selected_step`, `result` and submitted action are null/false, so action uncertainty is none. The post-checkpoint driver tail must be truncated on the next cold restore.
- Independent seal: `D:\ck3_mod_rewrite_process_assets\g2-r794-war-siege-recovery-c114759\evidence\R794-post-run-seal.json`, SHA-256 `C67F30556E075C14819ED8405C00A92B7677CA9B9EDB20E4A31DCA6D4A0B5842`. It binds the recoverability conclusion to the exact R794 report, driver and checkpoint.
- PID45908 and its process tree were reclaimed; shutdown, session report and driver close are all GREEN, and CK3 inventory returned to `0`.

## Readiness change

| Capability or gate | Before | Now | Evidence / limitation |
|---|---|---|---|
| Portable ordinary preview | GO | GO, unchanged | ZIP SHA-256 `AA9CABB5D4CA709E55AB367A94D0C58A088AAD20D540B76F7990DAD1FC81517C`; bounded scope only. |
| R793 rejected-siege repair | Static-ready | Production-live on the observed branch | R794 reached typed assault/stop, independent date progress and history260 checkpoint. |
| Ordinary succession consumer | Missing from formal ordinary path | Static integrated | Commit `1e8462b`; natural same-campaign live and cold proof remain. |
| Council final gate | 1/4 | 1/4 | Only already-councillor is closed; guest, pending and replacement-fireability remain open. |
| GEN-034 | 2/4 | 2/4 | No typed three-way terminal/material settlement/cold terminal closure. |
| G2 authority | 1/8 | 1/8 | Only G2-M1 is complete under `g2-requirements-v1.json`. |

## RED, blockers and unknowns

- R794 is a capability RED, not a harness failure. The focused fix removed the R793 rejected-siege blocker and produced durable progress, then a different route/threat state stopped turn11 fail-closed. It must not be retried unchanged.
- Independent R794 sealing is complete. History260/date `53149872` is unpolluted and is the unique recovery point. Driver history261-264 is abandoned, including history263's unpersisted six-day advance; the next cold path must truncate that tail before continuing.

## Files and commits

- Integrated source: `1e8462b`, `e19dd9c`, `c513aa2`.
- Report files in this increment: `docs/autonomous-agent-progress/daily/2026-09-17.md`, `docs/autonomous-agent-progress/weekly/2026-W38.md`.
- Report commit/push: `work/wp-report-r794-increment-20260917`; exact pushed commit is recorded in Git history and the coordinator handoff.

## Next steps

1. Inspect the exact route rejection/threat observations from the sealed history260 recovery anchor without launching another unchanged run.
2. Implement and focus-test only the deterministic planner/query correction required by that state.
3. Start one fresh-process cold replay from history260, require an independent material result, next-turn consumption, paired checkpoint and complete cleanup, then resume the same ordinary campaign.
4. Continue the succession, governance, Council and natural-event gates without advertising incomplete capabilities.

## Scope boundary

- The user-startable preview remains available now. This increment does not claim a 100-year prefix, full first campaign, second seed or new G2 milestone.
- Religion remains owner-deferred except for the allowed minimal holy-war and marriage legality inputs.

## R795-R796 ordinary-war continuation

- R795 used the prepared history260 pair but ended as a harness RED before semantic readiness: the 600-second outer wall clock expired at 602.96 seconds while readiness was allowed 720 seconds. It executed zero turns, queries, actions and checkpoints. Report/seal/closed-ledger SHA-256 are `6F53E81D...762625`, `164B1BD5...3118B` and `64636093...770AF`; cleanup returned CK3/injector inventory to zero.
- R796 corrected only that proven parameter error to `timeout=810 / readiness=720`, cold-restored history260, and discarded the abandoned R794 tail. It queried termination twice, advanced six days once, submitted exactly one typed `offer-white-peace-5`, received honest `submitted_pending`, advanced exactly one response day, then stopped at `native_war_no_safe_exact_route`. No duplicate proposal or later action occurred. Formal report/operator receipt/seal/closed-ledger SHA-256 are `F92AFE0E...52A09`, `17D4EA6A...4A398`, `A7A20DBA...561A` and `0E8BC7CD...ED81`; cleanup is GREEN.
- The R796 RED is now an evidence-backed strategy defect. The frozen native contract has `ai_min_reply_days=4 / ai_max_reply_days=9`, and the prior production loop observed application on the tenth daily advance; the old strategy only forced the same-day response advance and then fell back to an impossible military route after day one.
- Commit `836ad237` adds the narrow correction: when a submitted white peace still has the old WarID and every exact route is unsafe, advance one day at a time through the tenth-day observation boundary without querying or resubmitting; if the WarID remains afterward, stop explicitly at `native_war_white_peace_postcondition_unresolved`. Focused normal and `-O` checks passed 3 tests plus 2 subtests each; the full gameplay bridge file passed 231 tests plus 82 subtests.
- No new live replay was launched after the fix. The only durable save is still the pre-action history260 checkpoint `55A51F5D...FDE82`; R796's post-checkpoint submission exists only in the sealed driver tail and a cold restore would remove it. A replay from that checkpoint would submit on a rolled-back timeline, so it is not authorized by the handoff's no-unconfirmed-resubmit rule. Council work therefore did not start.

### Current readiness after R796

| Gate | Status | Evidence boundary |
|---|---|---|
| Portable ordinary preview | GO, unchanged | Frozen ZIP `AA9CABB5...517C`; this increment does not replace it. |
| Ordinary WarID5 exit | Capability RED; static repair ready | One real submission is pending only in an uncheckpointed, terminated timeline; no material postwar state or cold restore exists. |
| Council final gate | 1/4, unchanged | Deliberately not run before the war blocker closes. |
| GEN-034 | 2/4, unchanged | Ordinary WarID5 does not satisfy Raiktor C/D. |
| G2 authority | 1/8, unchanged | Only M1 remains complete. |

### Next safe step

Do not start CK3 or repeat `offer-white-peace-5` from history260 without an explicit recovery decision that treats the durable pre-action checkpoint as a rolled-back timeline. Once such a replay is authorized, it must use source containing `836ad237`, `810/720` or a later measured-safe timeout pair, one fresh process, one proposal maximum, daily response observation, material WarID disappearance, postwar checkpoint and a separate cold restore. Council follows only after that chain closes.

## R797-R802 Council observation and ordinary-war closure

- R797 consumed the materially different occupied-steward scene as a private query-only Council run. It returned 14 provider rows and 11 ordinary candidates; already-councillor had three positives, while guest, candidate-pending and replacement-fireability denial were all zero. Gameplay/date/checkpoint counts were zero, cleanup was GREEN and the same scene must not be queried again. Council remains **1/4**, public query/action/advertisement remain OFF.
- Recovery commits `b9b6f249`, `881e1ba5`, `f5a8914d`, `36a25a55`, `c9c371b4` add immediate `submitted_pending` checkpointing plus the exact-build sender-side outbound white-peace status query and its bounded production-owned entry. The query is also exposed through the versioned service/MCP contract; no capability advertisement was opened.
- R798 was a harness readiness-timeout RED with zero query/action and unchanged source pair. R799 performed the query and observed `exact_absent`, but the report correctly remained RED because its cold-restore ledger assumed append-only history. Commit `851c36dc` fixes that real report defect by accepting exact truncation to the checkpoint prefix; focused normal and `-O` tests passed 9/9 each.
- R800 then cold-restored the sealed history260 pair and returned full `GREEN_READ_ONLY / exact_absent`: history264 was truncated to checkpoint260, restore261 was exact, one typed query ran, date/save/war state stayed unchanged and cleanup was proven. Only this fully GREEN run authorized one new terminal submission.
- R801 used the formal production entry. Turn4 compared continue/white peace/surrender on the same frame and submitted only `offer-white-peace-5`; the new immediate fence saved history269/SHA `DECFA6FF...DA46`. Turn11 independently changed WarID5 from present to absent, turn12 consumed the result by disbanding Army33, and turn13 saved the peaceful history289 checkpoint SHA `F48AD4E5...D97F6`.
- The same 20-turn window then declared a new same-target war at turn16. That later history290-301 tail is explicitly abandoned and is not used as postwar evidence. The real B1 was fixed in `aba36a4e`: a material terminal result now holds the peaceful state for the existing bounded 30-day terminal horizon before any new declaration. Five focused strategy tests pass in normal and `-O`.
- R802 cold-restored the projected history289 postwar pair in a new process, truncating the abandoned tail. The first formal strategy turn selected `native_postwar_reentry_cooldown -> life-advance`, advanced 32 days with no war or army before/after, did not reoffer WarID5 or declare a new war, and saved history293/date `53150976`, checkpoint `DFC96CFD...71AF7`, driver `7C79FE4C...48EF0`. Cleanup returned CK3/injector inventory to zero.

### Incremental delivery table

| Delivery gate / capability | Status | Formal entry | Live and recovery evidence | Remaining boundary |
| --- | --- | --- | --- | --- |
| User-startable bounded preview | GO, unchanged | packaged `g2_preview_operator.py` / `native-auto-run` | ZIP `AA9CABB5...517C`; R790-R792 qualification remains the advertised evidence | Package still advertises only the frozen R783 bounded slice; R800-R802 are not yet repackaged |
| Council final gate | 1/4 | private gate-only query | R797 report `B6345292...4641`, no mutation, cleanup GREEN | guest/pending/replacement denial need materially different natural scenes; public OFF |
| Ordinary WarID5 terminal loop | **production-live loop for this exact branch** | formal `native-auto-run`, public sender-status query only for recovery | R800 `257104F8...133A`; R801 `E6E47677...48C6`; R802 `12BE7379...BD2`; exact action, material disappearance, next-turn cleanup, paired checkpoint and new-process cold restore | Does not satisfy Raiktor-specific GEN-034-C/D and is not a whole-campaign gate |
| GEN-034 | 2/4 | unchanged contract | Ordinary WarID5 evidence is retained as reusable production evidence only | C/D still require their Raiktor-specific projection/evaluator/action/postwar contracts |
| G2 authority | 1/8 | `g2-requirements-v1.json` | no milestone definition or denominator changed | Only M1 is complete |

### Code, tests and cleanup

- Integrated master commits in this increment: `0aa366ef`, `b9b6f249`, `881e1ba5`, `f5a8914d`, `36a25a55`, `c9c371b4`, `851c36dc`, `aba36a4e`.
- Sender-status native build: DLL `DA7CA992...7D3B7`, injector `46D43267...C75F`; 11 focused native tests GREEN. Python sender-query tests passed normal/`-O` 3/3; recovery bookkeeping passed normal/`-O` 9/9; postwar re-entry strategy passed normal/`-O` 5/5.
- R798-R802 each have persistent allocated/closed ledgers. Material seals are under the corresponding artifact roots. All temporary Git branches were deleted locally and remotely after ordinary fast-forward delivery; `origin/master` reached `aba36a4e` before this report update. No merge commit or shared-master force push was used.
- The next durable ordinary continuation source is `D:\ck3_mod_rewrite_process_assets\g2-war-r802-postwar-h289-aba36a4e\final-pair-h293`, bound to checkpoint/driver `DFC96CFD...71AF7` / `7C79FE4C...48EF0`. It must be re-prepared against the then-current clean source commit before the next CK3 launch.

## R803–R806 runnable-preview refresh and delivery

- A deterministic ordinary-preview builder was delivered through master `9bacc5af`. It stages an allowlisted sparse Git/source tree, performs a no-launch rebind/preflight, assembles a reproducible ZIP, verifies a fresh extraction, and promotes only through an external live qualification receipt bound to the unchanged ZIP SHA. Focused normal and `-O` tests passed 5/5.
- Three concrete packaging REDs were preserved and fixed before launch: the finalizer compared environment file bytes instead of its logical digest; ZIP extraction lost empty Git metadata directories; and `prepare-state` left an unignored `__pycache__` in the extracted source. The final package retains a functional sparse Git repository and remains clean after runtime cache creation.
- R803 was a no-launch eligibility RED for the runtime-cache issue. The rebuilt exact candidate then passed R804 fresh-extraction eligibility 9/9, standard feudal actor31853/date53150976 with empty active context, and fully reclaimed PID205880.
- R805 entered through packaged `g2_preview_operator.py run -> native-auto-run -> ck3_auto_turn`. It completed 13/13 turns with six visible gameplay turns: declarable-war query, same-frame entry assessment, typed declaration, independent WarID25 frame, next-turn termination query, troop raise, Army304 frame, move and battle. A controlled stop saved history316 checkpoint `96058952...FAE6` and driver `348FF39E...C047`; consumed `offer-white-peace-5` replay count was zero and PID167232 was reclaimed.
- R806 used a distinct PID52284 to cold-restore that exact pair and the same actor/episode/WarID25/Army304. It completed 5/5 turns with two gameplay advances to date53152848, submitted neither a duplicate declaration nor old white peace, saved checkpoint `E2044A6B...CD7B` / driver `A34BEBBF...B4FF`, and returned CK3/injector inventory to zero.
- The unchanged ZIP is now `GO_RUNNABLE_PREVIEW`: `D:\ck3_mod_rewrite_process_assets\g2-preview-ordinary-r802-9bacc5af-stage-20260917\g2-preview-ordinary-9bacc5af-r802.zip`, SHA `E32D2057...A1273`. GO manifest SHA is `CAA552E0...D4E5`; live qualification SHA is `9F7DA87B...C1AA3`. It supersedes the R783 package for current delivery.

### Delivery state after R806

| Delivery gate / capability | State | Evidence and boundary |
| --- | --- | --- |
| User-startable bounded preview | **GO, refreshed** | Exact immutable R802 ZIP plus R804 eligibility, R805 formal action/next-turn/controlled-stop and R806 new-process restore |
| Ordinary WarID5 terminal loop | production-live loop for observed branch | R800–R802; retained as consumed history in the new package |
| Current packaged continuation | production-live bounded | R805 declared WarID25 and R806 resumed its battle goal without replay |
| Council final gate | 1/4 | already-councillor only; guest/pending/replacement denial remain unobserved and public OFF |
| GEN-034 | 2/4 | packaged ordinary evidence does not satisfy Raiktor-specific C/D |
| G2 authority | 1/8 | only M1 complete; no denominator or milestone changed |

The next live queue remains Council gate-only read observation, then current-war/GEN-034 termination work and ordinary campaign continuation. No 100-year prefix, first full campaign, second seed, natural succession completion or new G2 milestone is claimed.

## R807–R835 Raiktor continuation and forced-state recovery

- R807 preserved a zero-turn readiness RED caused by the old `300s` bound. R808–R831 then continued the same Raiktor WarID33554473/episode from history178 through route, retreat and combat states to history641 and player-relative score `-100`. No competing terminal action was submitted.
- R832 selected a committed route because the three-way evaluator still treated continue as eligible at `-100`; CK3 naturally ended the war during that sentinel. The formal loop then disbanded both residual armies, ran two peaceful strategy cycles and saved history669/date53203752. Report/checkpoint/driver SHA-256 are `2BA58F7F...AE7A78`, `E45F1CE7...BB001`, `C327EF26...872CE`.
- This natural settlement is real postwar evidence but not GEN-034 C/D completion: the contract requires a same-frame three-way recommendation followed by one semantic white-peace/surrender action, independent material state, next-turn consumption and recovery. GEN-034 remains `2/4`; G2 remains `1/8`.
- R833 proved a new-process cold restore from R832, old WarID absence, two peaceful loops and no old war-action replay, then stopped honestly when a pending interaction deferred the bound checkpoint. R834 reproduced the exact `ransom_interaction` on sealed old runtime `1f117ab8` and fail-closed before reply because that version lacked the already integrated classification; report SHA `943C374F...258A`.
- A no-launch attempt to rebind R832 into ordinary `xar_off` was rejected: its source environment is actually `xar_on` and its legacy driver lacks ordinary lifecycle anchors. No CK3 round was allocated. This prevents the Raiktor artifact from being mislabeled as ordinary-preview evidence.
- R835 used the delivered `9bacc5af` runtime, native `881e1ba5`, exact CK3 `1.19.0.6` and a fresh independent `xar_on` profile. It cold-restored R832 and completed 20/20 turns with 16 queries, four gameplay actions and one final paired checkpoint. Two natural ransom requests were each queried, rejected once through the typed action, independently observed absent on the next paused revision, and followed by later production queries/advances without replay.
- R835 report/driver/checkpoint SHA-256 are `4ABB17D8...C1C2`, `99047F37...FDC0`, `B75E7606...DF30`; final history/date are 694/53204904. WarIDs and armies were absent throughout, PID47008 and injector were reclaimed, and the exact interaction B0 is closed for this `xar_on` recovery slice. Public state does not expose prisoner custody, ten-year refusal flag or player resource deltas, so those stronger material claims remain unmade.

### Readiness after R835

| Delivery gate / capability | State | Evidence / next dependency |
| --- | --- | --- |
| User-startable ordinary preview | **GO** | Immutable R802 ZIP `E32D2057...A1273`; R835 does not change its `xar_off` scope |
| R834 ransom forced-state B0 | production-live closed for exact definition | R835 two typed rejects, independent disappearance, later-turn consumption and paired checkpoint; R772/R773 retain matching definition/recovery evidence |
| Raiktor GEN-034 C/D | **2/4, open** | R832 ended naturally with zero terminal submission; fix the `-100` continue eligibility and replay from the safe R831 pre-terminal pair |
| Council final gate | 1/4 | R797 adds no new class; wait for a materially different natural scene |
| G2 authority | **1/8** | only M1 complete; no milestone or denominator changed |

The next single-instance candidate is not another R832 postwar replay. After the focused Raiktor evaluator change and tests, cold-restore the R831 pre-terminal pair under a new versioned asset and require exactly one legal semantic terminal action, material settlement, next-turn consumption, paired checkpoint and a distinct cold-restore round.

## R839–R847 terminal, event and ordinary recovery increment

- R839 closed GEN-034-C. On one paused frame the production policy kept all three choices visible, made continue ineligible under complete primary-attacker/player-relative `-100` terminal control, observed white peace as native-unavailable and selected surrender as the sole eligible action. It submitted `surrender-war-33554473` exactly once, verified the typed `raiktor_claim_cb / -100 / absolute -100:+100 / recipient accepts` receipt, observed independent WarID and both-army disappearance, consumed the peaceful result on later formal turns and saved history671. Report/checkpoint/driver/close-ledger SHA-256 are `BD798BAE...1FEE6`, `5D040C7E...7B236`, `842CCC35...97CC7` and `2AC20E2D...85611`.
- Authority therefore moves from GEN-034 `2/4` to **`3/4`**, while G2 remains **`1/8`** and G2-M0 remains `in_progress`. GEN-034-D is still `blocked_live`: the terminal scene lacks a matching creation-time source capture, and gold, prestige, directional truce plus persisted expiry, source-specific war-bound regiment destruction and postwar identity/recovery are not closed together as the fixed source-bound six-item certificate.
- R842 naturally observed `prison_notification.2002` and completed exact registry query → one typed option → independent old-instance disappearance → next formal-turn consumption. Report/close receipt are `60554323...24C2` / `EEB62CA4...F996`. Its later natural `health.7200` stopped on projection drift; R844 independently exposed the analogous exact `health.7500` drift. Commits `256d57e4` and `a5db1e5d` add only the source-reviewed exact projections. `health.7500` has not naturally recurred after its fix, so its live gate stays open.
- R846 started a distinct CK3 process from the R839 history671 pair under `master@a5db1e5d`. It kept WarID33554473 absent with zero surrender replay, rejected one exact `ransom_interaction`, naturally consumed `prison_notification.2002` again through one registry-bound option, observed its old instance absent on the independent frame, consumed that absence on turn18 and saved a new history693/date53204928 pair. The run is 20/20 with 15 queries, five gameplay turns and one checkpoint; report/checkpoint/driver are `63CFCDD4...34D1`, `9893F8C1...C08C`, `8315217A...F83`; cleanup is GREEN. The outer PowerShell capture returned code1 because it converted the CLI's normal stop-file stderr line into `NativeCommandError`; the complete report is `ok=true`, the stop file is absent and the managed tree is gone. The mismatch is preserved as harness-capture evidence and was not hidden by a retry.
- R845 closed the ordinary capital-preview dispatch B0. It ran 20/20, performed one read-only `preview-move-army-304-to-45`, one accepted/submitted move, eight later route-progress turns and then battle query/epoch consumption. Four post-move checkpoints were written; report/final save/driver are `2A86B334...5D8E`, `94396905...5D84`, `720E53C2...C1F4`. The final frame is still Province52 combat with target45/route `[45]`; there is no Province45 arrival frame, so arrival is not claimed.
- R847 cold-restored that exact post-move pair in a new CK3 process and exited through the real CLI with code0. Ten of ten turns passed: five queries and five gameplay turns. Driver history504–521 contains zero new preview/move/assault/terminal commands; production queried the same battle and termination state, advanced two battle epochs, observed Army304 transition to native retreat, then consumed three one-day retreat frames. It saved history516 and final history521/date53155584; report/final save/driver are `DBA987B5...0CE7B`, `50993AC4...10031`, `8290560E...3F57E`. War25 remains active at score `-9`; no war terminal, capital arrival or whole-campaign claim is made.

### Current incremental delivery table

| Delivery gate / capability | Status | Formal entry | Live and recovery evidence | Remaining boundary |
| --- | --- | --- | --- | --- |
| User-startable bounded preview | **GO, unchanged** | packaged `g2_preview_operator.py` / `native-auto-run` | ZIP `E32D2057...A1273`, manifest `CAA552E0...D4E5`, qualification `9F7DA87B...C1AA3` | Frozen R802 bounded scope only; R845/R847 are not silently added to its advertised range |
| GEN-034-C | **complete** | production `native-auto-run` | R839 same-frame three-way comparison, one surrender, independent material disappearance, later turn and paired checkpoint | None for C |
| GEN-034-D | `blocked_live` | same formal route plus source-bound certificate | R839 terminal chain and R846 new-process continuation/checkpoint are reusable | Matching creation-time capture and the complete fixed six-item source-bound certificate |
| Exact `prison_notification.2002` | production-live loop | exact registry consumer inside `native-auto-run` | R842 and independent R846 natural occurrences | Does not satisfy required `.0030` or `.1007`; generic event advertising stays bounded |
| Ordinary War25 continuation | production-live bounded | `native-auto-run` | R845 move/route/battle/checkpoints; R847 new-process no-duplicate battle/retreat continuation | War terminal, capital arrival, 100-year prefix and full campaign remain open |
| Council / G2 authority | Council `1/4`; G2 `1/8` | public Council remains OFF | no new qualifying Council scene or complete G2 milestone | guest, pending, replacement fireability and all other milestone gates |

### Delivery and compatibility notes

- The frozen preview ZIP remains valid because commits through `a5db1e5d` did not change its packaged operator, eligibility contract, native DLL/ABI or immutable bytes. The external delivery guide now makes the second-terminal status/controlled-stop/cold-restore commands self-contained by reloading `$Manifest`, `$Python` and the latest run output; this documentation correction does not require a ZIP rebuild.
- Commits `bbf53990`, `fd989120`, `01d48050`, `256d57e4`, `c1887eeb` and `a5db1e5d` do not change public schema, MCP registration or native ABI. The current open_kaishek combination remains compatible; no adapter release is required for this increment.
- R845/R846/R847 each have external allocated/closed ledgers. CK3 and injector inventory returned to zero after every run. Associated implementation branches/worktrees for the integrated code were deleted locally and remotely; the authority-sync branch remains only until this report commit reaches master, after which it must be removed immediately.

### Next actions

1. Continue ordinary War25 from the R847 history521 pair; consume native retreat/reorganization and reach an honest terminal without repeating the prior capital move.
2. For GEN-034-D, do not repeat R839. Acquire a new qualifying Raiktor creation-time source capture and close the fixed six-item source-bound certificate plus cold restore.
3. Wait for natural `tgp_travel_events.0030`, `death_management.1007`, a same-campaign ordinary succession scene or a materially different Council guest/pending/replacement scene; do not spend the singleton on unchanged retries.
4. Resume governance and the 100-year prefix only through already-ready bounded production paths; no full-campaign, second-seed or new G2 milestone is claimed by this increment.

## R848–R854 ordinary War25 terminal and declaration-query recovery

- R853 completed the ordinary War25 terminal chain before exposing the next B0: one surrender, an independent frame with WarID25 absent, Army304 disband, later production consumption and a safe h624 postwar checkpoint all succeeded. A later fresh declaration query nevertheless advertised `31506-17--1`; the same-frame power query succeeded, but the submit path's final `declare_war_interaction` validator rejected the row. No result or after frame existed, so no new war or side effect is claimed and the reason is not guessed as a truce.
- Commit `729c5b26` makes public declaration query and submit share the exact final-validation context. Validator rejection now filters a row, while structural/context failure still makes the whole query unavailable. Release and Debug native fixtures passed, and focused Python declaration/gameplay tests passed 241/241 in normal and optimized mode. Public schema, ABI, command literal, MCP surface and capability advertisement did not change.
- R854 used the new DLL `28FC55A5...5A75EAE29` and a new CK3 process to cold-restore the unpolluted R853 h624 pair. The old failed h625–h630 tail was truncated. Four fresh declaration queries at h629/h632/h636/h639 each returned an available empty set, each was consumed by a later formal turn, and four `life-advance` actions moved date `53157360→53160840` (+145 days). Post-restore replay counts for the old surrender, disband and rejected declaration are all zero.
- R854 ended `turn_limit / qualified / ok=true`, 8/8 turns, no first blocker. Report/driver/final checkpoint SHA-256 are `848E56BD...45464A`, `34FB1623...4C754` and `36E13194...D66FC`; cleanup/session are GREEN and CK3/injector inventory returned to zero. This closes only the exact-scene declaration-query B0. The h640 pair becomes the next ordinary continuation source; its cold restore, a future legal declaration positive case and the whole campaign remain open.

### Incremental delivery state after R854

| Delivery gate / capability | Status | Formal entry | Evidence | Remaining boundary |
| --- | --- | --- | --- | --- |
| User-startable bounded preview | **GO, unchanged** | packaged `g2_preview_operator.py` / `native-auto-run` | ZIP `E32D2057...A1273`; manifest `CAA552E0...D4E5`; qualification `9F7DA87B...C1AA3` | Frozen R802 scope only; R854 is not silently added |
| Ordinary War25 terminal | production-live for this exact branch | `native-auto-run` | R853 one surrender, independent WarID absence, disband, later consumption and h624 checkpoint | Not a whole-campaign or GEN-034-D closure |
| Declaration final-validator mismatch | **B0 closed for exact scene** | fresh public query through `native-auto-run` | R854 four empty queries, later turns, +145 days, replay0 and h640 checkpoint | Legal positive declaration and h640 cold restore remain open |
| GEN-034 / G2 authority | GEN-034 **3/4**; G2 **1/8** | unchanged contracts | no new Raiktor source-bound D certificate or G2 milestone | GEN-034-D remains `blocked_live`; only G2-M1 is complete |
| Council final gate | **1/4** | public query/action remain OFF | no materially different Council scene | guest, pending and replacement-fireability remain open |

No open_kaishek or MCP adapter work is required because the wire contract did not change. R853/R854 implementation branches and worktrees were integrated by ordinary fast-forward/rebase flow and removed; the R854 documentation branch must likewise be removed immediately after master delivery.
