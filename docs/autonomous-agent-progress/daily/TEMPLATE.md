# YYYY-MM-DD 日报

状态：**进行中 / 已收口**

报告截止：YYYY-MM-DD HH:MM（Asia/Shanghai）

起始分支/HEAD：`<branch>` / `<sha>`

## 今天完成了什么

- `<可见结果；注明证据等级>`

## 现在在做什么

- `<当前工作包、状态和下一门槛>`

## 为什么做这些

- `<它解除哪个真实 OODA 阻点或推进哪个长期能力>`

## 证据与验证

- `<测试命令/结果、artifact 路径与 SHA、PID/checkpoint/cleanup；未运行也明确写出>`

## 当日 Show-off 视频（必填）

状态：**已通过抽检 / 未完成**

| 项 | 值 |
|---|---|
| 成片 | `<path>` |
| 时长 / 分辨率 / 帧率 | `<seconds> / <WxH> / <fps>` |
| 编码 | `<video/audio>` |
| 语言 | `English primary + Simplified Chinese subtitles` |
| size / SHA-256 | `<bytes> / <sha256>` |
| 对应 live artifact | `<path / sha / GREEN-or-RED>` |
| 诚实边界 | `<fixture/production、是否执行动作、尚未闭合项>` |

- 抽检：`<可解码、中文渲染、关键实机场景、阶段字幕、最终证据卡>`
- 若本节未完成，本日报状态不得写“已收口”。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| `<name>` | `<status>` | `<status>` | `<source>` |

## 失败、阻点与未知

- `<保留 RED；区分 harness/capability；unknown 写明下一项可施工入口>`

## 文件与提交

- 文件：`<paths>`
- commit/push：`<sha/remote status；尚未提交则明确写 pending>`

## 下一步

1. `<最高优先级的可执行工作>`

## 范围边界

- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。
