# 2026-09-08 日报

状态：**进行中；R289 fresh 产品 loader `303/303`、fatal `0`、Load Save/map/mailbox GREEN。generation `16` 已 exact 关闭草药香囊报价，现停在 `learn_language_outcome.1001` 语言学习结果响应；PID `122132` 健康保留、restart `0`、事件合同回归 GREEN。T0-P2 继续 `LOCKED`**

正式收口：2026-09-09 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-08.md`](../meetings/daily/2026-09-08.md)

起始分支/HEAD：`master` / `f747a6ad0b7e9060d23674697a3eeed8bf38b424`

## 今天完成了什么

- 00:00 后闭合 exact `0x3BE2340` lookup 与前置 VFS publisher 原生树：lookup 的 ABI 为 `PathView16 const* → mount node* or NULL`；两个 settings 输入本身为合法相对路径，查询依赖 lazy singleton `module+0x585FA30` 的 mount 链，而不是直接访问 Windows 文件系统。
- 启动顺序已定：`0x7E7110` 在 task table 前调用 core init `0x3B5C410`，但 `0x7E713B` 不检查失败返回；初始节点由 `0x3B5D210 → 0x3B5CC60 → 0x3BE18C0` 发布到 `[manager+8]`。因此 R280 的 task-entered 与双 lookup NULL 完全兼容于 core failure、零候选或 publisher 全失败。
- R280 crash-time manager 最终内存并非空：`manager+0xFA=1`，head 指向 later mod mount `.../profile/mod-content/zhongguo_361`。这只证明 task3 之后曾有节点建立，不能倒推出两个 settings lookup 当时的 head；D2 必须保存严格时序快照，不能只读最终 manager。
- 启动了 D2 最小 observer 实现：将记录 core init 结果、publisher entry/return、目标 settings lookup 时刻的 manager/head 快照；default-OFF、read-only、all-or-none，不修改 VFS 分支。
- D2 已完成：冻结 `0x33F8` MSVC x64 state、64 个 `0xC8` publisher slots、core-init / publisher / 双 settings lookup / manager-before-after / 四处 hook 与 fault 状态；bridge、minidump extractor、synthetic test 和 source contract 已接线。Release `/Z7 + /DEBUG:FULL + /MAP` 全构建及 CTest `102/102` GREEN，default-OFF bridge/injector 也完成编译链接。
- CI/CD 失败根因已修复：R187–R250 与 Stage 11 共 244 行证据曾直接追加到 GENERATED ledger，却没有进入 `gen_361_mechanisms.py`。现已把内容完整纳入生成器，ledger 保持逐字节零 diff；原失败 `--check`、B1 current、scoreboard 及 `validate_local.py` 四项均 GREEN。
- T2 已将 active open_kaishek pin 从 `80fb3a4` 同步到远端主线 `33d690234d8217422978ee642055ab1b13e44c76`。外仓增量只含 CI/README/docs/CLI smoke，不改 Java、POM、profile、API/schema/architecture；跨仓 verifier `ok=true / GREEN_STATIC`，focused unittest `13/13` 与外仓 CLI smoke GREEN。
- Git 多身份写入/推送故障已解除。本批功能提交 `fb99dbd927f02d22ba1b501c25653446092c3956` 已推送，且 `HEAD == origin/master`；用户已有 runtime/artifact dirty 文件未纳入提交。
- R281 只执行了一次排他 CK3 启动。PID `128368` / generation `1` 不再命中 R280 `ck3+0x3BE33A9`：D2 core init `1/true`，publisher `34/34` 全成功，两个目标 settings lookup 非 NULL，两个 parser 均进入，`0x583` 成功插入；随后完整打印 303 个 database callback，却在 164 秒静默后仍无 Frontend/Load Save/In Game/native-ready，终态 `loader_terminal_missing_after_database_callbacks` RED。受管 cleanup GREEN、tree gone、最终 CK3/injector 槽为空。
- R281 report / loader gate / cleanup SHA-256 为 `144065DB...CFAFB` / `10CE0A86...716AC` / `C009FFF4...E4553`。本轮没有 map/mailbox、存档续接或玩法输入，不上调任何场景、阶段或宣传计数，也不把 observer 的时序扰动冒充修复因果。
- 远端 Official Runner CI 已恢复：`fb99dbd` 与报告提交 `a176cc1` 两次 master run 均为 success。
- R282 将 D2 与已有 completion/producer/consumer/wrapper-edge observers 合为同一 build，MSVC full CTest `102/102` GREEN。唯一 PID `23608` 完成 database `303/303` 并进入 Frontend；producer selected `1`，publish 后 wrapper 重入 `186870` 次，两条 consumer edge 持续运行，但 retained task identity match `0`。这证明 Frontend 可达并排除“wrapper 未再调度”，但不提升产品 readiness。
- R282 最终因 `continue-last-save` 在 300 秒内未进入 Load Save/In Game 而 `frontend_without_load_save` RED；cleanup GREEN、tree gone。report / loader gate / cleanup SHA-256 为 `AA3CCCB0...A69C9` / `12B44A2C...7EE4A` / `41CF1817...033B`。
- 首次 R283 child preflight 因 warm-up pipe 后缀含非十六进制字符而立即 RED；没有创建 state/artifact、没有启动 CK3。已给外层 Default-desktop relay 补上两类显式 pipe 的同格式校验，normal/`-O` 各 `7/7` GREEN；修复提交后再生成新 preflight，不复用旧的错误授权。
- R283b 采用新授权的 bridge-frontend-first：warm-up PID `199476` 在 `10.723s` 达到 authenticated responsive Frontend 并完整回收；final PID `145824` 显式载入 `autosave`，loader `303/303`、fatal `0`、gate `GREEN / load_save`，随后取得 paused/map/mailbox、玩家 `32904` 与日期 `53199480`。runner 先闭合 `debate_event.5110`、`ep3_decisions_event.2001`，再停在未登记的原版 `birth.3035`。
- 未知事件保护链真实写出 `xar_checkpoint.ck3`（`84,875,343` bytes，SHA-256 `6B5AE12D...20AD`）。保存动作会发布 fresh snapshot/revision，旧 helper 因 revision `405→406` / public `406→407` 误判 RED；修复后仍对玩家、日期、PID/generation、event instance 与 option count 做稳定性校验，只不再要求 post-save revision 与 pre-save 相等。原版 `birth.3034/.3035` 证明该中断只有一个 inert option；精确合同与 checkpoint 修复已在 `44883dd` 提交推送，相关回归 `31/31` GREEN。
- R283c 用同一 PID `145824`、generation `2`、restart count `0` 续接，按合同依次闭合 `birth.3035`、`birth.1010` 与产品 `zg361p2c.2`。这证明 late-save recovery、同 PID reconnect 与已知事件 drainage 可用，但产品入口随后产生 `4038` 条同源错误：B1 opener 先将 live vassal 放进 temporary candidate list，再由 `ordered_in_list` 返回仍通过 `is_alive/is_landed` 却拒绝变量写入的 `weak Character`。
- B1 生成源已改为 `every_vassal` 计数与 80 上限，再以相同资格条件直接 `ordered_vassal` 初始化 durable roster；temporary candidate list 已从 opener 移除。24 个生成文件已重建，生成器 `--check`、B1 `75/75` 与 `validate_local.py` 均 GREEN。该修复当前严格为 `static-ready`，R284 fresh projection/live 归零前不写成产品 GREEN。
- R283b report / loader gate / R283c resume report SHA-256 为 `FF18E811...BF72` / `3C221567...F380` / `2876E02B...B449`。PID `145824` 最终按 native-session queue 停止，外层 relay 完成回收，实时 `ck3=0 / injector=0`。本轮只恢复到产品入口并发现真实 blocker，不增加 strict scene、full-tree stage 或宣传计数。
- R284 从提交 `04ac889` 构建 fresh `phase2-full-release-r284-04ac889`：1,031 files，source/product tree SHA-256 `4960EDEE...AFD30`，projection manifest / release manifest SHA-256 `63E30B4F...365D5` / `30AD0639...5ED19`。source autosave 仍精确绑定 `C7E5CA11...219DA`；no-launch preflight READY，warm-up/final 串行执行。
- final PID `45100` 再次获得 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox；初始 client 依次闭合 `debate_event.5110`、`zg361p2c.2`、`zg361.40`、`death_management.1001`、`ep3_decisions_event.2001`。R283 的 `This scope doesn't support variables` 与 opener 文件签名均为 `0`，因此 `04ac889` 已升级为该旧族的 fresh-live 修复。
- R284 strict 停在原版 `chancellor_task.1002` instance `207` / date `53209248` / root `32904`；保存 scope 为 councillor `29346`、councillor_liege `32904`、target `34838`，仅 native option `0`。原版源码确认唯一不可避免效果是取消 target 对 root 的单向休战。独立合同及漂移/source 回归 `3/3`、manager baseline `13/13` GREEN，提交 `8e467fc` 已推送。
- generation `2` 同 PID reconnect 精确关闭该事件并验证 old instance 不再保留；其后自然推进到 `zg361b1.124`，runtime scan 捕获 `11` 条产品 RED：temporary `zg361_b1_agenda_candidates` 的 row 在排序时没有 `zg361_b1_agenda_sort_key`，并使后续 attention 读取缺失 `zg361_pending_grade`。这不是 R283 旧族复发。
- agenda 修复移除该 temporary candidate list：所有已 prune 的 durable roster row 先写 `-1000000000` sentinel，eligible row 覆盖真实 key并计数，`ordered_in_list` 直接排序 durable roster、以 eligible count 为 `max`。生成器 `--check`、B1 `75/75` 与 `validate_local.py` GREEN；fresh R285 前严格为 `static-ready`。PID 已由 native-session queue 回收，最终 CK3/injector 槽为空。
- R284 initial report / loader gate / resume1 / relay execution SHA-256 为 `FD684169...1FEC` / `AC5864A6...4041` / `2944B00D...7B85E` / `5B9EB8F1...1438`。本轮增加了可靠 recovery/drain evidence，但没有闭合新的 strict business scene ID 或 full-tree stage；T0 `50%`、`4/361`、`106/626`、`8/11`、总体 `67.5%` 不变。
- agenda 修复及报告已以 `3f42df2` 推送。R285 fresh product `phase2-full-release-r285-3f42df2` 为 1,031 files，tree / projection / release manifest / ZIP SHA-256 分别为 `A2956F69...4598D` / `8EB01E14...D373` / `40499BA0...C056` / `87F14C40...E96B`；逐文件 verify GREEN。no-launch preflight `09F1E164...E977` 为 READY，前后进程槽为空。
- R285 warm-up PID `76624` 在 11.003 秒抵达 authenticated Frontend 并完整回收；final PID `63200` 获得 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox。它精确关闭 `debate_event.5110` instance `202` / date `53203368`、`ep3_decisions_event.2001` instance `203` / `53203440` 与 `birth.3035` instance `204` / `53206920`。
- 同一 client 随后于 date `53211192` 合法再次出现 `debate_event.5110`，旧合同 `max_occurrences=1` 在第二次身份核验前 fail-closed；这是 harness RED，不是产品错误，故尚不能用本轮判断 agenda 11 条签名。合同现只扩到两次实机已见投递，第三次仍停止；focused 回归后再由 fresh R286 验证第二次 exact shape，并继续 B1。
- R285 report / loader gate / relay execution SHA-256 为 `97743C6D...36EC` / `6631296D...DB83` / `578377A7...7547`；final cleanup `tree_gone/cleanup_proven`，最终 CK3/injector 槽为空。本轮仍不增加 strict scene、full-tree stage 或宣传计数。
- debate occurrence 合同与报告已以 `bacde5a` 推送，focused `2/2` 双模式及 checkpoint runner `77/77` GREEN。R286 重新物化相同产品 tree `A2956F69...4598D`，projection / release manifest / ZIP 为 `55D514F5...8FA0` / `EBA2AE35...7B3A` / `87F14C40...E96B`；verify GREEN，no-launch preflight `41C80CA1...7762` READY。
- R286 final PID `199412` 再次 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox；exact 关闭 `debate_event.5110` `202/53203368`、`ep3_decisions_event.2001` `203/53203440` 与 `tgp_movement_events.0070` `204/53205336`，没有抵达第二次 debate，因此本轮不单独把 occurrence 修复升级为 live。
- 下一事件 `ep1_flavor.2040` 在 `53206056` 的 event/scope names/types/count/options/date/root 全部通过，仅冻结自 R183 的 `exotic_blade_holder=34092`、`owner=34092`、`foreign_merchant=65791` 三个偶然 ID 漂移。原版 source SHA `CC4CD67B...0924` 证明 caller 保存为 holder、artifact owner 等于 holder、target 为当前玩家、merchant 每次动态创建。合同已改为 holder=owner、两者不同于 target/merchant、merchant 与三者不同；仍保留 11-scope、3 authored/2 rendered `(1,2)` 与 refusal native `2`。
- R286 report / loader gate / entry / cleanup / relay execution SHA-256 为 `BC26E32B...43E1` / `733D09E3...E44A` / `A9132557...81E6` / `C697EBD1...93AD` / `5A9667BE...F412`。cleanup artifact 与实时检查均显示最终进程槽为空；尚无业务计数增量。
- R285/R286 的连续随机分叉证明 input-free known-contract RED 后清理健康 PID 会阻碍同一现场验证。recovery 已新增专用 known-contract exception/evidence，只对 occurrence-bound 或 identity-check 且 `selection_attempted=false` 的边界复用现有 map/bridge/PID 健康门保留 session；产品 runtime、选择前 frame 漂移和选择后 postcondition RED 不放宽。recovery normal/`-O` 各 `20/20`、checkpoint runner `77/77` 与 `py_compile` GREEN；R287 起可直接由原 resume client 续接此类首错。
- R287 从 `541c962` 物化 fresh `phase2-full-release-r287-541c962`，1,031-file product tree 仍为 `A2956F69...4598D`；projection / release manifest / ZIP 为 `8A54E4C5...926A` / `4FB228CB...1ABF` / `87F14C40...E96B`，verify 与 no-launch preflight `6644B565...2D03` GREEN。warm-up 完整回收，final PID `214748` 获得 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox。
- generation 1 关闭 debate `202/53203368` 与 ep3 decision `203/53203440`，随后在未登记的 `ep3_emperor_yearly.2170` `204/53205336` 前无输入 fail-closed并保留同一 PID。exact event context 为 root `32904`、唯一 `our_county: landed_title`、native options `(0,1)`；原版 SHA `5B59252E...DCD` 证明两路均写 50 年 county modifier，而 B 还写 25 年 `ep3_2170_success`、提高 governor efficiency 5。合同选择不写该角色 flag 的 A，focused normal/`-O` 各 `7/7`、checkpoint runner 各 `77/77`；提交 `35c36f5` 已推送。
- generation 2 原 PID reconnect 后，`.2170` 全部 identity/pre-selection checks 为真，authored/native `1/0` 后置条件 `event_instance_advanced` GREEN。随后还 exact 关闭 `zg361p2c.2` `205/53205408`、`zg361.40` `206/53205432`、`birth.3035` `207/53210280` 与第二次 debate `208/53211216`；这同时 live 验证 debate occurrence 2。R284 的 11 条 `agenda_sort_key` / `pending_grade` 旧签名均为 `0`，agenda 修复升级 fresh-live。
- `.122` 结算首次暴露 953 条产品错误，全部调用栈进入 `zg361_publish_scoreboard_effect`；首错为对 weak Character `27928` 读取 `zg361_kpi` 时 `no variables in scope`，95 条进一步落在 generated slots 35/36 文件。根因是同一 delayed chain 重复 publish 时复用了 `add_to_list` 临时候选名单，而权威 `zg361_b1_subjects` 已在本边界 prune。最小修复不再复制候选：B1 直接排序 durable roster，非 B1 直接 `ordered_vassal`，身份过滤、KPI 排序与 80 行上限保持不变。
- scoreboard 修复的 B1 normal/`-O` 各 `75/75`、scoreboard `36/36`、生成器 `--check`、`validate_local.py`、release test `9/9` 与 1,031-file reproducible build GREEN；manifest / ZIP 为 `9F17FB96...406` / `EB637106...60A7`。当前严格为 `static-ready`，fresh R288 将要求上述 953 条签名归零。
- R287 initial report / loader / resume report / resume entry / runtime diagnostics / relay SHA-256 为 `3FF264C2...9B3` / `6FC63E9B...C8E2` / `1407717C...20CD` / `919D28DE...D3DD` / `FBBC6BE6...E650` / `3A2E5BDF...D509`。旧产品进程已由 native-session queue 停止，实时 `ck3=0 / injector=0`；保留 profile/artifact 供取证。没有新增 strict business scene 或 full-tree stage，所有总进度计数不变。

## 早会承诺滚动状态

| # | 状态 | 当前证据 / 下一门槛 |
|---:|---|---|
| D1 exact lookup/publisher tree | **完成** | PathView ABI、singleton 字段、core/init/register 顺序、全部正常 NULL 分支与最小 seams 已闭合；专题文档更新在途 |
| D2 mount-state observer | **完成** | default-OFF/read-only/all-or-none；MSVC full build + CTest `102/102` GREEN，extractor/source contract 均已入库 |
| D3 单次新信息 CK3 recovery | **完成 / RED** | R287 loader、same-PID reconnect 与五项 drain GREEN；新 RED 为 `.122` scoreboard 的 953 条真实产品诊断 |
| D4 late-save lineage | 进行中 | exotic-arms、`.2170`、agenda 旧族已 live 关闭；scoreboard scratch-list 修复 static-ready，待 fresh R288 |
| D5 full-tree | pending | `8/11`，依赖 D4 |
| D6 G2/T2 | T1 `90%` / T2 `100% horizon` | G2 无诚实的额外离线代码包；active open_kaishek pin 已同步 `33d6902`，公共合同不变 |
| D7 报告/提交 | 进行中 / Git GREEN | `.2170` 合同 `35c36f5` 已推送；scoreboard 修复与 R287 报告在当前批提交 |
| D8 最终宣传视频 | **LOCKED** | T0-P1 未完成；registry `0/4`、footage `0/8`、MP4 `0/2` |

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| startup VFS root cause | 双 settings lookup NULL | R281 core init true、34/34 publishers success、settings lookup 非 NULL、`0x583` inserted | 单次成功不证明竞态根因已修复；不写成 complete |
| D2 dump observability | `static-ready` | `production-live primitive`，final heartbeat 已保留完整 state | 只认证本轮 VFS 生命周期，不认证 loader/save/gameplay |
| late-save recovery | Frontend only | explicit Load Save + paused/map/mailbox + same-PID reconnect | 产品入口仍被 B1 runtime diagnostics 阻断，不能写 full-tree GREEN |
| T0 strict CK3-live | `4/361` | `4/361` | startup diagnostics 不计业务场景 |
| full-tree stages | `8/11` | `8/11` | stage 9–11 pending |
| T0-P2 | locked | locked | T0-P1 hard gate 未满足 |

## 下一步

1. 提交并推送 scoreboard direct-roster 修复、回归、语法知识与 R287 报告。
2. 从提交后的 HEAD 构建 fresh R288 product projection；完成 no-launch preflight 与空 CK3 槽核验后串行复用同一 source save lineage。
3. R288 要求 `.122` scoreboard 的 953 条旧签名归零，并继续推进 stage 9 `.390` / full-tree；任何新事件仍先 exact fail-closed。

## 范围边界

- 不把 startup diagnostics 计入场景、阶段或宣传 readiness。
- 不盲拷 settings/map_data，不 patch VFS lookup，不提前制作宣传媒体。
- 宗教域保持 owner-deferred；仅保留圣战战争 OODA 与婚姻必要判定两项最小例外。

## R288 fresh scoreboard 归零与 debate 合同分支

- 当前已推送基线为 `c8656a0f8743659f77cbd69305a319469233fc48`。从该 HEAD 构建的 `phase2-full-release-r288-c8656a0` 含 1,031 files；product tree / projection manifest / release manifest / ZIP SHA-256 分别为 `7CA07BE0...B910` / `E8A34E17...D5E3` / `D192EF74...371D` / `EB637106...60A7`，逐文件 verify GREEN。
- 修正后的权威 Default-desktop no-launch preflight 为 READY，`child_process_started=false`、`ck3_launch_attempted=false`，SHA-256 `F8525705...4227`；执行前 CK3/injector 槽为空。此前一次缺少恢复参数的 relay child 只在 argparse 阶段退出，没有创建 CK3、state 或 live artifact，不计实机尝试。
- 唯一 final PID `38352` 获得 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox。fresh 产品在进入严格已知事件边界前没有重现 `.122` scoreboard 的 953 条 `zg361_kpi` / generated-slot 旧签名，因此 `c8656a0` scoreboard 修复升级为 fresh-live 归零；R284 agenda 旧签名同样保持为零。
- 时间线 exact 关闭产品 `zg361p2c.2`、原版 `ep3_decisions_event.2001` 与产品 `zg361.40` 后，在 `debate_event.5110` instance `205` / date `53203368` / generation `1` 无输入 fail-closed并保留健康 PID。唯一失败项是旧 regular variant 要求 `host` 必须等于 winner 或 loser；实机本轮为 `host=debate_opponent=debate_contender=29363`、`loser=27051`、`winner=30177`。
- CK3 1.19.0.6 原版 `debate_events.txt` 与 `tgp_debate_scripted_effects.txt` 证明：5110 delivery window 会从 `host` 重绑 opponent/contender，而 winner/loser 来自 activity 持久变量，故 host 可以与两项 outcome 都不同。合同仅把 regular variant 的 host 关系改为匹配 opponent/contender；胜负互异、7 个 scope 名称/类型/数量、root、date、两项 option 与 least-disruptive native option 1 均继续严格冻结。checkpoint runner normal/`-O` 各 `77/77`、`py_compile` GREEN。
- 本轮归零了一个真实产品旧错误族，但没有闭合新的 strict business scene ID 或 full-tree stage；T0 `50%`、`4/361`、`106/626`、`8/11`、T1 `90%`、T2 current horizon `100%`、总体 `67.5%` 与宣传 `0/4、0/8、0/2` 不变。下一步是提交/push 合同修复并以同 PID generation `2` 恢复。
- debate 合同修复已以 `a071cd2fc36fb2df6882aa889c6f609d9a3745ed` 推送；generation `2`、同 PID、restart `0` 下全部 identity/postcondition 为真并关闭 instance `205`。随后在 `tgp_movement_events.0080` instance `206` / date `53205336` / root `32904` 前无输入停止，三 scope 为 `root_scope: character=32904`、`my_movement: situation_participant_group`、`family_member: character=31137`，三项实显 option 映射 native `(1,2,3)`，authored 总数为 4。
- R288 checkpoint 已物化为 `83,777,761` bytes、SHA-256 `F36CED92...CDE9E`。原版 source SHA-256 `D9B172FC...10BC6` 证明 native 0 因非保守运动隐藏；native 1/2 会写 estate modifier 或 movement influence/power，native 3 直接终止且不写这两类状态。合同严格冻结 root/family 关系、scope 名称/类型/数量、hidden-option projection 与 native 3；manager focused normal/`-O` 各 `14/14`、checkpoint normal/`-O` 各 `77/77`、`py_compile` GREEN。resume report / entry / checkpoint artifact SHA-256 为 `12ED14E2...DBD1` / `E00778CC...1D94` / `26233701...F300`。
- `.0080` 合同已以 `5e5cd67108f3572618afefca720599b1d0dcb750` 推送；generation `3`、同 PID、restart `0` 下 native 3 / authored 4 的全部 identity 与 postcondition 为真。随后又 exact 关闭 `birth.3035` 与 `tgp_decision_events.0101`，再停在产品 `zg361.1`：仅 `saved_scope_names_exact` 失败，其他事件/root/option/typed scope 检查均通过。
- 该帧是已登记 Central-active outer stack 的严格超集，只新增仓库已有生产者明确写入的 CP-E 八项与 P3-AA 四项 tuple。annual-summary 合同新增这一个完整 58-name 集合，并为新增 tuple 补齐 `character/value` 类型校验；不接受任意 superset。checkpoint runner normal/`-O` 各 `77/77`、`py_compile` GREEN。generation 3 report / entry / runtime diagnostic SHA-256 为 `4287ABE0...23EB` / `FC9C7086...FCC3` / `029CE7BB...73E1`。

## R288 generation 4 产品 Route C 回执 RED 与静态修复

- annual-summary 合同提交 `e0502d22dde27d2a42025da59e3e429294b75dec` 已推送；generation `4`、同 PID `38352`、restart `0` 下 `zg361.1` 第八种 58-name outer-scope lineage 的全部 identity/type/postcondition 检查通过，并继续推进到 `zg361ch.950` 等既有链路。
- 首个真实产品 RED 出现在 `zg361pp.146` Route C：三条同源诊断依次为 `zg361_pp_m146_receipt_owner` 未设置、event-target link 返回 unset scope、右侧比较无效；调用栈精确落在 case-kernel full guard、`m146_consume_effect`、`m146_core_effect` 与 `zg361pp.146:option`。generation 4 report / entry / retained diagnostics SHA-256 为 `A32CA516...AB434` / `52C6E860...0C588` / `34BAD630...C6DCA`；relay execution 为 `B15EDF3C...992E`。
- 根因在 146–191 共用生成模板：Route C 的入口 guard 已验证 caller ticket 与当前 case 五元组相等，后续却把嵌套 scripted-effect 的作用域型 `$TICKET_*` 参数直接作为 `set_variable` 值；CK3 在该分支没有可靠物化 owner scope，回执 owner 因而未创建。修复令 Route C 从已验证的本地权威 case tuple 写入 owner/subject/cycle/case/state，不放宽 guard、不制造业务对象，也不改变 A/B 路径。
- 49 个生成产物已重建，其中 25 个 lifecycle shard 发生预期变更。核心运行时 normal/`-O` 各 `65/65`、恢复器各 `6/6`、经理中断各 `3/3`、生成器 `--check`、`py_compile`、release test `9/9` 与 1,031-file reproducible build 全部 GREEN；当前 manifest / ZIP SHA-256 为 `18231A80...6743` / `CBD576C2...E6CA`。该修复在 fresh R289 前严格标记为 `static-ready`。
- PID `38352` 已通过 native-session queue 停止，relay 完成，实时 CK3/injector 槽均为 `0`。没有新增 strict business scene ID 或 full-tree stage：T0 `50%`、`4/361`、`106/626`、`8/11`、T1 `90%`、T2 current horizon `100%`、总体 `67.5%` 与宣传 `0/4、0/8、0/2` 均不变；下一步为提交/push 后构建 fresh R289 并 live 验证 `.146` 三条签名归零。

## R289 fresh 产品与 EP1 语言争执中断

- Route C 最小修复及 R288 报告已以 `1afcc7af2f2e575c1c4571bbe4c5e31313af2af3` 推送，且 `HEAD == origin/master`。fresh `phase2-full-release-r289-1afcc7a` 为 1,031 files；product tree / projection manifest / release manifest / ZIP SHA-256 为 `0F314BCB...17FBC` / `1018A7B5...5435` / `B0F6CA33...66AAD` / `CBD576C2...0E6CA`，逐文件 verify GREEN。no-launch preflight `D3DA088B...31F9D` READY，前后 CK3/injector 槽为空。
- final PID `122132` 获得 loader `303/303`、fatal `0`、Load Save、paused/map/mailbox；先 exact 关闭 debate `202`、ep3 decision `203` 与 death-management `204`，随后在原版 `ep1_flavor.0021` instance `205` / date `53215344` 前无输入停止。checkpoint 为 `86,280,051` bytes / `0D1095DA...DCB1C`，同 PID generation `1` 健康保留。
- exact live context 为 root `32904`，三个 saved scopes：`rival_realm: landed_title`、`rival_monarch: character=36310`、`nitpicker: character=37929`；authored/rendered/native 均为三项 `(0,1,2)`。CK3 1.19.0.6 `ep1_flavor_events.txt` SHA-256 `CC4CD67B...10924` 证明 A 带随机外交 duel 与负面 court/culture，C 带同类负面效果及十年 modifier；B 无随机 duel、无 durable modifier/后续 event，只保留正面 opinion、friendship progress、cultural acceptance 与 trait stress，故合同选择 authored/native `2/1`。
- 合同冻结完整 scope 名称/类型/数量、两个动态角色各自不同于 root 且彼此不同、三项 option projection 与单次 occurrence。manager interrupt normal/`-O` 各 `16/16`、checkpoint runner normal/`-O` 各 `77/77`、`py_compile` GREEN。initial report / entry / checkpoint / loader gate SHA-256 为 `1519C1AF...B4F4` / `C33DDA3C...2251` / `EE42403E...A3B4C` / `0D04C213...DD0D6`。
- `.146` 五个旧签名在当前 final log 均为 `0`，但 `m146_option1_submission=null`，因此不把未触发路径冒充 live 修复；T0/T1/T2、full-tree 与宣传计数保持不变。下一步为提交/push 合同后同 PID generation `2` exact 关闭 `.0021` 并继续抵达 `.146`。

## R289 generation 2 疫病通知 option 分支

- EP1 合同本地提交曾为 `843a3fe`；push 时发现远端新增合法 `9de69ce`（独立“肃清曼荼罗”产品），Git 以 non-fast-forward 拒绝覆盖。已审阅增量并使用 autostash rebase 保留用户 runtime 脏文件，最终提交 `4224c9b74e8885f89cc715f08d211397a86d717a` 推送成功，`HEAD == origin/master`，未使用 force push。
- generation `2`、同 PID `122132`、restart `0` exact 关闭 `.0021` instance `205`，随后关闭 debate `206`、产品 `.2` `207` 与 `.40` `208`。88 次增量 runtime scan 的 blocking diagnostic 为 `0`；下一边界 `epidemic_events.1100` 仅 `authored_options_exact` 失败：R151 的无医师帧为 native `(0,2)`，R289 有医师帧为 `(0,1)`，其余 root/date/scope/type/count checks 均已通过。
- CK3 1.19.0.6 `epidemic_events.txt` SHA-256 `FEF2972B...7A1E` 证明 native 1/2 由 court physician 是否存在互斥：两者都会发起后续医师/health event；native 0 只可能施加源码已有的 -2 governor trait XP，仍是最小无后续链路路线。合同仅允许 `(0,1)` 与 `(0,2)` 两个完整二项 projection，并在两者都固定选 authored/native `1/0`；其他 option shape 继续 fail-closed。
- manager interrupt normal/`-O` 各 `16/16`、checkpoint runner normal/`-O` 各 `77/77` GREEN。generation 2 report / entry / diagnostics SHA-256 为 `FB489BBC...8C810` / `E7FB583A...150B` / `BDD68D50...E9DDB`。健康 PID 继续保留；计数不变，下一步是提交/push 后 generation `3` exact 关闭本分支并继续追踪 `.146`。

## R289 generation 3 疫病替罪羊中断

- `epidemic_events.1100` 合同已以 `37a23aaf6e51ad194912efb0533e7d39e52da580` 推送。generation `3` 在同 PID `122132`、restart `0` 下 exact 关闭该事件，随后关闭 `ep3_decisions_event.2001` 与 `adultery.0002`；104 次增量 runtime scan blocking diagnostic 为 `0`，两条 situation participant removal warning 记为非阻断原版引擎噪声。
- 下一无输入边界为 `epidemic_events.1060` instance `212` / date `53225568`。live context 冻结 root `32904`、`epidemic: epidemic`、`epidemic_scope: epidemic`、`story_scope: story`，snapshot 原生三项而当前完整可见投影为 native `(1,2)`。CK3 1.19.0.6 `epidemic_events.txt` SHA-256 `FEF2972B...7A1E` 证明 immediate 已在弹窗前创建 plague-witch-hunt story；当前隐藏的 native 0 需要极高虔诚，native 1 允许猖獗审判，native 2 明确减缓审判并且不再 `trigger_event`。合同选择 authored/native `3/2`，并冻结完整 scope 名称、类型、数量与单次 occurrence。
- manager interrupt normal/`-O` 各 `17/17`、checkpoint runner normal/`-O` 各 `77/77`、durable-checkpoint `3/3` 与 `py_compile` GREEN。generation 3 report / entry / diagnostics / durable checkpoint SHA-256 为 `D3C24AC6...01996` / `BACF1020...7D42` / `FF07C01B...5445` / `D6E9909D...008F`；新 checkpoint 为 87,832,911 bytes / `2169F03A...EF7C`。`.146` 仍未触发，业务场景、T0/T1/T2、full-tree 与宣传计数不变；健康 PID 保留待 generation `4`。

## R289 generation 4 民粹派系最后通牒

- 疫病替罪羊合同已以 `f5d3fdd` 推送。generation `4` exact 关闭 `.1060` 与 `tgp_movement_events.0080`，同 PID `122132` / restart `0`，63 次增量 runtime scan blocking diagnostic 与非阻断 warning 均为 `0`；随后在 `faction_demand.1001` instance `214` / date `53229048` 前无输入停止。
- exact frame 冻结 root/faction target `32904`、动态 peasant leader `16820110`、`faction`、`peasant_county`、`target_title` 共五个保存 scope，snapshot 四项而当前完整可见投影仅 native `(2,3)`。CK3 1.19.0.6 `faction_demands.txt` SHA-256 `B06241E6...BEA9` 证明 native 2 会立即执行 `successful_popular_revolt_outcome_effect`、割让目标并摧毁派系，native 3 则拒绝要求并 `faction_start_war`；转换路线在本帧隐藏。为避免选择瞬间破坏经理人的头衔/直属结构，合同固定选择保留当前结构的 authored/native `4/3`，战争交由现有 gameplay state surface 继续观测，不扩展宗教域。
- manager interrupt normal/`-O` 各 `18/18`、checkpoint runner normal/`-O` 各 `77/77` 与 `py_compile` GREEN。generation 4 report / entry / diagnostics / durable checkpoint SHA-256 为 `BBC5A78A...98E9` / `D834E9AB...07B6` / `7537209B...AFA8A` / `20E692FF...EC91`；新 checkpoint 为 88,162,779 bytes / `24025151...D86F0`。`.146` 尚未触发，业务与宣传计数不变；健康 PID 保留待 generation `5`。

## R289 generation 5 朝贡终止通知

- 民粹派系合同已以 `7a0469e` 推送。generation `5` 在同 PID/restart `0` 下 exact 选择拒绝，保留选择时 realm roster 并启动派系战争；16 次增量 runtime scan blocking diagnostic 与 warning 均为 `0`。下一边界为 `char_interaction.0370` instance `215` / date `53229720`。
- live frame 冻结 root/recipient `32904`、动态 actor `35923` 与三个确实 unavailable 的通用 interaction character slots，snapshot 两项而唯一可见投影为 native `(0)`。CK3 1.19.0.6 `character_interaction_events.txt` SHA-256 `D238E0A3...A150` 证明 tributary 关系已在 immediate 中结束；retaliation native 1 因 `can_retaliate_trigger` 为假而隐藏，同一条件也让 native 0 的可选 -50 legitimacy 分支不执行。合同因此 exact 选择 authored/native `1/0` 作为无进一步 gameplay mutation 的确认。
- manager interrupt normal/`-O` 各 `19/19`、checkpoint runner normal/`-O` 各 `77/77` 与 `py_compile` GREEN。generation 5 report / entry / diagnostics / durable checkpoint SHA-256 为 `F0209094...023A` / `54472F24...525A` / `C952CE15...49D1` / `B0B6E990...B623`；checkpoint 为 88,098,259 bytes / `D664E0FC...A491`。`.146` 尚未触发，业务与宣传计数不变；健康 PID 保留待 generation `6`。

## R289 generation 6/7 省份请愿 petitioner-self alias

- 朝贡通知合同已以 `7817320` 推送。generation `6` exact 关闭 `.0370`，60 次增量 runtime scan blocking diagnostic/warning 均为 `0`；随后 `tgp_decision_events.0101` 仅 `scope:province_change_recipient:matches_any` 失败，选择尚未尝试，健康 PID `122132` / restart `0` 由 known-contract recovery 保留。
- generation `7` 只读直连复核了 paused live frame：root/hegemon/petition recipient 为 `32904`，petitioner 与 `province_change_recipient` 同为 `30938`，`other_movement_member=28590`，七个完整 scope 与 native options `(0,1,2)` 均稳定。CK3 1.19.0.6 `tgp_decision_events.txt` / `10_dlc_tgp_scripted_effects.txt` SHA-256 为 `CEA02745...9C58` / `AEF36B88...7747`；`.0100` option A 明确 `province_change = scope:petitioner`，B-D 才写 house/disciple/other member，`.0101` immediate 再把所存值物化为 `province_change_recipient`。因此旧合同漏掉 petitioner 是过严合同，不是产品 identity RED。
- `_province_petition_scope_variant` 只把 `petitioner` 加入 source-authored alias 候选；未列名第三方仍 fail-closed，省份类型/member 组合、root、options、两次 occurrence 和固定拒绝路线均不放宽。checkpoint runner normal/`-O` 各 `77/77`、`py_compile` GREEN。generation 6 report / entry / diagnostics SHA-256 为 `18112CA6...C46C` / `FC7BFB93...F5AA` / `B96CEC3A...E0B2`；`.146` 尚未触发，业务与宣传计数不变。

## R289 generation 8 省份请愿 participant 组合

- petitioner-self alias 修复已以 `92ed519` 推送。generation `8` exact 关闭 instance `218` 的第一张 `.0101` 后，同事件在允许的第 2 次 occurrence 上再次出现；49 次增量 runtime scan blocking diagnostic/warning 均为 `0`，仅 `saved_scope_names_exact` / `saved_scope_count` 在未选择前失败并健康保留 PID/restart `122132/0`。
- 第二帧的完整 scope 集为 petitioner/actors movement/hegemon/petition recipient/province metropolitan/`disciple_movement_member`/`other_movement_member`/province recipient，共 8 项且没有 house member。原版 `.0100` 对 house、disciple、other 分别执行独立 ordered participant 查询，other 另有单独 fallback，因此 disciple+other/no-house 是源码明确组合；旧 `_PROVINCE_MEMBER_SHAPES` 只有 other、house+other 与三成员全量，漏掉本组合。
- 枚举仅新增 `("disciple_movement_member", "other_movement_member")`，其余完整名称/类型/alias/options/occurrence 门均保持。checkpoint runner normal/`-O` 各 `77/77`、`py_compile` GREEN。generation 8 report / entry / diagnostics SHA-256 为 `8D89FBDD...7AED` / `F6595272...A359` / `ABC79B27...A8D90`；`.146` 尚未触发，业务与宣传计数不变。

## R289 generation 9 疫病施舍提案

- disciple+other participant 枚举修复已以 `c143067` 推送。generation `9` 在同 PID `122132`、restart `0` 下 exact 关闭第 2 次 `.0101` instance `221` / date `53235864`；34 次增量 runtime scan blocking diagnostic 为 `0`，仅保留一条 situation participant removal 原版引擎非阻断 warning。随后在 `physician_epidemic_events.1040` instance `222` / date `53237808` 前无输入停止。
- live frame 冻结 root `32904`、动态 physician `49718` 与 `epidemic` / `epidemic_scope` 两个疫病 alias，共三个保存 scope；snapshot 与可见 native options 均为 `(0,1)`。CK3 1.19.0.6 `events/dlc/ce1/physician_epidemic_events.txt` SHA-256 `51ADEEA52F9A93406156ABAFA6608B0425003F63098F0CB033A5CB515C90F493` 证明 native 0 会给全部染疫伯爵领写十年 modifier，并执行疾病依赖的 5% 玩家感染判定；native 1 只损失少量虔诚并按特质增加压力，不写 modifier、不执行感染 RNG、也不触发后续事件。合同因此冻结并选择确定性终止的 authored/native `2/1`。
- manager interrupt normal/`-O` 各 `20/20`、checkpoint runner normal/`-O` 各 `77/77` 与 `py_compile` GREEN。generation 9 report / entry / diagnostics / durable checkpoint SHA-256 为 `953D3EDC40B6419DC05CD67AB3281D4A771ECAD454F004C822E2C13404F481EF` / `B6C58FF7349D0FFC6CA1125480CB7077389FCBA2F2F50170590C022E0173421D` / `4D15F63A7DD0C4D0EFB99327ACB92EE192200036CB9329803527961BC110E856` / `A9ECB8EF02C1024019417ADEAFF432AA6BFE9ECB189D2C40BC1DADEF50C4747D`；checkpoint 为 88,883,657 bytes / `619CB89AA60348896CDFC5E774556B5E774C941078B26841790CA43595320CF2`。`.146` 尚未触发，T0 `50%`、strict `4/361`、definitions `106/626`、stage `8/11`、T1 `90%`、T2 current horizon `100%`、总体 `67.5%` 与宣传 `0/4、0/8、0/2` 均不变；健康 PID 保留待 generation `10`。

## R289 generation 10 民粹领袖动态头衔形状

- 疫病施舍合同已以 `508ca13` 推送；generation `10` 在同 PID `122132`、restart `0` 下通过全部 identity/pre-selection/postcondition 门并选择 authored/native `2/1`，exact 关闭 instance `222`。11 次增量 runtime scan blocking diagnostic 与 warning 均为 `0`；时间线随后在第二次 `faction_demand.1001` instance `223` / date `53239560` 停止，未尝试选择并健康保留现场。
- 只读 generation `11` 直连确认该帧 root/faction target `32904`、动态 peasant leader `70343`、native options `(2,3)`，并且原五项 scope 全部稳定，只新增 `new_title: landed_title`。CK3 1.19.0.6 `common/scripted_effects/00_faction_effects.txt` SHA-256 `C8E0B3C57665F775973BC8559166CD1F6414471CA25017800777C3C6466DC5D3` 证明 demand 先调用 `setup_populist_leader_effect`；当新选 peasant leader 没有 primary title 时，该 effect 的 `create_dynamic_title` 会保存 `new_title`，已有头衔时则不产生它。因此合同只接受原五项和追加 typed `new_title` 的六项完整集合，任意其他名称、缺项或错误类型仍拒绝。
- manager interrupt normal/`-O` 各 `20/20`、checkpoint runner normal/`-O` 各 `77/77` 与 `py_compile` GREEN。generation 10 report / entry / diagnostics SHA-256 为 `FC1DB1A1458875CF60998C4EFDAE74A19F24719CEBB027152043087F075ED947` / `42CB837E2DA4753C6857F6AC7628EA5718BA0E5D412C607740312C343B6D07A1` / `BAE99D10B34A0650798730CD51381A3C4A7A97D193C394DBF0DBE6309BB1CB5C`。`.146` 尚未触发，业务、阶段、T1/T2 与宣传计数不变；PID 保留待下一次产品 reconnect。

## R289 generation 12/13 EP3 动态 vassal 关系

- 民粹动态头衔合同已以 `0735699` 推送。产品 reconnect generation `12` exact 关闭 `faction_demand.1001` instance `223`，随后依次关闭 `ep3_decisions_event.2001`、`tgp_interaction_event.0015` 与 `tgp_china_ministry.0100`；34 次增量 runtime scan blocking diagnostic 为 `0`，仅一条 dynastic-cycle participant removal 原版引擎非阻断 warning。时间线在 `ep3_emperor_yearly.2211` instance `227` / date `53244264` 因旧冻结 `vassal=27275` 与 live `28662` 不同而停在未选择边界。
- 只读 generation `13` 复核 root/liege `32904`、dynamic vassal `28662`、`potential_title: landed_title`、三项可见 native `(1,2,3)` 均稳定。CK3 1.19.0.6 `ep3_emperor_yearly_2.txt` SHA-256 `5B59252EF885BB605529B1AE76964A03BA447255DB77951CDBF2CB2AE267BDCD` 的 `.2210` immediate 明确保存 `liege -> liege`、`root -> vassal` 后向 liege 投递 `.2211`；因此 vassal 是每次动态提案人，不是 seed 常量。合同现冻结 liege 等于玩家、vassal 为非玩家唯一第三方，完整 names/types/count/options 与无 hook/modifier/appointment 后续的 terminal native 3 路线保持不变。
- EP3 focused normal/`-O` 各 `7/7`、checkpoint runner normal/`-O` 各 `77/77`、`py_compile` GREEN。generation 12 report / entry / diagnostics SHA-256 为 `0BCD0E43009DF4CAF759E879FF79082F961331D7D31BFC962A4464062CC89465` / `FB61F84F4D9C103291574B4674B3AFB0FC0C5285115DE1C0B6CF474BDBFA7504` / `80E3C760373EA25B00A8540C5B6F9A919B83E2E755BEFC36830055F054FB10C7`。`.146` 尚未触发；所有业务、阶段、T1/T2 与宣传计数不变，PID 保留待下一产品 reconnect。

## R289 generation 14/15 草药香囊报价

- EP3 动态 vassal 合同已以 `b4adcea` 推送。generation `14` 在同 PID `122132`、restart `0` 下 exact 关闭 `.2211` instance `227`，随后关闭 debate、第三次省份请愿与国库俸禄事件；45 次增量 runtime scan blocking diagnostic/warning 均为 `0`。时间线首次遇到未知 `epidemic_events.5009` instance `231` / date `53254032`，无输入停止并写出 90,401,985-byte checkpoint `7006EDA2E5CEA7802A42318D547412507BC7C22CBB569DF5DF8A915638021BCF`。
- 只读 generation `15` 冻结 11 个保存 scope：两个 epidemic alias、动态 merchant `63643`、flower flag、与 merchant 同一的 owner/creator、三项 value、location province 和新 artifact；snapshot 四项而完整可见 native 为 `(1,2,3)`。CK3 1.19.0.6 `epidemic_events.txt` SHA-256 `FEF2972BD4F778818CD3A414C337D036F5132C1598FEBAB0E2623E0252DB7A1E` 证明 native 0/1 会转移神器、付金币并写长期 modifier，native 2 仍写十年保护 modifier；native 3 什么也不买，只保留特质压力和所有路线共有的 merchant cleanup。合同选择 authored/native `4/3`，并精确绑定 merchant=owner=creator 与全部 scope/type/option 形状。
- manager interrupt normal/`-O` 各 `21/21`、checkpoint runner normal/`-O` 各 `77/77`、durable checkpoint `3/3` 与 `py_compile` GREEN。generation 14 report / entry / diagnostics / checkpoint artifact SHA-256 为 `4F67B17082BA5E96A078F536A9A15DAFFDB0367FF0D155288B9F3563E6602CD4` / `CD8BA0B68C51D667791098142C926DFAC0A10E7F20E09D54A81A612A8E8947B7` / `16486F2A87A40C2AE467E1335457A18083AB0BE32F437E3CF4A1E5C1F61FE837` / `0340B95ED2D310ED1AEA046FC88082B8D297FC736830FBC8CBBF531A1BF4449D`。`.146` 尚未触发；T0 `50%`、strict `4/361`、definitions `106/626`、stage `8/11`、T1 `90%`、T2 `100% horizon`、总体 `67.5%` 与宣传计数均不变，PID 保留待 generation `16`。

## R289 generation 16/17 语言学习结果响应

- 草药香囊合同已以 `5935fa8` 推送。generation `16` 在同 PID `122132`、restart `0` 下全部 exact 门通过并选择“不购买”的 authored/native `4/3`，关闭 instance `231`；16 次增量 runtime scan blocking diagnostic 为 `0`，只有一条同族原版 participant-removal warning。时间线随后在未知 `learn_language_outcome.1001` instance `232` / date `53257296` 无输入停止，并物化 90,842,473-byte checkpoint `EF8597C88A89417A418CF9C16A4970599618FF80E78B2261AC2E59046428B928`。
- generation `17` 只读帧冻结 root/target `32904`、dynamic scheme owner `62813`、scheme/artifact 与 `scheme_successful=true`，两项 native `(0,1)` 完整可见。CK3 1.19.0.6 `learn_language_outcome_events.txt` SHA-256 `6C3947B39A48C18E674207FF10D257C6853D126B6697AC6DFEC924FADBF39131` 证明两路都无后续 event：native 0 增加双向尊重及潜在友谊进度，native 1 增加负面评价、潜在敌对进度和玩家威望。合同选择不制造敌对关系的 authored/native `1/0`，并将 owner 精确限制为非玩家第三方。
- manager interrupt normal/`-O` 各 `22/22`、checkpoint runner normal/`-O` 各 `77/77`、durable checkpoint `3/3`、`py_compile` GREEN。generation 16 report / entry / diagnostics / checkpoint artifact SHA-256 为 `CA226B2E54A99ACAA14DD4E03A28F1A0D3421FD31084D693D3DC4CFB58E2729B` / `53C6E448882A06FF5733780811CE3166409FD6FBE8C519DD7EFFE56D7A7B4B1A` / `D0AC6BFD4DAE96C85272BA7A2E12C322684894AD97950B1233A4A91F837B8E7A` / `379AEAB71BD1FEED85CD44083E78860683F5823D8E303481D52285468B833915`。`.146` 尚未触发；业务/阶段/T1/T2/宣传计数不变，健康 PID 保留待 generation `18`。
## 计划外完成：肃清曼荼罗独立产品 1.0.0 正式发布

- `mod_remove_mandala` 已从提交 `9de69ce592cad20e85ba73798af1247c2b2ff532` / tag `remove-mandala-v1.0.0` 正式上传并公开为 Steam Workshop item [`3797711947`](https://steamcommunity.com/sharedfiles/filedetails/?id=3797711947)；双语长描述、640×640 主视觉与两张真实 CK3 截图均已在物品页可见。
- 实机验收 artifact `mrma_20260908_044123_6e4451cd` 为 non-debug GREEN：开局/第 1 日/月度的曼荼罗统治者全图归零、开局/第 2 日/年度的寺庙城寨全图归零、合法 AI 转制即时改回村社制；`report.json` SHA-256 `6ebd3d05...6547`。
- 上传后已重新生成无内嵌 `remote_file_id` 的 canonical staging；ID-bound manifest / deterministic ZIP SHA-256 为 `ec4733c...d1e` / `04cda5dc...0d9`。全新订阅缓存 `3797711947` 经 builder 严格核对为 15/15 文件 GREEN；Steam public details API 为 `result=1`、`visibility=0`。
- 正式测试计划、验收报告、截图账本与 initial-baseline changelog 均已落盘；这是独立用户请求，不改变自动游玩 T0/T1/T2、full-tree 或宣传计数。
