# 2026-09-03 日报

状态：**已收口（文字报告）**

正式收口时间：2026-09-04 00:00（Asia/Shanghai）

正式收口：2026-09-04 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-03.md`](../meetings/daily/2026-09-03.md)

本周计划会：[`../meetings/weekly/2026-W36.md`](../meetings/weekly/2026-W36.md)

起始分支/HEAD：`integration/progress-20260902` / `4fd92a75a8df36ebc277f539c5417ae0278b08bb`

## 今天完成了什么

- 00:04 完成 2026-09-02 日报正式收口，并召开本日早会；P0/P1/P2 未完成项按真实 readiness 顺延。
- owner 的永久 CK3 游戏内协议/通知授权已由 `4fd92a7` 写入 `AGENTS.md` 与 `docs/testing-workflow.md` 并推送；自动购买仍为唯一硬禁止项，系统 Toast 继续 close-only。
- `1b4ff63`（来源 `b80d650`）完成共享永久授权 handler 与 Phase2/G2 runner 接线：任何游戏内协议/consent 均可处理；普通通知只在
  tracked CK3 context 与安全动作 OCR 同时成立时处理；purchase/payment/order/checkout/store token 或控件在安全按钮查找前硬停。
  分类前 screenshot/SHA、raw/normalized OCR、授权版本、按钮、marker 前后状态保持可核验。
- 00:17 owner 进一步明确：禁止项只针对 Paradox/Steam/外部商店的真实付费，CK3 内金币、威望、虔诚等资源支出属于正常游戏动作；
  `1b4ff63` 的正文关键词前置硬停可能误拦只含 Close/OK 的信息通知。P0 live 已在启动前暂停，正在改为 action-aware commerce 判定。
- `dabbd25`（来源 `badfea7`）完成 action-aware commerce v3：购买控件，或 Confirm/Continue 与 Steam/Paradox Store/DLC/cart/
  checkout/order/payment/真实货币组合时硬停；纯购买信息通知只有 Close/OK 时可关闭；游戏内金币等资源支出退出 commerce handler，
  交给 gameplay policy；外部商业与游戏内资源信号冲突时 `CommerceActionAmbiguous`、零点击。`f850057` 同步权威文档。
- `d0fa156`（来源 `9436e3e`）闭合 G2 passive raw report → typed postprocess → SHA-bound acceptance report，normal/`-O`
  各 `24/24` GREEN，Official Runner CI `33654519809` SUCCESS；action/decision/expiry/war-bound readiness 均未提升。
- `6ed194e` 将“间歇性反思是否还有事情应该同时做”写入 `AGENTS.md` 和进度总入口；00:24 重新扫描后同时铺开 P0 分类器、
  native freeze、媒体环境预验、authoring 审阅、G2 静态研究、CI 与 open_kaishek 兼容复核，只有 CK3 启动继续排他。

## 早会承诺对照

| # | 早会承诺 | 结果 | 证据 / 未完成原因 | 是否顺延 |
|---:|---|---|---|---|
| D1 | 共享协议/通知处理器永久授权接线 | **完成** | `dabbd25` + `f850057`；五类 action-aware fixture、G2 `17/17`、Phase2 `15/15`、seed runner normal/`-O`、py_compile/diff-check GREEN | 否 |
| D2 | 新 exact-head producer-observer live | **完成（typed RED，目标观察面达成）** | exact `f850057` freeze/no-launch GREEN；唯一 live 取得 D2/D7 各 1838 次 producer 命中和完整 cleanup，但 loader 仍 RED，v1 last-only 不足以证明全分布 | 否；下一 distinct histogram v2 作为 D3 前置 |
| D3 | canonical seed、loaded-seed v2 与 8-span footage | **未完成** | startup recovery 与 Phase2 增量归因占用唯一 CK3 槽；真实 footage 仍为 0/8，seed/loaded-seed 未签核 | 顺延至 2026-09-04 |
| D4 | 最终宣传视频导出/发布 | **部分完成** | `c26cef8` 环境 GREEN，`790373a` 收窄过度宣称，宣传工具已更新至 `57c42fca`；但 0/8 素材使 TTS/render/export 均未启动，publish target 仍未指定 | 顺延；依赖 D3 |
| D5 | G2 当前迭代推进 | **部分完成后暂停** | 完成 passive evidence、strict generic war-bound intake、root/open_kaishek `15ab978f` 绑定与静态回归；GEN-034 新切片仍无 paused exact-build 双读，后按 owner 明确要求暂停 | 否；仅 owner 解除暂停后恢复 |
| D6 | open_kaishek 同步演进 | **完成** | `main == origin/main == 15ab978f879ed4562aacb74dacdaee702fbce54b`，Maven 127/127；Phase2 B1 仅做单文件 parser smoke，validator `UNKNOWN_OPCODE`/IR/runtime SKIPPED 保持诚实边界 | 否 |
| D7 | 报告、CI、commit/push 闭环 | **部分完成** | 日/周/专题与 handoff 已滚动记录；跨午夜正式生成器拆分与证据尚待统一 commit/push | 顺延至 2026-09-04 |

### 计划外完成

- 恢复有效 warm profile 启动入口，并把“CK3 完全打不开”纠正为历史冷/不完整 profile 的 harness 边界。
- 以 53-file case-kernel 与 55-file safe-core 两个完整进图 GREEN 建立 Phase2 最小可启动基线；保留 58-file 未拆 B1 的 1205.343 秒 pre-menu OCR RED。
- 新增 B1 77-block hash-bound 二分工具与 8 轮串行实机矩阵；七个 stub 子集及 77/77 正文 41/36 双文件诊断候选均 full-entry GREEN。
- 在午夜前完成正式 generator 双文件拆分：41+36 个定义、跨文件唯一、重组后 495,777 B / `CDB388…01B4` 与旧单文件逐字节一致；167 个定向测试 GREEN。
- 23:59 启动正式生成树 checkpoint；前两次 0.3 秒 attempt 因 probe 游戏路径绑定而在启动前 RED，均已保留并清理，显式绑定 exact EXE 的 r3 在 00:00 时仍运行，结果转入 2026-09-04 日报。

## 现在在做什么

- 截止 00:00，P0 正以 59-file 正式 B1 split 生成树执行唯一一轮 full-entry checkpoint；该轮跨日，结论归入 2026-09-04。
- seed、8-span footage、两版 TTS/render/export 均等待正式 B1 checkpoint；真实素材仍为 0/8。
- G2 按 owner 要求暂停；open_kaishek 保持 `15ab978f`，不因未知 opcode 自行扩张 profile。

## 为什么做这些

- 当天最短关键路径从协议 modal 转移到 Phase2 启动闭包。正式双文件拆分保留全部 B1 语义正文，同时已有实机证据表明它能跨过旧单文件候选的启动阻点；只有正式 checkpoint 通过，才值得进入 seed、paused native bridge 与素材采集。

## 证据与验证

- 开场 HEAD `4fd92a75a8df36ebc277f539c5417ae0278b08bb` 已推送至 `origin/integration/progress-20260902`。
- 授权实现 `1b4ff63` 已推送；G2 runner `16/16` normal + `-O`、Phase2 runner `15/15` normal + `-O`、seed runner
  normal + `-O`、py_compile 与 diff-check 均 GREEN，Official Runner CI
  [33653736041](https://github.com/XenoAmess/ck3_eternal_recurrence/actions/runs/33653736041) SUCCESS，未启动 CK3。
- Action-aware `dabbd25`：G2 `17/17`、Phase2 `15/15`、seed runner normal/`-O`、py_compile/diff-check GREEN；授权版本
  `2026-09-03-action-aware-commerce-v3`，classifier `action-aware-v1`。
- G2 `d0fa156`：normal/`-O` 各 `24/24` GREEN，Official Runner CI
  [33654519809](https://github.com/XenoAmess/ck3_eternal_recurrence/actions/runs/33654519809) SUCCESS。

## 00:43 P0 producer live、视频预验与 G2 并行增量

- P0 exact source `f8500576823deb798dc756ad8469aa87cc732c77` 的 fresh source ZIP
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-native-gate-20260903\producer-freeze-f850057\source-f850057-clean.zip`
  SHA `1257F300528A5482AF7F71404829F134AB432563F6492CF8AC9922640D11840A`，manifest SHA
  `89919DEC9D4852BBD3143B36BCF43CB10A6903CFDA84213B441A8AF670BBA193`，no-launch gate 全 GREEN。
- 唯一 live 于 00:32:32–00:37:55 运行 PID 67092，terminal 为
  `loader_terminal_missing_after_database_completion_publish`。producer observer installed=true/failure=0，D2/D7 各 1838，
  read_failure=0；最后样本 slot2 `0x817C20`、state 1→2。它证明 seam 活跃，但 v1 last-only 无法证明 1838 次完整分布，下一 distinct
  观察面是 bounded slot2 histogram、exact `0x88B480` counter 与 selected first/last identity，不重复本轮。
- Legal v3 为 GREEN/no_modal/0 click；preclassification screenshot SHA `2175453293A9975B456260AA369C63171ED08CCD7CAA4933C7624CC42477C6E6`。
  runner/legal/cleanup/terminal-index SHA 分别为 `06D8A458...55CBD`、`A501CC35...CB417`、`BADEC73B...64638`、
  `CFE6B15B...381459`。CK3/injector 已清空，process/source/runtime cleanup 全 GREEN；证据文档提交 `53c248c`。
- P0 视频环境提交 `c26cef8`：FFmpeg/ffprobe 8.1.1、H.264/AAC/MP4/libass、Pillow 12.3.0、edge-tts 7.2.8 Xiaoxiao
  catalogue、两字体、计划路径与约 571GB free 均 GREEN；receipt
  `Z:\p2r-25b6\final-promo-environment-preflight-20260903-v3.json` SHA
  `DD9D0AEB8D324A133075134CB1D3CE4014DEBC15C00F91B252CF924022F63F8B`。没有 TTS、字幕、encode、workdir 或候选视频。
- P0 authoring 提交 `790373a`（来源 `c51e613`）收窄 6 个超出 postcondition 的 cue，替换 finale 的 `COMPLETE` 过度标题，
  绑定简中 primary/英文 secondary、语义换行和 8/8 visible observations；所有 authoring/planner/intake normal/`-O` 测试 GREEN。
- G2 提交 `dbc21bb`（来源正确 SHA `390b1ee622b43d9381a2b78005ee7683adddec21`）先更新原生 AI evidence/Mermaid，再接入
  strict same-frame generic war-bound current；缺失/身份漂移保持 unavailable，source attribution/loss/action/automatic 均 false。
  normal/`-O` 各 243 项与 Official Runner CI GREEN。open_kaishek 对 `c26cef8`、`dbc21bb` 均 NO-CHANGE。
- 上一 P0 live 的 runner/legal/cleanup SHA 保留为 `D663CCF6...D6518` / `F8AB66E6...055E9` / `6B8D8275...16AF7`；
  它是法律门 harness RED，不是 producer capability 判定。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| CK3 游戏内协议/通知授权 | 必要协议窄 allowlist | **action-aware static-ready：任何协议/通知授权；只禁止真实付费** | `dabbd25` + `f850057`；尚待 distinct live |
| 天朝二期 producer observer | static-ready；live 被 legal gate 截停 | **不变** | 新 handler/候选/live 尚未完成 |
| 最终宣传片 | authoring-ready；footage/publish pending | **不变** | 0/8 real footage；无成片或发布 receipt |
| G2 | session-binding/index-7 live；passive observer static-ready | **不变** | expiry/war-bound/action 未闭合 |

## 失败、阻点与未知

- P0 旧 live 的协议分类证据不足与 `1b4ff63` 的过宽正文拦截均已静态修复；现在的未决项是 fresh exact-head freeze 与 distinct live，不把静态 GREEN 冒充 live。
- 外部视频发布平台、账号与 credential reference 仍未指定；这不阻止当前 native/footage/本地成片关键路径，但阻止最终 publish receipt。

## 文件与提交

- 文件：`AGENTS.md`、`docs/testing-workflow.md`、`docs/autonomous-agent-progress/daily/2026-09-02.md`、
  `docs/autonomous-agent-progress/meetings/daily/2026-09-03.md`、`docs/autonomous-agent-progress/daily/2026-09-03.md`、
  `docs/autonomous-agent-progress/weekly/2026-W36.md`
- commit/push：`4fd92a7`、`3f19390`、`1b4ff63`、`e844086`、G2 `d0fa156`、并发规则 `6ed194e`、action-aware
  `dabbd25` 与权威边界 `f850057` 均已推送；本次滚动报告提交进行中。

## 下一步

1. 合入并验证共享协议/通知处理器；重冻 exact HEAD。
2. P0 独占执行一个 distinct CK3 live；其余非 CK3 工作保持并行。

## 范围边界

- 宗教域继续 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。

## 01:22 滚动增量（截至 integration `d765063`）

本节取代上文 00:43 快照中的“当前状态/下一步”描述；既有证据仍保留，不将局部观察门通过写成 Loader、素材或成片完成。

### P0 天朝二期与两支最终宣传片

- 永久协议/通知授权及 action-aware commerce 分类已经完成并合入：`4fd92a7`、`1b4ff63`、`dabbd25`、`f850057`。CK3
  游戏内协议与安全通知动作可处理；只有 Paradox/Steam/外部商店真实付费仍零点击硬停，CK3 金币、威望、虔诚等游戏资源支出
  继续交给 gameplay policy。该项为实现与测试完成，不额外冒充本轮出现了协议弹窗或完成了购买相关 live。
- producer identity live 已证明 producer seam 活跃。随后 histogram v2 的唯一 exact-build live 使用 source
  `161962558e78d2351d136fb6d9fe15a35f12e983`，D2/D7 均为 `1838`；64-bin histogram 使用 25 bins，完整守恒、overflow/read
  failure 均为 0。精确 loader callback `0x88B480` 命中 **1** 次，first/last identity 一致，postprocessor 为
  `GREEN / selected-consistent-next-gate`、`next_gate_allowed=true`。这只闭合“真正 callback 在 producer transaction 中执行并发布
  state 2”；**Loader 总体仍以 `loader_terminal_missing_after_database_completion_publish` 结束 RED**，尚未证明 scheduler 消费该 task
  或发布 loader readiness。详见 [`../../ck3-native-ai/phase2-producer-slot2-histogram-live-2026-09-03.md`](../../ck3-native-ai/phase2-producer-slot2-histogram-live-2026-09-03.md)。
- histogram live 的 runner/postprocess/cleanup SHA-256 分别为
  `1A6D4FD930F9C4BC4B7AB14D02A769666933F3A833884329865CC2D958BB46F1`、
  `F50DF3775DCB34F393A4E0DAF1D4B9036EA3F19975B21C5CE37819C6A824E990`、
  `9C801DA2C7E9FC518BF42881D56B5656C458553E4150398C6094FD463ED9D6A9`；source ZIP/manifest SHA-256 为
  `29CDF95B17599BA4C0138AA32D8E71D58EEAD73C2F37797414E48832CA93D9A4` /
  `795B6F1517EB756CB0A08EB018D2F6CF0DAC797AAE88CE69BE9304DB7CE9FBD9`。清理与输入不变性均 GREEN。
- 用户要求的两种导演版本都已正式落盘，并被定义为两条必须分别剪辑、分别审阅的成片路线：人物版
  [`../../../mod_zhongguo_style/promo/phase2-character-director-treatment.md`](../../../mod_zhongguo_style/promo/phase2-character-director-treatment.md)
  （`08b2fc1`）与制度群像版
  [`../../../mod_zhongguo_style/promo/phase2-director-treatment.md`](../../../mod_zhongguo_style/promo/phase2-director-treatment.md)
  （`a9dfaa7`）。两版可以复用同一套合格二期真实素材与十章/八 span 基础，但不能只换标题交付同一条片。
- `82d6b77` 将 footage 合同修正为跨 span 可 clean restart：不再强制八段共用一个 PID/session；仍须绑定同一个 canonical
  seed/save lineage、完全相同的 source commit/tree、CK3 版本/EXE 和 product-only mod mount。每段自己的 pre → action → post
  必须在同一 session/PID/generation/revision 内连续，并有 checkpoint/save SHA、provider postcondition 和 GREEN cleanup；seed
  generation → loaded-seed proof 本身仍须连续同 session。旧一期与 fixture 素材禁止混入。该合同同时支撑两支 cut 复用一次合格采集。
- 独立宣传工具此前已经 fresh fetch 并验证 clean，`HEAD == origin/main ==
  57c42fca13ea459432c1caf76e069a1fbccf602c`（v0.2.1）；正式媒体生产开工前仍按门禁再 fetch/ff-only 并留下 fresh receipt。
- **两支视频当前都尚未生成。** canonical seed、loaded-seed proof、8/8 lineage-bound real spans、两套最终旁白/字幕与独立剪辑、
  源素材和两条最终 MP4 的完整 1× 审片、导出、外部发布与 receipt 全部仍 pending。现有两份 treatment 是导演方案，不是 live、
  TTS、候选片或成片。

### P1 G2

- exact candidate passive live 已完成，但为 `NO-GO / no_native_callsite_hit`：两个 hook 安装成功
  （`installed_mask=3`、`failure=0`），241 个 bounded heartbeat 样本中两个 site 都保持 pre/post=0；没有 `evaluated_days`，没有
  Context、mutation 或 time advance。raw/typed/acceptance SHA-256 分别为
  `1F319AE1F9D947D1BC766E304E8329CEE80E845B35833E45A2BB6F6E1DEB0AC9`、
  `64EC9BAA217DAF3A7797DBB1155B5896CC63A5A4B38EB407C0CFE2B6F0795693`、
  `2D2F2D4732951B76AF263772D9286AB6358A7EBD8E71693C0374011E83A4731E`（`5dbfce0`）。
- activation mapping（`39c4d5e`）解释了 no-hit：loaded index-7 是 `CAddTruceEffect<0>`，只有 site 0
  `0x2EDAF0F` 结构可达；site 1 属于 `<1>`。本轮是 paused heartbeat-only，没有 dispatch mutating slot 22，因而零命中符合执行路径；
  共享只读 preview `0x2E87140` 也不会调用 evaluator，不能产出 duration。静态 artifact SHA-256 为
  `23426315520BD45AB899F0A62FA3350C24F8CC04A95E2DAC6102248D747255AD`；public ABI/readiness 不变。
- `dbc21bb` 已合入 strict same-frame generic war-bound current intake；身份缺失/漂移继续 unavailable，source attribution、pre/loss、
  action 与 automatic policy 仍为 false。它提供独立静态可见值入口，不等于 G2 当前迭代或完整六域决策闭合。

### P2 open_kaishek 与准确状态清单

- 对上述 P0 native/media/docs 与 P1 runner/native/intake 增量的判定均为 **NO-CHANGE**：没有 adapter/API/profile/fixture/corpus
  变化需要同步实现。`open_kaishek` 保持 `0390b9a959fa1a59a968000ed49e827a03b8d4e4`，JAR SHA-256
  `421F49C93B21DBE5D96BFD81FFBFE422EB098B2170ECC498A415D4125490F2CB`；`certified=false` 不变。

| 工作包 | 当前进度 | 下一项 | 交付状态 |
|---|---|---|---|
| Phase2 native gate | producer identity + histogram v2 selected=1 GREEN；Loader 总体仍 RED | 关联 selected D7 task identity 与 scheduler consumer `0x3B9DEA7` 的动态指针 | 未完成 |
| 两支导演方案 | 人物版、制度群像版均已落盘；共享素材/双 cut 边界明确 | 用真实 footage 分别完成 narration、字幕、剪辑和独立审片 | 文档完成，媒体未生成 |
| Phase2 footage | clean-restart/同 lineage 合同已合入 | canonical seed → loaded-seed → 8/8 real spans | `0/8`，未完成 |
| 最终媒体 | 工具 main `57c42fca` 已验证，环境/authoring 静态门可用 | 开工前再 fresh fetch；完成两条 cut 的 TTS、合成、1× 审片、导出与发布 | 两条均未生成 |
| G2 | passive no-hit 已解释；generic war-bound intake 已合入 | 走 distinct 可触发/可观测 seam，不重复 heartbeat-only no-hit | 当前迭代未完成 |
| open_kaishek | NO-CHANGE，`certified=false` | 仅在 P0/P1 出现真实 adapter/API/corpus 变化时同步 | 伴随任务一致 |

## 01:46 滚动增量（截至 integration `cb99ec0`）

本节继续覆盖上一个滚动快照中的“下一项”栏；只记录已经合入或已有冻结 live artifact 的增量。

### 双片权威入口、独立配置与总交付闸门

- `48c378e` + `24d02af` 已把两份导演稿收敛到 canonical 文档入口
  [`../../phase2-promo/README.md`](../../phase2-promo/README.md)：人物版权威稿为
  [`../../phase2-promo/phase2-character-director-treatment.md`](../../phase2-promo/phase2-character-director-treatment.md)，制度群像版为
  [`../../phase2-promo/phase2-institution-director-treatment.md`](../../phase2-promo/phase2-institution-director-treatment.md)。mod 内旧路径只保留指针，
  不再维护可漂移的第二份正文。
- `2d4ebf8` + `908abfd` 完成双 cut 制作合同。人物版/制度版分别使用
  `phase2-promo-character-project.json` / `phase2-promo-institution-project.json` 与
  `phase2-authoring-character-claims.json` / `phase2-authoring-institution-claims.json` overlay；两者共享同一份通过 intake 的八段
  Phase2 原始素材哈希，但使用不同的 config、cue、run ID、workdir、旁白/字幕、候选媒体和审阅链。
- `09b7524` 统一最终文件名为 `zhongguo-361-phase2-character-led.mp4` 与
  `zhongguo-361-phase2-institution-led.mp4`；artifact ID 分别为 `zhongguo-361-phase2-character-led-video` 和
  `zhongguo-361-phase2-institution-led-video`。legacy 单片命名只为旧 receipt 兼容，不得用于新的双片交付。
- `4773bd3` 已实现双 completion gate：只有两支片分别通过候选检查、claims audit、各自两轮 1× 人审、导出与真实发布，且共享八段
  source span 的顺序/SHA 一致时，总状态才允许为 `COMPLETE`；任一缺失/RED、两个 cut 指向同一候选、改名副本或复用 sign-off 均保持
  `both_cuts_pending`。权威说明见
  [`../demos/zhongguo-phase2-dual-final-video-completion.md`](../demos/zhongguo-phase2-dual-final-video-completion.md)。
- **媒体产出仍为 0。** 两份 project 章节仍是 `planned`，cue/artifact 仍为空，cut overlay 仍
  `release_usable=false`。canonical seed、loaded-seed、8/8 实录、TTS、双语字幕、两套剪辑、两套人审、导出、发布均未发生；上述
  canonical docs/config/overlay/completion gate 是可执行合同，不是两支成片。

### span-session-v2 producer receipts

- `cb99ec0` 将 `span-session-v2` receipt 真正接进 managed producer，静态/fixture 路径 GREEN：每段 action 前后各取 paused native
  snapshot 并物化 start/end checkpoint，sidecar 记录 session ID、PID、generation、public/native revision、checkpoint bytes/SHA、
  provider postcondition 和 cleanup receipt。单段 pre → action → post 仍必须同 session；跨 span 可以 clean restart，但 canonical
  seed/save lineage、product tree、game/EXE 与 product-only mount 必须一致。
- v2 capture 明确只挂载 `zg361_acceptance.mod`，不安装 acceptance fixture；Phase1、fixture 与生成替代画面都不能满足 lineage。
  同一 immutable 8-span bundle 通过后可被两个独立 edit project 按哈希引用，不需要为第二支 cut 重录一遍游戏事实。该项仍是
  static/fixture GREEN；当前合格生产 receipt 数和 real span 数仍为 `0/8`。

### seed live 与 producer→consumer correlation live

- exact `82d6b77` canonical-seed live 已以 typed **RED** 收口：PID `9904` 在 `299.805s` 后仍停在 `database_init`，虽观测到
  `0x3B9CFD7` completion publish，但 `event_wait_authorized=false`、`native_readiness=null`，没有 seed candidate、capture 或
  loaded-seed proof。runner/loader/cleanup SHA-256 为
  `3A7D162EA755D72A531614C85AE54B629833217CC81C31AD05CA46543BF1B7E7`、
  `3843E3586EC6C33916FA9B07200602FEB45D4BFED3D83D76D1CDE6F20F63B596`、
  `4AD8B2778F63A953D3FD0BA970AB13CE8D7F388CE0D8E7284F8C2C023D8CD08A`；cleanup、输入与投影不变性均 GREEN。详见
  [`../../ck3-native-ai/phase2-seed-live-82d6b77-2026-09-03.md`](../../ck3-native-ai/phase2-seed-live-82d6b77-2026-09-03.md)。
- 随后的唯一 exact `09b7524` correlation live 在 PID `64304` 上取得 producer selected=`1`、task
  `0x18E97E09D50`、state 2、slot2 `0x88B480`；producer/consumer hook 均 installed、failure=0。但 consumer
  `0x3B9DEA7` 的 inline `raw_hit_count=0`，所以不是“match=0 已证明 task 不同”，而是本次 bounded window 根本没有跨过 consumer
  seam，typed decision 为 `RED / no-consumer-hook-hit`、`next_gate_allowed=false`。runner/postprocess/cleanup/terminal-index SHA-256 为
  `DC42BAA030E150F2D14A369C4485CA27D030D2932CDD9B2A04828602D2EE6B21`、
  `529F41DF9265966A6AF60EE722B95792FAEFB71C90E69C6842B79519D7A8703F`、
  `085E5E2915EBE412928F0ABB895C5D480BA702C579F59ECC70A6EEF50FC89728`、
  `0476483AAAA30BF22B7C5579DEAEE40F071DC3DD3E7F6E02ACBB9C3B1225A6AD`；cleanup GREEN、授权点击与真实付费点击均为 0。
- P0 下一条 distinct seam 已收窄为：保留 D7 selected task，在 completion wrapper `0x3B9E030` entry 以及通往
  `0x3B9DEA7` 的 branch/call edge 上关联，区分“wrapper 未再调度”和“wrapper 进入但走了另一分支”。不得重复 correlation-only
  timeout，也不得在此之前再跑同形 seed。

### G2、open_kaishek 与 CI

- `26784bc` 已把 `generic_war_bound_current` producer 接入真实 public query frame：原生 reader 双读完整 generation 的
  persistent/current/CArmy 图与各 `CArmyRegiment+0x38` 当前兵数，并由 driver 严格绑定 native/public revision、paused date、WarID、
  CB 与双方 identity。原生 focused `5/5`、Python normal/`-O` 各 `241` 项 GREEN。它最多只允许
  `generic_war_bound_current_ready=true`；source attribution、pre soldiers、proven loss、cleanup、action terms 与 automatic surrender
  仍 false/null。**对应 paused 双查询 live 尚未执行**，因此当前只到 static/fixture-ready。
- `open_kaishek` 对双片合同、span receipts、P0 native observer 与 G2 additive optional public child 均为 **NO-CHANGE**：未改变其
  Paradox 脚本 adapter/API/profile/fixture/corpus，保持 `0390b9a959fa1a59a968000ed49e827a03b8d4e4`、`certified=false`。
- integration 从 `c342f12` 到 `cb99ec0` 的 required Official Runner CI 均 GREEN；最新三项为双片 final names
  `33662200348`、G2 public producer `33662304455`、span receipts `33662554889`，结论均 `success`。这验证静态/fixture 集成，
  不提升 seed、footage、G2 live 或媒体状态。

| 工作包 | 当前状态 | 下一项 |
|---|---|---|
| Phase2 native/seed | seed RED；correlation selected=1，但 consumer raw hit=0 | wrapper-entry + branch/call edge distinct observer |
| 8-span producer | span-session-v2 receipt 生成/校验 static GREEN；real `0/8` | loader/seed 解锁后生成 canonical seed、loaded-seed 与 8 个 receipts |
| 人物版视频 | canonical docs/config/overlay/final name/completion role 已闭合；媒体 0 | 等共享素材后独立旁白、字幕、剪辑、审片、导出、发布 |
| 制度版视频 | canonical docs/config/overlay/final name/completion role 已闭合；媒体 0 | 等共享素材后独立旁白、字幕、剪辑、审片、导出、发布 |
| G2 | public `generic_war_bound_current` producer static/fixture GREEN | 排在 P0 CK3 槽后做一次 paused read-only 双查询 live |
| open_kaishek | NO-CHANGE，`certified=false` | 继续按真实 adapter/API/corpus 变化触发同步 |

## 03:50 wrapper-edge environment RED and G2 guarded startup advance

- The Phase2 wrapper/consumer-edge observer is statically frozen at local clean commit
  `3bf30280ffeb1dfbc73878ab9a56d6ac21ff5dd5` (tree `9d896f92096a444deece66920bbbf5887cdf39df`). Its only live started CK3 PID
  `60736` once and stopped about `0.13s` after PID binding as typed `harness/legal-capture RED / LegalConsentInspectionFailed` because PIL screen
  capture ran on `WinSta0\\CodexSandboxDesktop-*` while the active input desktop was `WinSta0\\Default`. The observer had not reached installation:
  `installed=false`, producer/wrapper/consumer/edge/identity counts were all unobserved zeroes, `native_readiness=null`, and no capability conclusion is
  drawn. Authorized, commerce, and real-money click counts were all zero. Cleanup is GREEN, source/runtime/external inputs are unchanged, and the
  closure SHA-256 is `C4B6DA2399717921AE60302448BE0DFC200BD2F2131A6C11C7DAC3962E2CBAD5`.
- The subsequent G2 guarded retry used the existing four-guard startup containment chain in the same bridge DLL. Its only live reached CK3 PID `7676`
  and exited after `17.695s`: it advanced past the previous `ck3+0x1DABD89` null-slot crash, then stopped at the historically known next main-thread
  access violation `ck3+0x369CB58` (`C0000005`, read address `0xD0`). Mailbox readiness was not reached, so the result remains `RED`, queries are
  `0/2`, `mcp_sequence=null`, mutation is empty, time did not advance, and no readiness flag is promoted. Cleanup and checkpoint/driver-state
  immutability are GREEN. The artifact directory is
  `artifacts/g2/2026-09-03/generic-war-bound-guarded-ready-20260903T034100`; live-report/dump/exception/typed-terminal SHA-256 values are
  `43351F9FF5FAF9C5BF3715E8F2B75FA98918DEC2567DC8B7CF1C4E19945AA80B`,
  `4D430A32157AFC08ADB27B8E2126564D950BF86D67383FDE0DED0943662734F6`,
  `ABA231DFCFF16B9587F378EAFAC84ADEAFA8637DBF3FDDA98D849D908DA56FDA`, and
  `DB2FCA905A566C5B32734CB04F27B6382027449E825CA2D31FF3BC8F0B798A68`. No duplicate live is authorized; the next work is the distinct,
  caller-local `0xAF4EE8` null-input guard candidate, kept default-off and static-only until it passes its own freeze gate.
- Both requested Phase2 edit ledgers were revalidated independently after the live attempts. Character-led and institution-led each remain
  `VALIDATION: GREEN` with ten draft chapters and all eight gameplay cues bound to future real clean spans, while both explicitly report
  `media generated: no`. The documentation/configuration deliverable is present; canonical seed, real `8/8` footage, both edits, both full-length
  reviews, exports and publication remain pending.

### Phase2 native-observer-only follow-up

- A separate explicit `--native-observer-only` path removed the desktop dependency without weakening the UI policy: after bridge binding it used only
  cached capability heartbeats and sent zero screenshot/OCR/UI/keyboard/gameplay/legal/payment operations. The exact clean freeze was commit
  `7fe6f036dd453b7dc7e0b8aad698ead8a38cdc81` (tree `fd2153b4792fa45a0de4c8b3f0c8cca26e4e5605`), with source ZIP SHA-256
  `215CE9BDEDC67AE1106B4999D5AF47EF09E592A2D14DB6B435C82F28BC869ED6` and no-launch preflight SHA-256
  `2BE3665B1129748EC863763B25B8D2AF126D76415DF59249720D5F08375A700A`.
- Its only live bound PID `37300`. Heartbeat sequence 1 was pre-install; sequences 2–5 showed the wrapper/consumer observer installed with
  `failure_flags=0`, while producer selection and all wrapper/consumer counters remained zero. CK3 then exited after about `13.5s` at the historical
  particle2 access violation `ck3+0x1DABD89`, before producer selection. The typed conclusion is therefore
  `startup-crash-before-producer-selection / capability indeterminate`, not `producer_not_observed`. Physical cleanup is GREEN although the stricter
  session cleanup contract is RED because the process crashed while final capabilities were still connected. Closure/runner/heartbeat/cleanup SHAs are
  `9A89777E50E7C8B9C301DB82C368C7E862C280311EBBD897947E57A238AFFE03`, `0DD38240...B509`, `A8400B53...DD07`, and
  `C7821036...8228`. The next P0 base combines this observer with the already proven startup guard chain; no same-shape retry is permitted.

### G2 fifth startup guard static freeze

- The distinct crash evidence at caller `0xAF4EE8` now has a minimal, default-off caller-local guard: only null `RDI` suppresses the call to
  `0x369CB30` and continues at `0xAF4EED`; non-null inputs replay the original arguments and call unchanged. Enabling this fifth guard requires the
  existing four-guard containment chain. `ck3_11906`, public query semantics, and readiness remain unchanged.
- Release build and the native executable test are GREEN; Python normal/optimized are `6/6`, with py_compile and diff-check GREEN. DLL/injector SHA-256
  values are `A1EFF67B1D985821B812A23DF7F507AB78E290B8B33E6522EAE2136CBEE6136C` and
  `CF848D94C145C6D5385B52640EE8B7AB1A1B405BA50199D2183C581799352E34`. The guard freeze and official no-launch preflight are
  `READY_TO_FREEZE` / `ready-to-run`, with SHA-256 `A035742A67D7EDD1FA440B7B6C3BE8C1D83BE6D5B000EB46923619B417A189D7` and
  `FCEDA3197850E978790DD49C3C15E4BA0B9D8B1C941D51427C3E8B4571733A70`. No CK3 was launched for this static package; P0 now consumes the same
  guard in its observer-combined candidate before G2 gets another live slot.
- `open_kaishek` remains an exact **NO-CHANGE** verdict at commit `0390b9a959fa1a59a968000ed49e827a03b8d4e4`, JAR SHA-256
  `421F49C93B21DBE5D96BFD81FFBFE422EB098B2170ECC498A415D4125490F2CB`, and `certified=false`: this increment changes only private native startup
  instrumentation and no adapter/API/profile/fixture or Paradox-script corpus.

### Phase2 five-guard observer live

- P0 combined all five default-off startup guards with the wrapper/consumer observer in clean local commit
  `eb85ffd1b800d14da34e66b5e8f25a2bce9ec67a` (tree `1070c65d1f1ff6366a2c9794b2112e43376c86ff`). Seven focused native tests,
  Phase2 Python normal/optimized `7/7`, and the fifth-guard contract normal/optimized `4/4` were GREEN before the one live.
- PID `31720` bound successfully. All guards installed with zero failure flags: particle2 producer suppressed `8` (mask `255`, last index `7`),
  particle2 consumer `1` (missing mask `255`), DX11 `1`, localize-current-root native misses `5`, and the fifth widget guard `0`. Before producer
  selection, the Phase2 observer recorded `190` wrapper/consumer entries, all from exact edge `0x3B9E175`; `0x3B9E10B` and other callers remained zero.
  Selected-after-publish, wrapper-post-publish, and identity-match counters were consequently zero.
- CK3 exited after `13.557s` with `C0000005`, but neither the CK3 crash reporter nor WER emitted an exception bundle, so the new RVA is unknown. The
  correct typed result is `startup-crash-before-producer-selection-with-preselection-consumer-edge / capability indeterminate`; the runner's narrower
  provisional `producer_selected_task_not_observed` string is not treated as a capability verdict. Zero desktop/legal/gameplay/payment input occurred.
  Physical cleanup and source/runtime invariants are GREEN; the stricter session cleanup contract is RED due to the crash. Closure, runner, cleanup and
  heartbeat SHA-256 values are `FCD6FEB3E3AE78C79281E73DC0052E19882FAA43C4C6DDFEBFE4FB2F47F5E3D9`, `AFD74B2D...E6A53`,
  `7813A733...955FC`, and `FFF3CC4E...CA250`. P0 now adds a read-only crash-address capture; no sixth guard is guessed without an exact RVA.

### G2 fifth-guard actual live

- After one launch-before harness RED caused by a Python environment without `win32api` (CK3 launch count remained zero), the frozen fifth-guard
  candidate ran its one actual live with the validated tools venv. PID `6796` exited after `17.390s`: the fifth guard advanced past the former
  `ck3+0x369CB58` crash, then produced a new captured `C0000005` at `ck3+0x3B67345`, reading `[RCX+8]` with `RCX=0`. Queries remained `0/2`,
  `mcp_sequence=null`, mutation empty, and time unchanged. Cleanup and checkpoint/driver-state invariants are GREEN.
- Minidump stack/register analysis binds the null to caller `0x390A9ED`: the caller executes `mov rcx,rbx`, and the exception context has `RBX=0`.
  The callee has many callers, so the distinct minimal seam is caller-local: skip only this call on null `RBX` and continue at `0x390A9F2`; a global
  callee patch is rejected. The live-report/minidump/exception/typed-terminal SHA-256 values are
  `92E182644F4430896DA918DDECC6AE70E7D53FC30B6A4C367EC24F2CF59ED3AD`,
  `596171DF429F18A92FA3C3FDB915BFB2501FFFF0B8403C555B73B9D5C78CFF27`,
  `AA52848DDFEE607FF55B54DB23E39BCA4BAF6C3AB4E6A609C7A6EB7CD7EE40A8`, and
  `2822CE8F92C0A95DA28116FEA17081E30DC7695F1A175AFB20251B23F62AAE6F`. The sixth caller-local guard proceeds static-only while P0 owns the next
  CK3 slot.

### Phase2 exact second-chance crash capture

- The first read-only debugger attempt was a typed harness scheduling RED: the coordinator waited only `45s`, while the runner published its CK3 PID
  after about `68s`. The capture therefore timed out before attachment even though the runner later completed its own guarded startup attempt. No
  capability conclusion is drawn. The failed-attempt closure and runner SHA-256 values are
  `0A3CF7607EA96E9D682F448A54587AC8D37168F61ECF02524CE538CC74CBF008` and
  `86A8B1E1...F600`.
- The harness was corrected to wait on the exact start-artifact `binding.bridge_pid` handshake, with a `300s` minimum. Its one live attached a
  read-only `DebugActiveProcess` observer to CK3 PID `56332` and captured the second-chance `C0000005` at exact RVA `0x3B67345` (VA
  `0x7FF6915D7345`, read address `0x8`, thread `19816`). `RCX=RBX=RSI=R14=R15=0`; the persisted 64-qword stack includes RVAs
  `0x390A9F2`, `0x390A283`, `0x3B9CFD2`, `0x3B9E72F`, `0x3B9D050`, and `0x3B9D0AA`.
- The Phase2 observer simultaneously remained installed with zero failure flags and again recorded `190` pre-selection consumer entries, all through
  exact edge `0x3B9E175`; producer selection and all post-publish identity counters remained zero. All five startup guards reported the same
  `8/1/1/5/0` suppressions. There was zero UI, legal, gameplay, or commerce input. Physical cleanup is GREEN; the strict session contract is RED
  because CK3 raised the exception. Exception/runner/cleanup/heartbeat/closure SHA-256 values are
  `592D20086AF2A8C96E164BEFD02C56EA64CE96D8010A2BA3EB7EE227C6665BAF`,
  `72BB2E8C648D11587C4E1437C2A4D663670FFF34F65C4CEE3B51A7789D175B5C`,
  `94418F55C4A174BECFCCFEE233B32A88C0701BF3BD8816BF5825140ABDC1DE57`,
  `CEC29D348959C6BC898EC82A73A0DD844DFF601D127F02AC4716A8A22A7C179D`, and
  `29E1E96230235F97754AB97AB57E04EB21040268C247006F5550E013490634E6`.
- This exact fault is identical to G2's fifth-guard result. G2's distinct sixth guard has already advanced beyond it, so P0 will not repeat the same
  sixth-guard live. Shared-engine evidence is reused only to diagnose startup; it does not promote Phase2 seed or footage readiness.

### G2 sixth-guard live exposes an initialization poison

- The sixth default-off caller-local guard covers only call `0x390A9ED -> 0x3B67330`: null `RBX` skips that call and continues at `0x390A9F2`,
  while non-null execution remains original. Its static artifact is
  `artifacts/g2/2026-09-03/generic-war-bound-rbx-guarded-static-ready-20260903T092500`; DLL, injector, manifest, preflight and artifact-index
  SHA-256 values are `A4B81851B74095A6A0AB3C09B1C751FB40B22775C79F9D9E74DB481EFA9946F1`,
  `D715D4A859024037022262FB0F63098E4107FF75318F6DF51A846CBE004F242D`,
  `E3DA9CB61322730F00E9C772779D94F5060032723FEE4E0156756EA01D3BA0A5`,
  `15F520BF506510E753D0558112CE2D4CC6E4E6BD6496DDC4332153F220F70373`, and
  `BE77044F8E11CF88B0F7EB0FD3C711AE5DEF63B6C2D4596A0E69A81C346380F3`.
- The one actual live (PID `46068`, `14.392s`) advanced past `0x3B67345`, then raised a new `C0000005` at RVA `0x3BE33A9` in callee
  `0x3BE3360`, reached by the direct call at `0x3B55D86`. Both `RCX` and `RBX` were `0x206E65704F534656`, the little-endian ASCII tag
  `VFSOpen `, so the input is a non-object poison/tag rather than another null. Queries remained `0/2`, with no mutation or time advance and GREEN
  physical cleanup. Minidump and exception SHA-256 values are `F08D7EC24540D70005CB2D222D8EC4F8A4B65F2A5EACA8A47544E0CCC4DC8908` and
  `9DB3D6B9C5CC4D4707FFD0CCDDD38D275BE674F4950BE691C2C0293F01D6732E`.
- The completed live-report, typed terminal, final index, live-entry verification, enriched diagnostic, and consumed-manifest SHA-256 values are
  `EB5ECAB0CC503EB1DE8927A8B6B11FD0F728676564F860C9A7A0172A9984D08C`,
  `81196AFB476B42FE7FE6B4F65779C1E772FC0C89793B90B4E96950F84A0E02CC`,
  `183C33D7263CD99C3488D74B3127CF1F489FA7DBDC745A349C859637882A602E`,
  `2B6D9FB53B5759A4B7D51EC289BFCEF71E9F54D993BEDA9E723E9486BFDFEE66`,
  `9AE56F6BCC5860DD5B61DB1019384D3969CC64BCD0E30A7F78BA7803FDEC3202`, and
  `F8898D8EAC0EA11DB02B8EBE39A6D35A6FED480F4EDCFA2B434FED3699EF3A6`.
- Static data-flow now binds the operation object at `0x2ADD35C06A0`: its `+0x38` variant has tag `1` and payload
  `VFSOpen Error:  not found`, while poll state `+0x0C` is still `0`. The pending branch at `0x3B55D74` loads the first eight payload bytes as an
  object pointer and calls `0x3BE3360`, producing the fault. The upstream loader has an empty base path and default path `/default.map`; the physical
  `game/map_data/default.map` exists and the working directory is normal, excluding a simple missing-install-file explanation. No existing guard has
  a proven direct data-flow edge to this VFS object; the honest current classification is a cold-map-startup asynchronous VFS error-variant/state race.
- Because this pattern can indicate skipped required initialization, a seventh guard is not authorized or guessed. P0 and G2 now trace the
  `VFSOpen ` value upstream and compare the historical no-guard producer-success runs before proposing any new native change.

### open_kaishek compatibility recheck

- A fresh read-only audit remains **NO-CHANGE**. `Z:\workspace\open_kaishek` is clean at
  `0390b9a959fa1a59a968000ed49e827a03b8d4e4`, matching `main` and `origin/main`; its JAR remains
  `421F49C93B21DBE5D96BFD81FFBFE422EB098B2170ECC498A415D4125490F2CB`.
- Current changes add only private exact-build startup observers/guards and a heartbeat-only Phase2 runner mode. Product Paradox `.txt/.gui` corpus,
  open_kaishek preflight adapter/CLI schemas, profiles, and parser/runtime fixtures have zero relevant diff. The minimal synchronization list is empty;
  `certified=false` is preserved until a real compatibility surface changes.

### Phase2 downstream execution audit

- A shortest-path audit found two product-live gaps that remain after startup recovery. The first scoreboard span still advertises only fail-closed
  transport because `bridge.cpp` keeps its production capability false. The workforce span's m360 gameplay preflight is deliberately RED until it can
  prove both an exact revision-bound owner/subject CharacterID player transition and three independent A/B/C restores from one byte-identical pre-#360
  checkpoint. Separate P0 work packages now close these two capabilities without lowering the live-evidence gate.
- A dual-cut command audit found three media-pipeline execution gaps: per-cut preflight commands omitted the selected `--project-config` and would bind
  the legacy single-cut config; there was no auditable Phase2 Xiaoxiao cache-prime command even though the builder consumes narration offline; and the
  institution treatment's foreground reordering/reprises could not be expressed by the linear canonical chapter order. Implementation is underway to
  bind a distinct receipt per cut, add an explicit post-preflight cache-prime stage, and separate source capture order from per-cut edit order. No TTS,
  placeholder footage, or media was generated during this audit.

### Phase2 historical/current startup comparison

- Historical producer/histogram/seed runs did not reach native readiness: they merely survived for roughly five minutes in `database_init`, executed
  the D2/D7 producer path, and still ended with two database callbacks, zero post-init and a loader timeout. The strongest histogram run recorded
  D2=D7=`1838` and selected exact callback `0x88B480` once. EXE, save, rules, product/fixture trees, enabled mods, launch implementation, arguments and
  working directory match the current attempts.
- The strongest proven boundary is execution environment: historical long runs captured the normal `xenoa / WinSta0\\Default` desktop, while current
  runs execute under `CodexSandboxDesktop-*`; even the current exact no-startup-guard freeze crashed at `0x1DABD89` after about `13.5s`. Therefore the
  first regression predates the five/six-guard cascade and cannot be attributed to those guards. Later VFS symptoms remain consequences observed only
  after containment, not a normal-startup equivalent.
- The minimum useful A/B is the current no-guard freeze `7fe6f03` run unchanged under the normal interactive desktop. If it clears `20s` and reaches
  `database_init`/producer selection, the desktop/token boundary is closed; if the same RVA reproduces, only then compare producer-only versus
  wrapper-composed observers on that same desktop. The repository contains a default-desktop handoff launcher, but this sandbox session will not use it
  to cross desktop isolation. Static P0 and media-pipeline work continue in parallel.

### Phase2 shortest executable capture path

- The honest minimum is two serial CK3 sessions: first generate a new canonical seed and freeze its save/identity contract; then launch a product-only
  capture owner that loads that exact seed, proves loaded-seed-v2 continuity, and records all eight spans. A successful capture is followed by a
  read-only footage intake. The current checked-in seed contract is legacy/blocked and cannot be substituted.
- Further static gaps are now split into parallel P0 packages: explicit source-event staging and between-span drain/close; manager action-to-`zg361mg.120`
  wait; incident X/Y/Z event progression; and business postcondition verifiers for promotion/compensation, projects/metrics identity, scoreboard
  calibration visibility, and endgame debt/default-change cycle. Revision movement or command ACK cannot satisfy these outcomes.
- After these code/desktop gates are actually closed, the seed-to-verified-`8/8` path is estimated at roughly `20–40` minutes (`5–10` minutes seed,
  `10–25` minutes capture, `1–3` minutes intake). Current remaining work is engineering measured in hours, so that downstream estimate is not presented
  as a current video-delivery ETA.

### Cold-map VFS observer static freeze

- G2 has frozen a default-off, strictly read-only three-point observer at constructor `0x3B55A40`, post-variant-move `0x3B55ADE`, and poll
  `0x3B55D50`. Installation is all-or-none; the hot path only copies diagnostics and does not modify branches, returns, objects or state. It is not a
  seventh guard and does not patch `0x3BE3360`.
- Release/native synthetic, Python normal/optimized `2/2`, py_compile, and a non-CK3 suspended-injection fixture are GREEN. A Cygwin `ctest` wrapper
  produced one harness BAD_COMMAND by prefixing a Windows absolute path twice; direct execution of the same four binaries was GREEN. No CK3 was
  started.
- Static artifact: `artifacts/g2/2026-09-03/cold-map-vfs-observer-static-ready-20260903T040314`. DLL, source-contract, no-launch-preflight,
  manifest, and artifact-index SHA-256 values are `8D0AE36B8B9B46A63E7FBDCC0E261FD4854E0C6A5016BE2710F21A5149EB8131`,
  `AFF56B88...65BF7`, `B3B3E798...95672`, `ADE0F0F0...CB17B`, and `196D9A72...B37D0`. The future A/B is explicitly outside this sandbox and
  compares normal Default desktop with sandbox using the same DLL/checkpoint/profile; current status is `static-ready-no-launch`, not live or fixed.

### Phase2 downstream static implementation increments

- Workforce is now runner-ready rather than permanently hard-coded RED: the exact owner/subject switch uses typed current-event `expected_revision`
  and later paused CharacterID/date checks; one pre-#360 checkpoint is independently restored for A/B/C, fixture activation, and final baseline. Static
  readiness remains distinct from live receipts. Focused helper/transition/isolation/full promo-capture tests are GREEN in normal and optimized mode.
- The capture driver now supports a strict product event choreography: source staging, action, clean hold, close, and drain for each canonical span;
  manager waits for exact `zg361mg.120`; incident proceeds `.190 -> .290 -> .390`; unknown events are RED and never auto-clicked. Natural timeline gaps
  remain fail-closed pending a lineage-bound per-span source-checkpoint provider.
- Business postcondition verifiers now reject ACK-only/revision-only or cross-identity joins. Existing production data can build scoreboard-calibration
  and endgame packets. Promotion/compensation and projects/metrics stay RED pending two dedicated read-only providers now under construction.
- The scoreboard production candidate is compiled behind a default-off macro and exposes diagnostic heartbeat only; public production advertisement
  remains false. Normal/optimized Python `13/13`, default/candidate MSVC fixtures, and candidate CMake/CTest are GREEN. The next static increment is a
  single-session two-surface open/switch/close/reopen batch collector; no live promotion has occurred.
- The dual-cut media chain now binds a distinct project config in each fresh media receipt, has an explicit reviewed-authoring promotion command and a
  post-preflight Xiaoxiao cache-prime command, and separates canonical capture order from per-cut edit order. Institution cut adds two audited 2-second
  picture-only reprises with independent silent audio, never duplicated narration. Planner steps are continuous `1..14`; normal/optimized planner,
  intake, preflight, builder, authoring, promotion, and cache-prime tests are GREEN. No TTS or candidate media was generated.
- Post-candidate placeholders are also removed: a per-cut materializer now emits the byte-bound probe, final storyboard, frame evidence bundle, pending
  review package, strict export policy, materialization receipt, and concrete audit/export command paths. It never creates a review decision, signs,
  exports, publishes, invokes TTS, or renders media. Materializer/planner/completion tests are GREEN in normal and optimized mode.
- A canonical per-span source-checkpoint registry and narrow restore service now cover source events that natural timeline cannot guarantee. Registry
  rows bind exact event/owner/player/date/save lineage and checkpoint bytes/SHA; restore must change PID, increment generation exactly once, return
  paused/map-ready with the same player/date/hash, and is followed by an independent exact-event query. Fixture, console, generic rebind, registry drift,
  and non-native backends remain RED. Status is `runner-ready/static-ready`; only a real seed/capture lineage can create the registry.

### G2 no-launch continuation

- The frozen `generic_war_bound_current` two-query runner remains hash-valid and needs no code change for normal-Desktop execution. A new no-launch
  verifier reports this sandbox desktop as ineligible and confirms zero process inventory; it does not launch CK3. Audit artifact index:
  `7C26050E887EF35ABFCBEC20BF94329ED6266EA5FB2820085D9F6396BF89EA9B`.
- The next minimal `evaluated_days` input seam is frozen at the exact `CAddTruce` slot-23 preview post-prolog `0x2E87155 -> 0x2E87165`, filtered only
  for vtables `0x4461CA8/0x4461D70`. Preview neither calls evaluator `0x3373000` nor reads `this+0x108`, so hits cannot be reported as evaluated days.
  A default-off read-only observer implementation is now in progress without shared bridge wiring or CK3 launch.

### Product receipt lineage and open_kaishek synchronization

- The Phase2 product now persists two previously absent cross-domain receipt chains through authoritative generators, not handwritten generated files.
  CP #026 issues a positive contribution receipt ID/revision and P3 #229 propagates it into the metrics result; PP #147 persists promotion receipt
  serial/revision and all 33 numbered compensation consumers publish a correlated posted receipt only after real consumption. Owner/subject/cycle/case
  identities and independent kernel-case boundaries remain explicit. Focused generator tests are GREEN in normal/optimized modes (`58+54` for CP/P3,
  `75` for promotion/compensation), as are the independent native provider fixtures/contracts. Both providers remain static-ready until shared wiring
  and paused live evidence.
- These real corpus/API changes triggered T2. Because canonical `Z:\workspace\open_kaishek` is read-only here, synchronization was completed in writable
  clone `Z:\ck3_mod_rewrite\_open-kaishek-sync-20260903` at commit
  `81109e46fce0c9b5efee49398f7411f772b1cfd9`. It adds two exact schema-only capabilities, fixtures, CLI/preflight wiring, and the minimum validator
  support for uppercase `NOT` plus trigger-side scalar `var:*` comparisons; effect-side use remains fail-closed and no runtime opcode was registered.
  Focused Maven/CLI checks are GREEN; parser/validator are GREEN, IR/runtime SKIPPED, native/runtime certification false.
- Canonical push could not authenticate and was not claimed. A reverse-apply-checked patch is frozen at
  `artifacts/open-kaishek-sync/2026-09-03/0001-Add-Zhongguo-business-postcondition-preflights.patch`, SHA-256
  `C6CE5DA5333A1551656A9E3B7D78F1CC609E30A192BC3587CF5638299820D80A`.

### Dual-cut production-chain aggregate verification

- The owner's latest direction makes both Phase2 treatments mandatory deliverables: the character-led cut and the institution-led cut remain separate
  project configs, narration/edit timelines, candidate bytes, review chains, and final filenames. They may share only the same hash-bound set of eight
  clean product-live source spans.
- The combined planner, completion gate, builder, reviewed-authoring promotion, Xiaoxiao cache-prime, and post-candidate materializer suite is GREEN in
  both normal and optimized Python modes (`41/41` each, `82/82` total). The first two invocations failed before test execution because the sandbox
  interpreter lacked the source-path/safe-directory process context; a command-scoped read-only Git/source configuration closed that harness issue.
- This result proves the two independent production paths are statically executable; it does not promote media readiness. Current real source footage
  remains `0/8`, neither MP4 exists, TTS/FFmpeg has not run, and the mandatory fresh `origin/main` update of the promo tool remains reserved for the
  actual media-production boundary after source intake.
- A second aggregate Phase2 product pass is GREEN in normal and optimized Python (`225/225` each, `450/450` total), covering CP/P3 and
  promotion/compensation generators, strict business postconditions, source-event choreography, canonical per-span checkpoint registry, and the narrow
  restore bridge. Scoreboard/workforce-focused coverage is also GREEN (`39/39` in each mode). These checks preserve the no-live boundary.
- The complete ZhongGuo product tree passes `validate_local.py`. Release-builder tests are `7/7` GREEN and the reproducibility check produced an
  identical 279-file projection with manifest SHA-256 `cee0af3d8039d3c31aa3de378c4fe91b15be9f659ca71f2c4d3182f1847f94b2`
  and ZIP SHA-256 `538ad2e67aa9bbfa9dc9c63b6b3ddddb97f53e8966a5e51e6070844134296d9e`. This validates packaging, not CK3 live behavior or either final video.
- The projects/metrics postcondition provider is now fully shared-wired in mailbox slot 24. Native adapter registry, main mailbox, projects reader,
  projects mailbox and bridge all exit `0` under the authoritative CK3 EXE path; Python provider/MCP/contract checks are GREEN (normal `12` provider/MCP
  cases; optimized provider checks `11`; `py_compile` GREEN). The earlier split-worktree CTest RED was only a relative-path lookup (`exe=0`) and was
  reproduced GREEN with `Z:\ck3_mod_rewrite\Crusader Kings III\binaries\ck3.exe`; no CK3 launch or live promotion occurred.
- G2 slot-23 truce-preview observer is now fully shared-wired as a private, default-off diagnostic. Both ON/OFF bridge builds pass in the MSVC/CMake
  matrix, observer ctest passes, Python integration is `205/205` in normal and optimized modes, and `py_compile`/`git diff --check` are GREEN.
  Artifact `artifacts/g2/2026-09-03/g2-truce-preview-entry-observer-integrated-static-ready-20260903T053339` has index SHA-256
  `02FCF992AEBD7A2063BD913CDE0407D70689DA56C5D150D5FC58EAB5A3C11126`; no CK3/injector ran and `evaluated_days` remains unresolved.
- A clean local freeze of the complete current source and documentation was created at
  `Z:\ck3_mod_rewrite\_local-freezes\phase2-dual-cut-g2-static-integrated-20260903-clean`, commit
  `165b47742fd05ff3713b8be4452711002328d57d`. The freeze excludes `.git` pointers, temporary build trees and process artifacts, and is clean after
  commit. The canonical worktree remains uncommitted because its `.git` is read-only; no false push claim is made.

### Seed-capture launch attempts after the clean freeze

- The corrected seed-runner no-launch preflight is GREEN (`preflight-ready`) with a run-unique named pipe. It confirms exact source/archive
  equivalence, unchanged external dependencies, product+fixture projection, and zero CK3 launch; the seed contract itself remains the honest
  `blocked_seed_generation_required` legacy contract.
- A direct execute attempt reached a bound CK3 PID but stopped at the authorized legal-consent inspection because `PIL.ImageGrab.grab()` could
  not capture the isolated desktop. No click, profile mutation, or commerce action occurred; the typed artifact is
  `artifacts-execute-01/runner-report.json` under the runtime seed directory.
- The `WinSta0\\Default` relay then ran the exact same frozen command. It crossed the launch boundary and produced a real crash dump, but CK3
  exited before loader/database logs at `C0000005 @ ck3+0x1DABD89`; the managed tree was cleaned. Repeating with the frozen four-guard bridge
  reproduced the same RVA and exit-before-loader, so the guards do not close this pre-loader boundary. These are preserved RED attempts, not
  video footage or seed evidence.
- A minimal no-mod/no-bridge CK3 launch on the same `WinSta0\\Default` relay reproduces `C0000005 @ ck3+0x1DABD89` in about `1.3s`; the
  corresponding sandbox-desktop launch reproduces the same exit code and crash. This closes the immediate attribution boundary at the CK3
  startup environment/binary level, independent of Phase2 content and bridge code. Diagnostic launcher and crash artifacts are under
  `_runtime/phase2-seed-20260903/plain-launch-*` and `plain-userdir/crashes/`.
- Current deliverable state is therefore unchanged for media: both dual-cut docs/configs are present, but real source footage is still `0/8`,
  neither target MP4 exists, and the remaining P0 gate is a normal-desktop-compatible CK3 startup/seed capture. Once that gate is closed, the
  already prepared seed-to-8-span intake path remains estimated at `20–40` minutes, followed by the mandatory fresh promo-tool update and media
  production for both cuts.

### Promo-tool fresh-update preparation

- The canonical promo-tool checkout at `Z:\\workspace\\xar_promo_toolchain` is read-only, so its `git fetch` cannot write `.git/FETCH_HEAD`.
  I created a separate writable clone at `Z:\\ck3_mod_rewrite\\_runtime\\promo-tool-fresh-20260903`, fetched GitHub `origin/main`, detached it at
  `57c42fca13ea459432c1caf76e069a1fbccf602c`, and verified `HEAD == origin/main` with a clean worktree. This is a fresh-update receipt for
  the future media boundary; no TTS, rendering, export, or publication has been run.

### open_kaishek synced-clone corpus preflight correction

- The writable sync clone `81109e46fce0c9b5efee49398f7411f772b1cfd9` was wired into a real offline Phase2 preflight. Its Maven/CLI build and focused
  parser tests remain GREEN, but validating the full frozen ZhongGuo corpus is still RED with `172255` `UNKNOWN_OPCODE` diagnostics. Therefore the
  clone is a valid synchronized development handoff, not a certified full-corpus validator/runtime; canonical checkout and push remain unchanged.

### Additional startup discriminators

- Reusing the historical `production10` bridge did not improve the current run: it reproduced `ck3+0x1DABD89` before loader output. The newer
  locally compiled guard-on bridge is the only build that advanced to the later `0x3BE33A9` VFS branch. This preserves the build-compatibility
  distinction and does not promote either bridge to a production/live claim.

### Continued static follow-up (latest turn)

- The writable promo-tool checkout performed a fresh `git fetch origin main --prune` successfully; `HEAD == origin/main ==
  57c42fca13ea459432c1caf76e069a1fbccf602c`, and the worktree is clean. The two-cut builder regression remains GREEN (`26/26`). No
  TTS, FFmpeg, export, or publication was started because source footage remains `0/8`.
- G2's targeted slot-23/truce observer and evaluator regression set is GREEN: `28 passed, 4 subtests passed` with `PYTHONPATH` bound to
  the current source tree. This is static/fixture evidence only; the required paused CK3 live observation remains pending the base-game startup fix.
- The synced `open_kaishek` clone's full Maven test suite is GREEN in offline mode against the existing user repository cache. The earlier
  network failure was dependency-download policy, not a source test failure; the full-corpus `UNKNOWN_OPCODE` validator boundary and
  `certified=false` status remain unchanged.
- Both final-cut target filenames were checked recursively under the workspace and remain absent. The docs index now records both cuts as
  mandatory independent deliverables and records the external `artifacts/demos/YYYY-MM-DD/` media storage boundary.
- The original shift-handoff document now has a 2026-09-03 continuation addendum covering the dual-cut contract, fresh promo-tool commit,
  CK3 startup/minidump boundary, G2 RTTI status, and open_kaishek certification boundary.

### Steam-library startup discriminator (06:18)

- To distinguish Steam handoff from installation-copy effects, a no-mod/no-bridge launch used the original Steam-library executable
  `Z:\SteamLibrary\steamapps\common\Crusader Kings III\binaries\ck3.exe` with a fresh isolated userdir. Its EXE SHA-256 matches the project
  reference copy and it reproduced `C0000005 @ ck3+0x1DABD89` before loader/database, with exception/minidump under
  `_runtime/phase2-seed-20260903/steam-library-bare-01/crashes/`. This rules out “only the project copy is broken”; the startup blocker remains
  at the CK3 environment/binary boundary.

### GDPR parameter discriminator (06:20)

- A second bare launch used the Steam-history argument `-gdpr-compliant` (with a correctly formed single `-userdir=<path>` and `-nographics`).
  It reproduced `C0000005 @ ck3+0x1DABD89` in about one second; exception/minidump are under
  `_runtime/phase2-seed-20260903/gdpr-bare-02/crashes/`. Missing GDPR consent arguments are therefore not the current early-crash cause.

### G2 residual RTTI offline recheck (06:23)

- Re-ran the exact-build residual RTTI extractor against the Steam-library `1.19.0.6` EXE and `game/` tree with no CK3 launch. It returned
  `GREEN`, the expected EXE SHA-256, and the already documented unique index-7 path
  `hidden_effect → attacker Context → expected CAddTruceEffect<0>`. Output SHA-256 is
  `3A56A1ACBF49591C0787EADE412C2C8F23E49E253DAC00C4ADB7A7624B628DB3`; live validation is still explicitly required.

### cwd and minidump discriminator (06:26)

- A bounded bare launch with the Steam-style game-root cwd (instead of `binaries`) still reproduced
  `C0000005 @ ck3+0x1DABD89`; artifact: `_runtime/phase2-seed-20260903/rootcwd-bare-01/crashes/`.
  Offline minidump disassembly identifies CK3 `.text+0x1DABD89` instruction `movsxd rdi, dword ptr [r14+0x4c]`, with the exception
  thread holding a small invalid `r14` (`0x7d8` in this run). This strengthens the conclusion that an internal early object/table is
  uninitialized; no mod/bridge or game-binary modification is justified.

### Graphics backend discriminator (06:31)

- A final bounded bare launch used the Steam-library EXE with game-root cwd, `-gdpr-compliant`, `-renderer=opengl`, and `-nographics`.
  It still crashed at `ck3+0x1DABD89`; artifact: `_runtime/phase2-seed-20260903/opengl-bare-01/crashes/`. D3D11, nographics, OpenGL,
  cwd, GDPR, and installation-copy variants all agree, so startup-parameter probing is closed until the external desktop or game installation changes.

### open_kaishek corpus/parser split recheck (06:39)

- The synchronized clone's `corpus --require-corpus` command parsed all 75 current ZhongGuo source files (`files=75, parsed=75, errors=0`), with corpus SHA-256
  `28e681358558f5e975fae911bf5ddf54eacb3c95dac3133852eaaeca6425c284`.
- The same input through `preflight --root` remains parser GREEN but validator RED with `172255 UNKNOWN_OPCODE` (exit code 1). This is a precise
  syntax-versus-semantic compatibility boundary; `certified=false` remains unchanged and no opcode was guessed or silently accepted.
- Receipts are preserved under `_runtime/open-kaishek-zg361-corpus-latest.json` (SHA-256
  `23932F820036BC6879A2F12207F043BA85D96A2199D3A5497926EC540A29F765`) and `_runtime/open-kaishek-zg361-preflight-latest.json` (SHA-256
  `1632824AB6C18FC0258A644B4F814403ADD6C4A868308AE56E9AAF87D58D617B`).

### G2/open_kaishek continuation regression (06:48)

- G2 preview-entry/seam/contract focused regression: `15 passed` (one pytest cache-permission warning only); exact-build residual RTTI extraction returned `GREEN`
  with output SHA-256 `3A56A1ACBF49591C0787EADE412C2C8F23E49E253DAC00C4ADB7A7624B628DB3`.
- The synchronized open_kaishek clone `81109e4` passed its full Maven test suite offline using the existing local dependency cache. These remain static/offline
  results; no `evaluated_days` live claim or full-corpus certification was promoted.

### Phase2 provider/choreography/static producer regression (06:50)

- The non-launch Phase2 business-postcondition, provider-packet, source-checkpoint, span-bridge, event-choreography, runner-plumbing, and authoring-promotion
  tests all passed (`12 + 7 + 4 + 3 + 4 + 8 + 15 + 2 + 1 + 3` tests respectively). The two promo-tool-dependent tests passed after binding
  `XAR_PROMO_SOURCE` to the clean fresh clone at `57c42fca`; no CK3 process was used for these tests and the synthetic runner PID messages are not live evidence.

### Steam `-applaunch` entry discriminator (06:44)

- One bounded Steam-client `-applaunch 1158310` attempt used the copied historical userdir with `-gdpr-compliant` and `-nographics`, without Mod or any purchase/store action.
  Steam exited normally, but no CK3 child process or new crash artifact appeared within 20 seconds. This entry therefore produced no usable session or footage and does not
  change the startup blocker; the same Steam entry will not be retried without an external-state change.

### Historical-userdir copy discriminator (06:42)

- A full copy of the known historical CK3 userdir was used as a new isolated profile (the original was untouched), then launched with the Steam-library EXE,
  `-gdpr-compliant`, `-nographics`, and no Mod. It exited after about 1.16 seconds with the same `C0000005 @ ck3+0x1DABD89`; crash evidence is under
  `_runtime/phase2-seed-20260903/userdir-cloned-history-01/crashes/ck3_20260903_064242/`. Historical settings/cache did not change the fault, so empty-userdir
  initialization is not a supported explanation and this class of duplicate experiment is closed.

### Phase2 seed preflight with synchronized open_kaishek (06:51)

- Re-ran the no-launch seed preflight against clean freeze `165b47742fd05ff3713b8be4452711002328d57d`, source ZIP SHA-256
  `77aa3e30f1c20763576dbeea71b1c7451cfd63a15fb53a46fc58a373d72338e8`, guard-on bridge, and synchronized open_kaishek `81109e46` using a valid 32-hex run-unique pipe.
  Result: `GREEN / preflight-ready`; source/archive equivalence, dependencies, bridge, projection, and static tests passed; `ck3_launch_attempted=false`.
- The seed contract remains `blocked_seed_generation_required` because no new real seed was captured. Receipt:
  `_runtime/phase2-seed-20260903/artifacts-preflight-current-03/preflight.json`, SHA-256
  `9057F967CFE97036AD4E3918C2640892371EBB006BBADDE18EE36DA7A4CABE2E`. The synchronized open_kaishek validator remains RED (`172255 UNKNOWN_OPCODE`),
  so this is a clean preflight boundary, not live or full-corpus certification.

### Dual-cut final static gate recheck (06:53)

- Re-ran the promo builder and dual-cut completion gate against the freshly updated promo-tool clone. Result: `31 passed, 4 subtests passed`.
- Promo tool remains clean with `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`. Both required MP4 targets are still absent; footage remains `0/8` for each cut, so no TTS/render/export was attempted.

### Documentation commit handoff (06:55)

- The updated handover, daily/weekly reports, and dual-cut production documents remain present in the integration worktree.
- A scoped `git add/commit/push` attempt was made, but the managed worktree denied creation of `.git/worktrees/_root-promo-split-20260902/index.lock`; the push also lacks a usable credential-manager/TTY. No claim of remote publication is made, and no source reset or cleanup was performed.

### Release/static integrity recheck (07:02)

- `validate_static.py`: GREEN (40 option keys, 5 custom loc, 88 modifiers, 9 languages).
- `build_release.py --check`: GREEN (86 files, deterministic manifest/ZIP).
- `build_vivhite_release.py --check`: GREEN (27 files; manifest SHA-256 `17EE1D49AA292C26E32025FBEB87C52DB3A2C94C488D378EEC4D3F2DD22CC0F0`; ZIP SHA-256 `B5D7F276C128878AE3F6A7F28110840515F7CE585FC6BF2E3CB998D73B460C08`).
- These checks confirm source/release integrity only; they do not change the CK3 startup or dual-video footage gate.

### Current-tree ZhongGuo acceptance preflight (07:03)

- Re-ran `run_zhongguo_acceptance.py --preflight` with the Steam-library exact-build executable, isolated user directory, workshop cache, and guard-on bridge/injector. Result: `ZHONGGUO 361 ACCEPTANCE PREFLIGHT: GREEN` with the expected CK3 1.19.0.6 executable SHA.
- This run did not launch CK3 and produced no gameplay or footage; the earlier missing-bridge/missing-executable parameter failures are superseded by this successful parameter-bound preflight.

### Phase2 preflight with open_kaishek G2 capability commit (07:07)

- Re-ran the bounded no-launch seed preflight with `open_kaishek` commit `981c79388a07e447b18f8e4472a16fd65e28c083`. Overall result is `GREEN / preflight-ready`, with `ck3_launch_attempted=false`, source/archive equivalence GREEN, and static projection GREEN.
- Receipt: `_runtime/phase2-seed-20260903/artifacts-preflight-open981-current-07/preflight.json`; SHA-256 `4D22CA2B43ABF75D3D895BB26C2EE8183909E711337C495F9AE54A5A45948DDB`.
- The nested open_kaishek corpus validator remains explicitly RED at `172255 UNKNOWN_OPCODE` while parser coverage is GREEN; this preserves the semantic certification boundary. Seed generation and live footage remain blocked.
- The writable open_kaishek clone is clean and `main...origin/main [ahead 2]` (the prior business-postcondition commit plus `981c793`); push remains blocked by the environment's missing credential-manager/TTY.

### open_kaishek replayable handoff artifacts (07:10)

- Exported the two local commits (`81109e4`, `981c793`) relative to `origin/main` as an applyable patch: `artifacts/open-kaishek-sync/2026-09-03/0002-g2-truce-capability-sync.patch`, SHA-256 `24C6260B67DB2811AD4F278659690C27B99260D17A56BED1DA2929B5D32849CF`, 107,588 bytes.
- Also preserved a Git bundle covering `origin/main..main`: `artifacts/open-kaishek-sync/2026-09-03/open-kaishek-main-981c793.bundle`, SHA-256 `30FCA309D8C9F4954DE1A7F101DD9CCE0FC0513D3C1080FDEBD02D5793115588`, 17,057 bytes. These are replayable handoff artifacts, not a claim that the remote branch was pushed.

### G2 capability contract verification boundary (07:13)

- `mvn -o` with the original cache hit a local JUnit JAR ACL error; an alternate writable cache then stopped at a missing Maven parent POM in offline mode. These are environment dependency failures, not source diagnostics.
- The newly compiled production classes passed an independent Java smoke assertion: `G2_CAPABILITY_ASSERTIONS_GREEN game.command.query-g2-truce-evaluated-days-v1`. The assertion checked required fields, same-frame double-read invariant, non-inferred expiry, read-only behavior, and both certification flags remaining false.

### Promo tool remote freshness recheck (07:16)

- Ran `git fetch origin main --prune` in the writable fresh promo-tool clone. It remains clean with `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`.
- No TTS, FFmpeg, render, export, or publish process was started; the check only preserves the required pre-video tool-version gate.

### Dual-video delivery index and intake boundary (07:20)

- Added `docs/phase2-promo/dual-video-delivery-status-2026-09-03.md`, explicitly indexing both mandatory outputs: `zhongguo-361-phase2-character-led.mp4` and `zhongguo-361-phase2-institution-led.mp4`. Both remain `authoring-ready`, real CK3 footage `0/8`, and not exported.
- Ran the phase-two footage intake against the available old fixture only as a read-only check. It returned `RED / footage_pending` with `ck3_started=false`, `ffmpeg_started=false`, and `media_generated=false`; receipt SHA-256 is `D34CF4070C0D474A06F29BA2C3A0707E4BA391D42D5C700992A6BE826D67A2A2`.
- Focused dual-cut builder/completion tests remain GREEN: `31 passed, 4 subtests passed`. This validates the gates and does not create media.

### G2/open_kaishek capability binding (07:22)

- Added `docs/ck3-native-ai/g2-truce-open-kaishek-sync-2026-09-03.md`, binding the root G2 truce wire to open_kaishek profile `game.command.query-g2-truce-evaluated-days-v1` at commit `981c79388a07e447b18f8e4472a16fd65e28c083`.
- The binding keeps `nativeCertified=false` and `runtimeCertified=false`; no paused exact-build evaluator artifact exists yet, so G2 readiness is unchanged.

### G2 focused regression (07:24)

- With the project-standard `PYTHONPATH=ck3_autonomous_player/src`, the bounded G2 truce/preview suite passed: `21 passed, 12 subtests passed` (one pytest cache ACL warning only).

### Phase2 business/choreography static regression (07:26)

- The current Phase2 business postcondition, event choreography, provider packet, source-checkpoint, and span-source bridge tests passed: `30 passed, 19 subtests passed`.
- This closes no live gate; the CK3 startup/8-span capture requirement remains separate and unchanged.

### Project static validation (07:28)

- `tools/validate_static.py` passed: `LOC VALIDATION OK` and `STATIC VALIDATION OK` (generated parity, BOM/loc, mechanics/AI, release allowlist/assets).
- This confirms the current source tree remains static-ready while the native/live and media gates stay open.

### External dependency freshness recheck (07:30)

- Promo tool `git fetch origin main --prune` succeeded; writable checkout remains clean at `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`.
- open_kaishek fetch also succeeded; local compatibility branch remains clean at `981c79388a07e447b18f8e4472a16fd65e28c083`, two commits ahead of `origin/main == 0390b9a959fa1a59a968000ed49e827a03b8d4e4`.
- No dependency changed, so no new video build or G2 contract rewrite was necessary; the existing frozen receipts remain authoritative.

### Root G2/open_kaishek binding (07:31)

- Added descriptive capability/profile/commit constants to `raiktor_surrender_truce_contract.py`, matching open_kaishek `game.command.query-g2-truce-evaluated-days-v1` at `981c79388a07e447b18f8e4472a16fd65e28c083`.
- The focused root contract test passed: `6 passed, 12 subtests passed`; the binding does not change native/runtime readiness or expiry semantics.

### G2 full focused regression after binding (07:33)

- Re-ran the complete truce/preview set after the root binding change: `22 passed, 12 subtests passed` (one known pytest cache ACL warning).

### G2 public bridge export (07:35)

- Exported the three frozen open_kaishek compatibility identities from `xar_autoplayer.bridge`; import smoke passed as `G2_OPEN_KAISHEK_PUBLIC_EXPORT_GREEN`.
- This remains descriptive and read-only; no native query, action, or readiness promotion was added.
### Correctly bound ZhongGuo acceptance preflight (07:36)

- Re-ran `run_zhongguo_acceptance.py --preflight` with `XAR_CK3_EXE` bound to the Steam-library exact-build executable and the guard-on bridge/injector. Result: `ZHONGGUO 361 ACCEPTANCE PREFLIGHT: GREEN`, CK3 `1.19.0.6`, expected EXE SHA, desktop `2560x1440`.
- The earlier invocation that omitted the bridge/game executable was a parameter error; this successful run supersedes it. No CK3 process, FFmpeg process, or footage was created.
### G2 generic war-bound successor seam regression (07:39)

- Ran the cold-map VFS observer, guarded retry, and normal-desktop preflight contracts: `6 passed` (one known pytest cache ACL warning).
- The observer remains `static-ready-no-launch`; the next admissible step is an A/B live capture only after the exclusive CK3 slot and a changed startup environment are available. No seventh guard was added.

### Cross-project acceptance coverage (07:40)

- Open_kaishek coverage, workshop acceptance, Phase2 seed runner, and promo-runner plumbing tests passed: `43 passed, 8 subtests passed`.
### G2 public export regression (07:42)

- Extended the root truce contract test to assert the `xar_autoplayer.bridge` public exports match the internal frozen binding; result `7 passed, 12 subtests passed` (one known pytest cache ACL warning).

### Dual-video production packet and runbooks (08:00)

- Added `docs/phase2-promo/dual-video-production-packet-2026-09-03.md`, which separately records the character-led and institution-led deliverables, their director treatments, target MP4 paths, independent review gates, and the current hard blocker.
- Generated independent planner runbooks at `_runtime/phase2-dual-runbooks-20260903-0800/character-runbook.json` and `institution-runbook.json`. Both are `RED / footage_pending`; no CK3, TTS, FFmpeg, export, or publish process was started.
- Both authoring ledgers validate GREEN (`10 chapter drafts; 8/8 gameplay cues remain bound to future real clean spans`). Phase2 completion regression passed 5 tests and `mod_zhongguo_style` static validation is GREEN.
- The two target MP4s remain ungenerated because CK3 still crashes before loader/database and real footage is `0/8`; the old smoke video remains excluded from both cuts.

### G2 activity schema boundary (08:08)

- Recorded `docs/ck3-native-ai/g2-open-kaishek-activity-schema-red-2026-09-03.md`: open_kaishek parser remains GREEN, while validator is RED with 172,255 `UNKNOWN_OPCODE` diagnostics concentrated in `common/activities/activity_types/zg361_jingcha.txt`.
- No guessed allow-list or runtime claim was added. The next minimum work is exact-build read-only schema/evaluator evidence for activity-type keys; this does not alter the dual-video footage gate.

### Dual-cut builder optimized regression (08:16)

- Re-ran the Phase2 completion and promo-runner plumbing suites under Python `-O`; both remained GREEN (`5` and `15` tests respectively).
- With `XAR_PROMO_SOURCE` bound to the fresh clone at tool HEAD `57c42fca13ea459432c1caf76e069a1fbccf602c`, the dual-cut builder suite under `-O` passed all `26` tests. The first invocation without that environment binding failed as an invocation precondition only and did not start media production.

### Current G2/static/release recheck (08:28)

- G2 truce contract, preview observer, and scoreboard action focused tests passed: `16 passed, 12 subtests passed` (one known pytest cache ACL warning).
- `tools/validate_static.py` returned `LOC VALIDATION OK` and `STATIC VALIDATION OK`; `build_release.py --check` returned `RELEASE CHECK OK: 86 files, deterministic manifest and ZIP`.
- Promo-tool freshness was rechecked with `git fetch origin main --prune`; writable clone remains clean with `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`. No target MP4 appeared.

### Continued-state audit (08:40)

- Rechecked CK3 executable identity: build `1.19.0.6`, SHA-256 `2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86`; no CK3/bridge process is running and no new Phase2 media artifact exists.
- Reconfirmed the external promo-tool checkout is clean and unchanged. The next media action remains gated on real eight-span capture; no duplicate launch attempt was made without an environment change.

### Independent release regression (08:47)

- White-Qi independent edition static validation and deterministic release check passed: `27` files, manifest SHA-256 `17ee1d49aa292c26e32025fbbe87c52db3a2c94c488d378eec4d3f2dd22cc0f0`, ZIP SHA-256 `b5d7f276c128878ae3f6a7f28110840515f7ce585fc6bf2e3cb998d73b460c08`.

### CK3 `-noWorkshop` startup A/B probe (09:02)

- Ran CK3 `1.19.0.6` from the isolated userdir with `-nolauncher -noWorkshop -debug_mode`; no bridge, mod, TTS, FFmpeg, or store action was involved.
- The process exited within 15 seconds with code `1`; crash remained `C0000005` at `ck3+0x1DABD89`, matching the prior no-Workshop-disabled path. No loader/database readiness, seed, or footage was produced.
- Isolated crash receipt is documented in `docs/phase2-promo/live-startup-probe-no-workshop-2026-09-03.md`; this closes the `-noWorkshop` hypothesis without adding another guard.

### CK3 `--userdir=` argument-form recheck (09:08)

- Repeated the isolated no-mod/no-bridge `-noWorkshop` probe using `--userdir=<isolated path>` rather than the separated short form. CK3 again exited with code `1` within 15 seconds at `ck3+0x1DABD89`.
- This closes the argument-form hypothesis as well; no seed, loader readiness, or footage was created.

### G2 focused contract evidence (08:32)

- Added [`g2-focused-test-evidence-2026-09-03.md`](../../ck3-native-ai/g2-focused-test-evidence-2026-09-03.md), recording a bounded offline G2 deliverable: the root `evaluated_days`/no-expiry contract, descriptive `open_kaishek` binding, exact-build preview seam, and default-off diagnostic-only wiring are covered by one focused regression.
- The standard Python invocation passed `18` tests and `12` subtests in `14.44s`. No CK3 process was started and no guessed activity opcode/schema was introduced.
- Readiness remains `static-ready-no-launch`: this confirms contract and wiring integrity only; it does not certify a paused native evaluator value, runtime read, persisted expiry, or action path. The next value-bearing step is still an exclusive CK3 launch slot for a same-frame two-read paused artifact.

### open_kaishek sync and remote push (08:35)

- The writable compatibility branch was fetched and pushed successfully. `HEAD == origin/main == 981c79388a07e447b18f8e4472a16fd65e28c083`; the push advanced remote `main` from `0390b9a959fa1a59a968000ed49e827a03b8d4e4`.
- Maven verification with an isolated local repository passed: `125` tests, `0` failures/errors/skips. The two focused Zhongguo business-postcondition fixtures and seven CK3 1.19.0.6 schema fixtures all returned parser/validator `GREEN`; IR/runtime remain explicitly `SKIPPED`, with `ck3_started=false`.
- A fresh patch and bundle were regenerated and verified. Patch SHA-256 is `0AB47A75A830FB9861009D20E252B6F93FF0D8A7355CB4535E4B9FE9010957EE`; bundle SHA-256 is `DCE6AF147692A8F9873D1D99CFE42D57D59D6C87B4596A3E60BF2F1BE47804E3`, and `git bundle verify` passed. Receipt: `artifacts/open-kaishek-sync/2026-09-03/open-kaishek-main-981c793-receipt.json`.
- No CK3 activity opcode/allow-list was guessed or changed. Native/runtime certification remains pending exact-build paused evaluator evidence.

### Steam `-applaunch` entry probe (09:20)

- Invoked the running Steam client with `steam.exe -applaunch 1158310` as a distinct launch entry and monitored for 35 seconds.
- No CK3 or dowser child appeared and no new crash artifact was created. This is a no-launch harness result, not game readiness; no seed, footage, media, store, purchase, or payment action occurred.

### Phase2 business/content regression (09:25)

- Ran the local Phase2 business-postcondition, event-choreography, and provider-packet suites: `12 + 4 + 7 = 23` tests passed.
- These are source/fixture checks only; CK3 native/live and final dual-video media gates remain unchanged.

### Final focused G2/Phase2 regression (09:40)

- The latest root truce, exact-build preview observer, scoreboard batch, Phase2 completion, and planner tests passed: `34 passed, 12 subtests passed in 15.43s`.
- This confirms the post-push tree is internally consistent; it does not create paused native evidence or either final MP4.

### G2 root/open_kaishek pin and static-gate recheck (12:34)

- Rechecked the canonical binding at root `HEAD=5285d3297c5bd3c004a79802bb4c997255bc7eb8`. The root fixture and truce contract now pin open_kaishek `15ab978f879ed4562aacb74dacdaee702fbce54b` (the activity-schema boundary commit); the writable checkout `Z:\workspace\open_kaishek` is clean with `HEAD == origin/main` at the same commit.
- Ran the no-launch cross-repository verifier:
  `py ck3_autonomous_player/native_bridge/research/verify_g2_open_kaishek_compatibility.py --checkout Z:\workspace\open_kaishek --require-checkout --require-clean`.
  Result: `GREEN_STATIC`, all checks true, with `native_certified=false`, `runtime_certified=false`, `production_live=false`, and no CK3 start/attach/mutation/allow-list change.
- Root offline G2 regression passed: `27 passed, 16 subtests passed` across the truce contract, callsite activation, preview seam/contract, and postprocess tests. The compatibility/preflight/activity boundary set passed an additional `22 tests`; the known pytest-cache ACL warning is non-functional.
- Re-ran `mvn -o -ntp test` in the pinned open_kaishek checkout: reactor `BUILD SUCCESS`; 127 tests, 0 failures, 0 errors, 0 skipped. The rebuilt shaded CLI JAR SHA-256 is `FC85947E9976B80345C10A9789A6520C8ABCEF557365284570C18C4677D891CC`.
- The deliberately negative activity fixture remains an honest RED boundary: parser `GREEN` (1 file, 205 bytes), validator `RED` with exactly 5 `UNKNOWN_OPCODE` diagnostics, IR/runtime `SKIPPED`. No activity opcode or profile allow-list was guessed or changed.

#### G2 当前进度与遗留项

| 项目 | 当前进度 | 下一项 | 交付预期 |
|---|---|---|---|
| root ↔ open_kaishek 绑定 | **static-ready / 已同步** | 仅在接口、profile 或 fixture 真有变化时再做 CHANGE | 当前 pin 已可复核；无额外代码提交必要 |
| `evaluated_days` 原生观测 | **未完成**；合同和只读 preview seam 已覆盖，尚无 paused exact-build 值 | 在 CK3 独占启动窗口取得同一暂停帧两次 evaluator read，并绑定 frame/date/角色身份 | 依赖 CK3 启动恢复；窗口未排定前不虚报时间 |
| expiry / war-bound / action 闭环 | **未完成**；`evaluated_days` 不推断 expiry，action/automatic 仍关闭 | 先补只读字段与持久化 expiry 证据，再评估策略入口 | 依赖上项 native artifact；当前不进入生产 live |
| activity-type schema | **RED（预期）**；5 个 activity-owned key 尚无 exact-build 归属证据 | 冻结 exact build 后做只读 schema/call-chain 差分；证据闭合前保持 RED | 不以静态 parser/validator 结果替代运行时能力 |

本次工作包没有启动或附加 CK3，也没有生成 live footage；这里的 `static-ready` 只指 GEN-034 的 `evaluated_days` / generic-war-bound 新切片，不覆盖既有固定-seed G2 aggregate `production-live loop`。新切片仍不是 `fixture-live`、`production-live` 或 `complete`；当天稍后已按 owner 明确要求暂停，不再等待 CK3 槽。

## 2026-09-03 13:20 continuation — startup recovery and parallel queue

### CK3 有效 profile 启动对照（12:48–12:55）

- 使用显式非空 disposable `-userdir`、完整 `pdx_settings.txt/account/dlc` 与 warm DX11 cache 的同一 CK3 `1.19.0.6`/EXE SHA 后，无 Mod 裸跑在 42.253 秒到达 `Frontend`；实际主菜单截图、debug/system/setup 日志和 `error.log=0` 已保存于 `Z:\ck3_mod_rewrite\_runtime\bare-nomod-20260903\`。
- 当前 Release bridge 变体 `formal-currentbridge-bare-20260903` 在 54.634 秒到达 `Frontend`，bridge hello、`ck3_build_match=true`，WM_CLOSE/exit 0/cleanup proven；RBX guard candidate `formal-guard-on-bare-20260903-r1` 在 45.582 秒同样 GREEN，未替换 freeze165b。
- 这组证据纠正了早先把冷/不完整 profile 的 `C0000005 @ ck3+0x1DABD89` 等同于“CK3 完全打不开”的表述。冷 profile 失败仍保留为失败 artifact；有效 warm profile 下启动链已恢复。三轮均未载入存档、未执行 gameplay、未点击协议/通知、未访问商店，也未购买/付款。

### Phase2 projection 串行 A/B（13:01–13:19）

- `formal-phase2-callable-core-20260903`：66 files/14,430,022B，timeout，`Total 881` 后无 history，error 2（缺 central callable 的级联）；未到正式 Frontend。
- `formal-phase2-event-core-20260903`：81 files/14,802,010B，timeout，error 323（event owner localization 缺口），同样停在 `Total 881`。
- `formal-phase2-event-locaug-20260903`：162 files/15,060,079B，timeout，窗口与 `frontend_main.gui` 出现但无 `End loading of history`；error 68（B1/B2/incident/manager loc fan-out 仍缺），cleanup proven。report SHA-256 `356C8F3C18E4933580E26CDCFE856CD861653E7A250D26332DCA2FBDD4D0A699`。
- `formal-phase2-event-locfull-20260903`：261 files/15,924,897B，完整 198 个 localization 文件，error/parser=0，仍在 `Total 881` 后 timeout；WM_CLOSE、exit 1、cleanup proven。由此缺失 localization 已排除，剩余挂点位于 central/workforce runtime/规模或依赖转换。
- `formal-phase2-workforce-left-full-20260903`：workforce blocks 0–161 的 disposable 变体在约 100.57 秒完成 `End loading of history` 并到 Frontend，exit 0/cleanup proven；因故意截断右半而产生的 unknown-effect/级联日志只用于定位，不能把该半区当完整功能 GREEN。当前挂点候选收窄到 blocks 162–323 或两半组合。
- 启动槽始终单实例串行；没有任何 CK3 运行与购买/付款动作并发。

### T0 宣传片、T1 G2、T2 open_kaishek

- 宣传工具 fresh clone 已更新并验证 `HEAD == origin/main == 57c42fca13ea459432c1caf76e069a1fbccf602c`；`df06581` 修复跨 SID checkout 的 `safe.directory` 误红，preflight/builder 选择测试 GREEN。字幕像素宽度预检已由 `0cbdda4` 记录（Pillow 12.3；人物中/英 1104/1010px、制度 1104/983px，均在安全宽内）。真实素材仍 `0/8`，两版 MP4 不存在，不启动 TTS/FFmpeg。
- G2 cross-repository fixture identity 增量 `fd18aa8` 已通过 38 tests/28 subtests；open_kaishek `15ab978f...` 同步审计 `47ac9de` 为 `NO-CHANGE`、verifier `GREEN_STATIC`，Maven 127/127，无 native/runtime 晋级。`evaluated_days` 仍等待一次 paused exact-build 双读。
- 本段新增/完成的并行包：startup valid-profile evidence、loc fan-out closure、workforce block segmentation (`8d7cfcc`)、promo safe-directory fix、G2/open sync audit；下一门槛是 workforce 右半/块级 A/B → seed → 8/8 clean spans → 两版独立 TTS/候选/审阅/导出。

### 当前 readiness 与 ETA 边界

| 工作包 | 当前状态 | 下一项 | 预计交付 |
|---|---|---|---|
| CK3 本体启动 | `startup-boundary GREEN`（有效 warm profile 可到 Frontend；尚无 paused seed） | 继续 Phase2 projection 单槽 A/B | 不再以“打不开”作为 blocker；每轮约 3–4 分钟 |
| 天朝二期代码/seed | `static-ready`，projection 仍 `RED` | 右半/块级依赖定位，随后新 seed | 依赖 A/B；未承诺固定时刻 |
| 人物版/制度群像版视频 | `static-ready`/`footage_pending`，`0/8` | 取得并入库 8 段真实 clean span；再各自制作审阅 | 素材齐后 intake 20–40 分钟；每版候选 45–90 分钟+两轮审阅 |
| G2 GEN-034 新切片 | `static-ready`，native/runtime false；aggregate 固定-seed 能力不降级 | **owner-deferred / paused**；仅解除暂停后才做 paused exact-build evaluator 双读 | 当前不占用 CK3 槽 |
| open_kaishek | `static-ready`/`NO-CHANGE` | 仅接口或 fixture 真变化时同步 | 当前无需额外 API 改动 |

### 30 分钟 CK3 完整投影轮次（13:49 起，进行中）

- 应项目所有者要求，将本轮 CK3 runner `timeout_seconds` 设置为 `1800.0`（30 分钟）；这是诊断观察窗口，不是 CK3/Steam/Windows 限制，也未设置 11 GB 内存上限。
- `formal-phase2-full-exact-1800-20260903` 使用完整 direct-union-v2 + localization fan-out（264 files，15,937,535 bytes）。截至 13:56，PID 46160 仍响应，debug 最后 marker 为 `Total of : 881`（13:49:47），无 Frontend/history，`error.log=0`；WS 约 11.0 GB、私有内存约 14.3 GB。
- 此处是当时的滚动快照，现已 **superseded**：该轮最终 timeout，停在 `Total 881`，没有 Frontend/history；`error.log=0`、exit 1、cleanup proven。它只保留为未拆 full projection control，不再列为进行中，也没有启动 7200 秒续跑。

### 用户将同一观察要求改为 120 分钟（14:02）

- 当前进程的 `TIMEOUT_SECONDS=1800` 已在启动时固定，不能安全热改；先按原 30 分钟封存完整 report/cleanup，再以同一 source、EXE、profile、bridge 和参数建立 `formal-phase2-full-exact-7200-20260903`，实际 `timeout_seconds=7200.0`。
- 续测会在 report 中记录 `parent_run`、source/tree SHA 和两轮的 elapsed/result/exit/cleanup；两轮之间不启动 B 变体或其他 CK3。该调整不改变 T0 素材 0/8、T1 static-ready、T2 NO-CHANGE 的 readiness 结论。

## 2026-09-03 Phase2 分批实测更新（15:38）

### P1 纯 case-kernel：r4 实机结果

- 本批以一期 byte-authoritative old-core 的 51 个文件为基线，只追加
  `common/scripted_effects/zg361_case_kernel_effects.txt` 与
  `common/scripted_triggers/zg361_case_kernel_triggers.txt`；投影为 53 个文件、
  7,313,274 B。冻结源声明为 `c91a1d01ee2708ae934591e4e8ae17e2f9bdf39b`，
  product tree SHA-256 为
  `f3edcb560e07f14967c73908b8558d252bd5d134ca7e3095d3a3dad72df37a7d`。
- r2 no-launch preflight 的总结果为 `GREEN / preflight-ready`；source/archive、
  bridge、product projection 和静态检查均 GREEN，`ck3_launch_attempted=false`、
  `seed_ready=false`（仍需后续 seed 生成）。可选的 open_kaishek 子检查保持已知
  schema-only RED（21,831 个 `UNKNOWN_OPCODE`），不把它误判成 CK3 启动失败。
  预验 artifact：
  `Z:\ck3_mod_rewrite\_runtime\phase2-revised-checkpoints-20260903-preflight\artifacts-p1-pure-kernel-r2\preflight.json`，
  SHA-256 `53988DB04F91F81814555222EA160AEE897EFE4B26F75004769C3E7A7797ED19`。
- 唯一 CK3 槽中的 r4 attempt
  `formal-phase2-revised-p1-pure-kernel-c91-20260903-r4`（PID `22996`）在
  `15:35:25` 同时出现 `Frontend` 和 `End loading of history`；没有停在早期
  `Total 881` 边界。runner 总报告为 `RED` 仅因专用 wrapper/consumer observer
  `heartbeat_not_observed`（`sample_count=0`），不是 CK3 加载崩溃；845 行
  `error.log` 来自刻意未挂载的后续 B1/incident 依赖，归类为
  `STARTUP_GREEN / projection-RED`。WM_CLOSE、进程树清理、最终 inventory 和
  source/runtime 不变性均 GREEN。报告 SHA-256：
  `2517CEF49FFCD34CB1D1D79D03EBF4CFFA60DDECD540C63624C90F33E76D07FB`。
- 结论：一期基线加纯 case-kernel 不会把 CK3 卡死在早期加载阶段；它证明了
  “基线 → 增量投影 → 单槽启动”这条归因方法可用，但 P1 尚非 parser-clean、
  seed-live 或最终内容完成。

### 分批总数与后续调度

- 管理层仍保留 **13 个名义阶段**（一期基线 + 12 个二期增量），但为使每次
  结果可归因，实际执行采用 **17 个固定 diagnostic checkpoints**（含 old-core
  基线、P1 case-kernel、B2/B1 闭包、central、scoreboard 和最终 endpoint）。
- 第 7 个 workforce checkpoint 不预先假定一次通过：先做 strict preflight，
  再按首个失败边界递归二分，预计额外 **3–6 次** 单槽启动。因此本轮预计
  **20–23 次 CK3 启动**；若 workforce 整体一次通过，则为 17 次。这个数字是
  诊断计划，不是把尚未发生的轮次报成已完成。
- P1 r4 后的下一项是 P2 `02-b2-closure` 的 strict no-launch preflight，
  通过后才占用下一次唯一 CK3 槽；每次离线物化通常数分钟，warm profile 下单次
  CK3 约 2–5 分钟，固定 17 checkpoint 的理论窗口约 35–85 分钟，workforce
  条件轮再加约 6–30 分钟。此前拟议的 7200 秒续测已取消，后续回到上述原计划
  和首失败即转二分的 bounded 观察方式。
- 详细阶段表与判定规则见
  [`phase2-incremental-startup-batch-plan-2026-09-03.md`](../../phase2-promo/phase2-incremental-startup-batch-plan-2026-09-03.md)
  及 P1 证据
  [`ck3-incremental-p1-pure-kernel-c91-evidence-2026-09-03.md`](../../phase2-promo/ck3-incremental-p1-pure-kernel-c91-evidence-2026-09-03.md)。

### P2/P3 修订 checkpoint 实测（15:45–15:54）

- **P2 `02-b2-closure`**：一期基线 + B2 闭包投影，共 64 个文件（7,630,438 B）。
  在 `15:45:22` 的同一 CK3 启动中到达 `Frontend` 与 `End loading of history`；
  专用 observer 仍为 `heartbeat_not_observed`（RED），而 native session 的
  WM_CLOSE、进程树回收与 cleanup 均 GREEN。该结果说明 P2 在启动层继续通过，
  但不能把 observer RED 或外层 runner 状态写成完整功能通过。
- **P3 `03-b1-b2-closure`**：在 P2 之上加入 B1/B2 依赖闭包，共 77 个文件
  （8,234,687 B）。`15:52:33` 出现 `End loading of history`（本轮未记录
  `Frontend` marker）；observer 同样 `heartbeat_not_observed`（RED），cleanup、
  source/runtime 不变性与进程回收均 GREEN。
- P2、P3 的首个新增脚本诊断仍指向**未挂载的 workforce/incident 后续依赖**，
  例如 `zg361_workforce_normal_exit_fact_begin_from_m075_offer_effect`、
  `zg361_b1_open_cycle_effect` 和 `zg361_ip_open_x_case_effect`。这些是刻意按
  checkpoint 截断造成的 projection 依赖缺口，不能据此断言 P2/P3 本身使 CK3
  早期崩溃；它们也尚未达到 parser-clean、seed-live 或完整内容 GREEN。
- 两轮均遵守 CK3 启动槽单实例串行规则，未载入存档、未执行 gameplay、未点击
  协议/通知、未访问商店，也未发生购买/付款动作。固定诊断进度现为 checkpoint
  0–3 已有启动证据（其中 P1/P2/P3 的 observer 仍 RED），剩余固定 checkpoint
  按依赖闭包继续；workforce 仍保留条件性 3–6 轮二分。

### P4、m275 disposable stub、P6 实机结果（15:58–16:15）

以下三轮都使用同一 c91 冻结源和 warm profile，并按 CK3 单实例串行规则执行。
`Total of : 881` 是 on_action 计数 marker，不单独等于失败；本组三轮在各自
bounded 观察窗口内都没有出现 `Frontend` 或 `End loading of history`，因此均不
晋级为启动/功能 GREEN。

- **P4 `04-callable-core-static`（66 files / 14,430,022 B）**：attempt/report
  路径为
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-revised-p4-callable-core-c91-20260903-r1\artifacts\runner-report.json`；
  运行约 `15:58:01–16:01:32`，`15:59:13` 命中 `Total of : 881`，随后在
  `zg361_we_m275_hold_due_effect` 处首先报
  `Unknown effect: zg361_p2c_schedule_m275_runner_requisition_effect`（并伴随
  `else/else_if` 级联）。runner report SHA-256
  `1040CFC46C78CD3529D90E47E09F65A28F767F9066C6BAA6172D32E5769A758B`；
  `debug.log` SHA `4D3001E49EA86C01EE6CFA169CFAF7D82523AC6EBE0168AA940D6743B11B8812`，
  `error.log` SHA `66ACBCFE43581FA6F7655C38431B9F2F2FCCE5B65BB90B99BAEFEA93931C8330`
  （2 行）。observer 为 `heartbeat_not_observed`/RED（sample_count=0），
  cleanup 为 GREEN（PID `34264`，tree gone/cleanup proven）。
- **m275 disposable stub（67 files / 14,430,535 B）**：attempt/report
  路径为
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-callable-core-m275-stub-c91-20260903-r1\artifacts\runner-report.json`；
  运行约 `16:03:17–16:06:48`，`16:04:29` 命中 `Total of : 881`。stub 后
  `error.log` 为 0 字节（SHA
  `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`），但
  仍未到 `Frontend`/history，说明 m275 缺口不是唯一的加载停滞原因。runner
  report SHA-256
  `A64D0121E25E13CD30086DEF55984CEDA22CC83B365B1209295930ECF8664F2D`；
  `debug.log` SHA `A8C1538920B98A49EB1CFEC04E96B1B7CEF4EEF1CC5DD4E5B3E567125032960E`。
  observer 同为 `heartbeat_not_observed`/RED（sample_count=0），cleanup 为
  GREEN（PID `25824`，tree gone/cleanup proven）。该 stub 仅用于诊断，不进入正式
  产品树。
- **P6 `06-central-callable-closure`（68 files / 14,572,179 B）**：attempt/report
  路径为
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-revised-p6-central-callable-c91-20260903-r1\artifacts\runner-report.json`；
  运行约 `16:11:47–16:15:17`，`16:12:56` 命中 `Total of : 881`。首要新增
  诊断收敛到 central 中未挂载的 portfolio owner（首先为
  `zg361_career_hc_open_portfolio_effect`，随后包括
  `zg361_comp_portfolio_open_next_effect`、`zg361_pp_manager_portfolio_adapter_effect`
  等），并出现 `revoke_court_position` block 参数语法错误；没有
  `Frontend`/history。runner report SHA-256
  `A9660B430D70215A454DF75354FD68045E64C83E992C52910DC1358A5E38FED6`；
  `debug.log` SHA `30F6BEC3D1DB1CE3872C87AA0010EAC94BFE037DE8D3F72C5C3729B72D6E58A7`，
  `error.log` SHA `E8089CA584BD1445F3447CA4FA845E80E908E7F554660D552E6A1AF73880C01F`
  （15 行）。observer 为 `heartbeat_not_observed`/RED（sample_count=0），cleanup
  为 GREEN（PID `4728`，tree gone/cleanup proven）。

### 下一轮

P6 之后先在 disposable 副本中补齐最小 **portfolio owner + B1 trigger closure**
（包括旧 trigger 替换和 `revoke_court_position` 的合法 block 形状），再跑 strict
no-launch preflight；只有 preflight GREEN 才占用下一次 CK3 槽。P4 的 m275 stub
对照已证明不能直接把单个缺口当作 `Total 881` 的唯一根因；每轮仍保留 report、
日志和 SHA，不覆盖失败证据。

### P7 portfolio/event closure 实机结果（16:31–16:37）

- **P7 `p7-portfolio-event-closure-repaired`**：在 P6 的 74-file disposable
  基础上加入 central event/workforce 闭包，共 **99 files / 20,929,655 B**。
  物化树与投影清单分别为
  `Z:\ck3_mod_rewrite\_runtime\phase2-p7-portfolio-event-closure-20260903\product`
  和 `projection.json`；product/runtime tree SHA-256 为
  `7b4c4961480e87d2da16a2fcfaff005b037304874f29f71e9a75d06cbd8ac8fa`，manifest
  SHA-256 为 `8f6410df4ed611190ed22aa852db51a5bb1379ed1707d3ad5f1b8435c9a550ca`。
  本次正式实机报告位于
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p7-portfolio-event-closure-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `2939E1F761C2574B6CCBB75D7EF6B65AB3B3DA0A92C28CFF419DEE137FCE7501`）。
- 使用 **300 s bounded observer-only** 窗口（report 的 `loader_seconds`/
  `total_event_wait_seconds` 均为 300；native observer 未发送 UI/gameplay 输入）。
  `16:33:35` 的 debug.log 记录 `Total of : 881`，但没有 observer 的 `Frontend`
  或 `End loading of history` readiness marker；native observer 为
  `heartbeat_not_observed`（RED，`sample_count=0`），所以本轮不晋级为启动/功能
  GREEN。对应 `debug.log` SHA-256 为
  `4013CD5849981AC9DBCE15C678B742B8D36C129239372FB5BEF25DE9908E08BE`。
- **首要阻点是 parser/localization RED，而非体积证据**：`error.log` 共
  **45,614 B / 323 条** `Unrecognized loc key`，其中 literal `zg361we.*` 为
  212 条，其余是同一 workforce localization 缺口的其他前缀；P7 投影漏挂
  workforce event localization（尤其 `zg361_workforce_endgame` 及其余
  workforce 事件文案），因此不能把该次停滞归因于 99-file/20.9 MB 的大小。
  保存的 error.log SHA-256 为
  `AC3AB24C2CF9BA246C2479741E635F86DDD3B43818D5B674395D6F23DE2D9012`。
- native session 的进程树回收和 cleanup **GREEN**（初始 CK3 PID `57556`，
  `tree_gone=true`、`cleanup_proven=true`，无遗留进程）；`runtime_unchanged=true`。
  本轮只读观察，未载入存档、未执行 gameplay、未点击协议/通知、未访问商店，
  没有购买/付款动作。

### P7 下一步

先在 disposable 投影中补齐**仅缺失的 workforce event localization 闭包**，重新
跑 strict no-launch preflight；preflight GREEN 后再占用唯一 CK3 启动槽复测。保留
本轮 parser/localization RED 和 observer RED 证据，不把 `Total 881` 或文件体积
当作单一根因。

### B7 四块 workforce stub 实机结果（16:42–16:47）

- **B7 `p6-portfolio-closure-b7-stub`**：在修订 P6 的 74-file 闭包上，仅将
  `zg361_workforce_endgame_runtime_effects.txt` 中四个 m360 顶层 block 替换为
  同名 no-op stub：
  `zg361_we_materialize_m360_route_a_from_central_effect`、
  `zg361_we_materialize_m360_route_b_from_central_effect`、
  `zg361_we_m360_route_a_effect`、
  `zg361_we_m360_route_b_effect`。投影共 **74 files / 18,936,168 B**；product/runtime
  tree SHA-256 为
  `902EE0DE0E2DB2A9277BEB578B88F8D829AB8818057566291504B4C4C21B5ED8`，manifest
  SHA-256 为 `ECCE93118ACCF57D3349963295B5BAEED55C3D3144A158225406A2F0F3EB9296`。
  正式实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p6-portfolio-closure-b7-stub-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `5435A111C824BAF63DB5F321615C1D69173AB4BF3D464F5B92077F3CE9A75F39`）。
- 采用约 **300 s bounded observer-only** 窗口；`16:43:26` 的 debug.log 仍记录
  `Total of : 881`，本轮没有 `End loading of history` marker（也未取得
  observer 的 Frontend/history readiness）。`error.log` 为 **0 B**（空文件 SHA
  `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`），所以
  这四块替换消除了当前日志错误，但没有越过启动边界。debug.log SHA-256 为
  `367ECC2B3A592F922D4CD0E57C50E7AB3F0DCCC8802113B1B4A6A456DBE77DAC`。
- native observer 仍为 `heartbeat_not_observed`（RED，`sample_count=0`、无 UI/
  gameplay 输入）；native session cleanup **GREEN**，初始 CK3 PID `54900`，
  `tree_gone=true`、`cleanup_proven=true`，无残留进程。该结果说明**四大块整体
  替换仍不足以越过边界**，不能据此把 workforce endgame 整体判为根因或通过。
  下一轮应沿四块内部递归细分（保持每轮 disposable、单槽、首失败证据），而不是
  一次性恢复完整 4.6 MB 文件。

### B7 下一步

在保留 B7 空 error.log 与 `Total 881`/无 history 证据的前提下，对四个 m360 block
逐块或按更小子块递归二分；每个候选先跑 strict no-launch preflight，只有
preflight GREEN 才进行下一次唯一 CK3 启动。B7 仍是诊断 stub，不进入正式产品树。

### P7a 全量 workforce event localization 修复实机结果（16:50–16:56）

- **P7a `p7a-portfolio-event-localization`**：在 P7 的 99-file 投影上补入
  13 个 localization family 的九语言文件（117 个新增文件），共 **216 files /
  21,360,068 B**；离线覆盖核对为 **311/311 unique missing loc keys → 0/311**。
  product/runtime tree SHA-256 为
  `092E1219F73A2D6F5AD59A2AB1674B7254470E2C17F49CCC2C62E3B7454B090F`，投影
  manifest SHA-256 为 `DF28C4A7A402DE6775328DA67FA122F33958716F789DDA5C23619C6178DEE543`。
  实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p7a-portfolio-event-localization-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `D1474A93474D95801922CD55C8C7A7FA1DA247414A53C49A218A9032E151E494`）。
- 使用约 **300 s bounded observer-only** 窗口；`16:51:46` 的 debug.log 记录
  `Total of : 881`，仍没有 `End loading of history` marker，也没有 observer 的
  `Frontend`/history readiness。`error.log` 为 **0 B**（SHA-256
  `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`）；
  debug.log SHA-256 为
  `AA64415B73B7A8DA19E6101145EF4D480DE228013FC42D89340595001F3D86CC`。
- **分类**：P7 的 localization/parser 错误已在静态与本轮日志层面修复（323 行
  missing-loc 不再出现），但 native loader boundary 仍 RED；observer 为
  `heartbeat_not_observed`（sample_count=0、无 UI/gameplay 输入），不能把
  `error.log=0` 或 `Total 881` 写成 CK3 启动/功能通过。native session cleanup
  **GREEN**、初始 CK3 PID `46436`，`tree_gone=true`、`cleanup_proven=true`，
  `runtime_unchanged=true`。
- P7a 是 disposable 诊断分支，不是新的固定阶段（正式 unified index 仍为
  checkpoints 00–07）；本轮未载入存档、未执行 gameplay、未点击协议/通知、未访问
  商店，也没有购买/付款动作。

### P7a 下一步

保留 P7/P7a 的错误收敛证据，下一项转向 native loader boundary（优先检查仍未
观测到的启动阶段/bridge 入口），并沿 checkpoint 依赖继续推进；P7a 的 strict
no-launch preflight 虽为 GREEN，但 `seed_ready=false`、`ck3_launch_attempted=false`
的前置结果不等于 live readiness。

### B7 materialize-only 实机结果（17:01–17:05）

- **B7-MATERIALIZE**：在修订 P6 的 74-file 投影上只替换 materialize 组两个
  m360 block（`zg361_we_materialize_m360_route_a_from_central_effect`、
  `zg361_we_materialize_m360_route_b_from_central_effect`；route_a/route_b 实现
  保持原样），共 **74 files / 19,590,855 B**。候选投影为
  `p6-portfolio-closure-b7-materialize-only`，product/runtime tree SHA-256 为
  `6E728D1CE477E6D32EBA46691A8C0C53C1390496AEDBC260C5BF729A0F6129B0`，manifest
  SHA-256 为 `6868591F24A583C0E2212F9E61567A78783755623CBDF3C6863CA5DFDAB508FA`。
  实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p6-portfolio-closure-b7-materialize-only-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `5FE7017F5ABAD8C01F502238EA35062F82D59DA693F2B0FB48715A32C515C3F2`）。
- 采用 **180 s bounded observer-only** 窗口；`17:03:26` 的 debug.log 记录
  `Total of : 881`，没有 `End loading of history` marker，也没有 observer 的
  `Frontend`/history readiness。`error.log` 为 **0 B**（SHA-256
  `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`）；
  debug.log SHA-256 为
  `F791AD30C4E7F5D6806BA6E287A5B983313FB79585BDA057F3E5F5C8FA08608A`。
- observer 仍为 `heartbeat_not_observed`（RED，`sample_count=0`、无 UI/gameplay
  输入）；native session cleanup **GREEN**、初始 CK3 PID `61948`，
  `tree_gone=true`、`cleanup_proven=true`，`runtime_unchanged=true`。因此，
  **仅替换 materialize 组（四块拆分中的该一组）仍未越过 loader boundary**；
  不能把该组单独判为唯一根因，下一步继续测试 route-only 互补分支，再按首失败
  递归细分。
- 本轮为 disposable 诊断投影，不进入正式产品树；未载入存档、未执行 gameplay、
  未点击协议/通知、未访问商店，也没有购买/付款动作。

### B7 materialize-only 下一步

保留本轮 `error.log=0`、`Total 881`/无 history 与 observer RED 证据，先完成
route-only 互补实测；之后按 materialize/route 内部 block 做更小粒度二分。每个
候选仍须先过 strict no-launch preflight，避免把诊断 stub 当成正式修复。

### B7 route-only 实机结果（17:05–17:09）

- **B7-ROUTE**：在修订 P6 的 74-file 投影上保留两个 materialize 定义，只将
  route 组两个 block（四块拆分中的 **317/318**）替换为同名 no-op stub：
  `zg361_we_m360_route_a_effect`、`zg361_we_m360_route_b_effect`。候选共
  **74 files / 19,883,952 B**；product/runtime tree SHA-256 为
  `075BE161D7C64FA7C2D468CB9C1E30C8B1EB8128282E7BECDBFB0D5FC515F5D5`，manifest
  SHA-256 为 `E18890B7A7C731C45BF500EBD39715605E5B46FF5B25A988FE4A7B82CFD325A1`。
  实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p6-portfolio-closure-b7-route-only-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `A22D3C4005798AC7AF750F3A56FE596FB1D67E0DB1A015C37DF37C72FD9CC815`）。
- 采用 **180 s bounded observer-only** 窗口；`17:07:40` 的 debug.log 仍记录
  `Total of : 881`，没有 `End loading of history` marker，也没有 observer 的
  `Frontend`/history readiness。`error.log` 为 **0 B**（SHA-256
  `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`）；
  debug.log SHA-256 为
  `1FFB91F91647D46DE2EF248505DA3EE3F414394B91DB31884BBDFA57BAD6B7D5`。
- observer 仍为 `heartbeat_not_observed`（RED，`sample_count=0`、无 UI/gameplay
  输入）；native session cleanup **GREEN**、初始 CK3 PID `7184`，
  `tree_gone=true`、`cleanup_proven=true`，`runtime_unchanged=true`。与
  materialize-only 互补结果合并看，单独替换 route 组也没有越过 loader boundary；
  因而不能把四块整体或 route 组直接定为唯一根因，下一步继续对两组内部做递归
  细分。
- 本轮为 disposable 诊断投影，不进入正式产品树；未载入存档、未执行 gameplay、
  未点击协议/通知、未访问商店，也没有购买/付款动作。

### B7 route-only 下一步

materialize-only 与 route-only 两个互补分支均保持 `error.log=0` 但停在
`Total 881`/无 history，说明需从四个 block 内部继续做更小粒度 A/B，而不是恢复
整份 workforce 文件。每个候选先过 strict no-launch preflight，再占用唯一 CK3 槽。

### Workforce exact-265 A+B 物理拆分实机结果（17:10–17:14）

- **Exact A+B diagnostic projection**：将单一 workforce monolith 物理拆成
  `zg361_workforce_endgame_runtime_effects_part_a.txt`（blocks 0–161）和
  `zg361_workforce_endgame_runtime_effects_part_b.txt`（blocks 162–323），其余
  文件保持不变，共 **265 files / 15,938,098 B**。两部分按规范化规则拼接后与
  原 4,636,271 B monolith 同源（normalized-concat SHA
  `926453FE4B3621B5381743D61F5D03AC29C1D498181702E05A9532739D334D8A`）。
  product/runtime tree SHA-256 为
  `AD7FBBAD979175C3F79445A8E480D1ACE3CA6EEFA263148295F0723586C56592`，manifest
  SHA-256 为 `519549D0EA74D260F9C92C750E77762ADCD59176BEDA8CC947B93E429DB62926`。
  实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-workforce-ab-exact-265-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `E32BC986BEE7B5EC7F69464014F1B9B0105146856F654EF03CAA6C8E125378EC`）。
- 采用 **180 s bounded observer-only** 窗口；`17:12:36` 的 debug.log 记录
  `Total of : 881`，仍没有 `End loading of history` marker，也没有 observer 的
  `Frontend`/history readiness。`error.log` 为 **0 B**（SHA-256
  `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`）；
  debug.log SHA-256 为
  `42B16790117625ADD05A9D4B6641D383D22A66D1A28D40E58D1117DBC30A372C`。
- observer 为 `heartbeat_not_observed`（RED，`sample_count=0`、无 UI/gameplay
  输入）；native session cleanup **GREEN**、初始 CK3 PID `17192`，
  `tree_gone=true`、`cleanup_proven=true`，`runtime_unchanged=true`。
  **结论：这是物理文件拆分的 A+B 对照，physical split no effect——拆分本身
  没有越过 loader boundary，也不能说明单体大小是根因。** 本轮仍是诊断证据，
  不进入正式产品树。
- 该 exact-265 A+B 是对既有 workforce checkpoint 的诊断变体，**不是新的固定
  batch/checkpoint**，不改变 13 个名义阶段、17 个固定 checkpoint 或 20–23 次
  条件性轮次口径；未载入存档、未执行 gameplay、未点击协议/通知、未访问商店，
  也没有购买/付款动作。

### Exact-265 下一步

物理拆分已排除“仅因文件是否拆成 A/B”这一变量；保留本轮 `Total 881`/无 history、
observer RED 与 cleanup GREEN 证据，继续沿 workforce effect 内部 block/依赖做
递归二分。每个候选先过 strict no-launch preflight，避免把物理拆分误报为修复。

### Workforce owner group-A stub 实机结果（17:17–17:21）

- **Group A disposable projection**：以修订 P6 的 74-file 投影为基线，仅把五个
  非 central owner 大文件的函数体替换为同名空 scripted-effect stub：
  `career_hc`、`career_learning`、`credit_project`、`feedback_promotion_pip`、
  `generated_compensation`。投影仍为 **74 files / 16,511,803 B**；五个文件共
  971 个顶层定义，名称全部保留。候选说明与差异表位于
  `Z:\ck3_mod_rewrite\_runtime\phase2-p6-owner-group-a-stub-20260903\README.md`
  和 `DIFF.md`，仅作诊断，未改 canonical。
- 产品树 SHA-256 为
  `6BDB3E82705FA70A809DED74FA1F98161D66A7B8A4652B25F51E5752CE5E9869`，projection
  manifest SHA-256 为
  `16DE79907C9156D28C5A93B100FF3DDFCFAE84B18B8C8800E27EE805D81B172F`；实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p6-owner-group-a-stub-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `19A7998BA3A11574D37FDB7926F731AED9C83CDFE2EBC7C1FC3C8EFC63325648`）。
- 采用 **180 s bounded observer-only** 窗口（CK3 session 实际约 185 s）；
  `17:19:34` 的 `debug.log` 记录 `Total of : 881`，没有
  `End loading of history` marker，也没有 observer 的 `Frontend`/history
  readiness。`error.log` 为 **0 B**（SHA-256
  `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`），
  debug.log SHA-256 为
  `18CE83D5C0B4F20CECE72CCFEEB5EDA37B49711872DFBEA42CEAA735B12B9128`。
- observer 结果为 `heartbeat_not_observed`（RED，`sample_count=0`、无 UI/gameplay
  输入）；native session cleanup **GREEN**（初始 CK3 PID `62548`，
  `tree_gone=true`、`cleanup_proven=true`、无残留）。因此 Group A 五个 owner
  文件整体置空仍未越过 loader boundary；这轮不能把该组判为唯一根因，也不能写成
  启动通过。未载入存档、未执行 gameplay、未点击协议/通知、未访问商店，也没有
  购买/付款动作。

### Group-A 后续计划（暂停 B）

按“更小批次”要求，Group B 暂不推进；下一候选改为 **P7a+CP-owner disposable**，
只在 P7a 投影上增加两个缺失的 court-position owner 文件（appointment 与 exit，
合计 **+2 files / +6,211 B**），先做 strict no-launch preflight，待实测后再更新
日报。该候选不改变统一的 13 个名义阶段、17 个固定 checkpoint 或 20–23 次条件性
轮次口径。

### P7a+CP-owner 最小增量实机结果（17:27–17:31）

- **P7a+CP-owner disposable projection**：在 P7a（216 files / 21,360,068 B）
  上只增加两份此前静态扫描确认缺失的 court-position owner 定义，未替换或删除
  其他文件：`common/court_positions/types/zg361_workforce_appointment_fact_court_positions.txt`
  （3,213 B，SHA-256 `B7E5D455DA319C9F1587FD6928C69FBE51CC22916FBE40C241851D715D9261BD`）
  与 `common/court_positions/types/zg361_workforce_exit_fact_court_positions.txt`
  （2,998 B，SHA-256 `AE7DDF10DF3CC36A4F8EF531F26703C9025D956007BE94F844D895EC80E8606C`）。
  合计 **218 files / 21,366,279 B**；两文件与 c91 clean source 逐字节一致，候选
  说明在 `Z:\ck3_mod_rewrite\_runtime\phase2-p7a-plus-cp-owner-20260903\README.md`
  和 `DIFF.md`。
- strict no-launch preflight 为 GREEN（artifact SHA-256
  `07FB696ABA03D61E11D28E073604907DF825D816892B8F70F7F05950C1DAE7D6`）。产品树
  SHA-256 为
  `CAC60E9DC48445AE6112C03BFBCC38BA0262210FCB63923F87598F8C28CDB203`，projection
  manifest SHA-256 为
  `5CCE89739D23026E59D5753CD21250245B5D5C0E33E3C6CF34F6CD5D29646472`；实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p7a-plus-cp-owner-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `625AC303B392D12564E96A2700392418034254C4DDAE7E90C12D2BDA93A6AB18`）。
- 采用 **180 s bounded observer-only** 窗口（session 约 183 s）；`17:29:43` 的
  `debug.log` 记录 `Total of : 881`，仍无 `End loading of history` marker，也无
  observer 的 `Frontend`/history readiness。`error.log` 为 **0 B**（SHA-256
  `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`），
  debug.log SHA-256 为
  `76A0F3401F0345394C1EC2DB7D9ACB6C704F00B75F7BA5057D6916A79C2C5E38`。
- observer 仍为 `heartbeat_not_observed`（RED，`sample_count=0`、无 UI/gameplay
  输入）；native session cleanup **GREEN**（初始 CK3 PID `25928`，
  `tree_gone=true`、`cleanup_proven=true`、无残留）。因此补上两个 owner 文件后
  仍未越过 native loader boundary；该最小增量不能判定为启动通过，也不能把
  Group-A/B 或文件体积定为唯一根因。未载入存档、未执行 gameplay、未点击协议/通知、
  未访问商店，也没有购买/付款动作。

### P7a+CP-owner 后续

本轮已完成“+2 文件”最小增量实测，结果仍为 `Total 881`/无 history、observer RED；
按当前调度先保留该失败证据，Group B 继续暂停，不恢复大块替换。CP appointment/exit
两个单文件目前仅完成 strict no-launch preflight，暂不写成下一项 live。当前实际序列
已转入 P7-5 remediation-chain 整组后的 **remediation effect-only** 单文件变体；
待其收口后，再按结果决定是否测 event-only。

### P7-5 remediation-chain 微批次实机结果（17:45–17:51）

- **P7-5 `p7-5-remediation-chain` disposable projection**：从已知可启动的
  `core-current` 51-file 基线（7,164,060 B）出发，只增加一条 workforce
  remediation 事件链及其 3 个本地化 owner，共增加 5 个文件 / 40,505 B，投影
  共 **56 files / 7,204,565 B**。新增项为
  `zg361_workforce_remediation_fact_effects.txt`、对应 events，以及 English、
  简中、French 三份 localization；候选说明在
  `Z:\ck3_mod_rewrite\_runtime\phase2-p7-5-remediation-chain-20260903\README.md`
  与 `DIFF.md`，仅作诊断，未改 canonical。
- strict preflight 为 GREEN（SHA-256
  `FCABE8D25F9BF34F1434203050BAE33599C9FE49ADD6C223768AB607EFC8FB60`）。产品树
  SHA-256 为
  `DFBFB1BE1E74B86DCAB825F75A253188FC1787CA7BABDC5CF10C1DC52E1D72FE`，projection
  manifest SHA-256 为
  `6A91B1E9ACEC1A3F89423EA7FC28D34D8AA77D41C97F76E24DD7F2CB44E7ACAE`；实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p7-5-remediation-chain-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `AAFE9A197A05206E49018F3E839E0CD33B610DC622ADDA2D21627F325117F90D`）。
- 采用 **180 s bounded observer-only**（session 约 210 s）；本轮不是 parser
  error：`error.log` 为 **0 B**（SHA-256
  `E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`）。
  `debug.log` 在 `17:46:14` 停在 GUI asset loading 的截断末行，未出现
  `End loading of history`、`Frontend`/history readiness 或 `Total of : 881`；
  debug.log SHA-256 为
  `8BBBD7CEA83BB6194F97704545C750117AFDBC798E843E2CF1FF038EA812E7CE`。
- native observer 为 `heartbeat_not_observed`（RED，`sample_count=0`、无 UI/gameplay
  输入），本轮结论标为 **`LOAD_BOUNDARY_RED (observer-only)`**：微批次在 GUI
  加载边界停住，不能据此归因于 remediation 链、文件体积或完整 Workforce。
  native session cleanup **GREEN**（初始 CK3 PID `7092`，`tree_gone=true`、
  `cleanup_proven=true`、无残留）。未载入存档、未执行 gameplay、未点击协议/通知、
  未访问商店，也没有购买/付款动作。

### P7-5 后续

该 56-file 微批次只提供 `LOAD_BOUNDARY_RED` 证据，不改变固定 checkpoint 口径；
P7-5 remediation-chain 整组已收口；当前正在测其 **remediation effect-only** 单文件
变体（不是 CP appointment/exit）。待 effect-only 收口后，再按需要测 event-only；CP
appointment/exit 目前仅有 preflight 证据，Group B 仍暂停，不占用下一 live 槽。

### P7-5 remediation effect-only 单文件实机结果（17:56–18:00）

- **Effect-only disposable projection**：从已知可启动的 `core-current` 51-file
  基线出发，仅增加 `common/scripted_effects/zg361_workforce_remediation_fact_effects.txt`
  （36,465 B）；事件文件及其 localization owner 故意不挂载，用来隔离单文件的
  注册/解析影响。投影共 **52 files / 7,200,525 B**，候选说明位于
  `Z:\ck3_mod_rewrite\_runtime\phase2-p7-5-remediation-effect-only-20260903\README.md`
  与 `DIFF.md`，未改 canonical。
- strict no-launch preflight 为 GREEN（SHA-256
  `24FFDE9AECF9C30757F2E7B7792BF8D8C1C7B526657D74E1220385131BBC37C6`）。产品树
  SHA-256 为
  `37DFBEEF55E50DCA06F0A2276EF442E4A2021732C3758A8D05BB97618425AA33`，projection
  manifest SHA-256 为
  `704E137D8476D85F0EA5B86C75D4DD4CAA3F32286E4AC8C4EA9E7347840741D5`；实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p7-5-remediation-effect-only-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `0DCA98D77CDA7DDAE74C0C48AAC4DB962CE9E16DF3BA9B6960575E06F924C26B`）。
- 采用 **180 s bounded observer-only**（session 约 187 s）；`17:59:02` 的
  `debug.log` 记录 `Total of : 881`，并在 `17:59:22` 出现
  `End loading of history`。这说明本候选已越过 native loader/history 边界，不能
  再归类为“停在 Total881 无 history”。但该最小基线预期会产生缺失后续依赖的
  解析/级联错误：`error.log` **53,019 B**（SHA-256
  `85FEC3420F5434A7A176999CB1ABE013A43B3938BF93B89440656645D615CE79`），不是
  clean production load；debug.log SHA-256 为
  `952E810E555EA463104072E8674ABB520E78C53759FDDDE73E69CA97764E690E`。
- runner overall 为 `RED`，原因仅为 native observer
  `heartbeat_not_observed`（`sample_count=0`、无 UI/gameplay 输入）；应分类为
  **loader boundary crossed / observer RED**，而非 capability GREEN。native session
  cleanup **GREEN**（初始 CK3 PID `35324`，`tree_gone=true`、`cleanup_proven=true`、
  无残留）。未载入存档、未执行 gameplay、未点击协议/通知、未访问商店，也没有
  购买/付款动作。

### remediation effect-only 后续

该单文件已证明其本身不会阻止到达 `End loading of history`；保留其预期依赖错误与
observer RED 证据。随后 event-only 也已完成对照（详见下节），因此两种单文件结果
均已收口。CP appointment/exit 仍只有 strict preflight，Group B 继续暂停；任何下一
轮先过 no-launch preflight，不要把 observer RED 或缺依赖日志误报成完整产品已通过。

### P7-5 remediation event-only 单文件实机结果（18:01–18:05）

- **Event-only disposable projection**：从已知可启动的 `core-current` 51-file
  基线出发，仅增加 `events/zg361_workforce_remediation_fact_events.txt`
  （2,427 B）；scripted-effect 与 localization owner 故意不挂载，用来隔离事件
  注册/解析影响。投影共 **52 files / 7,166,487 B**，候选说明位于
  `Z:\ck3_mod_rewrite\_runtime\phase2-p7-5-remediation-event-only-20260903\README.md`
  与 `DIFF.md`，未改 canonical。
- strict no-launch preflight 为 GREEN（SHA-256
  `70D156AFF2E6CB95AF6B9C87DC5AA380D24F538C79AAEF9106D765D33AD0F8BF`）。产品树
  SHA-256 为
  `6EF855B5F380356F27AB1D9E1B3645ED31384B089BB099835C98FEFD923D33A9`，projection
  manifest SHA-256 为
  `6CEBC98372B4AA2669AFDB36EA2B5A7845F470C00DEDFB252FDE13469724DC28`；实机报告：
  `Z:\ck3_mod_rewrite\_runtime\formal-phase2-p7-5-remediation-event-only-c91-20260903-r1\artifacts\runner-report.json`
  （report SHA-256
  `8CC63AC94AC56893B2D7FC22419D9519A0E387E55C7E9442FB646C03E0C431C9`）。
- 采用 **180 s bounded observer-only**（session 约 186 s）；`18:03:00` 的
  `debug.log` 记录 `Total of : 881`，并在 `18:03:14` 出现
  `End loading of history`。因此该单文件也越过 native loader/history 边界；但
  因 effect/localization owner 有意缺失，`error.log` 仍有预期的缺依赖级联，
  共 **36,757 B**（SHA-256
  `D78396F4A18642690D15D5643AA0185FCD290AC7D63E1D3D8B8070B29912803A`），不是
  clean production load；debug.log SHA-256 为
  `3CC83DD7025D2F576736FDF88F4494CCFE8029FBBD0AF342B416154EF848B0FC`。
- runner overall 为 `RED`，原因仍为 native observer
  `heartbeat_not_observed`（`sample_count=0`、无 UI/gameplay 输入）；应分类为
  **loader boundary crossed / observer RED**。native session cleanup **GREEN**
  （初始 CK3 PID `23816`，`tree_gone=true`、`cleanup_proven=true`、无残留）。
  未载入存档、未执行 gameplay、未点击协议/通知、未访问商店，也没有购买/付款动作。

### remediation 单文件对照后续

Effect-only 与 event-only 均已在各自缺依赖的诊断基线中到达
`End loading of history`，说明这两个单文件本身都不是阻止 loader/history 的充分
条件；两轮的预期级联错误与 observer RED 均保留。当前不重复启动这两轮，Group B
继续暂停；CP appointment/exit 仍仅有 strict preflight，下一 live 候选待协调者按差异
信息量调度。

### P1-minimal closed kernel full-entry primitive 实机结果（18:43–18:48）

- **r3 patched probe** 使用 `p1-minimal-closed-kernel` 投影（candidate **53 files /
  7,313,274 B**，tree SHA-256
  `BDB8D89162C1C4B01058A3987FE916BD0DDA16CBD67EDABB412ED9E3F0FD7265`）。正式报告为
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-minimal-full-entry-20260903-r3\report.json`
  （report SHA-256
  `AF6F671FFAB9CC38F96EA9446A276D81362274B75E813C403F3BEA51185BA883`），运行
  `300.656 s`。入口字段 `main_menu`、`bookmark_selected`、`start_clicked`、
  `gameplay_hud`、`map_rendered`、`paused` 全为 `true`；日志 marker 依次覆盖
  `End loading of history`（18:47:29）→ `Frontend`（18:47:30）→ `Bookmark`
  （18:48:03）→ `Completed generating game state from bookmark task`（18:48:11）→
  `Load Save`（18:48:11）→ `In Game`（18:48:14）。
- 地图/暂停截图为
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-minimal-full-entry-20260903-r3\complete_entry_map.png`，
  SHA-256 `FCE77CF853CD68FBA4461A84000694CBCE66FEA4DB73EA3D552A706D41818F3B`。
  `material_error_lines` 为 0；debug.log（890,926 B，SHA-256
  `8B4473AC551DCEEBE37EC56D4180DBF9F6038C7792A6CF370C750946EA3D6952`）、
  game.log（496 B，SHA-256
  `6A96CADCFF7B6D48F14ED8E57E4D36B0E7A04692A26D47CD4C83E6D5A8C2CA3C`）与
  error.log（201,348 B，SHA-256
  `ECF2685E929A7ED9AD2A5D5C2A0AA810D07DD9F02C6136DE87BC19A6197FCEB7`）均留存；
  error.log 中的 840 条 unused-variable 诊断均未被 material classifier 计入；这些非
  material 诊断仍不能被误写成 clean production load。
- 结果为 **GREEN / minimal full-entry primitive**，CK3 已退出（`ck3_running_after=false`），
  隔离 userdir 保留供复核；本轮证明该 53-file 诊断投影可以完成一次真实主菜单→开局→
  地图暂停入口，但不等于 Phase2 全功能或双片素材 readiness。后续仍须把 51/52-file
  history-boundary 与该 53-file full-entry 做矩阵对照，再继续增量定位；不能据此把全局
  Phase2 标成 complete。
- 前两次不是候选内容结论：r1 报告
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-minimal-full-entry-20260903\report.json`
  （SHA-256 `7324A47441C6F910EA246122DB960658B07E6823A0E67F52A484BB169466241F`）仅
  运行 3.351 s，因 probe 未绑定已安装游戏规则路径而报
  `vanilla game-rule declarations not found`，CK3 未启动，分类为 **harness-invalid**；
  r2 仅留下 loading screenshot
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-minimal-full-entry-20260903-r2\live_screen.png`
  （SHA-256 `0ECEEEE45D99850DB746AF222BF4D6DFCEEC11B8BE20F809591259EA1A085B07`），
  没有 `report.json`，旧 profile 硬编码未定义的 `xar_selftest/xar_inherit_100/xar_score_growth`
  并出现 failed key reference，分类为 **harness-invalid/incomplete**；二者均不得计为
  候选 RED。

### P1-minimal full-entry 后续

保留 r3 的 minimal full-entry primitive 证据，下一步先完成 51/52/53-file 矩阵与
`full-entry` gate 的同口径复核（paused、map-ready、alive played character、cleanup），
再按首个新增差异继续 disposable 增量；不把 53-file primitive 当作完整 Workforce、
Phase2 全功能或最终宣传片素材。

### 18:55 51/52/53 文件矩阵口径

矩阵复核只整理既有投影与实测，不新增 CK3 启动槽：51-file legacy 基线为
`7,137,587 B`（tree `ddac4703d99b7e498e276c37c685af28b2006ad73f4124f9cd77e745aa14a693`）；
51-file core-current 为 `7,164,060 B`（tree `b6d3ac4c…`）；52-file event-only 为
`7,166,487 B`（tree `6ef855b5…`）；52-file effect-only 为 `7,200,525 B`
（tree `37dfbeef…`）；53-file case-kernel 为 `7,313,274 B`（source tree
`f3edcb560e07f14967c73908b8558d252bd5d134ca7e3095d3a3dad72df37a7d`，formal
projection tree `bdb8d89162c1c4b01058a3987fe916bd0dda16cbd67edabb412ed9e3f0fd7265`）。
其中只有 53-file r3 已取得 full-entry GREEN；51/52 的既有证据仍分别是基线或
history-boundary/observer-only 诊断，不把字节数或文件数直接当作因果证明。除非后续
协调者明确要求 full-entry 重跑，否则矩阵本身不再占用 CK3 槽。

### baseline51 实机复测收口（18:57–19:02）

- 按同一 patched visual probe 对 51-file legacy 投影
  `Z:\ck3_mod_rewrite\_runtime\phase2-bisect-source-legacy51-20260903\mod_zhongguo_style`
  启动了 `phase2-minimal-full-entry-20260903-baseline51`（51 files / 7,137,587 B，
  timeout 1200 s）。CK3 于 18:57:53 启动，19:02:12 的 `debug.log` 出现
  `End loading of history`，但在进入主菜单/选人/地图完整入口前按当前调度被有意回收。
- 本轮 artifacts 目录没有生成 `report.json` 或 full-entry 截图；因此结论严格记为
  **HARNESS INTERRUPTED / INCONCLUSIVE**，不能写成候选 RED，也不能与 53-file r3
  的 full-entry GREEN 直接比较。userdir 中 debug.log 328,725 B（SHA-256
  `1F6F4F3070F385BDC346866A2CEFEF70F8641A476AC469F72B2AF65774B45E5A`），error.log
  976 B（SHA-256
  `FCAB24931889617C1AA853C160BCCF106F3CEE7C2717319B0BA294750B53232F`，仅 2 条
  unused-variable 诊断）；这些日志仅证明到达 history boundary。
- 当前范围已调整为**不再重跑 baseline51 full-entry**；保留本轮中断证据，继续按更小的
  Phase2 增量/功能闭包推进，并沿用 53-file primitive 作为已证明的 full-entry 参考基线。

### 18:35 history-boundary 与 full-entry gate 口径补充

- 对照复核确认：P7-5 effect-only 的 `debug.log` 在 `17:59:02` 记录
  `Total of : 881`、`17:59:22` 出现 `End loading of history`；event-only 分别为
  `18:03:00` 与 `18:03:14`。两轮因此可记为 **history-boundary reached**，但 runner
  仍为 `heartbeat_not_observed`/`sample_count=0`，`semantic_state_available=false`，
  且没有 Bookmark、game-state、In Game、map-ready 等信号；不能把它们写成完整进入或
  capability GREEN。该 observer-only 采样器只覆盖 wrapper/consumer heartbeat，
  `sample_count=0` 本身不等同于 CK3 启动失败。
- 新建的 `phase2-minimal-closed-kernel-20260903` 与
  `full-entry-contract-preflight-none-20260903` 目前都只是 strict no-launch
  preflight：结果 GREEN、`ck3_launch_attempted=false`、`seed_ready=false`；前者为
  `p1-minimal-closed-kernel` 53 files / 7,313,274 B（preflight SHA
  `F8BC88F61BAF5062A597E496EC58186BA6615E26F084AD8645ED9AA6CEEA5775`），后者为
  `none` 51 files / 7,137,587 B（preflight SHA
  `8FAD0750DBF97C4CAD79E7B7DCF7DBF96EC10363C41F06A70613B07986E64E80`）。两者均未
  启动 CK3，不能替代 live readiness；open_kaishek optional validator 的 RED 仍是已知
  工具边界，不作为 CK3 loader 结论。

### 19:15 safe-core ABI 增量 strict preflight（未启动 CK3）

- 在已实测的 53-file case-kernel primitive 上构造 exact **55-file / 7,325,646 B**
  `phase2-next-increment-safe-core-20260903` disposable projection；这是 current ABI
  增量（两份新增 runtime-value 文件，并更新两份既有 values/triggers 文件），不是新的
  固定 checkpoint。候选产品树位于
  `Z:\ck3_mod_rewrite\_root-promo-split-20260902\_runtime\phase2-next-increment-safe-core-20260903\product`。
- strict no-launch preflight 报告：
  `Z:\ck3_mod_rewrite\_root-promo-split-20260902\_runtime\phase2-next-increment-safe-core-20260903\preflight.json`，
  最终 SHA-256 `0373FE0BED32C1A63CC7B23BD251A8084C7405F3EDC769C0E029B098CAFF6D39`；
  `ck3_launch_attempted=false`、`launch_boundary=not-crossed`、native session 未启动。
- 静态闭包结果：定义总数 2,460、new 8、missing refs `[]`；49 份 text/script 的 UTF-8 BOM、
  括号、materializer 与 projection tests **11/11 GREEN**。四个 ABI 变更行分别为 triggers 4,497 B、values
  12,071 B、incident runtime values 6,427 B、manager runtime values 3,522 B。复核确认
  current values 已包含此前担心的 12 项旧 scalar defs，不需额外合并；仅 3 个旧 self-contained
  scalar 因 c91 删除且候选无引用，未把它们误报为缺失依赖。
- source/tree SHA `DB4DF6B93570E274F25B0A134A58778A4AFDE9F6EAED6675C50AEFB622166A00`，
  formal tree SHA `3D96D527D0486944492010DB05DDC4EB50C7BB3777DFCCDE4509CDE4E48659B7`，
  file-list SHA `136F935939687E2823B93E688FE3231355647073621A0110029657E165F0C907`，
  manifest SHA `FFF67D71B374DFE2DE2D64D8512213251761E485E0D3E901A1D4DF339EDF9D75`。
- **当前结论：static-ready / live pending**。本轮不启动 CK3，也不把 preflight 或 55-file
  体积变化写成已进入地图；待协调者按 CK3 独占槽决定是否对该 exact projection 做一次
  bounded full-entry 实测。

### 19:25–19:37 safe-core ABI 增量实机结果

- 沿用上述 exact `p2-safe-core-abi-20260903` projection 做一次 bounded full-entry
  实测；候选为 **55 files / 7,325,646 B**，tree SHA
  `3D96D527D0486944492010DB05DDC4EB50C7BB3777DFCCDE4509CDE4E48659B7`。实测报告：
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-next-increment-safe-core-full-entry-20260903\report.json`，
  SHA-256 `00DB2B83503F6AE66216925897E4394C5AFC32300602DFA9ED6A46700BD3D422`。probe
  起始 `19:25:53.668`，report/回收结束 `19:37:21.755`；候选 Mounted Data marker 为
  `19:26:15`。
- 运行耗时 **688.087 s**（约 11 分 28 秒），明确说明该投影不能用 180 s 作为充分超时
  假设。报告中的 `main_menu`、`bookmark_selected`、`start_clicked`、`gameplay_hud`、
  `map_rendered`、`paused`、`candidate_mounted` 与 `game_state_ready` 全为 `true`。
  日志链为：`End loading of history` 19:34:50 → `Frontend` 19:35:27 → `Bookmark`
  19:36:48 → `Completed generating game state from bookmark task` 19:36:57 →
  `Setting idler 'In Game'` 19:37:00。
- 地图证据为
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-next-increment-safe-core-full-entry-20260903\complete_entry_map.png`，
  SHA-256 `91F61D2107B6B06BD4B8224A73F4F2D02FAA59FB03E8A03C0819A61456D2E4BE`；候选
  Mounted Data 路径也由报告记录。`material_error_lines=[]`，但 userdir 的 error.log
  仍保留 840 条 unused-variable 与 144 条 unset-variable 的非阻塞 jomini_effect
  诊断，不能把“无 material error”误写成零日志。
- 结果：**GREEN / current safe-core full-entry primitive**；CK3 已退出，cleanup
  `ck3_running_after=false`、userdir retained，未留进程残留。本轮只验证 ABI 增量能够
  到达真实暂停地图入口，不等于完整 Workforce/Phase2 功能、native paused snapshot、
  事件/存档闭环或宣传片素材 readiness；也未执行协议以外的购买/付款动作。

### safe-core 后续

55-file safe-core 现可作为下一轮小批次的实机基线；后续按单文件/单功能闭包继续追加，
每次先做 strict no-launch preflight，再占用 CK3 独占槽，避免把 full-entry primitive
误报为 Phase2 全量完成。

### 19:40 滚动批次耗时统计（截至 safe-core）

为避免把不同 harness 的时间混在一起，表中“总时长”统一指该批 probe/report 的 wall
start→结束；“启动→history/首 marker”优先用 CK3 启动日志到 `End loading of history`，
没有 history 时记录到首个可比 `Total of : 881` 或 GUI stop，并显式标注。 “选人后”只在
真正取得 Bookmark→`In Game` 的 full-entry 批次填写。P2 的总时长由 debug.log 首行到
report finish 推导；r1/r2 harness-invalid 与 baseline51 有意中断不纳入均值/中位数。

| 批次 | 总时长 (s) | 启动→history/首 marker (s) | 选人后 (s) | 结果 |
|---|---:|---:|---:|---|
| core-current 51 | 214.185 | 81.3 / history | — | history-boundary，observer RED |
| P1 r4 53 | 217.442 | 84.1 / history | — | history-boundary，observer RED |
| P2 B2 closure 64 | 193.517 | 49.0 / history* | — | history-boundary，wrapper RED |
| P3 B1/B2 closure 77 | 223.823 | 131.1 / history | — | history-boundary，observer RED |
| P4 callable 66 | 211.323 | 71.4 / Total881 | — | Total881，无 history |
| m275 stub 67 | 211.230 | 71.3 / Total881 | — | Total881，无 history |
| P6 central 68 | 210.229 | 68.7 / Total881 | — | Total881，无 history |
| P6 portfolio 74 | 214.547 | 75.8 / Total881 | — | Total881，无 history |
| P7 portfolio/event 99 | 346.680 | 95.1 / Total881 | — | Total881，无 history |
| B7 四块 stub 74 | 335.113 | 75.7 / Total881 | — | Total881，无 history |
| P7a localization 216 | 332.557 | 72.1 / Total881 | — | Total881，无 history |
| B7 materialize-only 74 | 212.413 | 71.8 / Total881 | — | Total881，无 history |
| B7 route-only 74 | 220.820 | 104.3 / Total881 | — | Total881，无 history |
| exact-265 A+B 265 | 240.578 | 122.0 / Total881 | — | Total881，无 history |
| Group-A owner stub 74 | 237.683 | 140.8 / Total881 | — | Total881，无 history |
| P7a+CP-owner 218 | 242.907 | 158.4 / Total881 | — | Total881，无 history |
| P7-5 remediation chain 56 | 371.186 | 87.8 / GUI stop | — | LOAD_BOUNDARY_RED |
| remediation effect-only 52 | 272.055 | 252.3 / history | — | history-boundary，observer RED |
| remediation event-only 52 | 245.395 | 167.3 / history | — | history-boundary，observer RED |
| P1 r3 visual full-entry 53 | 300.656 | 236.8 / history | 11.0 | **GREEN full-entry primitive** |
| safe-core ABI full-entry 55 | 688.087 | 536.3 / history | 12.0 | **GREEN full-entry primitive** |
| baseline51（中断） | 259.0 observed | 259.0 / history | — | HARNESS INTERRUPTED，排除统计 |

`*` P2 的报告外层字段不完整，表中总时长按其 debug.log 首行 `15:44:33` 至 report
finish `15:47:46.517` 推导（约 193.517 s），并保留其 history marker 证据；这不是把
外层 wrapper RED 改写成 capability GREEN。

滚动统计（不含 harness-invalid 与 baseline 中断）：diagnostic/observer 样本 `n=19`，
总时长均值 **250.194 s**、中位数 **223.823 s**、范围 **193.517–371.186 s**；
已取得 full-entry 的视觉样本 `n=2`，均值/中位数 **494.372 s**、范围
**300.656–688.087 s**。合并 21 个完整 wall 样本则均值 **273.449 s**、中位数
**237.683 s**、范围 **193.517–688.087 s**。有 history marker 的 8 个样本中，
启动→history 均值 **192.285 s**、中位数 **149.178 s**、范围 **49.0–536.3 s**；
full-entry 的 Bookmark→In Game 仅 2 个样本，均值/中位数 **11.5 s**、范围 **11–12 s**。
这些统计只描述当前 runner/profile 与投影组合的观测耗时，不把时长单独解释为文件体积
因果；safe-core 的 688.087 s 也不代表后续每批必然需要同样窗口。

### 19:45–19:54 B1 三文件闭包 strict preflight（未启动 CK3）

- 以已实测 safe-core 55-file 为基线，构造简中 B1 最小真实闭包：新增
  `common/scripted_effects/zg361_b1_runtime_effects.txt`（495,777 B，SHA
  `CDB388005FFEAC6D332380E910FBBF929F49871047E118D047C63B8751C001B4`）、
  `events/zg361_b1_runtime_events.txt`（29,639 B，SHA
  `6576EA63F654D2321620A026471390F147B90719D98F69FEEA34F4C5779E8543`）及简中
  localization（7,136 B，SHA
  `00320692C35E9ED6DDB0D02F9D1D988876F22AA72B966A3247EFD87F9ADAC938`）。候选
  产品为 **58 files / 7,858,198 B**，路径
  `Z:\ck3_mod_rewrite\_root-promo-split-20260902\_runtime\phase2-next-increment-b1-20260903\product`。
- strict preflight：
  `Z:\ck3_mod_rewrite\_root-promo-split-20260902\_runtime\phase2-next-increment-b1-20260903\preflight.json`，
  SHA-256 `F9901D5971795EBE577D2F4E722CABC482E025907CD5BE4EAB3113DF4F0956A6`；
  status `GREEN_STATIC`、`ck3_launch=NOT_RUN`。projection manifest SHA
  `20346BA3EDA3218BBEBCE3252ED955C0CA4ED4AD28E8D182681E2C12608E6359`，source/tree
  SHA `E4EE07DE11317DBE12D44DD6659A4010CC5255CA2B9A9C858C423BB82FBEC92D`，formal
  tree SHA `F171B3CA307A9CD35E5F2BC7E9E2785D4EA0C31D0DE6DBCE342E624A4A9D8A3D`、
  file-list SHA `393DB845CAA5F8B16ECD4C379CD86F84F12257A8FA6D9BD0A6B2EBD9C110BF9F`。
- changed callable closure（87 refs）、event closure（15 defs/2 refs）、简中 loc
  closure（18/18）均 `missing=[]`；52 份 text/script BOM 与括号 GREEN，generator
  check、B1 单测 **56**、projection tests **11** 全部 GREEN。broad symbol scan
  仍仅为 diagnostic（4,390 unresolved，包含变量/native/localization token），不
  当作 parser-clean。
- **当前结论：static-ready / live pending**。这是 safe-core 后的三文件 B1 增量，
  不是完整 B1/B2/central 产品或新固定阶段；待协调者按 CK3 独占槽进行一次 bounded
  实机验证。此前 top-level `-r1` 旧变体（7,858,478 B）不作为本轮 live 身份。

### 20:05 safe-core 与 P1-r3 耗时差异 forensic 补录

- 对两次已通过 full-entry gate 的同类 visual probe 做只读时序对照：P1-r3（53 files）
  的 CK3 engine startup 为 `246.185372 s`，safe-core（55 files）为 `595.536113 s`，
  增量 **+349.350741 s**；probe/report wall 时长增量为 **+387.431 s**（日报表中分别
  为 300.656 s 与 688.087 s）。safe-core 仅比 r3 多 `12,372 B`（约 **+0.169%**），
  因而“文件体积小幅增加但等待显著变长”值得作为后续单变量 A/B 的观测线索，不能直接
  写成体积导致的因果结论。
- 分段对照显示：mount→history 约 **+222 s**（225→447 s），history 段约 **+62 s**
  （6→68 s），history-end→Frontend **+36 s**（1→37 s），Frontend→Bookmark **+48 s**
  （33→81 s），Bookmark→In Game 仅 **+1 s**。静默区间中 GUI 约 139→216 s（+77 s），
  post-on_action→最后 DB/history 约 19→152 s（+133 s）；数据库自身累计只多约
  **23.934 s**。本段数字是日志切片的比较值，不能替代完整 runner 报告。
- safe-core 相比 r3 多出的可见错误主要是 **144 条 unset-variable diagnostics**（72 个
  distinct 变量各出现两次，发生在 `19:31:10`/`19:34:50`）；debug/event/texture/thread
  配置未见其他对应差异，且 material-error 分类仍为空。当前没有 `resource.log` 的内存、
  profile 或系统资源证据，故性能异常仍标记 **原因未查明 / 不撤销 safe-core full-entry primitive GREEN**；下一步
  维持单功能、单文件增量并复用同一 probe，优先验证这些深层 `ip`/`mg` 值引用是否带来
  可重复的加载时序变化。

### 19:54–20:14 B1 三文件闭包 full-entry 实机结果

- 使用上一节 `GREEN_STATIC` 的简中 B1 三文件闭包（**58 files / 7,858,198 B**；候选
  tree/formal SHA `F171B3CA307A9CD35E5F2BC7E9E2785D4EA0C31D0DE6DBCE342E624A4A9D8A3D`）
  占用一次 CK3 独占槽做 bounded visual probe。报告位于
  `Z:\ck3_mod_rewrite_process_assets\zg361\phase2-next-increment-b1-closed-zh-full-entry-20260903\report.json`，
  SHA-256 `41942EFCB595B5A1B134156075F9B454A40DA38700735986F42831A9CBC5D650`；probe
  起始 `19:54:30.955`，耗时 **1,205.343 s**（约 20 分 5 秒）。
- runner 在前置菜单阶段等待中文 OCR 文本“新游戏”超时：`RunnerError: OCR timeout
  waiting for 新游戏`。报告中 `main_menu`、`bookmark_selected`、`start_clicked`、
  `gameplay_hud`、`map_rendered`、`paused`、`candidate_mounted`、`game_state_ready`
  均为 `false`，markers 为空；没有进入 Frontend/history/full-entry。超时画面
  `timeout_01_main_menu.png`（SHA `809879FCD5F45B31F96115065A2F99B3F33D7FAB929E2C76EE5CA7C58CBC624A`）
  仍是 CK3 加载画面，不能据此判定功能代码已失败。
- 清理结果 `ck3_running_after=false`、userdir 保留，未留 CK3 进程。该 userdir 的
  `error.log` 仅有变量 set/unset 类 jomini 诊断（无 parser/unknown-effect 等 material
  pattern；但末尾另有两个 list-target 诊断，material 分类仍为 0；本轮 `debug.log` SHA
  `C7BE685C24567B49419777E2AA18DE35DA8849CD6C4963483FE046C5D63451DE`、`error.log` SHA
  `3B96FDA043FCD18B502010F5677958D90B2C03B92B7E6EB9B4FCEA670A7B13C3`）。因此本轮分类为
  **HARNESS / PRE-MENU OCR RED**；它没有给出足以判定候选内容或 parser 成败的入口证据，
  同时也不能写成 B1 live GREEN。后续主线已被 22:39 后的完整块拆分矩阵取代，不再原样重跑。
- 交接与边界索引： [2026-09-02 交接文档](../../handover/2026-09-02-shift-handoff.md)，
  [Phase2 小域闭包说明](../../phase2-promo/phase2-safe-core-next-domain-closure-2026-09-03.md)。

### 22:39–23:42 B1 effect 完整块二分与语义等价拆文件实机矩阵

- 接班后没有重跑已证明的 53/55 文件基线，也没有覆盖旧 B1 RED。新增
  `tools/phase2_b1_effect_bisect.py` 与回归测试：精确绑定原 B1 effect/event/简中 loc
  SHA，按 77 个顶层括号完整块生成 disposable projection，保留名称表并验证 callable、
  event、loc、BOM/括号、projection replay；`7/7` tests GREEN。
- 所有候选均使用 CK3 `1.19.0.6`、EXE SHA
  `2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86`、probe SHA
  `F7DC208BF69F1B2E1F2BDAB81F718196493A15098378D5E6789123435655E333` 和独立冷
  userdir；每轮依次占用唯一 CK3 槽并自然清理。

| 候选 | effect 结构 | 候选规模 | full-entry | report SHA |
|---|---|---:|---:|---|
| all-stub r2 | 0 real / 77 stubs | 58 files / 7,376,850 B | **GREEN / 255.113 s** | `C08C84E6…43FA` |
| left-real | real 0–40 | 58 / 7,621,665 B | **GREEN / 180.403 s** | `130B7B6D…D5CD` |
| right-real | real 41–76 | 58 / 7,613,383 B | **GREEN / 178.968 s** | `0F4B8C00…D8BE` |
| event-root closure | 61 real / 16 stubs | 58 / 7,747,270 B | **GREEN / 181.360 s** | `5DADA085…C33F` |
| closure + excluded A | 70 real / 7 stubs | 58 / 7,801,741 B | **GREEN / 193.588 s** | `17382699…0538` |
| closure + excluded B | 68 real / 9 stubs | 58 / 7,803,727 B | **GREEN / 184.817 s** | `7BE948D9…E1E7` |
| all-but-76 | 76 real / only #76 stub | 58 / 7,829,342 B | **GREEN / 171.228 s** | `AF250EEB…F9EA` |
| balanced-files | **77/77 原始正文、0 stub；41/36 块分两文件** | 59 / 7,858,264 B | **GREEN / 180.396 s** | `CB5CB519…337F` |

- 八轮的 `main_menu/bookmark/start/HUD/map/paused/candidate_mounted/game_state_ready`
  均为 true，三类 game-state marker 齐全，`material_error_lines=[]`，cleanup 均为
  `ck3_running_after=false`；原始报告、地图、日志与静态哈希已追加到
  `Z:\ck3_mod_rewrite\_runtime\phase2-evidence-index-20260903.{json,md}`。
- `balanced-files` 两个 effect part 为 255,134 / 240,709 B，77 个定义正文逐块与原
  495,777 B 文件 byte-identical；这给出可实施的生成器拆文件候选。它与七个 stub
  子集的 GREEN 只提升启动归因精度：未改写的单文件 full B1 仍只有一次
  **1205.343 s pre-menu OCR RED**，不能据此把文件尺寸、#76 或某一半区写成唯一根因，
  也不能把 B1 delayed-path 语义或完整 Phase2 写成 GREEN。
- open_kaishek `15ab978f879ed4562aacb74dacdaee702fbce54b` 只用于候选 effect 单文件
  offline parser smoke；所有真实正文文件 parser 0 诊断，validator 的
  `UNKNOWN_OPCODE` 与 IR/runtime SKIPPED 是覆盖边界，不是 CK3 verdict。all-stub
  validator GREEN 也不认证 B1 语义。
- readiness 未晋级：Phase2 footage 仍为 **0/8**，人物版与制度群像版 MP4 均未生成；
  seed、paused native snapshot、observer heartbeat 与 production-live loop 均未完成。
  G2 继续按用户明确要求 **paused**，本轮没有恢复其实现或实机。

### 23:46 滚动下一步

1. 将已实机 GREEN 的 balanced-files 结构下沉到 B1 生成器，以生成器测试证明 77 个定义、
   调用面与内容分组稳定；不要手改带 `GENERATED FILE` 标记的产品文件。
2. 对生成器正式 split 输出做静态全链与一次独占 full-entry；只有正式 checkpoint GREEN
   后才进入 seed/paused-native bridge，不用诊断子集替代。
3. 在 8/8 clean spans 前不启动 TTS/render/video；G2 保持暂停。

## 00:00 正式收口

- 9 月 3 日最终能力边界：53-file case-kernel、55-file safe-core、七个 B1 stub 诊断子集和 77/77 正文双文件诊断候选均为 full-entry GREEN；未拆 58-file B1 仍只保留一次 pre-menu OCR RED。正式 generator 已完成 41/36 双文件拆分和 167 个定向测试，但 23:59 启动的正式 r3 checkpoint 在收口时仍运行，不能提前写 GREEN。
- 早会 D1、D2、D6 完成；D3 未完成；D4、D5、D7 部分完成，其中 G2 后续由 owner 暂停，D7 的最终 commit/push 与跨午夜实机归入 9 月 4 日。计划外完成项已在顶部单列，没有倒改早会。
- Readiness 未越级：B1 双文件仍待正式 checkpoint；seed、paused native snapshot、8/8 footage、两版 MP4 与发布 receipt 均未完成。G2 aggregate 既有能力不降级，但 GEN-034 新切片暂停。
- 失败 attempt 全部保留：旧单文件 B1 1205.343 秒 RED，以及正式轮次 r1/r2 的 0.3 秒 probe 路径绑定 RED；后两者均未启动 CK3、cleanup GREEN。r3 使用 exact EXE SHA `2D00FF…DB86`，跨日继续。
- commit/push：本日既有提交均已在正文登记；本次 generator、工具、证据与报告的原子提交在 00:00 尚未完成，明确顺延至 2026-09-04，不冒充已推送。
