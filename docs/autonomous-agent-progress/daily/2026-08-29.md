# 2026-08-29 日报

状态：**已收口**

正式收口：2026-08-30 00:00（Asia/Shanghai）

当天早会：**无准时早会记录**。当天工作由项目所有者连续插单并于晚间重新排序；不倒填成 00:00 已知计划。

起始分支/HEAD：`mod-zhongguo-style-wip` / `85daab6`

## 今天完成了什么

- `mod_zhongguo_style` 从 361 绩效文化研究、编号政策卡、考核榜、京察、3.25/PIP/申诉、第二 AI 例外推进到当前可批量验收基线；同时诚实纠正“361 个编号配置”不等于“361 个独立领域运行机制”。
- 冻结本版本功能范围：新增 361 领域状态机只保留设计文档和未接线原型，下版本再实现；三个未接线 Python 原型移入外部过程素材归档，没有进入产品源树。
- 完成 landed-title 地图定位的 exact-build 逆向、Python/MCP typed facade 与 native production wire 静态实现。冻结 CK3 `1.19.0.6` EXE SHA-256 `2D00FF3101EF70B566F2FCBAE292F09263199C80E9DC8F139B82D7D96F83DB86`；正式语义使用 recursive `title_bounds_center`，不把 `c_bianzhou` 错写成首府 ProvinceID 9822。
- 永久采用 US English HKL `0x04090409`；头衔定位正式路径转为 MCP/native，OCR/键鼠只保留历史 RED 证据。

## 早会承诺对照

当天没有 contemporaneous 早会，故没有可诚实逐项复制的 D1…Dn。所有完成项均列入“计划外完成”；晚间 owner reprioritization 成为 2026-08-30 早会输入。

### 计划外完成

- 形成 361 条设计目录、四份严格验收合同及 38 领域下版本架构；合同数量 `36 + 84 + 120 + 121 = 361`。
- 当前版本 361 配置 smoke：生成器 `11/11`、361/361 live-case 生成合同、80 个固定考核榜 slot、静态校验均 GREEN；当前运行边界仍是 17 个 A/B profile 加一个 C 路径、14 本共享账，不能冒充 361 套独立运行语义。
- 宣传工程冻结为中文主叙事、简中/英文同屏字幕、`zh-CN-XiaoxiaoNeural`、真实史实角色、无测试决议入镜、成片小于 20 分钟；22.854 秒流水线 smoke 已证明 TTS/ASS/编码链，但不是正式宣传片。
- 历史 19 个宣传 capture 根报告全部 RED，18 条 raw MKV 与失败截图、日志、userdir 均保留；未把任何 RED 素材冒充发布候选。

## 现在在做什么

- P0：为 title-map MCP 准备单次 managed CK3 实机矩阵；随后把它接入 `run_zhongguo_acceptance.py` 的录制前镜头 gate。
- P0：批量验收本版本现有功能和所有新增 GUI 阻塞态，修正 runner 已发现的 `shortcut = "close_window"` 旧字面合同。
- P1：重基线本版本 README/release checklist，避免下版本 361 领域运行时预研阻断当前版发版。

## 为什么做这些

- 21 次候选未收口的直接瓶颈已从输入法/OCR 收敛为夹具缺少 stable-title camera semantic action；补 MCP 观测/动作口后可以停止反复点瞬态 GUI。
- 项目所有者明确当前版先验收、发版、上传和制作演示物料；新机制必须留到下版本，不能继续扩大当前候选。

## 证据与验证

- Mod 批量静态门：11 + 9 + 32 个核心测试、promo 4 + 22 + 10 + 3 + 14 + 6 个测试、五套 root acceptance/release/cache 测试全部 GREEN。
- 生成/发布检查：361 mechanisms、80 scoreboard slots、thumbnail 640×640/699719 bytes、6 张 tracked Workshop JPEG 均可复现；双构建 51 files，manifest SHA-256 `fa928f54d7d733c04eba5fbba42088798dce5aad4f51777a7e6bf53aea8fd82c`，ZIP SHA-256 `2da28aa428bb47fe32b4dccfc6419200aaebca857df549033c04df040a163023`。
- Python title-map facade：17 项测试中 15 pass、2 项仅因 optional MCP SDK 未安装而 skip；typed service/driver、generic/planner isolation 已通过。
- Native fresh Release：`C:\Users\xenoa\AppData\Local\Temp\xar-title-map-native-static-review-20260829-release`，full build GREEN，CTest `41/41` GREEN、0 failed；**未启动 CK3，故仍是 static-ready，不是 fixture-live**。
- 今日未获得正式 Workshop item ID、fresh-cache 验证、正式宣传 GREEN capture 或最终 MP4。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| 361 编号目录/配置投影 | partial | `fixture-live` 配置 smoke | 361/361 编号路径；不代表独立领域语义 |
| 361 独立领域运行时 | 未区分 | `not-implemented / next-version research` | 38 领域架构与合同已落盘；当前版冻结 |
| title stable-key resolver/camera contract | research | `static-ready` | exact-build 逆向、typed Python/native、41/41；尚无 CK3 live |
| 当前版发布候选 | partial | static reproducible，仍 RED | 缺 MCP live、GUI 全矩阵、2 张 Workshop 图、正式 capture/upload |

## 失败、阻点与未知

- `run_zhongguo_acceptance.py` 仍调用 OCR/键鼠相机函数，尚未接 typed MCP；正式录制前必须替换。
- runner preflight 仍要求旧文本 `shortcut = "close_window"`，而当前 GUI 合同只允许 `shortcut = close_window`，会产生确定性假 RED。
- 当前 Workshop 媒体仅 6/8；BBCode 约 7751 bytes，只余 248 bytes，加入 #001/#361 两图前需先精简至少约 441 bytes并留余量。
- title/camera borrowed objects 全在第 13 application-main executor；pipe worker 仍沿用既有 `ReadSnapshot` 读取绑定。没有实际故障证据，不扩张为全桥线程重构。

## 文件与提交

- 主要文件：`mod_zhongguo_style/**`、`docs/ck3-native-title-map-navigation-contract.md`、`ck3_autonomous_player/src/xar_autoplayer/bridge/**`、`ck3_autonomous_player/native_bridge/**`。
- 已 push：`d00a74e`、`3896163`、`f5641e1`、`f0791af`，以及当天更早的 ZhongGuo 361/GUI/promo 提交。
- native production wire 与本日报在午夜仍待独立提交；未写成已 push。

## 下一步

1. 见 2026-08-30 早会：先完成 title-map MCP live，再以尽量少的 CK3 启动批量验收冻结版本，随后完成发布物料、上传和宣传片。

## 范围边界

- `mod_zhongguo_style` 当前版不再新增机制；361 领域扩展只写下版本预研文档。
- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。
