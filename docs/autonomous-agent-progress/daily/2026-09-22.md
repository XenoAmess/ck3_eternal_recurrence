# 2026-09-22 日报

状态：**进行中**

正式收口：2026-09-23 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-09-22.md`](../meetings/daily/2026-09-22.md)

起始分支/HEAD：`work/g2-m0-r0043-closure` / `3b36df12c80b3ae837beb104a156128fb972b18c`

## 今天完成了什么

- 已取得 R0043 独立只读审计：GEN-034-D、GEN-034 A-D 与 G2-M0 可 complete；没有阻止权威状态从 `1/8` 晋级为 `2/8` 的证据缺口。
- 已选择下一唯一实机方向：从 R891 h1484 标准封建普通 campaign pair 做 current-master continuation；Council R884 初始场景不重跑，目标自然事件与死亡不强制触发。
- R0044 从 R891 冷恢复并真实推进三天后，在两条 county-capital 路线均 timed-unsafe 时命中生产 B0；报告 `EDE952DC...2B08`，h1511 checkpoint `DFB93843...14A2F` 可恢复，CK3 完整回收。
- 根因是 planner 只把带 overmatch guard 标记的 `unsafe` 行计入 current-rally fallback；R0044 的 horizon 本身不安全，因而错误跳过已有 `8750` stationary query。原生决策树先更新，最小 Python 修复与回归以原 `72a88f29`、rebase 后 `0636a81f` 线性进入 master；normal/`-O` `GameplayBridgeTests` 各 `256/256` GREEN，临时分支已清理。
- R0045 从 h1511 真冷恢复，20/20 turns；修复后的 `8750` query 产生两次 proof-bound 一日推进，后续正式 turn 重新消费战争状态，最终 h1536 pair `2205BC09...8171` / `AA064220...76A9`，报告 `00296B7E...E877`，cleanup GREEN。
- R0046 再以新进程从 h1536 连续 100/100 turns、推进 28 游戏日：stationary unavoidable-contact 在独立后帧物化为战斗，下一 turn 查询 battle-control、执行一次 battle epoch；原军消失后正式 raise 新军并继续。最终 h1662 pair `03D08AE8...B301` / `13BFB01D...A842`，报告 `E1513900...D684`，cleanup GREEN。
- R0047 从 h1662 做 Council action-OFF 私有查询：already-councillor 有证据，其余 guest、candidate pending、replacement-fireability 正例均未出现，四门仍 `1/4`，公共能力不注册、不广告。
- R0049 在 ordinary production 从 h1662 完成 `100/100` turns、57 query/43 gameplay（16 visible）、约 260 秒，到 h1782/date `53284320`；R0050 真冷恢复后执行 `31/32` turns（19 query/12 gameplay），在第 32 turn/date `53284416` 因 `native_war_no_safe_target` RED 停止，旧 h1809/date `53284392` 配对 checkpoint 保留，未盲目投降或重交终局动作，两轮 CK3 进程均回收。R0048 只有 preflight，不计实机消费。
- 对当前交付制品做独立清单核验：R888 既有 `GO` 资格证据是真实有限范围实机证据，但 ZIP 缺正式运行依赖 `repo/tools/ck3_live_run_id.py`，因此撤销“用户可独立解压启动”的交付表述；新自包含制品 PRV-001 已在非 C 盘完成静态 staging，等待 no-launch 与同版实机资格验证。
- R0051 在 master `8b6202f` 的 action-OFF 私有 Council h1809 差异场景完成 201.616 秒只读验收；paused native revision/date/save 未变、CK3 PID117464 回收、报告 SHA `D725CBE1...FDF9FBD`。正例计数 already-councillor=1、guest=0、candidate-pending=0、replacement-fireability denial=0；场景再次缺三门，不能当作 Council final gate 成功，也不能重跑同一场景碰运气。
- PRV-001 新 ZIP SHA `FDE7BE61...45EED3` 的 2,361 条目已在 `PRV-002` 的全新 Z 解压目录逐项校验；因 R0051 唯一实例存活，no-launch `prepare-state` 曾正确等待，现进程已回收后继续。新 ZIP 仍 NO-GO。
- R0052 对这份精确 b7be2a4 自包含 ZIP 的 `PRV-002/package-fresh` 完成 fresh-extraction 只读资格 `GREEN_READ_ONLY`，报告 SHA `309CBDFB...9062D6`、PID129648 回收、日期/存档不变；尚不能冒充正式非空动作或 cold restore。唯一实例队列已进入下一轮 R0053 的 40-turn 正式生产验收。
- R0053 同一精确 ZIP 正式 40/40 turn、exit0、process-zero，23 query/17 gameplay、日期 `53282688→53282856`，但只有 life-advance、路线证明一日推进及 move 查询，**没有真正非空 typed 策略动作**。技术 ledger `completed-green` 只表示 runner 正常，产品门为 `evidence-insufficient/NO-GO`；不继续对此 h1380 切片重复或把日期推进算动作，也不为无效产品候选额外做 stop/cold。PRV-003 转选同版已有真实动作的普通封建 paired source。
- GOV-CONS-01/GOV-CLI-01 隔离实现默认 OFF 的正式私有建设消费与可执行 `native-auto-run --allow-private-construction-formal-trial`，normal/`-O` 聚焦各 `29/29`。R753 的确切 live 原生扫描虽 `checks_truncated=true`，但已有 6 个完整合法/成本 tuple，发现我方新代码误把全局截断判无候选；GOV-B0-R753 只去掉该硬门、保留选中项原生合法/费用/预算/idle，normal/`-O` 各 `13/13`。全部仍需最终同版 native 制品和真实生产循环/冷恢复，不广告。

## 现在在做什么

- WAR-B0-R0050 先落盘 exact-build 防御战争原生树，再聚焦修复 `score=-100` 时误排除 player-primary-defender 安全静止首府查询；CNL-B0-R0050 修受控 Council 场景工具对规范零填充 `R0050/R0051` 的拒绝；PRV-001 组装自包含有限预览，GOV-CONS-01 补 construction 正式 pending 消费，均与唯一 CK3 实机队列隔离。
- 唯一实机负责人准备以 h1809 配对点执行 Council 私有 query-only 差异场景，仍须先通过 run-ID 显式 Z 盘状态根、单实例和制品 preflight。C 盘旧 run-ID 命名空间四文件已原样迁到 Z 且 hash 一致；原目录删除遭工具政策拒绝，保留为 retired 残留，绝不作为本轮活动写 root。

## Readiness / 能力变化

| 能力或 gate | 之前 | 现在 | 证据/限制 |
|---|---|---|---|
| GEN-034 / G2-M0 | `3/4`, in progress | `4/4`, complete | R0043 outer/native `9294D8B8...13EE` / `2B726601...2D6`；只关闭固定 M0 visible outcome |
| G2 权威状态 | `1/8` | `2/8` | M0/M1 complete；其余六项定义与分母不变 |
| 普通 campaign continuation | R891 h1484 | R0046 h1662、date `53283744` | R0044 B0 已修并由 R0045/R0046 新进程连续消费；actor/episode/WarID 延续 |
| current-rally timed-conflict fallback | production B0 | production-live GREEN | `0636a81f`；R0045 两次安全推进，R0046 真实接触→battle epoch→新军继续 |
| 自然事件/继承 | 未遇到 | 仍未遇到 | R0045/R0046 均为真实 ordinary production，但不得把缺失 encounter 记为完成 |
| 普通 campaign 继续 | R0046 h1662 | R0049 h1782 GREEN、R0050 h1809 RED | R0050 末次 save/driver `93DB4D03...5A770D` / `1FA45B1E...70CAB9` 有效；未执行终局动作 |
| 预览包可独立启动 | 旧 R888 文档称 GO | NO-GO，待新包实机资格 | R888 缺 `ck3_live_run_id.py`；现有实机证据可复用其语义，不代替新包完整性与实机验证 |
| Council final gate | 1/4 | 仍 1/4，公共 OFF | R0051 source h1809 query-only GREEN 但后三类计数全部为零；需要真正不同的自然合法场景 |

## 失败、阻点与未知

- 当前没有存活 CK3/injector。R0044 的 `native_war_no_safe_player_held_county_route` RED 已保留并完成最小修复/两轮实机复验，不覆盖原失败 artifact。
- Council guest/pending/replacement-denial 仍缺真实正例；自然 `.0030`/`.1007` 与同 campaign 真实继承仍待 encounter。
- R0044 profile `error.log` 中仍保留原版脚本/loc 错误；它们未被 formal report 指认为本次 planner blocker，本轮不关闭、不降级，也不据此扩展无实证修复。
- R0050 防御战争 score `-100`、己方剩 2 人、敌方约 7759 且敌军同图交汇；native surrender 虽合法，但 `vassalization_cb` 的战后从属/长程结果未观察，不可用“可投降”自动替代缺失安全路线。WAR-B0-R0050 只修该确定性窄分支并短复验。
- C 盘旧 run-ID 目录物理删除被工具政策拒绝，不把已复制误称为清理完成；另有三处本任务旧临时目录含 R883 RED/存档，迁移前不能删除。新任务的 temp/cache/制品与工作副本仅写非 C 盘。
- 三处旧 C 临时目录的非凭据证据已向 Z 迁移 6,151 文件、1,153,921,845 B，逐文件路径/大小/SHA256 一致；R883 RED 与独有 driver 保留，个人账号目录未复制，C 源未删除。PR #2 的 CLA/signatures 已通过，静态 CI 仍进行中；之后只经受保护 rebase-only PR 集成。
- PR #2 首个 static CI `35649280202` 因旧 GEN-034 文档中的历史 shell fence 确定 RED，原失败保留，最小更正文档后本地 `validate_python_only.py` GREEN；一次工作流配置 `35650252992` 在 job 展开前 RED，已改为 runner 分配后设置非 C temp/cache/venv。最新 PR 静态任务首次通过 non-C storage、checkout、setup-python 与 D 盘虚拟环境/依赖安装并继续；同 commit 的重复 push 测试已明确取消，保留 PR 检查。此前一次直接 master push 出现保护绕过提示，已停止复用。
- PR #2 最终 head `b0cdc08` 的 CLA/signatures 和官方 PR static `35651082182` GREEN（重复 push run 单独取消，不冒充 RED）；仅使用 GitHub rebase merge，远端 master 到 `fadc2e5`，12 个提交 `range-diff` 逐项等价、无 merge commit。原 WAR `4152086→6e6fb4b`、GOV `3e1e5e4→9e27266`、CLI `ffae37a→45d01d4`、两项 R753 B0 `30a9e46→deaf250` / `564c231→fadc2e5`；三条临时远端/本地分支与三处 Z worktree 已在集成后 CAS 删除并复核。WAR 实机开发源已另冻 Z snapshot，不依赖原工作树。
- R0054 WAR 开发冻结 `4152086` 从有效 h1809 在独立 Z state 重新绑定原 pipe（此前错误新 pipe 的零启动 RED 保留），正式 production 20/20，报告 SHA `CAE4D413...F3F0D2C5`。起点 score `-50`，turn8 四敌军 same-frame stationary route query 后证明性推进；独立后帧 score `-100`；turn15 再获当前同帧完整 horizon、turn16 有证明的一日推进到 date `53284440`，turn17 继续消费。没有终局动作或未经核实的副作用，paired save/driver `D7F5F3AC...BCCA3` / `138ADFA0...F013D`、CK3 回收。该 B0 窄分支开发实机 GREEN，不冒充最终 master `6e6fb4b` 运行；待 PRV-003 后做最终版本映射或受影响短复验。
- 跨仓影响：WAR 修改内部 Python 决策过滤，GOV 增加默认 OFF 的私有 Python/CLI 参数；均未改公开 native ABI、MCP schema/注册/广告或现有调用的默认行为，故 open_kaishek 当前版本无必需代码适配。建设私有能力尚未发布，必须等新同版 native 制品及 live 消费后再评估下游广告；不把静态 29/29 或 R753 旧原语冒充 MCP 通用资产完成。
- PRV-003 精确 h1662+b7 自包含 ZIP `0E6B2B0E...D29AC5` fresh-extraction/no-launch 全文件 SHA/CRC 后，R0055 read-only eligibility GREEN；R0056 正式第 47 turn 唯一 typed `raise-troops-default`，独立后帧 army `[]→[301989888]` 且 `war_changed/army_changed`，第 48/49 turn 和后续正式策略消费，满足非空动作窄门。但第 58 turn/date `53284488` 在战分 `-100`、8750 敌军汇聚时 `native_war_no_safe_target` RED；报告 SHA `6D5B175D...6915667`、receipt `625B4460...10C76C`，h1737 安全 paired checkpoint 保留，CK3 回收。产品整体仍 **NO-GO**，不能缩短 60-turn 声明遮蔽确定 B0，也不对旧 ZIP 做 stop/cold。PRV-004 以最终 master `fadc2e5`（WAR `6e6fb4b`）和原 h1662 pair 新封不可变 ZIP，重新独立实机资格与动作/恢复链；旧失败保留。
- PRV-004 最终 master/h1662 候选 ZIP `Z:\ck3_mod_rewrite\.task-tmp\PRV-004\candidate-h1662-fadc2e5-v2\g2-preview-ordinary-h1662-fadc2e5-prv004.zip` SHA `BBCF366E530E668CC2D0AD09BDB720BDF23AF45AD674C6543ED2F9214CFC74DA`，fresh 解压 2367/2367 文件大小/SHA GREEN、no-launch GREEN；外部绑定 manifest SHA `D720EAFBADA2B4D60A1DDC29F1991514B7BB084C584F936965426D3EA3A5EEE0`。旧 R0056 原始报告含非 UTF-8 字节，builder 明确拒绝内嵌；原报告不改，ZIP 内仅附合法 UTF-8 的失败收据作索引，完整原报告留外部 artifact。仍为 **NO-GO**，唯一 CK3 owner 已接手精确 ZIP 的新进程 eligibility/60-turn/stop/cold 队列。

## 文件与提交

- 文件：G2 权威 JSON、路线图/执行文档、战争专题、交付/阻塞台账、2026-09-21 收口与本日报/早会；R0044 exact 原生输入账本、策略与回归。
- commit/push：G2-M0 closure `b1707193`；R0044 B0 原 `72a88f29` → master `0636a81f`。本增量报告提交待线性推送。

## 下一步

1. 若 R0046 pair 可直接使用现成 Council private gate query，则先只读查询 guest/pending/replacement-denial，action 保持 OFF；否则记录具体缺口并继续普通 campaign。
2. 从 h1662 配对点继续有界 production；遇自然 `.1007`、自然死亡或新 B0 时按对应合同消费/保留现场。
3. 线性提交并推送本增量报告，随后即时清理临时分支；G2 在无新增完整 milestone 时保持 `2/8`。

## 范围边界

- 宗教域默认 owner-deferred；只允许圣战战争 OODA 与婚姻必要判定两项最小例外。

## R0061–R0065 增量（本日滚动记录，非午夜收口）

- PR #3 官方 static/CLA GREEN，rebase-only 到 master `3da54dc`，原 PEND policy `4ce6d6d→d0e892e`；三条临时远端/本地分支和工作树同轮清。PRV-005 原 h1662 自包含 ZIP SHA `F5EB7657...B282CD`，fresh 解压 2369/2369、no-launch GREEN；R0061 只读资格 GREEN。R0062 精确 ZIP 60/60，turn47 typed 征兵、独立 ArmyID352321560，turn48/49 消费；报告 `1BAD32D3...B7259`。R0063 再60/60但没有目标 pending、没有发 stop，不补任何缺门。
- R0064 独立 PID119388 经正式包内 request-stop 在30/60 turn 成对停止，status `operator_stop_checkpointed/operator_stopped`，save `761D36...8E92`/driver `0FC287...2A3`，全树回收。R0065 不同 PID60220 从此 pair 真 cold 恢复，同 actor/episode/WarID/ArmyID，前16 turn 无重复征兵；第17 turn/date53284680 自然原版九选项 `stress_threshold_special.1001` 的注册 extended consumer 缺失而 RED，未盲选；raw 报告 `FA25A1E4...649F43` 和未污染最后 pair save `C58725E5...572273`/driver `1E8EC91F...9B3B3F` 留档，CK3=0。受控停止已验，冷恢复启动已验，但同一使用范围内自然强制事件 B0 使 **可运行预览仍 NO-GO**；先 exact 原版树/最小事件正式消费者，再同版聚焦复验。R0059 转封臣拒绝虽已静态入 master，真实后帧尚缺。
- GOV 私有正式 R0060 20/20 技术结束但471条命令无建设，不能称治理动作 GREEN。public/native 修订号误判真实根因先以 R0060 fixture RED 再 normal/`-O` 109项 GREEN；PR #4 官方检查通过/rebase-only 到 master `be53794`，两条临时分支/worktree 同轮清。必须从原 R753 pre-action pair 新 Z state 重绑定后聚焦验真实 typed 建设/后帧/下一循环，不能重复无变化的20轮。权威 G2 `2/8`、Council `1/4`，100年/首整局/双种子未完成；PRV-005 冻结版本为 `3da54dc`，不借后续 master 冒充同版 live。
- EVT-B0-R0065 原版树/九项选项、AI 默认权重和未闭调用边已先于策略实现落 `docs/ck3-native-ai/stress-threshold-special-1001.md`；本帧仅 native `[0,4,7]` enabled。native7 可 source-bound 选用，但明确接受无期限 `stress_frozen_grief` 的健康 `-0.5`、外交 `-2` 等长期成本；native4 若已持有 inappetetic 会升级饥饿甚至致死，当前 trait 真值未观测，故不选也不为本 B0 新增无用 bridge。正式消费者/独立压力后置/下一 turn 与 cold 仍待。
- GOV-LIVE-03 新版本 no-launch READY 后 R0066 从原 R753 pair 正式第1 turn 提交唯一 typed 建设（request ID `construction-submit-a138bf862f7e4684a5ab2e1dcf57a0fa`），原生 ACK 仅 `pending_receipt, applied=false`，receiver_calls=1。第5 turn 物质查询 `construction material source unavailable; keep pending` RED，CK3 PID69072 已回收；stdout SHA `8C4F7615...67AB`、pending ledger `A6DDE9...4668` 保留，原游戏 checkpoint SHA `D8BDC3...01474` 未更新。建设动作不得因 ACK 判生效、不得盲重提；GOV-B0-R0066 查 exact 物质源与恢复合同，预览事件 B0 优先。
