# 2026-08-31 日报

状态：**已收口；G2 第二完整寿命 GREEN，ZhongGuo 二期 static/central 361/361 且 MCP-first 验收继续 RED 收敛**

当前滚动更新时间：2026-09-01 00:00（Asia/Shanghai）

正式收口：2026-09-01 00:00（Asia/Shanghai）

当天早会：[`../meetings/daily/2026-08-31.md`](../meetings/daily/2026-08-31.md)

本周计划会：[`../meetings/weekly/2026-W36.md`](../meetings/weekly/2026-W36.md)

开场分支/HEAD：`master` / `8ce90834ce50`

## Opening baseline

- G2 attempt 04 留下 turn 722/history 1918/date 53290248/SHA `B3A9FF0C...D9A23` 的可恢复 checkpoint；旧 blocker 是缺少
  静止军团与来敌之间的精确接触时域。
- ZhongGuo 二期权威账为 Python `361/361`、CK3 static `220/361`、central `62/361`、bounded live `4/361`；一期已发布，
  二期 release/promo 尚未启动。

## 00:03 计划内增量：G2 attempt 05 缩小安全路线 RED

- run `20260830T155719Z-next-episode-f2241f93` 从上述 checkpoint cold resume；新 native stationary contact horizon 已真实跨过旧
  `date_raw=53290584` 阻点，完成 `151/152` turns、`55` 个可见 gameplay turns，并继续保存 durable checkpoint。
- 新锚点为 turn 148/history 2085/date 53291832/SHA-256
  `1B24C2486BF2258D3DBA8B201FB9AAC15B7B62825CAAA1330C14C3DD7B9EB7C6`。随后在 date 53291856 得到更窄、可行动的 RED：
  `one_day_contact_free=false`，敌军 `167772532` 将在 53291880 抵达玩家 Army `134218218` 所在目标省 `2619`，另有两路来敌；
  当前 planner 只有阻断判定，没有逃离/改道动作。
- report SHA-256 `D23D36FF...A0CED`，first-blocker SHA-256 `CC399FDD...25B72`；cleanup、process tree 与 English HKL 全绿。
  下一步先更新 exact-build 原生军队树，再实现最小 escape/reroute policy，并从新锚点续跑；不使用 OCR。

## ZhongGuo 二期滚动状态

- 指标/交付与职业/HC 的唯一 manager portfolio adapter 已分别入 `fbde704`、`89c1cd0`；玩家链按 D+1 串行，授权 AI 复用同一业务链后台执行。
- B1 配额/合池/交易/待定/重开纵切以 `2a2f6fe` 入库；独立复跑 runtime `22/22`、quota model `55/55`、phase2 runtime
  `38/38`、scoreboard `18/18`、generator/本地静态检查全绿，官方 Windows CI `33321700560` GREEN。42 项仍有明确程序性缺口，
  不把 central/static 冒充 live 或 complete。
- 补偿/LTI 33 项（082–091、278–300）以 `528c872` 入库，覆盖国库/经理个人金币 70/30 双付款、延期/退款/追回、3.75 LTI
  门槛、Good Leaver 默认不加速和 FIFO 回购；独立复跑 runtime `14/14`、model `46/46`、kernel `10/10`、generator/静态检查全绿。
  `93a3126` 将权威 readiness 从 CK3 static `220/361` 提升到 `253/361`，两次官方 CI `33322052747`、`33322121751` 均 GREEN。
- 反馈/晋升/PIP、劳动力/终局、职业学习仍在修独立审阅发现的真实 RED；未冻结候选不计 readiness。中央 hook 继续等待各包稳定，
  普通 CI 按规则跳过发布级七语审计。

## 00:24 计划内增量：G2 真实进入战斗并缩小 native query RED

- attempt 06 `20260830T160740Z-next-episode-c2ef48a8` 从 date 53291832 冷恢复；严格 one-day contact transition 在替代路线全部被拒后
  推进，并于 date 53291904 原生观察到 Army `134218218` 与敌军 `167772532`、`251658360` 的 `active_combat_observed`。
- 新 durable checkpoint 为 turn 9/history 2096/date 53291904/SHA
  `0D5B9F116DDAEFCD7C8DE0A9446924B88814D78FFBBD35FFD1F5E10C8D812858`；attempt 06 report SHA
  `8144BC04E7FB905AE9F212EFEDDB94C2EABF6B0C2751A5E4793AEF89F81F3014`，cleanup 与三份 English HKL 证据 GREEN。
- 新 blocker 是刚建立战斗后的 `query-battle-control-snapshot-v1` 返回 `state changed during query`。attempt 07 从同一 checkpoint 冷恢复
  后稳定复现，排除单帧偶发；下一步只加 read-only stage diagnostic，重新跑 native/Python tests 后启动 attempt 08，不使用 OCR。

## 01:16 计划内增量：signed 战斗身份闭环与二期 static 321/361

- G2 attempts 08–10 把 `state_changed` 继续缩小到 exact-build opaque combat identity：原生 reader 在 RVA `0x22771FC`
  读取完整 dword，在 `0x2277204` 仅用 low24 索引，在 `0x2277220` 再比较完整 dword。负数是合法 generation-tagged ID，
  只有 `-1` 表示 missing。attempt 10 只推进一次 D+1 后得到同一 checkpoint；之后所有查询均保持 `advance-days=0`。
- attempt 11 `20260831T011000Z-signed-combat-id-query` 越过 active CombatID 检查后，在 `BattleResultID` 的旧正数过滤处
  typed RED；日期 53291904 未变，report SHA `ED8AC58D...BC21CE6`，cleanup/HKL 全绿。它不是 capability 倒退，而是把
  同类 signed-generation 缺陷定位到第二个真实生产读取点。
- attempt 12 `20260831T011500Z-signed-battle-result-query` 在同一 checkpoint 完成零推进双查询：query seq `1→2`、
  frame equal=`true`，CombatID `-2147483647` (`0x80000001`)、BattleResultID `-2046820351` (`0x86000001`)，
  province 2619、maneuver day 1、attacker CUnits `[251658360,167772532]`、defender `[134218218]`。report SHA
  `63FE9E3C...741D66`，frame SHA `5AD7B6D6...630AF`，checkpoint SHA 仍为 `0D5B9F11...858`；cleanup、session、
  process tree、driver 与 US English HKL 全绿。这把 active combat identity + participants 提升为
  `production-live paused gate`，但尚不是完整战斗 OODA。
- ZhongGuo 职业学习以 `0be49b4`、readiness 以 `7c2567b` 入库；反馈/晋升/PIP 46 项以 `c64bf47`、readiness
  以 `bb0b425` 入库；补偿案卷绑定修复以 `3756f3d` 入库。权威累计门现为 Python `361/361`、CK3 static
  `321/361`、central `62/361`、bounded fixture-live `4/361`。static 仍是源码能力，不冒充 CK3 live。
- `f775589` 记录了 MCP-first 验收所缺的 mod case/receipt/deadline、named-widget、任意 subject 资源/关系与 AI case
  查询合同；该提交发生时仍受“只写能力合同”限制。项目所有者已于 2026-08-31 撤销该限制，当前允许并要求针对二期与 G2
  的真实 blocker 继续实现 provider/native bridge。考核榜 #013 ACL 以 `6d084d3` 入库，允许合法 B1 case `41` / result case
  `903`，A/B 分档且错误 policy fail closed；`ef9aa03` 清除旧静态计数硬编码，`6080608` 记录新增按钮数为 0 及下一轮
  named-widget 阻塞矩阵。前两次官方 CI `33324567573`、`33324619850` 已 GREEN，最新文档 CI 仍在运行。
- 余下 40 项 Workforce/Endgame 正在修复同 actor HC 双占、non-manager 的诚实 N/A terminal 与 360/361 resume；中央
  dispatcher 正收口跨轮覆盖、D+1 gap、无参 adapter 异人、CL partial-open、MG failed-open 与 M013 route C。B1 001–013
  当前约 92% 静态语义，剩余可静态项继续施工；共同任务信号与实机求值证明单列，不把 26/26 文本测试冒充完成。

## 01:34 计划内增量：二期 CK3 static 361/361

- Workforce/Endgame 40 项以 `7e60b59` 合入 `master`：#257/#263 的正式 HC 身份冲突原子拒绝，#360 按 cohort 冻结身份槽并
  校验全局唯一，伯爵/男爵在完成 38 项可参与操作后以 `status=7/reason=360361` 的诚实 N/A 终态关闭，不伪造仅公爵以上可执行的
  #360/#361。独立验证 runtime `73/73`（普通与 `-O`）、model `9/9`（普通与 `-O`）、kernel `10/10`、generator 与本地静态检查
  全绿；官方 Windows CI `33325387397` GREEN。
- 11 段中央串行 dispatcher 以 `c6c7290` 作为尚未接总 hook 的静态模块合入，关闭跨轮覆盖、D+1 gap、无参 adapter 异人、
  CL partial-open、MG failed-open、M013 C 路和 non-manager 尾段等 7 类审阅缺口；central `22/22`、相邻包 `196/196`、
  generator 与 `validate_local` 全绿。该模块尚未获得 B1 publication 总入口，因此 central readiness 仍诚实保持 `62/361`。
- `d1ad7f8` 将 242–277、355–356、360–361 共 40 项写入权威静态证据账，累计门现为 Python `361/361`、CK3 static
  `361/361`、central `62/361`、bounded fixture-live `4/361`；readiness/generator/wiring/Workforce 合计 `107/107` GREEN，
  23 文件生成复现 GREEN。B1 当前约 95%，正补完统一成绩 writer、共同战争任务证据、申诉翻案评价人信用和最终负例矩阵。
- MCP 层仍只保留能力合同，不越权实现；总接线完成后才一次启动 CK3，按 MCP-first 合批存读档、多周期、AI、考核榜 ACL 与 GUI
  阻塞矩阵。发布级七语审计、Steam 更新和二期增量宣传片仍未提前启动。

## 02:04 计划内增量：G2 战斗动作门、响应式考核榜与二期语义纠偏

- G2 attempts 13/14 证明 cold restore 会把未 checkpoint 的只读 history 截回锚点；attempt 15 则表明 planner 在出手前仍需完成
  battle-control 与三项战争终局查询。attempt 16 `20260830T174839Z-next-episode-daf8eb6f` 随后完成 6/6 turns：前四步只读，
  第五步执行唯一真实动作 `battle-decision-epoch-advance-to-53292264`，日期严格从 `53291904` 推进到 `53292072`（7 天），
  第六步在不再推进日期的 paused frame 复查同一 CUnit `134218218`、signed CombatID `-2147483647`、BattleResultID
  `-2046820351`，战斗阶段由 maneuver 进入 main、`finalized=false`，并命中 `combat_roster_changed` sentinel。新 durable
  checkpoint 为 history 2103/date 53292072/SHA `ED031039DA50C5FFA2FB9E5F47AF329BC2F4A56133816968584AF17B42C1C8E3`；
  report SHA `3954608B316B51B6F28F73EC2C10834F68DF2F15A2CF4656161D01520DEE9326`，唯一 blocker 只是本次有界
  `turn_limit/bounded_incomplete`，HKL 始终为 US English，cleanup 全绿。
- 上述 G2 生产增量以 `0d14075` 合入 `master`：45 个 tracked 文件，覆盖 signed battle identity、终局转换、Raiktor 特殊停战、
  stationary contact/horizon 与婚姻拒绝续跑。主线独立复验 Python `562 passed + 360 subtests`、Windows-native CTest `44/44`
  全绿；PATH 中 Cygwin `ctest` 的 0-test 结果不是能力测试，今后显式使用 Visual Studio 自带 `ctest.exe`。下一步从新 checkpoint
  继续第二角色直至自然死亡与结算，不把本段有界门冒充 G2 全流程完成。
- `9d3e859` 把二期 MCP 合批拓扑写入合同：正式 runner 必须先读取 runtime capabilities；现 provider 缺 ZhongGuo 案卷/资源/期限、
  产品决议、AI 案卷与 named-widget，因此当前应在启动前给 capability RED，不得回退 OCR。存读档会受控重启 CK3，故最省成本的
  诚实方案是一份 attempt、初始进程建案并保存、一次 restore 重启、第二进程串完 D+7 至 D+365 与三周期。
- 固定 `1220×820` 考核榜在 1366×768 的静态越界已由 `e6b5893` 修正为居中 `90%×90%` 面板；两张名单、四个案卷页和制度账
  共 7 个 bounded 双轴滚动 surface。1366×768、1920×1080、2560×1440 × 100%/125%/150% 的 3×3 静态合同、按钮增量
  审计、scoreboard `27/27`（普通与 `-O`）、generator、wiring `12/12`、`validate_local` 全绿；新增产品动作按钮为 0。当前
  GUI SHA `9550A4DA...2FC2` 仍须 MCP named-widget 实证百分比布局、滚动、焦点和阻塞。
- 对 B1 的聚焦语义审阅推翻了“约 95% 即将完成”的乐观估计：#135/#136/#137/#138/#140/#141/#142/#143/#144/#145
  仍有 receipt-only、未读取 A/B/C、配额不守恒或无真实 consumer 等缺口。静态 361/361 只表示每号已有 CK3 投影，不表示这十项
  业务闭环完成。当前正把它们补成稳定五元案卷、真实消费、审计字段与正负例；其余二期领域也已按独占文件并行做同样的语义闭环，
  共享 hook/GUI/readiness 留给主线串行集成。未完成前不启动发布验收。

## 02:49 计划内增量：G2 第二完整寿命与再次跨局 GREEN

- 用户 turn 中断的 `20260830T180744Z-next-episode-1cd83c9e` 只留下 `preflight_ready / finalized=false`，不记 capability RED；
  接手时 checkpoint 实物与 driver `last_checkpoint` 一致的最新 durable 锚点已前进到 history 2181/date 53295288/SHA
  `816B8B02...6D26`。fresh 222-step Release build 与 native CTest `44/44` GREEN，no-launch preflight 双哈希和进程清点 GREEN。
- 正式 continuation `20260830T182851Z-next-episode-19d679de` 完成 `472/472` turns、墙钟 `1198.576s`，310 query、
  160 gameplay、151 checkpoint、1 terminal、1 recovery。turn 468 在自然时间推进中观察
  `played_character_changed`，没有 `die`、控制台或人工死亡动作；turn 469 完成同 episode 结算，score `0`、7 blessings、
  heir gameplay `0`。
- turn 470 将 PID `72636→39036`、connection generation `1→2`、run ID `fffa...→6e06...` 并精确重载 immutable
  seed；turn 472 在第三 episode 推进一天并保存 history 4/date 53211576/SHA `56C00CDC...408E`。15/15 gates、cleanup、
  process tree 全绿，`first_blocker=null`；report / terminal / next-episode SHA 为
  `2D798DAB...C4DD / C72C3A11...667A / BB570624...33A3`。
- HKL 启动与十分钟复验都实际遇到系统回切中文，已立即纠正并保留英文；随后 5 秒只读 watchdog 只在异常时发
  `WM_INPUTLANGCHANGEREQUEST`，不做 OCR/键鼠游戏操作，CK3 退出后自动回收。

## 12:35 计划内增量：二期真实 Loader 债务、结果案卷 MCP 与 G2 泛化续跑

- 02:49 后二期又批量深化了 B1/B2、补偿、项目交付、PIP、经理治理、事故平台、Career/HC 与
  Workforce/Endgame 的对象、收据、期限和 consumer；中央产品链已保持 Python L0、CK3 static 与
  central-wired `361/361`。权威口径仍是 `domain_runtime=partial × 361 / complete × 0`、bounded
  fixture-live `4/361`；静态接线或本轮语义深化不能冒充逐项完成或实机 GREEN。

- 首份覆盖完整二期树的真实 CK3 Loader attempt
  `Z:\zg361_phase2_loader_live_20260831_1117` 为 **RED**。冻结 `report.json` SHA-256
  `0D3D5465D617AC7A7DE2D99514FE8841EF232D39D2D598686AE0040074AEE4AE`，`error.log` SHA-256
  `538134E409393CC1CAF7BC0736B385594D2344B55E2502C5D6D4A5BBDD9512`；`966` 条可解析 variable 诊断按名称去重后共有 `483` 个
  `used but never set`，主要集中在 Workforce `303`、PP `105`、CL `22`。该 artifact 的 HEAD 为
  `88095ba`，索引也明确保存了当时 B1/B2/runner 的 scoped dirty 状态；它是当前最新的 Loader live 真相。

- Loader RED 后已合入三项定向修复：`07acdfe` 将补偿追缴从旧的未生产 receipt 字段改绑真实
  frozen receipt tuple；`165cc8c` 用真实 character scope 锚定瞬时盲排名列表，并删除旧
  `recommended/slandered/backfire` 旁路；`627fd88` 删除七个只有 reset、没有 producer/reader/GUI consumer
  的废弃考核榜 detail 字段。三者官方 CI `33354888237 / 33355167505 / 33355199816` 均 GREEN。
  这些只是 static-ready 的针对性修复；下一份 CK3 Loader artifact 将对应签名归零前，`483` 不得改写为 GREEN。

- `b1f275d` 实现了独立的 received-self 结果案卷 MCP：
  `ck3_query_zhongguo_result_case_snapshot_v1`，capability 为
  `game.command.query-zhongguo-result-case-snapshot-v1`。native bridge 现发布 `67` 项 capability，application-main
  mailbox 扩为固定第 `15` 槽；provider 只读玩家本人 scope 的固定 `13` 项 `zg361_result_*` allowlist，
  required owner 只是对真实 kind-4 owner 的相等过滤。当前仅为 **static/fixture-ready**，没有 paused CK3
  result-case artifact；官方 CI `33355963507` GREEN。

- `460fdd2` 将正式验收的 `.50` 收件结果单元改用上述 result provider，从 typed event root 绑 subject、
  从 `zg361_notice_prompt_owner` saved scope 绑 owner，并新增零视觉/零 OCR/零坐标的 `--loader-smoke`。
  loader-smoke 只验 application-main mailbox、稳定 heartbeat/pump、mount 与 `error.log`，明确不进地图、不记 gameplay GREEN。
  这是 harness static-ready，尚未执行新的 CK3 live cell；官方 CI `33356783207` GREEN。

- `f541855` 把 Career/Learning 内部流动接成真实 subject flow：只有
  #314/#315/#318/#319/#321/#333 六类本人事件可写 response，伯爵、男爵仍只能回应、不能管理；
  #312 只认领 Career/HC 已成熟的真实 P#114 vacancy，#319 route A 只在到期后执行原版三段式转封，
  liege/title/holder 后置均真才结清 HC。该包仍是 CK3 script static-ready，没有 paused MCP/live 结论；
  官方 CI `33356200837` GREEN。

- `65cb41b` 把 Incident X/Y/Z 从无条件开案改为观测驱动：只在“战争+受评人首都低控制”、真实个人/国库赤字或
  首都低控制任一事实已存在时生成 positive incident；无事故必须有 exact probe/N/A tuple，不生成伪 X/Y/Z 对象。正案发布
  next-cycle KPI staged receipt，中央 stages 4–6 已区分 success 与 N/A；该提交落地时尚缺的一次性 consumer hook 已由
  后续 `efb4130` 合并。该包只记 static-ready/not-live-tested；官方 CI `33356967334` GREEN。

- `c7c7234` 将 Manager/Governance 的 F032/F035/346/347/354 改绑真实 result/PIP/离职与申诉事实：F032 产生下轮官方
  organization component-8 token，F035 只读 `ratio_override > game rule` 的 strict/relaxed/off 并产生下轮 bottom-slot
  token，346/347/354 不再从死变量伪造 signal/override/fairness。该包落地时留下的三处共享 KPI/rank/settlement
  hook 已随后由 `efb4130` 在 shared owner 合并（见下）；`c7c7234` 本身只记 static-ready，官方 CI
  `33357036816` GREEN。

- `80f2675` 将 Workforce #262/#264 接成真实、可收据化的交接链：A/B 从真实角色树发现并冻结天朝制公爵及以上 host
  manager；伯爵和男爵可被考核但不能主持交接，没有合法 host 时返回 typed RED `2621`，C 不伪造 host。玩家本人依次完成
  文档签收、30 日跟岗和再 30 日实操验收，每张 receipt 绑定真实 #261/#263/#256 对象；三张齐备才允许 #264 A，拒绝则进入
  B，`flow_consumed` 保证 owner 的 20 金币支付或退款至多执行一次。旧 caller-supplied adapter/hash/waiver 已删除。该包只记
  static-ready；其外部 producer ledger 已分类原 `303` 项告警，但 AD `80`、AL collective `167`、AL charter `28` 及 #361
  三周期 bootstrap 等运行时债仍须继续闭合。官方 CI `33357181244` GREEN。

- `321ae8e` 将 PIP 唯一权威收归 B2，PP 只做投影：#015 必须同时满足冻结 3.25、证据完整以及绝对低分/KPI/八类证据中至少
  三项真实负证据，单纯强制分布产生的 3.25 不会自动进 PIP；B2 持有不可变五元组、本人回应、支持/容量、D+180 资源交付与
  D+365 唯一结算，并将 `+10/-10/-15` 写入下周期绩效证据。缺 task progress 或独立 reviewer 均保持 typed RED；PP
  #183/#184/#185/#187 只读取或投影 B2 的签名、支持、交付与结算，不再占用容量或二次结算。该包只记 static-ready，官方 CI
  `33357230767` GREEN。

- `efb4130` 已合并上述共享 consumer：Manager 到期 organization KPI 作为正式第 8 组件进入
  `zg361_kpi_organization_evidence_value`，不是新增第 9 个顶层 KPI；B2 消费 management debt 并冻结结果后调用 manager
  due settlement，排名链消费 manager bottom-slot token，Incident 的 staged next-cycle KPI receipt 则在统一 freeze point
  一次性消费。由此 Incident/Manager 原列三处 shared hook 已关闭，但整包仍只到 static-ready/not-live-tested；官方 CI
  `33357355728` GREEN。

- 上述最新整合后的普通 Python 单测与 `-O` 优化模式单测各 `1033` 项全部通过，`validate_local` 也为 GREEN；这是
  static/L0 证据，不改变 `zg361_phase2_loader_live_20260831_1117` 的 `483` 项真实 Loader RED，也不构成 paused CK3 或
  多周期 gameplay live 结论。

- 对同事留下的 `4054ef3 / 36d1bc1 / eb1d813` 已做独立 KEEP 审计，三项官方 CI
  `33345902103 / 33346151319 / 33346343872` 均 GREEN，没有需要回滚的语义改动。唯一可复现问题是
  repository contract 在 B1 schema 加入后仍将 JSON 文件数硬编码为 `9`，干净 `eb1d813` 的普通与 `-O`
  全量单测各 `1542` 项时均只失败该项。`b1f275d` 已改为精确文件名集合，并覆盖新 result schema 加入后的
  `11` 项；该缺陷已闭合。

- G2 在冻结 clean-master `07acdfe` 的 detached worktree 上完成
  `call-ally-busy-reject-v1` production-live bounded GREEN：`4/4` turns、`2` typed query、`2` gameplay，
  `20/20` 独立门通过，`first_blocker=null`、stderr 空、cleanup GREEN。真实 `call_ally_interaction` 的 hash
  为 `936306703`，actor `30287`、recipient/player `29829`；本进程 full pending ID `1744830464` 被拒绝后消失，
  active WarID 集合严格保持 `{50331699}`。replay/checkpoint/driver SHA-256 为
  `C2C9759...DE02A / 34247FE7...50261 / B157033C...74865`，本轮无 OCR、视觉导航或键鼠 gameplay。

- 同一 G2 现场随后完成 `25/25` continuation，从 `34247FE7...50261` 推进到
  `date_raw=53223936 / history=1124`；最新 durable checkpoint SHA-256 为
  `60108A5DA03DC3A8315A3E79897D9CF2F49763910A8AA15A462E7DD0B6AAF164`，driver state SHA-256
  `4FB901C7...FF57E`。WarID `50331699` 的玩家战分已到 `-50`；native validator 证明 surrender 可用、对方会自动
  接受，但 `terms.status=unavailable / reason=cb_specific_terms_not_observable`，因此严格策略没有在看不见 Raiktor
  claim CB 失败条款时盲投降。这是新的真实能力债，不是 route 或 pending-interaction 再次失败。

- HKL 结论分层保留：call-ally watchdog 因前两次两秒 ACK 超时将自身记为 RED，但第三次已在 gameplay
  前把 CK3 UI thread 改为 US English，全部 gameplay 发生在其后，最终也保持英文；watchdog summary SHA-256
  `7E207DFB...E5F23`。25-turn continuation 的 initial/mid attestation 均为 US English GREEN；最后一次 probe 因 CK3
  已退出、无法再 focus 而 RED，它是 post-cleanup watcher 结果，不反写 gameplay GREEN。

- PP/B2 与 Workforce/Endgame 的本轮深化已分别由 `321ae8e / 80f2675` 提交，共享 hooks 也已由 `efb4130`
  合并；原先“尚未提交候选”的口径至此失效。三包仍只记 static-ready，不能预写 complete/live。二期接下来须闭合
  Workforce 外部 producer 与 #361 bootstrap 等剩余债务，冻结统一 provider/runtime 后重跑 MCP-first Loader 与多周期合批
  实机；七语发布审计、Steam 更新和二期宣传片均未启动。

## 当前边界与下一步

1. G2：同一冻结 seed 的第二完整寿命、结算与再次跨 episode 已完成；新泛化现场已闭合 call-ally 忙碌拒绝并续跑 25 turns，下一 blocker 是 Raiktor CB-specific surrender terms。
2. 二期：保留 static/central-wired 361/361 的准确含义；PP/B2、Workforce 与共享 hook 已提交，下一步是清理剩余外部 producer/#361 bootstrap 债务并用新 Loader artifact 复验，不能用静态计数替代 complete/live。
3. 统一 provider/runtime 冻结后，先用 `--loader-smoke` 只读复验 Loader 签名，再以一份 MCP-first CK3 合批 attempt 覆盖 `.50` result case、存读档、多周期、AI、资源与 GUI 阻塞。
4. 二期全部实现与实机门绿前，不做七语发布审计、Steam 更新或最终宣传片。

## 16:37 计划内增量：AD35、B2/Incident MCP 与 Raiktor Favor Hook

- `5adf01f` 将 Workforce AD 外部生产者债从 `80` 项降到诚实的 `45` 项：35 个重复外部 alias 已改绑当前案卷、内部
  receipt 或 B2 的 11 字段结算来源；#277 只有在 B2 pending tuple 与独立 exit receipt 同时成立、业务 operation 真正
  applied 后才消费，C/RED/stale/replay 均不吞来源。补齐 #274/#276 内部 provenance、正 `outcome_id` 与推荐政策/实际投票
  分账后，runtime 普通/`-O` 各 `95/95`、模型各 `9/9`、生成复现及 `validate_local` 双模式 GREEN。对应 Loader 真债按已冻结
  分类从 `247` 降到预计 `212`，仍须下一份 CK3 Loader artifact 才能改写 live verdict。官方 CI `33373289342` GREEN。
- `be413dc` 为 B2 新增统一 45 字段 PIP case reset：新 #015 写身份前和 route C 都完整清除上一案 response/support/
  midpoint/outcome/receipt，包括陈旧 `subject_response_author`；独立 next-cycle performance evidence 保持守恒不清。生成式
  `30/30`、B2 runtime+semantic `58/58` 均在普通/`-O` 下 GREEN，官方 CI `33373342684` GREEN。
- `b44031e` 将 B2 PIP 与 Incident snapshot 全垂直接入 native mailbox、adapter、bridge、driver、service 与 MCP：固定
  `69` capabilities、`17` mailbox slots，B2 为 `73+1` allowlist，Incident 为每 profile `50` 项。B2 同时拒绝
  self-owner、严格 join gate/PIP cycle+case、统一 8-field partial-PIP 判定和 support 互斥状态。fresh Release 全 CTest
  `56/56`、相关 Python 普通/`-O` 各 `492/492` GREEN；DLL SHA-256
  `3FBB8D2645C4C16C7AC4EA13A889990B79454FC88340891BDA0BF4E5EAAF5E73`，官方 CI `33373423112` GREEN。
  当前只提升到 static/fixture-ready，尚无 paused CK3 artifact。
- `c0c48b3` 完成 Raiktor 投降 terms 的 favor-hook exact-build 只读 helper：普通 `add_hook` 精确为
  `CAddHookEffect<1>` vtable `0x443D830`，no-toast 为 `<0>` / `0x443D708`；运行时通过 Hook DB lookup、canonical
  `favor_hook` key、scope/payload/node/mode 和 stock collector 全链核验，旧 broad reader 与 effect executor 保持禁用。
  fresh targeted native fixture GREEN，官方 CI `33373366060` GREEN；仍待 paused MCP live。
- 对正式 runner 的只读审阅确认 `--phase2-live-batch` 当前只加 loader gate，随后仍回落一期 OCR/坐标
  `run_scenario()`，即使没有 B2、Incident、Workforce、AI 或 named-widget 查询也可能写
  `gameplay_green_claimed=true`。因此该路径当前明确记 harness RED；正在拆为独立 MCP-only 场景，缺 collective、AI-owned
  case 或 named-widget capability 时必须启动前 fail-fast，禁止用旧视觉场景冒充二期 GREEN。
- #360 已完成 B1 真实六槽来源、MG 三经理真实成本桥和 Central route-neutral 三人组合候选；Workforce 正在实现唯一
  route-specific materializer。A 路按真实 exception/quota 扣各经理分与 realm trust，B 路强制末位且零成本，C 路不得残留
  sealed submission；四段同批 GREEN 前不提交、不启动 CK3。

## 20:05 计划内增量：二期真实种子、考核榜只读态与 Workforce 事实链

- `5b55f26 / 1add3cf` 把二期正式 runner 绑到已验证的同 mod-ID 真实旧存档，并要求当前 mount、feature manifest、paused
  player/date 与考核榜只读 capability 同时成立。旧存档的 source-tree hash 只作来源 provenance，不覆盖当前加载树；无 CK3
  dry-install preflight、普通与 `-O` runner 测试均 GREEN。完整 phase-two preflight 仍会因 collective、AI-owned case 与
  named-widget action/geometry 缺口在启动前 RED，不能回落 OCR。
- `fd0682e / 6ccc405` 已提供考核榜固定只读 state/ACL provider，并冻结按钮 action 研究边界：`CPdxGuiPushButton +0x7F8`
  只能称 primary/left-release callback 候选，尚不能称 exact `onclick` 或安全 activate。enabled、focus、modal、geometry、scroll
  与动作继续 typed unavailable；当前正给五个具体入口/关闭目标补稳定名字和 pointer/vtable 查询，为一次 paused probe 做前置，
  不借静态身份提升 action readiness。
- Workforce 事实纵切已连续合入：`48865de` 真实 #274 任命，`3665985` B2 terminal→probation，`ccd7bd1` 将玩家、授权 AI
  与异步 resume 三条 #274 成功路径统一经过 probation arm；`79e56bf` 把 #275-B 整改 requirement 和真实 HC release 分别接到
  D+1 open/consume 边界；`8e0ea27` 新增长期 native career-slot 与 failed-PIP #277 exit 事实包；`2370f19` 新增 #269
  三位面试官封存投票与显式 `6000/2000/2000` 主责政策事实包。整改 core 普通/`-O` 各 `98/98`、事实包各 `13/13`；
  归因包各 `19/19`，生成复现均 GREEN。exit 与 attribution 仍在接共享调用端，全部只记 static-ready/not-live。
- `4a18b8f / 6c202c1` 分别为 G2 的 Raiktor surrender 增加真实金币与俘虏释放条款只读观测；最新 F/prestige 切片正在 exact
  native core 中施工。上述本段已推送提交对应官方 CI `33385555313 / 33386107951 / 33387750492 / 33387851568 /
  33387961681 / 33388236907 / 33388826410 / 33388903259` 全部 GREEN。
- readiness 不变：二期结构设计与 CK3 static/central wiring 均为 `361/361`，但正式口径仍是
  `domain_runtime=partial ×361 / complete ×0`、bounded live `4/361`。新 Loader、paused MCP、多周期与 AI 矩阵完成前，
  不启动发布翻译、Steam 更新或二期宣传片。

## 23:40 计划内增量：二期 provider、MCP-only 观察矩阵与 seed 真相纠偏

- `e1db116` 将 AI-owned case 与 Workforce collective/three-cycle 两套只读 provider 全垂直接入 native DLL、固定
  application-main mailbox、adapter、driver、service、MCP、schema 与文档；同时收入 G2 Raiktor surrender prestige
  exact-build 只读探针。fresh MSVC Release 构建、9/9 聚焦 CTest、provider Python 普通/`-O` 各 29/29、共享 bridge
  247/247 均 GREEN；官方 CI `33408869820` GREEN。两套 provider 仍只到 static/fixture-ready，没有 paused CK3 artifact。
- `ccf3b12` 把 B2、Incident X/Y/Z、Workforce collective/三周期与 AI-owned 四域接入正式 `--phase2-live-batch` 的
  restore 前后 MCP 只读矩阵，覆盖正向 tuple、wrong-owner ACL、typed unavailable、存读档语义一致性与 capability fail-fast。
  观察单元可以独立 GREEN，但 runner 明列五个缺失 gameplay action cells 并强制整批 RED，禁止把只读响应或 command ACK
  冒充玩法完成；普通、`-O`、`py_compile` 与官方 CI `33409585678` 均 GREEN。
- 对旧 seed 的只读取证推翻了其 `played=32904/han_8052` 声明：同一保存现场的 paused MCP 证据显示真实玩家为
  `29037/han_6875`，`32904` 是上司。更关键的是 source commit `fa5c78d` 早于 B1/B2/Incident/Workforce 产品脚本，旧 save
  不可能包含四域正向状态；AI owner/subject 也只存在于未持久化的随机 scope。当前改为生成一份仅 acceptance 加载、绝不进入
  release/promo 的当前-runtime MCP seed：fixture 只选择真实历史与直属关系角色，经 typed event context 取回五个真实 ID，
  MCP 关窗后 save-checkpoint 并冻结 save/sidecar。禁止猜 ID、遍历碰运气或回退 OCR。
- 考核榜动作逆向纠正了旧候选：property `0x11B` 的精确字符串是 `oversound`，不是 `onclick`；真实 `onclick` 为 ID
  `0x307`，由 `0x36C6740` 写入 `CPdxGuiPushButton +0x338`。执行槽与 admission 尚未闭合，因此 named-widget action 继续
  RED，不能把 hover 音效路径误作安全点击。
- Workforce normal-exit/rehire 正在把 #274 arm、B2 #075 begin、D+1 exit、later growth、#276 prepare 与 A/B finalize
  接入真实调用端；合法 revoke 已优先消费精确票据，不再误记 `unexpected_native_end_seen`。当前聚焦生成/测试已 GREEN，
  单槽 probation/rehire 对多 owner 并发仍是明确设计边界，未在本轮无证据扩张。
- 计划外但已授权的工具工作：`adda323` 仅建立 `open_kaishek` JVM/Quarkus 子项目立项文档，未写代码；宣传视频通用化已冻结为
  “可安装 CLI/package + Codex Skill + 项目 adapter/preset”三层，Skill 初稿通过 quick validation，核心最小垂直切片仍在落盘。
- `4a8e29d` 将 Workforce 的 #274 任命→probation、B2 #075 normal exit→native revoke→D+1 canonical receipt、后续
  #269 growth→#276 prepare/finalize 接入真实调用端，并修复合法 normal-exit revoke 被误记为 unexpected native end；聚焦
  普通/`-O` 各 267/267、Workforce 102/102、五个生成器 `--check`、`validate_local` 与官方 CI `33410454600` GREEN。
  当前持久事实仍是单槽，跨多 owner 自然成长会被旧 tombstone 阻断；多代 bounded ledger 已作为下一施工单元，readiness 仍为
  `ck3-script-static-ready-not-live`。

## 00:00 早会承诺对照

| # | 早会承诺 | 结果 | 证据 / 未完成原因 | 是否顺延 |
|---:|---|---|---|---|
| D1 | 关闭 G2 安全路线 blocker | 完成 | stationary horizon、signed CombatID/BattleResultID、真实 battle action→paused verification 已闭合并进入 `0d14075`；后续又关闭 call-ally busy reject 与 Raiktor gold/prisoner/prestige/favor-hook 静态观测 | 否 |
| D2 | 从固定 checkpoint 续跑 G2 第二寿命 | 完成 | `20260830T182851Z-next-episode-19d679de` 完成 472/472 turns，自然死亡、matching settlement、下一 episode gameplay/checkpoint，15/15 gates 与 cleanup GREEN | 否 |
| D3 | 完成二期剩余 actual CK3 包和单入口 adapter | 完成静态门；产品语义继续深化 | `d1ad7f8` 达到 CK3 static 361/361，后续 central wiring 也达 361/361；这只表示源码投影与可达接线，不把 `complete ×0` 改写为完成 | live/semantic 顺延至 09-01 |
| D4 | 一次性中央接线与 L0 收口 | 部分完成 | 唯一 dispatcher、各领域 adapter 与共享 hooks 已接，普通/`-O` 大批回归和 CI GREEN；Workforce 多 owner ledger、HC partition 与五个 action cells 仍未闭合 | 顺延至 09-01 |
| D5 | MCP-first CK3 批量验收 | 部分完成 / live 阻塞 | B2、Incident、Workforce、AI-owned provider/四域 pre-save-post 观察矩阵已 static/fixture-ready；旧 seed 被证实身份写反且早于新 runtime，scoreboard action 与玩法 action 尚 RED，全程未退回 OCR | 顺延至 09-01 |
| D6 | 二期自动发版与增量宣传片 | 未开始，正确受门禁阻止 | 二期 `domain_runtime=partial ×361 / complete ×0`，故未执行七语、Steam 上传或最终宣传片；工具链通用抽离作为独立开发工作已启动 | 顺延，等待二期全绿 |
| D7 | 日/周证据收口 | 完成 | 日报/周报滚动更新，00:00 逐项对照；关键 commit、CI、live/RED 边界均落盘 | 否 |

### 计划外完成

- `adda323` 新建 `open_kaishek` JVM/Quarkus 子项目立项与参考目标，仅文档、未写代码，符合用户限定。
- `e1db116 / ccf3b12 / 4a8e29d / fce1c31 / da3b210 / efdfc15 / adc70c7` 分别闭合二期 provider、
  MCP 观察矩阵、Workforce exit/rehire seam、G2 favor-hook、B2 action helper 与 AI-owned action helper；对应已完成的官方 CI
  均 GREEN，最后三项的新 CI 于跨日后继续观察。
- 用户将宣传工具链抽离提升为最高优先级；正在运行的二期/G2线程不暂停，完成后并发槽转投通用 package、CK3 adapter、
  audit/evidence、TTS、legacy wrapper 与 Codex Skill。

## 收口时边界与下一步

1. 二期仍是 Python/CK3 static/central-wired 361/361，`domain_runtime=partial ×361 / complete ×0`，bounded live 4/361；
   新 seed、五个 action cells、named-widget 与 Loader/paused/save-restore/multi-cycle 矩阵未全绿。
2. G2 第二寿命已完成；favor-hook 当前只到 static/fixture-ready，下一步是统一 terms wire/MCP/paused live，不重复已完成寿命。
3. 宣传抽离成为 09-01 P0；首批必须是真实可安装、可运行、可验证的 CLI/package 与 Skill，不接受空壳 render/audit。
4. 收口 HEAD（报告提交前）为 `master` / `adc70c7ec705cbb3a9fc04f43e3c1c93f9eed68f`。
