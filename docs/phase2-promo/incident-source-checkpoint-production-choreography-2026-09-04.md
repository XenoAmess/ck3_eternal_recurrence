# Incident source-checkpoint production choreography (2026-09-04)

## Outcome

The existing ready Phase2 seed already contains a genuine, active B1 review
owned by AI character `32904`, with the played character `29037` as its direct
reviewed subject. The committed production graph can carry that review to a
real player-rooted `zg361.50`: B1 publication calls the ordinary grade
settlement, and a genuine player grade of 3.25 saves the distinct case owner
and schedules the notice one day later.

What is **not** proved is that character `29037` will receive the bottom grade.
The retained seed evidence exposes the owner/subject B1 tuple and stage 1, but
not the cohort, frozen KPI, rank, newcomer status, quota, pending grade, or
calibration outcome. Consequently the route is **conditionally reachable**,
not known-unreachable and not guaranteed. Readiness remains
`static-ready-live-pending` until a product-only run produces and freezes the
real paused notice.

The machine-readable companion is
`tools/zg361_phase2_incident_source_choreography_contract.json`.
`tools/test_zg361_phase2_incident_source_choreography.py` pins this account to
the current generated and hand-authored product graph without starting CK3.

## Frozen starting evidence

The seed contract records a 53,517,622-byte save with SHA-256
`bfc73fd9e7e80145cdf39aabc66bc2d731881122adab0cc0ba675fa07d1e6733`,
`date_raw=53146920`, paused/map-ready true, and played CharacterID `29037`
(`han_6875`). The final native snapshot is event-free. The product provider
evidence (SHA-256
`5e3f60ffc49d945b30805f6b2af6e33cad52a2d9a5c801886aaac35eef583058`)
observes:

- B1 owner `32904`, subject/player `29037`, cycle/case `1/1`;
- case state `1`, stage `targets_open`, active true;
- owner `32904` alive, AI, celestial-government hegemony, and authorized as
  the subject's immediate liege; and
- a same-frame `roster_lock` receipt for that exact owner/subject/cycle/case.

The bootstrap fixture explains how the save was created, but it must not be
mounted in the continuation. Its visible card was closed before save. The
continuation starts from persisted production tickets only; it must not reopen
B1, use the fixture again, invoke console, or rebind the player.

The earlier Incident probe is not the requested source. It recorded a genuine
X N/A terminal for owner `32904`, subject `29037`, cycle 2, while Y and Z were
unavailable. It neither proves the later `zg361.50` notice nor supplies the
required X/Y/Z post-action matrix.

## Shortest legal continuation from this seed

Native `date_raw` is measured in hours, so one scripted day is 24 raw units.
The offsets below are relative to the frozen seed date and are graph landmarks,
not claims that the player will receive 3.25.

1. Restore the exact seed into a managed **product-only** session. Reprove the
   bytes/hash/lineage, paused map, player `29037`, and same B1 owner/subject
   tuple. Keep player=root=subject `29037` and distinct live notice owner
   `32904` as invariants; death, succession, transfer, or owner replacement is
   a typed stop rather than permission to rebind.
2. Use only `set-speed-1`, `resume-map`, and `pause-map` for time. The existing
   hidden ticket reaches `zg361b1.100` at D+180 (`date_raw=53151240`), which
   schedules hidden `.101` for D+240 (`53152680`). `.101` opens the peer/self
   evidence window and schedules hidden `.102` for D+300 (`53154120`).
3. If visible `zg361b1.200` appears at D+241, require root `29037` and its saved
   owner/subject/cycle/case/state tuple, then select an authored option. Option
   3 is the target-directed canonical choice: it records a real conservative
   self-review (`evidence_mid - 15`). This is only a bounded input and does not
   guarantee a bottom rank. If policy disabled the self-review card, `.102`
   honestly records the default at its deadline; absence is not a blocker.
4. Hidden `.102` freezes the real D+300 facts, runs the ordinary rank/quota
   engine, opens the shadow, and schedules hidden `.103` for D+330
   (`53154840`). If visible `zg361b1.201` appears at D+301, require the exact
   tuple and select option 1. It accepts the non-final shadow without adding a
   new calibration delta. A policy route may suppress this card.
5. Hidden `.103` posts the owner's quota book at D+330. When all sibling
   managers are ready, the common-superior bank can run hidden `.110` at
   D+331; its frozen watchdog otherwise runs at D+335. Bank close schedules
   hidden `.111` one day later, so calibration opens between D+332 and D+336.
6. Calibration may finish immediately or traverse only its authored hidden
   continuations: skip-level `.124` at +1 day, per-subject pending `.121` at
   +30 days with owner watchdog `.125` at +31, continuation `.123` at +1, and
   at most one sealed-board reopen batch `.122` at +30. The longest committed
   delay envelope places settlement/source scheduling by D+399, provided the
   frozen identities remain valid. This is a diagnostic horizon, not proof of
   the player's grade.
7. Settlement applies each subject's genuine pending grade. For a living
   player still owned by `32904`, grade code 1 invokes
   `zg361_grade_325_apply_effect`; that effect saves owner `32904` separately,
   saves subject/root `29037`, and schedules `zg361.50` one day later. A
   subject-local `zg361b1.126` publication card is also scheduled at +1 day.
   If `.126` is presented first, verify its exact player tuple and select its
   sole option; never use a generic "close any one-option event" rule.
8. On `zg361.50`, pause without selecting anything. Require the native event
   query to show root/player/subject `29037`, saved notice owner `32904`, exact
   saved cycle/case/state, and option 1 shown/enabled. Then invoke the existing
   strict capture seam, which rechecks the same frame and archives bytes,
   SHA-256, date, seed lineage, capture lineage, provider receipt, and UI
   receipt.

Any other visible event is a typed RED and remains on screen. An option ACK is
only a command-submission receipt; it cannot prove a ticket fired, publication
completed, the player received 3.25, or the target event exists.

## Reachability blocker and honest terminal states

The single source-candidate blocker is **grade selection**, not a broken event
edge. The seed evidence does not reveal:

- cohort size or whether a non-newcomer bottom slot exists;
- player `29037`'s frozen KPI, rank, absolute grade, or newcomer protection;
- manager quota/bank/calibration inputs; or
- the player's final `zg361_pending_grade`/result grade.

The forced-distribution code creates bottom slots only from eligible
non-newcomer candidates. It does not promise that the played character is one
of them. The conservative `.200` choice can lower a bounded self-review input,
but cannot override the frozen business ranking. If publication yields grade
3.5/3.75, or the played subject/owner relationship ceases to be valid, the
correct outcome is a retained production RED such as
`player_not_grade_325`/`incident_source_identity_lost`, followed by selection
of another real product checkpoint or another genuine review cycle.

There is therefore no evidence-backed production repair to implement in this
pass. Forcing `zg361_pending_grade=1`, adding a debug decision, reopening
`zg361.50`, or altering the player/owner ACL would manufacture the source and
invalidate the checkpoint. The next live run should first observe the existing
cycle through the AI-owned B1 and result-case providers; only a reproducible
stall in those shipped edges would justify a minimal product fix.

## Live capture checklist

- Product-only continuation of the exact seed; fixture and console absent.
- Speed 1 with bounded pause/query loops; no coordinate/OCR input.
- Only exact `.200` option 3, `.201` option 1, and `.126` option 1 may be
  selected before the target; each selection is bound to event instance and
  revision and followed by independent disappearance/state observation.
- Stop at unrelated events, identity change, provider stall, non-3.25 result,
  or the D+399 authored-delay diagnostic horizon.
- Capture only an unselected paused `zg361.50` with
  player=root=subject `29037`, distinct owner `32904`, option 1 shown/enabled,
  same-frame provider/UI bindings, and complete byte/hash/date/lineage receipt.

No CK3 process was started, no B3 artifact was changed, and no production
effect was added or modified for this forensic package. The effect-file
1–10/20 boundary is therefore not engaged.
