textformatting = {
	format = {
		name = devour_red
		format = "color:{0.95, 0.05, 0.05}"
	}
	format = {
		name = devour_gray
		format = "color:{0.6, 0.6, 0.6}"
	}
	format = {
		name = devour_white
		format = "color:{0.95, 0.95, 0.95}"
	}
	format = {
		name = devour_green
		format = "color:{0.3, 0.85, 0.3}"
	}
	format = {
		name = devour_blue
		format = "color:{0.25, 0.65, 1.0}"
	}
	format = {
		name = devour_purple
		format = "color:{0.75, 0.35, 0.95}"
	}
	format = {
		name = devour_gold
		format = "color:{ 1.0, 0.78, 0.12 }"
	}
}